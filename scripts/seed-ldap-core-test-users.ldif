# Dev LDAP test users for Core Team Sign-in (password: inpact).
# Apply against a running ipf-ldap container:
#   Get-Content scripts/seed-ldap-core-test-users.ldif |
#     docker exec -i ipf-ldap ldapadd -x -H ldap://localhost -D "cn=admin,dc=inpact,dc=live" -w admin_dev_only
# Idempotent re-run: delete first if entries already exist, or ignore "Already exists".

dn: cn=PD Core,ou=core,dc=inpact,dc=live
objectClass: inetOrgPerson
cn: PD Core
sn: Core
givenName: PD
uid: PD_Core
mail: pd_core@inpact.live
description: PD-core
userPassword: inpact

dn: cn=ID Core,ou=core,dc=inpact,dc=live
objectClass: inetOrgPerson
cn: ID Core
sn: Core
givenName: ID
uid: ID_core
mail: id_core@inpact.live
description: ID-core
userPassword: inpact

dn: cn=PM Core,ou=core,dc=inpact,dc=live
objectClass: inetOrgPerson
cn: PM Core
sn: Core
givenName: PM
uid: PM_Core
mail: pm_core@inpact.live
description: PMGT-core
userPassword: inpact
