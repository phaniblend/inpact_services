L-01
Live Search Bar
debounceTimeswitchMapdistinctUntilChangedHttpClientasync pipeQ16Q18Q21Q33
In this lesson you will build a Live Search Bar component that queries a REST API as the user types, debouncing keystrokes to avoid flooding the network and automatically cancelling stale requests when a newer query arrives. You will wire a text input to an Observable pipeline using RxJS operators, render results with the async pipe, and handle both loading and error states inline. The finished widget performs exactly like the search bars in GitHub, Notion, or VS Code's command palette — responsive, efficient, and resilient to fast typing.
WHY IT MATTERS
Search-as-you-type is one of the most common UX patterns in modern web applications — used in e-commerce product search, admin user lookups, HR directory search, and code editors. Getting it right means understanding request cancellation, debounce timing, and graceful degradation when the API is slow. These are the exact patterns you will be asked about in senior Angular interviews when they ask "how does your search component handle race conditions?"
LEARNING OBJECTIVES
* 1Wire a reactive text input to an Observable using fromEvent or a FormControl's valueChanges stream
* 2Apply debounceTime and distinctUntilChanged to throttle API calls without losing keystrokes
* 3Use switchMap to cancel in-flight HTTP requests when a new search term arrives
* 4Handle errors inside the inner observable so the outer stream stays alive across network failures
* 5Render asynchronous results and loading state using the async pipe with no manual subscribe/unsubscribe
* 6Apply ChangeDetectionStrategy.OnPush to ensure the component only re-renders when results arrive
* 7Write a unit test that verifies debounce behaviour using RxJS marble testing with TestScheduler
L-02
Shopping Cart Badge Counter
Signalscomputed()effect()toSignal()OnPushQ8Q23Q06
In this lesson you will build a Shopping Cart Badge — the small numerical indicator on a cart icon that updates in real time as items are added, removed, or quantity-adjusted across any page of the application. You will implement the cart state using Angular Signals, derive the item count and subtotal using computed(), and observe side effects (persisting to localStorage) using effect(). The badge component itself uses OnPush change detection, staying perfectly in sync through Angular's signal graph without a single manual subscription.
WHY IT MATTERS
Cart badges, notification counters, and unread-message badges appear in virtually every consumer-facing application — e-commerce, social media, email clients, and project management tools. They are also the canonical example for explaining Angular Signals in interviews. Building this widget forces you to understand the difference between signal(), computed(), and effect() and how they replace BehaviorSubject patterns with zero boilerplate. Employers want to see you can choose between RxJS and Signals based on the nature of the state.
LEARNING OBJECTIVES
* 1Create a CartService that holds items as a writable Signal using signal()
* 2Derive the total item count and subtotal as computed() Signals with no manual calculation triggers
* 3Use effect() to synchronise cart state with localStorage as a side effect, without subscribing to observables
* 4Consume Signal values in a template without the async pipe and observe that Angular re-renders only the badge
* 5Bridge existing RxJS streams into the signal world using toSignal() with an initialValue
* 6Apply OnPush to the badge component and explain why Signals make OnPush less brittle than @Input-only approaches
* 7Compare the Signals implementation with the equivalent BehaviorSubject + async pipe version and articulate the tradeoffs
L-03
Theme Toggle Switch
BehaviorSubjectprovidedIn:'root'async pipe@HostBindingViewEncapsulationQ06Q09Q33Q05
In this lesson you will build a dark/light Theme Toggle Switch — a UI control that instantly switches the entire application's colour scheme and persists the user's preference across sessions. You will implement a ThemeService with a BehaviorSubject as the single source of truth, apply the active theme as a class on the document body using @HostBinding, and scope component styles correctly using ViewEncapsulation. The toggle widget itself consumes the theme observable via the async pipe and demonstrates how a singleton service broadcasts to multiple consumers with zero coupling.
WHY IT MATTERS
Global theme switching is present in almost every modern SaaS product — from VS Code and GitHub to Figma and Linear. It is also a textbook demonstration of the shared-service state management pattern. Understanding how to broadcast a single state change to multiple independent components (nav, sidebar, content area) without prop drilling or NgRx is a core architectural skill. Interviewers use this exact scenario to test whether a candidate understands the singleton service pattern and the difference between BehaviorSubject and Subject.
LEARNING OBJECTIVES
* 1Create a singleton ThemeService using providedIn:'root' with a private BehaviorSubject and a public observable
* 2Toggle between themes by calling .next() on the BehaviorSubject and explain why BehaviorSubject is correct here (vs Subject)
* 3Apply the active theme class to the document body using @HostBinding on a root-level directive
* 4Consume the theme stream in multiple unrelated components using the async pipe with zero manual subscriptions
* 5Persist the user's preference to localStorage inside the service and hydrate it on app initialisation
* 6Use ViewEncapsulation.None for the toggle component's global theme variables and explain when this is appropriate
* 7Refactor the service to use a Signal instead of BehaviorSubject and compare the resulting template code
L-04
Toast Notification Stack
ViewContainerRefComponentRefinject()Service-driven UIngOnDestroyQ04Q09Q34Q70
In this lesson you will build a Toast Notification Stack — an overlay system that can push success, warning, and error toasts from anywhere in the application without touching the component tree. You will create a ToastService that imperatively creates and destroys ToastComponent instances using ViewContainerRef.createComponent(), manage a queue of active toasts, and auto-dismiss them after a configurable duration. This is the canonical example of service-driven dynamic component rendering that underpins modals, drawers, tooltips, and snackbars across enterprise UIs.
WHY IT MATTERS
Global notification systems exist in every enterprise application — form submission confirmations, background job status updates, real-time alert broadcasts. The pattern behind them (a service creates, manages, and destroys UI components programmatically) is the same pattern powering Angular Material's MatSnackBar, MatDialog, and CDK Overlay. Understanding ViewContainerRef is what separates developers who can only use component libraries from those who can build them. Senior engineers are expected to implement this pattern from scratch.
LEARNING OBJECTIVES
* 1Inject ViewContainerRef into a service using the inject() function and understand why this requires a specific injection context
* 2Dynamically create a ToastComponent instance using ViewContainerRef.createComponent() and pass data via componentRef.setInput()
* 3Manage a stack of active toast references and implement FIFO or priority queue ordering
* 4Auto-dismiss a toast after a configurable duration and destroy its ComponentRef to prevent memory leaks
* 5Call the ToastService from any component, service, or HTTP interceptor using inject() without prop drilling
* 6Style each toast variant (success, warning, error, info) using @Input bindings and conditional CSS classes
* 7Implement ngOnDestroy on the ToastComponent to clean up any internal timers or subscriptions
L-05
Smart Login Form
ReactiveFormsModuleAsyncValidatorexhaustMapFormGrouptouched/dirtyQ14Q15Q16Q45
In this lesson you will build a Smart Login Form with email and password fields, real-time validation feedback, async email-existence checking, and double-submit prevention. You will use Reactive Forms with custom sync validators for password strength and a custom async validator that calls an API to check if an email is registered. The submit button uses exhaustMap to ignore repeated clicks while the login request is in flight. Error messages appear inline as the user interacts, following Angular's touched and dirty state model for polished UX.
WHY IT MATTERS
Authentication forms are the entry point of virtually every web application, and getting their UX right — showing errors at the right moment, preventing duplicate submissions, validating asynchronously — requires deep knowledge of Angular's Reactive Forms API. In senior interviews you will be asked to explain the difference between sync and async validators, how to avoid flickering validation messages, and how exhaustMap prevents race conditions on form submission. These are table-stakes concepts for any Angular developer working on enterprise apps.
LEARNING OBJECTIVES
* 1Build a FormGroup with email and password FormControls and compose multiple sync validators using Validators.compose()
* 2Write a custom sync password-strength validator function that returns a ValidationErrors object or null
* 3Implement an async validator that debounces API calls to check email availability, returning an Observable of errors
* 4Display inline error messages only when a field is both invalid and touched, avoiding premature error display
* 5Use exhaustMap on the submit button's click stream to fire the login request and ignore duplicate clicks while in flight
* 6Disable the submit button and show a loading indicator while the async validator or submit request is pending
* 7Reset the form and surface server-side errors (e.g., "invalid credentials") as form-level errors after a failed login
L-06
Invoice Line-Items Builder
FormArrayFormGroupcross-field validationvalueChangesTypedFormsQ14Q15Q63
In this lesson you will build an Invoice Line-Items Builder — a dynamic form where users can add, remove, and reorder product rows, each with a name, quantity, unit price, and computed line total. The invoice automatically sums all rows into a grand total. You will manage the rows as a FormArray of FormGroups, add and remove controls programmatically, derive computed totals from valueChanges, and apply cross-field validation (e.g., total must not exceed a credit limit). TypedForms (Angular 14+) give you full type safety across the entire form model.
WHY IT MATTERS
Dynamic line-item forms appear in invoicing tools, order management systems, expense reports, CRM quote builders, and procurement platforms. They are among the most complex UI patterns in enterprise Angular apps because they combine dynamic DOM, reactive validation, computed derived values, and often server-side persistence. Candidates who can implement a FormArray with dynamic validation and type safety stand out because most developers default to template-driven forms and lose control of the state. This is also the most reliable way to demonstrate Angular 14's Typed Reactive Forms in an interview.
LEARNING OBJECTIVES
* 1Create a typed FormArray of FormGroups using FormBuilder and Angular 14's strictly-typed form APIs
* 2Implement addItem() and removeItem(index) methods that mutate the FormArray programmatically
* 3Subscribe to valueChanges on the FormArray to compute and display a live running total without manual event handlers
* 4Write a cross-field validator on the parent FormGroup that compares the total against a maximum allowed amount
* 5Render each FormGroup row using formGroupName and formControlName directives with *ngFor and trackBy
* 6Mark individual row controls as touched when the user tries to submit, triggering per-row error messages
* 7Patch an existing invoice from an API response into the FormArray using patchValue and setControl
L-07
Virtual Employee Directory
CDK Virtual ScrolltrackByOnPushngForngOptimizedImageQ23Q24Q64
In this lesson you will build a Virtual Employee Directory that renders a list of 10,000 employee cards with avatars, names, departments, and status badges without any perceptible lag. Using Angular CDK's cdk-virtual-scroll-viewport, only the cards visible in the viewport are ever in the DOM. Combined with trackBy to prevent unnecessary DOM recreation, OnPush change detection to skip untouched components, and NgOptimizedImage for lazy avatar loading, you will produce a performant directory that feels native regardless of dataset size.
WHY IT MATTERS
Large list performance is one of the most frequently cited pain points in enterprise Angular applications — HR directories, customer lists, product catalogues, audit logs. Without virtual scrolling, rendering thousands of rows bogs down the browser's rendering engine, causing visible jank. Interviewers will ask how you have handled performance for large datasets; the answer involves virtual scrolling, trackBy, and OnPush as a trio. NgOptimizedImage (Angular 15) is increasingly expected knowledge for eliminating Largest Contentful Paint regressions in image-heavy lists.
LEARNING OBJECTIVES
* 1Set up cdk-virtual-scroll-viewport with a fixed itemSize and a container height to enable viewport-only rendering
* 2Replace *ngFor with *cdkVirtualFor and supply a trackBy function based on a stable employee ID
* 3Apply ChangeDetectionStrategy.OnPush to the employee card component and explain why it is safe to use with immutable data
* 4Use NgOptimizedImage with lazy loading to defer avatar requests until their card enters the viewport
* 5Measure the DOM node count before and after virtual scrolling using Chrome DevTools Elements panel
* 6Implement a live filter input that updates the displayed list without destroying and recreating the scroll viewport
* 7Profile change detection cycles with Angular DevTools and identify which components re-render during a filter update
L-08
Protected Dashboard Shell
canActivateResolveFnloadChildrenActivatedRouteRouterOutletQ11Q12Q13
In this lesson you will build a Protected Dashboard Shell — an application structure with a public login route and a private dashboard section that requires authentication. A functional canActivate guard checks the auth state and redirects unauthenticated users to the login page with a returnUrl query parameter. A route Resolver pre-fetches the logged-in user's profile before the dashboard renders, eliminating a loading flicker. The dashboard itself is lazy-loaded as a separate bundle that is never downloaded by users who are not authenticated.
WHY IT MATTERS
Every enterprise web application has authentication-gated routes. The implementation pattern — guard, resolver, lazy-loaded feature, redirect with returnUrl — is the same across banking portals, SaaS dashboards, admin consoles, and HR platforms. Interviewers will ask you to walk through an authenticated routing architecture, including what happens on token expiry, how you handle direct URL access, and the difference between canActivate and canMatch. Route resolvers are less commonly known but are expected knowledge at the senior level.
LEARNING OBJECTIVES
* 1Write a functional canActivate guard using inject() that reads auth state from AuthService and redirects to /login with a returnUrl
* 2Configure a lazy-loaded dashboard route using loadChildren pointing to a standalone routes array
* 3Implement a functional ResolveFn that fetches the user profile from the API before the route activates
* 4Access resolved data in the dashboard component via ActivatedRoute.snapshot.data
* 5Read the returnUrl query parameter after successful login and navigate back using safeRedirect validation
* 6Use canMatch to prevent the lazy module bundle from downloading for unauthenticated users
* 7Test the guard using TestBed with a mocked AuthService that returns both logged-in and logged-out states
L-09
Star Rating Input
ControlValueAccessorNG_VALUE_ACCESSORforwardRefformControlNamesetDisabledStateQ45Q47Q14
In this lesson you will build a Star Rating Input — a five-star clickable rating widget that integrates seamlessly with Angular's Reactive Forms API. It works identically to a native input: it accepts formControlName in a parent form, responds to form.patchValue(), participates in form validity, and disables itself when the parent FormGroup is disabled. Implementing ControlValueAccessor is the only correct way to plug a custom UI component into Angular Forms, and this widget demonstrates all four interface methods in a visually satisfying micro-app.
WHY IT MATTERS
Custom form controls are everywhere in enterprise applications — date pickers, colour inputs, rich-text editors, file upload inputs, phone number fields with country codes, and rating widgets in feedback forms or product reviews. Any time you use a component library's custom input (Angular Material's datepicker, for example), ControlValueAccessor is running under the hood. Senior Angular developers are expected to know how to build one from scratch — it appears in almost every advanced Angular technical screen because it tests DI knowledge, interface implementation, and forms integration simultaneously.
LEARNING OBJECTIVES
* 1Register the StarRatingComponent as a NG_VALUE_ACCESSOR provider using useExisting and forwardRef()
* 2Implement writeValue() to render the correct number of filled stars when the form sets a value programmatically
* 3Implement registerOnChange() and call the registered callback whenever the user clicks a star
* 4Implement registerOnTouched() and mark the control as touched on the first user interaction
* 5Implement setDisabledState() to visually disable the stars and block click events when the parent form is disabled
* 6Use the widget inside a Reactive Form with formControlName and verify that patchValue() updates the displayed stars
* 7Add hover-preview behaviour (highlight stars on hover before click) using @HostListener without breaking the form contract
L-10
Tooltip Directive
@Directive@HostListener@HostBindingElementRefRenderer2Q19Q59
In this lesson you will build a Tooltip Directive — an attribute directive that can be applied to any element to show a floating tooltip on hover. The directive uses @HostListener to listen for mouseenter and mouseleave events, Renderer2 to safely create and destroy the tooltip DOM node without directly touching the DOM, and @Input to accept the tooltip text, position, and delay as configuration. Because it is a standalone directive, it can be added to any component's imports array and used immediately with a single attribute on any element.
WHY IT MATTERS
Tooltips, highlight directives, clipboard-copy directives, infinite-scroll directives, and click-outside directives are the most common custom attribute directives in production Angular applications. They encapsulate reusable cross-cutting behaviour that would otherwise be duplicated across dozens of components. Writing a tooltip directive teaches you how Angular's directive system interacts with the host element, how to use Renderer2 instead of direct DOM manipulation (critical for SSR compatibility), and how @HostListener and @HostBinding map to the element's events and properties without touching the template.
LEARNING OBJECTIVES
* 1Create a standalone attribute directive with a selector in square brackets and apply it to any host element
* 2Accept tooltip text, placement, and show-delay as @Input() properties with sensible defaults
* 3Listen to mouseenter and mouseleave on the host element using @HostListener and manage show/hide timing with setTimeout
* 4Create and position the tooltip DOM node using Renderer2.createElement() and Renderer2.appendChild() to remain SSR-safe
* 5Destroy the tooltip node in ngOnDestroy and on mouseleave to prevent orphaned elements and memory leaks
* 6Calculate viewport-aware position (flip from top to bottom when near the top edge) in the positioning logic
* 7Write a test that simulates mouseenter and verifies the tooltip element appears in the DOM using a ComponentFixture
L-11
Smart Truncate Pipe
@PipePipeTransformpure pipeimpure pipepipe parametersQ20Q23
In this lesson you will build a Smart Truncate Pipe — a configurable pure pipe that shortens long text strings to a specified character limit and appends a customisable suffix (e.g., "…" or "read more"). You will then extend it into an impure variant that reacts to window resize events, truncating to a shorter limit on mobile viewports. By building both variants you will deeply understand what "pure" and "impure" mean in Angular's change detection context, why impure pipes can hurt performance, and how to audit your templates for accidental impure pipe misuse.
WHY IT MATTERS
Pipes are the most underused Angular feature in production apps — developers frequently use method calls in templates instead, which Angular re-executes on every single change detection cycle without memoisation. Pure pipes are Angular's built-in memoisation mechanism: they re-run only when their input reference changes. Custom pipes appear in news feeds (truncating article summaries), product catalogues (truncating description text), notification lists (showing preview text), and dashboards (formatting numbers, currencies, file sizes). Understanding pure vs impure is a common interview differentiator at the senior level.
LEARNING OBJECTIVES
* 1Implement a pure TruncatePipe with configurable limit and trail parameters using the PipeTransform interface
* 2Explain why a pure pipe is only re-evaluated when its input reference changes, and how this provides automatic memoisation
* 3Demonstrate that replacing a pipe with an equivalent template method call causes that method to execute on every CD cycle
* 4Extend the pipe to an impure variant (pure: false) and inject the current viewport width via a breakpoint service
* 5Articulate the performance implications of impure pipes and identify when they are justified versus when a computed Signal is superior
* 6Register the pipe as standalone and use it in multiple components without importing CommonModule
* 7Write unit tests for the pipe's transform method without any TestBed setup, verifying edge cases (empty string, limit longer than input)
L-12
Paginated Data Table
HttpClientasync pipequeryParamsqueryParamsHandlingshareReplayQ13Q21Q33Q37
In this lesson you will build a Paginated Data Table that fetches server-side pages of records, exposes page and sort controls, and synchronises the current page and sort order into the URL as query parameters. This means the table state is shareable and bookmark-able — refreshing the page returns to the same view. Data is fetched reactively by combining query parameter observables with switchMap into an HTTP request. A loading skeleton replaces the table rows during fetch, and results are cached with shareReplay to avoid duplicate requests when navigating back.
WHY IT MATTERS
Paginated data tables are the backbone of admin panels, CRM systems, financial dashboards, e-commerce back-offices, and analytics tools. The URL-as-state pattern (storing page, sort, and filter in query params) is critical for enterprise UX — users share links, use the browser back button, and bookmark specific table views. Combining this with reactive streams and HTTP caching tests a developer's ability to think in data flows rather than imperative event handlers. This lesson covers patterns that directly answer the interview question "how does your table handle the browser back button?"
LEARNING OBJECTIVES
* 1Read page and sort query parameters from ActivatedRoute.queryParams as an Observable and combine them with combineLatest
* 2Use switchMap to trigger an HTTP request whenever the combined query parameters change, cancelling stale requests
* 3Navigate to updated query params on page or sort change using queryParamsHandling:'merge' to preserve other params
* 4Display a loading state and empty state using the async pipe's structural directives without any component-level subscription
* 5Apply shareReplay(1) in the data service so navigating back to this route does not re-fire the same HTTP request
* 6Implement a sort header that cycles through ascending, descending, and unsorted states and encodes the result in the URL
* 7Test the component by providing a mock ActivatedRoute with queryParams as a BehaviorSubject and verifying HTTP call triggers
L-13
Real-time Price Ticker
NgZonerunOutsideAngular()WebSocketOnPushChangeDetectorRefQ23Q44Q61Q65
In this lesson you will build a Real-time Price Ticker widget that receives streaming price updates over a WebSocket and displays a live feed of asset prices with colour-coded up/down flash indicators. To avoid triggering Angular's change detection on every incoming message (which could arrive 100+ times per second), you will run the WebSocket handler outside Angular's zone using NgZone.runOutsideAngular(), buffer incoming prices, and re-enter the zone only when the component needs to visually update. The ticker component uses OnPush with manual ChangeDetectorRef.detectChanges() for surgical rendering control.
WHY IT MATTERS
High-frequency data UIs appear in financial trading platforms, live sports scoreboards, IoT sensor dashboards, real-time analytics, and multiplayer game leaderboards. Naively processing every WebSocket message inside Angular's zone causes constant full-tree change detection, degrading frame rate and making other interactions feel sluggish. NgZone.runOutsideAngular() is the correct solution — and it is a pattern that demonstrates to interviewers that you understand Zone.js at a deep level, not just OnPush. This is a senior-level question and a relatively uncommon differentiator.
LEARNING OBJECTIVES
* 1Establish a WebSocket connection in a service and expose price updates as an Observable using fromEvent or a custom Subject
* 2Open the connection and register the message handler inside NgZone.runOutsideAngular() to prevent zone-triggered CD on every message
* 3Buffer incoming messages using throttleTime or auditTime and call NgZone.run() only when it is time to update the UI
* 4Use ChangeDetectorRef.detach() on the ticker component and call detectChanges() manually after each buffered update
* 5Apply a flash animation class (green/red) when a price moves up or down, removing it after 300ms with a timer outside the zone
* 6Reconnect the WebSocket automatically on disconnect using retryWhen or retry with exponential back-off
* 7Verify in Chrome DevTools Performance tab that disabling Zone.js triggers reduces the number of layout recalculations
L-14
Permission Gate Directive
Structural directiveTemplateRefViewContainerRefinject()RBACQ30Q58Q59
In this lesson you will build a Permission Gate Directive — a structural directive (*hasPermission) that hides or shows any piece of UI based on the current user's role and permission set. Applied as *hasPermission="'invoice:delete'" on a delete button, the directive checks the AuthService and either renders the template or clears it from the DOM entirely (not just hides it). You will support compound permissions with AND/OR logic via an options input, and implement an *hasRole variant. Together they form the RBAC pattern used across enterprise Angular applications.
WHY IT MATTERS
Role-based access control at the UI level is a mandatory feature in every enterprise application — HR platforms, financial systems, healthcare portals, and admin dashboards all require that different user roles see different buttons, menu items, sections, and forms. Using *ngIf scattered throughout templates with inline auth checks creates duplicated logic that is impossible to audit. The structural directive approach centralises RBAC logic, makes templates declarative and self-documenting, and is the pattern Angular Material's own directives use internally. This is a frequently requested skill in senior Angular take-home assignments.
LEARNING OBJECTIVES
* 1Create a structural directive that receives a TemplateRef and ViewContainerRef via constructor injection
* 2Implement the @Input setter that triggers a permission check and calls createEmbeddedView() or clear() on the ViewContainerRef
* 3Inject AuthService using the inject() function inside the directive and read the current user's permission array
* 4Support an optional fallback template (@Input as 'appHasPermissionElse') that renders when the permission check fails
* 5Add OR logic support so *hasPermission="['admin','superuser']" shows the element if the user has either role
* 6Ensure the directive re-evaluates when the user's permission set changes (e.g., after assuming a role) by subscribing to an auth observable
* 7Test the directive with a mock AuthService and verify that the template is rendered or cleared based on the permission state
L-15
Auto-save Draft Widget
valueChangesdebounceTimedistinctUntilChangedoptimistic updateconcatMapQ06Q16Q22
In this lesson you will build an Auto-save Draft Widget — a text editor form that silently saves the user's content to the server every time they pause typing, displaying a status indicator (Saving… → Saved ✓ → Error ✗) without any explicit save button. You will tap the FormControl's valueChanges stream, debounce it to fire only after the user pauses, and use concatMap to queue save requests so they never run out of order. The widget uses an optimistic UI pattern — local state updates immediately while the network call is in flight, rolling back on failure.
WHY IT MATTERS
Auto-save is a foundational feature of modern productivity tools — Google Docs, Notion, Confluence, and GitHub Issues all auto-save as you type. From an Angular perspective, building it correctly requires deep knowledge of valueChanges, debounce timing, request ordering (concatMap prevents out-of-order saves), and optimistic UI. It is also the most natural demonstration of why concatMap exists — mergeMap would fire parallel requests that could arrive out of order, overwriting newer content with older saves. This distinction is a classic senior interview question about RxJS operator selection.
LEARNING OBJECTIVES
* 1Subscribe to FormControl.valueChanges and apply debounceTime(800) and distinctUntilChanged() to throttle save triggers
* 2Use concatMap to queue save HTTP requests so they execute sequentially and never overlap or arrive out of order
* 3Manage a save status signal (idle / saving / saved / error) and display it as a floating badge that auto-clears after 3 seconds
* 4Implement optimistic UI — update the local draft state immediately and roll back if the server returns an error
* 5Initialise the editor with the latest draft from an API call on ngOnInit using the resolver or a direct HTTP call
* 6Unsubscribe from valueChanges on destroy using takeUntilDestroyed(destroyRef) and explain why forgetting this causes a server spam leak
* 7Contrast concatMap with mergeMap for this use case and demonstrate with a marble diagram why mergeMap would cause data corruption
L-16
Breadcrumb Navigator
Router eventsActivatedRouteroute dataNavigationEndfilter()Q13Q11Q16
In this lesson you will build a Breadcrumb Navigator — an automatically generated trail that reflects the current route hierarchy (Home → Products → Electronics → Laptops) with each segment being a clickable link. The breadcrumbs update dynamically as the user navigates without any manual configuration per page. You will listen to the Router's NavigationEnd events, walk the ActivatedRoute tree to collect route data.breadcrumb values, and render them as a reactive list. Custom route data attached to each route definition is the single source of truth for labels and paths.
WHY IT MATTERS
Breadcrumbs are a UX staple in e-commerce category pages, file managers, CMS admin panels, settings hierarchies, and multi-level data drill-downs. The Angular implementation requires understanding Router events as an Observable stream, traversing the ActivatedRoute parent chain, and attaching custom data to route configurations — all concepts that interviewers test when probing routing depth. The automatic-generation approach (rather than hard-coding breadcrumbs per component) demonstrates architectural thinking: data-driven, DRY, and maintainable as the route tree grows.
LEARNING OBJECTIVES
* 1Inject the Router and subscribe to its events stream, filtering for NavigationEnd events using the filter() and instanceof operators
* 2Traverse the ActivatedRoute tree from root to the deepest child, collecting route.data.breadcrumb values at each level
* 3Attach breadcrumb data (label and optional icon) to each route definition in the route configuration array
* 4Build the breadcrumb array as an Observable and render it using the async pipe with an *ngFor loop
* 5Apply routerLink to each breadcrumb segment so clicking navigates to that ancestor route
* 6Handle dynamic route segments (e.g., a product name from route params) by resolving the label from the ActivatedRoute snapshot
* 7Use structured JSON-LD schema markup in the breadcrumb template for SEO-correct rich results in Google Search
L-17
Global HTTP Error Banner
HttpInterceptorFncatchErrorBehaviorSubjectasync pipethrowErrorQ21Q17Q18Q06
In this lesson you will build a Global HTTP Error Banner — a persistent top-of-page notification that automatically appears whenever any HTTP request in the application fails, displays the error type and a retry action, and dismisses itself or when the next request succeeds. The functional interceptor catches all HTTP errors, classifies them by status code (401, 403, 404, 500), and pushes them to a shared ErrorStateService. The banner component subscribes to this service via the async pipe and appears or disappears reactively without any component knowing about HTTP directly.
WHY IT MATTERS
Network error handling is one of the most neglected concerns in frontend development — many apps either silently fail or show raw JSON errors to users. A centralised HTTP interceptor that classifies and broadcasts errors is the professional pattern used in production SaaS products, banking applications, and enterprise dashboards. This lesson teaches you the exact architecture for global error handling: interceptor → state service → reactive banner component. Interviewers will specifically ask "where do you handle API errors in Angular" and this is the correct senior-level answer.
LEARNING OBJECTIVES
* 1Write a functional HttpInterceptorFn that intercepts all outgoing requests and catches errors using catchError
* 2Classify HTTP errors by status code (401 → redirect to login, 403 → forbidden page, 5xx → retry prompt) inside the interceptor
* 3Push classified errors to an ErrorStateService with a BehaviorSubject, exposing a public error$ observable
* 4Re-throw the original error after pushing it to the service so individual components can still handle errors locally if needed
* 5Build an ErrorBannerComponent that subscribes to error$ via async pipe and renders the banner with an actionable message
* 6Implement a dismiss() method and auto-clear the error state after a successful subsequent request to avoid stale banners
* 7Test the interceptor using HttpClientTestingModule by flushing error responses and asserting that the ErrorStateService emits the correct error
L-18
Multi-step Form Wizard
FormGroup per stepcanDeactivateRouter statestepper@OutputQ12Q14Q15Q02
In this lesson you will build a Multi-step Form Wizard — an onboarding flow split across three named steps (Account Details → Company Info → Plan Selection) with a progress indicator, back/next navigation, step-level validation that prevents advancing with invalid data, and an "unsaved changes" warning when the user tries to navigate away mid-wizard. You will maintain each step as its own FormGroup, merge them into a single payload on final submission, and protect the wizard route with a canDeactivate guard that checks for dirty form state before allowing navigation away.
WHY IT MATTERS
Multi-step wizards appear in every enterprise onboarding, checkout, configuration, and application flow. Breaking a complex form into steps reduces cognitive load and improves completion rates. The Angular-specific challenges are: maintaining state across steps without a global store, validating per-step before advancing, and preventing accidental data loss when the user navigates away. canDeactivate is the correct tool for the latter and is a commonly tested interview topic. This lesson also demonstrates sibling component communication via a parent orchestrator — a cleaner alternative to a shared service for tightly coupled step sequences.
LEARNING OBJECTIVES
* 1Create three independent FormGroups (one per step) and manage the active step index as a Signal or local component state
* 2Prevent advancing to the next step by checking the current step's FormGroup validity and marking all controls as touched to show errors
* 3Merge all three FormGroup values into a single submission payload and POST it on the final step's submit action
* 4Implement a functional canDeactivate guard that returns a confirmation Observable when the wizard has unsaved (dirty) changes
* 5Store the partially completed wizard state in sessionStorage so refreshing the page restores the user's progress
* 6Emit step-level data from child step components to the parent wizard via @Output EventEmitter without a shared service
* 7Display a step progress indicator that marks steps as complete, current, or pending based on the active step index
L-19
Confirmation Dialog
ng-templateTemplateRefViewContainerRefCDK OverlayEmbeddedViewRefQ42Q58Q70Q09
In this lesson you will build a Confirmation Dialog — a modal overlay that asks "Are you sure?" before a destructive action (delete, cancel, submit) and returns a boolean Observable that the caller awaits. You will implement two versions: one using Angular CDK Overlay for pixel-perfect positioning and focus trapping, and a lighter version using ng-template and ViewContainerRef.createEmbeddedView() for zero-dependency scenarios. A DialogService accepts any ng-template and renders it as a blocking overlay, returning the user's choice as an Observable that the calling component subscribes to.
WHY IT MATTERS
Confirmation dialogs are the most common modal pattern in enterprise applications — delete actions, form resets, session timeouts, destructive bulk operations, and payment confirmations all require explicit user acknowledgement. The implementation pattern (service opens a dynamic component, caller awaits a response Observable) is the same architecture used by Angular Material's MatDialog. Understanding how to build it without a UI library is what proves you understand ViewContainerRef, not just how to call dialog.open(). Accessibility (focus trapping, ARIA role, Escape key) makes this a complete, production-ready exercise.
LEARNING OBJECTIVES
* 1Create a DialogService that accepts a TemplateRef and renders it as an overlay using ViewContainerRef.createEmbeddedView()
* 2Return a Subject from the open() method and complete it with true/false when the user confirms or cancels
* 3Build a backdrop overlay using CDK Overlay with positionStrategy set to GlobalPositionStrategy for centred display
* 4Implement Escape key dismissal using @HostListener('document:keydown.escape') on the dialog service or overlay component
* 5Add ARIA attributes (role="dialog", aria-modal="true", aria-labelledby) and trap focus within the dialog for accessibility
* 6Animate the dialog entrance and exit using Angular Animations with the :enter and :leave states
* 7Call the dialog from a table row's delete button and chain the returned Observable with switchMap to the actual delete HTTP request
L-20
Feature Flag Gate
InjectionTokenAPP_INITIALIZERprovideAppInitializerenvironment.tsruntime configQ10Q38Q39Q60
In this lesson you will build a Feature Flag Gate — a system that fetches a remote feature-flag configuration before the Angular app boots, then makes those flags available anywhere in the app via an InjectionToken. A *featureFlag="'new-dashboard'" structural directive conditionally renders any UI block based on the flag state, and a FeatureFlagService provides programmatic access for guards and services. You will use APP_INITIALIZER (or Angular 17's provideAppInitializer) to block app bootstrap until the remote config is loaded, simulating how LaunchDarkly, Split.io, and ConfigCat are integrated in enterprise apps.
WHY IT MATTERS
Feature flags are used in virtually every production engineering team to ship code dark, run A/B tests, enable beta features per customer tier, and roll out changes incrementally without deployments. The Angular implementation pattern — fetch config via APP_INITIALIZER before rendering, expose via InjectionToken, consume via directive — is the correct approach used by teams integrating feature-flag vendors. This lesson also demonstrates that APP_INITIALIZER is the right tool any time you need to guarantee data is available before the first component renders — a nuanced point that separates senior developers from mid-level ones.
LEARNING OBJECTIVES
* 1Create an InjectionToken for the FEATURE_FLAGS config object and register it in the app providers with a useFactory
* 2Use provideAppInitializer() (Angular 17) or APP_INITIALIZER to fetch /assets/flags.json before the app renders
* 3Implement a FeatureFlagService that inject(FEATURE_FLAGS) and exposes isEnabled(flagKey: string): boolean
* 4Build a *featureFlag structural directive that renders or clears its template based on FeatureFlagService.isEnabled()
* 5Support an else template (*featureFlag="'key'; else fallback") for graceful degradation when a flag is off
* 6Use the flag service inside a canActivate guard to block a route when a feature is disabled, redirecting to a maintenance page
* 7Explain how this pattern differs from environment.ts and why runtime config is essential for feature flags in container-based deployments
L-21
Expandable FAQ Accordion
ng-content@ContentChildrenQueryList@ViewChildrenanimationsQ03Q04Q58
In this lesson you will build an Expandable FAQ Accordion — a set of question/answer panels where clicking a question expands it with a smooth animation while collapsing any other open panel (exclusive open mode) or allows multiple open panels simultaneously (multi-open mode). You will architect it as a parent AccordionComponent that uses @ContentChildren to discover its AccordionItemComponent children projected via ng-content, coordinate their open/close state from the parent, and apply Angular Animations for smooth height transitions. This is the correct content-projection architecture for any set of coordinated child components.
WHY IT MATTERS
Accordions, tab groups, steppers, and disclosure panels all share the same architectural pattern: a parent component that orchestrates the state of multiple projected child components. Understanding how to use @ContentChildren to discover and coordinate dynamically projected content is what allows you to build component libraries — it is the pattern inside Angular Material's MatTabGroup, MatExpansionPanel, and MatStepper. The accordion is the simplest, most relatable entry point to this pattern. Angular Animations also makes a first appearance here, teaching the most common use case: height collapse/expand with :enter/:leave transitions.
LEARNING OBJECTIVES
* 1Design an AccordionComponent and AccordionItemComponent pair where items are projected into the parent via ng-content
* 2Use @ContentChildren(AccordionItemComponent) in the parent to get a QueryList of all projected child items
* 3Implement exclusive open mode by iterating over QueryList and calling close() on all siblings when one item opens
* 4Support a multi-open @Input on the accordion parent that switches between exclusive and multi-open behaviour
* 5Apply an Angular Animation trigger on the content panel using the :enter and :leave states with style({height: 0}) transitions
* 6Use ngAfterContentInit to subscribe to QueryList.changes and handle items added or removed dynamically after initial render
* 7Add keyboard navigation (arrow keys to move between items, Space/Enter to toggle) and ARIA expanded attributes for accessibility
L-22
Parallel API Dashboard Card
forkJoincombineLatestcatchErrorloading statesNgRx ComponentStoreQ07Q62Q18Q17
In this lesson you will build a Parallel API Dashboard Card — a stats widget that simultaneously fetches data from three different API endpoints (user count, revenue total, and active sessions) and renders a composite card once all data is ready, with per-source error handling that shows a partial result if one API fails rather than blanking the entire card. You will use forkJoin with individual catchError wrappers for fault tolerance, model the card's state (loading/partial/complete/error) using NgRx ComponentStore as a local state container, and animate the count values from 0 to their final values on first load.
WHY IT MATTERS
Analytics dashboards, executive summary cards, and KPI widgets in every SaaS product aggregate data from multiple backend services simultaneously. The naive implementation fires requests sequentially (each waits for the previous), making the dashboard slow. The correct implementation uses parallel requests with forkJoin and degrades gracefully when individual sources fail. This lesson also introduces NgRx ComponentStore as the right tool for complex local UI state — a step beyond BehaviorSubject services but without the global store overhead of full NgRx. It is increasingly expected knowledge in senior frontend interviews at product companies.
LEARNING OBJECTIVES
* 1Fire three HTTP requests in parallel using forkJoin with an object notation so results are labelled rather than positional
* 2Wrap each individual source in catchError(() => of(null)) so a single failure does not kill the entire forkJoin
* 3Detect partial results in the subscriber and render a degraded card state (greyed-out stat) when a source returned null
* 4Set up NgRx ComponentStore with a typed DashboardState interface and implement updater methods for each data source
* 5Create selectors using ComponentStore.select() to derive loading, error, and individual stat values from the store state
* 6Animate each stat counter from 0 to its final value on first render using an Angular Animation or a custom RxJS timer sequence
* 7Add a refresh button that re-fires all three requests and uses the ComponentStore's patchState to reset loading state before the new fetch
INPACT / PAAL METHOD