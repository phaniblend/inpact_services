{
  "success": true,
  "config": {
    "lessonId": "ai-node-71",
    "track": "node",
    "lessonNum": 71,
    "title": "Build distributed lock for critical jobs",
    "shortName": "P71",
    "intro": {
      "tag": "LESSON #71",
      "title": "Build distributed lock for critical jobs",
      "body": "In this lesson, you will build a distributed lock system to prevent multiple instances of a Node.js application from running the same critical job simultaneously. You will learn the core concept of mutual exclusion in a distributed environment, ensuring only one process can execute a task at a time. The implementation will use Node.js's built-in modules and a simple file-based locking mechanism to simulate coordination across processes. By the end, you'll understand how to create, acquire, and release locks to manage critical jobs safely.",
      "usecase": "Distributed locks are essential in production systems where multiple instances of an application need to coordinate access to shared resources. For example, in an e-commerce dashboard, when processing inventory updates during a flash sale, a distributed lock ensures only one server processes the stock reduction for a specific product ID, preventing overselling. Similarly, in a banking application's transaction processing panel, distributed locks prevent duplicate transfers when multiple users attempt to transfer funds from the same account simultaneously. This pattern is also used in scheduled job runners across cloud infrastructure to ensure critical maintenance tasks like database backups don't run concurrently on different servers."
    },
    "objectives": [
      "Construct a Node.js application that implements a distributed lock mechanism",
      "Initialize a Redis client to serve as the lock storage backend",
      "Implement lock acquisition logic with expiration to prevent deadlocks",
      "Apply lock release functionality to free resources after job completion",
      "Configure error handling for lock failures and race conditions",
      "Integrate the distributed lock into a sample critical job processing function",
      "Manipulate lock parameters such as timeout and retry intervals"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 11",
        "title": "Import required Node.js modules",
        "instruction": "Import the 'crypto' module for generating unique lock IDs and the 'fs/promises' module for file operations.",
        "hint": "Use the standard Node.js import syntax with the 'require' function.",
        "analogousExample": "// Example: importing Node.js modules\nconst fs = require('fs');\nconst path = require('path');",
        "seedCode": "// Import the required modules here\n",
        "expectedOutcome": "The 'crypto' and 'fs/promises' modules are imported and ready to use.",
        "successCriteria": [
          "The 'crypto' and 'fs/promises' modules are imported using require()."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've imported some modules, but make sure to import both 'crypto' and 'fs/promises'.",
        "feedbackWrong": "The required modules are not imported correctly. Please import both 'crypto' and 'fs/promises'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "require('crypto')",
            "require('fs/promises')"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 11",
        "title": "Create the Lock class structure",
        "instruction": "Create a Lock class with a constructor that accepts a lock file path parameter.",
        "hint": "Define a class named Lock with a constructor method that takes a parameter (e.g., lockFilePath).",
        "analogousExample": "// Similar: a basic class with constructor\nclass Task {\n  constructor(taskName) {\n    this.name = taskName;\n  }\n}",
        "seedCode": "// Import the required modules here\n\n// Create the Lock class here",
        "expectedOutcome": "A Lock class is defined with a constructor that accepts a lock file path parameter.",
        "successCriteria": [
          "The Lock class is defined with a constructor that takes a lock file path parameter."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure the Lock class has a constructor that accepts a lock file path parameter.",
        "feedbackWrong": "The Lock class structure is not correctly defined. Ensure you create a class named Lock with a constructor that takes a lock file path parameter.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "class",
            "Lock",
            "constructor",
            "lockFilePath"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 11",
        "title": "Initialize lock state properties",
        "instruction": "Inside the Lock class constructor, add three properties: `lockFilePath` to store the path string, `lockId` to store a unique identifier, and `isLocked` to track the lock status (initialize it as false).",
        "hint": "Use `this.propertyName = value` inside the constructor to set up instance properties.",
        "analogousExample": "// Similar: class with constructor properties\nclass Car {\n  constructor(brand, model) {\n    this.brand = brand;\n    this.model = model;\n    this.isRunning = false;\n  }\n}",
        "seedCode": "// Import the required modules here\n\n// Create the Lock class here\nclass Lock {\n  constructor(lockFilePath, lockId) {\n    // Add properties here\n  }\n}",
        "expectedOutcome": "The Lock class constructor will have three instance properties initialized: lockFilePath, lockId, and isLocked.",
        "successCriteria": [
          "The constructor sets this.lockFilePath, this.lockId, and this.isLocked (initialized to false) as properties."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added some properties, but make sure all three are present: lockFilePath, lockId, and isLocked (set to false initially).",
        "feedbackWrong": "The constructor should initialize three properties: lockFilePath, lockId, and isLocked (set to false). Check your syntax.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "this.lockFilePath",
            "this.lockId",
            "this.isLocked",
            "false"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 11",
        "title": "Create method to generate unique lock ID",
        "instruction": "Inside the Lock class constructor, add a private method named _generateLockId that uses crypto.randomUUID() to return a unique identifier string.",
        "hint": "Define the method inside the class body (not in the constructor). Use crypto.randomUUID() to generate the ID.",
        "analogousExample": "// Example: private method for generating IDs\nclass UniqueGenerator {\n  _generateId() {\n    return crypto.randomUUID();\n  }\n}",
        "seedCode": "// Import the required modules here\n\n// Create the Lock class here\nclass Lock {\n  constructor(lockFilePath, lockId) {\n    // Add properties here\n  }\n  \n  // Add the _generateLockId method here\n}",
        "expectedOutcome": "The Lock class now has a private method that generates unique lock IDs using Node.js crypto module.",
        "successCriteria": [
          "The Lock class contains a method named _generateLockId that returns a unique string using crypto.randomUUID()."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure your method is named _generateLockId and uses crypto.randomUUID() to return the unique ID.",
        "feedbackWrong": "The method isn't quite right. Check that you've defined _generateLockId inside the class and it returns crypto.randomUUID().",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "_generateLockId",
            "crypto.randomUUID",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 11",
        "title": "Implement the acquire lock method",
        "instruction": "Inside the Lock class, create an async method named 'acquire' that attempts to create a lock file with exclusive access.",
        "hint": "The method should be async and use the fs module's open method with the 'wx' flag to create the file exclusively.",
        "analogousExample": "// Similar: async method for exclusive file creation\nasync function createExclusiveFile(path, content) {\n  try {\n    const fd = await fs.promises.open(path, 'wx');\n    await fs.promises.writeFile(fd, content);\n    await fd.close();\n    return true;\n  } catch (error) {\n    return false;\n  }\n}",
        "seedCode": "// Import the required modules here\n\n// Create the Lock class here\nclass Lock {\n  constructor(lockFilePath, lockId) {\n    // Add properties here\n  }\n  \n  // Add the _generateLockId method here\n  \n  // Create the async acquire method here\n}",
        "expectedOutcome": "The Lock class now has an async acquire() method that will attempt to create the lock file with exclusive access.",
        "successCriteria": [
          "The Lock class contains an async acquire() method that uses the fs module to create a file with exclusive access."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure the method is async and properly attempts to create the lock file with exclusive access.",
        "feedbackWrong": "The acquire method is missing or doesn't follow the required structure. Review the instruction and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "async",
            "acquire",
            "(",
            ")",
            "{",
            "fs",
            "open",
            "wx"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 11",
        "title": "Add lock file content with metadata",
        "instruction": "Inside the acquire method, after generating the lockId, write the lock file with JSON content containing the lockId and a timestamp.",
        "hint": "Use fs.writeFileSync to write a JSON string. The content should be an object with lockId and timestamp properties.",
        "analogousExample": "// Writing metadata to a file\nconst metadata = { id: 'unique-123', createdAt: new Date().toISOString() };\nfs.writeFileSync('data.json', JSON.stringify(metadata));",
        "seedCode": "// Import the required modules here\n\n// Create the Lock class here\nclass Lock {\n  constructor(lockFilePath, lockId) {\n    // Add properties here\n  }\n  \n  // Add the _generateLockId method here\n  \n  // Create the async acquire method here\n  async acquire() {\n    const lockId = this._generateLockId();\n    // Write lock file with metadata here\n  }\n}",
        "expectedOutcome": "The acquire method now writes a lock file containing the lock ID and timestamp as JSON.",
        "successCriteria": [
          "The acquire method writes a JSON object with lockId and timestamp properties to the lock file using fs.writeFileSync."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're writing a proper JSON object with both lockId and timestamp properties to the lock file.",
        "feedbackWrong": "The acquire method should write the lock file with JSON content containing the lockId and timestamp. Check that you're using fs.writeFileSync with JSON.stringify.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "writeFileSync",
            "JSON.stringify",
            "lockId",
            "timestamp"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 11",
        "title": "Implement lock acquisition retry logic",
        "instruction": "Inside the existing acquire() method, add a retry loop with exponential backoff to handle cases where the lock file already exists.",
        "hint": "Use a while loop that attempts to create the lock file, and if it fails (e.g., file already exists), wait with increasing delay before retrying.",
        "analogousExample": "// Example retry logic with exponential backoff\nasync function fetchWithRetry(url, maxRetries = 3) {\n  let retries = 0;\n  while (retries < maxRetries) {\n    try {\n      const response = await fetch(url);\n      return response;\n    } catch (error) {\n      retries++;\n      if (retries >= maxRetries) throw error;\n      await new Promise(resolve => setTimeout(resolve, 1000 * Math.pow(2, retries)));\n    }\n  }\n}",
        "seedCode": "// Import the required modules here\n\n// Create the Lock class here\nclass Lock {\n  constructor(lockFilePath, lockId) {\n    // Add properties here\n  }\n  \n  // Add the _generateLockId method here\n  \n  // Create the async acquire method here\n  async acquire() {\n    const lockId = this._generateLockId();\n    // Write lock file with metadata here\n    // Add retry logic with exponential backoff here\n  }\n}",
        "expectedOutcome": "The acquire() method will now automatically retry lock acquisition with increasing delays when the lock is already held by another process.",
        "successCriteria": [
          "The acquire() method contains a retry loop with exponential backoff that attempts to create the lock file multiple times before giving up."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added some retry logic, but check if it includes exponential backoff (increasing delays) and proper error handling for the lock file creation.",
        "feedbackWrong": "The acquire() method doesn't yet include the retry mechanism. Make sure to add a loop that retries lock acquisition with increasing wait times.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "while",
            "retry",
            "setTimeout",
            "Math.pow",
            "await"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 11",
        "title": "Create method to check lock validity",
        "instruction": "Inside the Lock class, add an async method named 'isLocked' that reads the lock file and verifies if the current lock ID matches the stored lock ID.",
        "hint": "Use fs.promises.readFile to read the lock file, then parse the JSON content and compare the lockId property with this.lockId.",
        "analogousExample": "// Similar: async method that reads and validates data\nasync isValid() {\n  const data = await fs.promises.readFile('data.json', 'utf8');\n  const parsed = JSON.parse(data);\n  return parsed.id === this.expectedId;\n}",
        "seedCode": "// Import the required modules here\n\n// Create the Lock class here\nclass Lock {\n  constructor(lockFilePath, lockId) {\n    // Add properties here\n  }\n  \n  // Add the _generateLockId method here\n  \n  // Create the async acquire method here\n  async acquire() {\n    const lockId = this._generateLockId();\n    // Write lock file with metadata here\n    // Add retry logic with exponential backoff here\n  }\n  \n  // Add the async isLocked method here\n}",
        "expectedOutcome": "The Lock class now has an async isLocked method that will read the lock file and compare lock IDs.",
        "successCriteria": [
          "The Lock class contains an async isLocked method that reads the lock file, parses its JSON content, and returns a boolean indicating if the stored lock ID matches this.lockId."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added a method, but it might be missing some key elements. Make sure it's async, reads the lock file, parses the JSON, and compares the lockId property with this.lockId.",
        "feedbackWrong": "The method doesn't seem to be implemented correctly. Review the hint and try again - you need an async method that reads the lock file and validates the lock ID.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "async",
            "isLocked",
            "readFile",
            "JSON.parse",
            "lockId",
            "this.lockId"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 11",
        "title": "Add lock timeout mechanism",
        "instruction": "In the acquire() method, modify the lock file content to include a timestamp property and add logic to check for expired locks before acquiring.",
        "hint": "Add a 'timestamp' field with Date.now() to the lock metadata. In the retry logic, check if an existing lock file's timestamp is older than a timeout threshold (e.g., 30 seconds).",
        "analogousExample": "// Similar: adding timeout check to lock acquisition\nasync acquire() {\n  const lockId = this._generateLockId();\n  const lockContent = {\n    lockId,\n    timestamp: Date.now(),\n    processId: process.pid\n  };\n  \n  // Check for expired lock\n  if (await this._isLockExpired()) {\n    await fs.promises.unlink(this.lockFilePath);\n  }\n}",
        "seedCode": "// Import the required modules here\n\n// Create the Lock class here\nclass Lock {\n  constructor(lockFilePath, lockId) {\n    // Add properties here\n  }\n  \n  // Add the _generateLockId method here\n  \n  // Create the async acquire method here\n  async acquire() {\n    const lockId = this._generateLockId();\n    // Write lock file with metadata here\n    // Add retry logic with exponential backoff here\n  }\n  \n  // Add the async isLocked method here\n  \n  // Add the async release method here\n}",
        "expectedOutcome": "The acquire() method will include timestamp metadata and check for expired locks to prevent deadlocks.",
        "successCriteria": [
          "Lock file content includes timestamp property and retry logic checks for expired locks before acquisition."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure to add both the timestamp property and the expired lock check.",
        "feedbackWrong": "The timeout mechanism isn't fully implemented. Check that you've added timestamp metadata and logic to detect expired locks.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "timestamp",
            "Date.now()",
            "expired",
            "unlink"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 11",
        "title": "Create example usage function",
        "instruction": "Create an async function named 'criticalJobExample' that demonstrates using the Lock class: acquire a lock, perform simulated critical work, and release the lock.",
        "hint": "Define an async function that creates a Lock instance, calls acquire(), logs a message for the critical work, then calls release().",
        "analogousExample": "// Example async function demonstrating a workflow\nasync function exampleWorkflow() {\n  const resource = await getResource();\n  await processResource(resource);\n  await cleanup();\n}",
        "seedCode": "// Import the required modules here\n\n// Create the Lock class here\nclass Lock {\n  constructor(lockFilePath, lockId) {\n    // Add properties here\n  }\n  \n  // Add the _generateLockId method here\n  \n  // Create the async acquire method here\n  async acquire() {\n    const lockId = this._generateLockId();\n    // Write lock file with metadata here\n    // Add retry logic with exponential backoff here\n  }\n  \n  // Add the async isLocked method here\n  \n  // Add the async release method here\n}\n\n// Create the criticalJobExample function here",
        "expectedOutcome": "You'll have a complete async function that shows the full lock usage pattern from acquisition to release.",
        "successCriteria": [
          "The criticalJobExample async function is created and contains calls to acquire(), simulated work, and release()."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've started the function, but make sure it's async and includes all three steps: acquire, work simulation, and release.",
        "feedbackWrong": "The function isn't complete or doesn't follow the required pattern. Create an async function that shows acquiring, working, and releasing the lock.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "async",
            "function",
            "criticalJobExample",
            "acquire",
            "release"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step11",
        "type": "question",
        "phase": "Step 11 of 11",
        "title": "Export the Lock class for use",
        "instruction": "Export the Lock class using module.exports so it can be imported and used in other files.",
        "hint": "Add a module.exports statement at the end of your file to export the Lock class.",
        "analogousExample": "// Exporting a class for use in other modules\nclass Calculator {\n  add(a, b) { return a + b; }\n}\nmodule.exports = Calculator;",
        "seedCode": "// Import the required modules here\n\n// Create the Lock class here\nclass Lock {\n  constructor(lockFilePath, lockId) {\n    // Add properties here\n  }\n  \n  // Add the _generateLockId method here\n  \n  // Create the async acquire method here\n  async acquire() {\n    const lockId = this._generateLockId();\n    // Write lock file with metadata here\n    // Add retry logic with exponential backoff here\n  }\n  \n  // Add the async isLocked method here\n  \n  // Add the async release method here\n}\n\n// Create the criticalJobExample function here\n\n// Export the Lock class here",
        "expectedOutcome": "The Lock class will be properly exported and available for import in other Node.js modules.",
        "successCriteria": [
          "The Lock class is exported using module.exports."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close, but the export statement needs to be added. Make sure to use module.exports to export the Lock class.",
        "feedbackWrong": "The Lock class isn't exported yet. Add module.exports = Lock; at the end of your file.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "module.exports",
            "Lock"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import required Node.js modules",
        "id": "step1"
      },
      {
        "label": "Create the Lock class structure",
        "id": "step2"
      },
      {
        "label": "Initialize lock state properties",
        "id": "step3"
      },
      {
        "label": "Create method to generate unique lock ID",
        "id": "step4"
      },
      {
        "label": "Implement the acquire lock method",
        "id": "step5"
      },
      {
        "label": "Add lock file content with metadata",
        "id": "step6"
      },
      {
        "label": "Implement lock acquisition retry logic",
        "id": "step7"
      },
      {
        "label": "Create method to check lock validity",
        "id": "step8"
      },
      {
        "label": "Add lock timeout mechanism",
        "id": "step9"
      },
      {
        "label": "Create example usage function",
        "id": "step10"
      },
      {
        "label": "Export the Lock class for use",
        "id": "step11"
      }
    ]
  },
  "source": "content"
}