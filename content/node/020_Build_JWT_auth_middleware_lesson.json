{
  "success": true,
  "config": {
    "lessonId": "ai-node-20",
    "track": "node",
    "lessonNum": 20,
    "title": "Build JWT auth middleware",
    "shortName": "P20",
    "intro": {
      "tag": "LESSON #20",
      "title": "Build JWT auth middleware",
      "body": "In this lesson, you will build a JWT (JSON Web Token) authentication middleware for a Node.js application. You'll learn how to create a function that intercepts incoming HTTP requests, verifies the JWT from the request headers, and allows or denies access to protected routes. The core programming concept is middleware, which processes requests before they reach your route handlers. You'll use technologies like the jsonwebtoken library and Express.js middleware structure. By the end, you'll have a reusable middleware that secures your API endpoints with token-based authentication.",
      "usecase": "JWT auth middleware is a core security pattern in modern web applications, ensuring only authenticated users can access protected resources. You'll implement this in Node.js to secure API endpoints, such as user dashboards, admin panels, or payment forms in e-commerce and SaaS products. For example, when a user logs into a banking app, this middleware validates their token before allowing access to account details or transaction history. As a developer, you'll use this to protect sensitive routes, manage user sessions, and maintain secure, stateless authentication across your services."
    },
    "objectives": [
      "Construct a middleware function that intercepts HTTP requests",
      "Implement token extraction logic from the Authorization header",
      "Apply JWT verification using a secret key to validate tokens",
      "Configure error handling for invalid or missing tokens",
      "Integrate the middleware into an Express.js route to protect endpoints",
      "Manipulate the request object to attach decoded user data for downstream handlers",
      "Initialize a test route to verify the middleware's authentication behavior"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 9",
        "title": "Import required Node.js modules (jsonwebtoken and dotenv)",
        "instruction": "Import the jsonwebtoken and dotenv modules using require() statements.",
        "hint": "Use require() to bring in external packages. Place the imports at the top of your file.",
        "analogousExample": "// Example: importing modules\nconst fs = require('fs');\nconst path = require('path');",
        "seedCode": "// Import jsonwebtoken and dotenv modules here",
        "expectedOutcome": "The jsonwebtoken and dotenv modules are imported and ready to use.",
        "successCriteria": [
          "jsonwebtoken and dotenv are imported using require() statements."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you've imported both jsonwebtoken and dotenv modules.",
        "feedbackWrong": "Check that you're using require() to import both jsonwebtoken and dotenv modules.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "require('jsonwebtoken')",
            "require('dotenv')"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 9",
        "title": "Configure environment variables with dotenv",
        "instruction": "Call dotenv.config() to load environment variables from the .env file.",
        "hint": "After importing dotenv, you need to call its config method to load variables from your .env file into process.env.",
        "analogousExample": "// Loading configuration from a file\nrequire('dotenv').config();",
        "seedCode": "// Import jsonwebtoken and dotenv modules here\n\n// Call dotenv.config() here",
        "expectedOutcome": "Environment variables from .env file are loaded and accessible via process.env.",
        "successCriteria": [
          "dotenv.config() is called to load environment variables."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're calling dotenv.config() correctly.",
        "feedbackWrong": "The configuration step is missing. You need to call dotenv.config() to load environment variables.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "dotenv.config()"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 9",
        "title": "Define the middleware function signature",
        "instruction": "Create a function named authMiddleware that takes req, res, and next parameters. The function body can be empty for now.",
        "hint": "Remember that Express middleware functions receive three parameters: request, response, and next callback.",
        "analogousExample": "// Example: middleware function structure\nfunction loggerMiddleware(req, res, next) {\n  console.log('Request received');\n  next();\n}",
        "seedCode": "// Import jsonwebtoken and dotenv modules here\nconst jwt = require('jsonwebtoken');\nrequire('dotenv').config();\n\n// Call dotenv.config() here\n\n// Create your authMiddleware function here",
        "expectedOutcome": "You'll have a properly structured middleware function ready to implement authentication logic in later steps.",
        "successCriteria": [
          "A function named authMiddleware exists with req, res, and next parameters"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure your function is named authMiddleware and accepts all three parameters.",
        "feedbackWrong": "The function signature doesn't match the requirements. Check the function name and parameters.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "authMiddleware",
            "req",
            "res",
            "next"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 9",
        "title": "Extract the authorization header from request",
        "instruction": "Inside your authMiddleware function, extract the authorization header from the request object's headers property.",
        "hint": "Access the authorization header using req.headers.authorization or req.headers['authorization'].",
        "analogousExample": "// Example: accessing a specific header in middleware\nfunction logUserAgent(req, res, next) {\n  const userAgent = req.headers['user-agent'];\n  console.log(userAgent);\n  next();\n}",
        "seedCode": "const jwt = require('jsonwebtoken');\nrequire('dotenv').config();\n\nfunction authMiddleware(req, res, next) {\n  // Extract the authorization header here\n}",
        "expectedOutcome": "The middleware function now contains code to access the authorization header from the incoming request.",
        "successCriteria": [
          "The authMiddleware function includes a line that extracts the authorization header from req.headers."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're extracting the authorization header specifically from req.headers.",
        "feedbackWrong": "The authorization header extraction is missing or incorrect. Check that you're accessing req.headers.authorization or req.headers['authorization'].",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "req.headers",
            "authorization"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 9",
        "title": "Check if authorization header exists",
        "instruction": "Inside the authMiddleware function, add a conditional check to verify if the authorization header exists. If it's missing, send a 401 Unauthorized response and stop execution.",
        "hint": "You can check if the authorization header is undefined or empty, then use res.status(401).json() to send an error response.",
        "analogousExample": "// Example: checking for required data\nfunction validateRequest(req, res, next) {\n  if (!req.body.email) {\n    return res.status(400).json({ error: 'Email is required' });\n  }\n  next();\n}",
        "seedCode": "const jwt = require('jsonwebtoken');\nrequire('dotenv').config();\n\nfunction authMiddleware(req, res, next) {\n  // Extract the authorization header here\n  // Add conditional check for authorization header\n}",
        "expectedOutcome": "The middleware now validates that the authorization header is present before proceeding.",
        "successCriteria": [
          "The authMiddleware function includes a conditional check for the authorization header and returns a 401 response if missing."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added some validation logic, but make sure to check specifically for the authorization header and return a 401 response when it's missing.",
        "feedbackWrong": "The authorization header check isn't implemented correctly. The middleware should verify the header exists and respond with 401 Unauthorized if it doesn't.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "authorization",
            "401",
            "return",
            "res.status",
            "json"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 9",
        "title": "Extract the token from the authorization header",
        "instruction": "Inside the authMiddleware function, after checking if the authorization header exists, extract the JWT token from the 'Bearer ' prefix by splitting the header string.",
        "hint": "The authorization header format is 'Bearer <token>'. Use the split method on the header string to separate the prefix from the token.",
        "analogousExample": "// Extracting a token from a Bearer header\nconst authHeader = 'Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9';\nconst token = authHeader.split(' ')[1];",
        "seedCode": "const jwt = require('jsonwebtoken');\nrequire('dotenv').config();\n\nfunction authMiddleware(req, res, next) {\n  // Extract the authorization header here\n  // Add conditional check for authorization header\n  // Extract the token from the authorization header\n}",
        "expectedOutcome": "The authorization header is parsed to isolate the JWT token, ready for verification.",
        "successCriteria": [
          "The token variable is extracted from the authorization header using split."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure to split the authorization header string and assign the token part to a variable.",
        "feedbackWrong": "The token extraction is missing or incorrect. Remember to split the header string to get the token after 'Bearer '.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "split",
            "token",
            "[1]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 9",
        "title": "Handle successful token verification",
        "instruction": "Inside the try block of your authMiddleware function, attach the decoded payload to req.user and call next() to continue the middleware chain.",
        "hint": "After verifying the token, you'll receive a decoded payload. Assign it to req.user as a property, then call next() to pass control to the next middleware.",
        "analogousExample": "// Example: processing verified data and continuing\nfunction processRequest(req, res, next) {\n  try {\n    const verifiedData = verifyData(req.token);\n    req.userData = verifiedData;\n    next();\n  } catch (error) {\n    res.status(401).json({ error: 'Invalid data' });\n  }\n}",
        "seedCode": "const jwt = require('jsonwebtoken');\nrequire('dotenv').config();\n\nfunction authMiddleware(req, res, next) {\n  const authHeader = req.headers.authorization;\n  \n  if (!authHeader) {\n    return res.status(401).json({ error: 'Authorization header missing' });\n  }\n  \n  const token = authHeader.split(' ')[1];\n  \n  try {\n    // Verify the token and handle successful verification here\n  } catch (error) {\n    return res.status(401).json({ error: 'Invalid token' });\n  }\n}",
        "expectedOutcome": "The try block now contains code that attaches the verified token payload to req.user and calls next(), allowing authenticated requests to proceed.",
        "successCriteria": [
          "The try block includes jwt.verify() with the token and secret, assigns the decoded payload to req.user, and calls next()."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're: 1) calling jwt.verify() with the token and process.env.JWT_SECRET, 2) assigning the result to req.user, and 3) calling next() in the try block.",
        "feedbackWrong": "The try block should contain the token verification, assignment to req.user, and next() call. Check that you're using jwt.verify() with the correct parameters.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "jwt.verify",
            "req.user",
            "next()"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 9",
        "title": "Handle token verification errors",
        "instruction": "Inside the catch block, return a 401 status for invalid tokens and a 403 status for expired tokens with appropriate error messages.",
        "hint": "Check the error message to determine if it's an expired token (like 'jwt expired') and send the corresponding HTTP status code.",
        "analogousExample": "// Example: Differentiating error types in a catch block\ntry {\n  // Some operation\n} catch (error) {\n  if (error.message.includes('expired')) {\n    return res.status(403).json({ error: 'Token expired' });\n  } else {\n    return res.status(401).json({ error: 'Invalid token' });\n  }\n}",
        "seedCode": "const jwt = require('jsonwebtoken');\nrequire('dotenv').config();\n\nfunction authMiddleware(req, res, next) {\n  const authHeader = req.headers.authorization;\n  \n  if (!authHeader) {\n    return res.status(401).json({ error: 'Authorization header missing' });\n  }\n  \n  const token = authHeader.split(' ')[1];\n  \n  try {\n    // Verify the token and handle successful verification here\n  } catch (error) {\n    // Handle token verification errors here\n  }\n}",
        "expectedOutcome": "The catch block now distinguishes between invalid and expired tokens, returning appropriate HTTP status codes and error messages.",
        "successCriteria": [
          "The catch block returns a 401 status for invalid tokens and a 403 status for expired tokens with descriptive error messages."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure to check the error message and return the correct status codes (401 for invalid, 403 for expired).",
        "feedbackWrong": "The catch block should check the error message and return appropriate status codes: 401 for invalid tokens, 403 for expired tokens.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "catch",
            "error.message",
            "includes",
            "expired",
            "403",
            "401"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 9",
        "title": "Export the middleware function",
        "instruction": "Export the authMiddleware function using module.exports so it can be imported in other files.",
        "hint": "Add an export statement at the end of your file.",
        "analogousExample": "// Exporting a function for use elsewhere\nfunction greet() {}\nmodule.exports = greet;",
        "seedCode": "const jwt = require('jsonwebtoken');\nrequire('dotenv').config();\n\nfunction authMiddleware(req, res, next) {\n  const authHeader = req.headers.authorization;\n  \n  if (!authHeader) {\n    return res.status(401).json({ error: 'Authorization header missing' });\n  }\n  \n  const token = authHeader.split(' ')[1];\n  \n  try {\n    // Verify the token and handle successful verification here\n  } catch (error) {\n    // Handle token verification errors here\n  }\n}\n\n// Export the authMiddleware function here",
        "expectedOutcome": "The authMiddleware function is exported and ready to be used in other parts of your application.",
        "successCriteria": [
          "The authMiddleware function is exported using module.exports."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're exporting the authMiddleware function using module.exports.",
        "feedbackWrong": "The middleware function isn't exported correctly. Check that you're using module.exports with the authMiddleware function.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "module.exports",
            "authMiddleware"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import required Node.js modules (jsonwebtoken and dotenv)",
        "id": "step1"
      },
      {
        "label": "Configure environment variables with dotenv",
        "id": "step2"
      },
      {
        "label": "Define the middleware function signature",
        "id": "step3"
      },
      {
        "label": "Extract the authorization header from request",
        "id": "step4"
      },
      {
        "label": "Check if authorization header exists",
        "id": "step5"
      },
      {
        "label": "Extract the token from the authorization header",
        "id": "step6"
      },
      {
        "label": "Handle successful token verification",
        "id": "step7"
      },
      {
        "label": "Handle token verification errors",
        "id": "step8"
      },
      {
        "label": "Export the middleware function",
        "id": "step9"
      }
    ]
  },
  "source": "content"
}