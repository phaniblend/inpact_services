{
  "success": true,
  "config": {
    "lessonId": "ai-node-42",
    "track": "node",
    "lessonNum": 42,
    "title": "Build outbound webhook retry queue",
    "shortName": "P42",
    "intro": {
      "tag": "LESSON #42",
      "title": "Build outbound webhook retry queue",
      "body": "In this lesson, you will build a simple outbound webhook retry queue using Node.js. You will learn how to handle failed webhook calls by storing them in a queue and retrying them later. The core programming concept is using an array as a basic in-memory queue to manage retry tasks. You will work with JavaScript functions, timers, and error handling to implement the retry logic. This project introduces you to fundamental queue operations and asynchronous task management in a backend context.",
      "usecase": "Webhook retry queues are essential in production systems where reliable third-party integrations are critical. You'll see this pattern in payment processing platforms like Stripe, which must guarantee transaction notifications reach merchant systems, or in e-commerce platforms that sync inventory updates to external warehouse management systems. Developer dashboards for services like Twilio or SendGrid prominently display webhook delivery status and retry logs, allowing engineers to monitor integration health. When implementing notification systems, user activity feeds, or data synchronization between microservices, robust retry logic prevents data loss during temporary network failures or service outages."
    },
    "objectives": [
      "Construct a retry queue data structure using JavaScript arrays",
      "Implement a function to add failed webhook requests to the retry queue",
      "Configure a scheduled job to process queued retry attempts",
      "Apply exponential backoff logic to calculate retry intervals",
      "Manipulate queue items to track retry counts and timestamps",
      "Initialize a Node.js application with queue management dependencies",
      "Control queue processing to prevent duplicate retry attempts"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 8",
        "title": "Import required Node.js modules",
        "instruction": "Import the events, http, and util modules from Node.js.",
        "hint": "Use the require() function to import each module. These are built-in Node.js modules, so you don't need to install anything.",
        "analogousExample": "// Example: importing built-in modules\nconst fs = require('fs');\nconst path = require('path');",
        "seedCode": "// Import events, http, and util modules here",
        "expectedOutcome": "The events, http, and util modules are imported and available for use in your webhook retry queue implementation.",
        "successCriteria": [
          "The events, http, and util modules are imported using require()."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you've imported all three modules: events, http, and util.",
        "feedbackWrong": "That doesn't look right. Remember to import the events, http, and util modules using require().",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "require('events')",
            "require('http')",
            "require('util')"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 8",
        "title": "Create WebhookRetryQueue class structure",
        "instruction": "Create the WebhookRetryQueue class with a constructor that initializes an empty array property named 'queue'.",
        "hint": "Use the class keyword followed by the class name and define a constructor method that sets this.queue to an empty array.",
        "analogousExample": "// Similar: a basic class with constructor\nclass TaskList {\n  constructor() {\n    this.tasks = [];\n  }\n}",
        "seedCode": "// Import events, http, and util modules here\n\n// Create WebhookRetryQueue class here",
        "expectedOutcome": "A WebhookRetryQueue class with a constructor that initializes a queue property as an empty array.",
        "successCriteria": [
          "The WebhookRetryQueue class is defined with a constructor that sets this.queue to an empty array."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you've defined the WebhookRetryQueue class with a constructor that sets this.queue to an empty array.",
        "feedbackWrong": "The class structure isn't correct. You need to define the WebhookRetryQueue class with a constructor that initializes this.queue as an empty array.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "class",
            "WebhookRetryQueue",
            "constructor",
            "this.queue",
            "[]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 8",
        "title": "Initialize queue and retry configuration",
        "instruction": "Inside the WebhookRetryQueue class constructor, initialize the queue array, maxRetries, and retryDelay properties.",
        "hint": "Set up the internal data structures by assigning initial values to these properties in the constructor.",
        "analogousExample": "// Similar: initializing properties in a class constructor\nclass TaskQueue {\n  constructor() {\n    this.tasks = [];\n    this.maxTasks = 10;\n    this.delay = 1000;\n  }\n}",
        "seedCode": "// Import events, http, and util modules here\n\n// Create WebhookRetryQueue class here\nclass WebhookRetryQueue {\n  constructor() {\n    // Initialize queue array, maxRetries, and retryDelay here\n  }\n}",
        "expectedOutcome": "The WebhookRetryQueue class now has internal properties ready to store webhook jobs and retry configuration.",
        "successCriteria": [
          "The constructor initializes this.queue as an empty array, this.maxRetries as a number (e.g., 3), and this.retryDelay as a number (e.g., 5000)."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've started setting up the properties, but make sure all three (queue, maxRetries, retryDelay) are initialized in the constructor.",
        "feedbackWrong": "The constructor doesn't yet initialize the required properties. Add this.queue, this.maxRetries, and this.retryDelay with appropriate initial values.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "this.queue",
            "this.maxRetries",
            "this.retryDelay",
            "constructor"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 8",
        "title": "Add webhook to the queue",
        "instruction": "Inside the WebhookRetryQueue class, create an addToQueue method that accepts url, payload, and headers parameters.",
        "hint": "Define a method that takes three parameters and adds them as an object to the queue array.",
        "analogousExample": "// Similar: method to add items to a list\nclass TaskQueue {\n  addTask(taskName, priority) {\n    this.tasks.push({ taskName, priority });\n  }\n}",
        "seedCode": "// Import events, http, and util modules here\n\n// Create WebhookRetryQueue class here\nclass WebhookRetryQueue {\n  constructor() {\n    // Initialize queue array, maxRetries, and retryDelay here\n  }\n  \n  // Create addToQueue method here\n}",
        "expectedOutcome": "The WebhookRetryQueue class now has an addToQueue method that can accept webhook request details.",
        "successCriteria": [
          "The addToQueue method is defined with three parameters (url, payload, headers) and adds them to the queue array."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure your method accepts all three parameters and adds them to the queue array.",
        "feedbackWrong": "The addToQueue method should be defined inside the class with three parameters and add them to the queue.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "addToQueue",
            "url",
            "payload",
            "headers",
            "push"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 8",
        "title": "Process the queue",
        "instruction": "Create a method named processQueue in the WebhookRetryQueue class that processes items from the queue.",
        "hint": "Add a method that checks if there are items in the queue and processes them.",
        "analogousExample": "// Similar: a method that processes items\nclass TaskQueue {\n  processQueue() {\n    while (this.queue.length > 0) {\n      const task = this.queue.shift();\n      console.log('Processing:', task);\n    }\n  }\n}",
        "seedCode": "// Import events, http, and util modules here\nconst http = require('http');\nconst EventEmitter = require('events');\nconst util = require('util');\n\n// Create WebhookRetryQueue class here\nclass WebhookRetryQueue {\n  constructor() {\n    // Initialize queue array, maxRetries, and retryDelay here\n    this.queue = [];\n    this.maxRetries = 3;\n    this.retryDelay = 5000;\n  }\n  \n  // Create addToQueue method here\n  addToQueue(webhook) {\n    this.queue.push(webhook);\n  }\n  \n  // Create retryAttempt method here\n  \n  // Create calculateDelay method here\n  \n  // Create processQueue method here\n  \n}",
        "expectedOutcome": "The WebhookRetryQueue class will have a processQueue method that can process items from the queue.",
        "successCriteria": [
          "A processQueue method is defined in the WebhookRetryQueue class."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you've created a method named processQueue inside the WebhookRetryQueue class.",
        "feedbackWrong": "The processQueue method is not properly defined. Please create a method named processQueue in the WebhookRetryQueue class.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "processQueue",
            "method",
            "WebhookRetryQueue"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 8",
        "title": "Add event emitter for status updates",
        "instruction": "Make your WebhookRetryQueue class extend EventEmitter to enable event-driven status monitoring.",
        "hint": "Use the 'extends' keyword to inherit from EventEmitter in your class declaration.",
        "analogousExample": "// Extending EventEmitter\nclass NotificationSystem extends EventEmitter {\n  constructor() {\n    super();\n  }\n}",
        "seedCode": "// Import events, http, and util modules here\nconst http = require('http');\nconst EventEmitter = require('events');\nconst util = require('util');\n\n// Create WebhookRetryQueue class here\nclass WebhookRetryQueue {\n  constructor() {\n    // Initialize queue array, maxRetries, and retryDelay here\n    this.queue = [];\n    this.maxRetries = 3;\n    this.retryDelay = 5000;\n  }\n  \n  // Create addToQueue method here\n  addToQueue(webhook) {\n    this.queue.push(webhook);\n  }\n  \n  // Create retryAttempt method here\n  \n  // Create calculateDelay method here\n  \n  // Create processQueue method here\n  \n}",
        "expectedOutcome": "Your WebhookRetryQueue class will inherit event-emitting capabilities from Node.js's EventEmitter class.",
        "successCriteria": [
          "WebhookRetryQueue class extends EventEmitter and calls super() in constructor"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure to both extend EventEmitter and call super() in the constructor.",
        "feedbackWrong": "Check if you're using the 'extends' keyword correctly and calling super() in the constructor.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "extends",
            "EventEmitter",
            "super()"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 8",
        "title": "Create usage example",
        "instruction": "Create a usage example that instantiates the WebhookRetryQueue class and adds a sample webhook request to the queue.",
        "hint": "Instantiate the class with 'new WebhookRetryQueue()', then call addToQueue() with a sample webhook object containing URL and data.",
        "analogousExample": "// Example: creating and using a class instance\nconst queue = new WebhookRetryQueue();\nqueue.addToQueue({ url: 'https://example.com', data: { event: 'test' } });",
        "seedCode": "// Import events, http, and util modules here\nconst http = require('http');\nconst EventEmitter = require('events');\nconst util = require('util');\n\n// Create WebhookRetryQueue class here\nclass WebhookRetryQueue {\n  constructor() {\n    // Initialize queue array, maxRetries, and retryDelay here\n    this.queue = [];\n    this.maxRetries = 3;\n    this.retryDelay = 5000;\n  }\n  \n  // Create addToQueue method here\n  addToQueue(webhook) {\n    this.queue.push(webhook);\n  }\n  \n  // Create retryAttempt method here\n  \n  // Create calculateDelay method here\n  \n  // Create processQueue method here\n  \n}\n\n// Create usage example here",
        "expectedOutcome": "The code will include a usage example that demonstrates how to instantiate the WebhookRetryQueue and add a webhook request.",
        "successCriteria": [
          "The WebhookRetryQueue is instantiated and addToQueue() is called with a sample webhook object."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you both instantiate the WebhookRetryQueue class and call addToQueue() with a sample webhook object.",
        "feedbackWrong": "The usage example is missing or incomplete. You need to instantiate WebhookRetryQueue and add a webhook to the queue.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "new WebhookRetryQueue",
            "addToQueue",
            "{"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 8",
        "title": "Test the retry queue functionality",
        "instruction": "Add a test webhook request and event listeners to verify the retry behavior in your usage example.",
        "hint": "Create a mock webhook object with a URL and data, add it to the queue, then listen for 'retry' and 'success' events to log the status.",
        "analogousExample": "// Testing a queue with events\nconst queue = new WebhookRetryQueue();\nqueue.on('retry', (attempt, url) => {\n  console.log(`Retry attempt ${attempt} for ${url}`);\n});\nqueue.on('success', (url) => {\n  console.log(`Success for ${url}`);\n});\nqueue.addToQueue({ url: 'https://example.com', data: { test: true } });",
        "seedCode": "// Import events, http, and util modules here\nconst http = require('http');\nconst EventEmitter = require('events');\nconst util = require('util');\n\n// Create WebhookRetryQueue class here\nclass WebhookRetryQueue {\n  constructor() {\n    // Initialize queue array, maxRetries, and retryDelay here\n    this.queue = [];\n    this.maxRetries = 3;\n    this.retryDelay = 5000;\n  }\n  \n  // Create addToQueue method here\n  addToQueue(webhook) {\n    this.queue.push(webhook);\n  }\n  \n  // Create retryAttempt method here\n  \n  // Create calculateDelay method here\n  \n  // Create processQueue method here\n  \n}\n\n// Create usage example here\n// Add test webhook requests and event listeners here",
        "expectedOutcome": "The usage example will include a test webhook object and event listeners that log retry attempts and successes.",
        "successCriteria": [
          "A test webhook object is created and added to the queue, and event listeners for 'retry' and 'success' events are set up to log messages."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added some test code, but make sure to include both a test webhook object and event listeners for 'retry' and 'success' events.",
        "feedbackWrong": "The test code is missing or incomplete. Please add a test webhook object and event listeners to verify the retry behavior.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "addToQueue",
            "on",
            "retry",
            "success",
            "console.log"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import required Node.js modules",
        "id": "step1"
      },
      {
        "label": "Create WebhookRetryQueue class structure",
        "id": "step2"
      },
      {
        "label": "Initialize queue and retry configuration",
        "id": "step3"
      },
      {
        "label": "Add webhook to the queue",
        "id": "step4"
      },
      {
        "label": "Process the queue",
        "id": "step5"
      },
      {
        "label": "Add event emitter for status updates",
        "id": "step6"
      },
      {
        "label": "Create usage example",
        "id": "step7"
      },
      {
        "label": "Test the retry queue functionality",
        "id": "step8"
      }
    ]
  },
  "source": "content"
}