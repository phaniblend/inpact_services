{
  "success": true,
  "config": {
    "lessonId": "ai-node-11",
    "track": "node",
    "lessonNum": 11,
    "title": "Error handling",
    "shortName": "P11",
    "intro": {
      "tag": "LESSON #11",
      "title": "Error handling",
      "body": "In this lesson, you will build a simple Node.js application that handles errors gracefully when reading a file. You will learn the core concept of error handling using try-catch blocks to manage and respond to runtime errors. The main technologies involved are Node.js's built-in 'fs' module for file operations and JavaScript's error handling constructs. By the end, you'll understand how to prevent crashes and provide useful feedback when things go wrong in your code.",
      "usecase": "Error handling is essential in any Node.js application that interacts with external systems or user input. In real products, this appears when APIs fail to respond, database connections drop, or users submit invalid data in forms. For example, an e-commerce checkout system must gracefully handle payment gateway errors by showing user-friendly messages instead of crashing. A dashboard displaying analytics needs to manage failed data fetches by showing fallback content or retry options. Proper error handling ensures applications remain stable and provide helpful feedback rather than cryptic technical failures."
    },
    "objectives": [
      "Implement try-catch blocks to handle synchronous errors in Node.js functions",
      "Construct error objects with custom messages and properties for specific scenarios",
      "Apply error-first callback patterns in asynchronous operations",
      "Configure global error handlers to catch unhandled exceptions in Node.js applications",
      "Integrate error handling middleware in Express.js route handlers",
      "Manipulate error propagation in promise chains using .catch() methods",
      "Initialize custom error classes by extending the built-in Error object"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Import required Node.js modules",
        "instruction": "Import the fs (file system) module using require()",
        "hint": "Use the require() function to import the built-in fs module. This will give you access to file system operations.",
        "analogousExample": "// Example of importing a built-in module\nconst path = require('path');",
        "seedCode": "// Import the fs module here",
        "expectedOutcome": "The fs module will be imported and available for use in your error handling code.",
        "successCriteria": [
          "The fs module is imported using require('fs')"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're using require('fs') to import the file system module.",
        "feedbackWrong": "That doesn't look right. Remember to use require('fs') to import the built-in file system module.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "require",
            "fs"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Create a synchronous function that reads a file",
        "instruction": "Define a function named readFileSync that takes a filename parameter and uses fs.readFileSync to read the file synchronously. Return the file content.",
        "hint": "Use the fs module you imported earlier. The function should take one parameter and return the result of fs.readFileSync.",
        "analogousExample": "// Example: a function that reads a file\nfunction readFileSync(filename) {\n  const content = fs.readFileSync(filename, 'utf8');\n  return content;\n}",
        "seedCode": "// Import the fs module here\nconst fs = require('fs');\n\n// Define the readFileSync function here",
        "expectedOutcome": "A function named readFileSync is defined that can read files synchronously using the fs module.",
        "successCriteria": [
          "The readFileSync function is defined with a filename parameter, uses fs.readFileSync, and returns the file content."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure your function uses fs.readFileSync and returns the result.",
        "feedbackWrong": "The function isn't quite right. Check that it's named readFileSync, takes a filename parameter, and uses fs.readFileSync to read the file.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "readFileSync",
            "filename",
            "fs.readFileSync",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Add try-catch block to handle synchronous errors",
        "instruction": "Inside your synchronous file reading function, wrap the fs.readFileSync call in a try block and add a catch block to handle potential errors.",
        "hint": "Use try { ... } catch (error) { ... } syntax. Inside the catch block, you can log the error or handle it appropriately.",
        "analogousExample": "// Example: try-catch for synchronous operation\nfunction readConfig() {\n  try {\n    const data = fs.readFileSync('config.json', 'utf8');\n    return data;\n  } catch (error) {\n    console.error('Error reading config:', error.message);\n    return null;\n  }\n}",
        "seedCode": "// Import the fs module here\nconst fs = require('fs');\n\n// Define the readFileSync function here\nfunction readFileSyncExample() {\n  // Wrap fs.readFileSync in try-catch here\n}",
        "expectedOutcome": "The function will have a try block containing the file reading operation and a catch block to handle errors.",
        "successCriteria": [
          "The function contains a try block with fs.readFileSync inside it and a catch block that handles errors."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you have both a try block (with fs.readFileSync inside) and a catch block to handle errors.",
        "feedbackWrong": "The function doesn't have proper try-catch error handling. Remember to wrap the file reading operation in a try block and add a catch block to handle errors.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "try",
            "catch",
            "fs.readFileSync"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Log success and error messages in the try-catch",
        "instruction": "Inside the try block of your readFileSyncExample function, add a console.log statement that logs a success message when the file is read successfully. Inside the catch block, add a console.log statement that logs the error message.",
        "hint": "Use console.log() with a descriptive string in the try block. In the catch block, you can log the error parameter (e.g., console.log(error)).",
        "analogousExample": "// Example of logging in try-catch\nfunction exampleFunction() {\n  try {\n    console.log('Operation succeeded!');\n  } catch (error) {\n    console.log('Operation failed:', error);\n  }\n}",
        "seedCode": "// Import the fs module here\nconst fs = require('fs');\n\n// Define the readFileSync function here\nfunction readFileSyncExample() {\n  // Wrap fs.readFileSync in try-catch here\n  try {\n    // Add success log here\n  } catch (error) {\n    // Add error log here\n  }\n}",
        "expectedOutcome": "The function will log a success message when the file read operation succeeds and an error message when it fails, making the error handling visible.",
        "successCriteria": [
          "The try block contains a console.log statement with a success message. The catch block contains a console.log statement that logs the error parameter."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you have both a success log in the try block and an error log in the catch block.",
        "feedbackWrong": "Check that you've added console.log statements in both the try and catch blocks with appropriate messages.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "try",
            "console.log",
            "catch",
            "console.log"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Create an asynchronous function that reads a file",
        "instruction": "Define an async function named readFileAsync that takes a filename parameter. Place it after the existing synchronous function.",
        "hint": "Use the async keyword before function to make it asynchronous. The function should accept one parameter.",
        "analogousExample": "// Example of an async function with a parameter\nasync function fetchData(url) {\n  // async operations go here\n}",
        "seedCode": "// Import the fs module here\nconst fs = require('fs');\n\n// Define the readFileSync function here\nfunction readFileSyncExample() {\n  // Wrap fs.readFileSync in try-catch here\n  try {\n    // Add success log here\n  } catch (error) {\n    // Add error log here\n  }\n}\n\n// Define the async function here\n",
        "expectedOutcome": "You will have an async function named readFileAsync that accepts a filename parameter, ready for implementing file reading logic.",
        "successCriteria": [
          "The code contains an async function named readFileAsync with one parameter."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure the function is async and has the correct name and parameter.",
        "feedbackWrong": "The function isn't defined correctly. Check that it's async, named readFileAsync, and accepts a filename parameter.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "async",
            "function",
            "readFileAsync",
            "filename"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Use try-catch with await for async error handling",
        "instruction": "Inside the readFileAsync function, add a try-catch block and use await with fs.promises.readFile() to handle asynchronous errors.",
        "hint": "Remember to use the async keyword on the function and await inside the try block. Use fs.promises.readFile() for the asynchronous file reading.",
        "analogousExample": "// Example: async function with try-catch\nasync function fetchData() {\n  try {\n    const data = await fetch('url');\n    console.log('Data fetched:', data);\n  } catch (error) {\n    console.error('Fetch error:', error);\n  }\n}",
        "seedCode": "// Import the fs module here\nconst fs = require('fs');\n\n// Define the readFileSync function here\nfunction readFileSyncExample() {\n  // Wrap fs.readFileSync in try-catch here\n  try {\n    // Add success log here\n  } catch (error) {\n    // Add error log here\n  }\n}\n\n// Define the async function here\nasync function readFileAsync() {\n  // Add try-catch with await here\n}",
        "expectedOutcome": "The readFileAsync function should have a try-catch block that uses await with fs.promises.readFile() to handle asynchronous file reading errors.",
        "successCriteria": [
          "The readFileAsync function contains a try-catch block with await fs.promises.readFile() inside the try block."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you have both the try and catch blocks, and use await with fs.promises.readFile().",
        "feedbackWrong": "Check that you've added a try-catch block inside readFileAsync and used await with fs.promises.readFile().",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "try",
            "catch",
            "await",
            "fs.promises.readFile"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Log results from the async function",
        "instruction": "Inside the readFileAsync function, add console.log statements in both the try and catch blocks to log success and error messages.",
        "hint": "Use console.log with descriptive messages in both blocks. In the try block, log a success message when the file is read. In the catch block, log the error message.",
        "analogousExample": "// Example of logging in try-catch\nasync function exampleAsync() {\n  try {\n    console.log('Operation succeeded!');\n  } catch (err) {\n    console.log('Error occurred:', err.message);\n  }\n}",
        "seedCode": "// Import the fs module here\nconst fs = require('fs');\n\n// Define the readFileSync function here\nfunction readFileSyncExample() {\n  // Wrap fs.readFileSync in try-catch here\n  try {\n    // Add success log here\n  } catch (error) {\n    // Add error log here\n  }\n}\n\n// Define the async function here\nasync function readFileAsync() {\n  // Add try-catch with await here\n  try {\n    // Add success log here\n  } catch (error) {\n    // Add error log here\n  }\n}",
        "expectedOutcome": "The readFileAsync function will have console.log statements in both try and catch blocks to handle success and error outcomes.",
        "successCriteria": [
          "The readFileAsync function contains console.log statements in both the try and catch blocks."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you have console.log statements in both the try and catch blocks of the readFileAsync function.",
        "feedbackWrong": "The readFileAsync function should have console.log statements in both the try and catch blocks to log success and error messages.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "console.log",
            "try",
            "catch"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Create a function that throws a custom error",
        "instruction": "Define a function called validateInput that throws a new Error with a custom message if the input is invalid. The function should accept one parameter and throw an error when the parameter is falsy.",
        "hint": "Create a function that checks if the input exists (is truthy). If it's falsy, throw a new Error with a message like 'Invalid input'.",
        "analogousExample": "// Example of a function that throws a custom error\nfunction validateAge(age) {\n  if (age < 0) {\n    throw new Error('Age cannot be negative');\n  }\n}",
        "seedCode": "// Import the fs module here\nconst fs = require('fs');\n\n// Define the readFileSync function here\nfunction readFileSyncExample() {\n  // Wrap fs.readFileSync in try-catch here\n  try {\n    // Add success log here\n  } catch (error) {\n    // Add error log here\n  }\n}\n\n// Define the async function here\nasync function readFileAsync() {\n  // Add try-catch with await here\n  try {\n    // Add success log here\n  } catch (error) {\n    // Add error log here\n  }\n}\n\n// Define the validateInput function here\n",
        "expectedOutcome": "You will have a validateInput function that throws a custom error when given invalid input.",
        "successCriteria": [
          "The validateInput function is defined, accepts one parameter, and throws a new Error with a custom message when the input is falsy."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure your function throws a new Error with a custom message when the input is invalid (falsy).",
        "feedbackWrong": "Create a function called validateInput that checks if the input is falsy and throws a new Error with a custom message.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "validateInput",
            "function",
            "throw",
            "Error"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "Call the custom error function with error handling",
        "instruction": "Inside the validateInput function, add a try-catch block that calls validateInput with an invalid input and logs any caught errors.",
        "hint": "Wrap the function call in a try block and use console.error to log the error in the catch block.",
        "analogousExample": "// Example of calling a function with error handling\nfunction riskyOperation() {\n  try {\n    someFunctionThatMightThrow();\n  } catch (error) {\n    console.error('Caught error:', error.message);\n  }\n}",
        "seedCode": "// Import the fs module here\nconst fs = require('fs');\n\n// Define the readFileSync function here\nfunction readFileSyncExample() {\n  // Wrap fs.readFileSync in try-catch here\n  try {\n    // Add success log here\n  } catch (error) {\n    // Add error log here\n  }\n}\n\n// Define the async function here\nasync function readFileAsync() {\n  // Add try-catch with await here\n  try {\n    // Add success log here\n  } catch (error) {\n    // Add error log here\n  }\n}\n\n// Define the validateInput function here\nfunction validateInput() {\n  // Add try-catch block here\n}",
        "expectedOutcome": "The validateInput function will now include error handling logic that demonstrates catching and logging custom errors.",
        "successCriteria": [
          "validateInput contains a try-catch block that calls itself with invalid input and logs the caught error."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you have both try and catch blocks with proper error logging.",
        "feedbackWrong": "The validateInput function should contain a try-catch block that calls itself and logs errors.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "try",
            "catch",
            "validateInput",
            "console.error"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Create the main function to demonstrate all error handling",
        "instruction": "Create a function named main that calls readFileSyncExample, readFileAsync, and validateInput with proper error handling for each.",
        "hint": "Define a function called main that uses try-catch blocks to call each of the three functions you've created, logging results appropriately.",
        "analogousExample": "// Example of a main function calling multiple functions\nfunction main() {\n  try {\n    functionA();\n  } catch (error) {\n    console.log('Error in functionA:', error.message);\n  }\n  \n  try {\n    functionB();\n  } catch (error) {\n    console.log('Error in functionB:', error.message);\n  }\n}",
        "seedCode": "// Import the fs module here\nconst fs = require('fs');\n\n// Define the readFileSync function here\nfunction readFileSyncExample() {\n  // Wrap fs.readFileSync in try-catch here\n  try {\n    // Add success log here\n  } catch (error) {\n    // Add error log here\n  }\n}\n\n// Define the async function here\nasync function readFileAsync() {\n  // Add try-catch with await here\n  try {\n    // Add success log here\n  } catch (error) {\n    // Add error log here\n  }\n}\n\n// Define the validateInput function here\nfunction validateInput() {\n  // Add try-catch block here\n}\n\n// Create the main function here",
        "expectedOutcome": "A main function that demonstrates error handling by calling all three functions with proper try-catch blocks.",
        "successCriteria": [
          "The main function is defined and calls readFileSyncExample, readFileAsync, and validateInput with error handling for each."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your main function calls all three functions with proper error handling.",
        "feedbackWrong": "The main function should be defined and call readFileSyncExample, readFileAsync, and validateInput with error handling.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "function main",
            "readFileSyncExample",
            "readFileAsync",
            "validateInput",
            "try",
            "catch"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import required Node.js modules",
        "id": "step1"
      },
      {
        "label": "Create a synchronous function that reads a file",
        "id": "step2"
      },
      {
        "label": "Add try-catch block to handle synchronous errors",
        "id": "step3"
      },
      {
        "label": "Log success and error messages in the try-catch",
        "id": "step4"
      },
      {
        "label": "Create an asynchronous function that reads a file",
        "id": "step5"
      },
      {
        "label": "Use try-catch with await for async error handling",
        "id": "step6"
      },
      {
        "label": "Log results from the async function",
        "id": "step7"
      },
      {
        "label": "Create a function that throws a custom error",
        "id": "step8"
      },
      {
        "label": "Call the custom error function with error handling",
        "id": "step9"
      },
      {
        "label": "Create the main function to demonstrate all error handling",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}