{
  "track": "react-ts",
  "lessons": [
    {
      "lesson": 1,
      "file": "inpact_ts01_engine.jsx",
      "title": "Counter App — Typed",
      "objectives": [
        "Establish a typed component contract so the component can be used correctly and invalid props are caught at compile time.",
        "Model the counter value as typed state so updates stay numeric and invalid assignments fail before runtime.",
        "Wire button interactions through typed handlers so clicks produce the intended update path without loose function signatures.",
        "Pass typed props across the boundary, deliberately observe a type mismatch, and fix it so the learner sees the compiler as a design guardrail."
      ]
    },
    {
      "lesson": 2,
      "file": "inpact_ts02_engine.jsx",
      "title": "Toggle Visibility — Typed",
      "objectives": [
        "Model a boolean state variable using useState<boolean> so state, refs, or values keep a predictable shape as the feature evolves, and data shape and updates remain explicit throughout the lesson.",
        "Type a toggle handler as () => void and bind it to an onClick prop so invalid interactions are rejected by the compiler instead of leaking into runtime.",
        "Use a boolean state variable to conditionally return JSX via a ternary expression so the pattern drives visible behavior, and the rendered branch clearly follows the current state.",
        "Establish a VisibilityProps interface and type the component as `(props: VisibilityProps) => JSX.Element` (or destructure props) with an explicit JSX.Element return — avoid React.FC in new code."
      ]
    },
    {
      "lesson": 3,
      "file": "inpact_ts03_engine.jsx",
      "title": "Controlled Input — Typed",
      "objectives": [
        "Model a string state variable using useState<string> to hold input value so state, refs, or values keep a predictable shape as the feature evolves, and data shape and updates remain explicit throughout the lesson.",
        "Type a change handler as React.ChangeEventHandler<HTMLInputElement> so invalid interactions are rejected by the compiler instead of leaking into runtime.",
        "Bind state to an input element via value and onChange to create a controlled component so the UI stays connected to the intended source of truth.",
        "Display the live state value in JSX to demonstrate two-way binding so the user-facing result makes the underlying state or validation visible and verify that the screen always mirrors the latest React state."
      ]
    },
    {
      "lesson": 4,
      "file": "inpact_ts04_engine.jsx",
      "title": "Multiple State Variables — Typed",
      "objectives": [
        "Model multiple independent state variables each with an explicit type parameter so state, refs, or values keep a predictable shape as the feature evolves.",
        "Type each setter function implicitly from useState and use it in a dedicated handler so invalid interactions are rejected by the compiler, and data shape and updates remain explicit throughout the lesson.",
        "Group related state fields into a typed object interface and manage them with a single useState call so related data can be updated cohesively without weakening the type contract so data shape and updates remain explicit throughout the lesson.",
        "Apply partial<T> to update only changed fields within a single setState call so the learner sees how a typed pattern changes behavior, reuse, or safety in practice."
      ]
    },
    {
      "lesson": 5,
      "file": "inpact_ts05_engine.jsx",
      "title": "Conditional Rendering with Ternary — Typed",
      "objectives": [
        "Use a boolean state variable to drive a ternary expression that renders one of two typed components so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Apply the && short-circuit operator to conditionally render a JSX element so the learner sees how a typed pattern changes behavior, reuse, or safety in practice so the UI exposes only the branch that matches the current state.",
        "Type a conditional render helper function with return type JSX.Element | null so invalid interactions are rejected by the compiler instead of leaking into runtime.",
        "Pass a boolean prop to a child component and use it to switch between two render paths so data crossing component boundaries stays explicit, safe, and easy to trace."
      ]
    },
    {
      "lesson": 6,
      "file": "inpact_ts06_engine.jsx",
      "title": "List Rendering with map() — Typed",
      "objectives": [
        "Establish a TypeScript interface for a list item object (e.g. id, label, value) so the contract is explicit and incorrect usage is caught early.",
        "Model a typed array state variable using useState<Item[]> so state, refs, or values keep a predictable shape as the feature evolves, and data shape and updates remain explicit throughout the lesson.",
        "Use the array .map() method to iterate over typed objects and return a JSX element per item so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Supply a key prop of type string | number to each mapped element to satisfy React's reconciler so each consumer receives exactly the data or behavior it depends on."
      ]
    },
    {
      "lesson": 7,
      "file": "inpact_ts07_engine.jsx",
      "title": "useEffect & Side Effects — Typed",
      "objectives": [
        "Call useEffect with a typed async function to run a side effect after render so the hook or API is used with an intentional contract, and side effects stay predictable across renders and teardown.",
        "Model a dependency array and explain the type contract between deps and the effect body so state, refs, or values keep a predictable shape as the feature evolves.",
        "Return a cleanup function typed as () => void from useEffect to cancel a subscription or timer so consumers receive a stable contract and the effect of that contract can be tested immediately so the effect leaves no stale subscriptions, timers, or listeners behind.",
        "Type the result of a fetch call using a generic type parameter to avoid implicit any so invalid interactions are rejected by the compiler, and async data is handled with an explicit and safe data shape."
      ]
    },
    {
      "lesson": 8,
      "file": "inpact_ts08_engine.jsx",
      "title": "Forms & Validation — Typed",
      "objectives": [
        "Establish a FormState interface and manage all form fields with a single useState<FormState> so the contract is explicit, and data shape and updates remain explicit throughout the lesson.",
        "Type a form submit handler as React.FormEventHandler<HTMLFormElement> and call e.preventDefault() so invalid interactions are rejected by the compiler instead of leaking into runtime.",
        "Model a typed errors object using Partial<Record<keyof FormState, string>> and populate it in a validate function so state, refs, or values keep a predictable shape as the feature evolves.",
        "Display per-field error messages conditionally using the && operator against the typed errors object so the user-facing result makes the underlying state or validation visible."
      ]
    },
    {
      "lesson": 9,
      "file": "inpact_ts09_engine.jsx",
      "title": "Color Picker — Typed",
      "objectives": [
        "Model a string state variable to hold a CSS hex color value using useState<string> so state, refs, or values keep a predictable shape as the feature evolves, and data shape and updates remain explicit throughout the lesson.",
        "Type a change handler for an <input type='color'> as React.ChangeEventHandler<HTMLInputElement> so invalid interactions are rejected by the compiler instead of leaking into runtime.",
        "Apply the selected color as an inline style using React.CSSProperties typed object so the learner sees how a typed pattern changes behavior, reuse, or safety in practice.",
        "Establish a ColorPickerProps interface with an onColorChange callback typed as (color: string) => void so the contract is explicit and incorrect usage is caught early."
      ]
    },
    {
      "lesson": 10,
      "file": "inpact_ts10_engine.jsx",
      "title": "Reusable Button — Typed",
      "objectives": [
        "Establish a ButtonProps interface that extends React.ButtonHTMLAttributes<HTMLButtonElement> so the contract is explicit and incorrect usage is caught early.",
        "Use the variant prop typed as a string literal union ('primary' | 'secondary' | 'danger') to apply conditional classes so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Forward the children prop typed as React.ReactNode to render arbitrary button content so the wrapper preserves the capability while keeping ownership clear.",
        "Spread the rest of the HTML button attributes onto the <button> element using the rest operator so reusable wrappers preserve native behavior without losing type clarity."
      ]
    },
    {
      "lesson": 11,
      "file": "inpact_ts11_engine.jsx",
      "title": "Card Component — Typed",
      "objectives": [
        "Establish a CardProps interface with required and optional fields and type the component with `({ ... }: CardProps): JSX.Element` — avoid React.FC in new code.",
        "Use the children prop typed as React.ReactNode to compose arbitrary content inside the card so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Apply conditional rendering using the && operator to show optional sections only when props are defined so the learner sees how a typed pattern changes behavior, reuse, or safety in practice.",
        "Use the ?? nullish coalescing operator to supply fallback values for optional string props so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler."
      ]
    },
    {
      "lesson": 12,
      "file": "inpact_ts12_engine.jsx",
      "title": "Props Drilling — Typed",
      "objectives": [
        "Establish a shared TypeScript interface and import it across parent and child component files so the contract is explicit and incorrect usage is caught early.",
        "Pass typed props through two or more component layers without losing type safety at any level so data crossing component boundaries stays explicit, safe, and easy to trace.",
        "Destructure typed props in a child component signature and confirm no implicit any warnings so the returned contract is readable and its parts are used intentionally.",
        "Identify the compile error produced when a required prop is omitted and fix it at the call site so the learner can spot the failure mode before applying the fix."
      ]
    },
    {
      "lesson": 13,
      "file": "inpact_ts13_engine.jsx",
      "title": "Default Props — Typed",
      "objectives": [
        "Model optional props using the ? modifier in a TypeScript interface so state, refs, or values keep a predictable shape as the feature evolves.",
        "Apply default values to optional props using ES6 destructuring defaults in the function signature so the learner sees how a typed pattern changes behavior, reuse, or safety in practice.",
        "Demonstrate that omitting an optional prop at the call site does not produce a TypeScript error so the learner can observe the consequence and attach a mental model to it.",
        "Type a defaultProps-equivalent pattern using Partial<T> and object spread so invalid interactions are rejected by the compiler instead of leaking into runtime."
      ]
    },
    {
      "lesson": 14,
      "file": "inpact_ts14_engine.jsx",
      "title": "Children Prop — Typed",
      "objectives": [
        "Model the children prop explicitly typed as React.ReactNode in a custom interface so state, refs, or values keep a predictable shape as the feature evolves.",
        "Use react.PropsWithChildren<T> utility type to compose children with custom props so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Render the children prop inside a wrapper component to implement a layout slot pattern so the learner can connect the typed data shape to the final UI output.",
        "Type children with React.PropsWithChildren<CustomProps> (or explicit `children: ReactNode`) and an explicit JSX.Element return — avoid React.FC in new code."
      ]
    },
    {
      "lesson": 15,
      "file": "inpact_ts15_engine.jsx",
      "title": "TypeScript Interface for Props — Typed",
      "objectives": [
        "Establish a TypeScript interface to describe a component's prop contract and apply it with an explicit JSX.Element return type on the function — avoid React.FC in new code.",
        "Use a string literal union type to constrain a prop to a finite set of allowed values so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Extend one interface from another using the extends keyword to share common prop fields so reuse does not weaken the contract or blur responsibility.",
        "Model an index signature in an interface to type a props object with dynamic keys so state, refs, or values keep a predictable shape as the feature evolves."
      ]
    },
    {
      "lesson": 16,
      "file": "inpact_ts16_engine.jsx",
      "title": "Component Composition — Typed",
      "objectives": [
        "Build a compound UI from multiple typed child components composed inside a parent so multiple typed pieces work together as one coherent UI pattern.",
        "Pass typed render functions as props typed as (data: T) => JSX.Element for flexible slots so data crossing component boundaries stays explicit, safe, and easy to trace.",
        "Use react.ReactElement as a return type on a composition wrapper component so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Apply the children-as-function pattern typed as (ctx: ContextType) => React.ReactNode so the learner sees how a typed pattern changes behavior, reuse, or safety in practice so shared data can move without losing type safety across components."
      ]
    },
    {
      "lesson": 17,
      "file": "inpact_ts17_engine.jsx",
      "title": "Event Handling — Typed",
      "objectives": [
        "Type mouse event handlers using React.MouseEvent<HTMLButtonElement> so invalid interactions are rejected by the compiler instead of leaking into runtime.",
        "Type keyboard event handlers using React.KeyboardEvent<HTMLInputElement> and read e.key so invalid interactions are rejected by the compiler instead of leaking into runtime.",
        "Type focus and blur handlers using React.FocusEvent<HTMLInputElement> so invalid interactions are rejected by the compiler instead of leaking into runtime.",
        "Model an inline event handler directly on a JSX element with an explicit type annotation so state, refs, or values keep a predictable shape as the feature evolves."
      ]
    },
    {
      "lesson": 18,
      "file": "inpact_ts18_engine.jsx",
      "title": "Conditional Classes — Typed",
      "objectives": [
        "Build a className string dynamically using a ternary expression based on a boolean state variable so multiple typed pieces work together as one coherent UI pattern.",
        "Establish a string literal union prop for variant and map it to a class name using an object literal lookup typed as Record<Variant, string> so the contract is explicit and incorrect usage is caught early.",
        "Use the clsx or classnames library with TypeScript to compose class names from conditional expressions so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Extract a typed getClassName helper function with return type string that encapsulates class logic so repeated logic becomes reusable without obscuring the contract."
      ]
    },
    {
      "lesson": 19,
      "file": "inpact_ts19_engine.jsx",
      "title": "Inline Styles — Typed",
      "objectives": [
        "Model a style object typed as React.CSSProperties and apply it to a JSX element's style prop so state, refs, or values keep a predictable shape as the feature evolves.",
        "Build a dynamic style object by merging a base React.CSSProperties object with state-derived values so multiple typed pieces work together as one coherent UI pattern.",
        "Use a CSS custom property (CSS variable) via React.CSSProperties to pass runtime values to CSS so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Type a component prop as React.CSSProperties and merge it with internal styles using object spread so invalid interactions are rejected by the compiler instead of leaking into runtime."
      ]
    },
    {
      "lesson": 20,
      "file": "inpact_ts20_engine.jsx",
      "title": "CSS Modules — Typed",
      "objectives": [
        "Import a CSS Module file and type the resulting styles object using the implicit module declaration so the learner can explain both the mechanism and the visible result.",
        "Apply a CSS Module class to a JSX element using styles.className and verify no string typo errors so the learner sees how a typed pattern changes behavior, reuse, or safety in practice.",
        "Compose multiple CSS Module classes using a template literal or clsx with typed keys so the learner sees how smaller typed pieces form a larger UI capability.",
        "Model a custom .d.ts module declaration for a CSS Module file to enable named class exports so state, refs, or values keep a predictable shape as the feature evolves."
      ]
    },
    {
      "lesson": 21,
      "file": "inpact_ts21_engine.jsx",
      "title": "Styled Component Pattern — Typed",
      "objectives": [
        "Create a styled component using a tagged template literal and type its props with a generic parameter so the design pattern is reusable and its contract is clear to downstream code.",
        "Extend an existing styled component and override specific styles while inheriting its type so reuse does not weaken the contract or blur responsibility.",
        "Pass a typed prop to a styled component's template literal to compute a dynamic CSS value so data crossing component boundaries stays explicit, safe, and easy to trace.",
        "Use the css helper from styled-components with TypeScript to extract reusable typed style blocks so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler."
      ]
    },
    {
      "lesson": 22,
      "file": "inpact_ts22_engine.jsx",
      "title": "Lifting State Up — Typed",
      "objectives": [
        "Move a useState declaration from a child component to the nearest common parent so the learner can explain both the mechanism and the visible result, and data shape and updates remain explicit throughout the lesson.",
        "Pass the state value and its setter (typed as React.Dispatch<React.SetStateAction<T>>) as props to child components so data crossing component boundaries stays explicit, safe, and easy to trace.",
        "Establish a callback prop typed as (value: T) => void to allow a child to communicate changes to its parent so the contract is explicit and incorrect usage is caught early.",
        "Demonstrate that removing local state from a child and receiving it as a prop eliminates duplicate state so the learner can observe the consequence and attach a mental model to it."
      ]
    },
    {
      "lesson": 23,
      "file": "inpact_ts23_engine.jsx",
      "title": "Controlled vs Uncontrolled — Typed",
      "objectives": [
        "Implement a controlled input by binding value to a useState<string> variable and onChange to a typed handler so the feature works end to end, and data shape and updates remain explicit throughout the lesson.",
        "Implement an uncontrolled input using useRef<HTMLInputElement>(null) and reading .current.value on submit so the feature works end to end and the resulting behavior can be observed and explained.",
        "Type a ref as React.RefObject<HTMLInputElement> and access its .current property safely with a null check so invalid interactions are rejected by the compiler instead of leaking into runtime.",
        "Demonstrate the compiler difference between a typed controlled handler and a ref read at point-of-use so the learner can observe the consequence and attach a mental model to it."
      ]
    },
    {
      "lesson": 24,
      "file": "inpact_ts24_engine.jsx",
      "title": "Simple Todo List — Typed",
      "objectives": [
        "Establish a Todo interface with id, text, and completed fields and declare useState<Todo[]> so the contract is explicit, and data shape and updates remain explicit throughout the lesson.",
        "Implement an addTodo function that uses the array spread operator to append a new typed object to state so the feature works end to end and the resulting behavior can be observed and explained.",
        "Implement a toggleTodo function that uses .map() to return a new array with one item's completed field flipped so the feature works end to end and the resulting behavior can be observed and explained.",
        "Implement a deleteTodo function that uses .filter() to return a new array excluding the item matching a given id so the feature works end to end and the resulting behavior can be observed and explained."
      ]
    },
    {
      "lesson": 25,
      "file": "inpact_ts25_engine.jsx",
      "title": "Star Rating Component — Typed",
      "objectives": [
        "Establish a StarRatingProps interface with rating, maxStars, and an onRate callback typed as (n: number) => void so the contract is explicit and incorrect usage is caught early.",
        "Use array.from({ length: maxStars }) with .map() to generate an array of typed star elements so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Model a hover state variable using useState<number | null> to drive visual feedback so state, refs, or values keep a predictable shape as the feature evolves, and data shape and updates remain explicit throughout the lesson.",
        "Type mouse event handlers as React.MouseEventHandler<HTMLSpanElement> to update hover and rating state so invalid interactions are rejected by the compiler instead of leaking into runtime."
      ]
    },
    {
      "lesson": 26,
      "file": "inpact_ts26_engine.jsx",
      "title": "Accordion — Typed",
      "objectives": [
        "Establish an AccordionItem interface with id, title, and content fields and declare useState<string | null> for the open item so the contract is explicit, and data shape and updates remain explicit throughout the lesson.",
        "Use .map() over a typed AccordionItem[] array to render a list of panel elements so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Implement a toggle handler typed as (id: string) => void that sets state to null when the active panel is re-clicked so the feature works end to end and the resulting behavior can be observed and explained.",
        "Pass an isOpen boolean prop to each panel child component and use it to conditionally render content so data crossing component boundaries stays explicit, safe, and easy to trace so the UI exposes only the branch that matches the current state."
      ]
    },
    {
      "lesson": 27,
      "file": "inpact_ts27_engine.jsx",
      "title": "Image Gallery — Typed",
      "objectives": [
        "Establish an Image interface and declare useState<Image[]> to hold the gallery data so the contract is explicit, and data shape and updates remain explicit throughout the lesson.",
        "Use .map() over typed Image objects to render a grid of <img> elements with typed src and alt props so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Model a useState<Image | null> variable to track the selected image for a lightbox view so state, refs, or values keep a predictable shape as the feature evolves, and data shape and updates remain explicit throughout the lesson.",
        "Type the onClick handler on each gallery item as React.MouseEventHandler<HTMLImageElement> so invalid interactions are rejected by the compiler instead of leaking into runtime."
      ]
    },
    {
      "lesson": 28,
      "file": "inpact_ts28_engine.jsx",
      "title": "useFetch — Typed",
      "objectives": [
        "Write a custom hook useFetch<T>(url: string) with a generic type parameter T for the response data so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Model typed state variables for data (T | null), loading (boolean), and error (Error | null) so state, refs, or values keep a predictable shape as the feature evolves.",
        "Call fetch inside useEffect, parse the JSON response, and cast it to T using the generic parameter so the hook or API is used with an intentional contract, and async data is handled with an explicit and safe data shape.",
        "Return a typed object { data, loading, error } from the custom hook and consume it in a component so consumers receive a stable contract and the effect of that contract can be tested immediately."
      ]
    },
    {
      "lesson": 29,
      "file": "inpact_ts29_engine.jsx",
      "title": "useDebounce — Typed",
      "objectives": [
        "Write a custom hook useDebounce<T>(value: T, delay: number): T with explicit input and return types so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Model a useState<T> variable for the debounced value initialized to the input value so state, refs, or values keep a predictable shape as the feature evolves, and data shape and updates remain explicit throughout the lesson.",
        "Use useEffect with a setTimeout to update the debounced state and return a clearTimeout cleanup so the pattern drives visible behavior, and the effect leaves no stale subscriptions, timers, or listeners behind.",
        "Consume useDebounce in a search component and demonstrate that the API call fires only after the delay so the learner can connect the typed API to real screen behavior."
      ]
    },
    {
      "lesson": 30,
      "file": "inpact_ts30_engine.jsx",
      "title": "useLocalStorage — Typed",
      "objectives": [
        "Write a custom hook useLocalStorage<T>(key: string, initialValue: T): [T, (val: T) => void] so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Read from localStorage in the useState initializer function and parse the JSON with a try/catch so the learner can explain what runtime information is driving the next decision so data shape and updates remain explicit throughout the lesson.",
        "Write a typed setter that serializes the new value with JSON.stringify before calling localStorage.setItem so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Return the typed [value, setValue] tuple and consume it as a drop-in replacement for useState so consumers receive a stable contract and the effect of that contract can be tested immediately so data shape and updates remain explicit throughout the lesson."
      ]
    },
    {
      "lesson": 31,
      "file": "inpact_ts31_engine.jsx",
      "title": "useToggle — Typed",
      "objectives": [
        "Write a custom hook useToggle(initialValue: boolean): [boolean, () => void] with explicit return type so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Implement the toggle function using the functional form of useState setter: setVal(v => !v) so the feature works end to end, and data shape and updates remain explicit throughout the lesson.",
        "Return a tuple typed as [boolean, () => void] and destructure it at the call site so consumers receive a stable contract and the effect of that contract can be tested immediately.",
        "Extend the hook to accept an optional forceValue: boolean parameter typed in the setter overload so reuse does not weaken the contract or blur responsibility."
      ]
    },
    {
      "lesson": 32,
      "file": "inpact_ts32_engine.jsx",
      "title": "useWindowSize — Typed",
      "objectives": [
        "Establish a WindowSize interface with width and height fields both typed as number so the contract is explicit and incorrect usage is caught early.",
        "Write a custom hook useWindowSize(): WindowSize that initializes state from window.innerWidth and window.innerHeight so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Attach a resize event listener in useEffect and return a cleanup that calls removeEventListener so the event or DOM boundary behaves correctly and can be verified during interaction so the effect leaves no stale subscriptions, timers, or listeners behind.",
        "Consume the typed WindowSize object in a component to conditionally render based on breakpoints so the learner can connect the typed API to real screen behavior, and the UI exposes only the branch that matches the current state."
      ]
    },
    {
      "lesson": 33,
      "file": "inpact_ts33_engine.jsx",
      "title": "usePrevious — Typed",
      "objectives": [
        "Write a custom hook usePrevious<T>(value: T): T | undefined with a generic type parameter so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Model a useRef<T | undefined>(undefined) to store the previous value without triggering re-renders so state, refs, or values keep a predictable shape as the feature evolves.",
        "Update the ref inside useEffect after every render so it always holds the prior value so the learner can explain both the mechanism and the visible result, and side effects stay predictable across renders and teardown.",
        "Consume usePrevious in a component and display both current and previous values to verify behavior so the learner can connect the typed API to real screen behavior."
      ]
    },
    {
      "lesson": 34,
      "file": "inpact_ts34_engine.jsx",
      "title": "useClickOutside — Typed",
      "objectives": [
        "Write a custom hook useClickOutside<T extends HTMLElement>(callback: () => void): React.RefObject<T> so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Model a useRef<T>(null) and attach it to a DOM element to obtain a typed element reference so state, refs, or values keep a predictable shape as the feature evolves.",
        "Attach a mousedown event listener to document in useEffect and check if ref.current.contains(e.target as Node) so the event or DOM boundary behaves correctly and can be verified during interaction so side effects stay predictable across renders and teardown.",
        "Return the typed ref from the hook and attach it to a JSX element to close a dropdown on outside click so consumers receive a stable contract and the effect of that contract can be tested immediately."
      ]
    },
    {
      "lesson": 35,
      "file": "inpact_ts35_engine.jsx",
      "title": "useKeyPress — Typed",
      "objectives": [
        "Write a custom hook useKeyPress(targetKey: string): boolean with explicit return type so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Model a useState<boolean> to track whether the target key is currently pressed so state, refs, or values keep a predictable shape as the feature evolves, and data shape and updates remain explicit throughout the lesson.",
        "Attach keydown and keyup event listeners in useEffect typed as (e: KeyboardEvent) => void so the event or DOM boundary behaves correctly and can be verified during interaction so side effects stay predictable across renders and teardown.",
        "Return the boolean keyPressed value and use it in a component to trigger a UI state change so consumers receive a stable contract and the effect of that contract can be tested immediately."
      ]
    },
    {
      "lesson": 36,
      "file": "inpact_ts36_engine.jsx",
      "title": "useOnlineStatus — Typed",
      "objectives": [
        "Write a custom hook useOnlineStatus(): boolean that returns navigator.onLine as initial state so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Attach online and offline event listeners to window in useEffect and update boolean state in each handler so the event or DOM boundary behaves correctly and can be verified during interaction so side effects stay predictable across renders and teardown.",
        "Return a cleanup function from useEffect that removes both event listeners so consumers receive a stable contract and the effect of that contract can be tested immediately so the effect leaves no stale subscriptions, timers, or listeners behind.",
        "Consume the boolean return value in a component to render a typed status indicator so the learner can connect the typed API to real screen behavior."
      ]
    },
    {
      "lesson": 37,
      "file": "inpact_ts37_engine.jsx",
      "title": "useMediaQuery — Typed",
      "objectives": [
        "Write a custom hook useMediaQuery(query: string): boolean using window.matchMedia so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Initialize useState<boolean> from matchMedia(query).matches so the learner can explain both the mechanism and the visible result, and data shape and updates remain explicit throughout the lesson.",
        "Attach a change event listener on a MediaQueryList object typed as MediaQueryListEvent in useEffect so the event or DOM boundary behaves correctly and can be verified during interaction so side effects stay predictable across renders and teardown.",
        "Return the boolean match value and use it in a component to conditionally render a typed layout so consumers receive a stable contract and the effect of that contract can be tested immediately so the UI exposes only the branch that matches the current state."
      ]
    },
    {
      "lesson": 38,
      "file": "inpact_ts38_engine.jsx",
      "title": "Theme Context — Typed",
      "objectives": [
        "Establish a ThemeContextType interface and create a typed context with createContext<ThemeContextType | undefined>(undefined) so the contract is explicit, and shared data can move without losing type safety across components.",
        "Write a ThemeProvider component that wraps children with Context.Provider and supplies a typed value so the abstraction solves a concrete UI lesson and exposes a reliable typed contract so shared data can move without losing type safety across components.",
        "Write a useTheme custom hook that calls useContext, checks for undefined, and returns the typed context value so the abstraction solves a concrete UI lesson and exposes a reliable typed contract so shared data can move without losing type safety across components.",
        "Consume useTheme in a child component and apply the theme token to a React.CSSProperties object so the learner can connect the typed API to real screen behavior."
      ]
    },
    {
      "lesson": 39,
      "file": "inpact_ts39_engine.jsx",
      "title": "Auth Context — Typed",
      "objectives": [
        "Establish an AuthContextType interface with user (User | null), login, and logout typed as functions so the contract is explicit, and shared data can move without losing type safety across components.",
        "Create a typed context with createContext<AuthContextType | undefined>(undefined) so the design pattern is reusable, and shared data can move without losing type safety across components.",
        "Implement a useAuth hook that reads the context with useContext and throws if used outside the provider so the feature works end to end, and shared data can move without losing type safety across components.",
        "Consume the typed user object from useAuth in a component to conditionally render authenticated UI so the learner can connect the typed API to real screen behavior, and the UI exposes only the branch that matches the current state."
      ]
    },
    {
      "lesson": 40,
      "file": "inpact_ts40_engine.jsx",
      "title": "Cart Context — Typed",
      "objectives": [
        "Establish cartItem and CartContextType interfaces and create a typed context with createContext so the contract is explicit, and shared data can move without losing type safety across components.",
        "Implement addToCart and removeFromCart functions typed as (item: CartItem) => void in the provider so the feature works end to end and the resulting behavior can be observed and explained.",
        "Use useReducer<CartState, CartAction> inside the provider to manage cart state with typed actions so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Consume the typed CartContext in a product component to add items and in a cart component to list them so the learner can connect the typed API to real screen behavior, and shared data can move without losing type safety across components."
      ]
    },
    {
      "lesson": 41,
      "file": "inpact_ts41_engine.jsx",
      "title": "Notification Context — Typed",
      "objectives": [
        "Establish a Notification interface and a string literal union type for notification kind ('info' | 'success' | 'error') so the contract is explicit and incorrect usage is caught early.",
        "Create a typed NotificationContext and implement addNotification typed as (n: Omit<Notification, 'id'>) => void so the design pattern is reusable, and shared data can move without losing type safety across components.",
        "Use .filter() in a removeNotification function typed as (id: string) => void to update notification state so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Consume the context in a component to dispatch a typed notification and render the list with .map() so the learner can connect the typed API to real screen behavior."
      ]
    },
    {
      "lesson": 42,
      "file": "inpact_ts42_engine.jsx",
      "title": "Context Performance — Typed",
      "objectives": [
        "Split a single context into two typed contexts — one for state and one for dispatch — to limit re-render scope so the learner can explain both the mechanism and the visible result.",
        "Wrap the context value object in useMemo with a typed return to prevent unnecessary consumer re-renders so the boundary is explicit and the fallback or optimization can be verified at runtime.",
        "Wrap dispatch callbacks in useCallback with explicit return type () => void to stabilize references so the boundary is explicit and the fallback or optimization can be verified at runtime.",
        "Use react.memo on a typed consumer component and verify it skips re-render when irrelevant context changes so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler and verify whether stable props actually reduce avoidable re-renders."
      ]
    },
    {
      "lesson": 43,
      "file": "inpact_ts43_engine.jsx",
      "title": "useReducer vs useState — Typed",
      "objectives": [
        "Establish a State interface and a discriminated union type for Action with a type field so the contract is explicit and incorrect usage is caught early.",
        "Implement a typed reducer function with signature (state: State, action: Action): State so the feature works end to end and the resulting behavior can be observed and explained.",
        "Call useReducer<State, Action>(reducer, initialState) and destructure the typed [state, dispatch] tuple so the hook or API is used with an intentional contract and observable outcome.",
        "Dispatch typed action objects from event handlers and demonstrate that the compiler rejects unknown action types so state transitions remain explicit, traceable, and type-safe."
      ]
    },
    {
      "lesson": 44,
      "file": "inpact_ts44_engine.jsx",
      "title": "Compound Component (Tabs) — Typed",
      "objectives": [
        "Create a parent Tabs component that holds activeTab state and exposes it via a typed context so the design pattern is reusable and its contract is clear to downstream code.",
        "Build tab and TabPanel child components that consume the typed context to determine visibility so multiple typed pieces work together as one coherent UI pattern.",
        "Attach child components as static properties on the parent (Tabs.Tab, Tabs.Panel) with typed signatures so the event or DOM boundary behaves correctly and can be verified during interaction.",
        "Compose the compound component at the call site using only JSX nesting with no explicit prop threading so the learner sees how smaller typed pieces form a larger UI capability."
      ]
    },
    {
      "lesson": 45,
      "file": "inpact_ts45_engine.jsx",
      "title": "Unnecessary Re-renders — Typed",
      "objectives": [
        "Wrap a typed child component with React.memo to prevent re-renders when parent state changes but its props have not so the boundary is explicit and the fallback or optimization can be verified at runtime and verify whether stable props actually reduce avoidable re-renders.",
        "Stabilize a callback prop using useCallback with a typed signature to avoid breaking React.memo so the learner can explain both the mechanism and the visible result and verify whether stable props actually reduce avoidable re-renders.",
        "Use the React DevTools Profiler to identify which typed components re-render and record the cause so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler and validate the behavior with runtime evidence, not guesswork.",
        "Lift an unchanged state variable out of a frequently-updating parent to eliminate child re-renders so the learner can explain both the mechanism and the visible result."
      ]
    },
    {
      "lesson": 46,
      "file": "inpact_ts46_engine.jsx",
      "title": "useMemo for Expensive Computation — Typed",
      "objectives": [
        "Wrap an expensive computation in useMemo with an explicit return type and a typed dependency array so the boundary is explicit and the fallback or optimization can be verified at runtime.",
        "Demonstrate that without useMemo the computation re-runs on every render by logging inside the function so the learner can observe the consequence and attach a mental model to it.",
        "Model the dependency array so that the memoized value recomputes only when its typed input changes so state, refs, or values keep a predictable shape as the feature evolves.",
        "Distinguish the return type of useMemo<T> from the return type of useCallback to avoid misuse so the learner can choose the right abstraction instead of mixing similar APIs."
      ]
    },
    {
      "lesson": 47,
      "file": "inpact_ts47_engine.jsx",
      "title": "useCallback for Stable References — Typed",
      "objectives": [
        "Wrap an event handler in useCallback with a typed signature (...args) => returnType and a dependency array so the boundary is explicit and the fallback or optimization can be verified at runtime.",
        "Pass the memoized callback as a prop to a React.memo child and verify the child no longer re-renders on parent state change so data crossing component boundaries stays explicit, safe, and easy to trace and verify whether stable props actually reduce avoidable re-renders.",
        "Model the correct dependencies in the useCallback array so the function closes over fresh values so state, refs, or values keep a predictable shape as the feature evolves.",
        "Demonstrate the referential equality check that React.memo performs on the callback prop between renders so the learner can observe the consequence and attach a mental model to it and verify whether stable props actually reduce avoidable re-renders."
      ]
    },
    {
      "lesson": 48,
      "file": "inpact_ts48_engine.jsx",
      "title": "React.memo — Typed",
      "objectives": [
        "Wrap a typed functional component with React.memo and explain its shallow prop comparison behavior so the boundary is explicit and the fallback or optimization can be verified at runtime and verify whether stable props actually reduce avoidable re-renders.",
        "Supply a custom comparison function typed as (prev: Props, next: Props) => boolean to React.memo so each consumer receives exactly the data or behavior it depends on and verify whether stable props actually reduce avoidable re-renders.",
        "Demonstrate that React.memo does not prevent re-renders caused by context changes inside the component so the learner can observe the consequence and attach a mental model to it and verify whether stable props actually reduce avoidable re-renders.",
        "Use a stable useCallback callback and a memoized object prop to make React.memo effective so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler and verify whether stable props actually reduce avoidable re-renders."
      ]
    },
    {
      "lesson": 49,
      "file": "inpact_ts49_engine.jsx",
      "title": "List Virtualization — Typed",
      "objectives": [
        "Install and import react-window and type the FixedSizeList props using its provided TypeScript definitions so the learner can explain both the mechanism and the visible result.",
        "Establish a typed Row component using ListChildComponentProps to render a single virtualized row so the contract is explicit and incorrect usage is caught early.",
        "Pass a typed itemData object through FixedSizeList's itemData prop and read it in the Row via data field so data crossing component boundaries stays explicit, safe, and easy to trace.",
        "Measure DOM node count before and after virtualization using browser DevTools to confirm the win so the learner can explain both the mechanism and the visible result and validate the behavior with runtime evidence, not guesswork."
      ]
    },
    {
      "lesson": 50,
      "file": "inpact_ts50_engine.jsx",
      "title": "Lazy Loading Routes — Typed",
      "objectives": [
        "Wrap a route component import in React.lazy(() => import('./Page')) to create a lazy-loaded module so the boundary is explicit and the fallback or optimization can be verified at runtime.",
        "Wrap lazy components in <React.Suspense fallback={...}> and type the fallback prop as React.ReactNode so the boundary is explicit and the fallback or optimization can be verified at runtime.",
        "Use react Router's <Route> element with a typed component prop to register the lazy component so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Verify code splitting by inspecting the Network tab for a separate chunk loaded on route navigation so the learner can explain both the mechanism and the visible result and confirm the runtime behavior instead of assuming the optimization worked."
      ]
    },
    {
      "lesson": 51,
      "file": "inpact_ts51_engine.jsx",
      "title": "Image Lazy Loading — Typed",
      "objectives": [
        "Create a typed LazyImage component that uses useRef<HTMLImageElement>(null) to observe a DOM node so the design pattern is reusable and its contract is clear to downstream code.",
        "Instantiate an IntersectionObserver in useEffect and type its callback as IntersectionObserverCallback so the generic or runtime object proves its value in a concrete case so side effects stay predictable across renders and teardown.",
        "Swap the src attribute from a placeholder to the real URL when isIntersecting becomes true so the learner can explain both the mechanism and the visible result.",
        "Return an observer.disconnect() cleanup from useEffect to avoid memory leaks so consumers receive a stable contract and the effect of that contract can be tested immediately so the effect leaves no stale subscriptions, timers, or listeners behind."
      ]
    },
    {
      "lesson": 52,
      "file": "inpact_ts52_engine.jsx",
      "title": "HOC withAuth — Typed",
      "objectives": [
        "Write a higher-order component withAuth<P>(Component: React.ComponentType<P>): React.ComponentType<P> with a generic type parameter so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Read authentication state from a typed context inside the HOC and redirect unauthenticated users so the learner can explain what runtime information is driving the next decision.",
        "Spread the original props typed as P onto the wrapped Component to pass through all existing props so reusable wrappers preserve native behavior without losing type clarity.",
        "Apply the HOC at the export site and confirm the wrapped component retains its full typed prop surface so the learner sees how a typed pattern changes behavior, reuse, or safety in practice."
      ]
    },
    {
      "lesson": 53,
      "file": "inpact_ts53_engine.jsx",
      "title": "Render Props (MouseTracker) — Typed",
      "objectives": [
        "Establish a Position interface with x and y fields and a MouseTrackerProps interface with a render prop typed as (pos: Position) => React.ReactNode so the contract is explicit and incorrect usage is caught early.",
        "Implement a MouseTracker component that tracks mousemove events and calls the render prop with a typed Position object so the feature works end to end and the resulting behavior can be observed and explained.",
        "Type the mousemove handler as React.MouseEventHandler<HTMLDivElement> to capture event coordinates so invalid interactions are rejected by the compiler instead of leaking into runtime.",
        "Consume the render prop at the call site by passing an arrow function that returns JSX using the typed position so the learner can connect the typed API to real screen behavior."
      ]
    },
    {
      "lesson": 54,
      "file": "inpact_ts54_engine.jsx",
      "title": "Controlled DatePicker — Typed",
      "objectives": [
        "Model a state variable using useState<Date | null>(null) to store the selected date so state, refs, or values keep a predictable shape as the feature evolves, and data shape and updates remain explicit throughout the lesson.",
        "Type the onChange prop of the date picker as (date: Date | null) => void so invalid interactions are rejected by the compiler instead of leaking into runtime.",
        "Format the Date object to a display string using toLocaleDateString with typed locale options so the learner can explain both the mechanism and the visible result.",
        "Establish a DatePickerProps interface with value, onChange, min, and max fields using appropriate types so the contract is explicit and incorrect usage is caught early."
      ]
    },
    {
      "lesson": 55,
      "file": "inpact_ts55_engine.jsx",
      "title": "Portal — Typed",
      "objectives": [
        "Use reactDOM.createPortal to render a typed JSX element into a DOM node outside the component tree so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Select the portal target using document.getElementById and narrow its type from Element | null to Element with a null check so the learner can explain both the mechanism and the visible result.",
        "Wrap portal rendering in a typed Modal component that accepts children as React.ReactNode and an isOpen boolean so the boundary is explicit and the fallback or optimization can be verified at runtime.",
        "Attach a keydown handler typed as (e: KeyboardEvent) => void to close the modal on Escape key press so the event or DOM boundary behaves correctly and can be verified during interaction."
      ]
    },
    {
      "lesson": 56,
      "file": "inpact_ts56_engine.jsx",
      "title": "Error Boundary — Typed",
      "objectives": [
        "Create a class-based ErrorBoundary component that implements the React.Component<Props, State> generic so the design pattern is reusable and its contract is clear to downstream code.",
        "Implement the static getDerivedStateFromError(error: Error): State lifecycle to set hasError state so the feature works end to end and the resulting behavior can be observed and explained.",
        "Implement the componentDidCatch(error: Error, info: React.ErrorInfo): void lifecycle to log error details so the feature works end to end and the resulting behavior can be observed and explained.",
        "Wrap a typed child subtree with ErrorBoundary and verify it catches thrown errors without crashing the app so the boundary is explicit and the fallback or optimization can be verified at runtime."
      ]
    },
    {
      "lesson": 57,
      "file": "inpact_ts57_engine.jsx",
      "title": "Recursive TreeView — Typed",
      "objectives": [
        "Establish a TreeNode interface with id, label, and an optional children: TreeNode[] recursive field so the contract is explicit and incorrect usage is caught early.",
        "Write a TreeNode component typed as `({ node }: { node: TreeNode }): JSX.Element` that calls itself for each child node so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Model a useState<Set<string>> to track expanded node IDs and update it in a typed toggle handler so state, refs, or values keep a predictable shape as the feature evolves, and data shape and updates remain explicit throughout the lesson.",
        "Use the && operator to conditionally render children only when a node's ID is in the expanded set so the pattern drives visible behavior, and the UI exposes only the branch that matches the current state."
      ]
    },
    {
      "lesson": 58,
      "file": "inpact_ts58_engine.jsx",
      "title": "Pagination — Typed",
      "objectives": [
        "Establish a typed usePagination<T>(items: T[], pageSize: number) hook that returns currentPage, totalPages, and a typed page slice so the contract is explicit and incorrect usage is caught early.",
        "Use math.ceil to compute totalPages and Array.prototype.slice to extract the current page items so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Model useState<number> for currentPage and implement goToPage, nextPage, and prevPage functions typed as () => void so state, refs, or values keep a predictable shape as the feature evolves, and data shape and updates remain explicit throughout the lesson.",
        "Consume the typed hook in a component and render page items with .map() and navigation buttons so the learner can connect the typed API to real screen behavior."
      ]
    },
    {
      "lesson": 59,
      "file": "inpact_ts59_engine.jsx",
      "title": "Infinite Scroll — Typed",
      "objectives": [
        "Attach a useRef<HTMLDivElement>(null) to a sentinel element at the bottom of a typed list so the event or DOM boundary behaves correctly and can be verified during interaction.",
        "Instantiate an IntersectionObserver in useEffect to watch the sentinel ref and load the next page when visible so the generic or runtime object proves its value in a concrete case so side effects stay predictable across renders and teardown.",
        "Model useState<Item[]> for accumulated items and append new pages using the array spread operator so state, refs, or values keep a predictable shape as the feature evolves, and data shape and updates remain explicit throughout the lesson.",
        "Model useState<boolean> for hasMore and stop fetching when the API signals no further pages so state, refs, or values keep a predictable shape as the feature evolves, and async data is handled with an explicit and safe data shape."
      ]
    },
    {
      "lesson": 60,
      "file": "inpact_ts60_engine.jsx",
      "title": "Debounced Search — Typed",
      "objectives": [
        "Model separate useState<string> variables for the raw input value and the debounced search term so state, refs, or values keep a predictable shape as the feature evolves, and data shape and updates remain explicit throughout the lesson.",
        "Apply the useDebounce<string> hook to produce a debounced query string that updates after a delay so the learner sees how a typed pattern changes behavior, reuse, or safety in practice.",
        "Trigger a typed useFetch<Result[]> call inside useEffect only when the debounced query changes so the learner can explain both the mechanism and the visible result, and side effects stay predictable across renders and teardown.",
        "Render search results by mapping over a typed Result[] array returned from the fetch hook so the learner can connect the typed data shape to the final UI output so async data is handled with an explicit and safe data shape."
      ]
    },
    {
      "lesson": 61,
      "file": "inpact_ts61_engine.jsx",
      "title": "Multi-Step Form — Typed",
      "objectives": [
        "Establish a typed FormData interface covering all steps and declare useState<FormData> with an empty initial object so the contract is explicit, and data shape and updates remain explicit throughout the lesson.",
        "Model useState<number> for the current step index and implement next and back functions typed as () => void so state, refs, or values keep a predictable shape as the feature evolves, and data shape and updates remain explicit throughout the lesson.",
        "Pass the current form slice and a typed onChange handler as props to each step component so data crossing component boundaries stays explicit, safe, and easy to trace.",
        "On final submit, call a handler typed as (data: FormData) => void with the fully accumulated state so the learner can explain both the mechanism and the visible result."
      ]
    },
    {
      "lesson": 62,
      "file": "inpact_ts62_engine.jsx",
      "title": "Generic List<T> — Typed",
      "objectives": [
        "Write a generic component List<T> that accepts items: T[] and a renderItem prop typed as (item: T) => React.ReactNode so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Add a keyExtractor prop typed as (item: T) => string | number to supply stable keys in the .map() call so the UI becomes more robust, accessible, or verifiable in use.",
        "Apply a generic type constraint <T extends { id: string }> to guarantee a fallback key field so the learner sees how a typed pattern changes behavior, reuse, or safety in practice.",
        "Instantiate list<Product>, List<User>, and List<Order> at the call site and confirm each enforces its typed item shape so the generic or runtime object proves its value in a concrete case."
      ]
    },
    {
      "lesson": 63,
      "file": "inpact_ts63_engine.jsx",
      "title": "Discriminated Union Props — Typed",
      "objectives": [
        "Establish two prop interfaces that share a type discriminant field ('success' | 'error') with different additional fields so the contract is explicit and incorrect usage is caught early.",
        "Create a discriminated union type by combining the two interfaces with the | operator so the design pattern is reusable and its contract is clear to downstream code.",
        "Type a component to accept the union type and use a switch statement on the type field to narrow to each branch so invalid interactions are rejected by the compiler instead of leaking into runtime.",
        "Demonstrate that accessing a field only present on one branch without narrowing produces a TypeScript error so the learner can observe the consequence and attach a mental model to it."
      ]
    },
    {
      "lesson": 64,
      "file": "inpact_ts64_engine.jsx",
      "title": "useRef Typing — Typed",
      "objectives": [
        "Model a DOM ref using useRef<HTMLInputElement>(null) and attach it to a JSX element via the ref prop so state, refs, or values keep a predictable shape as the feature evolves.",
        "Access the ref's .current property with a null check before calling DOM methods like .focus() so the learner can explain both the mechanism and the visible result.",
        "Model a mutable value ref using useRef<number>(0) to persist a value across renders without triggering re-renders so state, refs, or values keep a predictable shape as the feature evolves.",
        "Distinguish between React.RefObject<T> (read-only .current) and React.MutableRefObject<T> (writable .current) so the learner can choose the right abstraction instead of mixing similar APIs."
      ]
    },
    {
      "lesson": 65,
      "file": "inpact_ts65_engine.jsx",
      "title": "Event Typing — Typed",
      "objectives": [
        "Type a change handler for a text input as React.ChangeEvent<HTMLInputElement> and read e.target.value so invalid interactions are rejected by the compiler instead of leaking into runtime.",
        "Type a change handler for a select element as React.ChangeEvent<HTMLSelectElement> so invalid interactions are rejected by the compiler instead of leaking into runtime.",
        "Type a form submit handler as React.FormEvent<HTMLFormElement> and call e.preventDefault() so invalid interactions are rejected by the compiler instead of leaking into runtime.",
        "Type a drag event handler as React.DragEvent<HTMLDivElement> and read e.dataTransfer.getData so invalid interactions are rejected by the compiler instead of leaking into runtime."
      ]
    },
    {
      "lesson": 66,
      "file": "inpact_ts66_engine.jsx",
      "title": "Generic useFetch<T> — Typed",
      "objectives": [
        "Write a generic hook useFetch<T>(url: string): { data: T | null; loading: boolean; error: Error | null } so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Cast the fetch JSON response to T using the as keyword and explain its type-assertion semantics so the learner can explain both the mechanism and the visible result, and async data is handled with an explicit and safe data shape.",
        "Instantiate useFetch<User[]>, useFetch<Product>, and useFetch<Order> and confirm each returns a correctly typed data field so the generic or runtime object proves its value in a concrete case.",
        "Add a generic constraint <T extends object> to prevent the hook from being instantiated with primitive types so the UI becomes more robust, accessible, or verifiable in use."
      ]
    },
    {
      "lesson": 67,
      "file": "inpact_ts67_engine.jsx",
      "title": "Utility Types — Typed",
      "objectives": [
        "Apply partial<T> to make all fields of a props interface optional in an update handler so the learner sees how a typed pattern changes behavior, reuse, or safety in practice.",
        "Apply required<T> to assert all optional fields are present before passing data to a submit function so the learner sees how a typed pattern changes behavior, reuse, or safety in practice.",
        "Apply pick<T, K> to extract a subset of fields from a large interface for a read-only display component so the learner sees how a typed pattern changes behavior, reuse, or safety in practice.",
        "Apply omit<T, K> to strip sensitive fields from an interface before passing data to a child component so the learner sees how a typed pattern changes behavior, reuse, or safety in practice."
      ]
    },
    {
      "lesson": 68,
      "file": "inpact_ts68_engine.jsx",
      "title": "useImperativeHandle — Typed",
      "objectives": [
        "Establish a typed handle interface (e.g. InputHandle with a focus method) to describe exposed imperative methods so the contract is explicit and incorrect usage is caught early.",
        "Wrap a functional component with React.forwardRef<InputHandle, InputProps> to accept a typed ref so the boundary is explicit and the fallback or optimization can be verified at runtime.",
        "Call useImperativeHandle(ref, () => ({ focus: ... })) to expose only the methods defined in the handle interface so the hook or API is used with an intentional contract and observable outcome.",
        "Model a useRef<InputHandle>(null) in the parent, attach it to the child, and call handle methods imperatively so state, refs, or values keep a predictable shape as the feature evolves."
      ]
    },
    {
      "lesson": 69,
      "file": "inpact_ts69_engine.jsx",
      "title": "useSyncExternalStore — Typed",
      "objectives": [
        "Create a typed external store with a subscribe function, a getSnapshot function, and a getServerSnapshot function so the design pattern is reusable and its contract is clear to downstream code.",
        "Call useSyncExternalStore<T>(subscribe, getSnapshot) with a generic type parameter to get a typed snapshot so the hook or API is used with an intentional contract and observable outcome.",
        "Implement a notify pattern in the store so state changes trigger all subscriber callbacks so the feature works end to end and the resulting behavior can be observed and explained.",
        "Consume the typed store value in multiple components and verify they all update synchronously on a single store mutation so the learner can connect the typed API to real screen behavior."
      ]
    },
    {
      "lesson": 70,
      "file": "inpact_ts70_engine.jsx",
      "title": "useTransition — Typed",
      "objectives": [
        "Destructure [isPending, startTransition] from useTransition and type isPending as boolean so the returned contract is readable and its parts are used intentionally.",
        "Wrap a slow state update inside startTransition(() => { ... }) to mark it as a non-urgent transition so the boundary is explicit and the fallback or optimization can be verified at runtime.",
        "Use the isPending boolean to render a loading indicator while the transition is in progress so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Demonstrate that urgent state updates (e.g. input onChange) remain responsive while the transition is pending so the learner can observe the consequence and attach a mental model to it."
      ]
    },
    {
      "lesson": 71,
      "file": "inpact_ts71_engine.jsx",
      "title": "useDeferredValue — Typed",
      "objectives": [
        "Call useDeferredValue<T>(value) with an explicit generic type parameter to create a deferred copy of a state variable so the hook or API is used with an intentional contract and observable outcome.",
        "Pass the deferred value to a slow child component while passing the immediate value to a responsive input so data crossing component boundaries stays explicit, safe, and easy to trace.",
        "Wrap the slow child in React.memo so it only re-renders when the deferred value actually changes so the boundary is explicit and the fallback or optimization can be verified at runtime and verify whether stable props actually reduce avoidable re-renders.",
        "Compare the behavior of useDeferredValue with useTransition by observing which API controls the timing of the deferred work so the learner can explain both the mechanism and the visible result."
      ]
    },
    {
      "lesson": 72,
      "file": "inpact_ts72_engine.jsx",
      "title": "useLayoutEffect vs useEffect — Typed",
      "objectives": [
        "Call useLayoutEffect with a typed side effect that reads a DOM element's getBoundingClientRect() synchronously after paint so the hook or API is used with an intentional contract and observable outcome.",
        "Model a useRef<HTMLDivElement>(null) and use it inside useLayoutEffect to measure the element before the browser paints so state, refs, or values keep a predictable shape as the feature evolves.",
        "Demonstrate the visual flash that occurs when the same measurement is done in useEffect instead so the learner can observe the consequence and attach a mental model to it so side effects stay predictable across renders and teardown.",
        "Return a typed cleanup function () => void from useLayoutEffect to reset any synchronous DOM mutations so consumers receive a stable contract and the effect of that contract can be tested immediately so the effect leaves no stale subscriptions, timers, or listeners behind."
      ]
    },
    {
      "lesson": 73,
      "file": "inpact_ts73_engine.jsx",
      "title": "Mini Redux — Typed",
      "objectives": [
        "Establish a typed State interface and a discriminated union Action type with multiple action shapes so the contract is explicit and incorrect usage is caught early.",
        "Implement a pure reducer function typed as (state: State, action: Action): State so the feature works end to end and the resulting behavior can be observed and explained.",
        "Create a typed store object with getState, dispatch, and subscribe methods without using any external library so the design pattern is reusable and its contract is clear to downstream code.",
        "Consume the store in a component using useSyncExternalStore with typed getSnapshot and subscribe arguments so the learner can connect the typed API to real screen behavior."
      ]
    },
    {
      "lesson": 74,
      "file": "inpact_ts74_engine.jsx",
      "title": "Optimistic UI — Typed",
      "objectives": [
        "Apply an optimistic state update immediately on user action before the async API call resolves so the learner sees how a typed pattern changes behavior, reuse, or safety in practice.",
        "Model a typed rollback function that restores the previous state if the API call rejects so state, refs, or values keep a predictable shape as the feature evolves.",
        "Use a try/catch block around the async operation to call the rollback function on failure so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Type the optimistic item with a pending: true field using an intersection type so the UI can show a spinner so invalid interactions are rejected by the compiler instead of leaking into runtime."
      ]
    },
    {
      "lesson": 75,
      "file": "inpact_ts75_engine.jsx",
      "title": "Request Deduplication — Typed",
      "objectives": [
        "Create a typed in-flight cache using Map<string, Promise<T>> to store pending requests by URL key so the design pattern is reusable and its contract is clear to downstream code.",
        "Write a typed fetchOnce<T>(url: string): Promise<T> function that returns the existing promise if a request is already in flight so the abstraction solves a concrete UI lesson and exposes a reliable typed contract so async data is handled with an explicit and safe data shape.",
        "Remove the cache entry in the .finally() handler of the promise to allow re-fetching after completion so the learner can explain both the mechanism and the visible result, and async data is handled with an explicit and safe data shape.",
        "Consume fetchOnce in multiple components that mount simultaneously and verify only one network request fires so the learner can connect the typed API to real screen behavior, and async data is handled with an explicit and safe data shape."
      ]
    },
    {
      "lesson": 76,
      "file": "inpact_ts76_engine.jsx",
      "title": "Polling Hook — Typed",
      "objectives": [
        "Write a custom hook usePolling<T>(fetchFn: () => Promise<T>, interval: number): { data: T | null; error: Error | null } so the abstraction solves a concrete UI lesson and exposes a reliable typed contract so async data is handled with an explicit and safe data shape.",
        "Use setInterval inside useEffect to call fetchFn on each tick and update typed state with the result so the pattern drives visible behavior, and async data is handled with an explicit and safe data shape.",
        "Return a clearInterval cleanup from useEffect to stop polling when the component unmounts so consumers receive a stable contract and the effect of that contract can be tested immediately so the effect leaves no stale subscriptions, timers, or listeners behind.",
        "Expose a stop function typed as () => void from the hook so the consumer can pause polling manually so consumers interact with a deliberate surface instead of reaching into implementation details."
      ]
    },
    {
      "lesson": 77,
      "file": "inpact_ts77_engine.jsx",
      "title": "WebSocket Hook — Typed",
      "objectives": [
        "Write a custom hook useWebSocket<T>(url: string) that opens a WebSocket connection inside useEffect so the abstraction solves a concrete UI lesson and exposes a reliable typed contract so side effects stay predictable across renders and teardown.",
        "Type the onmessage handler as (e: MessageEvent) => void and parse e.data with JSON.parse, casting the result to T so invalid interactions are rejected by the compiler instead of leaking into runtime.",
        "Model a useState<T | null> to store the latest typed message and update it in the message handler so state, refs, or values keep a predictable shape as the feature evolves, and data shape and updates remain explicit throughout the lesson.",
        "Return a send function typed as (payload: T) => void that calls ws.send(JSON.stringify(payload)) so consumers receive a stable contract and the effect of that contract can be tested immediately."
      ]
    },
    {
      "lesson": 78,
      "file": "inpact_ts78_engine.jsx",
      "title": "Feature Flag Hook — Typed",
      "objectives": [
        "Establish a FeatureFlags interface with boolean fields for each flag and create a typed context for it so the contract is explicit and incorrect usage is caught early.",
        "Write a useFeatureFlag(flag: keyof FeatureFlags): boolean hook that reads and returns the typed flag value so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Consume useFeatureFlag in a component to conditionally render a typed JSX block with the && operator so the learner can connect the typed API to real screen behavior, and the UI exposes only the branch that matches the current state.",
        "Implement a FeatureFlagProvider that merges server-fetched flags with a typed default flags object so the feature works end to end, and async data is handled with an explicit and safe data shape."
      ]
    },
    {
      "lesson": 79,
      "file": "inpact_ts79_engine.jsx",
      "title": "Undo/Redo — Typed",
      "objectives": [
        "Establish a typed History<T> interface with past: T[], present: T, and future: T[] fields so the contract is explicit and incorrect usage is caught early.",
        "Implement a typed reducer with 'SET', 'UNDO', and 'REDO' action types that operate on the History<T> structure so the feature works end to end and the resulting behavior can be observed and explained.",
        "Call useReducer<History<T>, HistoryAction<T>>(reducer, initialHistory) and destructure state and dispatch so the hook or API is used with an intentional contract and observable outcome.",
        "Expose undo and redo dispatch calls via a typed useUndoRedo<T> hook with canUndo and canRedo boolean fields so consumers interact with a deliberate surface instead of reaching into implementation details."
      ]
    },
    {
      "lesson": 80,
      "file": "inpact_ts80_engine.jsx",
      "title": "Form Library from Scratch — Typed",
      "objectives": [
        "Write a generic useForm<T extends object>(initialValues: T) hook that manages typed field values and errors so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Implement a typed handleChange function using keyof T to update individual fields without losing type safety so the feature works end to end and the resulting behavior can be observed and explained.",
        "Implement a validate function typed as (values: T) => Partial<Record<keyof T, string>> that returns per-field errors so the feature works end to end and the resulting behavior can be observed and explained.",
        "Return a typed register function from the hook that produces value, onChange, and onBlur props for any field so consumers receive a stable contract and the effect of that contract can be tested immediately."
      ]
    },
    {
      "lesson": 81,
      "file": "inpact_ts81_engine.jsx",
      "title": "Component Library Theming — Typed",
      "objectives": [
        "Establish a Theme interface with typed token fields (colors, spacing, typography) and create a typed ThemeContext so the contract is explicit, and shared data can move without losing type safety across components.",
        "Write a typed useTheme hook that reads the ThemeContext and returns the full Theme object so the abstraction solves a concrete UI lesson and exposes a reliable typed contract so shared data can move without losing type safety across components.",
        "Apply theme tokens inside a component by reading from the typed theme object and assigning to React.CSSProperties so the learner sees how a typed pattern changes behavior, reuse, or safety in practice.",
        "Extend the Theme interface to add a new token and confirm that all components using useTheme receive a compiler error until the token is provided so reuse does not weaken the contract or blur responsibility and use the compiler feedback to correct the contract before runtime."
      ]
    },
    {
      "lesson": 82,
      "file": "inpact_ts82_engine.jsx",
      "title": "Micro-frontend Shell — Typed",
      "objectives": [
        "Configure a Webpack Module Federation plugin to expose and consume typed remote components so the infrastructure supports the typed runtime behavior the feature depends on.",
        "Model a module type definition file (.d.ts) for a remote federated module to restore TypeScript type safety so state, refs, or values keep a predictable shape as the feature evolves.",
        "Load a remote component lazily using React.lazy(() => import('remote/Component')) inside a Suspense boundary so the learner can explain both the mechanism and the visible result.",
        "Establish a shared typed contract interface used by both the shell and the remote to communicate props so the contract is explicit and incorrect usage is caught early."
      ]
    },
    {
      "lesson": 83,
      "file": "inpact_ts83_engine.jsx",
      "title": "Race Condition Fix — Typed",
      "objectives": [
        "Reproduce a race condition by firing two overlapping fetch requests and showing the stale response wins so the learner can explain both the mechanism and the visible result, and async data is handled with an explicit and safe data shape.",
        "Model a boolean ignore flag inside useEffect and set it to true in the cleanup to discard stale responses so state, refs, or values keep a predictable shape as the feature evolves, and the effect leaves no stale subscriptions, timers, or listeners behind.",
        "Use an AbortController typed as AbortController and pass its signal to the fetch call to cancel in-flight requests so the pattern drives visible behavior, and async data is handled with an explicit and safe data shape.",
        "Return controller.abort() from useEffect cleanup and verify the race condition no longer occurs so consumers receive a stable contract and the effect of that contract can be tested immediately so the effect leaves no stale subscriptions, timers, or listeners behind."
      ]
    },
    {
      "lesson": 84,
      "file": "inpact_ts84_engine.jsx",
      "title": "Memoization Strategy — Typed",
      "objectives": [
        "Apply useMemo with a typed return type to memoize a derived data transformation over a typed array so the learner sees how a typed pattern changes behavior, reuse, or safety in practice.",
        "Apply useCallback with a typed signature to memoize an event handler passed to a React.memo child so the learner sees how a typed pattern changes behavior, reuse, or safety in practice and verify whether stable props actually reduce avoidable re-renders.",
        "Identify when memoization is unnecessary by tracing the component tree for referential stability so the learner can spot the failure mode before applying the fix.",
        "Write a typed selector function and compose it with useMemo to derive computed state from a context value so the abstraction solves a concrete UI lesson and exposes a reliable typed contract."
      ]
    },
    {
      "lesson": 85,
      "file": "inpact_ts85_engine.jsx",
      "title": "Concurrent Mode Gotchas — Typed",
      "objectives": [
        "Identify a tearing bug caused by reading from an external store without useSyncExternalStore and fix it so the learner can spot the failure mode before applying the fix.",
        "Replace a direct state mutation in a transition with an immutable update to prevent concurrent mode inconsistencies so the learner can explain both the mechanism and the visible result.",
        "Wrap a state update in startTransition to demonstrate deferred rendering for a slow non-urgent update so the boundary is explicit and the fallback or optimization can be verified at runtime.",
        "Use the React DevTools Profiler in concurrent mode to observe interrupted renders and commit phases so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler and validate the behavior with runtime evidence, not guesswork."
      ]
    },
    {
      "lesson": 86,
      "file": "inpact_ts86_engine.jsx",
      "title": "Memory Leak Hunt — Typed",
      "objectives": [
        "Identify a memory leak caused by a setState call after component unmount and fix it with a cleanup flag so the learner can spot the failure mode before applying the fix so the effect leaves no stale subscriptions, timers, or listeners behind.",
        "Return a clearTimeout or clearInterval cleanup function typed as () => void from useEffect so consumers receive a stable contract and the effect of that contract can be tested immediately so the effect leaves no stale subscriptions, timers, or listeners behind.",
        "Abort an in-flight fetch request using AbortController.abort() inside the useEffect cleanup so the learner can explain both the mechanism and the visible result, and the effect leaves no stale subscriptions, timers, or listeners behind.",
        "Use the Chrome Memory tab to take a heap snapshot and confirm a suspect component is no longer retained after unmount so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler."
      ]
    },
    {
      "lesson": 87,
      "file": "inpact_ts87_engine.jsx",
      "title": "Test useFetch — Typed",
      "objectives": [
        "Mock the global fetch function using vi.fn() or jest.fn() with a typed return value matching the API response so tests stay realistic while preserving the same contract as production code so async data is handled with an explicit and safe data shape.",
        "Write a typed renderHook call from @testing-library/react to invoke useFetch<T> in a test environment so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Assert that the loading state is true before the fetch resolves and false after using waitFor so the expected behavior is proven rather than assumed so async data is handled with an explicit and safe data shape.",
        "Assert that the returned data matches the typed mock response using expect with typed matchers so the expected behavior is proven rather than assumed."
      ]
    },
    {
      "lesson": 88,
      "file": "inpact_ts88_engine.jsx",
      "title": "Test Async Component — Typed",
      "objectives": [
        "Render a component that fetches data using render from @testing-library/react with typed mock props so the learner can connect the typed data shape to the final UI output so async data is handled with an explicit and safe data shape.",
        "Use waitFor or findByText to assert that async content appears in the DOM after the mock resolves so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Mock a named module export using vi.mock or jest.mock and type the mock implementation to match the real signature so tests stay realistic while preserving the same contract as production code.",
        "Assert that an error message renders when the mock fetch rejects with a typed Error object so the expected behavior is proven rather than assumed so async data is handled with an explicit and safe data shape."
      ]
    },
    {
      "lesson": 89,
      "file": "inpact_ts89_engine.jsx",
      "title": "Test User Interactions — Typed",
      "objectives": [
        "Use userEvent.type to simulate keyboard input on a controlled input and assert the typed state updates so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Use userEvent.click to fire a button press and assert the resulting DOM change so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Use userEvent.selectOptions on a typed <select> element and verify the controlled value updates so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Assert that a form submit handler is called with the correct typed payload using a jest.fn() spy so the expected behavior is proven rather than assumed."
      ]
    },
    {
      "lesson": 90,
      "file": "inpact_ts90_engine.jsx",
      "title": "Test Context — Typed",
      "objectives": [
        "Create a typed custom render function that wraps the component under test in a typed context Provider so the design pattern is reusable and its contract is clear to downstream code.",
        "Assert that a component correctly reads a typed context value by inspecting its rendered output so the expected behavior is proven rather than assumed.",
        "Simulate a context value change by re-rendering with a different typed provider value and asserting the UI update so the learner can verify behavior from the user perspective rather than only from implementation details.",
        "Test a useContext-based custom hook using renderHook wrapped in a typed Provider so the learner can explain both the mechanism and the visible result, and shared data can move without losing type safety across components."
      ]
    },
    {
      "lesson": 91,
      "file": "inpact_ts91_engine.jsx",
      "title": "Test Error Boundary — Typed",
      "objectives": [
        "Write a typed child component that conditionally throws an Error to trigger the error boundary so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Render the ErrorBoundary wrapping the typed throwing child and assert the fallback UI is displayed so the learner can connect the typed data shape to the final UI output.",
        "Spy on console.error and suppress it during the test to avoid noisy output from expected errors so the learner can explain both the mechanism and the visible result.",
        "Re-render with a non-throwing child and assert the error boundary resets and shows the normal UI so the learner can explain both the mechanism and the visible result."
      ]
    },
    {
      "lesson": 92,
      "file": "inpact_ts92_engine.jsx",
      "title": "Design DataTable API — Typed",
      "objectives": [
        "Establish a generic Column<T> interface with key: keyof T, header: string, and an optional render prop typed as (val: T[keyof T], row: T) => React.ReactNode so the contract is explicit and incorrect usage is caught early.",
        "Write a generic DataTable<T> component that accepts rows: T[] and columns: Column<T>[] and renders a typed <table> so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Implement a typed sort handler that reads the Column key and sorts the rows array using Array.prototype.sort so the feature works end to end and the resulting behavior can be observed and explained.",
        "Expose a typed onRowClick prop as (row: T) => void and call it inside the row's onClick handler so consumers interact with a deliberate surface instead of reaching into implementation details."
      ]
    },
    {
      "lesson": 93,
      "file": "inpact_ts93_engine.jsx",
      "title": "Design Auth Flow — Typed",
      "objectives": [
        "Establish user, AuthState, and AuthAction interfaces and implement a typed auth reducer so the contract is explicit and incorrect usage is caught early.",
        "Implement typed login and logout functions that dispatch the correct action shapes to the reducer so the feature works end to end and the resulting behavior can be observed and explained.",
        "Store the auth token using the typed useLocalStorage<string | null> hook and read it on app load to rehydrate state so the state can survive the required boundary while keeping its intended type.",
        "Protect a route component by consuming the typed AuthContext and redirecting unauthenticated users so the learner can explain both the mechanism and the visible result, and shared data can move without losing type safety across components."
      ]
    },
    {
      "lesson": 94,
      "file": "inpact_ts94_engine.jsx",
      "title": "Design Notification System — Typed",
      "objectives": [
        "Establish a Notification interface and a typed NotificationContext with add and remove functions so the contract is explicit, and shared data can move without losing type safety across components.",
        "Implement a typed useReducer with ADD_NOTIFICATION and REMOVE_NOTIFICATION action shapes in the provider so the feature works end to end and the resulting behavior can be observed and explained.",
        "Write a useNotify hook typed as () => (n: Omit<Notification, 'id'>) => void that dispatches add actions so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Auto-dismiss notifications by scheduling a setTimeout in the reducer's dispatch side effect and typing its cleanup so the learner can explain both the mechanism and the visible result, and the effect leaves no stale subscriptions, timers, or listeners behind."
      ]
    },
    {
      "lesson": 95,
      "file": "inpact_ts95_engine.jsx",
      "title": "Design Permission System — Typed",
      "objectives": [
        "Establish a string literal union type Permission and a User interface with a permissions: Permission[] field so the contract is explicit and incorrect usage is caught early.",
        "Write a usePermission(p: Permission): boolean hook that checks if the current user holds the given permission so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Build a typed <CanAccess permission={Permission}> component that renders children only when the hook returns true so multiple typed pieces work together as one coherent UI pattern.",
        "Apply the Omit<T, K> utility type to strip permission-sensitive fields before passing data to an unprivileged component so the learner sees how a typed pattern changes behavior, reuse, or safety in practice."
      ]
    },
    {
      "lesson": 96,
      "file": "inpact_ts96_engine.jsx",
      "title": "Design Real-Time Dashboard — Typed",
      "objectives": [
        "Establish typed interfaces for each metric panel's data shape and connect them via the useWebSocket<T> hook so the contract is explicit and incorrect usage is caught early.",
        "Compose typed chart components by passing strongly-typed data arrays as props so the learner sees how smaller typed pieces form a larger UI capability.",
        "Use useSyncExternalStore with a typed external metrics store to guarantee consistent reads across panels so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Wrap expensive chart computations in useMemo<ChartData> to prevent unnecessary recalculations on WebSocket updates so the boundary is explicit and the fallback or optimization can be verified at runtime."
      ]
    },
    {
      "lesson": 97,
      "file": "inpact_ts97_engine.jsx",
      "title": "useId & Stable IDs — Typed",
      "objectives": [
        "Call useId() and assign its string return value to a const to generate a stable, unique ID per component instance so the hook or API is used with an intentional contract and observable outcome.",
        "Use the generated ID to associate a <label> htmlFor with an <input> id to satisfy accessibility requirements so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Append a suffix to the base ID (e.g. id + '-input') to create multiple related typed IDs within one component so the learner can explain both the mechanism and the visible result.",
        "Demonstrate that useId produces different IDs across multiple instances of the same component so the learner can observe the consequence and attach a mental model to it."
      ]
    },
    {
      "lesson": 98,
      "file": "inpact_ts98_engine.jsx",
      "title": "forwardRef & Exposing DOM Nodes — Typed",
      "objectives": [
        "Wrap a functional component with React.forwardRef<HTMLInputElement, InputProps> to accept a typed ref from a parent so the boundary is explicit and the fallback or optimization can be verified at runtime.",
        "Attach the forwarded ref to the underlying <input> DOM element via the ref prop so the event or DOM boundary behaves correctly and can be verified during interaction.",
        "Model a useRef<HTMLInputElement>(null) in the parent and pass it to the forwarded component to call .focus() so state, refs, or values keep a predictable shape as the feature evolves.",
        "Combine forwardRef with useImperativeHandle to expose a limited typed API instead of the raw DOM node so the learner can explain both the mechanism and the visible result."
      ]
    },
    {
      "lesson": 99,
      "file": "inpact_ts99_engine.jsx",
      "title": "AbortController & Cleanup in useEffect — Typed",
      "objectives": [
        "Instantiate an AbortController inside useEffect and pass its signal to the fetch call as a typed RequestInit field so the generic or runtime object proves its value in a concrete case so async data is handled with an explicit and safe data shape.",
        "Return controller.abort() as the cleanup function from useEffect to cancel the request on unmount so consumers receive a stable contract and the effect of that contract can be tested immediately so the effect leaves no stale subscriptions, timers, or listeners behind.",
        "Catch the AbortError by name in the catch block to distinguish a user-cancelled request from a real network failure so the learner can explain both the mechanism and the visible result.",
        "Demonstrate that without the abort cleanup a component that unmounts mid-fetch still calls setState and logs a warning so the learner can observe the consequence and attach a mental model to it so the effect leaves no stale subscriptions, timers, or listeners behind."
      ]
    },
    {
      "lesson": 100,
      "file": "inpact_ts100_engine.jsx",
      "title": "React Router Basics — Typed",
      "objectives": [
        "Wrap the app in a <BrowserRouter> and define typed <Route path='' element={} /> entries inside a <Routes> block so the boundary is explicit and the fallback or optimization can be verified at runtime.",
        "Use the useNavigate hook typed as NavigateFunction to programmatically navigate on a button click so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Use the useParams<{ id: string }>() hook to read a typed URL parameter from the current route so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Use the useLocation hook to read the typed location.pathname and display the active route in a nav component so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler."
      ]
    },
    {
      "lesson": 101,
      "file": "inpact_ts101_engine.jsx",
      "title": "Nested Routes & Protected Routes — Typed",
      "objectives": [
        "Define nested <Route> elements inside a parent route and render an <Outlet /> in the parent component to display child routes so the learner can explain the contract and apply it deliberately in code.",
        "Build a typed ProtectedRoute component that reads auth state and returns <Navigate to='/login' /> for unauthenticated users so multiple typed pieces work together as one coherent UI pattern.",
        "Use the useOutletContext<T>() hook to pass typed shared data from a layout route to its child routes so the pattern drives visible behavior, and shared data can move without losing type safety across components.",
        "Chain multiple nested routes and confirm that each child renders inside the parent layout without full remounts so the learner can explain both the mechanism and the visible result."
      ]
    },
    {
      "lesson": 102,
      "file": "inpact_ts102_engine.jsx",
      "title": "TanStack Query Basics — Typed",
      "objectives": [
        "Call useQuery<ResponseType, Error>({ queryKey, queryFn }) with explicit type parameters to get a typed data field so the hook or API is used with an intentional contract and observable outcome.",
        "Consume the isLoading, isError, and data fields from the typed useQuery result to drive conditional rendering so the learner can connect the typed API to real screen behavior.",
        "Call useMutation<ResponseType, Error, Variables>({ mutationFn }) and invoke mutate with a typed variables argument so the hook or API is used with an intentional contract and observable outcome.",
        "Invalidate a typed query key using queryClient.invalidateQueries after a successful mutation to trigger a refetch so the learner can explain both the mechanism and the visible result, and async data is handled with an explicit and safe data shape."
      ]
    },
    {
      "lesson": 103,
      "file": "inpact_ts103_engine.jsx",
      "title": "Zustand for Global State — Typed",
      "objectives": [
        "Establish a typed store interface and create a Zustand store using create<StoreType>((set) => ({ ... })) so the contract is explicit and incorrect usage is caught early.",
        "Read a typed state slice from the store using a selector typed as (state: StoreType) => T so the learner can explain what runtime information is driving the next decision.",
        "Call a typed action from the store by destructuring it from useStore and invoking it in an event handler so the hook or API is used with an intentional contract and observable outcome.",
        "Demonstrate that components subscribing to different slices re-render independently using selector isolation so the learner can observe the consequence and attach a mental model to it."
      ]
    },
    {
      "lesson": 104,
      "file": "inpact_ts104_engine.jsx",
      "title": "Next.js App Router Fundamentals — Typed",
      "objectives": [
        "Create a typed Server Component by exporting an async function that fetches data and returns JSX without useState so the design pattern is reusable, and async data is handled with an explicit and safe data shape.",
        "Mark a component as a Client Component by adding the 'use client' directive and using useState or useEffect so the learner can explain both the mechanism and the visible result, and side effects stay predictable across renders and teardown.",
        "Establish a typed generateStaticParams function that returns an array of param objects to pre-render dynamic routes so the contract is explicit and incorrect usage is caught early.",
        "Use the typed params and searchParams props in a page component to read route and query string values so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler."
      ]
    },
    {
      "lesson": 105,
      "file": "inpact_ts105_engine.jsx",
      "title": "React Server Components & SSR vs SSG — Typed",
      "objectives": [
        "Implement a typed Server Component that calls fetch directly in the component body and passes data as typed props to a Client Component so the feature works end to end, and async data is handled with an explicit and safe data shape.",
        "Use the cache() wrapper typed as <T>(fn: () => Promise<T>) => () => Promise<T> to deduplicate server-side fetch calls so the pattern drives visible behavior, and async data is handled with an explicit and safe data shape.",
        "Configure a Next.js page with export const revalidate = 60 to implement ISR and explain the type-level difference from SSG so the infrastructure supports the typed runtime behavior the feature depends on.",
        "Pass serializable typed props from a Server Component to a Client Component and demonstrate the compiler error when non-serializable values (e.g. functions) are passed so data crossing component boundaries stays explicit, safe, and easy to trace and use the compiler feedback to correct the contract before runtime."
      ]
    },
    {
      "lesson": 106,
      "file": "inpact_ts106_engine.jsx",
      "title": "Accessibility (a11y) Fundamentals — Typed",
      "objectives": [
        "Add aria-label, aria-labelledby, and aria-describedby attributes typed as string to interactive JSX elements so the UI becomes more robust, accessible, or verifiable in use.",
        "Use the useId hook to generate stable IDs and wire them between a <label> and its associated <input> so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Implement keyboard navigation by typing a keydown handler as React.KeyboardEventHandler<HTMLElement> and responding to Enter and Space so the feature works end to end and the resulting behavior can be observed and explained.",
        "Add role, aria-expanded, and aria-controls attributes to a typed disclosure (accordion/menu) component so the UI becomes more robust, accessible, or verifiable in use."
      ]
    },
    {
      "lesson": 107,
      "file": "inpact_ts107_engine.jsx",
      "title": "JWT Storage & Decode in React — Typed",
      "objectives": [
        "Establish a JwtPayload interface and use a JWT decode library typed as (token: string) => JwtPayload to parse the token so the contract is explicit and incorrect usage is caught early.",
        "Store the JWT string in localStorage using the typed useLocalStorage<string | null> hook so the state can survive the required boundary while keeping its intended type.",
        "Read the exp field from the typed payload and compare it with Date.now() / 1000 to check token expiry so the learner can explain what runtime information is driving the next decision.",
        "Clear the token from storage and reset typed auth state when the token is expired or invalid so the learner can explain both the mechanism and the visible result."
      ]
    },
    {
      "lesson": 108,
      "file": "inpact_ts108_engine.jsx",
      "title": "Session Timeout Hook — Typed",
      "objectives": [
        "Write a useSessionTimeout(timeoutMs: number, onTimeout: () => void): void hook so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Reset a setTimeout reference stored in a useRef<ReturnType<typeof setTimeout> | null> on each user interaction so the learner can explain both the mechanism and the visible result.",
        "Attach activity event listeners (mousemove, keydown) in useEffect and call resetTimer in each handler so the event or DOM boundary behaves correctly and can be verified during interaction so side effects stay predictable across renders and teardown.",
        "Return a clearTimeout cleanup from useEffect to prevent a stale timeout from firing after unmount so consumers receive a stable contract and the effect of that contract can be tested immediately so the effect leaves no stale subscriptions, timers, or listeners behind."
      ]
    },
    {
      "lesson": 109,
      "file": "inpact_ts109_engine.jsx",
      "title": "Token Refresh with Axios Interceptor — Typed",
      "objectives": [
        "Register a typed Axios response interceptor using axios.interceptors.response.use with an error handler typed as (e: AxiosError) => Promise<T> so the learner can explain both the mechanism and the visible result.",
        "Detect a 401 status in the interceptor and call a typed refreshToken(): Promise<string> function so the learner can explain both the mechanism and the visible result.",
        "Retry the original request by merging the new token into the typed AxiosRequestConfig and returning axios(config) so the learner can explain both the mechanism and the visible result.",
        "Eject the interceptor in a useEffect cleanup to prevent duplicate handlers on re-renders so the learner can explain both the mechanism and the visible result, and the effect leaves no stale subscriptions, timers, or listeners behind."
      ]
    },
    {
      "lesson": 110,
      "file": "inpact_ts110_engine.jsx",
      "title": "Role-Based Route Protection — Typed",
      "objectives": [
        "Establish a string literal union type Role ('admin' | 'editor' | 'viewer') and add it to the typed User interface so the contract is explicit and incorrect usage is caught early.",
        "Build a typed RequireRole component that reads the current user's role and redirects if it is not in an allowedRoles: Role[] prop so multiple typed pieces work together as one coherent UI pattern.",
        "Wrap route elements with <RequireRole allowedRoles={['admin']}> in the React Router config so the boundary is explicit and the fallback or optimization can be verified at runtime.",
        "Demonstrate that passing an invalid role string to allowedRoles produces a TypeScript compiler error so the learner can observe the consequence and attach a mental model to it and use the compiler feedback to correct the contract before runtime."
      ]
    },
    {
      "lesson": 111,
      "file": "inpact_ts111_engine.jsx",
      "title": "Cookie-Based Auth Flow — Typed",
      "objectives": [
        "Read an HttpOnly cookie value using document.cookie and write a typed parseCookie(name: string): string | null helper so the learner can explain what runtime information is driving the next decision.",
        "Implement a typed login function that posts credentials, receives a Set-Cookie header, and updates typed auth state so the feature works end to end and the resulting behavior can be observed and explained.",
        "Implement a typed logout function that calls a server endpoint to clear the cookie and resets local auth state so the feature works end to end and the resulting behavior can be observed and explained.",
        "Handle cSRF protection by reading a typed csrf-token cookie and attaching it as an X-CSRF-Token header in POST requests so the edge case is explicitly managed instead of silently leaking into runtime."
      ]
    },
    {
      "lesson": 112,
      "file": "inpact_ts112_engine.jsx",
      "title": "Zustand Persist Middleware — Typed",
      "objectives": [
        "Wrap a typed Zustand store with the persist middleware using create<StoreType>()(persist(storeInitializer, { name: '...' })) so the boundary is explicit and the fallback or optimization can be verified at runtime.",
        "Supply a typed storage option to the persist config (e.g. sessionStorage) implementing the PersistStorage<T> interface so each consumer receives exactly the data or behavior it depends on.",
        "Use the partialize option typed as (state: StoreType) => Partial<StoreType> to persist only a subset of the store so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Read the persisted state on app load and verify the correct typed values are rehydrated from storage so the learner can explain what runtime information is driving the next decision."
      ]
    },
    {
      "lesson": 113,
      "file": "inpact_ts113_engine.jsx",
      "title": "OAuth2 PKCE Flow in a SPA — Typed",
      "objectives": [
        "Generate a typed code verifier and code challenge by calling crypto.subtle.digest with ArrayBuffer typed conversions so the learner can explain both the mechanism and the visible result.",
        "Build the authorization URL with typed URLSearchParams and store the code verifier in sessionStorage so multiple typed pieces work together as one coherent UI pattern.",
        "Exchange the authorization code for tokens by posting a typed payload to the token endpoint and parsing the typed TokenResponse so the learner can explain both the mechanism and the visible result.",
        "Store the typed TokenResponse fields (access_token, refresh_token, expires_in) using the typed useLocalStorage hook so the state can survive the required boundary while keeping its intended type."
      ]
    },
    {
      "lesson": 114,
      "file": "inpact_ts114_engine.jsx",
      "title": "RBAC Permission Hook — Typed",
      "objectives": [
        "Establish a Permission string literal union and a RolePermissions type as Record<Role, Permission[]> so the contract is explicit and incorrect usage is caught early.",
        "Write a useHasPermission(permission: Permission): boolean hook that looks up the current user's role in RolePermissions so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Write a useHasAnyPermission(permissions: Permission[]): boolean hook using Array.prototype.some against the role's permissions so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Consume both hooks in a component and use their boolean returns with the && operator to show/hide typed UI elements so the learner can connect the typed API to real screen behavior."
      ]
    },
    {
      "lesson": 115,
      "file": "inpact_ts115_engine.jsx",
      "title": "Multi-Tab Auth Sync — Typed",
      "objectives": [
        "Attach a storage event listener typed as (e: StorageEvent) => void to the window to detect auth token changes across tabs so the event or DOM boundary behaves correctly and can be verified during interaction.",
        "Parse the typed e.newValue from the storage event and dispatch the appropriate typed auth action to keep state in sync so the learner can explain both the mechanism and the visible result.",
        "Return a removeEventListener cleanup from useEffect to prevent duplicate storage listeners so consumers receive a stable contract and the effect of that contract can be tested immediately so the effect leaves no stale subscriptions, timers, or listeners behind.",
        "Simulate a logout in one browser tab and verify that other tabs redirect to the login page via the storage event so the learner can verify behavior from the user perspective rather than only from implementation details."
      ]
    },
    {
      "lesson": 116,
      "file": "inpact_ts116_engine.jsx",
      "title": "Secure Token Rotation — Typed",
      "objectives": [
        "Write a typed refreshAccessToken(): Promise<TokenResponse> function that sends the refresh token and returns a new typed token pair so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Schedule a proactive token refresh using setTimeout before the access token expires, reading the exp field from the typed JWT payload so the learner can explain both the mechanism and the visible result.",
        "Store the refresh timeout ID in a useRef<ReturnType<typeof setTimeout> | null> and clear it in the useEffect cleanup so the state can survive the required boundary while keeping its intended type so the effect leaves no stale subscriptions, timers, or listeners behind.",
        "Revoke the refresh token on logout by calling a typed endpoint and clearing all token values from the typed useLocalStorage hook so the learner can explain both the mechanism and the visible result."
      ]
    },
    {
      "lesson": 117,
      "file": "inpact_ts117_engine.jsx",
      "title": "Form Validation with Zod + React Hook Form — Typed",
      "objectives": [
        "Establish a Zod schema using z.object() with typed field validators and infer the TypeScript type using z.infer<typeof schema> so the contract is explicit and incorrect usage is caught early.",
        "Pass the Zod schema to the zodResolver() adapter and provide it to useForm<FormValues> as the resolver option so data crossing component boundaries stays explicit, safe, and easy to trace.",
        "Destructure register, handleSubmit, and formState.errors from the typed useForm call so the returned contract is readable and its parts are used intentionally.",
        "Apply the register function to each input and render per-field errors by reading the typed formState.errors object so the learner sees how a typed pattern changes behavior, reuse, or safety in practice."
      ]
    },
    {
      "lesson": 118,
      "file": "inpact_ts118_engine.jsx",
      "title": "Redux Toolkit — createSlice & createAsyncThunk — Typed",
      "objectives": [
        "Establish a typed SliceState interface and create a slice using createSlice<SliceState, 'sliceName', CaseReducers> so the contract is explicit and incorrect usage is caught early.",
        "Write typed case reducers in the reducers field and export the auto-generated typed action creators so the abstraction solves a concrete UI lesson and exposes a reliable typed contract.",
        "Create an async thunk using createAsyncThunk<ReturnType, ArgType, ThunkApiConfig>('action/type', async (arg) => { ... }) so the design pattern is reusable and its contract is clear to downstream code.",
        "Handle the thunk's pending, fulfilled, and rejected cases in extraReducers using builder.addCase with typed payloads so the edge case is explicitly managed instead of silently leaking into runtime."
      ]
    },
    {
      "lesson": 119,
      "file": "inpact_ts119_engine.jsx",
      "title": "RTK Query — Data Fetching & Cache — Typed",
      "objectives": [
        "Create a typed API slice using createApi with a typed baseQuery and an endpoints builder function so the design pattern is reusable and its contract is clear to downstream code.",
        "Establish a typed query endpoint using builder.query<ResponseType, ArgType> and auto-generate a typed useGetXQuery hook so the contract is explicit and incorrect usage is caught early.",
        "Consume the auto-generated hook in a component and destructure the typed data, isLoading, and error fields so the learner can connect the typed API to real screen behavior.",
        "Establish a typed mutation endpoint using builder.mutation<ResponseType, ArgType> and call the auto-generated trigger function so the contract is explicit and incorrect usage is caught early."
      ]
    },
    {
      "lesson": 120,
      "file": "inpact_ts120_engine.jsx",
      "title": "Creating RTK Endpoints (from Zero) — Typed",
      "objectives": [
        "Create an empty API slice using createApi with fetchBaseQuery typed to a base URL and an empty endpoints object so the design pattern is reusable, and async data is handled with an explicit and safe data shape.",
        "Inject a typed query endpoint into an existing API slice using the injectEndpoints method so the learner can explain both the mechanism and the visible result.",
        "Establish the endpoint's query function typed as (arg: ArgType) => string | FetchArgs to build the request URL so the contract is explicit and incorrect usage is caught early.",
        "Export the injected endpoint's auto-generated typed hook and consume it in a feature component so the learner can explain both the mechanism and the visible result."
      ]
    },
    {
      "lesson": 121,
      "file": "inpact_ts121_engine.jsx",
      "title": "Query Building in RTK — Typed",
      "objectives": [
        "Build a query with dynamic parameters by typing the endpoint arg as an object (e.g. { page: number; filter: string }) and constructing the URL with template literals so multiple typed pieces work together as one coherent UI pattern.",
        "Use the transformResponse option typed as (raw: RawResponse) => TransformedType to normalize API data before it hits the cache so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Apply the providesTags option typed as (result, error, arg) => TagDescription<TagType>[] to associate cache tags with a query result so the learner sees how a typed pattern changes behavior, reuse, or safety in practice.",
        "Apply the invalidatesTags option on a mutation to automatically refetch all queries that provide the invalidated tag so the learner sees how a typed pattern changes behavior, reuse, or safety in practice so async data is handled with an explicit and safe data shape."
      ]
    },
    {
      "lesson": 122,
      "file": "inpact_ts122_engine.jsx",
      "title": "Redux Toolkit: Writing Your First createAsyncThunk — Typed",
      "objectives": [
        "Create a typed async thunk using createAsyncThunk<ReturnType, ArgType>('feature/action', async (arg, thunkAPI) => { ... }) so the design pattern is reusable and its contract is clear to downstream code.",
        "Use thunkAPI.rejectWithValue typed as (value: ErrorType) => RejectWithValue<ErrorType> to return structured error payloads so the pattern drives visible behavior and can be verified in the UI, runtime, or compiler.",
        "Read thunkAPI.getState() cast to RootState to access other slices inside the thunk body so the learner can explain what runtime information is driving the next decision.",
        "Dispatch the typed thunk from a component using the typed AppDispatch hook and handle the returned promise with .unwrap() so state transitions remain explicit, traceable, and type-safe."
      ]
    }
  ]
}