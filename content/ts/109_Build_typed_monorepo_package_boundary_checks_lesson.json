{
  "success": true,
  "config": {
    "lessonId": "ai-ts-109",
    "track": "ts",
    "lessonNum": 109,
    "title": "Build typed monorepo package boundary checks",
    "shortName": "P109",
    "intro": {
      "tag": "LESSON #109",
      "title": "Build typed monorepo package boundary checks",
      "body": "In this lesson, you will build a simple TypeScript tool that checks if code is correctly importing from allowed packages within a monorepo. You'll learn how to use TypeScript's type system to enforce boundaries between different packages, preventing unauthorized imports. The core concept involves creating a custom type guard and a validation function to analyze import statements. You'll work with key TypeScript constructs like interfaces, type aliases, and modules. By the end, you'll have a basic typed checker that helps maintain clean architecture in multi-package projects.",
      "usecase": "In large-scale applications like enterprise dashboards or e-commerce platforms, monorepo package boundary checks ensure that components and utilities are only imported from approved packages, preventing dependency violations. For example, a dashboard's analytics module should not directly import internal payment processing utilities, maintaining clean separation between business domains. This pattern is crucial when building modular UI libraries where design system components must be isolated from business logic packages. Developers implement these checks to enforce architectural boundaries, similar to how a complex form builder prevents its validation logic from accessing unrelated data-fetching modules."
    },
    "objectives": [
      "Construct a TypeScript monorepo structure with separate packages",
      "Configure package.json files with proper dependencies and versioning",
      "Implement type definitions for cross-package communication",
      "Apply TypeScript path mapping to resolve internal package imports",
      "Initialize type checking across package boundaries",
      "Manipulate tsconfig.json files to enforce strict type boundaries",
      "Integrate build tools to validate package dependencies"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 6",
        "title": "Import TypeScript and Node.js modules for monorepo structure",
        "instruction": "Import the fs, path, and typescript modules using ES module syntax with type annotations.",
        "hint": "Use import statements with the 'import' keyword. Remember to include type annotations for TypeScript.",
        "analogousExample": "// Example: importing Node.js modules\nimport fs from 'fs';\nimport path from 'path';\nimport ts from 'typescript';",
        "seedCode": "// Import fs, path, and typescript modules here",
        "expectedOutcome": "The fs, path, and typescript modules are imported and ready to use for monorepo boundary checking.",
        "successCriteria": [
          "fs, path, and typescript modules are imported from their respective packages."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've imported some modules, but make sure to import all three: fs, path, and typescript.",
        "feedbackWrong": "The imports are missing or incorrect. You need to import fs, path, and typescript modules.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "fs",
            "path",
            "typescript"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 6",
        "title": "Define TypeScript interfaces for package configuration",
        "instruction": "Create an interface named PackageConfig with properties: name (string), version (string), dependencies (optional Record<string, string>), and private (optional boolean).",
        "hint": "Use the 'interface' keyword followed by the interface name and define the properties with their types.",
        "analogousExample": "// Example: Interface for a user profile\ninterface UserProfile {\n  id: string;\n  name: string;\n  email?: string;\n  age?: number;\n}",
        "seedCode": "// Import fs, path, and typescript modules here\n\n// Create the PackageConfig interface here",
        "expectedOutcome": "You'll have a TypeScript interface that defines the structure for package configuration data.",
        "successCriteria": [
          "PackageConfig interface is defined with the required properties and correct types."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but check that all properties have the correct types and optional modifiers.",
        "feedbackWrong": "The interface definition is missing or incorrect. Make sure to use the 'interface' keyword and define all required properties.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "interface",
            "PackageConfig",
            "name:",
            "string",
            "version:",
            "string",
            "dependencies?:",
            "Record<string, string>",
            "private?:",
            "boolean"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 6",
        "title": "Create main PackageBoundaryChecker class",
        "instruction": "Define a TypeScript class named PackageBoundaryChecker with typed properties for packageConfig and tsConfig.",
        "hint": "Use the class keyword followed by the class name, then define properties with type annotations inside the class body.",
        "analogousExample": "// Example: a simple typed class\nclass DataValidator {\n  private schema: object;\n  private strictMode: boolean;\n  \n  constructor(schema: object, strictMode: boolean) {\n    this.schema = schema;\n    this.strictMode = strictMode;\n  }\n}",
        "seedCode": "// Import fs, path, and typescript modules here\n\n// Create the PackageConfig interface here\n\n// Define the PackageBoundaryChecker class here",
        "expectedOutcome": "A TypeScript class with typed properties is defined, ready for constructor implementation.",
        "successCriteria": [
          "PackageBoundaryChecker class is defined with typed properties packageConfig and tsConfig."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've started the class definition, but make sure to include both typed properties: packageConfig and tsConfig.",
        "feedbackWrong": "The class definition is missing or doesn't include the required typed properties. Review the instruction and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "class",
            "PackageBoundaryChecker",
            "packageConfig",
            "tsConfig",
            ":"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 6",
        "title": "Initialize package configuration data",
        "instruction": "Inside the PackageBoundaryChecker class, create a private property called 'packageConfigs' and initialize it with an empty array typed to your PackageConfig interface.",
        "hint": "You can declare a private property using TypeScript syntax like 'private packageConfigs: PackageConfig[] = []'.",
        "analogousExample": "// Similar: typed array initialization\nprivate items: Item[] = [];",
        "seedCode": "// Import fs, path, and typescript modules here\n\n// Create the PackageConfig interface here\n\n// Define the PackageBoundaryChecker class here\n\nclass PackageBoundaryChecker {\n  // Add packageConfigs property here\n}",
        "expectedOutcome": "The PackageBoundaryChecker class now has a typed array property ready to store package configurations.",
        "successCriteria": [
          "The class contains a private 'packageConfigs' property initialized as an empty array with the PackageConfig[] type."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure the property is private, has the correct type (PackageConfig[]), and is initialized as an empty array.",
        "feedbackWrong": "The packageConfigs property is missing or incorrectly typed. Review the TypeScript syntax for typed class properties.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "packageConfigs",
            "PackageConfig[]",
            "private",
            "=",
            "[]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 6",
        "title": "Create CLI entry point with typed arguments",
        "instruction": "Create a main function named runCLI that parses process.argv and accepts typed options for the package boundary checker.",
        "hint": "Define a function that takes command line arguments and processes them. You can use process.argv.slice(2) to get the arguments passed to the script.",
        "analogousExample": "// Example: CLI function with typed arguments\nfunction runCLI(args: string[]): void {\n  const options = {\n    configPath: args[0] || './packages',\n    verbose: args.includes('--verbose')\n  };\n  console.log('Running with options:', options);\n}",
        "seedCode": "// Import fs, path, and typescript modules here\n\n// Create the PackageConfig interface here\n\n// Define the PackageBoundaryChecker class here\n\nclass PackageBoundaryChecker {\n  // Add packageConfigs property here\n  \n  async readPackageJson(packagePath: string): Promise<PackageConfig> {\n    \n  }\n  \n  // Define validateDependencies method here\n  \n  // Add checkImportBoundaries method here\n  \n  // Define checkAllPackages method here\n}\n\n// Create CLI entry point function here",
        "expectedOutcome": "A runCLI function is defined that can process command line arguments with typed options.",
        "successCriteria": [
          "The runCLI function is created with proper TypeScript typing and processes process.argv arguments."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've started the CLI function, but make sure it properly accepts and processes the command line arguments with TypeScript typing.",
        "feedbackWrong": "The CLI function isn't properly defined. Remember to create a function named runCLI that handles process.argv with typed options.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "function runCLI",
            "process.argv",
            "args",
            "string[]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 6",
        "title": "Add error handling and result reporting",
        "instruction": "Add a try-catch block inside the checkAllPackages method to catch errors and return a typed result object with success status and error messages.",
        "hint": "Wrap the existing method logic in a try block, catch any errors, and return an object with properties like success (boolean) and errors (string array).",
        "analogousExample": "// Example: Function with typed error handling\nfunction processData(): { success: boolean; errors: string[] } {\n  try {\n    // Existing logic here\n    return { success: true, errors: [] };\n  } catch (error) {\n    return { success: false, errors: [error.message] };\n  }\n}",
        "seedCode": "// Import fs, path, and typescript modules here\n\n// Create the PackageConfig interface here\n\n// Define the PackageBoundaryChecker class here\n\nclass PackageBoundaryChecker {\n  // Add packageConfigs property here\n  \n  async readPackageJson(packagePath: string): Promise<PackageConfig> {\n    \n  }\n  \n  // Define validateDependencies method here\n  \n  // Add checkImportBoundaries method here\n  \n  // Define checkAllPackages method here\n  async checkAllPackages(): Promise<{ success: boolean; errors: string[] }> {\n    // Add try-catch block here\n  }\n}\n\n// Create CLI entry point function here",
        "expectedOutcome": "The checkAllPackages method will now safely handle errors and return a typed result object indicating success/failure with error details.",
        "successCriteria": [
          "The checkAllPackages method contains a try-catch block that returns an object with success (boolean) and errors (string[]) properties."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure the method returns the exact typed object structure with success and errors properties.",
        "feedbackWrong": "The error handling isn't complete. Remember to wrap the logic in a try-catch block and return the typed result object.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "try",
            "catch",
            "success",
            "errors",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import TypeScript and Node.js modules for monorepo structure",
        "id": "step1"
      },
      {
        "label": "Define TypeScript interfaces for package configuration",
        "id": "step2"
      },
      {
        "label": "Create main PackageBoundaryChecker class",
        "id": "step3"
      },
      {
        "label": "Initialize package configuration data",
        "id": "step4"
      },
      {
        "label": "Create CLI entry point with typed arguments",
        "id": "step5"
      },
      {
        "label": "Add error handling and result reporting",
        "id": "step6"
      }
    ]
  },
  "source": "content"
}