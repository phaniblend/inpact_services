{
  "success": true,
  "config": {
    "lessonId": "ai-ts-106",
    "track": "ts",
    "lessonNum": 106,
    "title": "Build typed OpenAPI client generator skeleton",
    "shortName": "P106",
    "intro": {
      "tag": "LESSON #106",
      "title": "Build typed OpenAPI client generator skeleton",
      "body": "In this lesson, you will build the skeleton for a typed OpenAPI client generator using TypeScript. You'll learn how to create a basic structure that can later be expanded to automatically generate type-safe API clients from OpenAPI specifications. The core concept involves using TypeScript's type system and interfaces to define the shape of API requests and responses. You'll work with key constructs like interfaces, type aliases, and basic file organization. By the end, you'll have a foundational project setup ready for integrating OpenAPI parsing logic.",
      "usecase": "This pattern is essential for building API clients in modern web applications like e-commerce platforms, where product listings need typed API calls to fetch and filter items. In dashboard tools such as analytics or admin panels, typed clients ensure safe data retrieval from multiple endpoints for charts and tables. Developers use this skeleton to generate reliable TypeScript clients from OpenAPI specs, reducing runtime errors in forms, search filters, and data submission flows. It's a foundational step for maintaining type safety across frontend-backend communication in scalable applications."
    },
    "objectives": [
      "Construct a TypeScript interface from an OpenAPI schema object",
      "Implement a function that generates TypeScript type definitions from parsed OpenAPI data",
      "Configure a TypeScript project to compile and output generated client code",
      "Initialize a basic code generation pipeline that reads and processes OpenAPI specifications",
      "Manipulate JSON schema structures to extract relevant type information for client generation",
      "Apply type safety patterns when generating API endpoint method signatures",
      "Integrate validation logic to ensure OpenAPI document compatibility before generation"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 13",
        "title": "Import TypeScript modules for OpenAPI parsing and file operations",
        "instruction": "Import the required modules: fs/promises for file operations, path for path manipulation, and yaml for YAML parsing.",
        "hint": "Use import statements at the top of your TypeScript file. Remember to use the correct module names: 'fs/promises', 'path', and 'yaml'.",
        "analogousExample": "// Example: importing multiple modules\nimport fs from 'fs/promises';\nimport path from 'path';\nimport yaml from 'yaml';",
        "seedCode": "// Import the required modules here",
        "expectedOutcome": "The file will have import statements for fs/promises, path, and yaml modules at the top.",
        "successCriteria": [
          "fs/promises, path, and yaml modules are imported using valid TypeScript import statements."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've imported some modules, but not all. Make sure to import fs/promises, path, and yaml.",
        "feedbackWrong": "The imports are missing or incorrect. You need to import fs/promises, path, and yaml modules.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "fs/promises",
            "path",
            "yaml"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 13",
        "title": "Define the OpenAPISpec interface",
        "instruction": "Define a TypeScript interface named OpenAPISpec with properties for openapi, info, paths, and components.",
        "hint": "Start with 'interface OpenAPISpec {' and add each property with its type. Use string for openapi and info, and Record<string, any> for paths and components.",
        "analogousExample": "// Example: a simple interface for a user\ninterface User {\n  id: number;\n  name: string;\n  email: string;\n}",
        "seedCode": "// Import the required modules here\n\n// Define the OpenAPISpec interface here",
        "expectedOutcome": "You will have an OpenAPISpec interface that defines the basic structure of an OpenAPI specification.",
        "successCriteria": [
          "An interface named OpenAPISpec is defined with properties openapi (string), info (any), paths (Record<string, any>), and components (Record<string, any>)."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've defined an interface, but it's missing some required properties. Make sure to include openapi, info, paths, and components.",
        "feedbackWrong": "The interface is not defined correctly. Please define an interface named OpenAPISpec with the specified properties.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "interface",
            "OpenAPISpec",
            "openapi",
            "info",
            "paths",
            "components"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 13",
        "title": "Define the Operation interface",
        "instruction": "Define a TypeScript interface named Operation with properties for summary (string), description (string), parameters (array of any), and responses (object).",
        "hint": "Create an interface using the 'interface' keyword followed by the name 'Operation' and define the required properties with their types.",
        "analogousExample": "// Similar: another TypeScript interface\ninterface User {\n  name: string;\n  age: number;\n  hobbies: string[];\n}",
        "seedCode": "// Import the required modules here\n\n// Define the OpenAPISpec interface here\n\n// Define the Operation interface here",
        "expectedOutcome": "An Operation interface is created with the specified properties and types.",
        "successCriteria": [
          "The Operation interface is defined with summary (string), description (string), parameters (any[]), and responses (Record<string, any>) properties."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but check that all properties (summary, description, parameters, responses) are included with correct types.",
        "feedbackWrong": "The Operation interface is not correctly defined. Make sure to use the 'interface' keyword and include all required properties with proper TypeScript types.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "interface",
            "Operation",
            "summary",
            "description",
            "parameters",
            "responses"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 13",
        "title": "Define the ClientFunction interface",
        "instruction": "Define a TypeScript interface named ClientFunction with properties: name (string), method (string), path (string), and parameters (array of strings).",
        "hint": "Create an interface with the specified properties and their types. Remember to use proper TypeScript syntax with colons for type annotations.",
        "analogousExample": "// Example: Another interface with typed properties\ninterface User {\n  id: number;\n  name: string;\n  email: string;\n}",
        "seedCode": "// Import the required modules here\n\n// Define the OpenAPISpec interface here\n\n// Define the Operation interface here\n\n// Define the ClientFunction interface here",
        "expectedOutcome": "A ClientFunction interface is defined with the correct property types.",
        "successCriteria": [
          "The interface ClientFunction exists with properties name: string, method: string, path: string, and parameters: string[]."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Check that all four properties (name, method, path, parameters) are defined with the correct TypeScript types.",
        "feedbackWrong": "The ClientFunction interface is not properly defined. Make sure to create an interface with the exact property names and types specified.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "interface",
            "ClientFunction",
            "name:",
            "string",
            "method:",
            "string",
            "path:",
            "string",
            "parameters:",
            "string[]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 13",
        "title": "Create the parseOpenAPIFile function",
        "instruction": "Create an async function named parseOpenAPIFile that takes a filePath parameter of type string and returns a Promise<OpenAPISpec>.",
        "hint": "Use the async keyword before function, specify the parameter type, and declare the return type as Promise<OpenAPISpec>. The function body can be empty for now.",
        "analogousExample": "// Similar: async function that reads a file\nasync function readConfigFile(filePath: string): Promise<Config> {\n  // Implementation would go here\n  return {} as Config;\n}",
        "seedCode": "// Import the required modules here\n\n// Define the OpenAPISpec interface here\n\n// Define the Operation interface here\n\n// Define the ClientFunction interface here\n\n// Create the parseOpenAPIFile function here",
        "expectedOutcome": "You'll have an async function declaration with the correct signature that can later be implemented to read and parse OpenAPI files.",
        "successCriteria": [
          "The parseOpenAPIFile function is declared as async, takes a string parameter named filePath, and returns Promise<OpenAPISpec>."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure your function is declared as async, has the filePath parameter with type string, and returns Promise<OpenAPISpec>.",
        "feedbackWrong": "Create an async function called parseOpenAPIFile that accepts a string parameter and returns Promise<OpenAPISpec>.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "async",
            "parseOpenAPIFile",
            "filePath",
            "string",
            "Promise<OpenAPISpec>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 13",
        "title": "Create the extractOperations function",
        "instruction": "Create a function named extractOperations that takes an OpenAPISpec object as a parameter and returns an array of ClientFunction objects.",
        "hint": "Define the function with proper TypeScript typing for the parameter and return type. The function body can return an empty array for now.",
        "analogousExample": "// Example of a function that processes data\nfunction processData(data: DataSpec): ProcessedItem[] {\n  return [];\n}",
        "seedCode": "// Import the required modules here\n\n// Define the OpenAPISpec interface here\n\n// Define the Operation interface here\n\n// Define the ClientFunction interface here\n\n// Create the parseOpenAPIFile function here\n\n// Create the extractOperations function here",
        "expectedOutcome": "You will have a function named extractOperations that accepts an OpenAPISpec and returns a ClientFunction array.",
        "successCriteria": [
          "The extractOperations function is defined with correct parameter and return types."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've started the function, but check the parameter type or return type. It should accept OpenAPISpec and return ClientFunction[].",
        "feedbackWrong": "The function is missing or has incorrect structure. Create a function named extractOperations that takes OpenAPISpec and returns ClientFunction[].",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "extractOperations",
            "OpenAPISpec",
            "ClientFunction[]",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 13",
        "title": "Create the generateClientCode function",
        "instruction": "Create a function named generateClientCode that takes an array of ClientFunction objects as a parameter and returns a string of TypeScript code.",
        "hint": "Define the function with a parameter typed as ClientFunction[] and a return type of string. You can start by returning an empty string for now.",
        "analogousExample": "// Example: a function that takes an array and returns a string\nfunction formatNames(names: string[]): string {\n  return names.join(', ');\n}",
        "seedCode": "// Import the required modules here\n\n// Define the OpenAPISpec interface here\n\n// Define the Operation interface here\n\n// Define the ClientFunction interface here\n\n// Create the parseOpenAPIFile function here\n\n// Create the extractOperations function here\n\n// Create the generateClientCode function here",
        "expectedOutcome": "A generateClientCode function is defined with the correct signature.",
        "successCriteria": [
          "The generateClientCode function exists, accepts a ClientFunction[] parameter, and returns a string."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure the function takes a ClientFunction[] parameter and returns a string.",
        "feedbackWrong": "The function is missing or has incorrect parameters or return type. Review the expected signature.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "generateClientCode",
            "ClientFunction[]",
            "string"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 13",
        "title": "Create the writeClientFile function",
        "instruction": "Create an async function named writeClientFile that takes two parameters: outputPath (string) and code (string), and writes the code to the file system using the fs module's writeFile.",
        "hint": "Use the fs module's writeFile function with await. Remember to handle the async operation properly.",
        "analogousExample": "// Similar: another async file writing function\nasync function saveData(filePath: string, data: string) {\n  await fs.writeFile(filePath, data);\n}",
        "seedCode": "// Import the required modules here\n\n// Define the OpenAPISpec interface here\n\n// Define the Operation interface here\n\n// Define the ClientFunction interface here\n\n// Create the parseOpenAPIFile function here\n\n// Create the extractOperations function here\n\n// Create the generateClientCode function here\n\n// Create the writeClientFile function here",
        "expectedOutcome": "The writeClientFile function is defined and ready to be used for writing generated client code to a file.",
        "successCriteria": [
          "The writeClientFile function is declared as async, takes outputPath (string) and code (string) parameters, and uses fs.writeFile to write the code to the file system."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure the function is async, takes the correct parameters, and properly uses fs.writeFile.",
        "feedbackWrong": "The function doesn't match the requirements. Check that it's async, has the right parameters, and uses fs.writeFile correctly.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "async",
            "writeClientFile",
            "outputPath",
            "code",
            "fs.writeFile"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 13",
        "title": "Create the main generateClient function",
        "instruction": "Create an async function named generateClient that takes two string parameters: inputPath and outputPath. Inside the function, call parseOpenAPIFile with inputPath,",
        "hint": "Remember to use await for the async function calls and handle the data flow from parsing to writing.",
        "analogousExample": "// Similar: an async orchestration function\nasync function processData(input: string, output: string) {\n  const raw = await parseInput(input);\n  const processed = transformData(raw);\n  const result = generateOutput(processed);\n  await writeResult(result, output);\n}",
        "seedCode": "// Import the required modules here\n\n// Define the OpenAPISpec interface here\n\n// Define the Operation interface here\n\n// Define the ClientFunction interface here\n\n// Create the parseOpenAPIFile function here\n\n// Create the extractOperations function here\n\n// Create the generateClientCode function here\n\n// Create the writeClientFile function here\n\n// Create the main generateClient function here",
        "expectedOutcome": "The generateClient function is defined and orchestrates the entire client generation process by calling the four helper functions in sequence.",
        "successCriteria": [
          "The generateClient function is declared as async, takes inputPath and outputPath parameters, and calls parseOpenAPIFile, extractOperations, generateClientCode, and writeClientFile in the correct order."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure the function is async, takes the correct parameters, and calls all four helper functions in sequence.",
        "feedbackWrong": "The generateClient function is missing or incorrect. It should be an async function that orchestrates the entire client generation process.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "async",
            "generateClient",
            "inputPath",
            "outputPath",
            "parseOpenAPIFile",
            "extractOperations",
            "generateClientCode",
            "writeClientFile"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 13",
        "title": "pass the result to extractOperations, then pa",
        "instruction": "pass the result to extractOperations,",
        "hint": "Remember to use await for the async function calls and handle the data flow from parsing to writing.",
        "analogousExample": "// Similar: an async orchestration function\nasync function processData(input: string, output: string) {\n  const raw = await parseInput(input);\n  const processed = transformData(raw);\n  const result = generateOutput(processed);\n  await writeResult(result, output);\n}",
        "seedCode": "// Import the required modules here\n\n// Define the OpenAPISpec interface here\n\n// Define the Operation interface here\n\n// Define the ClientFunction interface here\n\n// Create the parseOpenAPIFile function here\n\n// Create the extractOperations function here\n\n// Create the generateClientCode function here\n\n// Create the writeClientFile function here\n\n// Create the main generateClient function here",
        "expectedOutcome": "",
        "successCriteria": [
          "Complete the step as instructed."
        ],
        "feedbackCorrect": "Great! Step completed.",
        "feedbackPartial": "Review the instruction and try again.",
        "feedbackWrong": "Check the instruction and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "async",
            "generateClient",
            "inputPath",
            "outputPath",
            "parseOpenAPIFile",
            "extractOperations",
            "generateClientCode",
            "writeClientFile"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step11",
        "type": "question",
        "phase": "Step 11 of 13",
        "title": "pass that result to generateClientCode, and f",
        "instruction": "pass that result to generateClientCode, and finally call writeClientFile with the generated code and outputPath.",
        "hint": "Remember to use await for the async function calls and handle the data flow from parsing to writing.",
        "analogousExample": "// Similar: an async orchestration function\nasync function processData(input: string, output: string) {\n  const raw = await parseInput(input);\n  const processed = transformData(raw);\n  const result = generateOutput(processed);\n  await writeResult(result, output);\n}",
        "seedCode": "// Import the required modules here\n\n// Define the OpenAPISpec interface here\n\n// Define the Operation interface here\n\n// Define the ClientFunction interface here\n\n// Create the parseOpenAPIFile function here\n\n// Create the extractOperations function here\n\n// Create the generateClientCode function here\n\n// Create the writeClientFile function here\n\n// Create the main generateClient function here",
        "expectedOutcome": "",
        "successCriteria": [
          "Complete the step as instructed."
        ],
        "feedbackCorrect": "Great! Step completed.",
        "feedbackPartial": "Review the instruction and try again.",
        "feedbackWrong": "Check the instruction and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "async",
            "generateClient",
            "inputPath",
            "outputPath",
            "parseOpenAPIFile",
            "extractOperations",
            "generateClientCode",
            "writeClientFile"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step12",
        "type": "question",
        "phase": "Step 12 of 13",
        "title": "Add error handling to the main function",
        "instruction": "Wrap the logic inside the generateClient function in a try-catch block to handle errors gracefully.",
        "hint": "Place the existing function body inside a try block, and add a catch block to handle any errors that occur.",
        "analogousExample": "// Example: wrapping function logic in try-catch\nfunction processData() {\n  try {\n    // Existing logic here\n    console.log('Processing...');\n  } catch (error) {\n    console.error('Error occurred:', error);\n  }\n}",
        "seedCode": "// Import the required modules here\n\n// Define the OpenAPISpec interface here\n\n// Define the Operation interface here\n\n// Define the ClientFunction interface here\n\n// Create the parseOpenAPIFile function here\n\n// Create the extractOperations function here\n\n// Create the generateClientCode function here\n\n// Create the writeClientFile function here\n\n// Create the main generateClient function here\nfunction generateClient(openAPIFilePath: string, outputFilePath: string) {\n  // Wrap the following logic in a try-catch block\n  const spec = parseOpenAPIFile(openAPIFilePath);\n  const operations = extractOperations(spec);\n  const clientCode = generateClientCode(operations);\n  writeClientFile(outputFilePath, clientCode);\n  console.log(`Client generated successfully at ${outputFilePath}`);\n}",
        "expectedOutcome": "The generateClient function will now handle errors by catching them and logging an error message instead of crashing.",
        "successCriteria": [
          "The generateClient function contains a try block with the existing logic and a catch block that logs an error."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure the entire function body is inside the try block and you have a catch block to handle errors.",
        "feedbackWrong": "The generateClient function should have a try-catch block. Review the example and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "try",
            "catch",
            "generateClient"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step13",
        "type": "question",
        "phase": "Step 13 of 13",
        "title": "Export the main function and interfaces",
        "instruction": "Export the generateClient function and all three interfaces (OpenAPISpec, Operation, ClientFunction) from the module using named exports.",
        "hint": "Add export statements before each interface definition and the generateClient function declaration.",
        "analogousExample": "// Example of named exports\ninterface Person { name: string; }\nfunction greet() {}\nexport { Person, greet };",
        "seedCode": "// Import the required modules here\n\n// Define the OpenAPISpec interface here\n\n// Define the Operation interface here\n\n// Define the ClientFunction interface here\n\n// Create the parseOpenAPIFile function here\n\n// Create the extractOperations function here\n\n// Create the generateClientCode function here\n\n// Create the writeClientFile function here\n\n// Create the main generateClient function here\nfunction generateClient(openAPIFilePath: string, outputFilePath: string) {\n  // Wrap the following logic in a try-catch block\n  const spec = parseOpenAPIFile(openAPIFilePath);\n  const operations = extractOperations(spec);\n  const clientCode = generateClientCode(operations);\n  writeClientFile(outputFilePath, clientCode);\n  console.log(`Client generated successfully at ${outputFilePath}`);\n}",
        "expectedOutcome": "The module will export the main generateClient function and all three interfaces, making them available for import in other TypeScript files.",
        "successCriteria": [
          "The generateClient function and all three interfaces (OpenAPISpec, Operation, ClientFunction) are exported using named exports."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've exported some items, but make sure to export all three interfaces and the generateClient function.",
        "feedbackWrong": "The exports are missing or incorrect. Remember to use named exports for all three interfaces and the generateClient function.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "export",
            "OpenAPISpec",
            "Operation",
            "ClientFunction",
            "generateClient"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import TypeScript modules for OpenAPI parsing and file operations",
        "id": "step1"
      },
      {
        "label": "Define the OpenAPISpec interface",
        "id": "step2"
      },
      {
        "label": "Define the Operation interface",
        "id": "step3"
      },
      {
        "label": "Define the ClientFunction interface",
        "id": "step4"
      },
      {
        "label": "Create the parseOpenAPIFile function",
        "id": "step5"
      },
      {
        "label": "Create the extractOperations function",
        "id": "step6"
      },
      {
        "label": "Create the generateClientCode function",
        "id": "step7"
      },
      {
        "label": "Create the writeClientFile function",
        "id": "step8"
      },
      {
        "label": "Create the main generateClient function",
        "id": "step9"
      },
      {
        "label": "pass the result to extractOperations, then pa",
        "id": "step10"
      },
      {
        "label": "pass that result to generateClientCode, and f",
        "id": "step11"
      },
      {
        "label": "Add error handling to the main function",
        "id": "step12"
      },
      {
        "label": "Export the main function and interfaces",
        "id": "step13"
      }
    ]
  },
  "source": "content"
}