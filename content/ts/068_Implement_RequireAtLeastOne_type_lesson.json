{
  "success": true,
  "config": {
    "lessonId": "ai-ts-68",
    "track": "ts",
    "lessonNum": 68,
    "title": "Implement RequireAtLeastOne type",
    "shortName": "P68",
    "intro": {
      "tag": "LESSON #68",
      "title": "Implement RequireAtLeastOne type",
      "body": "In this lesson, you will build a custom TypeScript utility type called RequireAtLeastOne. This type ensures that at least one property from a given set is present in an object, making it more flexible than requiring all properties. You will learn to use mapped types and conditional types to iterate over keys and construct the required type. The main technologies involved are TypeScript's type system features like unions and the keyof operator. By the end, you will understand how to create precise type constraints for your objects.",
      "usecase": "The RequireAtLeastOne type is essential in TypeScript applications where you need flexible but validated configurations. In dashboard settings, you might have optional display properties where at least one must be selected—like requiring either a chart type or data source to be specified. Form validation often uses this pattern when users must provide at least one contact method (email OR phone) while keeping other fields optional. API request builders benefit from it by ensuring query parameters include at least one filter criterion without mandating all possible filters."
    },
    "objectives": [
      "Construct a generic type that accepts an object type as a parameter",
      "Manipulate mapped types to transform object properties",
      "Apply conditional types to enforce property requirements",
      "Integrate union types to create flexible type constraints",
      "Configure the RequireAtLeastOne type to ensure at least one property is present",
      "Implement type utilities that combine multiple TypeScript features",
      "Apply the RequireAtLeastOne type to real-world object scenarios"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Import TypeScript types and utilities",
        "instruction": "Add an import statement for the TypeScript utility types that will be used in this lesson.",
        "hint": "You'll need to import from 'typescript' or use built-in utilities. For this lesson, we'll use TypeScript's built-in utility types.",
        "analogousExample": "// Importing a utility type in TypeScript\ntype MyPartial<T> = Partial<T>;",
        "seedCode": "// Add your import here",
        "expectedOutcome": "The code will have the necessary TypeScript utility imports ready for the lesson.",
        "successCriteria": [
          "The import statement for TypeScript utility types is added."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure to import the correct TypeScript utilities.",
        "feedbackWrong": "The import statement is missing or incorrect. Check the instruction and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "type",
            "utility"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Create the RequireAtLeastOne type definition",
        "instruction": "Define a generic type called RequireAtLeastOne that takes a single type parameter T. Use the syntax: type RequireAtLeastOne<T> = ...",
        "hint": "Start with 'type RequireAtLeastOne<T> =' followed by the type definition. You'll implement the logic in the next step.",
        "analogousExample": "// Example: a generic type with a placeholder\ntype Wrapper<T> = T;",
        "seedCode": "// Add your import here\n\ntype RequireAtLeastOne<T> = // Define your generic type here",
        "expectedOutcome": "You'll have a generic type definition with a placeholder for the implementation.",
        "successCriteria": [
          "The code defines a generic type named RequireAtLeastOne with one type parameter T."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you've defined a type named RequireAtLeastOne with a single generic parameter T.",
        "feedbackWrong": "Define a generic type called RequireAtLeastOne with one type parameter T.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "type",
            "RequireAtLeastOne",
            "<T>",
            "="
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Add keyof constraint to the type",
        "instruction": "Add a constraint to the generic type parameter T in your RequireAtLeastOne definition to ensure it only accepts object types. Use the 'extends object' constraint.",
        "hint": "Add 'extends object' after the generic type parameter T in the type definition.",
        "analogousExample": "// Generic type with constraint\ntype KeysOfObject<T extends object> = keyof T;\n\n// Usage\ninterface Person {\n  name: string;\n  age: number;\n}\ntype PersonKeys = KeysOfObject<Person>; // 'name' | 'age'",
        "seedCode": "// Add your import here\n\ntype RequireAtLeastOne<T> = // Define your generic type here",
        "expectedOutcome": "The RequireAtLeastOne type will now only accept object types as its generic parameter, preventing invalid usage with non-object types.",
        "successCriteria": [
          "The RequireAtLeastOne type definition includes 'T extends object' constraint"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you've added 'extends object' to the generic type parameter T.",
        "feedbackWrong": "The constraint is missing. Add 'extends object' after the T in your type definition.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "RequireAtLeastOne",
            "<T",
            "extends",
            "object"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Create mapped type for all keys",
        "instruction": "Inside the RequireAtLeastOne type definition, create a mapped type that iterates over all keys in the generic type T.",
        "hint": "Use the syntax [K in keyof T] to create a mapped type that transforms each property key K.",
        "analogousExample": "// Example: mapped type that makes all properties optional\ntype Optional<T> = {\n  [K in keyof T]?: T[K];\n};",
        "seedCode": "// Add your import here\n\ntype RequireAtLeastOne<T> = {\n  // Create mapped type here\n}",
        "expectedOutcome": "The RequireAtLeastOne type should contain a mapped type that iterates over all keys of T.",
        "successCriteria": [
          "The type definition includes a mapped type using [K in keyof T] syntax."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're using the correct mapped type syntax with [K in keyof T].",
        "feedbackWrong": "The mapped type wasn't created correctly. Remember to use [K in keyof T] to iterate over all keys.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "[",
            "K",
            "in",
            "keyof",
            "T",
            "]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Make each property optional except one",
        "instruction": "Inside the mapped type, add a conditional type that makes each property optional by default, but ensures at least one property is required.",
        "hint": "Use the syntax `[K in keyof T]?: T[K]` to make all properties optional, but you'll need to combine this with another type to require at least one.",
        "analogousExample": "// Example: Making properties optional in a mapped type\ntype OptionalProps<T> = {\n  [K in keyof T]?: T[K]\n}",
        "seedCode": "// Add your import here\n\ntype RequireAtLeastOne<T> = {\n  // Create mapped type here\n  [K in keyof T]: T[K]\n}",
        "expectedOutcome": "The mapped type will have all properties marked as optional with the `?` modifier.",
        "successCriteria": [
          "The mapped type uses `[K in keyof T]?: T[K]` to make each property optional."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but check that you've added the `?` modifier after `keyof T` to make properties optional.",
        "feedbackWrong": "Try again. You need to modify the mapped type to make each property optional using the `?` syntax.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "[K in keyof T]?:",
            "T[K]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Create union type of all possible combinations",
        "instruction": "Transform your mapped type into a union type that represents all possible combinations where exactly one property is required.",
        "hint": "You'll need to use a mapped type that creates a union of objects where each key becomes required while others remain optional.",
        "analogousExample": "// Example: Union of possible required properties\ninterface Person {\n  name?: string;\n  age?: number;\n}\ntype RequireNameOrAge = { name: string; age?: number } | { name?: string; age: number };",
        "seedCode": "// Add your import here\n\ntype RequireAtLeastOne<T> = {\n  // Create mapped type here\n  [K in keyof T]: T[K]\n}",
        "expectedOutcome": "The RequireAtLeastOne type should now be a union type where each union member has exactly one required property from the original type.",
        "successCriteria": [
          "RequireAtLeastOne is defined as a union type using mapped types with the correct syntax."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but check your union type syntax. Make sure you're creating a proper union of all possible combinations.",
        "feedbackWrong": "The union type isn't correctly formed. Review how to create a union from a mapped type where each key becomes required.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "type",
            "RequireAtLeastOne",
            "=",
            "|",
            "{",
            "}",
            "in",
            "keyof"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Define test interface for validation",
        "instruction": "Define a TypeScript interface named 'TestProps' with three properties: 'name' of type string, 'age' of type number, and 'email' of type string.",
        "hint": "Create an interface using the 'interface' keyword followed by the interface name and property definitions inside curly braces.",
        "analogousExample": "// Example interface definition\ninterface User {\n  username: string;\n  id: number;\n  isActive: boolean;\n}",
        "seedCode": "// Add your import here\n\ntype RequireAtLeastOne<T> = {\n  // Create mapped type here\n  [K in keyof T]: T[K]\n}\n\n// Define your test interface here",
        "expectedOutcome": "You'll have a TestProps interface that can be used to test the RequireAtLeastOne type implementation.",
        "successCriteria": [
          "TestProps interface is defined with name (string), age (number), and email (string) properties."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've defined an interface, but it's missing some required properties. Make sure to include name (string), age (number), and email (string).",
        "feedbackWrong": "The interface definition is incorrect or missing. Use the 'interface' keyword to define TestProps with the three specified properties.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "interface",
            "TestProps",
            "name:",
            "string",
            "age:",
            "number",
            "email:",
            "string"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Apply RequireAtLeastOne to test interface",
        "instruction": "Create a type alias named 'TestRequirement' that applies the 'RequireAtLeastOne' generic type to your test interface.",
        "hint": "Use the syntax: type AliasName = GenericType<InterfaceName>",
        "analogousExample": "// Similar: applying a generic type to an interface\ninterface Person {\n  name: string;\n  age: number;\n}\n\ntype OptionalPerson = Partial<Person>;\n// OptionalPerson makes all Person properties optional",
        "seedCode": "// Add your import here\n\ntype RequireAtLeastOne<T> = {\n  // Create mapped type here\n  [K in keyof T]: T[K]\n}\n\n// Define your test interface here\n\n// Create your type alias here",
        "expectedOutcome": "You'll have a type alias that enforces at least one property from your test interface must be provided.",
        "successCriteria": [
          "A type alias named 'TestRequirement' is created that applies 'RequireAtLeastOne' to your test interface."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're creating a type alias named 'TestRequirement' that uses the RequireAtLeastOne generic type with your test interface.",
        "feedbackWrong": "The type alias wasn't created correctly. You need to create a type alias named 'TestRequirement' that applies the RequireAtLeastOne generic type to your test interface.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "type",
            "TestRequirement",
            "=",
            "RequireAtLeastOne",
            "<",
            ">"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "Create valid test objects",
        "instruction": "Create two test objects that satisfy the RequireAtLeastOne constraint by providing at least one property from the test interface.",
        "hint": "Define objects with the correct type annotation using your type alias. For example, provide only 'name', only 'age', or both.",
        "analogousExample": "// Example: creating typed objects\ninterface Person {\n  name?: string;\n  age?: number;\n}\n\nconst person1: Person = { name: 'Alice' };\nconst person2: Person = { age: 30 };",
        "seedCode": "// Add your import here\n\ntype RequireAtLeastOne<T> = {\n  // Create mapped type here\n  [K in keyof T]: T[K]\n}\n\n// Define your test interface here\n\n// Create your type alias here\n\n// Create your test objects here",
        "expectedOutcome": "Two test objects are created with the correct type annotation, each providing at least one property from the test interface.",
        "successCriteria": [
          "Two objects are defined with the type alias, each containing at least one property from the test interface."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created some objects, but ensure both have the correct type annotation and provide at least one property each.",
        "feedbackWrong": "No valid test objects were created. Please define two objects with the correct type annotation, each providing at least one property.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "const",
            ":",
            "TestType",
            "{",
            "name",
            "}",
            "{",
            "age",
            "}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Create invalid test objects to verify errors",
        "instruction": "Create two test objects that violate the RequireAtLeastOne constraint to verify TypeScript catches the errors.",
        "hint": "Create objects that have none of the required properties from your test interface, or that have incorrect property types.",
        "analogousExample": "// Example of invalid objects for a different type\ninterface Example {\n  name?: string;\n  age?: number;\n}\n\nconst invalid1 = {}; // Missing all properties\nconst invalid2 = { name: 123 }; // Wrong type for name",
        "seedCode": "// Add your import here\n\ntype RequireAtLeastOne<T> = {\n  // Create mapped type here\n  [K in keyof T]: T[K]\n}\n\n// Define your test interface here\n\n// Create your type alias here\n\n// Create your test objects here\n\n// Create invalid test objects here",
        "expectedOutcome": "You'll create two invalid test objects that TypeScript should flag as errors when using the RequireAtLeastOne type.",
        "successCriteria": [
          "Two invalid test objects are created that violate the RequireAtLeastOne constraint."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created some invalid objects, but make sure you have two distinct invalid cases that violate the constraint.",
        "feedbackWrong": "The invalid test objects don't properly demonstrate the constraint violation. Try creating objects that are missing all required properties or have incorrect property types.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "invalid",
            "test",
            "object",
            "const",
            "=",
            "{}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import TypeScript types and utilities",
        "id": "step1"
      },
      {
        "label": "Create the RequireAtLeastOne type definition",
        "id": "step2"
      },
      {
        "label": "Add keyof constraint to the type",
        "id": "step3"
      },
      {
        "label": "Create mapped type for all keys",
        "id": "step4"
      },
      {
        "label": "Make each property optional except one",
        "id": "step5"
      },
      {
        "label": "Create union type of all possible combinations",
        "id": "step6"
      },
      {
        "label": "Define test interface for validation",
        "id": "step7"
      },
      {
        "label": "Apply RequireAtLeastOne to test interface",
        "id": "step8"
      },
      {
        "label": "Create valid test objects",
        "id": "step9"
      },
      {
        "label": "Create invalid test objects to verify errors",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}