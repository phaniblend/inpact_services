{
  "success": true,
  "config": {
    "lessonId": "ai-ts-79",
    "track": "ts",
    "lessonNum": 79,
    "title": "Build typed finite workflow graph",
    "shortName": "P79",
    "intro": {
      "tag": "LESSON #79",
      "title": "Build typed finite workflow graph",
      "body": "In this lesson, you will build a typed finite workflow graph to model a simple process, like a user sign-up flow, using TypeScript. You'll learn how to define a graph structure where each node represents a state (e.g., 'start', 'pending', 'complete') and each edge represents a valid transition between states. The core concept involves using TypeScript's type system—specifically, union types and interfaces—to enforce that only allowed transitions can be defined, preventing invalid state changes. This will help you understand how to create type-safe, predictable workflows in your applications.",
      "usecase": "Typed finite workflow graphs are essential for modeling predictable state transitions in complex user interfaces. You'll see this pattern in multi-step forms like checkout processes, where each step (shipping → payment → confirmation) must follow specific rules. They're also used in dashboard wizards, approval workflows in admin panels, and interactive data filtering systems where UI states must remain valid and type-safe. As a developer, implementing these graphs ensures your application logic is maintainable, prevents invalid state transitions, and provides clear type definitions for every possible workflow path."
    },
    "objectives": [
      "Construct a typed finite workflow graph using TypeScript interfaces",
      "Implement state transitions with type-safe functions",
      "Configure workflow nodes with explicit type annotations",
      "Manipulate graph data structures while maintaining type integrity",
      "Initialize a workflow instance with validated input parameters",
      "Apply conditional logic to control state transitions",
      "Integrate type guards to ensure runtime type safety"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Import TypeScript types and interfaces",
        "instruction": "Create a new TypeScript file and define your own types and interfaces for this lesson. We'll start by creating a simple interface for a workflow node.",
        "hint": "Use the 'interface' keyword to define a TypeScript interface. You don't need to import anything yet - we're creating our own types.",
        "analogousExample": "// Defining a simple interface\ninterface User {\n  id: number;\n  name: string;\n  email: string;\n}",
        "seedCode": "// Create your TypeScript types and interfaces here\n",
        "expectedOutcome": "You'll have a TypeScript file with at least one interface defined for a workflow node.",
        "successCriteria": [
          "A TypeScript interface named 'WorkflowNode' is defined with properties for a workflow graph node"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you've defined an interface with properties that could represent a workflow node (like id, name, or status).",
        "feedbackWrong": "Create an interface using the 'interface' keyword followed by a name and property definitions in curly braces.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "interface",
            "WorkflowNode",
            "{",
            "}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Define Node interface for workflow states",
        "instruction": "Define a TypeScript interface named 'Node' with three properties: 'id' (string), 'label' (string), and 'type' (string).",
        "hint": "Use the 'interface' keyword followed by the interface name and property definitions with type annotations.",
        "analogousExample": "// Example interface definition\ninterface User {\n  id: string;\n  name: string;\n  role: string;\n}",
        "seedCode": "// Create your TypeScript types and interfaces here\n\n// Define Node interface here",
        "expectedOutcome": "A Node interface is created with the required typed properties.",
        "successCriteria": [
          "The Node interface is defined with id: string, label: string, and type: string properties."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure your Node interface has all three required properties with correct string type annotations.",
        "feedbackWrong": "The Node interface definition is missing or incorrect. Please define an interface named Node with id, label, and type properties, all typed as string.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "interface",
            "Node",
            "id:",
            "label:",
            "type:",
            "string"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Define Edge interface for transitions",
        "instruction": "Define an Edge interface with source, target, and label properties. Make all properties required strings.",
        "hint": "Create an interface named Edge with three properties: source, target, and label, all typed as string.",
        "analogousExample": "// Example: another interface with string properties\ninterface Connection {\n  from: string;\n  to: string;\n  type: string;\n}",
        "seedCode": "// Create your TypeScript types and interfaces here\n\n// Define Node interface here\n\n// Define Edge interface here",
        "expectedOutcome": "You will have a typed Edge interface representing transitions between workflow states.",
        "successCriteria": [
          "An Edge interface is defined with source, target, and label properties, all typed as string."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Check that all three properties (source, target, label) are present and typed as strings.",
        "feedbackWrong": "The Edge interface is missing or incorrect. Ensure you define an interface named Edge with three string properties.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "interface",
            "Edge",
            "source:",
            "target:",
            "label:",
            "string"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Define WorkflowGraph type",
        "instruction": "Create a WorkflowGraph type that contains nodes and edges arrays, using the Node and Edge interfaces you defined earlier.",
        "hint": "Define a new type alias with the 'type' keyword that has two properties: nodes (array of Node) and edges (array of Edge).",
        "analogousExample": "// Example: a type that groups related data\ntype Library = {\n  books: Book[];\n  members: Member[];\n};",
        "seedCode": "// Create your TypeScript types and interfaces here\n\n// Define Node interface here\n\n// Define Edge interface here\n\n// Define WorkflowGraph type here",
        "expectedOutcome": "A WorkflowGraph type is defined, combining the Node and Edge interfaces into a complete graph structure.",
        "successCriteria": [
          "The WorkflowGraph type is defined with nodes and edges properties, both typed as arrays of the respective interfaces."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your WorkflowGraph type includes both nodes and edges properties with the correct array types.",
        "feedbackWrong": "The WorkflowGraph type definition is missing or incorrect. It should have nodes (Node[]) and edges (Edge[]) properties.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "type",
            "WorkflowGraph",
            "nodes",
            "Node[]",
            "edges",
            "Edge[]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Create sample workflow data",
        "instruction": "Create a constant named 'sampleWorkflow' that implements the WorkflowGraph type with sample data.",
        "hint": "Define a constant with the WorkflowGraph type, providing arrays of nodes and edges that match your interface definitions.",
        "analogousExample": "// Example: typed constant implementing an interface\ninterface Person { name: string; age: number; }\nconst samplePerson: Person = { name: 'Alice', age: 30 };",
        "seedCode": "// Create your TypeScript types and interfaces here\n\n// Define Node interface here\ninterface Node {\n  id: string;\n  label: string;\n}\n\n// Define Edge interface here\ninterface Edge {\n  source: string;\n  target: string;\n  label: string;\n}\n\n// Define WorkflowGraph type here\ntype WorkflowGraph = {\n  nodes: Node[];\n  edges: Edge[];\n};\n\n// Create sample workflow data here\n",
        "expectedOutcome": "A constant 'sampleWorkflow' is defined with the WorkflowGraph type, containing sample nodes and edges.",
        "successCriteria": [
          "The constant 'sampleWorkflow' is declared with the WorkflowGraph type and contains valid arrays of nodes and edges."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Ensure 'sampleWorkflow' is typed as WorkflowGraph and contains both nodes and edges arrays.",
        "feedbackWrong": "The constant 'sampleWorkflow' is missing or doesn't match the WorkflowGraph type. Review the interface definitions.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "sampleWorkflow",
            "WorkflowGraph",
            "nodes",
            "edges"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Create NodeDisplay component",
        "instruction": "Create a functional component named NodeDisplay that accepts a single prop of type Node. The component should return a <div> element containing the node's label.",
        "hint": "Define the component with proper TypeScript typing for its props. Use the Node interface you created earlier.",
        "analogousExample": "// Example: typed component accepting props\ninterface Person {\n  name: string;\n}\n\nfunction PersonDisplay(props: Person) {\n  return <div>{props.name}</div>;\n}",
        "seedCode": "// Create your TypeScript types and interfaces here\n\n// Define Node interface here\ninterface Node {\n  id: string;\n  label: string;\n}\n\n// Define Edge interface here\ninterface Edge {\n  source: string;\n  target: string;\n  label: string;\n}\n\n// Define WorkflowGraph type here\ntype WorkflowGraph = {\n  nodes: Node[];\n  edges: Edge[];\n};\n\n// Create sample workflow data here\n\n// Create NodeDisplay component here",
        "expectedOutcome": "A NodeDisplay component that renders a node's label in a div element.",
        "successCriteria": [
          "NodeDisplay component is defined with proper TypeScript props typing and returns JSX containing the node label."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure your component accepts a prop of type Node and returns JSX containing the node's label.",
        "feedbackWrong": "The component doesn't meet the requirements. Check that you're using the Node interface for props and returning JSX with the label.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "NodeDisplay",
            "Node",
            "props",
            "label",
            "return",
            "<div>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Create EdgeDisplay component",
        "instruction": "Create a functional component named EdgeDisplay that accepts a single prop of type Edge and returns a paragraph element displaying the edge's source, target, and label.",
        "hint": "Define a component with props typed as { edge: Edge } and return JSX showing the edge details.",
        "analogousExample": "// Similar: component with typed props\ninterface Props {\n  item: string;\n}\n\nfunction ItemDisplay({ item }: Props) {\n  return <p>Item: {item}</p>;\n}",
        "seedCode": "// Create your TypeScript types and interfaces here\n\n// Define Node interface here\ninterface Node {\n  id: string;\n  label: string;\n}\n\n// Define Edge interface here\ninterface Edge {\n  source: string;\n  target: string;\n  label: string;\n}\n\n// Define WorkflowGraph type here\ntype WorkflowGraph = {\n  nodes: Node[];\n  edges: Edge[];\n};\n\n// Create sample workflow data here\n\n// Create NodeDisplay component here\n\n// Create EdgeDisplay component here",
        "expectedOutcome": "A new EdgeDisplay component is defined with proper TypeScript typing for its props.",
        "successCriteria": [
          "EdgeDisplay component exists, accepts an edge prop of type Edge, and returns a paragraph showing edge details."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created a component, but check that it accepts an edge prop with the correct type and returns the expected JSX.",
        "feedbackWrong": "The EdgeDisplay component is missing or doesn't match the required structure. Review the instruction and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "EdgeDisplay",
            "edge",
            "Edge",
            "return",
            "<p>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Create WorkflowGraphVisualizer component",
        "instruction": "Create a functional component named WorkflowGraphVisualizer that takes a WorkflowGraph as props and returns a wrapper div element.",
        "hint": "Define a component with props typed as WorkflowGraph and return a div element.",
        "analogousExample": "// Similar: component with typed props\ninterface Props {\n  data: string;\n}\n\nfunction DataDisplay({ data }: Props) {\n  return <div>{data}</div>;\n}",
        "seedCode": "// Create your TypeScript types and interfaces here\n\n// Define Node interface here\ninterface Node {\n  id: string;\n  label: string;\n}\n\n// Define Edge interface here\ninterface Edge {\n  source: string;\n  target: string;\n  label: string;\n}\n\n// Define WorkflowGraph type here\ntype WorkflowGraph = {\n  nodes: Node[];\n  edges: Edge[];\n};\n\n// Create sample workflow data here\n\n// Create NodeDisplay component here\n\n// Create EdgeDisplay component here\n\n// Create WorkflowGraphVisualizer component here",
        "expectedOutcome": "A WorkflowGraphVisualizer component is defined with typed props and returns a div element.",
        "successCriteria": [
          "The WorkflowGraphVisualizer component is created with proper TypeScript props typing and returns a div element."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure your component accepts WorkflowGraph props and returns a div element.",
        "feedbackWrong": "The component isn't quite right. Check that you've defined a component named WorkflowGraphVisualizer with typed props and a return statement.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "WorkflowGraphVisualizer",
            "WorkflowGraph",
            "return",
            "<div>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "Render the sample workflow",
        "instruction": "In your component, pass the sampleWorkflow data as a prop to the WorkflowGraphVisualizer component.",
        "hint": "Look for the WorkflowGraphVisualizer component you created earlier and pass sampleWorkflow to it using the appropriate prop name.",
        "analogousExample": "// Similar: passing data to a component\n<DisplayData data={myData} />",
        "seedCode": "// Create your TypeScript types and interfaces here\n\n// Define Node interface here\ninterface Node {\n  id: string;\n  label: string;\n}\n\n// Define Edge interface here\ninterface Edge {\n  source: string;\n  target: string;\n  label: string;\n}\n\n// Define WorkflowGraph type here\ntype WorkflowGraph = {\n  nodes: Node[];\n  edges: Edge[];\n};\n\n// Create sample workflow data here\nconst sampleWorkflow: WorkflowGraph = {\n  nodes: [\n    { id: 'start', label: 'Start' },\n    { id: 'process', label: 'Process' },\n    { id: 'end', label: 'End' }\n  ],\n  edges: [\n    { source: 'start', target: 'process', label: 'to process' },\n    { source: 'process', target: 'end', label: 'to end' }\n  ]\n};\n\n// Create NodeDisplay component here\nconst NodeDisplay = ({ node }: { node: Node }) => {\n  return <div>{node.label}</div>;\n};\n\n// Create EdgeDisplay component here\nconst EdgeDisplay = ({ edge }: { edge: Edge }) => {\n  return <div>{edge.label}</div>;\n};\n\n// Create WorkflowGraphVisualizer component here\nconst WorkflowGraphVisualizer = ({ graph }: { graph: WorkflowGraph }) => {\n  return (\n    <div>\n      {graph.nodes.map(node => (\n        <NodeDisplay key={node.id} node={node} />\n      ))}\n      {graph.edges.map(edge => (\n        <EdgeDisplay key={`${edge.source}-${edge.target}`} edge={edge} />\n      ))}\n    </div>\n  );\n};\n\n// Add your component here that renders the workflow\nconst WorkflowApp = () => {\n  return (\n    <div>\n      <WorkflowGraphVisualizer />\n    </div>\n  );\n};\n\nexport default WorkflowApp;",
        "expectedOutcome": "The WorkflowGraphVisualizer component receives the sampleWorkflow data and renders it.",
        "successCriteria": [
          "sampleWorkflow is passed as a prop to WorkflowGraphVisualizer."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're passing sampleWorkflow as a prop to WorkflowGraphVisualizer.",
        "feedbackWrong": "Try again. You need to pass the sampleWorkflow data to the WorkflowGraphVisualizer component.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "sampleWorkflow",
            "WorkflowGraphVisualizer",
            "graph={sampleWorkflow}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Add type safety validation",
        "instruction": "Create a function named validateWorkflow that takes a WorkflowGraph parameter and returns a boolean indicating whether the graph structure is valid.",
        "hint": "Check that all edges reference existing node IDs from the nodes array.",
        "analogousExample": "// Example: a validation function\nfunction validateWorkflow(graph: WorkflowGraph): boolean {\n  // Check that all edge sources and targets exist in nodes\n  const nodeIds = graph.nodes.map(node => node.id);\n  return graph.edges.every(edge => \n    nodeIds.includes(edge.source) && nodeIds.includes(edge.target)\n  );\n}",
        "seedCode": "// Create your TypeScript types and interfaces here\n\n// Define Node interface here\ninterface Node {\n  id: string;\n  label: string;\n}\n\n// Define Edge interface here\ninterface Edge {\n  source: string;\n  target: string;\n  label: string;\n}\n\n// Define WorkflowGraph type here\ntype WorkflowGraph = {\n  nodes: Node[];\n  edges: Edge[];\n};\n\n// Create sample workflow data here\nconst sampleWorkflow: WorkflowGraph = {\n  nodes: [\n    { id: 'start', label: 'Start' },\n    { id: 'process', label: 'Process' },\n    { id: 'end', label: 'End' }\n  ],\n  edges: [\n    { source: 'start', target: 'process', label: 'to process' },\n    { source: 'process', target: 'end', label: 'to end' }\n  ]\n};\n\n// Create NodeDisplay component here\nconst NodeDisplay = ({ node }: { node: Node }) => {\n  return <div>{node.label}</div>;\n};\n\n// Create EdgeDisplay component here\nconst EdgeDisplay = ({ edge }: { edge: Edge }) => {\n  return <div>{edge.label}</div>;\n};\n\n// Create WorkflowGraphVisualizer component here\nconst WorkflowGraphVisualizer = ({ graph }: { graph: WorkflowGraph }) => {\n  return (\n    <div>\n      {graph.nodes.map(node => (\n        <NodeDisplay key={node.id} node={node} />\n      ))}\n      {graph.edges.map(edge => (\n        <EdgeDisplay key={`${edge.source}-${edge.target}`} edge={edge} />\n      ))}\n    </div>\n  );\n};\n\n// Add your component here that renders the workflow\nconst WorkflowApp = () => {\n  return (\n    <div>\n      <WorkflowGraphVisualizer />\n    </div>\n  );\n};\n\n// Create validation function here\n",
        "expectedOutcome": "A validateWorkflow function that checks edge references against node IDs.",
        "successCriteria": [
          "The validateWorkflow function is defined with proper TypeScript typing and returns a boolean."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your function has the correct signature and validation logic.",
        "feedbackWrong": "The validation function is missing or doesn't match the expected structure. Try implementing a function that validates edge references.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "validateWorkflow",
            "WorkflowGraph",
            "boolean",
            "edges",
            "nodes",
            "source",
            "target"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import TypeScript types and interfaces",
        "id": "step1"
      },
      {
        "label": "Define Node interface for workflow states",
        "id": "step2"
      },
      {
        "label": "Define Edge interface for transitions",
        "id": "step3"
      },
      {
        "label": "Define WorkflowGraph type",
        "id": "step4"
      },
      {
        "label": "Create sample workflow data",
        "id": "step5"
      },
      {
        "label": "Create NodeDisplay component",
        "id": "step6"
      },
      {
        "label": "Create EdgeDisplay component",
        "id": "step7"
      },
      {
        "label": "Create WorkflowGraphVisualizer component",
        "id": "step8"
      },
      {
        "label": "Render the sample workflow",
        "id": "step9"
      },
      {
        "label": "Add type safety validation",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}