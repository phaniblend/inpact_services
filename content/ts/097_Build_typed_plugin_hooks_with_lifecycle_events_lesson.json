{
  "success": true,
  "config": {
    "lessonId": "ai-ts-97",
    "track": "ts",
    "lessonNum": 97,
    "title": "Build typed plugin hooks with lifecycle events",
    "shortName": "P97",
    "intro": {
      "tag": "LESSON #97",
      "title": "Build typed plugin hooks with lifecycle events",
      "body": "In this lesson, you will build a simple, typed plugin system that uses lifecycle events to manage plugins. You will learn how to use TypeScript interfaces to define the structure and expected behavior of a plugin, ensuring type safety. The core concept involves creating a hook manager that can register plugins and trigger specific events, like 'initialize' or 'cleanup', at the right times. You'll work with TypeScript's type system, interfaces, and arrays to handle plugin operations. By the end, you'll have a functional system where plugins can be added and their lifecycle methods called in a type-safe way.",
      "usecase": "Typed plugin hooks with lifecycle events are essential in enterprise applications like content management systems, e-commerce platforms, and developer tools. For example, in a dashboard builder like WordPress or Shopify, plugins need to initialize data when loaded, respond to user interactions in admin panels, and clean up resources when disabled. This pattern ensures plugins integrate safely with core systems—validating data on form submissions, managing state in analytics widgets, and handling cleanup in modal workflows. As a developer, you'll implement these hooks to create extensible, maintainable plugins that follow predictable lifecycle patterns."
    },
    "objectives": [
      "Construct a TypeScript interface to define plugin lifecycle events",
      "Implement a typed hook that exposes plugin lifecycle methods",
      "Configure a plugin manager to register and manage typed hooks",
      "Apply type constraints to ensure hook methods receive correct parameters",
      "Integrate lifecycle events with a plugin execution pipeline",
      "Manipulate plugin state through typed hook callbacks",
      "Initialize a plugin system with proper TypeScript type safety"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 9",
        "title": "Import TypeScript and define plugin lifecycle event types",
        "instruction": "Create type definitions for plugin lifecycle events: initialize, beforeMount, afterMount, beforeUnmount, and afterUnmount.",
        "hint": "Use the TypeScript 'type' keyword to define a union type containing the five lifecycle event strings.",
        "analogousExample": "// Example: defining a type for status values\ntype Status = 'pending' | 'active' | 'completed';",
        "seedCode": "// Define your plugin lifecycle event types here",
        "expectedOutcome": "A TypeScript type definition that includes all five lifecycle event strings as union members.",
        "successCriteria": [
          "A type named PluginLifecycleEvent is defined with the five required event strings as union members."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close, but make sure your type includes all five lifecycle events: initialize, beforeMount, afterMount, beforeUnmount, and afterUnmount.",
        "feedbackWrong": "The type definition is missing or incorrect. Create a type named PluginLifecycleEvent that includes all five lifecycle event strings.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "type",
            "PluginLifecycleEvent",
            "initialize",
            "beforeMount",
            "afterMount",
            "beforeUnmount",
            "afterUnmount"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 9",
        "title": "Define the Plugin interface with lifecycle methods",
        "instruction": "Define a TypeScript interface named 'Plugin' that includes optional lifecycle methods: 'onInit', 'onMount', and 'onUnmount', each typed as functions that return void.",
        "hint": "Use the 'interface' keyword followed by the interface name, then define the methods with optional '?' modifier and proper function type signatures.",
        "analogousExample": "// Example: defining an interface with optional methods\ninterface Device {\n  powerOn?(): void;\n  powerOff?(): void;\n}",
        "seedCode": "// Define your plugin lifecycle event types here\n\n// Define the Plugin interface below",
        "expectedOutcome": "A Plugin interface is defined with optional lifecycle methods matching the event types from step 1.",
        "successCriteria": [
          "The Plugin interface is defined with optional onInit, onMount, and onUnmount methods that return void."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure all three methods (onInit, onMount, onUnmount) are optional and return void.",
        "feedbackWrong": "The Plugin interface definition is missing or incorrect. Check that you've defined an interface with optional lifecycle methods.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "interface",
            "Plugin",
            "onInit?",
            "onMount?",
            "onUnmount?",
            "void"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 9",
        "title": "Create the PluginHook interface for managing plugins",
        "instruction": "Create a TypeScript interface named PluginHook with methods to manage plugin lifecycle. Define a method to register plugins and another to trigger lifecycle events.",
        "hint": "Think of PluginHook as a manager that will handle plugin registration and lifecycle event triggering. Use the Plugin interface you defined earlier as a parameter type.",
        "analogousExample": "// Example: Interface for a service manager\ninterface ServiceManager {\n  registerService(service: Service): void;\n  startAllServices(): void;\n}",
        "seedCode": "// Define your plugin lifecycle event types here\n\n// Define the Plugin interface below\n\n// Create the PluginHook interface here",
        "expectedOutcome": "A PluginHook interface with registerPlugin and triggerLifecycleEvent methods is added to the code.",
        "successCriteria": [
          "The PluginHook interface is defined with registerPlugin and triggerLifecycleEvent methods using proper TypeScript syntax."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've started the PluginHook interface, but make sure to include both registerPlugin and triggerLifecycleEvent methods with proper TypeScript signatures.",
        "feedbackWrong": "The PluginHook interface is missing or incorrect. Create an interface named PluginHook with methods to register plugins and trigger lifecycle events.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "interface",
            "PluginHook",
            "registerPlugin",
            "triggerLifecycleEvent"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 9",
        "title": "Implement the createPluginHook function",
        "instruction": "Create a function named createPluginHook that returns a PluginHook implementation. The function should take no parameters and return an object matching the PluginHook interface.",
        "hint": "Define a function that returns an object with the properties defined in the PluginHook interface (plugins, register, unregister, trigger). You can initialize plugins as an empty array.",
        "analogousExample": "// Similar: a factory function returning an interface implementation\nfunction createCounter() {\n  return {\n    count: 0,\n    increment() { this.count++ },\n    decrement() { this.count-- }\n  };\n}",
        "seedCode": "// Define your plugin lifecycle event types here\n\n// Define the Plugin interface below\n\n// Create the PluginHook interface here\n\n// Implement the createPluginHook function here",
        "expectedOutcome": "A createPluginHook function is implemented that returns a PluginHook object with the required methods and properties.",
        "successCriteria": [
          "The createPluginHook function is defined and returns an object with plugins array, register, unregister, and trigger methods."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your function returns an object with all the properties defined in the PluginHook interface.",
        "feedbackWrong": "The createPluginHook function is not properly implemented. It should return an object matching the PluginHook interface structure.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "createPluginHook",
            "return",
            "plugins",
            "register",
            "unregister",
            "trigger"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 9",
        "title": "Initialize the plugins array in createPluginHook",
        "instruction": "Inside the createPluginHook function, declare a private plugins array variable to store registered plugins. Initialize it as an empty array.",
        "hint": "You need to create a variable inside the function closure. Use the Plugin interface type for the array elements.",
        "analogousExample": "// Example: internal state in a closure\nfunction createCounter() {\n  let count = 0; // private variable\n  return {\n    increment() { count++; },\n    getCount() { return count; }\n  };\n}",
        "seedCode": "// Define your plugin lifecycle event types here\n\n// Define the Plugin interface below\n\n// Create the PluginHook interface here\n\n// Implement the createPluginHook function here\nfunction createPluginHook() {\n  // Initialize the plugins array here\n  \n  return {\n    // PluginHook methods will go here\n  };\n}",
        "expectedOutcome": "The createPluginHook function now has a private plugins array variable that will store registered plugin instances.",
        "successCriteria": [
          "A plugins array variable is declared inside createPluginHook and initialized as an empty array."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you've declared the plugins array variable inside the createPluginHook function and initialized it as an empty array.",
        "feedbackWrong": "The plugins array needs to be declared inside the createPluginHook function. Try adding a variable like 'plugins' and setting it to an empty array.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "plugins",
            "[]",
            "createPluginHook"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 9",
        "title": "Implement the registerPlugin method",
        "instruction": "Inside the createPluginHook function, add a registerPlugin method that takes a Plugin parameter and pushes it to the plugins array.",
        "hint": "The method should be part of the returned object and have a signature like registerPlugin(plugin: Plugin): void.",
        "analogousExample": "// Adding a method to an object\nfunction createStore() {\n  const items: string[] = [];\n  \n  return {\n    addItem(item: string) {\n      items.push(item);\n    }\n  };\n}",
        "seedCode": "// Define your plugin lifecycle event types here\n\n// Define the Plugin interface below\n\n// Create the PluginHook interface here\n\n// Implement the createPluginHook function here\nfunction createPluginHook() {\n  // Initialize the plugins array here\n  \n  return {\n    // PluginHook methods will go here\n  };\n}",
        "expectedOutcome": "The createPluginHook function now returns an object with a registerPlugin method that can add plugins to the internal array.",
        "successCriteria": [
          "The registerPlugin method is defined inside the returned object, accepts a Plugin parameter, and pushes it to the plugins array."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure registerPlugin is a method in the returned object that takes a Plugin parameter and pushes it to the plugins array.",
        "feedbackWrong": "The registerPlugin method isn't implemented correctly. It should be inside the returned object and add plugins to the array.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "registerPlugin",
            "plugin",
            "push",
            "plugins"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 9",
        "title": "Create the triggerLifecycleEvent helper function",
        "instruction": "Inside the createPluginHook function, create a helper function named triggerLifecycleEvent that takes an eventName parameter of type string and returns void.",
        "hint": "Define a function that will iterate through plugins and call their lifecycle methods. Start by declaring the function signature.",
        "analogousExample": "// Example: a helper function that processes items\nfunction processItems(items: string[]): void {\n  // Implementation goes here\n}",
        "seedCode": "// Define your plugin lifecycle event types here\n\n// Define the Plugin interface below\n\n// Create the PluginHook interface here\n\n// Implement the createPluginHook function here\nfunction createPluginHook() {\n  // Initialize the plugins array here\n  \n  // Create the triggerLifecycleEvent helper function here\n  \n  return {\n    // PluginHook methods will go here\n  };\n}",
        "expectedOutcome": "The createPluginHook function now contains a triggerLifecycleEvent helper function declaration with proper TypeScript typing.",
        "successCriteria": [
          "A function named triggerLifecycleEvent is defined inside createPluginHook with a string parameter eventName and void return type."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created a function, but check the name, parameter type, or return type. Make sure it's named triggerLifecycleEvent, takes a string parameter called eventName, and returns void.",
        "feedbackWrong": "The triggerLifecycleEvent helper function is not properly defined. Create a function inside createPluginHook with the correct signature.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "triggerLifecycleEvent",
            "eventName",
            "string",
            "void"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 9",
        "title": "Create a sample plugin implementation",
        "instruction": "Create a LoggerPlugin class that implements the Plugin interface with lifecycle methods that log to console.",
        "hint": "Define a class named LoggerPlugin that implements Plugin. Add the required lifecycle methods (initialize, mount, unmount) that each log a message to console.",
        "analogousExample": "// Example: a class implementing an interface\nclass Greeter implements Greeting {\n  greet() {\n    console.log('Hello!');\n  }\n}",
        "seedCode": "// Define your plugin lifecycle event types here\n\n// Define the Plugin interface below\n\n// Create the PluginHook interface here\n\n// Implement the createPluginHook function here\nfunction createPluginHook() {\n  // Initialize the plugins array here\n  \n  // Create the triggerLifecycleEvent helper function here\n  \n  return {\n    // PluginHook methods will go here\n    initialize: function() {\n      // Call triggerLifecycleEvent with 'initialize'\n    },\n    mount: function() {\n      // Implement mount method here\n    },\n    unmount: function() {\n      // Implement unmount method here\n    }\n  };\n}\n\n// Create the LoggerPlugin class here",
        "expectedOutcome": "A LoggerPlugin class is created that implements the Plugin interface with console logging in each lifecycle method.",
        "successCriteria": [
          "LoggerPlugin class exists, implements Plugin, and has initialize, mount, and unmount methods that each log a message to console."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure LoggerPlugin implements the Plugin interface and all three lifecycle methods (initialize, mount, unmount) log messages to console.",
        "feedbackWrong": "The LoggerPlugin class is missing or doesn't properly implement the Plugin interface with logging lifecycle methods.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "class",
            "LoggerPlugin",
            "implements",
            "Plugin",
            "initialize",
            "mount",
            "unmount",
            "console.log"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 9",
        "title": "Test the plugin hook system with the sample plugin",
        "instruction": "Create a plugin hook instance, register the LoggerPlugin, and call its initialize, mount, and unmount methods to test the lifecycle events.",
        "hint": "Instantiate the plugin hook using createPluginHook(), then call registerPlugin with a new LoggerPlugin instance. Finally, call each lifecycle method in sequence.",
        "analogousExample": "// Testing a similar system\nconst hook = createPluginHook();\nhook.registerPlugin(new SamplePlugin());\nhook.initialize();\nhook.mount();\nhook.unmount();",
        "seedCode": "// Define your plugin lifecycle event types here\n\n// Define the Plugin interface below\n\n// Create the PluginHook interface here\n\n// Implement the createPluginHook function here\nfunction createPluginHook() {\n  // Initialize the plugins array here\n  \n  // Create the triggerLifecycleEvent helper function here\n  \n  return {\n    // PluginHook methods will go here\n    initialize: function() {\n      // Call triggerLifecycleEvent with 'initialize'\n    },\n    mount: function() {\n      // Implement mount method here\n    },\n    unmount: function() {\n      // Implement unmount method here\n    }\n  };\n}\n\n// Create the LoggerPlugin class here\n\n// Test the plugin hook system here",
        "expectedOutcome": "The plugin hook system will be tested by creating an instance, registering the LoggerPlugin, and triggering all lifecycle events in sequence.",
        "successCriteria": [
          "A plugin hook instance is created, LoggerPlugin is registered, and initialize, mount, and unmount methods are called."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you create the hook instance, register the LoggerPlugin, and call all three lifecycle methods.",
        "feedbackWrong": "Check that you're creating the plugin hook instance, registering LoggerPlugin, and calling initialize, mount, and unmount methods.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "createPluginHook()",
            "registerPlugin",
            "LoggerPlugin",
            "initialize()",
            "mount()",
            "unmount()"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import TypeScript and define plugin lifecycle event types",
        "id": "step1"
      },
      {
        "label": "Define the Plugin interface with lifecycle methods",
        "id": "step2"
      },
      {
        "label": "Create the PluginHook interface for managing plugins",
        "id": "step3"
      },
      {
        "label": "Implement the createPluginHook function",
        "id": "step4"
      },
      {
        "label": "Initialize the plugins array in createPluginHook",
        "id": "step5"
      },
      {
        "label": "Implement the registerPlugin method",
        "id": "step6"
      },
      {
        "label": "Create the triggerLifecycleEvent helper function",
        "id": "step7"
      },
      {
        "label": "Create a sample plugin implementation",
        "id": "step8"
      },
      {
        "label": "Test the plugin hook system with the sample plugin",
        "id": "step9"
      }
    ]
  },
  "source": "content"
}