{
  "success": true,
  "config": {
    "lessonId": "ai-ts-28",
    "track": "ts",
    "lessonNum": 28,
    "title": "Build money currency branded type",
    "shortName": "P28",
    "intro": {
      "tag": "LESSON #28",
      "title": "Build money currency branded type",
      "body": "In this lesson, you will build a branded type to represent money currency, ensuring that values like 'USD' or 'EUR' are treated as distinct types to prevent mixing them accidentally. You'll learn about TypeScript's type branding technique, which adds a unique property to a base type to create a new, incompatible type. The main constructs involved are type aliases, literal types, and intersection types. By the end, you'll have a practical, type-safe way to handle different currencies in financial applications.",
      "usecase": "In financial applications like banking dashboards or e-commerce platforms, ensuring currency values are never confused with other numeric data is critical. For example, when displaying account balances, processing payments, or calculating taxes, using branded types prevents accidentally mixing USD amounts with EUR amounts or treating monetary values as simple numbers. This pattern appears in transaction forms where currency conversion must be type-safe, in reporting panels that aggregate different currencies, and in API responses that must guarantee currency integrity throughout the application."
    },
    "objectives": [
      "Implement a branded type for currency using TypeScript's intersection types",
      "Construct a type guard function to validate currency values at runtime",
      "Apply the branded currency type to function parameters and return types",
      "Manipulate currency values while preserving type safety through helper functions",
      "Configure a TypeScript project to recognize nominal typing patterns",
      "Integrate the branded currency type with existing primitive number operations",
      "Initialize variables with the branded currency type using type assertions"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 9",
        "title": "Import TypeScript type utilities",
        "instruction": "Import the TypeScript type utilities that will be used to create a branded type for currency. Add the import statement at the top of the file.",
        "hint": "You'll need to import from 'typescript' or use built-in TypeScript utilities. For this lesson, we'll use TypeScript's built-in type utilities that don't require explicit imports.",
        "analogousExample": "// Example: Importing type utilities\nimport { Omit, Pick } from 'typescript'",
        "seedCode": "// Import TypeScript type utilities here\n",
        "expectedOutcome": "The file will have the necessary imports for TypeScript type utilities to create branded types.",
        "successCriteria": [
          "TypeScript type utilities are imported or available for creating branded types."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're importing the correct type utilities for creating branded types.",
        "feedbackWrong": "That doesn't look right. Remember to import the TypeScript type utilities that help create branded types.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "type",
            "brand",
            "utility"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 9",
        "title": "Define the CurrencyBrand type",
        "instruction": "Define a unique symbol type that will serve as the brand identifier for currency types. Create a type alias named 'CurrencyBrand' that uses a unique symbol.",
        "hint": "Use the 'unique symbol' type in TypeScript to create a brand identifier that cannot be duplicated.",
        "analogousExample": "// Similar: creating a unique brand type for IDs\ntype IDBrand = {\n  readonly __brand: unique symbol;\n};",
        "seedCode": "// Import TypeScript type utilities here\n\n// Define the CurrencyBrand type here",
        "expectedOutcome": "A type alias 'CurrencyBrand' is created with a unique symbol property, establishing a brand identifier for currency types.",
        "successCriteria": [
          "The CurrencyBrand type is defined with a unique symbol property."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're defining a type alias named 'CurrencyBrand' that includes a unique symbol property.",
        "feedbackWrong": "The CurrencyBrand type wasn't defined correctly. Remember to create a type alias with a unique symbol property to serve as the brand identifier.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "type",
            "CurrencyBrand",
            "unique",
            "symbol"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 9",
        "title": "Create the Money type with brand",
        "instruction": "Create a Money type that brands a number with the CurrencyBrand to prevent mixing different currency values. Use a type alias that intersects a base number type with the CurrencyBrand.",
        "hint": "You can create a branded type by intersecting a base type (like number) with your brand type using the & operator.",
        "analogousExample": "// Similar: branded type for temperature\ntype Temperature = number & { readonly __brand: 'temperature' };",
        "seedCode": "// Import TypeScript type utilities here\n\n// Define the CurrencyBrand type here\n\n// Create the Money type with brand here",
        "expectedOutcome": "A Money type that prevents accidental mixing of currency values by branding numbers with the CurrencyBrand.",
        "successCriteria": [
          "The Money type is defined as a type alias that intersects number with CurrencyBrand."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure your Money type properly brands a number with the CurrencyBrand using type intersection.",
        "feedbackWrong": "The Money type should be a branded type that prevents mixing currency values. Try intersecting a number type with the CurrencyBrand.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "type",
            "Money",
            "number",
            "CurrencyBrand",
            "&"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 9",
        "title": "Create a type-safe constructor function",
        "instruction": "Create a function named 'createMoney' that takes a number parameter 'amount' and returns a value of type 'Money'.",
        "hint": "Define a function with proper TypeScript parameter and return type annotations. The function should return the amount cast to the Money type.",
        "analogousExample": "// Example: a type-safe constructor function\nfunction createId(value: string): UserId {\n    return value as UserId;\n}",
        "seedCode": "// Import TypeScript type utilities here\n\n// Define the CurrencyBrand type here\n\n// Create the Money type with brand here\n\n// Create a type-safe constructor function here",
        "expectedOutcome": "A function named createMoney that accepts a number and returns a Money value, providing type safety when creating money values.",
        "successCriteria": [
          "The createMoney function is defined with a number parameter and returns a Money type value."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure your function has a number parameter and returns a value of type Money.",
        "feedbackWrong": "Create a function named createMoney that takes a number and returns a Money value.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "createMoney",
            "amount",
            "number",
            "Money",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 9",
        "title": "Create a type guard function",
        "instruction": "Create a function named 'isMoney' that takes a value of type 'any' and returns a type predicate 'value is Money'.",
        "hint": "The function should check if the value is an object, has a 'currency' property of type 'string', and has an 'amount' property of type 'number'.",
        "analogousExample": "// Type guard for a branded type\nfunction isEmail(value: any): value is Email {\n  return typeof value === 'string' && value.includes('@');\n}",
        "seedCode": "// Import TypeScript type utilities here\n\n// Define the CurrencyBrand type here\n\n// Create the Money type with brand here\n\n// Create a type-safe constructor function here\n\n// Create a type guard function here",
        "expectedOutcome": "A type guard function 'isMoney' that validates runtime values and returns a type predicate.",
        "successCriteria": [
          "The 'isMoney' function is defined, takes a parameter of type 'any', returns 'value is Money', and includes runtime checks for the Money structure."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created a function, but it needs to return a type predicate 'value is Money' and include proper runtime checks.",
        "feedbackWrong": "The type guard function is missing or incorrect. It should validate the Money structure and return a type predicate.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "isMoney",
            "value is Money",
            "typeof",
            "object",
            "currency",
            "amount"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 9",
        "title": "Define a type-safe addition function",
        "instruction": "Create a function named 'addMoney' that takes two parameters of type 'Money' and returns a new 'Money' value with the sum of their amounts.",
        "hint": "The function should accept two Money values, add their amounts, and return a new Money object with the same brand.",
        "analogousExample": "// Similar: a function that adds two numbers with type safety\nfunction addNumbers(a: number, b: number): number {\n  return a + b;\n}",
        "seedCode": "// Import TypeScript type utilities here\nimport { Brand } from './types';\n\n// Define the CurrencyBrand type here\ntype CurrencyBrand = Brand<string, 'Currency'>;\n\n// Create the Money type with brand here\ntype Money = number & CurrencyBrand;\n\n// Create a type-safe constructor function here\nfunction createMoney(amount: number): Money {\n  return amount as Money;\n}\n\n// Create a type guard function here\nfunction isMoney(value: unknown): value is Money {\n  return typeof value === 'number';\n}\n\n// Create a type-safe addition function here\n",
        "expectedOutcome": "A function 'addMoney' is defined that properly adds two Money values while preserving type safety.",
        "successCriteria": [
          "The 'addMoney' function is created with correct parameter types (Money) and return type (Money), and it returns the sum of the amounts as a Money value."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure the function takes two Money parameters and returns a Money value with the correct type annotation.",
        "feedbackWrong": "The addition function is missing or incorrect. Remember to create a function named 'addMoney' that adds two Money values and returns a new Money value.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "addMoney",
            "Money",
            "return",
            "amount"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 9",
        "title": "Initialize test data with the constructor",
        "instruction": "Create several Money values using the createMoney constructor function to test the branded type implementation. Declare at least two variables with different amounts.",
        "hint": "Use the createMoney function you defined earlier to create typed Money values. For example: const salary = createMoney(5000);",
        "analogousExample": "// Creating typed values with a constructor\nconst temperature = createTemperature(25);\nconst humidity = createHumidity(65);",
        "seedCode": "// Import TypeScript type utilities here\nimport { Brand } from './types';\n\n// Define the CurrencyBrand type here\ntype CurrencyBrand = Brand<string, 'Currency'>;\n\n// Create the Money type with brand here\ntype Money = number & CurrencyBrand;\n\n// Create a type-safe constructor function here\nfunction createMoney(amount: number): Money {\n  return amount as Money;\n}\n\n// Create a type guard function here\nfunction isMoney(value: unknown): value is Money {\n  return typeof value === 'number';\n}\n\n// Create a type-safe addition function here\n\n// Create test Money values here\n",
        "expectedOutcome": "You will have created multiple Money values using the constructor function, demonstrating how to initialize properly typed currency values.",
        "successCriteria": [
          "At least two Money variables are declared using createMoney with different numeric amounts"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created some Money values, but try to create at least two different amounts to fully test the constructor function.",
        "feedbackWrong": "The test values weren't created correctly. Remember to use the createMoney function to create properly typed Money values with different amounts.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "createMoney",
            "const",
            "Money",
            "="
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 9",
        "title": "Test type safety with invalid operations",
        "instruction": "Write a line that attempts to add a Money value to a regular number to demonstrate TypeScript's type error.",
        "hint": "Use the addition operator (+) between a Money value (like testMoney1) and a plain number (like 10).",
        "analogousExample": "// Attempting invalid operation\ntestMoney1 + 10;",
        "seedCode": "// Import TypeScript type utilities here\nimport { Brand } from './types';\n\n// Define the CurrencyBrand type here\ntype CurrencyBrand = Brand<string, 'Currency'>;\n\n// Create the Money type with brand here\ntype Money = number & CurrencyBrand;\n\n// Create a type-safe constructor function here\nfunction createMoney(amount: number): Money {\n  return amount as Money;\n}\n\n// Create a type guard function here\nfunction isMoney(value: unknown): value is Money {\n  return typeof value === 'number';\n}\n\n// Create a type-safe addition function here\n\n// Create test Money values here\n\n// Test type safety with invalid operations here",
        "expectedOutcome": "You should see a TypeScript error when trying to add Money and number, demonstrating how branded types prevent mixing different types.",
        "successCriteria": [
          "The code includes an attempt to add a Money value to a regular number, triggering a TypeScript type error."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're trying to add a Money value (like testMoney1) directly to a regular number (like 10).",
        "feedbackWrong": "This step requires demonstrating type safety by attempting an invalid operation. Try adding a Money value to a plain number.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "+",
            "Money",
            "number"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 9",
        "title": "Use the type guard function",
        "instruction": "Create a function named validateAndAdd that takes two unknown parameters, uses the isMoney type guard to validate them, and returns their sum if both are valid Money values.",
        "hint": "Use the isMoney function to check each parameter, then cast them to Money and use addition.",
        "analogousExample": "// Example: Using a type guard to validate values\nfunction validateAndProcess(value: unknown) {\n  if (typeof value === 'string') {\n    return value.toUpperCase();\n  }\n  return null;\n}",
        "seedCode": "// Import TypeScript type utilities here\nimport { Brand } from './types';\n\n// Define the CurrencyBrand type here\ntype CurrencyBrand = Brand<string, 'Currency'>;\n\n// Create the Money type with brand here\ntype Money = number & CurrencyBrand;\n\n// Create a type-safe constructor function here\nfunction createMoney(amount: number): Money {\n  return amount as Money;\n}\n\n// Create a type guard function here\nfunction isMoney(value: unknown): value is Money {\n  return typeof value === 'number';\n}\n\n// Create a type-safe addition function here\n\n// Create test Money values here\n\n// Test type safety with invalid operations here\n\n// Create the validateAndAdd function here",
        "expectedOutcome": "A validateAndAdd function that safely validates unknown values using the type guard before performing Money addition.",
        "successCriteria": [
          "The validateAndAdd function is defined, uses isMoney to check both parameters, and returns their sum when both are valid Money values."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure to use the isMoney function to check both parameters and handle the case where they are valid Money values.",
        "feedbackWrong": "The function doesn't properly use the type guard to validate the parameters. Review how to apply type guards for runtime type checking.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "validateAndAdd",
            "isMoney",
            "Money",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import TypeScript type utilities",
        "id": "step1"
      },
      {
        "label": "Define the CurrencyBrand type",
        "id": "step2"
      },
      {
        "label": "Create the Money type with brand",
        "id": "step3"
      },
      {
        "label": "Create a type-safe constructor function",
        "id": "step4"
      },
      {
        "label": "Create a type guard function",
        "id": "step5"
      },
      {
        "label": "Define a type-safe addition function",
        "id": "step6"
      },
      {
        "label": "Initialize test data with the constructor",
        "id": "step7"
      },
      {
        "label": "Test type safety with invalid operations",
        "id": "step8"
      },
      {
        "label": "Use the type guard function",
        "id": "step9"
      }
    ]
  },
  "source": "content"
}