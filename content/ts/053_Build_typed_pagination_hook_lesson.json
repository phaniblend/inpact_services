{
  "success": true,
  "config": {
    "lessonId": "ai-ts-53",
    "track": "ts",
    "lessonNum": 53,
    "title": "Build typed pagination hook",
    "shortName": "P53",
    "intro": {
      "tag": "LESSON #53",
      "title": "Build typed pagination hook",
      "body": "In this lesson, you will build a reusable, typed custom React hook for pagination in TypeScript. You'll learn to manage pagination state—like the current page and items per page—using TypeScript generics to keep your data type-safe. The core concepts involve creating a custom hook with useState and useMemo, and structuring a return object with page data and navigation functions. By the end, you'll have a flexible hook that can paginate any array of items while ensuring full type safety throughout your application.",
      "usecase": "Typed pagination hooks are essential for managing large datasets in real-world applications like e-commerce product listings, admin dashboards with user tables, or content-heavy platforms displaying articles or search results. In these scenarios, developers need to handle paginated API responses while maintaining type safety for items, page numbers, and loading states. For example, when building a customer management dashboard, a typed pagination hook ensures that user data, current page, and total count are properly typed, preventing runtime errors and improving developer experience. This pattern also enables consistent pagination behavior across different components, such as data tables, product grids, or comment sections, while leveraging TypeScript's autocomplete and error checking."
    },
    "objectives": [
      "Implement a TypeScript interface to define pagination state structure",
      "Construct a custom React hook that manages pagination logic",
      "Initialize pagination state with typed default values",
      "Manipulate pagination state through typed handler functions",
      "Configure the hook to accept and enforce type-safe parameters",
      "Integrate the pagination hook with a component to control data display",
      "Apply TypeScript generics to make the pagination hook reusable across data types"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Import React and useState",
        "instruction": "Import React and useState from 'react'.",
        "hint": "Use the import statement to bring in React and the useState hook from the 'react' package.",
        "analogousExample": "// Example: importing React and a hook\nimport React, { useState } from 'react';",
        "seedCode": "// Import React and useState here",
        "expectedOutcome": "The React library and useState hook are imported, making them available for use in the custom pagination hook.",
        "successCriteria": [
          "React and useState are imported from 'react'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but ensure you've imported both React and useState from 'react'.",
        "feedbackWrong": "Check your import statement. You need to import React and useState from the 'react' package.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "React",
            "useState",
            "'react'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Define the PaginationState interface",
        "instruction": "Define a TypeScript interface named PaginationState with the following properties: currentPage (number), totalPages (number), pageSize (number), and totalItems (number).",
        "hint": "Use the 'interface' keyword followed by the interface name and property definitions with type annotations.",
        "analogousExample": "// Example: defining an interface for user data\ninterface UserData {\n  id: number;\n  name: string;\n  email: string;\n}",
        "seedCode": "import React, { useState } from 'react';\n// Define the PaginationState interface here",
        "expectedOutcome": "You'll have a properly typed interface that describes the shape of pagination state data.",
        "successCriteria": [
          "The PaginationState interface is defined with all four required properties and correct type annotations."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but the interface is missing some required properties or has incorrect type annotations. Check that you have currentPage, totalPages, pageSize, and totalItems, all typed as numbers.",
        "feedbackWrong": "The interface definition doesn't match the requirements. Make sure to define an interface named PaginationState with the four specified properties, all typed as numbers.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "interface",
            "PaginationState",
            "currentPage",
            "totalPages",
            "pageSize",
            "totalItems",
            "number"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Create the usePagination hook function",
        "instruction": "Create a function called usePagination that accepts a parameter named options of type PaginationOptions and returns a value of type PaginationState.",
        "hint": "Define a function with the name usePagination, specify its parameter with the correct type, and declare its return type.",
        "analogousExample": "// Example: a typed function that returns a state object\nfunction useCounter(initial: number): { count: number } {\n  return { count: initial };\n}",
        "seedCode": "import React, { useState } from 'react';\n\ninterface PaginationState {\n  currentPage: number;\n  pageSize: number;\n  totalItems: number;\n}\n\n// Define PaginationOptions interface here\n\n// Create the usePagination hook function here",
        "expectedOutcome": "A usePagination function is defined with proper TypeScript parameter and return type annotations.",
        "successCriteria": [
          "The code contains a function named usePagination that takes an options parameter of type PaginationOptions and declares a return type of PaginationState."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure the function is named usePagination, accepts an options parameter with type PaginationOptions, and specifies a return type of PaginationState.",
        "feedbackWrong": "The function definition is missing or incorrect. Create a function named usePagination that accepts options: PaginationOptions and returns PaginationState.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "usePagination",
            "options",
            "PaginationOptions",
            "PaginationState"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Initialize pagination state with useState",
        "instruction": "Inside the usePagination function, call useState to initialize pagination state with initial values derived from the options parameter.",
        "hint": "Use useState with an initial state object that matches the PaginationState interface, using values from the options parameter.",
        "analogousExample": "// Example: initializing state with useState\nconst [state, setState] = useState({ count: 0, name: 'initial' });",
        "seedCode": "import React, { useState } from 'react';\n\ninterface PaginationState {\n  currentPage: number;\n  pageSize: number;\n  totalItems: number;\n}\n\ninterface PaginationOptions {\n  initialPage?: number;\n  initialPageSize?: number;\n  totalItems: number;\n}\n\nfunction usePagination(options: PaginationOptions) {\n  // Initialize pagination state here\n}",
        "expectedOutcome": "The usePagination function will have a useState call that creates pagination state with initial values from options.",
        "successCriteria": [
          "useState is called inside usePagination with an initial state object containing currentPage, pageSize, and totalItems derived from options."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you're calling useState with an object that has currentPage, pageSize, and totalItems properties, using values from the options parameter.",
        "feedbackWrong": "Inside usePagination, you need to call useState to create state with initial values from the options parameter.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useState",
            "currentPage",
            "pageSize",
            "totalItems",
            "options"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Calculate totalPages in the state",
        "instruction": "Inside the usePagination hook, calculate totalPages by dividing totalItems by pageSize and rounding up using Math.ceil,",
        "hint": "Use Math.ceil(totalItems / pageSize) to get the total number of pages. Add this calculation to your state initialization.",
        "analogousExample": "// Similar: calculating derived state\nconst [state, setState] = useState(() => {\n  const derivedValue = Math.ceil(total / perPage);\n  return { value: 1, derivedValue };\n});",
        "seedCode": "import React, { useState } from 'react';\n\ninterface PaginationState {\n  currentPage: number;\n  pageSize: number;\n  totalItems: number;\n}\n\ninterface PaginationOptions {\n  initialPage?: number;\n  initialPageSize?: number;\n  totalItems: number;\n}\n\nfunction usePagination(options: PaginationOptions) {\n  // Initialize pagination state here\n  const [paginationState, setPaginationState] = useState<PaginationState>({\n    currentPage: options.initialPage || 1,\n    pageSize: options.initialPageSize || 10,\n    totalItems: options.totalItems,\n  });\n}",
        "expectedOutcome": "The pagination state now includes a totalPages property calculated from totalItems and pageSize.",
        "successCriteria": [
          "The useState initialization includes a totalPages property calculated with Math.ceil(totalItems / pageSize)."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure to include totalPages in the state object and use Math.ceil for proper rounding.",
        "feedbackWrong": "The calculation isn't quite right. Remember to divide totalItems by pageSize and round up using Math.ceil, then include it in the state.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "totalPages",
            "Math.ceil",
            "totalItems",
            "pageSize"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "include it in the state object returned from ",
        "instruction": "include it in the state object returned from useState.",
        "hint": "Use Math.ceil(totalItems / pageSize) to get the total number of pages. Add this calculation to your state initialization.",
        "analogousExample": "// Similar: calculating derived state\nconst [state, setState] = useState(() => {\n  const derivedValue = Math.ceil(total / perPage);\n  return { value: 1, derivedValue };\n});",
        "seedCode": "import React, { useState } from 'react';\n\ninterface PaginationState {\n  currentPage: number;\n  pageSize: number;\n  totalItems: number;\n}\n\ninterface PaginationOptions {\n  initialPage?: number;\n  initialPageSize?: number;\n  totalItems: number;\n}\n\nfunction usePagination(options: PaginationOptions) {\n  // Initialize pagination state here\n  const [paginationState, setPaginationState] = useState<PaginationState>({\n    currentPage: options.initialPage || 1,\n    pageSize: options.initialPageSize || 10,\n    totalItems: options.totalItems,\n  });\n}",
        "expectedOutcome": "",
        "successCriteria": [
          "Complete the step as instructed."
        ],
        "feedbackCorrect": "Great! Step completed.",
        "feedbackPartial": "Review the instruction and try again.",
        "feedbackWrong": "Check the instruction and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "totalPages",
            "Math.ceil",
            "totalItems",
            "pageSize"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Create the goToPage handler",
        "instruction": "Inside the usePagination hook, define a goToPage function that accepts a page number parameter and updates the paginationState if the page is within valid bounds (1 to totalPages).",
        "hint": "Create a function that takes a page number, checks if it's between 1 and totalPages, then calls setPaginationState with the new currentPage.",
        "analogousExample": "// Example: typed handler that updates state\nfunction updateScore(newScore: number) {\n  if (newScore >= 0 && newScore <= 100) {\n    setScore(newScore);\n  }\n}",
        "seedCode": "import React, { useState } from 'react';\n\ninterface PaginationState {\n  currentPage: number;\n  pageSize: number;\n  totalItems: number;\n}\n\ninterface PaginationOptions {\n  initialPage?: number;\n  initialPageSize?: number;\n  totalItems: number;\n}\n\nfunction usePagination(options: PaginationOptions) {\n  // Initialize pagination state here\n  const [paginationState, setPaginationState] = useState<PaginationState>({\n    currentPage: options.initialPage || 1,\n    pageSize: options.initialPageSize || 10,\n    totalItems: options.totalItems,\n  });\n\n  // Calculate totalPages\n  const totalPages = Math.ceil(paginationState.totalItems / paginationState.pageSize);\n\n  // Define goToPage handler here\n}",
        "expectedOutcome": "The hook now contains a typed goToPage handler function that validates page numbers before updating state.",
        "successCriteria": [
          "A goToPage function is defined that accepts a page number parameter, validates it's within 1 to totalPages, and calls setPaginationState with the updated currentPage."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've defined a goToPage function, but make sure it validates the page number is between 1 and totalPages before updating state.",
        "feedbackWrong": "The goToPage handler is missing or doesn't properly validate page numbers and update the pagination state.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "goToPage",
            "page",
            "totalPages",
            "setPaginationState"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Create the nextPage and prevPage handlers",
        "instruction": "Inside the usePagination hook, define a nextPage function that calls goToPage with currentPage + 1, and a prevPage function that calls goToPage with currentPage - 1.",
        "hint": "Both functions should be defined inside the hook and should use the existing goToPage handler to change pages.",
        "analogousExample": "// Example: convenience handlers that call another function\nfunction increment() {\n  setCount(count + 1);\n}\nfunction decrement() {\n  setCount(count - 1);\n}",
        "seedCode": "import React, { useState } from 'react';\n\ninterface PaginationState {\n  currentPage: number;\n  pageSize: number;\n  totalItems: number;\n}\n\ninterface PaginationOptions {\n  initialPage?: number;\n  initialPageSize?: number;\n  totalItems: number;\n}\n\nfunction usePagination(options: PaginationOptions) {\n  // Initialize pagination state here\n  const [paginationState, setPaginationState] = useState<PaginationState>({\n    currentPage: options.initialPage || 1,\n    pageSize: options.initialPageSize || 10,\n    totalItems: options.totalItems,\n  });\n\n  // Calculate totalPages\n  const totalPages = Math.ceil(paginationState.totalItems / paginationState.pageSize);\n\n  // Define goToPage handler here\n  const goToPage = (page: number) => {\n    // Implementation will be added later\n  };\n\n  // Define nextPage and prevPage handlers here\n}",
        "expectedOutcome": "The usePagination hook now includes nextPage and prevPage functions that will handle moving to the next and previous pages.",
        "successCriteria": [
          "The nextPage and prevPage functions are defined inside the hook and call goToPage with the appropriate page numbers."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've defined one of the handlers, but both nextPage and prevPage are needed.",
        "feedbackWrong": "The nextPage and prevPage handlers are not correctly defined or missing.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "nextPage",
            "prevPage",
            "goToPage",
            "currentPage",
            "+",
            "-"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "Return the hook's API",
        "instruction": "Return an object from the usePagination hook containing the pagination state and all handler functions (goToPage, nextPage, prevPage).",
        "hint": "At the end of your hook function, use the return keyword followed by an object with the state and handlers as properties.",
        "analogousExample": "// Example: returning values from a custom hook\nfunction useCounter() {\n  const [count, setCount] = useState(0);\n  const increment = () => setCount(count + 1);\n  return { count, increment };\n}",
        "seedCode": "import React, { useState } from 'react';\n\ninterface PaginationState {\n  currentPage: number;\n  pageSize: number;\n  totalItems: number;\n}\n\ninterface PaginationOptions {\n  initialPage?: number;\n  initialPageSize?: number;\n  totalItems: number;\n}\n\nfunction usePagination(options: PaginationOptions) {\n  // Initialize pagination state here\n  const [paginationState, setPaginationState] = useState<PaginationState>({\n    currentPage: options.initialPage || 1,\n    pageSize: options.initialPageSize || 10,\n    totalItems: options.totalItems,\n  });\n\n  // Calculate totalPages\n  const totalPages = Math.ceil(paginationState.totalItems / paginationState.pageSize);\n\n  // Define goToPage handler here\n  const goToPage = (page: number) => {\n    // Implementation will be added later\n  };\n\n  // Define nextPage and prevPage handlers here\n  \n  // Return the hook's API here\n}",
        "expectedOutcome": "The hook now returns an object with paginationState, totalPages, goToPage, nextPage, and prevPage, making these values available to components that use the hook.",
        "successCriteria": [
          "The usePagination hook returns an object containing paginationState, totalPages, goToPage, nextPage, and prevPage."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close, but make sure you're returning an object with all required properties: paginationState, totalPages, goToPage, nextPage, and prevPage.",
        "feedbackWrong": "The hook should return an object containing the pagination state and handler functions. Check that you're using the return keyword with the correct object structure.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "return",
            "{",
            "paginationState",
            "totalPages",
            "goToPage",
            "nextPage",
            "prevPage"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Export the usePagination hook",
        "instruction": "Export the usePagination function as the default export so it can be imported in other components.",
        "hint": "Add the export statement at the end of the file, after the function definition.",
        "analogousExample": "// Similar: exporting a function\nfunction calculateTotal() {}\nexport default calculateTotal;",
        "seedCode": "import React, { useState } from 'react';\n\ninterface PaginationState {\n  currentPage: number;\n  pageSize: number;\n  totalItems: number;\n}\n\ninterface PaginationOptions {\n  initialPage?: number;\n  initialPageSize?: number;\n  totalItems: number;\n}\n\nfunction usePagination(options: PaginationOptions) {\n  // Initialize pagination state here\n  const [paginationState, setPaginationState] = useState<PaginationState>({\n    currentPage: options.initialPage || 1,\n    pageSize: options.initialPageSize || 10,\n    totalItems: options.totalItems,\n  });\n\n  // Calculate totalPages\n  const totalPages = Math.ceil(paginationState.totalItems / paginationState.pageSize);\n\n  // Define goToPage handler here\n  const goToPage = (page: number) => {\n    // Implementation will be added later\n  };\n\n  // Define nextPage and prevPage handlers here\n  \n  // Return the hook's API here\n}\n\n// Add default export here",
        "expectedOutcome": "The usePagination hook is exported and ready to be used in other TypeScript components.",
        "successCriteria": [
          "The usePagination function is exported as the default export."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you're using the correct export syntax for default exports.",
        "feedbackWrong": "The hook isn't exported correctly. Remember to use 'export default usePagination' at the end of the file.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "export",
            "default",
            "usePagination"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import React and useState",
        "id": "step1"
      },
      {
        "label": "Define the PaginationState interface",
        "id": "step2"
      },
      {
        "label": "Create the usePagination hook function",
        "id": "step3"
      },
      {
        "label": "Initialize pagination state with useState",
        "id": "step4"
      },
      {
        "label": "Calculate totalPages in the state",
        "id": "step5"
      },
      {
        "label": "include it in the state object returned from ",
        "id": "step6"
      },
      {
        "label": "Create the goToPage handler",
        "id": "step7"
      },
      {
        "label": "Create the nextPage and prevPage handlers",
        "id": "step8"
      },
      {
        "label": "Return the hook's API",
        "id": "step9"
      },
      {
        "label": "Export the usePagination hook",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}