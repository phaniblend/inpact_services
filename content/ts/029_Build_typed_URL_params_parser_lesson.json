{
  "success": true,
  "config": {
    "lessonId": "ai-ts-29",
    "track": "ts",
    "lessonNum": 29,
    "title": "Build typed URL params parser",
    "shortName": "P29",
    "intro": {
      "tag": "LESSON #29",
      "title": "Build typed URL params parser",
      "body": "In this lesson, you will build a typed URL parameters parser in TypeScript. You will learn how to extract query parameters from a URL string and convert them into a structured, type-safe object. The core concept involves using TypeScript's type system to define the expected shape of the parameters and ensure type safety. You will work with built-in JavaScript Web APIs like URL and URLSearchParams to parse the URL. By the end, you'll have a reusable function that validates and returns typed parameters, enhancing code reliability and developer experience.",
      "usecase": "In real applications, typed URL parameter parsing is essential for building robust filtering systems in dashboards and data tables. For example, an e-commerce product listing page might use URL parameters like `?category=electronics&priceRange=100-500&sort=rating` to maintain filter state across page refreshes or when sharing links. This pattern also appears in analytics dashboards where users can save and share specific chart configurations through URL parameters. As a developer, implementing type-safe URL parsing ensures that your application correctly handles user inputs and prevents runtime errors from malformed parameters."
    },
    "objectives": [
      "Construct a TypeScript interface to define the expected structure of URL parameters",
      "Implement a function that parses URL query strings into typed objects",
      "Apply type guards to validate parameter values during parsing",
      "Configure the parser to handle optional and required parameters differently",
      "Integrate the typed parser into a simulated URL handling scenario",
      "Manipulate URL parameter objects using type-safe operations",
      "Initialize and test the parser with various URL query string examples"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 9",
        "title": "Import TypeScript types and utilities",
        "instruction": "Import Record and URLSearchParams from their respective modules. Use the 'Record' type from 'typescript' and 'URLSearchParams' from the global scope (no import needed for URLSearchParams in TypeScript).",
        "hint": "You'll need to import Record from 'typescript'. URLSearchParams is available globally in TypeScript, so you don't need to import it.",
        "analogousExample": "// Importing a type utility\nimport { Record } from 'typescript';\n// URLSearchParams is available globally",
        "seedCode": "// Import Record and URLSearchParams\n",
        "expectedOutcome": "The code will have the necessary imports for Record type and URLSearchParams utility.",
        "successCriteria": [
          "Record is imported from 'typescript' and URLSearchParams is available globally."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you're importing Record from 'typescript'. URLSearchParams doesn't need an import.",
        "feedbackWrong": "import Record from 'typescript'. URLSearchParams is available globally in TypeScript.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "Record",
            "typescript"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 9",
        "title": "Define the URL parameters type interface",
        "instruction": "Create an interface named 'URLParams' that defines the expected URL parameters with their types: 'id' as a string and 'page' as a number.",
        "hint": "Use the 'interface' keyword followed by the interface name and define the properties inside curly braces with type annotations.",
        "analogousExample": "// Example: defining an interface for user data\ninterface UserData {\n  name: string;\n  age: number;\n}",
        "seedCode": "// Import Record and URLSearchParams\n\n// Create the URLParams interface here",
        "expectedOutcome": "An interface named 'URLParams' with 'id: string' and 'page: number' properties will be defined.",
        "successCriteria": [
          "The interface 'URLParams' is defined with both 'id' (string) and 'page' (number) properties."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure both 'id' (string) and 'page' (number) properties are included in the interface.",
        "feedbackWrong": "The interface definition is missing or incorrect. Remember to use the 'interface' keyword and define both properties with their types.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "interface",
            "URLParams",
            "id:",
            "string",
            "page:",
            "number"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 9",
        "title": "Create the main parser function signature",
        "instruction": "Declare a function named 'parseUrlParams' that takes a generic type parameter 'T' constrained by the 'URLParams' interface, and returns a value of type 'T'.",
        "hint": "Use the 'function' keyword or arrow function syntax. The generic parameter should extend 'URLParams' to ensure it matches our interface.",
        "analogousExample": "// Generic function with constraint\nfunction parseData<T extends Record<string, string>>(input: string): T {\n  // Implementation would go here\n  return {} as T;\n}",
        "seedCode": "// Import Record and URLSearchParams\n\n// Create the URLParams interface here\n\n// Declare the parseUrlParams function with proper generics",
        "expectedOutcome": "A function signature for parseUrlParams is defined with TypeScript generics and proper return type.",
        "successCriteria": [
          "The parseUrlParams function is declared with a generic type parameter 'T extends URLParams' and returns type 'T'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure the function has a generic type parameter that extends URLParams and returns type T.",
        "feedbackWrong": "The function signature doesn't match the requirements. It needs a generic type parameter constrained by URLParams and should return type T.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "parseUrlParams",
            "T extends URLParams",
            "T"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 9",
        "title": "Initialize URLSearchParams object",
        "instruction": "Inside the parseUrlParams function, create a URLSearchParams instance from the input URL string.",
        "hint": "Use the URLSearchParams constructor with the URL's search string. You can get it by creating a new URL object or extracting the query string directly.",
        "analogousExample": "// Example: creating URLSearchParams from a URL string\nfunction parseQuery(url: string) {\n  const searchParams = new URLSearchParams(new URL(url).search);\n  // rest of function\n}",
        "seedCode": "// Import Record and URLSearchParams\n\n// Create the URLParams interface here\n\n// Declare the parseUrlParams function with proper generics\nfunction parseUrlParams<T extends Record<string, string>>(url: string): Partial<T> {\n  // Create URLSearchParams instance here\n}",
        "expectedOutcome": "The parseUrlParams function now has a URLSearchParams instance ready to extract query parameters.",
        "successCriteria": [
          "A URLSearchParams object is created inside the parseUrlParams function using the input URL string."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're creating the URLSearchParams object correctly from the URL string parameter.",
        "feedbackWrong": "The URLSearchParams instance wasn't created properly. Remember to use the URL string parameter to extract the query parameters.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "URLSearchParams",
            "new",
            "url"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 9",
        "title": "Iterate through parameter definitions",
        "instruction": "Inside the parseUrlParams function, add a for...of loop that iterates through Object.entries of the generic type parameter T to access each parameter name and type.",
        "hint": "Use Object.entries with the generic type T to get key-value pairs, then destructure them in the loop.",
        "analogousExample": "// Iterating through an object's entries\ninterface Config {\n  theme: string;\n  language: string;\n}\n\nfunction processConfig(config: Config) {\n  for (const [key, value] of Object.entries(config)) {\n    console.log(`${key}: ${value}`);\n  }\n}",
        "seedCode": "// Import Record and URLSearchParams\n\n// Create the URLParams interface here\n\n// Declare the parseUrlParams function with proper generics\nfunction parseUrlParams<T extends Record<string, string>>(url: string): Partial<T> {\n  // Create URLSearchParams instance here\n  \n  // Create result object with initial values\n  \n  // Iterate through parameter definitions here\n}",
        "expectedOutcome": "The function now has a loop that will iterate through each parameter definition from the generic type T.",
        "successCriteria": [
          "A for...of loop using Object.entries(T) is added inside the function body."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're using Object.entries with the generic type T and a proper for...of loop structure.",
        "feedbackWrong": "The iteration logic is missing or incorrect. Remember to use Object.entries(T) to loop through the parameter definitions.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "for",
            "of",
            "Object.entries",
            "T"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 9",
        "title": "Extract parameter value from URL",
        "instruction": "Inside the iteration loop, extract the current parameter's value from the URLSearchParams object.",
        "hint": "Use the .get() method on the URLSearchParams instance, passing the current parameter name as the argument.",
        "analogousExample": "// Example: retrieving a value from URLSearchParams\nconst params = new URLSearchParams('?name=John&age=25');\nconst nameValue = params.get('name'); // 'John'",
        "seedCode": "// Import Record and URLSearchParams\nimport { Record } from 'typescript';\n\n// Create the URLParams interface here\ninterface URLParams {\n  [key: string]: string;\n}\n\n// Declare the parseUrlParams function with proper generics\nfunction parseUrlParams<T extends Record<string, string>>(url: string): Partial<T> {\n  // Create URLSearchParams instance here\n  const searchParams = new URLSearchParams(url);\n  \n  // Create result object with initial values\n  const result: Partial<T> = {};\n  \n  // Iterate through parameter definitions here\n  const paramNames = Object.keys({} as T);\n  for (const paramName of paramNames) {\n    // Extract parameter value from URL\n  }\n}",
        "expectedOutcome": "The loop now retrieves each parameter's value from the URL search parameters.",
        "successCriteria": [
          "The .get() method is called on the URLSearchParams instance with the current parameter name."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're calling .get() on the searchParams object with the paramName variable.",
        "feedbackWrong": "That's not quite right. You need to retrieve the value using the URLSearchParams .get() method.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "searchParams",
            "get",
            "paramName"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 9",
        "title": "Implement type conversion logic",
        "instruction": "Inside the for loop, add conditional logic to convert the extracted string value to its defined type (number, boolean, or string) before assigning it to the result object.",
        "hint": "Use typeof checks or type guards to determine the target type and convert the string value appropriately. For numbers, use parseFloat or Number(); for booleans, check if the string equals 'true'.",
        "analogousExample": "// Example: Type conversion in a loop\nfor (const key of keys) {\n  const strValue = data[key];\n  if (typeof targetType === 'number') {\n    result[key] = parseFloat(strValue);\n  } else if (typeof targetType === 'boolean') {\n    result[key] = strValue === 'true';\n  } else {\n    result[key] = strValue;\n  }\n}",
        "seedCode": "// Import Record and URLSearchParams\nimport { Record } from 'typescript';\n\n// Create the URLParams interface here\ninterface URLParams {\n  [key: string]: string;\n}\n\n// Declare the parseUrlParams function with proper generics\nfunction parseUrlParams<T extends Record<string, string>>(url: string): Partial<T> {\n  // Create URLSearchParams instance here\n  const searchParams = new URLSearchParams(url);\n  \n  // Create result object with initial values\n  const result: Partial<T> = {};\n  \n  // Iterate through parameter definitions here\n  const paramNames = Object.keys({} as T);\n  for (const paramName of paramNames) {\n    // Extract parameter value from URL\n    const stringValue = searchParams.get(paramName);\n    // Add type conversion logic here\n  }\n}",
        "expectedOutcome": "The parseUrlParams function now includes logic to convert URL parameter string values to their appropriate types (number, boolean, or string) based on the generic type T.",
        "successCriteria": [
          "Conditional type conversion logic is implemented inside the for loop, handling number, boolean, and string conversions appropriately."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added some conversion logic, but make sure to handle all three types (number, boolean, string) with appropriate conditional checks.",
        "feedbackWrong": "The type conversion logic is missing or incorrect. Review how to convert string values to numbers and booleans using parseFloat/Number() and equality checks.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "parseFloat",
            "Number",
            "=== 'true'",
            "typeof",
            "if",
            "else"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 9",
        "title": "Assign converted value to result",
        "instruction": "Inside the for loop, assign the converted value to the corresponding property in the result object.",
        "hint": "Use bracket notation to assign the value to the result object using the parameter name as the key.",
        "analogousExample": "// Assigning a value to an object property\nconst user = {};\nuser['name'] = 'Alice';",
        "seedCode": "// Import Record and URLSearchParams\nimport { Record } from 'typescript';\n\n// Create the URLParams interface here\ninterface URLParams {\n  [key: string]: string;\n}\n\n// Declare the parseUrlParams function with proper generics\nfunction parseUrlParams<T extends Record<string, string>>(url: string): Partial<T> {\n  // Create URLSearchParams instance here\n  const searchParams = new URLSearchParams(url);\n  \n  // Create result object with initial values\n  const result: Partial<T> = {};\n  \n  // Iterate through parameter definitions here\n  const paramNames = Object.keys({} as T);\n  for (const paramName of paramNames) {\n    // Extract parameter value from URL\n    const stringValue = searchParams.get(paramName);\n    // Add type conversion logic here\n    // Assign converted value to result\n  }\n}",
        "expectedOutcome": "The result object will now contain properly typed values for each URL parameter.",
        "successCriteria": [
          "The result object has properties assigned with converted values from the URL parameters."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you're using bracket notation to assign the value to the result object with the parameter name as the key.",
        "feedbackWrong": "Inside the loop, you need to assign the converted value to the result object using the parameter name as the property key.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "result",
            "[",
            "paramName",
            "]",
            "="
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 9",
        "title": "Return the typed result object",
        "instruction": "Return the fully populated and typed result object from the parseUrlParams function.",
        "hint": "Add a return statement at the end of your function to output the result object.",
        "analogousExample": "// Similar: returning a typed object from a function\nfunction processData<T>(data: T): T {\n  // ... processing logic\n  return data;\n}",
        "seedCode": "// Import Record and URLSearchParams\nimport { Record } from 'typescript';\n\n// Create the URLParams interface here\ninterface URLParams {\n  [key: string]: string;\n}\n\n// Declare the parseUrlParams function with proper generics\nfunction parseUrlParams<T extends Record<string, string>>(url: string): Partial<T> {\n  // Create URLSearchParams instance here\n  const searchParams = new URLSearchParams(url);\n  \n  // Create result object with initial values\n  const result: Partial<T> = {};\n  \n  // Iterate through parameter definitions here\n  const paramNames = Object.keys({} as T);\n  for (const paramName of paramNames) {\n    // Extract parameter value from URL\n    const stringValue = searchParams.get(paramName);\n    // Add type conversion logic here\n    // Assign converted value to result\n  }\n  \n  // Return the typed result object\n}",
        "expectedOutcome": "The parseUrlParams function now returns the typed result object, completing the URL parameter parser implementation.",
        "successCriteria": [
          "The parseUrlParams function includes a return statement that outputs the result object."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you're returning the result object from the parseUrlParams function.",
        "feedbackWrong": "The function should return the result object. Add a return statement at the end of the function.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "return",
            "result"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import TypeScript types and utilities",
        "id": "step1"
      },
      {
        "label": "Define the URL parameters type interface",
        "id": "step2"
      },
      {
        "label": "Create the main parser function signature",
        "id": "step3"
      },
      {
        "label": "Initialize URLSearchParams object",
        "id": "step4"
      },
      {
        "label": "Iterate through parameter definitions",
        "id": "step5"
      },
      {
        "label": "Extract parameter value from URL",
        "id": "step6"
      },
      {
        "label": "Implement type conversion logic",
        "id": "step7"
      },
      {
        "label": "Assign converted value to result",
        "id": "step8"
      },
      {
        "label": "Return the typed result object",
        "id": "step9"
      }
    ]
  },
  "source": "content"
}