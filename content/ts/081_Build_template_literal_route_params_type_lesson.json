{
  "success": true,
  "config": {
    "lessonId": "ai-ts-81",
    "track": "ts",
    "lessonNum": 81,
    "title": "Build template literal route params type",
    "shortName": "P81",
    "intro": {
      "tag": "LESSON #81",
      "title": "Build template literal route params type",
      "body": "In this lesson, you will build a TypeScript type that extracts parameter names from a route string, like `:id` or `:username`, using template literal types. You'll learn how template literals can manipulate string types to create powerful, type-safe utilities. The main constructs involved are template literal types, conditional types, and string manipulation. By the end, you'll have a reusable type that can parse any route pattern and provide autocomplete for parameters.",
      "usecase": "Template literal route params types are essential in modern web frameworks like Next.js and React Router for building type-safe routing systems. Developers use these types to create dynamic routes like `/users/[id]/posts/[postId]` where the parameter segments are automatically extracted and validated. This pattern appears in dashboard applications where users navigate between different data views, e-commerce sites with product detail pages, or admin panels with resource-specific URLs. By implementing these types, developers catch routing errors at compile time, ensuring that path parameters match expected formats before runtime."
    },
    "objectives": [
      "Construct a generic type that extracts parameter names from route template literals",
      "Implement conditional type logic to process template literal segments",
      "Apply recursive type patterns to handle multiple route parameters",
      "Manipulate string literal types to isolate parameter identifiers",
      "Initialize a type utility that transforms route strings into parameter objects",
      "Integrate template literal types with conditional type inference",
      "Configure type constraints to validate route parameter syntax"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Import TypeScript utilities for template literal types",
        "instruction": "Import the necessary TypeScript utilities for working with template literal types. Import the 'ExtractRouteParams' type from the './types' module.",
        "hint": "Use the import statement to bring in the type from the specified module path.",
        "analogousExample": "// Importing a type from a module\nimport { User } from './models';",
        "seedCode": "// Import the ExtractRouteParams type from './types'",
        "expectedOutcome": "The ExtractRouteParams type is imported and available for use in the file.",
        "successCriteria": [
          "ExtractRouteParams is imported from './types'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're importing ExtractRouteParams from the correct module path './types'.",
        "feedbackWrong": "The import statement is missing or incorrect. You need to import ExtractRouteParams from './types' to proceed.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "ExtractRouteParams",
            "./types"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Create a basic template literal type for route patterns",
        "instruction": "Define a type named 'StaticRoute' using template literal syntax that represents a route pattern with static segments like '/users' or '/products'.",
        "hint": "Use the `type` keyword with backticks to create a template literal type. For now, just define it with a placeholder string literal.",
        "analogousExample": "// Example: a simple template literal type\ntype Greeting = `Hello, ${string}`;",
        "seedCode": "// Import the ExtractRouteParams type from './types'\n\n// Define a type named StaticRoute using template literal syntax",
        "expectedOutcome": "A StaticRoute type is defined with template literal syntax, ready to be expanded with dynamic segments later.",
        "successCriteria": [
          "The StaticRoute type is defined using the `type` keyword and template literal backticks."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using the `type` keyword and template literal syntax with backticks (`).",
        "feedbackWrong": "Define a type named StaticRoute using template literal syntax. Remember to use backticks around the pattern.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "type",
            "StaticRoute",
            "`"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Extract parameter names from route patterns",
        "instruction": "Create a conditional type named ExtractParam that extracts parameter names from route patterns. It should take a route string type and return the parameter name when the segment matches the pattern `:${string}`.",
        "hint": "Use a conditional type with template literal inference: `T extends `:${infer Param}` ? Param : never`",
        "analogousExample": "// Extracting a parameter from a pattern\ntype ExtractParam<T> = T extends `:${infer Param}` ? Param : never;",
        "seedCode": "// Import the ExtractRouteParams type from './types'\n\n// Define a type named StaticRoute using template literal syntax\n\n// Create a conditional type named ExtractParam that extracts parameter names from route patterns\n",
        "expectedOutcome": "You'll have a conditional type that can identify parameter segments in route patterns.",
        "successCriteria": [
          "The ExtractParam conditional type is defined and uses template literal inference to extract parameter names."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your conditional type uses template literal inference with `:${infer Param}` pattern.",
        "feedbackWrong": "Try again. You need to create a conditional type named ExtractParam that extracts parameter names when the segment matches `:${string}` pattern.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "type",
            "ExtractParam",
            "extends",
            "`",
            "infer",
            "Param",
            "?",
            "never"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Handle multiple parameters in route patterns",
        "instruction": "Update the ExtractParam type to recursively process multiple parameters by using a conditional type that splits on '/' and processes each segment.",
        "hint": "Use a conditional type with template literal inference to capture the first segment before '/' and the rest, then recursively apply ExtractParam to the rest.",
        "analogousExample": "// Similar: recursive type for splitting strings\ntype SplitPath<S extends string> = S extends `${infer First}/${infer Rest}` ? [First, ...SplitPath<Rest>] : [S];",
        "seedCode": "// Import the ExtractRouteParams type from './types'\n\n// Define a type named StaticRoute using template literal syntax\n\n// Create a conditional type named ExtractParam that extracts parameter names from route patterns\ntype ExtractParam<Route extends string> = Route extends `:${infer Param}` ? Param : never;",
        "expectedOutcome": "The ExtractParam type will now handle routes like '/users/:id/posts/:postId' by recursively extracting 'id' and 'postId'.",
        "successCriteria": [
          "ExtractParam uses a conditional type that splits the route string on '/' and recursively processes each segment to extract parameter names."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure ExtractParam splits the route on '/' and recursively processes each segment.",
        "feedbackWrong": "Check the structure of ExtractParam. It should use a conditional type with template literal inference to split the route and recursively extract parameters.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "ExtractParam",
            "extends",
            "infer",
            "?",
            ":",
            "never",
            "Rest",
            "recursively",
            "/"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Create the RouteParams type definition",
        "instruction": "Define a new type named RouteParams that uses ExtractParam to map parameter names from a route pattern to string values.",
        "hint": "You'll need to create a conditional type that takes a route string and returns an object type with keys from ExtractParam<Route> and string values.",
        "analogousExample": "// Example: mapping keys to values in a type\ntype Params<T extends string> = T extends `:${infer K}` ? { [P in K]: string } : never;",
        "seedCode": "// Import the ExtractRouteParams type from './types'\n\n// Define a type named StaticRoute using template literal syntax\n\n// Create a conditional type named ExtractParam that extracts parameter names from route patterns\ntype ExtractParam<Route extends string> = Route extends `:${infer Param}` ? Param : never;\n\n// Define the RouteParams type here\n",
        "expectedOutcome": "A RouteParams type is defined that will produce an object type mapping route parameters to strings.",
        "successCriteria": [
          "RouteParams is defined as a conditional type that uses ExtractParam to create a mapped type with string values."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure RouteParams is a conditional type that uses ExtractParam and creates a mapped type with string values.",
        "feedbackWrong": "The RouteParams type definition is missing or incorrect. It should be a conditional type that maps parameter names from ExtractParam to string values.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "type",
            "RouteParams",
            "ExtractParam",
            "{",
            "}",
            "string"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Test the RouteParams type with a simple route",
        "instruction": "Create a type alias named TestRouteParams that uses the RouteParams type with the route pattern '/users/:id'.",
        "hint": "Use the RouteParams type you defined earlier, passing it the literal string '/users/:id' as a type argument.",
        "analogousExample": "// Example: testing a type with a specific input\ntype TestResult = MyType<'example-input'>;",
        "seedCode": "// Import the ExtractRouteParams type from './types'\n\n// Define a type named StaticRoute using template literal syntax\n\n// Create a conditional type named ExtractParam that extracts parameter names from route patterns\ntype ExtractParam<Route extends string> = Route extends `:${infer Param}` ? Param : never;\n\n// Define the RouteParams type here\n\n// Test the RouteParams type with a simple route\n",
        "expectedOutcome": "A new type alias TestRouteParams is created that evaluates RouteParams<'/users/:id'>.",
        "successCriteria": [
          "TestRouteParams is defined as a type alias that uses RouteParams with the '/users/:id' route pattern."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you're creating a type alias named TestRouteParams that uses RouteParams with the exact string '/users/:id'.",
        "feedbackWrong": "The test case wasn't created correctly. You need to define a type alias that applies RouteParams to the route pattern '/users/:id'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "type",
            "TestRouteParams",
            "RouteParams",
            "'/users/:id'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Test with multiple parameters in a route",
        "instruction": "Create a new type alias named 'MultiRouteParams' that uses your RouteParams type with a route pattern containing two parameters, like '/users/:userId/posts/:postId'.",
        "hint": "Use the RouteParams type you defined earlier, passing it a template literal string with two colon-prefixed parameters separated by static segments.",
        "analogousExample": "// Example: Testing a type with multiple parameters\ntype MultiTest = RouteParams<'/api/:resource/:id'>;",
        "seedCode": "// Import the ExtractRouteParams type from './types'\n\n// Define a type named StaticRoute using template literal syntax\n\n// Create a conditional type named ExtractParam that extracts parameter names from route patterns\ntype ExtractParam<Route extends string> = Route extends `:${infer Param}` ? Param : never;\n\n// Define the RouteParams type here\n\n// Test the RouteParams type with a simple route\n\n// Test with multiple parameters in a route\n",
        "expectedOutcome": "The MultiRouteParams type should correctly infer an object type with both parameter names as keys, like { userId: string; postId: string; }.",
        "successCriteria": [
          "MultiRouteParams is defined as a type alias that uses RouteParams with a route pattern containing two parameters."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using the RouteParams type with a route pattern that has two colon-prefixed parameters, like '/users/:userId/posts/:postId'.",
        "feedbackWrong": "The MultiRouteParams type isn't defined correctly. It should be a type alias that uses RouteParams with a route pattern containing two parameters.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "MultiRouteParams",
            "RouteParams",
            "/users/:userId/posts/:postId"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Handle optional parameters in routes",
        "instruction": "Update the ExtractParam type to handle optional parameters marked with ? in route patterns.",
        "hint": "You'll need to extend the conditional type to check for patterns like ':param?' and extract just the parameter name without the question mark.",
        "analogousExample": "// Example: extracting optional parameter names\ntype ExtractOptional<Route> = Route extends `:${infer Param}?` ? Param : never;\n\n// This would extract 'id' from ':id?'",
        "seedCode": "// Import the ExtractRouteParams type from './types'\n\n// Define a type named StaticRoute using template literal syntax\n\n// Create a conditional type named ExtractParam that extracts parameter names from route patterns\ntype ExtractParam<Route extends string> = Route extends `:${infer Param}` ? Param : never;\n\n// Define the RouteParams type here\n\n// Test the RouteParams type with a simple route\n\n// Test with multiple parameters in a route",
        "expectedOutcome": "The ExtractParam type will correctly extract parameter names from both required parameters (':param') and optional parameters (':param?').",
        "successCriteria": [
          "The ExtractParam type handles optional parameters by matching patterns that end with '?' and extracting the parameter name without the question mark."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your type extracts just the parameter name without the '?' character for optional parameters.",
        "feedbackWrong": "The ExtractParam type should handle both required and optional parameters. Check that your conditional type matches patterns ending with '?' and extracts only the parameter name.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "ExtractParam",
            "extends",
            "`",
            "infer",
            "Param",
            "?",
            "never"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "Test the type with mixed required and optional parameters",
        "instruction": "Test the RouteParams type with a route containing both required and optional parameters. Create a type alias named 'MixedRouteParams' that uses RouteParams with the pattern '/users/:userId/posts/:postId?'.",
        "hint": "Use the RouteParams type you defined earlier with a template literal route pattern that includes both required (:userId) and optional (:postId?) parameters.",
        "analogousExample": "// Testing a type with mixed parameters\ntype MixedRouteParams = RouteParams<'/users/:userId/posts/:postId?'>;",
        "seedCode": "// Import the ExtractRouteParams type from './types'\n\n// Define a type named StaticRoute using template literal syntax\n\n// Create a conditional type named ExtractParam that extracts parameter names from route patterns\ntype ExtractParam<Route extends string> = Route extends `:${infer Param}` ? Param : never;\n\n// Define the RouteParams type here\n\n// Test the RouteParams type with a simple route\n\n// Test with multiple parameters in a route\n\n// Test the type with mixed required and optional parameters\n",
        "expectedOutcome": "You should see that MixedRouteParams correctly infers { userId: string; postId?: string } from the route pattern '/users/:userId/posts/:postId?'.",
        "successCriteria": [
          "MixedRouteParams is defined using RouteParams with the pattern '/users/:userId/posts/:postId?' and correctly infers both required and optional parameters."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using the RouteParams type with the exact pattern '/users/:userId/posts/:postId?'.",
        "feedbackWrong": "Check that you're using the RouteParams type with a route pattern containing both required (:userId) and optional (:postId?) parameters.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "MixedRouteParams",
            "RouteParams",
            "'/users/:userId/posts/:postId?'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Create a utility function using the RouteParams type",
        "instruction": "Create a function named 'getRouteParams' that takes a route string and returns an object of type RouteParams for that route.",
        "hint": "Define a function that accepts a route parameter and returns an empty object typed with RouteParams<typeof route>.",
        "analogousExample": "// Similar: a function that returns typed data\nfunction getConfig<T>(key: string): T {\n  return {} as T;\n}",
        "seedCode": "// Import the ExtractRouteParams type from './types'\n\n// Define a type named StaticRoute using template literal syntax\n\n// Create a conditional type named ExtractParam that extracts parameter names from route patterns\ntype ExtractParam<Route extends string> = Route extends `:${infer Param}` ? Param : never;\n\n// Define the RouteParams type here\n\n// Test the RouteParams type with a simple route\n\n// Test with multiple parameters in a route\n\n// Test the type with mixed required and optional parameters\n\n// Create the getRouteParams function here",
        "expectedOutcome": "A utility function is implemented that leverages the RouteParams type for type-safe parameter extraction.",
        "successCriteria": [
          "The getRouteParams function is defined, accepts a route string, and returns an object typed with RouteParams<typeof route>."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure your function returns an object typed with RouteParams<typeof route>.",
        "feedbackWrong": "The function isn't implemented correctly. Check that it accepts a route parameter and returns the proper type.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "function",
            "getRouteParams",
            "route",
            "RouteParams",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import TypeScript utilities for template literal types",
        "id": "step1"
      },
      {
        "label": "Create a basic template literal type for route patterns",
        "id": "step2"
      },
      {
        "label": "Extract parameter names from route patterns",
        "id": "step3"
      },
      {
        "label": "Handle multiple parameters in route patterns",
        "id": "step4"
      },
      {
        "label": "Create the RouteParams type definition",
        "id": "step5"
      },
      {
        "label": "Test the RouteParams type with a simple route",
        "id": "step6"
      },
      {
        "label": "Test with multiple parameters in a route",
        "id": "step7"
      },
      {
        "label": "Handle optional parameters in routes",
        "id": "step8"
      },
      {
        "label": "Test the type with mixed required and optional parameters",
        "id": "step9"
      },
      {
        "label": "Create a utility function using the RouteParams type",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}