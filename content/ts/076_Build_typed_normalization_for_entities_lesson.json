{
  "success": true,
  "config": {
    "lessonId": "ai-ts-76",
    "track": "ts",
    "lessonNum": 76,
    "title": "Build typed normalization for entities",
    "shortName": "P76",
    "intro": {
      "tag": "LESSON #76",
      "title": "Build typed normalization for entities",
      "body": "In this lesson, you will build a typed normalization system for entity data, such as users or products, using TypeScript. You'll learn to transform an array of objects into a normalized structure with separate `ids` and `entities` objects, making data access more efficient. The core concept involves using TypeScript generics and interfaces to enforce type safety throughout the normalization process. You'll work with key TypeScript constructs like `Record` and `Pick` to define and manipulate types. By the end, you'll have a reusable, type-safe normalization function that ensures your data is consistently structured and easy to manage.",
      "usecase": "Typed normalization is essential in applications with complex data relationships, like e-commerce platforms where products have categories, reviews, and inventory data. In dashboards displaying user analytics, normalization ensures consistent user profiles across charts, tables, and filters. When building social media feeds with posts, comments, and reactions, typed normalization prevents data duplication and maintains type safety during updates. This pattern is also crucial in form builders with reusable field components, where normalized schemas guarantee data integrity across nested sections."
    },
    "objectives": [
      "Construct a normalized data structure using TypeScript interfaces",
      "Implement a normalization function that transforms nested entity arrays",
      "Apply type annotations to ensure consistent entity ID references",
      "Configure lookup tables to optimize entity access patterns",
      "Manipulate normalized data to update specific entity properties",
      "Integrate denormalization logic to reconstruct the original nested structure",
      "Initialize a complete normalization system with typed input and output"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 11",
        "title": "Import TypeScript types and utilities",
        "instruction": "Import the Record utility type and create a NormalizedState interface from TypeScript's built-in types.",
        "hint": "Use the 'type' keyword to import Record and 'interface' to define NormalizedState with generic types T and K.",
        "analogousExample": "// Example: importing a utility type and defining an interface\ntype ExampleRecord = Record<string, number>;\ninterface ExampleState<T> {\n  byId: Record<string, T>;\n  allIds: string[];\n}",
        "seedCode": "// Import Record utility type and define NormalizedState interface here",
        "expectedOutcome": "You'll have the foundational TypeScript types needed for typed normalization of entities.",
        "successCriteria": [
          "Record type is imported and NormalizedState interface is defined with byId and allIds properties."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure to import Record and define the NormalizedState interface with both byId and allIds properties.",
        "feedbackWrong": "Check that you've imported the Record type and defined the NormalizedState interface with the correct structure.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "Record",
            "interface",
            "NormalizedState",
            "byId",
            "allIds"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 11",
        "title": "Define the Entity interface",
        "instruction": "Create an Entity interface with an id property of type number and a name property of type string.",
        "hint": "Use the interface keyword followed by the name Entity, then define the properties inside curly braces.",
        "analogousExample": "// Example: a simple interface\ninterface User {\n  id: number;\n  name: string;\n}",
        "seedCode": "// Import Record utility type and define NormalizedState interface here\n\n// Create the Entity interface here",
        "expectedOutcome": "An Entity interface is defined with the required typed properties.",
        "successCriteria": [
          "The Entity interface exists with id: number and name: string properties."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure both id and name properties are included with the correct TypeScript types.",
        "feedbackWrong": "The Entity interface wasn't defined correctly. Check that you used the interface keyword and included both required properties with proper types.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "interface",
            "Entity",
            "id:",
            "number",
            "name:",
            "string"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 11",
        "title": "Define the NormalizedState type",
        "instruction": "Create a NormalizedState type with two properties: entities (a Record mapping string IDs to Entity objects) and ids (an array of strings).",
        "hint": "Use the Record utility type from TypeScript for the entities property. The ids property should be an array of strings.",
        "analogousExample": "// Example: a typed normalized state structure\ntype NormalizedState = {\n  entities: Record<string, Entity>;\n  ids: string[];\n};",
        "seedCode": "// Import Record utility type and define NormalizedState interface here\n\n// Create the Entity interface here\n\n// Define the NormalizedState type here",
        "expectedOutcome": "You will have a NormalizedState type that defines the structure for storing normalized entity data.",
        "successCriteria": [
          "The NormalizedState type is defined with entities (Record<string, Entity>) and ids (string[]) properties."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure both properties (entities and ids) are included with the correct types.",
        "feedbackWrong": "The NormalizedState type definition is missing or incorrect. Check that it has both required properties with proper types.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "NormalizedState",
            "entities",
            "ids",
            "Record<string, Entity>",
            "string[]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 11",
        "title": "Create the initial normalization function",
        "instruction": "Create a function named 'normalizeEntities' that takes an array of Entity objects as a parameter and returns a NormalizedState object.",
        "hint": "Define a function with the specified name, parameter type (Entity[]), and return type (NormalizedState). Inside, you'll need to implement the normalization logic in later steps.",
        "analogousExample": "// Example: a function that processes data\nfunction processItems(items: Item[]): ProcessedResult {\n  // Implementation goes here\n  return { processed: true };\n}",
        "seedCode": "// Import Record utility type and define NormalizedState interface here\n\n// Create the Entity interface here\n\n// Define the NormalizedState type here\n\n// Create the initial normalization function here",
        "expectedOutcome": "A function named 'normalizeEntities' is defined with the correct TypeScript signature.",
        "successCriteria": [
          "The function 'normalizeEntities' exists, accepts an Entity[] parameter, and returns NormalizedState."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but check the function name, parameter type, or return type. Make sure it matches 'normalizeEntities(entities: Entity[]): NormalizedState'.",
        "feedbackWrong": "The function definition is missing or incorrect. Create a function named 'normalizeEntities' that takes an array of Entity objects and returns a NormalizedState object.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "normalizeEntities",
            "Entity[]",
            "NormalizedState"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 11",
        "title": "Add type parameters to the normalization function",
        "instruction": "Make your normalization function generic by adding a type parameter <T extends Entity> to the function signature.",
        "hint": "Add <T extends Entity> after the function name, then use T instead of Entity for the parameter and return types.",
        "analogousExample": "// Making a function generic\nfunction processItem<T extends Item>(item: T): T {\n  return item;\n}",
        "seedCode": "// Import Record utility type and define NormalizedState interface here\n\n// Create the Entity interface here\n\n// Define the NormalizedState type here\n\n// Create the initial normalization function here\nfunction normalizeEntities(entities: Entity[]): NormalizedState<Entity> {\n  const byId: Record<string, Entity> = {};\n  const allIds: string[] = [];\n\n  entities.forEach((entity) => {\n    byId[entity.id] = entity;\n    allIds.push(entity.id);\n  });\n\n  return { byId, allIds };\n}",
        "expectedOutcome": "The function will accept any type that satisfies the Entity interface, providing better type safety.",
        "successCriteria": [
          "The normalizeEntities function has a type parameter <T extends Entity> and uses T for both the parameter type and return type."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but check that you've added the type parameter <T extends Entity> and updated both the parameter and return types to use T.",
        "feedbackWrong": "The function should be generic. Add <T extends Entity> after the function name and replace Entity with T in the parameter and return type annotations.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<T extends Entity>",
            "entities: T[]",
            "NormalizedState<T>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 11",
        "title": "Create a User entity interface",
        "instruction": "Define a User interface that extends the Entity interface and adds an email property of type string.",
        "hint": "Use the 'interface' keyword, followed by 'User', then 'extends Entity', and finally declare the email property inside curly braces.",
        "analogousExample": "// Example: extending a base interface\ninterface Animal {\n  name: string;\n}\n\ninterface Dog extends Animal {\n  breed: string;\n}",
        "seedCode": "// Import Record utility type and define NormalizedState interface here\n\n// Create the Entity interface here\n\n// Define the NormalizedState type here\n\n// Create the initial normalization function here\nfunction normalizeEntities(entities: Entity[]): NormalizedState<Entity> {\n  const byId: Record<string, Entity> = {};\n  const allIds: string[] = [];\n\n  entities.forEach((entity) => {\n    byId[entity.id] = entity;\n    allIds.push(entity.id);\n  });\n\n  return { byId, allIds };\n}\n\n// Define the User interface here",
        "expectedOutcome": "A User interface is created that extends Entity and includes an email property.",
        "successCriteria": [
          "The User interface extends Entity and has an email property of type string."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure the User interface extends Entity and includes an email property of type string.",
        "feedbackWrong": "The User interface should extend Entity and include an email property of type string.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "interface",
            "User",
            "extends",
            "Entity",
            "email",
            "string"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 11",
        "title": "Initialize sample user data",
        "instruction": "Create an array of User objects with id, name, and email properties. Name the array 'sampleUsers'.",
        "hint": "Define a constant array with at least two User objects, each having id (string), name (string), and email (string) properties.",
        "analogousExample": "// Example: creating sample data\nconst sampleProducts = [\n  { id: 'p1', name: 'Laptop', price: 999 },\n  { id: 'p2', name: 'Mouse', price: 25 }\n];",
        "seedCode": "// Import Record utility type and define NormalizedState interface here\n\n// Create the Entity interface here\n\n// Define the NormalizedState type here\n\n// Create the initial normalization function here\nfunction normalizeEntities(entities: Entity[]): NormalizedState<Entity> {\n  const byId: Record<string, Entity> = {};\n  const allIds: string[] = [];\n\n  entities.forEach((entity) => {\n    byId[entity.id] = entity;\n    allIds.push(entity.id);\n  });\n\n  return { byId, allIds };\n}\n\n// Define the User interface here\n\n// Create sample user data here",
        "expectedOutcome": "You will have a sampleUsers array containing User objects with proper TypeScript typing.",
        "successCriteria": [
          "The sampleUsers array is defined with at least two User objects, each containing id, name, and email properties."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created an array, but make sure it contains User objects with id, name, and email properties.",
        "feedbackWrong": "The sampleUsers array is missing or doesn't match the User interface requirements.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "sampleUsers",
            "id",
            "name",
            "email"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 11",
        "title": "Normalize the user data",
        "instruction": "Call the normalizeEntities function with your sample user array and store the result in a constant named normalizedUsers.",
        "hint": "Use the function you created earlier, passing the user array as the argument. Assign the result to a constant with type annotation.",
        "analogousExample": "// Example: calling a function and storing the result\nconst processedData = processItems(itemList);",
        "seedCode": "// Import Record utility type and define NormalizedState interface here\n\n// Create the Entity interface here\n\n// Define the NormalizedState type here\n\n// Create the initial normalization function here\nfunction normalizeEntities(entities: Entity[]): NormalizedState<Entity> {\n  const byId: Record<string, Entity> = {};\n  const allIds: string[] = [];\n\n  entities.forEach((entity) => {\n    byId[entity.id] = entity;\n    allIds.push(entity.id);\n  });\n\n  return { byId, allIds };\n}\n\n// Define the User interface here\n\n// Create sample user data here\n\n// Call normalizeEntities with the user array and store the result",
        "expectedOutcome": "The user data is normalized using your typed function, producing a NormalizedState object stored in a constant.",
        "successCriteria": [
          "normalizeEntities is called with the user array and the result is stored in a constant named normalizedUsers."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're calling normalizeEntities with the user array and storing the result in a constant.",
        "feedbackWrong": "That doesn't look right. You need to call the normalizeEntities function with your user data and store the result.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "normalizeEntities",
            "normalizedUsers",
            "="
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 11",
        "title": "Create a denormalization function",
        "instruction": "Create a function named denormalizeState that takes a NormalizedState<T> and returns an array of T entities.",
        "hint": "The function should iterate through allIds and map each id to its corresponding entity in byId.",
        "analogousExample": "// Example: a function that reverses a mapping\nfunction getValuesFromMap<T>(map: Record<string, T>, keys: string[]): T[] {\n  return keys.map(key => map[key]);\n}",
        "seedCode": "// Import Record utility type and define NormalizedState interface here\n\n// Create the Entity interface here\n\n// Define the NormalizedState type here\n\n// Create the initial normalization function here\nfunction normalizeEntities(entities: Entity[]): NormalizedState<Entity> {\n  const byId: Record<string, Entity> = {};\n  const allIds: string[] = [];\n\n  entities.forEach((entity) => {\n    byId[entity.id] = entity;\n    allIds.push(entity.id);\n  });\n\n  return { byId, allIds };\n}\n\n// Define the User interface here\n\n// Create sample user data here\n\n// Call normalizeEntities with the user array and store the result\n\n// Create the denormalization function here",
        "expectedOutcome": "A denormalizeState function that converts normalized state back to an array of entities.",
        "successCriteria": [
          "The denormalizeState function is defined with proper TypeScript typing and returns an array of entities."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure your function accepts a NormalizedState<T> parameter and returns T[].",
        "feedbackWrong": "The denormalization function isn't quite right. It should take normalized state and return an array of entities.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "denormalizeState",
            "NormalizedState",
            "T[]",
            "allIds",
            "byId"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 11",
        "title": "Test the normalization round-trip",
        "instruction": "Call the denormalization function with the normalized state to verify it reconstructs the original user array,",
        "hint": "Use the denormalizeEntities function you created earlier, passing it the normalized state object. Store the result in a variable, then log both the original users array and the reconstructed array.",
        "analogousExample": "// Similar: verifying a transformation round-trip\nconst original = [1, 2, 3];\nconst transformed = original.map(x => x * 2);\nconst reversed = transformed.map(x => x / 2);\nconsole.log('Original:', original);\nconsole.log('Reconstructed:', reversed);",
        "seedCode": "// Import Record utility type and define NormalizedState interface here\n\n// Create the Entity interface here\n\n// Define the NormalizedState type here\n\n// Create the initial normalization function here\nfunction normalizeEntities(entities: Entity[]): NormalizedState<Entity> {\n  const byId: Record<string, Entity> = {};\n  const allIds: string[] = [];\n\n  entities.forEach((entity) => {\n    byId[entity.id] = entity;\n    allIds.push(entity.id);\n  });\n\n  return { byId, allIds };\n}\n\n// Define the User interface here\n\n// Create sample user data here\n\n// Call normalizeEntities with the user array and store the result\n\n// Create the denormalization function here\n\n// Test the normalization round-trip here",
        "expectedOutcome": "The console will show that the original user array and the denormalized array are identical, confirming the normalization/denormalization process works correctly.",
        "successCriteria": [
          "The denormalizeEntities function is called with the normalized state, the result is stored, and both original and reconstructed arrays are logged to console."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you're calling denormalizeEntities with the normalized state you created earlier, storing the result, and logging both the original users array and the reconstructed array.",
        "feedbackWrong": "call the denormalizeEntities function with your normalized state, store the result, then log both the original users array and the reconstructed array to verify they match.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "denormalizeEntities",
            "normalizedState",
            "console.log",
            "users"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step11",
        "type": "question",
        "phase": "Step 11 of 11",
        "title": "log both the original and reconstructed array",
        "instruction": "log both the original and reconstructed arrays to the console.",
        "hint": "Use the denormalizeEntities function you created earlier, passing it the normalized state object. Store the result in a variable, then log both the original users array and the reconstructed array.",
        "analogousExample": "// Similar: verifying a transformation round-trip\nconst original = [1, 2, 3];\nconst transformed = original.map(x => x * 2);\nconst reversed = transformed.map(x => x / 2);\nconsole.log('Original:', original);\nconsole.log('Reconstructed:', reversed);",
        "seedCode": "// Import Record utility type and define NormalizedState interface here\n\n// Create the Entity interface here\n\n// Define the NormalizedState type here\n\n// Create the initial normalization function here\nfunction normalizeEntities(entities: Entity[]): NormalizedState<Entity> {\n  const byId: Record<string, Entity> = {};\n  const allIds: string[] = [];\n\n  entities.forEach((entity) => {\n    byId[entity.id] = entity;\n    allIds.push(entity.id);\n  });\n\n  return { byId, allIds };\n}\n\n// Define the User interface here\n\n// Create sample user data here\n\n// Call normalizeEntities with the user array and store the result\n\n// Create the denormalization function here\n\n// Test the normalization round-trip here",
        "expectedOutcome": "",
        "successCriteria": [
          "Complete the step as instructed."
        ],
        "feedbackCorrect": "Great! Step completed.",
        "feedbackPartial": "Review the instruction and try again.",
        "feedbackWrong": "Check the instruction and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "denormalizeEntities",
            "normalizedState",
            "console.log",
            "users"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import TypeScript types and utilities",
        "id": "step1"
      },
      {
        "label": "Define the Entity interface",
        "id": "step2"
      },
      {
        "label": "Define the NormalizedState type",
        "id": "step3"
      },
      {
        "label": "Create the initial normalization function",
        "id": "step4"
      },
      {
        "label": "Add type parameters to the normalization function",
        "id": "step5"
      },
      {
        "label": "Create a User entity interface",
        "id": "step6"
      },
      {
        "label": "Initialize sample user data",
        "id": "step7"
      },
      {
        "label": "Normalize the user data",
        "id": "step8"
      },
      {
        "label": "Create a denormalization function",
        "id": "step9"
      },
      {
        "label": "Test the normalization round-trip",
        "id": "step10"
      },
      {
        "label": "log both the original and reconstructed array",
        "id": "step11"
      }
    ]
  },
  "source": "content"
}