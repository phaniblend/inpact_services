{
  "success": true,
  "config": {
    "lessonId": "ai-ts-40",
    "track": "ts",
    "lessonNum": 40,
    "title": "Build typed sessionStorage wrapper",
    "shortName": "P40",
    "intro": {
      "tag": "LESSON #40",
      "title": "Build typed sessionStorage wrapper",
      "body": "In this lesson, you will build a typed wrapper around the browser's sessionStorage API using TypeScript. You'll learn how to create a custom class that provides type safety, ensuring only specific data types can be stored and retrieved. The core programming concept involves using TypeScript generics to define flexible yet strict types for your storage operations. You'll work with key technologies like TypeScript classes, interfaces, and the native sessionStorage object. By the end, you'll have a reusable, type-safe utility that prevents common errors and improves code reliability in web applications.",
      "usecase": "Typed sessionStorage wrappers are commonly used in e-commerce checkout flows where user selections (shipping options, payment methods) need to persist across page navigation without server calls. In analytics dashboards, they maintain filter states (date ranges, metric selections) as users toggle between different report views. This pattern also appears in multi-step forms for applications or onboarding wizards, where partially completed data needs temporary storage with type safety to prevent data corruption during the user's session."
    },
    "objectives": [
      "Implement a TypeScript interface to define the structure of sessionStorage wrapper methods",
      "Construct a class that encapsulates sessionStorage operations with type safety",
      "Apply TypeScript generics to create flexible get and set methods for various data types",
      "Initialize and configure the wrapper to handle JSON serialization and deserialization",
      "Integrate the typed wrapper into a simple script to demonstrate its usage",
      "Manipulate sessionStorage data using the wrapper's methods to store and retrieve typed values",
      "Control data integrity by implementing error handling for parsing and storage failures"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Import TypeScript types and sessionStorage API",
        "instruction": "Import the necessary TypeScript types and reference the sessionStorage API. Since this is a TypeScript-only lesson, you'll need to import the appropriate types and reference the browser's sessionStorage API.",
        "hint": "You'll need to import the 'Storage' type from the TypeScript DOM library and reference the global sessionStorage object.",
        "analogousExample": "// Importing types and referencing browser APIs\nimport { Storage } from 'typescript-dom';\nconst localStorage = window.localStorage;",
        "seedCode": "// Import the necessary types and reference sessionStorage\n",
        "expectedOutcome": "The code will have the required imports and sessionStorage reference to build the typed wrapper.",
        "successCriteria": [
          "The Storage type is imported from the appropriate TypeScript DOM library, and sessionStorage is referenced from the global window object."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're importing the Storage type and referencing sessionStorage from the global window object.",
        "feedbackWrong": "For this step, you need to import the Storage type and reference the sessionStorage API.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "Storage",
            "sessionStorage",
            "window"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Define the generic StorageWrapper interface",
        "instruction": "Define a TypeScript interface named StorageWrapper with three generic methods: get, set, and remove.",
        "hint": "Use the interface keyword followed by StorageWrapper. Inside, declare methods with appropriate signatures using a generic type parameter T.",
        "analogousExample": "// Example of a generic interface\ninterface DataStore<T> {\n  retrieve(): T;\n  store(value: T): void;\n  clear(): void;\n}",
        "seedCode": "// Import the necessary types and reference sessionStorage\n\n// Define the StorageWrapper interface here",
        "expectedOutcome": "You'll have a typed interface that defines the contract for storage operations with proper TypeScript generics.",
        "successCriteria": [
          "StorageWrapper interface is defined with get, set, and remove methods using a generic type parameter T."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure all three methods (get, set, remove) are defined with correct signatures using the generic type T.",
        "feedbackWrong": "The interface definition is missing or incorrect. Remember to use the interface keyword and include all three methods with proper TypeScript syntax.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "interface",
            "StorageWrapper",
            "get",
            "set",
            "remove",
            "T"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Create the typedSessionStorage function signature",
        "instruction": "Declare a function named typedSessionStorage that returns an object implementing the StorageWrapper interface. Use a generic type parameter <T> to make it type-safe.",
        "hint": "Start with 'function typedSessionStorage<T>(): StorageWrapper<T> { }' or use an arrow function with the same signature.",
        "analogousExample": "// Example: generic function returning an interface\nfunction createCache<T>(): Cache<T> {\n  return {\n    get: (key) => null,\n    set: (key, value) => {}\n  };\n}",
        "seedCode": "// Import the necessary types and reference sessionStorage\n\n// Define the StorageWrapper interface here\n\n// Declare the typedSessionStorage function here",
        "expectedOutcome": "A function signature for typedSessionStorage is added, ready to implement the wrapper methods.",
        "successCriteria": [
          "The typedSessionStorage function is declared with a generic type parameter <T> and returns StorageWrapper<T>."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure the function returns StorageWrapper<T> and uses a generic type parameter.",
        "feedbackWrong": "The function signature is missing or incorrect. It should accept a generic type and return StorageWrapper<T>.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "typedSessionStorage",
            "<T>",
            "StorageWrapper<T>",
            "function",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Implement the get method with type safety",
        "instruction": "Inside the typedSessionStorage function, implement the get method that retrieves a value from sessionStorage, parses it as JSON, and returns typed data.",
        "hint": "The get method should take a key parameter, use sessionStorage.getItem(key), parse the result with JSON.parse, and return it with the appropriate type.",
        "analogousExample": "// Example of a type-safe getter function\nfunction getItem<T>(key: string): T | null {\n  const stored = localStorage.getItem(key);\n  return stored ? JSON.parse(stored) as T : null;\n}",
        "seedCode": "// Import the necessary types and reference sessionStorage\n\n// Define the StorageWrapper interface here\n\n// Declare the typedSessionStorage function here\nfunction typedSessionStorage<T>(): StorageWrapper<T> {\n  return {\n    // Implement the get method here\n    get(key: string): T | null {\n      // Retrieve, parse, and return typed data\n    }\n  };\n}",
        "expectedOutcome": "The typedSessionStorage function now has a get method that retrieves and parses sessionStorage items with type safety.",
        "successCriteria": [
          "The get method is implemented, uses sessionStorage.getItem, parses JSON, and returns typed data or null."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure your get method retrieves from sessionStorage, parses the JSON, and returns the typed value or null.",
        "feedbackWrong": "The get method isn't implemented correctly. It should retrieve from sessionStorage, parse JSON, and return typed data.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "get",
            "sessionStorage.getItem",
            "JSON.parse",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Return the wrapper object with all methods",
        "instruction": "Complete the typedSessionStorage function by returning an object that contains the get, set, and remove methods you implemented.",
        "hint": "The function should return an object with three methods: get, set, and remove. Make sure each method is properly defined with its parameters and return type.",
        "analogousExample": "// Example: returning an object with methods\nfunction createCounter() {\n  return {\n    get: () => 0,\n    increment: () => {},\n    reset: () => {}\n  };\n}",
        "seedCode": "// Import the necessary types and reference sessionStorage\n\n// Define the StorageWrapper interface here\n\n// Declare the typedSessionStorage function here\nfunction typedSessionStorage<T>(): StorageWrapper<T> {\n  return {\n    // Implement the get method here\n    get(key: string): T | null {\n      // Retrieve, parse, and return typed data\n    },\n    // Add the set method here\n    // Add the remove method here\n  };\n}",
        "expectedOutcome": "The typedSessionStorage function now returns a complete wrapper object with all three methods defined.",
        "successCriteria": [
          "The typedSessionStorage function returns an object containing get, set, and remove methods."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure the returned object includes all three methods: get, set, and remove.",
        "feedbackWrong": "The function doesn't return a complete wrapper object. Check that you're returning an object with get, set, and remove methods.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "return",
            "{",
            "get",
            "set",
            "remove",
            "}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Create a usage example with a specific type",
        "instruction": "Define a User interface with name and age properties,",
        "hint": "Create an interface with name: string and age: number, then call typedSessionStorage<User>() to get a typed wrapper.",
        "analogousExample": "// Example: creating a typed wrapper for a specific type\ninterface Product {\n  id: number;\n  name: string;\n}\n\nconst productStorage = typedSessionStorage<Product>();",
        "seedCode": "// Import the necessary types and reference sessionStorage\n\n// Define the StorageWrapper interface here\n\n// Declare the typedSessionStorage function here\nfunction typedSessionStorage<T>(): StorageWrapper<T> {\n  return {\n    // Implement the get method here\n    get(key: string): T | null {\n      // Retrieve, parse, and return typed data\n      return null;\n    },\n    // Add the set method here\n    set(key: string, value: T): void {\n      // Serialize and store the value\n    },\n    // Add the remove method here\n    remove(key: string): void {\n      // Remove the item\n    }\n  };\n}\n\n// Define User interface and create typed wrapper instance here",
        "expectedOutcome": "A User interface and a typed wrapper instance created for demonstration.",
        "successCriteria": [
          "User interface is defined with name:string and age:number",
          "typedSessionStorage<User>() is called to create a wrapper instance"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you've defined the User interface with both properties and called typedSessionStorage<User>().",
        "feedbackWrong": "Check that you've defined the User interface and created a typed wrapper instance using typedSessionStorage<User>().",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "interface User",
            "name:",
            "age:",
            "typedSessionStorage<User>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "create a typed wrapper instance for it",
        "instruction": "create a typed wrapper instance for it.",
        "hint": "Create an interface with name: string and age: number, then call typedSessionStorage<User>() to get a typed wrapper.",
        "analogousExample": "// Example: creating a typed wrapper for a specific type\ninterface Product {\n  id: number;\n  name: string;\n}\n\nconst productStorage = typedSessionStorage<Product>();",
        "seedCode": "// Import the necessary types and reference sessionStorage\n\n// Define the StorageWrapper interface here\n\n// Declare the typedSessionStorage function here\nfunction typedSessionStorage<T>(): StorageWrapper<T> {\n  return {\n    // Implement the get method here\n    get(key: string): T | null {\n      // Retrieve, parse, and return typed data\n      return null;\n    },\n    // Add the set method here\n    set(key: string, value: T): void {\n      // Serialize and store the value\n    },\n    // Add the remove method here\n    remove(key: string): void {\n      // Remove the item\n    }\n  };\n}\n\n// Define User interface and create typed wrapper instance here",
        "expectedOutcome": "",
        "successCriteria": [
          "Complete the step as instructed."
        ],
        "feedbackCorrect": "Great! Step completed.",
        "feedbackPartial": "Review the instruction and try again.",
        "feedbackWrong": "Check the instruction and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "interface User",
            "name:",
            "age:",
            "typedSessionStorage<User>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Test the wrapper by storing and retrieving typed d",
        "instruction": "In the usage example area, call the wrapper's set method to store a User object,",
        "hint": "After creating your wrapper instance, use wrapper.set('userKey', userObject) to store data, then const retrieved = wrapper.get('userKey') to retrieve it. Finally, console.log('Retrieved user:', retrieved).",
        "analogousExample": "// Testing a similar typed function\ninterface Product { id: number; name: string; }\nconst productStorage = typedSessionStorage<Product>();\nconst sampleProduct: Product = { id: 1, name: 'Laptop' };\nproductStorage.set('currentProduct', sampleProduct);\nconst retrievedProduct = productStorage.get('currentProduct');\nconsole.log('Product retrieved:', retrievedProduct);",
        "seedCode": "// Import the necessary types and reference sessionStorage\n\n// Define the StorageWrapper interface here\n\n// Declare the typedSessionStorage function here\nfunction typedSessionStorage<T>(): StorageWrapper<T> {\n  return {\n    // Implement the get method here\n    get(key: string): T | null {\n      // Retrieve, parse, and return typed data\n      return null;\n    },\n    // Add the set method here\n    set(key: string, value: T): void {\n      // Serialize and store the value\n    },\n    // Add the remove method here\n    remove(key: string): void {\n      // Remove the item\n    }\n  };\n}\n\n// Define User interface and create typed wrapper instance here\ninterface User {\n  id: number;\n  name: string;\n}\nconst userStorage = typedSessionStorage<User>();\n// Test the wrapper by storing and retrieving typed data\n",
        "expectedOutcome": "The wrapper will successfully store a typed User object in sessionStorage and retrieve it with proper type safety, demonstrating the wrapper's functionality.",
        "successCriteria": [
          "The wrapper's set method is called with a User object, the get method retrieves it, and the result is logged to the console."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you're calling both set and get methods on your wrapper instance and logging the retrieved result to the console.",
        "feedbackWrong": "The testing code doesn't seem to be complete. Remember to call wrapper.set() with a User object, then wrapper.get() to retrieve it, and finally console.log() the result.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "userStorage.set",
            "userStorage.get",
            "console.log"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "call get to retrieve it and log the result to",
        "instruction": "call get to retrieve it and log the result to the console.",
        "hint": "After creating your wrapper instance, use wrapper.set('userKey', userObject) to store data, then const retrieved = wrapper.get('userKey') to retrieve it. Finally, console.log('Retrieved user:', retrieved).",
        "analogousExample": "// Testing a similar typed function\ninterface Product { id: number; name: string; }\nconst productStorage = typedSessionStorage<Product>();\nconst sampleProduct: Product = { id: 1, name: 'Laptop' };\nproductStorage.set('currentProduct', sampleProduct);\nconst retrievedProduct = productStorage.get('currentProduct');\nconsole.log('Product retrieved:', retrievedProduct);",
        "seedCode": "// Import the necessary types and reference sessionStorage\n\n// Define the StorageWrapper interface here\n\n// Declare the typedSessionStorage function here\nfunction typedSessionStorage<T>(): StorageWrapper<T> {\n  return {\n    // Implement the get method here\n    get(key: string): T | null {\n      // Retrieve, parse, and return typed data\n      return null;\n    },\n    // Add the set method here\n    set(key: string, value: T): void {\n      // Serialize and store the value\n    },\n    // Add the remove method here\n    remove(key: string): void {\n      // Remove the item\n    }\n  };\n}\n\n// Define User interface and create typed wrapper instance here\ninterface User {\n  id: number;\n  name: string;\n}\nconst userStorage = typedSessionStorage<User>();\n// Test the wrapper by storing and retrieving typed data\n",
        "expectedOutcome": "",
        "successCriteria": [
          "Complete the step as instructed."
        ],
        "feedbackCorrect": "Great! Step completed.",
        "feedbackPartial": "Review the instruction and try again.",
        "feedbackWrong": "Check the instruction and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "userStorage.set",
            "userStorage.get",
            "console.log"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Add error handling for JSON parsing",
        "instruction": "In the get method of your typedSessionStorage function, wrap the JSON.parse call in a try-catch block to handle parsing errors gracefully.",
        "hint": "Add try { ... } catch { ... } around the parsing logic. In the catch block, return null to indicate the data couldn't be retrieved.",
        "analogousExample": "// Example: safe JSON parsing with error handling\nfunction safeParse(jsonString: string): any | null {\n  try {\n    return JSON.parse(jsonString);\n  } catch {\n    return null;\n  }\n}",
        "seedCode": "// Import the necessary types and reference sessionStorage\n\n// Define the StorageWrapper interface here\n\n// Declare the typedSessionStorage function here\nfunction typedSessionStorage<T>(): StorageWrapper<T> {\n  return {\n    // Implement the get method here\n    get(key: string): T | null {\n      // Retrieve, parse, and return typed data\n      return null;\n    },\n    // Add the set method here\n    set(key: string, value: T): void {\n      // Serialize and store the value\n    },\n    // Add the remove method here\n    remove(key: string): void {\n      // Remove the item\n    }\n  };\n}\n\n// Define User interface and create typed wrapper instance here\ninterface User {\n  id: number;\n  name: string;\n}\nconst userStorage = typedSessionStorage<User>();\n// Test the wrapper by storing and retrieving typed data",
        "expectedOutcome": "The get method will now safely handle invalid JSON data by returning null instead of throwing an error.",
        "successCriteria": [
          "The get method contains a try-catch block that wraps JSON.parse and returns null in the catch block."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure the try-catch block is properly implemented in the get method.",
        "feedbackWrong": "Try adding a try-catch block around the JSON.parse call in the get method to handle parsing errors.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "try",
            "catch",
            "JSON.parse",
            "return null"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import TypeScript types and sessionStorage API",
        "id": "step1"
      },
      {
        "label": "Define the generic StorageWrapper interface",
        "id": "step2"
      },
      {
        "label": "Create the typedSessionStorage function signature",
        "id": "step3"
      },
      {
        "label": "Implement the get method with type safety",
        "id": "step4"
      },
      {
        "label": "Return the wrapper object with all methods",
        "id": "step5"
      },
      {
        "label": "Create a usage example with a specific type",
        "id": "step6"
      },
      {
        "label": "create a typed wrapper instance for it",
        "id": "step7"
      },
      {
        "label": "Test the wrapper by storing and retrieving typed d",
        "id": "step8"
      },
      {
        "label": "call get to retrieve it and log the result to",
        "id": "step9"
      },
      {
        "label": "Add error handling for JSON parsing",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}