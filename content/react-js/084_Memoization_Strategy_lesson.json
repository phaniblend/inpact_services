{
  "success": true,
  "config": {
    "lessonId": "ai-react-js-84",
    "track": "react-js",
    "lessonNum": 84,
    "title": "Memoization Strategy",
    "shortName": "P84",
    "intro": {
      "tag": "LESSON #84",
      "title": "Memoization Strategy",
      "body": "In this lesson, you will build a simple React component that demonstrates memoization to optimize performance. You'll learn how to use the `useMemo` hook to prevent expensive calculations from re-running on every render. The core concept is memoization, a technique that caches the results of a function call and returns the cached result when the same inputs occur again. You'll work with React state, props, and the `useMemo` hook to implement this strategy in a beginner-friendly counter example. By the end, you'll understand how memoization can improve your app's efficiency by avoiding unnecessary computations.",
      "usecase": "Memoization is essential in data-heavy React applications like financial dashboards or e-commerce analytics platforms where expensive calculations run repeatedly. When users filter transaction data or adjust date ranges, memoized selectors prevent redundant processing of thousands of records. This pattern also optimizes interactive components like real-time search filters, complex form validations, and dynamically updated charts—ensuring smooth UI responsiveness while minimizing unnecessary re-renders."
    },
    "objectives": [
      "Implement a React component that uses the useMemo hook to cache a computed value.",
      "Apply memoization to prevent expensive recalculations on every render.",
      "Construct a scenario where a component re-renders without causing unnecessary computations.",
      "Configure useMemo with a dependency array to control when the memoized value updates.",
      "Integrate a memoized value into JSX to render dynamic content efficiently.",
      "Manipulate state variables to observe the effect of memoization on performance.",
      "Initialize a functional component with state and a memoized calculation."
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 13",
        "title": "Import React, useState, useMemo, and useCallback",
        "instruction": "Import React and the hooks useState, useMemo, and useCallback from the 'react' package.",
        "hint": "Use a single import statement with curly braces to bring in multiple named exports from 'react'.",
        "analogousExample": "// Importing multiple hooks from React\nimport React, { useState, useEffect } from 'react';",
        "seedCode": "// Import React, useState, useMemo, and useCallback here\n",
        "expectedOutcome": "The necessary React hooks for memoization are imported and ready to use.",
        "successCriteria": [
          "React, useState, useMemo, and useCallback are imported from 'react'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close, but check that you've imported all four: React, useState, useMemo, and useCallback.",
        "feedbackWrong": "The imports are incorrect or incomplete. Please import React, useState, useMemo, and useCallback from 'react'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "React",
            "useState",
            "useMemo",
            "useCallback",
            "'react'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 13",
        "title": "Create the main App component",
        "instruction": "Create a functional component named App that returns an empty div element.",
        "hint": "Define a function called App that returns JSX with a single <div></div>.",
        "analogousExample": "// Example of a basic component\nfunction App() {\n  return <div></div>;\n}",
        "seedCode": "import React, { useState, useMemo, useCallback } from 'react';\n\n// Create the App component here",
        "expectedOutcome": "The App component is defined and returns an empty div.",
        "successCriteria": [
          "A functional component named App exists and returns a <div> element."
        ],
        "feedbackCorrect": "Great! You've created the main App component structure.",
        "feedbackPartial": "You're close. Make sure you've defined a component named App that returns a <div> element.",
        "feedbackWrong": "The component isn't defined correctly. Create a function called App that returns a <div>.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "App",
            "return",
            "<div>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 13",
        "title": "Initialize state for counter and input value",
        "instruction": "Inside your App component, use useState to create two state variables: 'count' initialized to 0 and 'inputValue' initialized to an empty string ''.",
        "hint": "Call useState twice: once for count with initial value 0, and once for inputValue with initial value ''.",
        "analogousExample": "// Example: initializing multiple state variables\nconst [score, setScore] = useState(0);\nconst [name, setName] = useState('');",
        "seedCode": "import React, { useState, useMemo, useCallback } from 'react';\n\n// Create the App component here\nconst App = () => {\n  // Initialize state variables here\n  \n  return null;\n};",
        "expectedOutcome": "Your App component will have two state variables ready to track the counter and input value.",
        "successCriteria": [
          "The App component contains two useState calls: one for count with initial value 0, and one for inputValue with initial value ''."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created at least one state variable, but you need both: count (initial 0) and inputValue (initial '').",
        "feedbackWrong": "Check that you've used useState twice inside the App component to create both required state variables.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useState(0)",
            "useState('')",
            "count",
            "inputValue"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 13",
        "title": "Create a non-memoized expensive calculation function",
        "instruction": "Inside your App component, create a function named 'expensiveCalculation' that simulates heavy computation by performing a loop that runs 1,000,000 times and returns the result.",
        "hint": "Define a regular function (not memoized) that uses a for loop to iterate many times, performing a simple arithmetic operation inside the loop.",
        "analogousExample": "// Example of a function that simulates heavy computation\nfunction simulateHeavyWork() {\n  let result = 0;\n  for (let i = 0; i < 1000000; i++) {\n    result += Math.random();\n  }\n  return result;\n}",
        "seedCode": "import React, { useState, useMemo, useCallback } from 'react';\n\n// Create the App component here\nconst App = () => {\n  // Initialize state variables here\n  \n  // Create the expensiveCalculation function here\n  \n  return null;\n};",
        "expectedOutcome": "You'll have a function that demonstrates performance issues when called repeatedly without memoization.",
        "successCriteria": [
          "The App component contains a function named 'expensiveCalculation' that uses a loop to simulate heavy computation."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your function is named 'expensiveCalculation' and contains a loop that runs many times to simulate heavy computation.",
        "feedbackWrong": "The function wasn't created correctly. Create a function named 'expensiveCalculation' that uses a loop to simulate heavy computation.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "expensiveCalculation",
            "function",
            "for",
            "loop",
            "1000000",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 13",
        "title": "Create a memoized version with useMemo",
        "instruction": "Inside your App component, use the useMemo hook to create a memoizedResult variable that calls expensiveCalculation only when inputValue changes.",
        "hint": "Call useMemo with a function that returns expensiveCalculation(inputValue) and pass [inputValue] as the dependency array.",
        "analogousExample": "// Example: memoizing a computed value\nconst memoizedValue = useMemo(() => {\n  return expensiveComputation(dependency);\n}, [dependency]);",
        "seedCode": "import React, { useState, useMemo, useCallback } from 'react';\n\n// Create the App component here\nconst App = () => {\n  // Initialize state variables here\n  \n  // Create the expensiveCalculation function here\n  \n  // Create memoizedResult with useMemo here\n  \n  return null;\n};",
        "expectedOutcome": "A memoizedResult variable is created using useMemo, which will only recalculate when inputValue changes.",
        "successCriteria": [
          "useMemo is called with a function that returns expensiveCalculation(inputValue) and has [inputValue] as its dependency array."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track with useMemo. Make sure your function calls expensiveCalculation(inputValue) and includes [inputValue] in the dependency array.",
        "feedbackWrong": "Try using the useMemo hook to create a memoized version of the expensive calculation. The calculation should only run when inputValue changes.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useMemo",
            "expensiveCalculation",
            "inputValue"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 13",
        "title": "Create event handlers for counter and input",
        "instruction": "Inside your App component, create two handler functions: one named 'incrementCount' that increments the counter state, and another named 'handleInputChange' that updates the input value state based on the event target's value.",
        "hint": "Both handlers should be defined as functions that take the necessary parameters (no parameters for incrementCount, an event for handleInputChange) and call the appropriate state setter functions.",
        "analogousExample": "// Example of similar event handlers\nfunction incrementCount() {\n  setCount(prev => prev + 1);\n}\n\nfunction handleInputChange(e) {\n  setInputValue(e.target.value);\n}",
        "seedCode": "import React, { useState, useMemo, useCallback } from 'react';\n\n// Create the App component here\nconst App = () => {\n  // Initialize state variables here\n  \n  // Create the expensiveCalculation function here\n  \n  // Create memoizedResult with useMemo here\n  \n  // Create event handlers for counter and input here\n  \n  return null;\n};",
        "expectedOutcome": "Two handler functions are defined: incrementCount updates the counter state, and handleInputChange updates the input value state based on user input.",
        "successCriteria": [
          "The incrementCount function increments the counter state, and handleInputChange updates the input value state using e.target.value."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created at least one handler function, but both are needed. Make sure you have incrementCount that increments the counter and handleInputChange that updates the input value from the event.",
        "feedbackWrong": "The handlers aren't defined correctly. Remember to create both incrementCount and handleInputChange functions that update their respective state values.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "incrementCount",
            "handleInputChange",
            "setCounter",
            "setInputValue",
            "e.target.value"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 13",
        "title": "Create a non-memoized callback function",
        "instruction": "Inside your App component, create a regular function named 'nonMemoizedCallback' that logs a message to the console when called.",
        "hint": "Define a function that uses console.log() to output a message. This function will be recreated on every render.",
        "analogousExample": "// Example of a non-memoized function\nfunction logMessage() {\n  console.log('Function recreated');\n}",
        "seedCode": "import React, { useState, useMemo, useCallback } from 'react';\n\n// Create the App component here\nconst App = () => {\n  // Initialize state variables here\n  \n  // Create the expensiveCalculation function here\n  \n  // Create memoizedResult with useMemo here\n  \n  // Create event handlers for counter and input here\n  \n  // Create a non-memoized callback function here\n  \n  return null;\n};",
        "expectedOutcome": "A regular function 'nonMemoizedCallback' is defined inside the App component that logs a message when called.",
        "successCriteria": [
          "The App component contains a function named 'nonMemoizedCallback' that calls console.log() with a message."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your function is named exactly 'nonMemoizedCallback' and includes a console.log() statement.",
        "feedbackWrong": "The function wasn't created correctly. Remember to define a function named 'nonMemoizedCallback' that logs a message using console.log().",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "nonMemoizedCallback",
            "console.log"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 13",
        "title": "Create a memoized callback with useCallback",
        "instruction": "Inside your App component, create a memoized callback function named 'memoizedCallback' using the useCallback hook. This function should log a message to the console (e.g., 'Memoized callback executed') and should be memoized based on its dependencies.",
        "hint": "Use the useCallback hook you imported earlier. Pass it a function that logs a message, and specify the appropriate dependency array.",
        "analogousExample": "// Example of memoized callback\nconst memoizedGreeting = useCallback(() => {\n  console.log('Hello!');\n}, []);",
        "seedCode": "import React, { useState, useMemo, useCallback } from 'react';\n\n// Create the App component here\nconst App = () => {\n  // Initialize state variables here\n  \n  // Create the expensiveCalculation function here\n  \n  // Create memoizedResult with useMemo here\n  \n  // Create event handlers for counter and input here\n  \n  // Create a non-memoized callback function here\n  \n  // Create a memoized callback with useCallback here\n  \n  return null;\n};",
        "expectedOutcome": "You will have a memoized callback function that logs a message and only re-creates when its dependencies change.",
        "successCriteria": [
          "The memoizedCallback function is defined using useCallback, logs a message to the console, and has a proper dependency array."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you're using the useCallback hook with a function that logs a message and includes the dependency array.",
        "feedbackWrong": "use the useCallback hook to create a memoized function that logs a message.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "memoizedCallback",
            "useCallback",
            "console.log",
            "=>",
            "{"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 13",
        "title": "Add the basic JSX structure with headings",
        "instruction": "In your App component's return statement, replace 'null' with a JSX fragment containing a main heading and a subheading to explain memoization.",
        "hint": "Use a fragment (<>...</>) or a <div> wrapper. Add an <h1> with a title like 'Memoization Demo' and an <h2> with a brief description.",
        "analogousExample": "// Example: basic JSX structure with headings\nreturn (\n  <>\n    <h1>Component Demo</h1>\n    <h2>This demonstrates a key React concept.</h2>\n  </>\n);",
        "seedCode": "import React, { useState, useMemo, useCallback } from 'react';\n\n// Create the App component here\nconst App = () => {\n  // Initialize state variables here\n  \n  // Create the expensiveCalculation function here\n  \n  // Create memoizedResult with useMemo here\n  \n  // Create event handlers for counter and input here\n  \n  // Create a non-memoized callback function here\n  \n  // Create a memoized callback with useCallback here\n  \n  return null;\n};",
        "expectedOutcome": "The component returns JSX with at least two heading elements that introduce the memoization demonstration.",
        "successCriteria": [
          "The return statement contains a fragment or div with an <h1> and an <h2> element."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you have both an <h1> and an <h2> element in your JSX.",
        "feedbackWrong": "Check that you replaced 'null' with JSX containing heading elements. Remember to use a fragment or wrapper.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "return",
            "<h1>",
            "<h2>",
            "(",
            ")"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 13",
        "title": "Add counter controls and input field",
        "instruction": "Inside the return statement of your App component, add a button with the text 'Increment Counter' and an onClick handler that calls incrementCounter.",
        "hint": "Place these elements after the headings you added earlier. The button should trigger the counter increment, and the input should update the inputValue state.",
        "analogousExample": "// Adding interactive elements\n<button onClick={incrementCounter}>Increment</button>\n<input type=\"text\" value={inputValue} onChange={handleInputChange} />",
        "seedCode": "import React, { useState, useMemo, useCallback } from 'react';\n\n// Create the App component here\nconst App = () => {\n  // Initialize state variables here\n  const [counter, setCounter] = useState(0);\n  const [inputValue, setInputValue] = useState('');\n  \n  // Create the expensiveCalculation function here\n  const expensiveCalculation = (num) => {\n    console.log('Calculating...');\n    let result = 0;\n    for (let i = 0; i < 1000000000; i++) {\n      result += num;\n    }\n    return result;\n  };\n  \n  // Create memoizedResult with useMemo here\n  const memoizedResult = useMemo(() => expensiveCalculation(counter), [counter]);\n  \n  // Create event handlers for counter and input here\n  const incrementCounter = () => {\n    setCounter(counter + 1);\n  };\n  \n  const handleInputChange = (e) => {\n    setInputValue(e.target.value);\n  };\n  \n  // Create a non-memoized callback function here\n  const nonMemoizedCallback = () => {\n    console.log('Non-memoized callback called');\n  };\n  \n  // Create a memoized callback with useCallback here\n  const memoizedCallback = useCallback(() => {\n    console.log('Memoized callback called');\n  }, []);\n  \n  return (\n    <div>\n      <h1>Memoization Strategy Demo</h1>\n      <h2>Counter: {counter}</h2>\n      <h2>Input Value: {inputValue}</h2>\n    </div>\n  );\n};",
        "expectedOutcome": "The UI will have interactive elements: a button to increment the counter and an input field that updates the inputValue state when typed into.",
        "successCriteria": [
          "Button with text 'Increment Counter' and onClick={incrementCounter}",
          "Input field with type='text', value={inputValue}, and onChange={handleInputChange}"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added some elements, but make sure both the button and input field are present with the correct attributes and handlers.",
        "feedbackWrong": "The required interactive elements are missing or incorrect. Check that you've added both a button and an input field with the specified properties.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<button",
            "onClick={incrementCounter}",
            "Increment Counter",
            "<input",
            "type=\"text\"",
            "value={inputValue}",
            "onChange={handleInputChange}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step11",
        "type": "question",
        "phase": "Step 11 of 13",
        "title": "add an input field with type='text', value bo",
        "instruction": "add an input field with type='text', value bound to inputValue, and an onChange handler that calls handleInputChange.",
        "hint": "Place these elements after the headings you added earlier. The button should trigger the counter increment, and the input should update the inputValue state.",
        "analogousExample": "// Adding interactive elements\n<button onClick={incrementCounter}>Increment</button>\n<input type=\"text\" value={inputValue} onChange={handleInputChange} />",
        "seedCode": "import React, { useState, useMemo, useCallback } from 'react';\n\n// Create the App component here\nconst App = () => {\n  // Initialize state variables here\n  const [counter, setCounter] = useState(0);\n  const [inputValue, setInputValue] = useState('');\n  \n  // Create the expensiveCalculation function here\n  const expensiveCalculation = (num) => {\n    console.log('Calculating...');\n    let result = 0;\n    for (let i = 0; i < 1000000000; i++) {\n      result += num;\n    }\n    return result;\n  };\n  \n  // Create memoizedResult with useMemo here\n  const memoizedResult = useMemo(() => expensiveCalculation(counter), [counter]);\n  \n  // Create event handlers for counter and input here\n  const incrementCounter = () => {\n    setCounter(counter + 1);\n  };\n  \n  const handleInputChange = (e) => {\n    setInputValue(e.target.value);\n  };\n  \n  // Create a non-memoized callback function here\n  const nonMemoizedCallback = () => {\n    console.log('Non-memoized callback called');\n  };\n  \n  // Create a memoized callback with useCallback here\n  const memoizedCallback = useCallback(() => {\n    console.log('Memoized callback called');\n  }, []);\n  \n  return (\n    <div>\n      <h1>Memoization Strategy Demo</h1>\n      <h2>Counter: {counter}</h2>\n      <h2>Input Value: {inputValue}</h2>\n    </div>\n  );\n};",
        "expectedOutcome": "",
        "successCriteria": [
          "Complete the step as instructed."
        ],
        "feedbackCorrect": "Great! Step completed.",
        "feedbackPartial": "Review the instruction and try again.",
        "feedbackWrong": "Check the instruction and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<button",
            "onClick={incrementCounter}",
            "Increment Counter",
            "<input",
            "type=\"text\"",
            "value={inputValue}",
            "onChange={handleInputChange}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step12",
        "type": "question",
        "phase": "Step 12 of 13",
        "title": "Display calculation results and performance comparison",
        "instruction": "Add two paragraphs below the existing headings to display both the memoized calculation result and the non-memoized calculation result.",
        "hint": "Create two <p> elements: one showing memoizedResult and another showing expensiveCalculation(counter) directly.",
        "analogousExample": "// Displaying computed values\n<p>Memoized result: {memoizedValue}</p>\n<p>Non-memoized result: {calculateValue(input)}</p>",
        "seedCode": "import React, { useState, useMemo, useCallback } from 'react';\n\n// Create the App component here\nconst App = () => {\n  // Initialize state variables here\n  const [counter, setCounter] = useState(0);\n  const [inputValue, setInputValue] = useState('');\n  \n  // Create the expensiveCalculation function here\n  const expensiveCalculation = (num) => {\n    console.log('Calculating...');\n    let result = 0;\n    for (let i = 0; i < 1000000000; i++) {\n      result += num;\n    }\n    return result;\n  };\n  \n  // Create memoizedResult with useMemo here\n  const memoizedResult = useMemo(() => expensiveCalculation(counter), [counter]);\n  \n  // Create event handlers for counter and input here\n  const incrementCounter = () => {\n    setCounter(counter + 1);\n  };\n  \n  const handleInputChange = (e) => {\n    setInputValue(e.target.value);\n  };\n  \n  // Create a non-memoized callback function here\n  const nonMemoizedCallback = () => {\n    console.log('Non-memoized callback called');\n  };\n  \n  // Create a memoized callback with useCallback here\n  const memoizedCallback = useCallback(() => {\n    console.log('Memoized callback called');\n  }, []);\n  \n  return (\n    <div>\n      <h1>Memoization Strategy Demo</h1>\n      <h2>Counter: {counter}</h2>\n      <h2>Input Value: {inputValue}</h2>\n      {/* Add paragraphs for calculation results here */}\n    </div>\n  );\n};",
        "expectedOutcome": "The UI will show two new lines: one displaying the memoized calculation result and another displaying the non-memoized calculation result, allowing comparison.",
        "successCriteria": [
          "Two <p> elements are added: one showing memoizedResult and another showing expensiveCalculation(counter)."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added some result display, but make sure to include both the memoized and non-memoized calculations.",
        "feedbackWrong": "The calculation results aren't displayed correctly. Add two paragraphs showing memoizedResult and expensiveCalculation(counter).",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<p>",
            "memoizedResult",
            "expensiveCalculation(counter)"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step13",
        "type": "question",
        "phase": "Step 13 of 13",
        "title": "Add buttons to demonstrate function memoization",
        "instruction": "Add two buttons inside the div: one that calls memoizedCallback when clicked, and another that calls nonMemoizedCallback when clicked. Label them appropriately to show which is memoized.",
        "hint": "Use the onClick prop on button elements to call the respective callback functions. Place the buttons after the existing paragraphs.",
        "analogousExample": "// Adding buttons with click handlers\n<button onClick={handleClick}>Click me</button>\n<button onClick={anotherHandler}>Another action</button>",
        "seedCode": "import React, { useState, useMemo, useCallback } from 'react';\n\n// Create the App component here\nconst App = () => {\n  // Initialize state variables here\n  const [counter, setCounter] = useState(0);\n  const [inputValue, setInputValue] = useState('');\n  \n  // Create the expensiveCalculation function here\n  const expensiveCalculation = (num) => {\n    console.log('Calculating...');\n    let result = 0;\n    for (let i = 0; i < 1000000000; i++) {\n      result += num;\n    }\n    return result;\n  };\n  \n  // Create memoizedResult with useMemo here\n  const memoizedResult = useMemo(() => expensiveCalculation(counter), [counter]);\n  \n  // Create event handlers for counter and input here\n  const incrementCounter = () => {\n    setCounter(counter + 1);\n  };\n  \n  const handleInputChange = (e) => {\n    setInputValue(e.target.value);\n  };\n  \n  // Create a non-memoized callback function here\n  const nonMemoizedCallback = () => {\n    console.log('Non-memoized callback called');\n  };\n  \n  // Create a memoized callback with useCallback here\n  const memoizedCallback = useCallback(() => {\n    console.log('Memoized callback called');\n  }, []);\n  \n  return (\n    <div>\n      <h1>Memoization Strategy Demo</h1>\n      <h2>Counter: {counter}</h2>\n      <h2>Input Value: {inputValue}</h2>\n      {/* Add paragraphs for calculation results here */}\n      {/* Add buttons for memoized and non-memoized callbacks here */}\n    </div>\n  );\n};",
        "expectedOutcome": "Two buttons appear in the UI: one labeled for the memoized callback and one for the non-memoized callback, each with appropriate click handlers.",
        "successCriteria": [
          "Two button elements are added with onClick handlers calling memoizedCallback and nonMemoizedCallback respectively, with clear labels."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added at least one button, but make sure to include both buttons with the correct onClick handlers and labels.",
        "feedbackWrong": "The buttons are missing or not correctly wired. Add two buttons with onClick handlers for memoizedCallback and nonMemoizedCallback.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<button",
            "onClick={memoizedCallback}",
            "onClick={nonMemoizedCallback}",
            "Memoized",
            "Non-memoized"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import React, useState, useMemo, and useCallback",
        "id": "step1"
      },
      {
        "label": "Create the main App component",
        "id": "step2"
      },
      {
        "label": "Initialize state for counter and input value",
        "id": "step3"
      },
      {
        "label": "Create a non-memoized expensive calculation function",
        "id": "step4"
      },
      {
        "label": "Create a memoized version with useMemo",
        "id": "step5"
      },
      {
        "label": "Create event handlers for counter and input",
        "id": "step6"
      },
      {
        "label": "Create a non-memoized callback function",
        "id": "step7"
      },
      {
        "label": "Create a memoized callback with useCallback",
        "id": "step8"
      },
      {
        "label": "Add the basic JSX structure with headings",
        "id": "step9"
      },
      {
        "label": "Add counter controls and input field",
        "id": "step10"
      },
      {
        "label": "add an input field with type='text', value bo",
        "id": "step11"
      },
      {
        "label": "Display calculation results and performance comparison",
        "id": "step12"
      },
      {
        "label": "Add buttons to demonstrate function memoization",
        "id": "step13"
      }
    ]
  },
  "source": "content"
}