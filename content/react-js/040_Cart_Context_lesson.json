{
  "success": true,
  "config": {
    "lessonId": "ai-react-js-40",
    "track": "react-js",
    "lessonNum": 40,
    "title": "Cart Context",
    "shortName": "P40",
    "intro": {
      "tag": "LESSON #40",
      "title": "Cart Context",
      "body": "In this lesson, you will build a shopping cart feature for an e-commerce app using React's Context API. You will learn how to create a centralized Cart Context to manage cart state, such as items and totals, and make it accessible across multiple components without prop drilling. The core programming concept involves using React's useContext hook and Context.Provider to share state globally within your application. By the end, you will have a functional cart that can add items, update quantities, and calculate the total price, all managed through a clean, reusable context structure.",
      "usecase": "Cart Context is essential for e-commerce platforms like Amazon or Shopify, where shopping cart data must be accessible across multiple components—product listings, cart summaries, checkout pages, and header badges. This pattern allows users to add items from product cards while instantly updating the cart total in a sidebar panel, without prop drilling through intermediate components. Developers implement Cart Context to maintain consistent state across complex UI flows, ensuring cart persistence during navigation and enabling features like saved-for-later items or multi-step checkout processes."
    },
    "objectives": [
      "Initialize a React context to manage cart state across components",
      "Construct a custom provider component that supplies cart data and functions",
      "Implement state management logic for adding, removing, and updating cart items",
      "Apply the useContext hook to access cart functionality in child components",
      "Configure context values to include both cart items and manipulation methods",
      "Integrate the cart context provider into the application component hierarchy",
      "Manipulate cart state through context functions from different UI components"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 11",
        "title": "Import React, createContext, useContext, and useState",
        "instruction": "Import React, createContext, useContext, and useState from 'react'.",
        "hint": "Use the import statement to bring in the necessary modules from the 'react' package.",
        "analogousExample": "// Example: importing React and a hook\nimport React, { useState } from 'react';",
        "seedCode": "// Import React, createContext, useContext, and useState from 'react' here",
        "expectedOutcome": "The import statements are added at the top of the file, making React, createContext, useContext, and useState available for use.",
        "successCriteria": [
          "React, createContext, useContext, and useState are imported from 'react'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've imported some of the required modules, but not all. Make sure to import React, createContext, useContext, and useState from 'react'.",
        "feedbackWrong": "The import statements are missing or incorrect. You need to import React, createContext, useContext, and useState from 'react'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "React",
            "createContext",
            "useContext",
            "useState",
            "'react'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 11",
        "title": "Create CartContext using createContext",
        "instruction": "Create a CartContext using React.createContext() and export it as a named export.",
        "hint": "Call createContext from React to create a new context object. Use export const to make it available for import in other files.",
        "analogousExample": "// Creating and exporting a context\nconst ThemeContext = React.createContext();\nexport { ThemeContext };",
        "seedCode": "// Import React, createContext, useContext, and useState from 'react' here\nimport React, { createContext, useContext, useState } from 'react';\n\n// Create CartContext using createContext and export it\n",
        "expectedOutcome": "A CartContext object is created and exported, ready to be used as a context provider and consumer.",
        "successCriteria": [
          "CartContext is created with React.createContext() and exported as a named export."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you're using createContext from React and exporting the CartContext variable.",
        "feedbackWrong": "create a context object using React.createContext() and export it so other components can use it.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "CartContext",
            "createContext",
            "export"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 11",
        "title": "Create CartProvider component",
        "instruction": "Create a functional component named CartProvider that accepts children as props.",
        "hint": "Define a component that takes a children prop and returns something (you can return null for now).",
        "analogousExample": "// Similar: a provider component\nfunction ThemeProvider({ children }) {\n  return null;\n}",
        "seedCode": "import React, { createContext, useContext, useState } from 'react';\n\n// Create CartContext using createContext and export it\n\n// Create CartProvider component here",
        "expectedOutcome": "A CartProvider component is defined with children as a prop.",
        "successCriteria": [
          "CartProvider is defined as a function that accepts children as a prop."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you define a function named CartProvider that takes children as a prop.",
        "feedbackWrong": "The component wasn't created correctly. Create a function named CartProvider that accepts children as a prop.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "CartProvider",
            "children",
            "function",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 11",
        "title": "Initialize cart state with useState",
        "instruction": "Inside the CartProvider component, use the useState hook to create a state variable named 'cart' initialized as an empty array.",
        "hint": "Call useState with an empty array [] as the initial value, and destructure the returned array into 'cart' and 'setCart'.",
        "analogousExample": "// Example: state for a list\nconst [items, setItems] = useState([]);",
        "seedCode": "import React, { createContext, useContext, useState } from 'react';\n\n// Create CartContext using createContext and export it\n\n// Create CartProvider component here\nconst CartProvider = ({ children }) => {\n  // Initialize cart state here\n  return null;\n};",
        "expectedOutcome": "The CartProvider component will have a cart state variable ready to hold cart items.",
        "successCriteria": [
          "The CartProvider component contains a useState call that initializes 'cart' as an empty array."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using useState with an empty array [] as the initial value.",
        "feedbackWrong": "Check that you've used useState inside CartProvider to create a 'cart' state variable initialized to an empty array.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useState",
            "cart",
            "[]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 11",
        "title": "Create addToCart function",
        "instruction": "Inside the CartProvider component, define a function named addToCart that takes an item object as a parameter.",
        "hint": "You can use either a function declaration or an arrow function. The function should be defined before the return statement.",
        "analogousExample": "// Example: a simple function that takes an item\nfunction addItem(item) {\n  // Function body will go here\n}",
        "seedCode": "import React, { createContext, useContext, useState } from 'react';\n\n// Create CartContext using createContext and export it\n\n// Create CartProvider component here\nconst CartProvider = ({ children }) => {\n  // Initialize cart state here\n  \n  // Define addToCart function here\n  \n  return null;\n};",
        "expectedOutcome": "You should have an addToCart function defined inside the CartProvider component.",
        "successCriteria": [
          "The addToCart function is defined with a single parameter named 'item'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure the function is named exactly 'addToCart' and takes one parameter called 'item'.",
        "feedbackWrong": "The addToCart function isn't defined correctly. Check that it's inside the CartProvider component and has the right name and parameter.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "addToCart",
            "item"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 11",
        "title": "Prepare value object for CartContext.Provider",
        "instruction": "Inside the CartProvider component, create a value object that contains the cart state and all three cart functions (addToCart, removeFromCart, clearCart).",
        "hint": "Create a JavaScript object with properties for cart, addToCart, removeFromCart, and clearCart. Use the variable names you've already defined.",
        "analogousExample": "// Example: creating a value object\nconst userData = {\n  name: 'Alex',\n  age: 25,\n  greet: function() { console.log('Hello!'); }\n};",
        "seedCode": "import React, { createContext, useContext, useState } from 'react';\n\n// Create CartContext using createContext and export it\n\n// Create CartProvider component here\nconst CartProvider = ({ children }) => {\n  // Initialize cart state here\n  \n  // Define addToCart function here\n  \n  // Define removeFromCart function here\n  \n  // Define clearCart function here\n  \n  // Create value object here\n  \n  return null;\n};",
        "expectedOutcome": "A value object is created inside CartProvider, containing cart state and the three cart functions as properties.",
        "successCriteria": [
          "A value object is defined with properties for cart, addToCart, removeFromCart, and clearCart."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your value object includes all four properties: cart, addToCart, removeFromCart, and clearCart.",
        "feedbackWrong": "Try creating a JavaScript object with properties for cart, addToCart, removeFromCart, and clearCart. Remember to use the variable names you've already defined.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "cart",
            "addToCart",
            "removeFromCart",
            "clearCart",
            "{"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 11",
        "title": "Wrap children with CartContext.Provider",
        "instruction": "In your CartProvider component, return CartContext.Provider with the value object, wrapping the children prop.",
        "hint": "Use the CartContext.Provider component and pass the value object as a prop. The children prop should be placed between the opening and closing tags.",
        "analogousExample": "// Similar: wrapping children with a provider\nconst ThemeProvider = ({ children }) => {\n  const theme = 'dark';\n  return (\n    <ThemeContext.Provider value={theme}>\n      {children}\n    </ThemeContext.Provider>\n  );\n};",
        "seedCode": "import React, { createContext, useContext, useState } from 'react';\n\n// Create CartContext using createContext and export it\n\n// Create CartProvider component here\nconst CartProvider = ({ children }) => {\n  // Initialize cart state here\n  \n  // Define addToCart function here\n  \n  // Define removeFromCart function here\n  \n  // Define clearCart function here\n  \n  // Create value object here\n  \n  return (\n    // Return CartContext.Provider with value and children here\n  );\n};",
        "expectedOutcome": "The CartProvider component returns CartContext.Provider with the value object, wrapping the children prop.",
        "successCriteria": [
          "CartContext.Provider is returned with the value object and children prop wrapped inside."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you're returning CartContext.Provider with both the value prop and the children prop wrapped inside.",
        "feedbackWrong": "That doesn't look right. Remember to return CartContext.Provider with the value object and wrap the children prop inside it.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "CartContext.Provider",
            "value",
            "children",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 11",
        "title": "Create useCart custom hook",
        "instruction": "Define and export a custom hook named 'useCart' that returns the value from useContext(CartContext).",
        "hint": "Create a function that calls useContext with CartContext and returns its value. Remember to export it so other components can use it.",
        "analogousExample": "// Similar: custom hook for theme context\nfunction useTheme() {\n  return useContext(ThemeContext);\n}",
        "seedCode": "import React, { createContext, useContext, useState } from 'react';\n\n// Create CartContext using createContext and export it\n\n// Create CartProvider component here\nconst CartProvider = ({ children }) => {\n  // Initialize cart state here\n  \n  // Define addToCart function here\n  \n  // Define removeFromCart function here\n  \n  // Define clearCart function here\n  \n  // Create value object here\n  \n  return (\n    // Return CartContext.Provider with value and children here\n  );\n};\n\n// Define and export useCart hook here",
        "expectedOutcome": "A custom hook named useCart is defined and exported, which returns the value from useContext(CartContext).",
        "successCriteria": [
          "The useCart hook is defined and exported, and it returns useContext(CartContext)."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your useCart hook returns the value from useContext(CartContext) and is exported.",
        "feedbackWrong": "The useCart hook should be defined and exported, and it should return useContext(CartContext). Try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useCart",
            "useContext",
            "CartContext",
            "return",
            "export"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 11",
        "title": "Create ProductItem component",
        "instruction": "Create a functional component named ProductItem that accepts product data as props and returns a simple div with the product name.",
        "hint": "Define a component that takes props as its parameter and returns JSX. You can access the product name via props.product.name.",
        "analogousExample": "// Example of a component that receives props\nfunction UserCard(props) {\n  return (\n    <div>\n      <h3>{props.user.name}</h3>\n    </div>\n  );\n}",
        "seedCode": "import React, { createContext, useContext, useState } from 'react';\n\n// Create CartContext using createContext and export it\n\n// Create CartProvider component here\nconst CartProvider = ({ children }) => {\n  // Initialize cart state here\n  \n  // Define addToCart function here\n  \n  // Define removeFromCart function here\n  \n  // Define clearCart function here\n  \n  // Create value object here\n  \n  return (\n    // Return CartContext.Provider with value and children here\n  );\n};\n\n// Define and export useCart hook here\n\n// Create ProductItem component here\n",
        "expectedOutcome": "You will have a ProductItem component that can display product information.",
        "successCriteria": [
          "ProductItem component is defined, accepts props, and returns JSX containing the product name."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've started creating the ProductItem component, but make sure it accepts props and returns JSX with the product name.",
        "feedbackWrong": "The ProductItem component isn't properly defined yet. Create a component that takes props and returns a div with the product name.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "ProductItem",
            "props",
            "return",
            "div",
            "product.name"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 11",
        "title": "Use useCart hook in ProductItem and add Add to Car",
        "instruction": "Inside the ProductItem component, use the useCart hook to get the addToCart function,",
        "hint": "Call useCart() to get the addToCart function, then create a button element with onClick={() => addToCart(product)}.",
        "analogousExample": "// Example: Using a custom hook in a component\nfunction ProductItem({ product }) {\n  const { addToCart } = useCart();\n  return (\n    <div>\n      <button onClick={() => addToCart(product)}>Add to Cart</button>\n    </div>\n  );\n}",
        "seedCode": "import React, { createContext, useContext, useState } from 'react';\n\nconst CartContext = createContext();\n\nconst CartProvider = ({ children }) => {\n  const [cart, setCart] = useState([]);\n\n  const addToCart = (product) => {\n    setCart([...cart, product]);\n  };\n\n  const removeFromCart = (productId) => {\n    setCart(cart.filter(item => item.id !== productId));\n  };\n\n  const clearCart = () => {\n    setCart([]);\n  };\n\n  const value = {\n    cart,\n    addToCart,\n    removeFromCart,\n    clearCart\n  };\n\n  return (\n    <CartContext.Provider value={value}>\n      {children}\n    </CartContext.Provider>\n  );\n};\n\nconst useCart = () => {\n  const context = useContext(CartContext);\n  if (!context) {\n    throw new Error('useCart must be used within CartProvider');\n  }\n  return context;\n};\n\nconst ProductItem = ({ product }) => {\n  // Use useCart hook here\n  \n  return (\n    <div>\n      <h3>{product.name}</h3>\n      <p>${product.price}</p>\n      {/* Add Add to Cart button here */}\n    </div>\n  );\n};",
        "expectedOutcome": "The ProductItem component will have access to the addToCart function via useCart, and an 'Add to Cart' button that adds the product to the cart when clicked.",
        "successCriteria": [
          "useCart hook is called inside ProductItem",
          "addToCart function is destructured from useCart",
          "Button element with onClick handler is added",
          "onClick calls addToCart with the product object"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you're calling useCart() to get the addToCart function, and the button's onClick should call addToCart with the product.",
        "feedbackWrong": "Let's review: Inside ProductItem, call useCart() to get the addToCart function, then add a button with onClick={() => addToCart(product)}.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useCart",
            "addToCart",
            "onClick",
            "product"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step11",
        "type": "question",
        "phase": "Step 11 of 11",
        "title": "add a button with an onClick handler that cal",
        "instruction": "add a button with an onClick handler that calls addToCart with the product object.",
        "hint": "Call useCart() to get the addToCart function, then create a button element with onClick={() => addToCart(product)}.",
        "analogousExample": "// Example: Using a custom hook in a component\nfunction ProductItem({ product }) {\n  const { addToCart } = useCart();\n  return (\n    <div>\n      <button onClick={() => addToCart(product)}>Add to Cart</button>\n    </div>\n  );\n}",
        "seedCode": "import React, { createContext, useContext, useState } from 'react';\n\nconst CartContext = createContext();\n\nconst CartProvider = ({ children }) => {\n  const [cart, setCart] = useState([]);\n\n  const addToCart = (product) => {\n    setCart([...cart, product]);\n  };\n\n  const removeFromCart = (productId) => {\n    setCart(cart.filter(item => item.id !== productId));\n  };\n\n  const clearCart = () => {\n    setCart([]);\n  };\n\n  const value = {\n    cart,\n    addToCart,\n    removeFromCart,\n    clearCart\n  };\n\n  return (\n    <CartContext.Provider value={value}>\n      {children}\n    </CartContext.Provider>\n  );\n};\n\nconst useCart = () => {\n  const context = useContext(CartContext);\n  if (!context) {\n    throw new Error('useCart must be used within CartProvider');\n  }\n  return context;\n};\n\nconst ProductItem = ({ product }) => {\n  // Use useCart hook here\n  \n  return (\n    <div>\n      <h3>{product.name}</h3>\n      <p>${product.price}</p>\n      {/* Add Add to Cart button here */}\n    </div>\n  );\n};",
        "expectedOutcome": "",
        "successCriteria": [
          "Complete the step as instructed."
        ],
        "feedbackCorrect": "Great! Step completed.",
        "feedbackPartial": "Review the instruction and try again.",
        "feedbackWrong": "Check the instruction and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useCart",
            "addToCart",
            "onClick",
            "product"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import React, createContext, useContext, and useState",
        "id": "step1"
      },
      {
        "label": "Create CartContext using createContext",
        "id": "step2"
      },
      {
        "label": "Create CartProvider component",
        "id": "step3"
      },
      {
        "label": "Initialize cart state with useState",
        "id": "step4"
      },
      {
        "label": "Create addToCart function",
        "id": "step5"
      },
      {
        "label": "Prepare value object for CartContext.Provider",
        "id": "step6"
      },
      {
        "label": "Wrap children with CartContext.Provider",
        "id": "step7"
      },
      {
        "label": "Create useCart custom hook",
        "id": "step8"
      },
      {
        "label": "Create ProductItem component",
        "id": "step9"
      },
      {
        "label": "Use useCart hook in ProductItem and add Add to Car",
        "id": "step10"
      },
      {
        "label": "add a button with an onClick handler that cal",
        "id": "step11"
      }
    ]
  },
  "source": "content"
}