{
  "success": true,
  "config": {
    "lessonId": "ai-react-js-24",
    "track": "react-js",
    "lessonNum": 24,
    "title": "Simple Todo List — CRUD",
    "shortName": "JS — TODO LIST",
    "intro": {
      "tag": "LESSON #24",
      "title": "Simple Todo List — CRUD",
      "body": "You will build a small todo app: add tasks, mark them complete, and delete them. You will use React `useState` for list state, plain objects for each task (optionally documented with JSDoc), and immutable updates with `map`, `filter`, and functional `setState`. By the end you will have a clear CRUD loop you can reuse in dashboards, carts, and admin UIs.",
      "usecase": "The same pattern appears everywhere: task lists, shopping carts, notification queues, and editable tables. Managing an array in state, deriving UI with `map`, and updating with pure-style setters is core React work."
    },
    "objectives": [
      "Apply state management to maintain and mutate a dynamic collection of structured data items.",
      "Implement create, update, and delete operations on a stateful list without side effects.",
      "Construct event-driven handlers that modify specific items in a collection by unique identity.",
      "Produce immutable state updates that guarantee UI consistency across add, toggle, and delete operations.",
      "Render a dynamic data collection as a structured list with stable identity binding per item.",
      "Wire user interaction events to corresponding state mutation handlers in a component's UI."
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 14",
        "title": "Import React and useState",
        "instruction": "Import React and the `useState` hook from the `react` package.",
        "hint": "Use: `import React, { useState } from 'react'`.",
        "analogousExample": "import React, { useState } from 'react';",
        "seedCode": "// Import React and useState here\n",
        "expectedOutcome": "Imports are present so the file can use JSX and `useState`.",
        "successCriteria": [
          "React and `useState` are imported from `'react'`.",
          "The file is ready for a component and hooks."
        ],
        "feedbackCorrect": "✅ Imports look good. Next: document the todo shape.",
        "feedbackPartial": "Add both React and `useState` from `'react'`.",
        "feedbackWrong": "You need `import` lines for React and `useState` from `'react'`.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "useState",
            "'react'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 14",
        "title": "Document todo shape (JSDoc)",
        "instruction": "Add a JSDoc `@typedef` named `Todo` describing each task: `id` (string), `text` (string), and `completed` (boolean). Use `@property` lines for each field.",
        "hint": "Example: `/** @typedef {Object} Todo` plus `@property {string} id` etc., closed with `*/`.",
        "analogousExample": "/**\n * @typedef {Object} Item\n * @property {string} id\n * @property {string} label\n */",
        "seedCode": "import React, { useState } from 'react';\n\n// Add JSDoc @typedef Todo here\n",
        "expectedOutcome": "The todo object shape is documented for the rest of the lesson.",
        "successCriteria": [
          "`Todo` is described with `id`, `text`, and `completed`."
        ],
        "feedbackCorrect": "✅ Shape documented. Next: scaffold the list component.",
        "feedbackPartial": "Include `@typedef`, `Todo`, and `@property` for id, text, and completed.",
        "feedbackWrong": "Add a JSDoc block with `@typedef {Object} Todo` and `@property` for each field.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "@typedef",
            "Todo",
            "@property",
            "id",
            "text",
            "completed"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 14",
        "title": "Create main TodoList component",
        "instruction": "Create a functional component named `TodoList` that returns a JSX `<div>` wrapper (you will fill it in later).",
        "hint": "Use `const TodoList = () => { ... return ( <div>...</div> ); };` or `export default function TodoList()`.",
        "analogousExample": "export default function Panel() {\n  return <div className=\"panel\" />;\n}",
        "seedCode": "import React, { useState } from 'react';\n\n/**\n * @typedef {Object} Todo\n * @property {string} id\n * @property {string} text\n * @property {boolean} completed\n */\n\n// Create the TodoList component here\n",
        "expectedOutcome": "A `TodoList` component exists as the root of the UI.",
        "successCriteria": [
          "Component is named `TodoList` and returns JSX."
        ],
        "feedbackCorrect": "✅ Component shell is in place.",
        "feedbackPartial": "Ensure the component is named `TodoList` and returns a `<div>`.",
        "feedbackWrong": "Add a function component `TodoList` that returns JSX.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "TodoList",
            "return",
            "<div>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 14",
        "title": "Initialize todos state",
        "instruction": "Inside `TodoList`, add `const [todos, setTodos] = useState([])` (you can start with `[]` or a few demo objects).",
        "hint": "Each item in the array should look like `{ id, text, completed }` to match your JSDoc.",
        "analogousExample": "const [items, setItems] = useState([]);",
        "seedCode": "import React, { useState } from 'react';\n\n/**\n * @typedef {Object} Todo\n * @property {string} id\n * @property {string} text\n * @property {boolean} completed\n */\n\nconst TodoList = () => {\n  // Initialize todos state here\n\n  return null;\n};\n",
        "expectedOutcome": "List state is ready to hold todo objects.",
        "successCriteria": [
          "`todos` and `setTodos` from `useState` with an array initial value."
        ],
        "feedbackCorrect": "✅ State is initialized. Next: add the labeled new-todo input in the JSX.",
        "feedbackPartial": "Use `useState([...])` and destructure `todos` and `setTodos`.",
        "feedbackWrong": "Declare state with `useState` and an initial array.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "todos",
            "useState",
            "[]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 14",
        "title": "New todo field (label + input)",
        "instruction": "In the `return`, add a `<label>` whose text is `new todo` and a `<input type=\"text\" />` for typing the next task. Link them with `htmlFor` on the label and a matching `id` on the input (for example `new-todo-input`). Do not add `value` or `onChange` yet—you will connect this field to React state in the following steps.",
        "hint": "`<label htmlFor=\"new-todo-input\">new todo</label>` plus `<input id=\"new-todo-input\" type=\"text\" />`",
        "analogousExample": "<label htmlFor=\"q\">Question</label><input id=\"q\" type=\"text\" />",
        "seedCode": "import React, { useState } from 'react';\n\n/**\n * @typedef {Object} Todo\n * @property {string} id\n * @property {string} text\n * @property {boolean} completed\n */\n\nconst TodoList = () => {\n  const [todos, setTodos] = useState([]);\n  // Add label (text: new todo) + text input inside the div\n\n  return (\n    <div>\n    </div>\n  );\n};\n",
        "expectedOutcome": "The form control exists in the UI before it is made controlled.",
        "successCriteria": [
          "A `<label>` with the text `new todo`.",
          "A `<input type=\"text\" />` linked via `htmlFor` / `id`."
        ],
        "feedbackCorrect": "✅ The field is on the page. Next: store the draft text in state.",
        "feedbackPartial": "Use paired `htmlFor` and `id`, and `type=\"text\"` on the input.",
        "feedbackWrong": "Add a label reading `new todo` and a text input, associated with `htmlFor` and `id`.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<label",
            "<input",
            "type",
            "text",
            "htmlFor"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 14",
        "title": "State for new todo text",
        "instruction": "Declare a state variable to hold the text the user will type into the new todo field, and bind that state to the `<input>` from Step 5 using `value={newTodoText}`. You will add `onChange` in a later step so keystrokes update this state.",
        "hint": "String state for form drafts almost always starts as an empty string: `useState('')`.",
        "analogousExample": "const [draft, setDraft] = useState('');",
        "seedCode": "import React, { useState } from 'react';\n\n/**\n * @typedef {Object} Todo\n * @property {string} id\n * @property {string} text\n * @property {boolean} completed\n */\n\nconst TodoList = () => {\n  const [todos, setTodos] = useState([]);\n  // Declare newTodoText state here (useState for the draft string)\n\n  return (\n    <div>\n      <label htmlFor=\"new-todo-input\">new todo</label>\n      {/* Add value={newTodoText} on the input below */}\n      <input id=\"new-todo-input\" type=\"text\" />\n    </div>\n  );\n};\n",
        "expectedOutcome": "Draft text lives in state and is bound to the input’s `value` before `onChange` is wired.",
        "successCriteria": [
          "`newTodoText` / `setNewTodoText` (or equivalent) from `useState('')`.",
          "The `<input>` uses `value={newTodoText}`."
        ],
        "feedbackCorrect": "✅ State is bound to the field’s `value`. Next: add a change handler for that draft.",
        "feedbackPartial": "Add `useState('')` for the draft and pass `value={newTodoText}` to the text input.",
        "feedbackWrong": "Declare `newTodoText` with `useState` and set `value={newTodoText}` on the new-todo `<input>`.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "newTodoText",
            "useState",
            "setNewTodoText",
            "value"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 14",
        "title": "Handler for new-todo text changes",
        "instruction": "In `TodoList`, add an `onChange` handler named `handleNewTodoChange`. Each time it runs, pass `e.target.value` to `setNewTodoText`. Define it after your state hooks and before the `return`—you will pass it to the `<input>` in Step 8; for now, only implement this function.",
        "hint": "`const handleNewTodoChange = (e) => setNewTodoText(e.target.value)`",
        "analogousExample": "const handleDraftChange = (e) => setDraft(e.target.value);",
        "seedCode": "import React, { useState } from 'react';\n\n/**\n * @typedef {Object} Todo\n * @property {string} id\n * @property {string} text\n * @property {boolean} completed\n */\n\nconst TodoList = () => {\n  const [todos, setTodos] = useState([]);\n  const [newTodoText, setNewTodoText] = useState('');\n  // Define handleNewTodoChange here\n\n  return (\n    <div>\n      <label htmlFor=\"new-todo-input\">new todo</label>\n      <input id=\"new-todo-input\" type=\"text\" value={newTodoText} />\n    </div>\n  );\n};\n",
        "expectedOutcome": "A named handler updates the draft string from a change event; the input already uses `value={newTodoText}`.",
        "successCriteria": [
          "A function like `handleNewTodoChange` is defined inside `TodoList`.",
          "It calls `setNewTodoText` with `e.target.value`."
        ],
        "feedbackCorrect": "✅ Change handler is defined. Next: render the controlled input that uses it.",
        "feedbackPartial": "Declare `handleNewTodoChange` and call `setNewTodoText` with `e.target.value`.",
        "feedbackWrong": "Add `handleNewTodoChange` that updates `newTodoText` from the event.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "handleNewTodoChange",
            "setNewTodoText",
            "target"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 14",
        "title": "Make the new-todo input controlled",
        "instruction": "On the `<input>` that already has `value={newTodoText}`, add `onChange={handleNewTodoChange}` and a helpful `placeholder`. Keep the same `id` and `<label htmlFor>` pairing from Step 5. Reference the handler by name—do not duplicate the change logic inline in JSX.",
        "hint": "Controlled field: `value` from state (Step 6), `onChange` points at your handler function.",
        "analogousExample": "<input type=\"text\" value={draft} onChange={handleDraftChange} placeholder=\"…\" />",
        "seedCode": "import React, { useState } from 'react';\n\n/**\n * @typedef {Object} Todo\n * @property {string} id\n * @property {string} text\n * @property {boolean} completed\n */\n\nconst TodoList = () => {\n  const [todos, setTodos] = useState([]);\n  const [newTodoText, setNewTodoText] = useState('');\n  const handleNewTodoChange = (e) => setNewTodoText(e.target.value);\n\n  return (\n    <div>\n      <label htmlFor=\"new-todo-input\">new todo</label>\n      {/* Add onChange={handleNewTodoChange} and placeholder on the input below */}\n      <input id=\"new-todo-input\" type=\"text\" value={newTodoText} />\n    </div>\n  );\n};\n",
        "expectedOutcome": "Keystrokes update `newTodoText` because both `value` and `onChange` are set.",
        "successCriteria": [
          "`onChange={handleNewTodoChange}` on the text input.",
          "A `placeholder` on the field."
        ],
        "feedbackCorrect": "✅ Controlled input is wired. Next: implement `addTodo`.",
        "feedbackPartial": "Add `onChange={handleNewTodoChange}` and a `placeholder` to the input that already has `value`.",
        "feedbackWrong": "Pass `onChange={handleNewTodoChange}` (and a `placeholder`) to the `<input>` with `value={newTodoText}`.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<input",
            "value",
            "newTodoText",
            "onChange",
            "handleNewTodoChange"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 14",
        "title": "Create addTodo handler",
        "instruction": "Add `addTodo(text)` that appends a new todo with a unique `id`, the given `text`, and `completed: false`. Use `setTodos` with a new array. You already have `newTodoText`, `handleNewTodoChange`, and the controlled input from the previous steps; a later step will wire the button to call `addTodo(newTodoText)`.",
        "hint": "`setTodos((prev) => [...prev, { id: Date.now().toString(), text, completed: false }])` keeps updates immutable.",
        "analogousExample": "setItems((prev) => [...prev, { id: String(Date.now()), label, done: false }]);",
        "seedCode": "import React, { useState } from 'react';\n\n/**\n * @typedef {Object} Todo\n * @property {string} id\n * @property {string} text\n * @property {boolean} completed\n */\n\nconst TodoList = () => {\n  const [todos, setTodos] = useState([]);\n  const [newTodoText, setNewTodoText] = useState('');\n  const handleNewTodoChange = (e) => setNewTodoText(e.target.value);\n  // Create addTodo handler here\n\n  return (\n    <div>\n      <label htmlFor=\"new-todo-input\">new todo</label>\n      <input\n        id=\"new-todo-input\"\n        type=\"text\"\n        value={newTodoText}\n        onChange={handleNewTodoChange}\n        placeholder=\"What needs to be done?\"\n      />\n    </div>\n  );\n};\n",
        "expectedOutcome": "New tasks can be added without mutating the previous array.",
        "successCriteria": [
          "`addTodo` takes text and calls `setTodos` with a new todo object."
        ],
        "feedbackCorrect": "✅ Add path is defined.",
        "feedbackPartial": "Use `setTodos` and spread or a functional update to append.",
        "feedbackWrong": "Implement `addTodo` with `setTodos` and a new todo object.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "addTodo",
            "setTodos",
            "text",
            "completed"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 14",
        "title": "Create toggleTodo handler",
        "instruction": "Add `toggleTodo(id)` that flips `completed` for the todo with that `id` (map over todos and return updated objects).",
        "hint": "`setTodos((prev) => prev.map((t) => (t.id === id ? { ...t, completed: !t.completed } : t)))`",
        "analogousExample": "setItems((prev) => prev.map((x) => (x.id === id ? { ...x, done: !x.done } : x)));",
        "seedCode": "import React, { useState } from 'react';\n\n/**\n * @typedef {Object} Todo\n * @property {string} id\n * @property {string} text\n * @property {boolean} completed\n */\n\nconst TodoList = () => {\n  const [todos, setTodos] = useState([\n    { id: '1', text: 'Learn React', completed: false },\n    { id: '2', text: 'Build a todo app', completed: false }\n  ]);\n  const [newTodoText, setNewTodoText] = useState('');\n\n  const handleNewTodoChange = (e) => setNewTodoText(e.target.value);\n\n  const addTodo = (text) => {\n    const newTodo = {\n      id: Date.now().toString(),\n      text,\n      completed: false\n    };\n    setTodos((prev) => [...prev, newTodo]);\n  };\n\n  // Create toggleTodo handler here\n\n  return (\n    <div>\n      <label htmlFor=\"new-todo-input\">new todo</label>\n      <input\n        id=\"new-todo-input\"\n        type=\"text\"\n        value={newTodoText}\n        onChange={handleNewTodoChange}\n        placeholder=\"What needs to be done?\"\n      />\n    </div>\n  );\n};\n",
        "expectedOutcome": "Clicking a checkbox can toggle completion in state.",
        "successCriteria": [
          "`toggleTodo` uses `setTodos` and `map` to update one item."
        ],
        "feedbackCorrect": "✅ Toggle logic is in place.",
        "feedbackPartial": "Map todos and flip `completed` when `id` matches.",
        "feedbackWrong": "Implement `toggleTodo(id)` with immutable `map` + `setTodos`.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "toggleTodo",
            "setTodos",
            "map",
            "completed"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step11",
        "type": "question",
        "phase": "Step 11 of 14",
        "title": "Create deleteTodo handler",
        "instruction": "Add `deleteTodo(id)` that removes the todo with that `id` using `filter`.",
        "hint": "`setTodos((prev) => prev.filter((t) => t.id !== id))`",
        "analogousExample": "setItems((prev) => prev.filter((x) => x.id !== id));",
        "seedCode": "import React, { useState } from 'react';\n\n/**\n * @typedef {Object} Todo\n * @property {string} id\n * @property {string} text\n * @property {boolean} completed\n */\n\nconst TodoList = () => {\n  const [todos, setTodos] = useState([\n    { id: '1', text: 'Learn React', completed: false },\n    { id: '2', text: 'Build a todo app', completed: false }\n  ]);\n  const [newTodoText, setNewTodoText] = useState('');\n\n  const handleNewTodoChange = (e) => setNewTodoText(e.target.value);\n\n  const addTodo = (text) => {\n    const newTodo = {\n      id: Date.now().toString(),\n      text,\n      completed: false\n    };\n    setTodos((prev) => [...prev, newTodo]);\n  };\n\n  const toggleTodo = (id) => {\n    setTodos((prev) =>\n      prev.map((t) => (t.id === id ? { ...t, completed: !t.completed } : t))\n    );\n  };\n\n  // Create deleteTodo handler here\n\n  return (\n    <div>\n      <label htmlFor=\"new-todo-input\">new todo</label>\n      <input\n        id=\"new-todo-input\"\n        type=\"text\"\n        value={newTodoText}\n        onChange={handleNewTodoChange}\n        placeholder=\"What needs to be done?\"\n      />\n    </div>\n  );\n};\n",
        "expectedOutcome": "A todo can be removed from state by id.",
        "successCriteria": [
          "`deleteTodo` uses `filter` and `setTodos`."
        ],
        "feedbackCorrect": "✅ Delete path is defined.",
        "feedbackPartial": "Filter out the matching `id` and set state.",
        "feedbackWrong": "Implement `deleteTodo(id)` with `filter` + `setTodos`.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "deleteTodo",
            "setTodos",
            "filter"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step12",
        "type": "question",
        "phase": "Step 12 of 14",
        "title": "Add Todo button and clear input",
        "instruction": "Add a button labeled `Add Todo` with `type=\"button\"`. On click, call `addTodo(newTodoText)` and then `setNewTodoText('')` to reset the field. Optionally ignore empty strings.",
        "hint": "`onClick={() => { addTodo(newTodoText); setNewTodoText(''); }}` or guard if `!newTodoText.trim()` return.",
        "analogousExample": "<input value={draft} onChange={handleDraftChange} />",
        "seedCode": "import React, { useState } from 'react';\n\n/**\n * @typedef {Object} Todo\n * @property {string} id\n * @property {string} text\n * @property {boolean} completed\n */\n\nconst TodoList = () => {\n  const [todos, setTodos] = useState([\n    { id: '1', text: 'Learn React', completed: false },\n    { id: '2', text: 'Build a todo app', completed: false }\n  ]);\n  const [newTodoText, setNewTodoText] = useState('');\n\n  const handleNewTodoChange = (e) => setNewTodoText(e.target.value);\n\n  const addTodo = (text) => {\n    const newTodo = {\n      id: Date.now().toString(),\n      text,\n      completed: false\n    };\n    setTodos((prev) => [...prev, newTodo]);\n  };\n\n  const toggleTodo = (id) => {\n    setTodos((prev) =>\n      prev.map((t) => (t.id === id ? { ...t, completed: !t.completed } : t))\n    );\n  };\n\n  const deleteTodo = (id) => {\n    setTodos((prev) => prev.filter((t) => t.id !== id));\n  };\n\n  return (\n    <div>\n      <label htmlFor=\"new-todo-input\">new todo</label>\n      <input\n        id=\"new-todo-input\"\n        type=\"text\"\n        value={newTodoText}\n        onChange={handleNewTodoChange}\n        placeholder=\"What needs to be done?\"\n      />\n      {/* Add Add Todo button here */}\n    </div>\n  );\n};\n",
        "expectedOutcome": "Users can submit the draft text to add a todo and see the field clear.",
        "successCriteria": [
          "`Add Todo` button calls `addTodo` with `newTodoText`.",
          "Input clears after add (or is ready to)."
        ],
        "feedbackCorrect": "✅ Add UI is present.",
        "feedbackPartial": "Include `<input>`, placeholder text, and a button `Add Todo`.",
        "feedbackWrong": "Add a text input and button in the returned JSX.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<button",
            "Add Todo",
            "onClick",
            "addTodo",
            "newTodoText"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step13",
        "type": "question",
        "phase": "Step 13 of 14",
        "title": "Render todos list",
        "instruction": "Map `todos` to a list: show text, a checkbox (`checked={todo.completed}`), and a Delete button. Use `key={todo.id}`.",
        "hint": "`{todos.map((todo) => ( <li key={todo.id}> ... </li> ))}`",
        "analogousExample": "items.map((item) => <li key={item.id}>{item.label}</li>)",
        "seedCode": "import React, { useState } from 'react';\n\n/**\n * @typedef {Object} Todo\n * @property {string} id\n * @property {string} text\n * @property {boolean} completed\n */\n\nconst TodoList = () => {\n  const [todos, setTodos] = useState([\n    { id: '1', text: 'Learn React', completed: false },\n    { id: '2', text: 'Build a todo app', completed: false }\n  ]);\n  const [newTodoText, setNewTodoText] = useState('');\n\n  const handleNewTodoChange = (e) => setNewTodoText(e.target.value);\n\n  const addTodo = (text) => {\n    const newTodo = {\n      id: Date.now().toString(),\n      text,\n      completed: false\n    };\n    setTodos((prev) => [...prev, newTodo]);\n  };\n\n  const toggleTodo = (id) => {\n    setTodos((prev) =>\n      prev.map((t) => (t.id === id ? { ...t, completed: !t.completed } : t))\n    );\n  };\n\n  const deleteTodo = (id) => {\n    setTodos((prev) => prev.filter((t) => t.id !== id));\n  };\n\n  return (\n    <div>\n      <label htmlFor=\"new-todo-input\">new todo</label>\n      <input\n        id=\"new-todo-input\"\n        type=\"text\"\n        value={newTodoText}\n        onChange={handleNewTodoChange}\n        placeholder=\"What needs to be done?\"\n      />\n      <button type=\"button\" onClick={() => { addTodo(newTodoText); setNewTodoText(''); }}>\n        Add Todo\n      </button>\n      {/* Map through todos here */}\n    </div>\n  );\n};\n",
        "expectedOutcome": "Each todo row shows text, checkbox, and delete control.",
        "successCriteria": [
          "`todos.map`",
          "`key={todo.id}`",
          "checkbox",
          "button for delete"
        ],
        "feedbackCorrect": "✅ List rendering looks good.",
        "feedbackPartial": "Use `map`, `key`, checkbox, and a delete button per row.",
        "feedbackWrong": "Map `todos` to JSX with stable keys and controls.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "todos.map",
            "key",
            "checkbox",
            "button"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step14",
        "type": "question",
        "phase": "Step 14 of 14",
        "title": "Wire handlers to todo items",
        "instruction": "Wire `onChange` on the checkbox to `toggleTodo(todo.id)` and `onClick` on Delete to `deleteTodo(todo.id)`. Ensure the Add button uses `addTodo(newTodoText)` and clears the field. Export `TodoList` as default.",
        "hint": "`onChange={() => toggleTodo(todo.id)}` and `onClick={() => deleteTodo(todo.id)}` — use `checked` + `onChange` for controlled checkboxes.",
        "analogousExample": "<input type=\"checkbox\" checked={todo.completed} onChange={() => toggleTodo(todo.id)} />",
        "seedCode": "import React, { useState } from 'react';\n\n/**\n * @typedef {Object} Todo\n * @property {string} id\n * @property {string} text\n * @property {boolean} completed\n */\n\nconst TodoList = () => {\n  const [todos, setTodos] = useState([\n    { id: '1', text: 'Learn React', completed: false },\n    { id: '2', text: 'Build a todo app', completed: false }\n  ]);\n  const [newTodoText, setNewTodoText] = useState('');\n\n  const handleNewTodoChange = (e) => setNewTodoText(e.target.value);\n\n  const addTodo = (text) => {\n    const newTodo = {\n      id: Date.now().toString(),\n      text,\n      completed: false\n    };\n    setTodos((prev) => [...prev, newTodo]);\n  };\n\n  const toggleTodo = (id) => {\n    setTodos((prev) =>\n      prev.map((t) => (t.id === id ? { ...t, completed: !t.completed } : t))\n    );\n  };\n\n  const deleteTodo = (id) => {\n    setTodos((prev) => prev.filter((t) => t.id !== id));\n  };\n\n  return (\n    <div>\n      <label htmlFor=\"new-todo-input\">new todo</label>\n      <input\n        id=\"new-todo-input\"\n        type=\"text\"\n        value={newTodoText}\n        onChange={handleNewTodoChange}\n        placeholder=\"Add a new todo\"\n      />\n      <button type=\"button\" onClick={() => { addTodo(newTodoText); setNewTodoText(''); }}>\n        Add Todo\n      </button>\n      <ul>\n        {todos.map((todo) => (\n          <li key={todo.id}>\n            <input type=\"checkbox\" checked={todo.completed} readOnly />\n            <span style={{ textDecoration: todo.completed ? 'line-through' : 'none' }}>\n              {todo.text}\n            </span>\n            <button type=\"button\">Delete</button>\n          </li>\n        ))}\n      </ul>\n    </div>\n  );\n};\n",
        "expectedOutcome": "Full CRUD loop works in the editor preview: add, toggle, delete.",
        "successCriteria": [
          "Checkbox and delete call the correct handlers with `todo.id`.",
          "Add flow calls `addTodo` with the input text.",
          "Default export present."
        ],
        "feedbackCorrect": "✅ Todo CRUD is wired end to end.",
        "feedbackPartial": "Connect `onChange`/`onClick` to `toggleTodo`/`deleteTodo` and add from input.",
        "feedbackWrong": "Ensure handlers receive `todo.id` and the Add button uses `addTodo`.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "onChange",
            "toggleTodo",
            "todo.id",
            "onClick",
            "deleteTodo",
            "export"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import React and useState",
        "id": "step1"
      },
      {
        "label": "Document todo shape (JSDoc)",
        "id": "step2"
      },
      {
        "label": "Create main TodoList component",
        "id": "step3"
      },
      {
        "label": "Initialize todos state",
        "id": "step4"
      },
      {
        "label": "New todo field (label + input)",
        "id": "step5"
      },
      {
        "label": "State for new todo text",
        "id": "step6"
      },
      {
        "label": "Handler for new-todo text changes",
        "id": "step7"
      },
      {
        "label": "Make the new-todo input controlled",
        "id": "step8"
      },
      {
        "label": "Create addTodo handler",
        "id": "step9"
      },
      {
        "label": "Create toggleTodo handler",
        "id": "step10"
      },
      {
        "label": "Create deleteTodo handler",
        "id": "step11"
      },
      {
        "label": "Add Todo button and clear input",
        "id": "step12"
      },
      {
        "label": "Render todos list",
        "id": "step13"
      },
      {
        "label": "Wire handlers to todo items",
        "id": "step14"
      }
    ]
  },
  "source": "content"
}
