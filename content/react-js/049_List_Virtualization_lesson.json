{
  "success": true,
  "config": {
    "lessonId": "ai-react-js-49",
    "track": "react-js",
    "lessonNum": 49,
    "title": "List Virtualization",
    "shortName": "P49",
    "intro": {
      "tag": "LESSON #49",
      "title": "List Virtualization",
      "body": "In this lesson, you will build a React component that displays a long list of items efficiently using list virtualization. You will learn how to render only the items visible on the screen, which improves performance by reducing the number of DOM elements. The core programming concepts involve using React hooks like useState and useEffect, along with JavaScript calculations for scroll position and item height. You'll work with JSX for the component structure and event handlers to manage scrolling. By the end, you'll understand how to implement a basic virtualized list to handle large datasets smoothly in a web application.",
      "usecase": "List virtualization is essential in modern web applications that handle large datasets. You'll see this pattern in social media feeds like Facebook or Twitter, where only visible posts are rendered while scrolling through thousands of items. E-commerce sites like Amazon use it for product listings, and data dashboards implement it for displaying extensive analytics tables. As a developer, you'll apply virtualization whenever rendering performance degrades with long lists, ensuring smooth scrolling and optimal memory usage."
    },
    "objectives": [
      "Initialize a virtualized list component with a fixed height and scrollable container",
      "Configure a custom hook to calculate visible items based on scroll position and viewport size",
      "Manipulate the DOM by rendering only the visible subset of a large dataset",
      "Apply dynamic styling to position items correctly within the virtualized container",
      "Integrate scroll event handlers to update the visible range in real-time",
      "Construct a reusable virtual list component that accepts data and item renderers as props",
      "Control performance by implementing memoization for item rendering to prevent unnecessary re-renders"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 11",
        "title": "Import React, useState, useRef, and useEffect",
        "instruction": "Import React, useState, useRef, and useEffect from the 'react' package.",
        "hint": "Use a single import statement to bring in all four items from 'react'.",
        "analogousExample": "// Importing multiple hooks from React\nimport React, { useState, useEffect } from 'react';",
        "seedCode": "// Import React, useState, useRef, and useEffect from 'react'",
        "expectedOutcome": "The necessary React hooks for list virtualization are imported.",
        "successCriteria": [
          "React, useState, useRef, and useEffect are imported from 'react'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close, but make sure to import all four items: React, useState, useRef, and useEffect from 'react'.",
        "feedbackWrong": "Check the import statement. You need to import React, useState, useRef, and useEffect from the 'react' package.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "React",
            "useState",
            "useRef",
            "useEffect",
            "'react'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 11",
        "title": "Create the main VirtualList component",
        "instruction": "Create a functional component named VirtualList that returns an empty div element.",
        "hint": "Define a function called VirtualList that returns JSX containing a <div> tag.",
        "analogousExample": "// Example of a simple component returning a div\nfunction MyComponent() {\n  return <div></div>;\n}",
        "seedCode": "// Import React, useState, useRef, and useEffect from 'react'\n\n// Create the VirtualList component here",
        "expectedOutcome": "A component named VirtualList is defined and returns an empty div.",
        "successCriteria": [
          "The code defines a function or arrow function named VirtualList that returns a <div> element."
        ],
        "feedbackCorrect": "Great! You've created the VirtualList component structure.",
        "feedbackPartial": "You're close. Make sure you've defined a component named VirtualList that returns a <div>.",
        "feedbackWrong": "Check that you've created a component called VirtualList that returns a div element.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "VirtualList",
            "return",
            "<div>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 11",
        "title": "Initialize state for scroll position",
        "instruction": "Inside your VirtualList component, use the useState hook to create a state variable called scrollTop initialized to 0.",
        "hint": "Call useState with an initial value of 0 and destructure the returned array into scrollTop and setScrollTop.",
        "analogousExample": "// Similar: creating a counter state\nconst [count, setCount] = useState(0);",
        "seedCode": "import React, { useState, useRef, useEffect } from 'react';\n\nconst VirtualList = () => {\n  // Initialize scrollTop state here\n\n  return null;\n};\n\nexport default VirtualList;",
        "expectedOutcome": "The component will have a scrollTop state variable that tracks the current scroll position, starting at 0.",
        "successCriteria": [
          "The VirtualList component contains a useState call that creates a scrollTop state variable initialized to 0."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using useState to create a state variable named scrollTop with an initial value of 0 inside the VirtualList component.",
        "feedbackWrong": "Inside the VirtualList component, use useState to create a state variable called scrollTop initialized to 0.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useState",
            "scrollTop",
            "0"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 11",
        "title": "Define configuration constants",
        "instruction": "Inside the VirtualList component, define three constants: itemHeight set to 50, totalItems set to 1000, and visibleItems set to 10.",
        "hint": "Use const declarations with descriptive names and assign numeric values.",
        "analogousExample": "// Example: configuration constants\nconst width = 800;\nconst height = 600;\nconst itemsPerPage = 20;",
        "seedCode": "import React, { useState, useRef, useEffect } from 'react';\n\nconst VirtualList = () => {\n  // Initialize scrollTop state here\n  \n  \n  \n  return null;\n};\n\nexport default VirtualList;",
        "expectedOutcome": "The component will have three constants defined that will be used for virtualization calculations.",
        "successCriteria": [
          "Constants itemHeight, totalItems, and visibleItems are defined with the specified numeric values."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You defined some constants, but make sure you have all three: itemHeight (50), totalItems (1000), and visibleItems (10).",
        "feedbackWrong": "Try defining three constants with the exact names and values specified in the instruction.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "itemHeight",
            "totalItems",
            "visibleItems",
            "50",
            "1000",
            "10"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 11",
        "title": "Create the scroll handler function",
        "instruction": "Inside the VirtualList component, create a function named handleScroll that updates the scrollTop state with the current scroll position of the container element.",
        "hint": "The function should get the current scrollTop value from containerRef.current and call setScrollTop with that value.",
        "analogousExample": "// Example: a scroll handler function\nfunction handleScroll() {\n  const scrollPosition = containerRef.current.scrollTop;\n  setScrollTop(scrollPosition);\n}",
        "seedCode": "import React, { useState, useRef, useEffect } from 'react';\n\nconst VirtualList = () => {\n  // Initialize scrollTop state here\n  \n  \n  \n  // Create the scroll handler function here\n  \n  \n  \n  return null;\n};\n\nexport default VirtualList;",
        "expectedOutcome": "A handleScroll function is defined inside the component that updates the scrollTop state when called.",
        "successCriteria": [
          "The handleScroll function is defined, accesses containerRef.current.scrollTop, and calls setScrollTop with that value."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created a function, but make sure it updates the scrollTop state with containerRef.current.scrollTop.",
        "feedbackWrong": "The handleScroll function is missing or doesn't properly update the scrollTop state.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "handleScroll",
            "containerRef.current.scrollTop",
            "setScrollTop"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 11",
        "title": "Calculate visible range of items",
        "instruction": "Inside your VirtualList component, calculate the startIndex and endIndex based on the scrollTop state. Use the formula: startIndex = Math.floor(scrollTop / itemHeight) and endIndex = startIndex + visibleItems.",
        "hint": "You'll need to use the scrollTop state variable and the itemHeight and visibleItems constants you defined earlier.",
        "analogousExample": "// Example: calculating a range\nconst start = Math.floor(currentPosition / itemSize);\nconst end = start + itemsToShow;",
        "seedCode": "import React, { useState, useRef, useEffect } from 'react';\n\nconst VirtualList = () => {\n  // Initialize scrollTop state here\n  const [scrollTop, setScrollTop] = useState(0);\n  \n  // Create a ref for the container element\n  const containerRef = useRef(null);\n  \n  // Define configuration constants\n  const itemHeight = 50;\n  const totalItems = 1000;\n  const visibleItems = 10;\n  \n  // Create the scroll handler function here\n  const handleScroll = () => {\n    if (containerRef.current) {\n      setScrollTop(containerRef.current.scrollTop);\n    }\n  };\n  \n  // Calculate visible range of items here\n  \n  \n  return null;\n};\n\nexport default VirtualList;",
        "expectedOutcome": "The component will have startIndex and endIndex variables calculated from the current scroll position.",
        "successCriteria": [
          "startIndex is calculated as Math.floor(scrollTop / itemHeight) and endIndex is calculated as startIndex + visibleItems."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're using the correct variables: scrollTop, itemHeight, and visibleItems.",
        "feedbackWrong": "The calculation isn't quite right. Remember: startIndex = Math.floor(scrollTop / itemHeight) and endIndex = startIndex + visibleItems.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "startIndex",
            "Math.floor",
            "scrollTop",
            "/",
            "itemHeight",
            "endIndex",
            "startIndex",
            "+",
            "visibleItems"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 11",
        "title": "Generate visible items array",
        "instruction": "Inside your component, create a visibleItemsArray using Array.from() with length (endIndex - startIndex) and map each element to create an object with id and content properties.",
        "hint": "Use Array.from({ length: endIndex - startIndex }) to create an array of the correct size, then .map() to generate objects with id and content.",
        "analogousExample": "// Example: creating an array of items\nconst visibleItemsArray = Array.from({ length: 5 }, (_, i) => ({\n  id: i,\n  content: `Item ${i}`\n}));",
        "seedCode": "import React, { useState, useRef, useEffect } from 'react';\n\nconst VirtualList = () => {\n  // Initialize scrollTop state here\n  const [scrollTop, setScrollTop] = useState(0);\n  \n  // Create a ref for the container element\n  const containerRef = useRef(null);\n  \n  // Define configuration constants\n  const itemHeight = 50;\n  const totalItems = 1000;\n  const visibleItems = 10;\n  \n  // Create the scroll handler function here\n  const handleScroll = () => {\n    if (containerRef.current) {\n      setScrollTop(containerRef.current.scrollTop);\n    }\n  };\n  \n  // Calculate visible range of items here\n  \n  // Generate visible items array here\n  \n  return null;\n};\n\nexport default VirtualList;",
        "expectedOutcome": "You will have a visibleItemsArray variable containing objects with id and content properties for the visible range.",
        "successCriteria": [
          "visibleItemsArray is created using Array.from() with length (endIndex - startIndex) and maps to objects with id and content properties."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure to use Array.from() with the correct length and map to objects with both id and content.",
        "feedbackWrong": "The visible items array wasn't created correctly. Use Array.from() with length (endIndex - startIndex) and map to create objects with id and content.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "visibleItemsArray",
            "Array.from",
            "length",
            "map",
            "id",
            "content"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 11",
        "title": "Add the main container structure",
        "instruction": "Return a div element from your component with ref={containerRef}, onScroll={handleScroll}, and inline styles for height, overflow, and border.",
        "hint": "The container div should be scrollable and have a fixed height. Use the ref and handler you created earlier.",
        "analogousExample": "// Similar: a scrollable container\nfunction ScrollableBox() {\n  const boxRef = useRef(null);\n  const handleScroll = () => {};\n  return (\n    <div\n      ref={boxRef}\n      onScroll={handleScroll}\n      style={{ height: '400px', overflow: 'auto', border: '1px solid gray' }}\n    >\n      {/* content */}\n    </div>\n  );\n}",
        "seedCode": "import React, { useState, useRef, useEffect } from 'react';\n\nconst VirtualList = () => {\n  // Initialize scrollTop state here\n  const [scrollTop, setScrollTop] = useState(0);\n  \n  // Create a ref for the container element\n  const containerRef = useRef(null);\n  \n  // Define configuration constants\n  const itemHeight = 50;\n  const totalItems = 1000;\n  const visibleItems = 10;\n  \n  // Create the scroll handler function here\n  const handleScroll = () => {\n    if (containerRef.current) {\n      setScrollTop(containerRef.current.scrollTop);\n    }\n  };\n  \n  // Calculate visible range of items here\n  \n  // Generate visible items array here\n  \n  return (\n    <div\n      ref={containerRef}\n      onScroll={handleScroll}\n      style={{ height: '500px', overflow: 'auto', border: '1px solid #ccc' }}\n    >\n      {/* visible items will go here */}\n    </div>\n  );\n};\n\nexport default VirtualList;",
        "expectedOutcome": "The component returns a scrollable div container with the ref and scroll handler attached, ready to hold virtualized items.",
        "successCriteria": [
          "The component returns a div with ref={containerRef}, onScroll={handleScroll}, and inline styles for height, overflow, and border."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure the div has ref={containerRef}, onScroll={handleScroll}, and the required inline styles.",
        "feedbackWrong": "The container structure is missing or incorrect. Check that you're returning a div with the ref, handler, and styles.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "return",
            "div",
            "ref={containerRef}",
            "onScroll={handleScroll}",
            "style={{",
            "height:",
            "overflow:",
            "border:"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 11",
        "title": "Add inner spacer element",
        "instruction": "Inside the container div, add a div element with inline style setting its height to totalItems * itemHeight to create proper scroll height.",
        "hint": "Calculate the total height by multiplying totalItems by itemHeight, then set this as the height style of the inner div.",
        "analogousExample": "// Adding a spacer element with dynamic height\n<div style={{ height: totalItems * itemHeight }}>\n  {/* content goes here */}\n</div>",
        "seedCode": "import React, { useState, useRef, useEffect } from 'react';\n\nconst VirtualList = () => {\n  // Initialize scrollTop state here\n  const [scrollTop, setScrollTop] = useState(0);\n  \n  // Create a ref for the container element\n  const containerRef = useRef(null);\n  \n  // Define configuration constants\n  const itemHeight = 50;\n  const totalItems = 1000;\n  const visibleItems = 10;\n  \n  // Create the scroll handler function here\n  const handleScroll = () => {\n    if (containerRef.current) {\n      setScrollTop(containerRef.current.scrollTop);\n    }\n  };\n  \n  // Calculate visible range of items here\n  \n  // Generate visible items array here\n  \n  return (\n    <div\n      ref={containerRef}\n      onScroll={handleScroll}\n      style={{ height: '500px', overflow: 'auto', border: '1px solid #ccc' }}\n    >\n      {/* Add inner spacer div here */}\n    </div>\n  );\n};\n\nexport default VirtualList;",
        "expectedOutcome": "The container will have an inner div that provides the correct total scrollable height based on the number of items and their height.",
        "successCriteria": [
          "A div element is added inside the container with style={{ height: totalItems * itemHeight }}."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You added a div, but check if its height is set correctly using totalItems * itemHeight.",
        "feedbackWrong": "The spacer element is missing or incorrect. Add a div with style={{ height: totalItems * itemHeight }} inside the container.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<div",
            "style={{",
            "height:",
            "totalItems",
            "*",
            "itemHeight",
            "}}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 11",
        "title": "Render the visible items",
        "instruction": "Inside the component, map through the visibleItemsArray to render each visible item as a div positioned absolutely with top based on its index.",
        "hint": "Use the map method on visibleItemsArray to return a div for each item. Set the top style to itemHeight * index and include the item content.",
        "analogousExample": "// Example: mapping an array to render items\nconst items = ['A', 'B', 'C'];\nconst rendered = items.map((item, index) => (\n  <div key={index} style={{ top: index * 30 }}>\n    {item}\n  </div>\n));",
        "seedCode": "import React, { useState, useRef, useEffect } from 'react';\n\nconst VirtualList = () => {\n  // Initialize scrollTop state here\n  const [scrollTop, setScrollTop] = useState(0);\n  \n  // Create a ref for the container element\n  const containerRef = useRef(null);\n  \n  // Define configuration constants\n  const itemHeight = 50;\n  const totalItems = 1000;\n  const visibleItems = 10;\n  \n  // Create the scroll handler function here\n  const handleScroll = () => {\n    if (containerRef.current) {\n      setScrollTop(containerRef.current.scrollTop);\n    }\n  };\n  \n  // Calculate visible range of items here\n  const startIndex = Math.floor(scrollTop / itemHeight);\n  const endIndex = Math.min(startIndex + visibleItems, totalItems - 1);\n  \n  // Generate visible items array here\n  const visibleItemsArray = [];\n  for (let i = startIndex; i <= endIndex; i++) {\n    visibleItemsArray.push({ id: i, content: `Item ${i}` });\n  }\n  \n  return (\n    <div\n      ref={containerRef}\n      onScroll={handleScroll}\n      style={{ height: '500px', overflow: 'auto', border: '1px solid #ccc' }}\n    >\n      {/* Add inner spacer div here */}\n      <div style={{ height: `${totalItems * itemHeight}px`, position: 'relative' }}>\n        {/* Render visible items here */}\n      </div>\n    </div>\n  );\n};\n\nexport default VirtualList;",
        "expectedOutcome": "The visible items will be rendered inside the spacer div, each positioned correctly based on its index.",
        "successCriteria": [
          "visibleItemsArray is mapped to render div elements with absolute positioning and correct top values."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but check that each item div has a key, uses absolute positioning, and calculates top correctly.",
        "feedbackWrong": "The visible items are not rendered correctly. Ensure you map through visibleItemsArray and return a div for each item with appropriate styles.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "visibleItemsArray",
            "map",
            "div",
            "style",
            "top",
            "itemHeight",
            "index"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step11",
        "type": "question",
        "phase": "Step 11 of 11",
        "title": "Add useEffect to log virtualization metrics",
        "instruction": "Add a useEffect hook that logs the startIndex, endIndex, and visibleItemsArray.length to the console whenever scrollTop changes.",
        "hint": "Place the useEffect inside your component, after the visibleItemsArray calculation. The dependency array should include scrollTop.",
        "analogousExample": "// Example: logging state changes\nuseEffect(() => {\n  console.log('Current count:', count);\n}, [count]);",
        "seedCode": "import React, { useState, useRef, useEffect } from 'react';\n\nconst VirtualList = () => {\n  // Initialize scrollTop state here\n  const [scrollTop, setScrollTop] = useState(0);\n  \n  // Create a ref for the container element\n  const containerRef = useRef(null);\n  \n  // Define configuration constants\n  const itemHeight = 50;\n  const totalItems = 1000;\n  const visibleItems = 10;\n  \n  // Create the scroll handler function here\n  const handleScroll = () => {\n    if (containerRef.current) {\n      setScrollTop(containerRef.current.scrollTop);\n    }\n  };\n  \n  // Calculate visible range of items here\n  const startIndex = Math.floor(scrollTop / itemHeight);\n  const endIndex = Math.min(startIndex + visibleItems, totalItems - 1);\n  \n  // Generate visible items array here\n  const visibleItemsArray = [];\n  for (let i = startIndex; i <= endIndex; i++) {\n    visibleItemsArray.push({ id: i, content: `Item ${i}` });\n  }\n  \n  // Add useEffect here\n  \n  return (\n    <div\n      ref={containerRef}\n      onScroll={handleScroll}\n      style={{ height: '500px', overflow: 'auto', border: '1px solid #ccc' }}\n    >\n      {/* Add inner spacer div here */}\n      <div style={{ height: `${totalItems * itemHeight}px`, position: 'relative' }}>\n        {/* Render visible items here */}\n      </div>\n    </div>\n  );\n};\n\nexport default VirtualList;",
        "expectedOutcome": "The useEffect will log the current start index, end index, and number of visible items to the console each time the user scrolls, demonstrating how virtualization reduces the number of rendered items.",
        "successCriteria": [
          "A useEffect hook is added that logs startIndex, endIndex, and visibleItemsArray.length when scrollTop changes."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added a useEffect, but it might not be logging all three metrics or might have incorrect dependencies. Make sure it logs startIndex, endIndex, and visibleItemsArray.length, and includes scrollTop in the dependency array.",
        "feedbackWrong": "The useEffect hook is missing or doesn't log the required metrics. Add a useEffect that logs startIndex, endIndex, and visibleItemsArray.length when scrollTop changes.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useEffect",
            "startIndex",
            "endIndex",
            "visibleItemsArray.length",
            "scrollTop"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import React, useState, useRef, and useEffect",
        "id": "step1"
      },
      {
        "label": "Create the main VirtualList component",
        "id": "step2"
      },
      {
        "label": "Initialize state for scroll position",
        "id": "step3"
      },
      {
        "label": "Define configuration constants",
        "id": "step4"
      },
      {
        "label": "Create the scroll handler function",
        "id": "step5"
      },
      {
        "label": "Calculate visible range of items",
        "id": "step6"
      },
      {
        "label": "Generate visible items array",
        "id": "step7"
      },
      {
        "label": "Add the main container structure",
        "id": "step8"
      },
      {
        "label": "Add inner spacer element",
        "id": "step9"
      },
      {
        "label": "Render the visible items",
        "id": "step10"
      },
      {
        "label": "Add useEffect to log virtualization metrics",
        "id": "step11"
      }
    ]
  },
  "source": "content"
}