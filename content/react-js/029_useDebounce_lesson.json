{
  "success": true,
  "config": {
    "lessonId": "ai-react-js-29",
    "track": "react-js",
    "lessonNum": 29,
    "title": "useDebounce",
    "shortName": "P29",
    "intro": {
      "tag": "LESSON #29",
      "title": "useDebounce",
      "body": "In this lesson, you will build a custom React hook called `useDebounce` that delays the update of a value until a specified time has passed without any new changes. You'll learn the core concept of debouncing, which helps optimize performance by preventing rapid, repeated function calls, like those from user input in a search box. The main technologies involved are React hooks, specifically `useState` and `useEffect`, along with JavaScript's `setTimeout` and `clearTimeout` functions. By the end, you'll understand how to implement and apply debouncing in your React applications to create smoother user experiences.",
      "usecase": "The useDebounce pattern is essential in modern web applications to optimize performance and user experience. You'll see it implemented in search bars like those on e-commerce sites (Amazon, eBay) where typing triggers product suggestions without overwhelming the server with every keystroke. It's also crucial in dashboard filters where adjusting date ranges or metrics should update visualizations smoothly, and in form validation that checks username availability as users type. This technique prevents excessive API calls and ensures responsive interfaces in data-heavy applications."
    },
    "objectives": [
      "Implement a custom useDebounce hook that delays execution of a function",
      "Configure the debounce delay parameter to control timing behavior",
      "Apply the useDebounce hook to handle user input in a search component",
      "Initialize state variables to manage debounced values within React components",
      "Integrate the debounced function with event handlers for real-time input",
      "Render UI elements that respond to debounced state updates",
      "Manipulate the debounce behavior by adjusting delay values dynamically"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 11",
        "title": "Import React and useState",
        "instruction": "Import React and useState from 'react'.",
        "hint": "Use the import statement to bring in React and the useState hook.",
        "analogousExample": "// Importing React and a hook\nimport React, { useState } from 'react';",
        "seedCode": "// Import React and useState here",
        "expectedOutcome": "React and useState are imported and ready to use.",
        "successCriteria": [
          "React and useState are imported from 'react'."
        ],
        "feedbackCorrect": "Great! You've successfully imported React and useState.",
        "feedbackPartial": "You're on the right track, but make sure to import both React and useState from 'react'.",
        "feedbackWrong": "Check the import statement. You need to import both React and useState from 'react'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "React",
            "useState",
            "'react'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 11",
        "title": "Create the SearchInput component",
        "instruction": "Create a functional component named SearchInput that returns an empty fragment.",
        "hint": "Define a component using either function declaration or arrow function syntax. Return React.Fragment (<> </>) or a div placeholder.",
        "analogousExample": "// Example of a simple component returning fragment\nfunction ExampleComponent() {\n  return <> </>;\n}",
        "seedCode": "import React, { useState } from 'react';\n\n// Create the SearchInput component here",
        "expectedOutcome": "A SearchInput component is defined and returns an empty fragment.",
        "successCriteria": [
          "SearchInput component is defined and returns a fragment."
        ],
        "feedbackCorrect": "Great! You've created the SearchInput component structure.",
        "feedbackPartial": "You're close. Make sure the component is named SearchInput and returns a fragment (<> </>).",
        "feedbackWrong": "The component isn't defined correctly. Check the name and return statement.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "SearchInput",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 11",
        "title": "Initialize search query state",
        "instruction": "Inside your SearchInput component, use the useState hook to create a state variable called 'searchQuery' initialized to an empty string.",
        "hint": "Call useState with an empty string as the initial value, and destructure the returned array into two variables.",
        "analogousExample": "// Example of initializing state with an empty string\nconst [inputValue, setInputValue] = useState('');",
        "seedCode": "import React, { useState } from 'react';\n\n// Create the SearchInput component here\nconst SearchInput = () => {\n  // Initialize searchQuery state here\n  \n  return null;\n};",
        "expectedOutcome": "The component will have a searchQuery state variable ready to track the user's search input.",
        "successCriteria": [
          "The SearchInput component contains a useState call that creates a 'searchQuery' state variable initialized to an empty string."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're using useState with an empty string initial value and naming the variable 'searchQuery'.",
        "feedbackWrong": "The searchQuery state isn't properly initialized. Remember to use useState('') and name the variable 'searchQuery'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "searchQuery",
            "useState",
            "''"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 11",
        "title": "Initialize debounced query state",
        "instruction": "Inside your SearchInput component, use useState to create a state variable called debouncedQuery initialized to an empty string.",
        "hint": "Add another useState call similar to your searchQuery state, but name the variable debouncedQuery.",
        "analogousExample": "// Example: creating a second state variable\nconst [count, setCount] = useState(0);\nconst [name, setName] = useState('');",
        "seedCode": "import React, { useState } from 'react';\n\n// Create the SearchInput component here\nconst SearchInput = () => {\n  // Initialize searchQuery state here\n  const [searchQuery, setSearchQuery] = useState('');\n  \n  // Initialize debouncedQuery state here\n  \n  return null;\n};",
        "expectedOutcome": "Your component will have two state variables: searchQuery for immediate input and debouncedQuery for the delayed version.",
        "successCriteria": [
          "A debouncedQuery state variable is declared using useState with an initial value of empty string."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're using useState to create a variable called debouncedQuery initialized to an empty string.",
        "feedbackWrong": "Let's focus on this step: use useState to create a state variable named debouncedQuery with an initial value of empty string.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "debouncedQuery",
            "useState",
            "''"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 11",
        "title": "Create handleInputChange handler",
        "instruction": "Inside your SearchInput component, define a function named handleInputChange that takes an event parameter and updates the searchQuery state with the input's current value.",
        "hint": "The function should extract the value from event.target.value and call setSearchQuery with that value.",
        "analogousExample": "// Example: event handler function\nfunction handleChange(event) {\n  setValue(event.target.value);\n}",
        "seedCode": "import React, { useState } from 'react';\n\n// Create the SearchInput component here\nconst SearchInput = () => {\n  // Initialize searchQuery state here\n  const [searchQuery, setSearchQuery] = useState('');\n  \n  // Initialize debouncedQuery state here\n  \n  // Create handleInputChange handler here\n  \n  return null;\n};",
        "expectedOutcome": "A handleInputChange function is defined that updates searchQuery when called with an input event.",
        "successCriteria": [
          "The handleInputChange function is defined, takes an event parameter, and calls setSearchQuery with event.target.value."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure your function extracts the value from event.target.value and calls setSearchQuery with it.",
        "feedbackWrong": "The handleInputChange function isn't defined correctly. It should take an event parameter and update searchQuery with the input value.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "handleInputChange",
            "event",
            "setSearchQuery",
            "target.value"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 11",
        "title": "Create the useDebounce custom hook",
        "instruction": "Create a custom hook named useDebounce that takes a value and a delay as parameters, and returns the debounced value.",
        "hint": "Define a function called useDebounce that accepts two arguments: value and delay. Inside, you'll need to use useState and useEffect to implement the debouncing logic, but for now just return the value directly.",
        "analogousExample": "// Similar: a custom hook that returns a value\nfunction useCustomHook(value) {\n  return value;\n}",
        "seedCode": "import React, { useState } from 'react';\n\n// Create the SearchInput component here\nconst SearchInput = () => {\n  // Initialize searchQuery state here\n  const [searchQuery, setSearchQuery] = useState('');\n  \n  // Initialize debouncedQuery state here\n  \n  // Create handleInputChange handler here\n  \n  return null;\n};\n\n// Create the useDebounce custom hook here\n",
        "expectedOutcome": "A useDebounce function is defined that accepts value and delay parameters and returns a value.",
        "successCriteria": [
          "A function named useDebounce is created with parameters (value, delay) and returns a value."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you've defined a function named useDebounce that takes value and delay parameters and returns something.",
        "feedbackWrong": "The useDebounce hook wasn't created correctly. Create a function named useDebounce that accepts value and delay parameters and returns a value.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useDebounce",
            "value",
            "delay",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 11",
        "title": "Implement debounce logic in useDebounce",
        "instruction": "Inside the useDebounce function, implement the debounce logic using setTimeout to update the debounced value after the specified delay.",
        "hint": "Set up a setTimeout that calls setDebouncedValue after the delay, and clear the previous timeout before setting a new one.",
        "analogousExample": "// Similar: debounce logic in a function\nfunction useDebounce(value, delay) {\n  const [debouncedValue, setDebouncedValue] = useState(value);\n  \n  useEffect(() => {\n    const timer = setTimeout(() => {\n      setDebouncedValue(value);\n    }, delay);\n    \n    return () => {\n      clearTimeout(timer);\n    };\n  }, [value, delay]);\n  \n  return debouncedValue;\n}",
        "seedCode": "import React, { useState } from 'react';\n\n// Create the SearchInput component here\nconst SearchInput = () => {\n  // Initialize searchQuery state here\n  const [searchQuery, setSearchQuery] = useState('');\n  \n  // Initialize debouncedQuery state here\n  \n  // Create handleInputChange handler here\n  \n  return null;\n};\n\n// Create the useDebounce custom hook here\nconst useDebounce = (value, delay) => {\n  const [debouncedValue, setDebouncedValue] = useState(value);\n  \n  // Implement debounce logic here\n  \n  return debouncedValue;\n};",
        "expectedOutcome": "The useDebounce hook will now properly debounce value updates using setTimeout and clearTimeout.",
        "successCriteria": [
          "The useDebounce function contains setTimeout to update debouncedValue after the delay, and clears the previous timeout before setting a new one."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure to both set a new timeout and clear the previous one.",
        "feedbackWrong": "The debounce logic isn't complete. Remember to use setTimeout to update the value after the delay and clearTimeout to cancel previous timeouts.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "setTimeout",
            "clearTimeout",
            "setDebouncedValue"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 11",
        "title": "Use the useDebounce hook in SearchInput",
        "instruction": "Inside the SearchInput component, call the useDebounce hook with the searchQuery state and a 500ms delay,",
        "hint": "Call useDebounce(searchQuery, 500) and store the result in a variable. This will give you the debounced version of the search query.",
        "analogousExample": "// Similar: calling a custom hook\nconst debouncedValue = useCustomHook(someState, 1000);",
        "seedCode": "import React, { useState } from 'react';\n\n// Create the SearchInput component here\nconst SearchInput = () => {\n  // Initialize searchQuery state here\n  const [searchQuery, setSearchQuery] = useState('');\n  \n  // Initialize debouncedQuery state here\n  \n  // Create handleInputChange handler here\n  \n  // Call useDebounce here\n  \n  return null;\n};\n\n// Create the useDebounce custom hook here\nconst useDebounce = (value, delay) => {\n  const [debouncedValue, setDebouncedValue] = useState(value);\n  \n  // Implement debounce logic here\n  \n  return debouncedValue;\n};",
        "expectedOutcome": "The SearchInput component will have a debouncedQuery variable that holds the debounced version of the search query.",
        "successCriteria": [
          "The useDebounce hook is called with searchQuery and 500, and the result is stored in a variable named debouncedQuery."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're calling useDebounce with searchQuery and 500, and storing the result in a variable named debouncedQuery.",
        "feedbackWrong": "Try calling the useDebounce hook with the searchQuery state and a 500ms delay, then assign the returned value to a variable named debouncedQuery.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useDebounce",
            "searchQuery",
            "500",
            "debouncedQuery"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 11",
        "title": "assign the returned value to a variable named",
        "instruction": "assign the returned value to a variable named debouncedQuery.",
        "hint": "Call useDebounce(searchQuery, 500) and store the result in a variable. This will give you the debounced version of the search query.",
        "analogousExample": "// Similar: calling a custom hook\nconst debouncedValue = useCustomHook(someState, 1000);",
        "seedCode": "import React, { useState } from 'react';\n\n// Create the SearchInput component here\nconst SearchInput = () => {\n  // Initialize searchQuery state here\n  const [searchQuery, setSearchQuery] = useState('');\n  \n  // Initialize debouncedQuery state here\n  \n  // Create handleInputChange handler here\n  \n  // Call useDebounce here\n  \n  return null;\n};\n\n// Create the useDebounce custom hook here\nconst useDebounce = (value, delay) => {\n  const [debouncedValue, setDebouncedValue] = useState(value);\n  \n  // Implement debounce logic here\n  \n  return debouncedValue;\n};",
        "expectedOutcome": "",
        "successCriteria": [
          "Complete the step as instructed."
        ],
        "feedbackCorrect": "Great! Step completed.",
        "feedbackPartial": "Review the instruction and try again.",
        "feedbackWrong": "Check the instruction and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useDebounce",
            "searchQuery",
            "500",
            "debouncedQuery"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 11",
        "title": "Add search input field",
        "instruction": "In the SearchInput component's return statement, add an input element with type='text', placeholder='Search...', value bound to searchQuery, and onChange wired to handleInputChange.",
        "hint": "The input element should be the only thing returned by the component. Use JSX syntax: <input type=\"text\" placeholder=\"Search...\" value={searchQuery} onChange={handleInputChange} />",
        "analogousExample": "// Similar: input element with state binding\nfunction NameInput() {\n  const [name, setName] = useState('');\n  const handleChange = (e) => setName(e.target.value);\n  return <input type=\"text\" value={name} onChange={handleChange} />;\n}",
        "seedCode": "import React, { useState } from 'react';\n\n// Create the SearchInput component here\nconst SearchInput = () => {\n  // Initialize searchQuery state here\n  const [searchQuery, setSearchQuery] = useState('');\n  \n  // Initialize debouncedQuery state here\n  \n  // Create handleInputChange handler here\n  \n  // Call useDebounce here\n  \n  return (\n    // Add input element here\n  );\n};\n\n// Create the useDebounce custom hook here\nconst useDebounce = (value, delay) => {\n  const [debouncedValue, setDebouncedValue] = useState(value);\n  \n  // Implement debounce logic here\n  \n  return debouncedValue;\n};",
        "expectedOutcome": "The SearchInput component now returns an input field where users can type their search query, with the input value controlled by the searchQuery state.",
        "successCriteria": [
          "The SearchInput component returns an input element with type='text', placeholder='Search...', value={searchQuery}, and onChange={handleInputChange}."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure your input element has all required attributes: type='text', placeholder='Search...', value={searchQuery}, and onChange={handleInputChange}.",
        "feedbackWrong": "The input element is missing or incomplete. Check that you've added it to the return statement with all required attributes.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<input",
            "type=\"text\"",
            "placeholder=\"Search...\"",
            "value={searchQuery}",
            "onChange={handleInputChange}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step11",
        "type": "question",
        "phase": "Step 11 of 11",
        "title": "Display debounced query result",
        "instruction": "Add a paragraph element below the input to display the debouncedQuery value.",
        "hint": "Use JSX to render a paragraph that shows the debouncedQuery state value.",
        "analogousExample": "// Displaying a state value in JSX\n<p>Current value: {someState}</p>",
        "seedCode": "import React, { useState, useEffect } from 'react';\n\n// Create the SearchInput component here\nconst SearchInput = () => {\n  // Initialize searchQuery state here\n  const [searchQuery, setSearchQuery] = useState('');\n  \n  // Initialize debouncedQuery state here\n  const [debouncedQuery, setDebouncedQuery] = useState('');\n  \n  // Create handleInputChange handler here\n  const handleInputChange = (e) => {\n    setSearchQuery(e.target.value);\n  };\n  \n  // Call useDebounce here\n  useDebounce(searchQuery, 500);\n  \n  return (\n    <div>\n      <input\n        type=\"text\"\n        value={searchQuery}\n        onChange={handleInputChange}\n        placeholder=\"Type to search...\"\n      />\n      {/* Add paragraph here to display debouncedQuery */}\n    </div>\n  );\n};\n\n// Create the useDebounce custom hook here\nconst useDebounce = (value, delay) => {\n  const [debouncedValue, setDebouncedValue] = useState(value);\n  \n  // Implement debounce logic here\n  useEffect(() => {\n    const timer = setTimeout(() => {\n      setDebouncedValue(value);\n    }, delay);\n    \n    return () => {\n      clearTimeout(timer);\n    };\n  }, [value, delay]);\n  \n  return debouncedValue;\n};",
        "expectedOutcome": "The UI will show the debounced query value updating with a delay after typing stops.",
        "successCriteria": [
          "A paragraph element displays the debouncedQuery value below the input field."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you added a paragraph element that shows the debouncedQuery value.",
        "feedbackWrong": "Check the instruction: you need to add a paragraph element that displays the debouncedQuery value.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<p>",
            "{debouncedQuery}",
            "</p>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import React and useState",
        "id": "step1"
      },
      {
        "label": "Create the SearchInput component",
        "id": "step2"
      },
      {
        "label": "Initialize search query state",
        "id": "step3"
      },
      {
        "label": "Initialize debounced query state",
        "id": "step4"
      },
      {
        "label": "Create handleInputChange handler",
        "id": "step5"
      },
      {
        "label": "Create the useDebounce custom hook",
        "id": "step6"
      },
      {
        "label": "Implement debounce logic in useDebounce",
        "id": "step7"
      },
      {
        "label": "Use the useDebounce hook in SearchInput",
        "id": "step8"
      },
      {
        "label": "assign the returned value to a variable named",
        "id": "step9"
      },
      {
        "label": "Add search input field",
        "id": "step10"
      },
      {
        "label": "Display debounced query result",
        "id": "step11"
      }
    ]
  },
  "source": "content"
}