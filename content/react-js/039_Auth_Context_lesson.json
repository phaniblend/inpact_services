{
  "success": true,
  "config": {
    "lessonId": "ai-react-js-39",
    "track": "react-js",
    "lessonNum": 39,
    "title": "Auth Context",
    "shortName": "P39",
    "intro": {
      "tag": "LESSON #39",
      "title": "Auth Context",
      "body": "In this lesson, you will build a React authentication context to manage user login state across your app. You'll learn how to use React's Context API to create a centralized provider that shares authentication data, like a user object or login status, with any component. The main technologies involved are React's createContext, useContext hook, and useState for state management. By the end, you'll have a working auth context that can be used to conditionally render content based on whether a user is logged in.",
      "usecase": "Auth Context is essential for managing user authentication state across entire React applications, similar to how platforms like Netflix or Spotify maintain your login status across all pages. In e-commerce sites like Amazon, it controls access to checkout flows and personal dashboards while keeping user data secure. Developers implement this pattern in admin panels, banking apps, and social media platforms to conditionally render protected routes, user menus, and permission-based UI elements. This centralized approach prevents prop drilling and ensures consistent authentication logic throughout complex applications."
    },
    "objectives": [
      "Construct a React context to manage authentication state across components",
      "Initialize authentication state with user data and loading status",
      "Implement context provider functions for login and logout operations",
      "Configure components to consume authentication context using the useContext hook",
      "Bind authentication state to control UI rendering of protected content",
      "Update component behavior based on changes in authentication status",
      "Integrate authentication context with existing React component structures"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 12",
        "title": "Import React, createContext, useContext, and useState",
        "instruction": "Import React, createContext, useContext, and useState from 'react'",
        "hint": "Use the import statement to bring in the necessary React APIs for creating and using context.",
        "analogousExample": "// Example: importing multiple items from a module\nimport React, { useState, useEffect } from 'react';",
        "seedCode": "// Import React, createContext, useContext, and useState from 'react'",
        "expectedOutcome": "The necessary React APIs are imported and available for use in the Auth Context implementation.",
        "successCriteria": [
          "React, createContext, useContext, and useState are imported from 'react'"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close, but make sure to import all four items: React, createContext, useContext, and useState from 'react'.",
        "feedbackWrong": "Please import React, createContext, useContext, and useState from 'react' to proceed.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "React",
            "createContext",
            "useContext",
            "useState",
            "'react'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 12",
        "title": "Create the AuthContext",
        "instruction": "Create an AuthContext using React.createContext() and export it as a named export.",
        "hint": "Use the createContext function you imported to create a new context object. Name it AuthContext and export it.",
        "analogousExample": "// Creating and exporting a context\nconst ThemeContext = React.createContext();\nexport { ThemeContext };",
        "seedCode": "// Import React, createContext, useContext, and useState from 'react'\n\n// Create the AuthContext here\n",
        "expectedOutcome": "You should have an AuthContext object created and exported, ready to be used by other components.",
        "successCriteria": [
          "AuthContext is created using React.createContext() and exported as a named export."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using React.createContext() and exporting the AuthContext as a named export.",
        "feedbackWrong": "create an AuthContext using React.createContext() and export it.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "AuthContext",
            "createContext",
            "export"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 12",
        "title": "Create the AuthProvider component",
        "instruction": "Create a functional component named AuthProvider that takes children as props and returns them wrapped in AuthContext.Provider.",
        "hint": "Define a component that accepts children and returns <AuthContext.Provider value={null}>{children}</AuthContext.Provider>.",
        "analogousExample": "// Similar: a provider component\nfunction ThemeProvider({ children }) {\n  return (\n    <ThemeContext.Provider value=\"light\">\n      {children}\n    </ThemeContext.Provider>\n  );\n}",
        "seedCode": "// Import React, createContext, useContext, and useState from 'react'\nimport React, { createContext, useContext, useState } from 'react';\n\n// Create the AuthContext here\nconst AuthContext = createContext();\n\n// Create the AuthProvider component\n",
        "expectedOutcome": "An AuthProvider component is defined that returns AuthContext.Provider wrapping children.",
        "successCriteria": [
          "AuthProvider is a functional component that accepts children and returns AuthContext.Provider with a placeholder value prop."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure AuthProvider is a function that takes children and returns AuthContext.Provider with a value prop (even if null for now).",
        "feedbackWrong": "The component isn't defined correctly. Create a function named AuthProvider that returns AuthContext.Provider wrapping children.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "AuthProvider",
            "children",
            "AuthContext.Provider",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 12",
        "title": "Initialize authentication state in AuthProvider",
        "instruction": "Inside the AuthProvider component, add a useState hook to manage an isAuthenticated state variable, initializing it to false.",
        "hint": "Use the useState hook you imported earlier. Declare it inside the component body.",
        "analogousExample": "// Similar: useState for a boolean state\nfunction ExampleComponent() {\n  const [isVisible, setIsVisible] = useState(false);\n  return null;\n}",
        "seedCode": "// Import React, createContext, useContext, and useState from 'react'\nimport React, { createContext, useContext, useState } from 'react';\n\n// Create the AuthContext here\nconst AuthContext = createContext();\n\n// Create the AuthProvider component\nconst AuthProvider = ({ children }) => {\n  // Add useState here\n  \n  return (\n    <AuthContext.Provider value={{}}>\n      {children}\n    </AuthContext.Provider>\n  );\n};",
        "expectedOutcome": "The AuthProvider component now has a state variable isAuthenticated initialized to false.",
        "successCriteria": [
          "The useState hook is declared inside AuthProvider with isAuthenticated and setIsAuthenticated, initialized to false."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using useState with isAuthenticated and setIsAuthenticated, initialized to false.",
        "feedbackWrong": "That doesn't look right. You need to add a useState hook inside AuthProvider to manage isAuthenticated state.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useState",
            "isAuthenticated",
            "false"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 12",
        "title": "Create login handler function",
        "instruction": "Inside the AuthProvider component, create a function named login that sets the isAuthenticated state to true.",
        "hint": "Define a function that calls the setter function from useState with the value true.",
        "analogousExample": "// Example of a simple handler function\nfunction login() {\n  setIsAuthenticated(true);\n}",
        "seedCode": "// Import React, createContext, useContext, and useState from 'react'\nimport React, { createContext, useContext, useState } from 'react';\n\n// Create the AuthContext here\nconst AuthContext = createContext();\n\n// Create the AuthProvider component\nconst AuthProvider = ({ children }) => {\n  // Add useState here\n  const [isAuthenticated, setIsAuthenticated] = useState(false);\n  \n  // Create the login handler function here\n  \n  return (\n    <AuthContext.Provider value={{}}>\n      {children}\n    </AuthContext.Provider>\n  );\n};",
        "expectedOutcome": "The AuthProvider component now contains a login function that will update the authentication state.",
        "successCriteria": [
          "A function named login is defined inside AuthProvider and it calls setIsAuthenticated(true)."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you've defined a function named login that calls setIsAuthenticated(true).",
        "feedbackWrong": "The login function is missing or doesn't properly set isAuthenticated to true.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "login",
            "setIsAuthenticated",
            "true"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 12",
        "title": "Create logout handler function",
        "instruction": "Inside your AuthProvider component, create a function named handleLogout that sets isAuthenticated to false.",
        "hint": "Define a function that calls setIsAuthenticated with false. This function will be used later to log out the user.",
        "analogousExample": "// Similar: a function that updates state\nfunction handleLogout() {\n  setIsAuthenticated(false);\n}",
        "seedCode": "// Import React, createContext, useContext, and useState from 'react'\nimport React, { createContext, useContext, useState } from 'react';\n\n// Create the AuthContext here\nconst AuthContext = createContext();\n\n// Create the AuthProvider component\nconst AuthProvider = ({ children }) => {\n  // Add useState here\n  const [isAuthenticated, setIsAuthenticated] = useState(false);\n  \n  // Create the login handler function here\n  \n  // Create the logout handler function here\n  \n  return (\n    <AuthContext.Provider value={{}}>\n      {children}\n    </AuthContext.Provider>\n  );\n};",
        "expectedOutcome": "The AuthProvider component now contains a handleLogout function that updates the authentication state.",
        "successCriteria": [
          "A function named handleLogout is defined inside AuthProvider and calls setIsAuthenticated(false)."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you've defined a function named handleLogout that sets isAuthenticated to false.",
        "feedbackWrong": "The logout handler function is missing or incorrect. Create a function named handleLogout that calls setIsAuthenticated(false).",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "handleLogout",
            "setIsAuthenticated",
            "false"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 12",
        "title": "Set up AuthProvider return structure",
        "instruction": "In the AuthProvider component's return statement, pass the authentication state and handler functions as the value prop to AuthContext.Provider.",
        "hint": "The value prop should be an object containing isAuthenticated, login, and logout.",
        "analogousExample": "// Similar: passing values to a context provider\n<ThemeContext.Provider value={{ theme, toggleTheme }}>\n  {children}\n</ThemeContext.Provider>",
        "seedCode": "// Import React, createContext, useContext, and useState from 'react'\nimport React, { createContext, useContext, useState } from 'react';\n\n// Create the AuthContext here\nconst AuthContext = createContext();\n\n// Create the AuthProvider component\nconst AuthProvider = ({ children }) => {\n  // Add useState here\n  const [isAuthenticated, setIsAuthenticated] = useState(false);\n  \n  // Create the login handler function here\n  const login = () => {\n    setIsAuthenticated(true);\n  };\n  \n  // Create the logout handler function here\n  const logout = () => {\n    setIsAuthenticated(false);\n  };\n  \n  return (\n    <AuthContext.Provider value={{}}>\n      {children}\n    </AuthContext.Provider>\n  );\n};",
        "expectedOutcome": "The AuthProvider component now provides authentication state and handlers to its children via context.",
        "successCriteria": [
          "The AuthContext.Provider's value prop contains an object with isAuthenticated, login, and logout."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure the value prop includes all three items: isAuthenticated, login, and logout.",
        "feedbackWrong": "The AuthProvider's return structure is incomplete. The value prop should contain the authentication state and handler functions.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "value={{",
            "isAuthenticated",
            "login",
            "logout",
            "}}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 12",
        "title": "Create useAuth custom hook",
        "instruction": "Define a custom hook named useAuth that returns the value from useContext(AuthContext).",
        "hint": "Create a function that calls useContext with AuthContext as its argument and returns the result.",
        "analogousExample": "// Example of a custom hook\nfunction useTheme() {\n  return useContext(ThemeContext);\n}",
        "seedCode": "// Import React, createContext, useContext, and useState from 'react'\nimport React, { createContext, useContext, useState } from 'react';\n\n// Create the AuthContext here\nconst AuthContext = createContext();\n\n// Create the AuthProvider component\nconst AuthProvider = ({ children }) => {\n  // Add useState here\n  const [isAuthenticated, setIsAuthenticated] = useState(false);\n  \n  // Create the login handler function here\n  const login = () => {\n    setIsAuthenticated(true);\n  };\n  \n  // Create the logout handler function here\n  const logout = () => {\n    setIsAuthenticated(false);\n  };\n  \n  return (\n    <AuthContext.Provider value={{}}>\n      {children}\n    </AuthContext.Provider>\n  );\n};\n\n// Define the useAuth custom hook here",
        "expectedOutcome": "A custom hook named useAuth is created, which will allow components to easily access the authentication context.",
        "successCriteria": [
          "The useAuth function is defined and returns the result of useContext(AuthContext)."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure your useAuth function calls useContext with AuthContext and returns the result.",
        "feedbackWrong": "The useAuth hook isn't quite right. Remember to define a function named useAuth that returns useContext(AuthContext).",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useAuth",
            "useContext",
            "AuthContext",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 12",
        "title": "Create a LoginButton component",
        "instruction": "Create a functional component named LoginButton that uses the useAuth hook.",
        "hint": "Define a new component that calls useAuth to get the auth context values.",
        "analogousExample": "// Example: a component that uses a custom hook\nfunction UserProfile() {\n  const user = useUser();\n  return <div>Hello {user.name}</div>;\n}",
        "seedCode": "// Import React, createContext, useContext, and useState from 'react'\nimport React, { createContext, useContext, useState } from 'react';\n\n// Create the AuthContext here\nconst AuthContext = createContext();\n\n// Create the AuthProvider component\nconst AuthProvider = ({ children }) => {\n  // Add useState here\n  const [isAuthenticated, setIsAuthenticated] = useState(false);\n  \n  // Create the login handler function here\n  const login = () => {\n    setIsAuthenticated(true);\n  };\n  \n  // Create the logout handler function here\n  const logout = () => {\n    setIsAuthenticated(false);\n  };\n  \n  return (\n    <AuthContext.Provider value={{}}>\n      {children}\n    </AuthContext.Provider>\n  );\n};\n\n// Define the useAuth custom hook here\n\n// Create the LoginButton component here",
        "expectedOutcome": "A LoginButton component is defined that will later use the auth context.",
        "successCriteria": [
          "The LoginButton component is created and uses the useAuth hook."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created a component, but make sure it uses the useAuth hook.",
        "feedbackWrong": "The component is missing or doesn't use the useAuth hook.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "LoginButton",
            "useAuth"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 12",
        "title": "Add login button and wire onClick to login handler",
        "instruction": "Inside the LoginButton component, add a button element with the text 'Log In' and wire its onClick event to call the login function from the AuthContext.",
        "hint": "Use the useAuth hook to access the login function from context, then create a button with onClick={login}.",
        "analogousExample": "// Example: button with click handler\nfunction ActionButton() {\n  const { performAction } = useActionContext();\n  return <button onClick={performAction}>Do Action</button>;\n}",
        "seedCode": "// Import React, createContext, useContext, and useState from 'react'\nimport React, { createContext, useContext, useState } from 'react';\n\n// Create the AuthContext here\nconst AuthContext = createContext();\n\n// Create the AuthProvider component\nconst AuthProvider = ({ children }) => {\n  // Add useState here\n  const [isAuthenticated, setIsAuthenticated] = useState(false);\n  \n  // Create the login handler function here\n  const login = () => {\n    setIsAuthenticated(true);\n  };\n  \n  // Create the logout handler function here\n  const logout = () => {\n    setIsAuthenticated(false);\n  };\n  \n  return (\n    <AuthContext.Provider value={{}}>\n      {children}\n    </AuthContext.Provider>\n  );\n};\n\n// Define the useAuth custom hook here\n\n// Create the LoginButton component here\nconst LoginButton = () => {\n  // Add button here\n};",
        "expectedOutcome": "The LoginButton component will render a button that triggers the login function when clicked.",
        "successCriteria": [
          "LoginButton component contains a button element with text 'Log In' and onClick={login}."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using the login function from context and wiring it to the button's onClick.",
        "feedbackWrong": "The button isn't properly connected to the login function. Check that you're using the login function from context and the onClick attribute.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "LoginButton",
            "button",
            "onClick={login}",
            "'Log In'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step11",
        "type": "question",
        "phase": "Step 11 of 12",
        "title": "Add logout button and wire onClick to logout handler",
        "instruction": "In the LoginButton component, add a logout button element that calls the logout function from context when clicked.",
        "hint": "Add a second button element with an onClick handler that calls logout. Place it after the existing login button.",
        "analogousExample": "// Example: adding a second button with onClick\nfunction ActionButtons() {\n  const { action1, action2 } = useSomeContext();\n  return (\n    <div>\n      <button onClick={action1}>Do First</button>\n      <button onClick={action2}>Do Second</button>\n    </div>\n  );\n}",
        "seedCode": "// Import React, createContext, useContext, and useState from 'react'\nimport React, { createContext, useContext, useState } from 'react';\n\n// Create the AuthContext here\nconst AuthContext = createContext();\n\n// Create the AuthProvider component\nconst AuthProvider = ({ children }) => {\n  // Add useState here\n  const [isAuthenticated, setIsAuthenticated] = useState(false);\n  \n  // Create the login handler function here\n  const login = () => {\n    setIsAuthenticated(true);\n  };\n  \n  // Create the logout handler function here\n  const logout = () => {\n    setIsAuthenticated(false);\n  };\n  \n  return (\n    <AuthContext.Provider value={{}}>\n      {children}\n    </AuthContext.Provider>\n  );\n};\n\n// Define the useAuth custom hook here\n\n// Create the LoginButton component here\nconst LoginButton = () => {\n  // Add button here\n};",
        "expectedOutcome": "The LoginButton component will have two buttons: one for login and one for logout, both wired to their respective context handlers.",
        "successCriteria": [
          "A logout button element is added to the LoginButton component with an onClick handler that calls the logout function."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added a logout button, but make sure it has an onClick handler that calls the logout function from context.",
        "feedbackWrong": "The logout button is missing or not properly wired. Add a button element with onClick={logout} inside the LoginButton component.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<button",
            "onClick={logout}",
            "Logout"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step12",
        "type": "question",
        "phase": "Step 12 of 12",
        "title": "Display authentication status in LoginButton",
        "instruction": "Inside the LoginButton component, display the current isAuthenticated value from context by using the useAuth hook.",
        "hint": "Call useAuth() to get the authentication context, then display the isAuthenticated value in your JSX.",
        "analogousExample": "// Example: Displaying context value in a component\nconst StatusDisplay = () => {\n  const { isActive } = useStatus();\n  return (\n    <div>\n      <p>Current status: {isActive ? 'Active' : 'Inactive'}</p>\n    </div>\n  );\n};",
        "seedCode": "// Import React, createContext, useContext, and useState from 'react'\nimport React, { createContext, useContext, useState } from 'react';\n\n// Create the AuthContext here\nconst AuthContext = createContext();\n\n// Create the AuthProvider component\nconst AuthProvider = ({ children }) => {\n  // Add useState here\n  const [isAuthenticated, setIsAuthenticated] = useState(false);\n  \n  // Create the login handler function here\n  const login = () => {\n    setIsAuthenticated(true);\n  };\n  \n  // Create the logout handler function here\n  const logout = () => {\n    setIsAuthenticated(false);\n  };\n  \n  return (\n    <AuthContext.Provider value={{}}>\n      {children}\n    </AuthContext.Provider>\n  );\n};\n\n// Define the useAuth custom hook here\n\n// Create the LoginButton component here\nconst LoginButton = () => {\n  // Add button here\n};",
        "expectedOutcome": "The LoginButton component will show the current authentication status from context.",
        "successCriteria": [
          "LoginButton uses useAuth to get isAuthenticated and displays it in the UI."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're calling useAuth() and displaying the isAuthenticated value in your JSX.",
        "feedbackWrong": "The LoginButton component doesn't yet display the authentication status. Remember to use the useAuth hook to access the context value.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useAuth",
            "isAuthenticated",
            "{"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import React, createContext, useContext, and useState",
        "id": "step1"
      },
      {
        "label": "Create the AuthContext",
        "id": "step2"
      },
      {
        "label": "Create the AuthProvider component",
        "id": "step3"
      },
      {
        "label": "Initialize authentication state in AuthProvider",
        "id": "step4"
      },
      {
        "label": "Create login handler function",
        "id": "step5"
      },
      {
        "label": "Create logout handler function",
        "id": "step6"
      },
      {
        "label": "Set up AuthProvider return structure",
        "id": "step7"
      },
      {
        "label": "Create useAuth custom hook",
        "id": "step8"
      },
      {
        "label": "Create a LoginButton component",
        "id": "step9"
      },
      {
        "label": "Add login button and wire onClick to login handler",
        "id": "step10"
      },
      {
        "label": "Add logout button and wire onClick to logout handler",
        "id": "step11"
      },
      {
        "label": "Display authentication status in LoginButton",
        "id": "step12"
      }
    ]
  },
  "source": "content"
}