{
  "success": true,
  "config": {
    "lessonId": "ai-react-js-94",
    "track": "react-js",
    "lessonNum": 94,
    "title": "Design Notification System",
    "shortName": "P94",
    "intro": {
      "tag": "LESSON #94",
      "title": "Design Notification System",
      "body": "In this lesson, you will build a simple notification system in React that displays messages to users, such as alerts or updates. You will learn how to manage component state to control when notifications appear and disappear. The main technologies involved are React's useState hook for state management and JSX for rendering the notification UI. By the end, you'll have a functional component that can show and hide notifications dynamically.",
      "usecase": "Notification systems are essential in modern web applications, appearing in products like social media platforms (Facebook's notifications), collaboration tools (Slack's message alerts), and e-commerce sites (Amazon's order updates). Developers implement these as interactive UI components that display real-time feedback, error messages, or system alerts in dashboards, forms, and user panels. This pattern allows users to stay informed about important events without disrupting their workflow, requiring developers to manage state, timing, and user interactions effectively."
    },
    "objectives": [
      "Initialize a state variable to manage notification data",
      "Construct a reusable Notification component with props",
      "Implement a function to add new notifications to the system",
      "Configure conditional rendering to display notifications based on state",
      "Apply styling to differentiate notification types (success, error, warning)",
      "Integrate a remove function to clear notifications from the display",
      "Manipulate state to control notification visibility and timing"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Import React and useState",
        "instruction": "Import React and the useState hook from the 'react' package.",
        "hint": "Use the import statement to bring in React and useState from the 'react' module.",
        "analogousExample": "// Importing React and a hook\nimport React, { useState } from 'react';",
        "seedCode": "// Import React and useState here",
        "expectedOutcome": "The necessary React dependencies are imported for state management.",
        "successCriteria": [
          "React and useState are imported from 'react'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're importing both React and useState from 'react'.",
        "feedbackWrong": "That doesn't look right. You need to import React and useState from the 'react' package.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "React",
            "useState",
            "'react'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Create the NotificationSystem component",
        "instruction": "Create a functional component named NotificationSystem that returns an empty div element.",
        "hint": "Define a function called NotificationSystem that returns JSX containing a single div.",
        "analogousExample": "// Example: a simple component returning a div\nfunction ExampleComponent() {\n  return <div></div>;\n}",
        "seedCode": "import React, { useState } from 'react';\n\n// Create the NotificationSystem component here",
        "expectedOutcome": "The NotificationSystem component is defined and returns an empty div.",
        "successCriteria": [
          "A functional component named NotificationSystem exists and returns a div element."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created a component, but make sure it's named exactly 'NotificationSystem' and returns a div.",
        "feedbackWrong": "The component wasn't created correctly. Check that you defined a function named NotificationSystem that returns a div.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "NotificationSystem",
            "return",
            "<div>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Initialize notifications state",
        "instruction": "Inside your NotificationSystem component, use useState to create a state variable named 'notifications' initialized as an empty array.",
        "hint": "Call useState with an empty array [] as the initial value, and destructure the returned array into a variable and setter function.",
        "analogousExample": "// Example: state initialization\nconst [items, setItems] = useState([]);",
        "seedCode": "import React, { useState } from 'react';\n\n// Create the NotificationSystem component here\nconst NotificationSystem = () => {\n  // Initialize notifications state here\n\n  return null;\n};",
        "expectedOutcome": "The component will have a state variable 'notifications' ready to store notification objects.",
        "successCriteria": [
          "The useState hook is called with [] as initial value, and the state variable is named 'notifications'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using useState with an empty array [] and naming the variable 'notifications'.",
        "feedbackWrong": "Inside the component, use useState to create a state variable named 'notifications' initialized as an empty array.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useState",
            "notifications",
            "[]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Define addNotification handler",
        "instruction": "Inside the NotificationSystem component, define a function called addNotification that takes message and type parameters and adds a new notification object to the state.",
        "hint": "Create a function that receives message and type, then uses setNotifications to add a new object with id, message, type, and timestamp.",
        "analogousExample": "// Similar: function to add items to state\nfunction addItem(item) {\n  setItems(prev => [...prev, { id: Date.now(), name: item }]);\n}",
        "seedCode": "import React, { useState } from 'react';\n\nconst NotificationSystem = () => {\n  const [notifications, setNotifications] = useState([]);\n\n  // Define addNotification function here\n\n  return null;\n};",
        "expectedOutcome": "A function named addNotification is defined that creates new notification objects and updates the state array.",
        "successCriteria": [
          "The addNotification function is defined with message and type parameters, creates a notification object with id, message, type, and timestamp, and updates the notifications state using setNotifications."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your function properly creates the notification object with all required properties and updates the state correctly.",
        "feedbackWrong": "The addNotification function isn't defined correctly. Check that it takes message and type parameters and properly updates the notifications state.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "addNotification",
            "message",
            "type",
            "setNotifications",
            "id",
            "timestamp"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Create the main container structure",
        "instruction": "In your NotificationSystem component, return a div element with className 'notification-system' that contains an h1 element with the text 'Notification System'.",
        "hint": "Replace the current return statement (return null;) with a JSX structure. Start with a div that has the specified className, and place the h1 inside it.",
        "analogousExample": "// Example: returning a simple container with a heading\nfunction MyComponent() {\n  return (\n    <div className=\"my-container\">\n      <h1>My App</h1>\n    </div>\n  );\n}",
        "seedCode": "import React, { useState } from 'react';\n\nconst NotificationSystem = () => {\n  const [notifications, setNotifications] = useState([]);\n\n  // Define addNotification function here\n\n  // Define removeNotification function here\n\n  return null;\n};",
        "expectedOutcome": "The component now returns a structured JSX layout with a main container and a title heading.",
        "successCriteria": [
          "The component returns a div with className 'notification-system' containing an h1 with the text 'Notification System'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you have a div with className 'notification-system' and an h1 with exactly 'Notification System' inside it.",
        "feedbackWrong": "The structure doesn't match. Check that you're returning a div with the correct className and an h1 with the specified text.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "return",
            "div",
            "className=\"notification-system\"",
            "h1",
            "Notification System"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Add notification controls section",
        "instruction": "Inside the NotificationSystem component's return statement, add a <div> with className 'controls' that contains two buttons: one for adding success notifications and one for adding error notifications.",
        "hint": "Create a div with className='controls' and place two button elements inside it. Give each button appropriate text like 'Add Success' and 'Add Error'.",
        "analogousExample": "// Example: Adding a controls section\n<div className=\"controls\">\n  <button>Action 1</button>\n  <button>Action 2</button>\n</div>",
        "seedCode": "import React, { useState } from 'react';\n\nconst NotificationSystem = () => {\n  const [notifications, setNotifications] = useState([]);\n\n  // Define addNotification function here\n\n  // Define removeNotification function here\n\n  return (\n    <div className=\"notification-system\">\n      {/* Add controls section here */}\n    </div>\n  );\n};",
        "expectedOutcome": "A controls section with two buttons appears in the component's structure, ready to be wired to handlers in the next step.",
        "successCriteria": [
          "A div with className 'controls' containing two button elements is added to the return statement."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You added some controls, but make sure you have a div with className 'controls' containing both success and error buttons.",
        "feedbackWrong": "The controls section is missing or incomplete. Add a div with className 'controls' containing two buttons.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<div",
            "className=\"controls\"",
            "<button",
            "</div>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Map through notifications state",
        "instruction": "Inside the notifications container, map through the notifications state array and return a div for each notification with appropriate className based on type.",
        "hint": "Use the map method on the notifications array to create a div element for each notification. The className should be 'notification' plus the notification type (e.g., 'notification success').",
        "analogousExample": "// Example: mapping through an array to create elements\nconst items = ['apple', 'banana', 'orange'];\nconst list = items.map((item, index) => (\n  <div key={index} className={'item ' + item}>\n    {item}\n  </div>\n));",
        "seedCode": "import React, { useState } from 'react';\n\nconst NotificationSystem = () => {\n  const [notifications, setNotifications] = useState([]);\n\n  // Define addNotification function here\n\n  // Define removeNotification function here\n\n  return (\n    <div className=\"notification-system\">\n      {/* Add controls section here */}\n      <div className=\"notifications-container\">\n        {/* Map through notifications here */}\n      </div>\n    </div>\n  );\n};",
        "expectedOutcome": "The notifications container will have a div for each notification in the state array, with a className that includes the notification type.",
        "successCriteria": [
          "The notifications array is mapped to create div elements, each with a key prop and a className that combines 'notification' and the notification type."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you're using the map method on the notifications array and returning a div with a key prop and appropriate className.",
        "feedbackWrong": "Check your code. You need to map through the notifications array and return a div for each notification with a key prop and className based on its type.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "notifications",
            "map",
            "key",
            "className"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Display notification content",
        "instruction": "Inside each notification div, add a span element to show the notification message and a small element to show the notification type.",
        "hint": "Access the notification's message and type properties from the map callback parameter. Use JSX curly braces to display them.",
        "analogousExample": "// Example: displaying item details in a list\nitems.map(item => (\n  <div key={item.id}>\n    <span>{item.name}</span>\n    <small>{item.category}</small>\n  </div>\n))",
        "seedCode": "import React, { useState } from 'react';\n\nconst NotificationSystem = () => {\n  const [notifications, setNotifications] = useState([]);\n\n  // Define addNotification function here\n\n  // Define removeNotification function here\n\n  return (\n    <div className=\"notification-system\">\n      {/* Add controls section here */}\n      <div className=\"notifications-container\">\n        {notifications.map(notification => (\n          <div key={notification.id} className=\"notification\">\n            {/* Add notification content here */}\n          </div>\n        ))}\n      </div>\n    </div>\n  );\n};",
        "expectedOutcome": "Each notification element will display its message and type when rendered.",
        "successCriteria": [
          "Each notification div contains a span with the message and a small tag with the type."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you've added both the span for the message and the small tag for the type inside each notification div.",
        "feedbackWrong": "The notification content isn't displaying correctly. Check that you've added both elements inside the map callback and are accessing the notification properties correctly.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "span",
            "small",
            "notification.message",
            "notification.type"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "Add close button to notifications",
        "instruction": "Inside each notification's div, add a button element with the text '×' and className 'close-btn'. Wire its onClick to call removeNotification with that notification's id.",
        "hint": "Place the button inside the notification div, before or after the content. Use onClick={() => removeNotification(notification.id)}.",
        "analogousExample": "// Adding a close button to an item\nfunction Item({ id, removeItem }) {\n  return (\n    <div className=\"item\">\n      <span>Item {id}</span>\n      <button className=\"close-btn\" onClick={() => removeItem(id)}>×</button>\n    </div>\n  );\n}",
        "seedCode": "import React, { useState } from 'react';\n\nconst NotificationSystem = () => {\n  const [notifications, setNotifications] = useState([]);\n\n  // Define addNotification function here\n\n  // Define removeNotification function here\n\n  return (\n    <div className=\"notification-system\">\n      {/* Add controls section here */}\n      <div className=\"notifications-container\">\n        {notifications.map(notification => (\n          <div key={notification.id} className=\"notification\">\n            {/* Add notification content here */}\n          </div>\n        ))}\n      </div>\n    </div>\n  );\n};",
        "expectedOutcome": "Each notification will have a close button that, when clicked, removes that specific notification from the state.",
        "successCriteria": [
          "A button with text '×' and className 'close-btn' is added inside each notification div, with onClick calling removeNotification using the notification's id."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added a button, but check: does it have the correct text '×', className 'close-btn', and onClick that calls removeNotification with the notification's id?",
        "feedbackWrong": "The close button is missing or not correctly configured. Ensure each notification div contains a button with text '×', className 'close-btn', and an onClick handler that calls removeNotification with the notification's id.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "button",
            "×",
            "close-btn",
            "onClick",
            "removeNotification",
            "notification.id"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Add clear all notifications button",
        "instruction": "Add a button with the text 'Clear All' and className 'clear-all-btn' inside the controls section of your component. Wire its onClick handler to set the notifications state to an empty array.",
        "hint": "Place the button in the controls section, which is currently empty. Use setNotifications([]) to clear all notifications.",
        "analogousExample": "// Adding a clear button\n<button className=\"clear-all-btn\" onClick={() => setItems([])}>Clear All</button>",
        "seedCode": "import React, { useState } from 'react';\n\nconst NotificationSystem = () => {\n  const [notifications, setNotifications] = useState([]);\n\n  // Define addNotification function here\n\n  // Define removeNotification function here\n\n  return (\n    <div className=\"notification-system\">\n      {/* Add controls section here */}\n      <div className=\"notifications-container\">\n        {notifications.map(notification => (\n          <div key={notification.id} className=\"notification\">\n            {/* Add notification content here */}\n          </div>\n        ))}\n      </div>\n    </div>\n  );\n};",
        "expectedOutcome": "A 'Clear All' button appears in the controls section. Clicking it removes all notifications from the UI.",
        "successCriteria": [
          "The button has text 'Clear All', className 'clear-all-btn', and onClick handler that sets notifications to []."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added a button, but make sure it has the correct text, className, and onClick handler.",
        "feedbackWrong": "The button is missing or doesn't have the required attributes. Check the instruction again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "Clear All",
            "clear-all-btn",
            "onClick",
            "setNotifications([])"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import React and useState",
        "id": "step1"
      },
      {
        "label": "Create the NotificationSystem component",
        "id": "step2"
      },
      {
        "label": "Initialize notifications state",
        "id": "step3"
      },
      {
        "label": "Define addNotification handler",
        "id": "step4"
      },
      {
        "label": "Create the main container structure",
        "id": "step5"
      },
      {
        "label": "Add notification controls section",
        "id": "step6"
      },
      {
        "label": "Map through notifications state",
        "id": "step7"
      },
      {
        "label": "Display notification content",
        "id": "step8"
      },
      {
        "label": "Add close button to notifications",
        "id": "step9"
      },
      {
        "label": "Add clear all notifications button",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}