{
  "success": true,
  "config": {
    "lessonId": "ai-react-js-42",
    "track": "react-js",
    "lessonNum": 42,
    "title": "Context Performance",
    "shortName": "P42",
    "intro": {
      "tag": "LESSON #42",
      "title": "Context Performance",
      "body": "In this lesson, you will build a simple React app that demonstrates how to optimize performance when using the Context API. You'll learn to prevent unnecessary re-renders by splitting context providers and using React.memo. The core concept is understanding how context updates trigger component re-renders and applying techniques to control them. You'll work with React hooks like useContext, useState, and the React.memo higher-order component. By the end, you'll have a functional app that efficiently updates only the relevant parts of the UI when context values change.",
      "usecase": "In complex React applications like e-commerce platforms or analytics dashboards, context performance optimization is crucial when multiple components depend on shared state. For example, a product filtering system where dozens of UI elements (price sliders, category checkboxes, sort dropdowns) all consume filter state from a single context. Without proper optimization, changing one filter could trigger unnecessary re-renders across the entire interface, causing laggy user interactions. Developers implement context performance patterns to ensure that only the relevant components update when context values change, maintaining smooth UX in data-heavy applications."
    },
    "objectives": [
      "Initialize a React context with default values",
      "Configure a context provider to wrap application components",
      "Apply the useContext hook to access context values in child components",
      "Manipulate context values through provider state updates",
      "Construct a performance-optimized context by preventing unnecessary re-renders",
      "Integrate memoization techniques to control component updates within context",
      "Render components that selectively respond to specific context value changes"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 11",
        "title": "Import React, useState, useContext, createContext and useMemo",
        "instruction": "Import React, useState, useContext, createContext, and useMemo from 'react'.",
        "hint": "Use a single import statement with curly braces to import multiple named exports from the 'react' package.",
        "analogousExample": "// Importing multiple hooks from React\nimport React, { useState, useEffect } from 'react';",
        "seedCode": "// Import React and the required hooks here",
        "expectedOutcome": "The necessary React APIs and hooks are imported, enabling you to use state, context, and memoization in this lesson.",
        "successCriteria": [
          "React, useState, useContext, createContext, and useMemo are all imported from 'react'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've imported some of the required items, but not all. Make sure to import React, useState, useContext, createContext, and useMemo from 'react' in a single import statement.",
        "feedbackWrong": "The imports are missing or incorrect. You need to import React, useState, useContext, createContext, and useMemo from 'react' to proceed with this lesson.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "React",
            "useState",
            "useContext",
            "createContext",
            "useMemo",
            "'react'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 11",
        "title": "Create a ThemeContext using createContext",
        "instruction": "Create a ThemeContext using React.createContext() and export it.",
        "hint": "Use createContext to create a new context object. Remember to export it so it can be imported in other files.",
        "analogousExample": "// Example: creating and exporting a context\nconst UserContext = React.createContext();\nexport default UserContext;",
        "seedCode": "// Import React and the required hooks here\nimport React, { useState, useContext, createContext, useMemo } from 'react';\n\n// Create and export ThemeContext here",
        "expectedOutcome": "A ThemeContext object is created and exported, ready to hold theme values.",
        "successCriteria": [
          "ThemeContext is created with createContext and exported."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're both creating the context with createContext() and exporting it so other components can use it.",
        "feedbackWrong": "create a ThemeContext using React.createContext() and export it so it can be imported elsewhere.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "ThemeContext",
            "createContext",
            "export"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 11",
        "title": "Create a ThemeProvider component",
        "instruction": "Create a functional component named ThemeProvider that accepts children as props.",
        "hint": "Define a component that takes children as a parameter and returns them wrapped in ThemeContext.Provider.",
        "analogousExample": "// Example: a wrapper component\nfunction Wrapper({ children }) {\n  return (\n    <div>\n      {children}\n    </div>\n  );\n}",
        "seedCode": "// Import React and the required hooks here\nimport React, { useState, useContext, createContext, useMemo } from 'react';\n\n// Create and export ThemeContext here\n\n// Create ThemeProvider component here",
        "expectedOutcome": "A ThemeProvider component is defined that can wrap children.",
        "successCriteria": [
          "ThemeProvider is a functional component that accepts children as props."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure ThemeProvider is a functional component that accepts children as a prop.",
        "feedbackWrong": "ThemeProvider should be a functional component that accepts children as props. Check the syntax.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "ThemeProvider",
            "children",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 11",
        "title": "Initialize theme state in ThemeProvider",
        "instruction": "Inside the ThemeProvider component, use useState to create a state variable called 'theme' initialized to the string 'light'.",
        "hint": "Call useState with the initial value 'light' and destructure the returned array into two variables: theme and setTheme.",
        "analogousExample": "// Example: initializing state in a component\nfunction Counter() {\n  const [count, setCount] = useState(0);\n  return null;\n}",
        "seedCode": "// Import React and the required hooks here\nimport React, { useState, useContext, createContext, useMemo } from 'react';\n\n// Create and export ThemeContext here\nconst ThemeContext = createContext();\n\n// Create ThemeProvider component here\nconst ThemeProvider = ({ children }) => {\n  // Initialize theme state here\n  \n  return (\n    <ThemeContext.Provider value={{}}>\n      {children}\n    </ThemeContext.Provider>\n  );\n};",
        "expectedOutcome": "The ThemeProvider component should have a 'theme' state variable initialized to 'light'.",
        "successCriteria": [
          "ThemeProvider uses useState to declare a 'theme' state variable initialized to 'light'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using useState to create a state variable called 'theme' with initial value 'light'.",
        "feedbackWrong": "That doesn't look right. You need to use useState inside ThemeProvider to create a 'theme' state variable initialized to 'light'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useState",
            "theme",
            "'light'",
            "light"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 11",
        "title": "Create toggleTheme handler in ThemeProvider",
        "instruction": "Inside the ThemeProvider component, create a function named toggleTheme that toggles the theme state between 'light' and 'dark'.",
        "hint": "The function should update the theme state using the setTheme function. Use the previous state to determine the new theme value.",
        "analogousExample": "// Similar: a function that toggles a boolean state\nfunction toggleSwitch() {\n  setSwitch(prev => !prev);\n}",
        "seedCode": "import React, { useState, useContext, createContext, useMemo } from 'react';\n\nconst ThemeContext = createContext();\n\nconst ThemeProvider = ({ children }) => {\n  const [theme, setTheme] = useState('light');\n  \n  // Create toggleTheme function here\n  \n  return (\n    <ThemeContext.Provider value={{}}>\n      {children}\n    </ThemeContext.Provider>\n  );\n};",
        "expectedOutcome": "A toggleTheme function is defined inside ThemeProvider that toggles the theme state between 'light' and 'dark'.",
        "successCriteria": [
          "The toggleTheme function is created and correctly toggles the theme state value."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created a function, but make sure it properly toggles between 'light' and 'dark' using the setTheme function.",
        "feedbackWrong": "The toggleTheme function is missing or doesn't correctly toggle the theme state. Review how to update state based on previous state.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "toggleTheme",
            "setTheme",
            "prev",
            "=>",
            "'light'",
            "'dark'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 11",
        "title": "Memoize context value with useMemo",
        "instruction": "Inside the ThemeProvider component, use the useMemo hook to create a memoized context value object containing theme and toggleTheme.",
        "hint": "Call useMemo with a function that returns an object with theme and toggleTheme properties, and pass [theme] as the dependency array.",
        "analogousExample": "// Similar: memoizing an object value\nconst memoizedValue = useMemo(() => ({\n  data: someData,\n  update: updateFunction\n}), [someData]);",
        "seedCode": "import React, { useState, useContext, createContext, useMemo } from 'react';\n\nconst ThemeContext = createContext();\n\nconst ThemeProvider = ({ children }) => {\n  const [theme, setTheme] = useState('light');\n  \n  const toggleTheme = () => {\n    setTheme(prevTheme => prevTheme === 'light' ? 'dark' : 'light');\n  };\n  \n  // Use useMemo to create memoized context value here\n  \n  return (\n    <ThemeContext.Provider value={{}}>\n      {children}\n    </ThemeContext.Provider>\n  );\n};",
        "expectedOutcome": "The ThemeProvider component now includes a useMemo call that creates a memoized object with theme and toggleTheme properties.",
        "successCriteria": [
          "useMemo is called with a function returning { theme, toggleTheme } and [theme] as dependency array."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure useMemo returns an object with both theme and toggleTheme properties, and includes [theme] as the dependency array.",
        "feedbackWrong": "Use useMemo to create a memoized object containing theme and toggleTheme, with theme as the dependency.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useMemo",
            "theme",
            "toggleTheme",
            "[theme]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 11",
        "title": "Return ThemeContext.Provider with memoized value",
        "instruction": "In the ThemeProvider component, update the ThemeContext.Provider's value prop to use the memoized context value you created in the previous step.",
        "hint": "The value prop should receive the memoized object containing theme and toggleTheme.",
        "analogousExample": "// Similar: providing context value\n<MyContext.Provider value={memoizedValue}>\n  {children}\n</MyContext.Provider>",
        "seedCode": "import React, { useState, useContext, createContext, useMemo } from 'react';\n\nconst ThemeContext = createContext();\n\nconst ThemeProvider = ({ children }) => {\n  const [theme, setTheme] = useState('light');\n  \n  const toggleTheme = () => {\n    setTheme(prevTheme => prevTheme === 'light' ? 'dark' : 'light');\n  };\n  \n  const memoizedValue = useMemo(() => ({\n    theme,\n    toggleTheme\n  }), [theme]);\n  \n  return (\n    <ThemeContext.Provider value={memoizedValue}>\n      {children}\n    </ThemeContext.Provider>\n  );\n};",
        "expectedOutcome": "The ThemeContext.Provider now provides the memoized context value to its children.",
        "successCriteria": [
          "ThemeContext.Provider's value prop is set to the memoizedValue variable."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're passing the memoizedValue variable to the value prop of ThemeContext.Provider.",
        "feedbackWrong": "The ThemeContext.Provider should receive the memoizedValue as its value prop. Check that you're using the correct variable name.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "ThemeContext.Provider",
            "value={memoizedValue}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 11",
        "title": "Add button styling based on theme",
        "instruction": "Inside the ThemedButton component, add conditional styling to the button element so it has a dark background when the theme is 'dark' and a light background when the theme is 'light'.",
        "hint": "You can use a ternary operator to set the button's style attribute conditionally based on the theme value.",
        "analogousExample": "// Conditional styling example\nconst StyledButton = ({ isActive }) => {\n  return (\n    <button style={{\n      backgroundColor: isActive ? 'green' : 'gray'\n    }}>\n      Click me\n    </button>\n  );\n};",
        "seedCode": "import React, { useState, useContext, createContext, useMemo } from 'react';\n\nconst ThemeContext = createContext();\n\nconst ThemeProvider = ({ children }) => {\n  const [theme, setTheme] = useState('light');\n  \n  const toggleTheme = () => {\n    setTheme(prevTheme => prevTheme === 'light' ? 'dark' : 'light');\n  };\n  \n  const memoizedValue = useMemo(() => ({\n    theme,\n    toggleTheme\n  }), [theme]);\n  \n  return (\n    <ThemeContext.Provider value={memoizedValue}>\n      {children}\n    </ThemeContext.Provider>\n  );\n};\n\n// Create ThemedButton component here\nconst ThemedButton = () => {\n  const { theme } = useContext(ThemeContext);\n  \n  return (\n    <button>\n      Toggle Theme\n    </button>\n  );\n};",
        "expectedOutcome": "The button should have different background colors based on the current theme value from context.",
        "successCriteria": [
          "The button element has a style attribute with conditional backgroundColor based on the theme value."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you're applying the style attribute to the button and using the theme value to conditionally set the backgroundColor.",
        "feedbackWrong": "The button doesn't have conditional styling based on the theme. Check that you're using the theme value from context and applying it to the button's style attribute.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "style",
            "backgroundColor",
            "theme",
            "?"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 11",
        "title": "Wire onClick to toggleTheme handler",
        "instruction": "In the ThemedButton component, add an onClick handler to the button that calls the toggleTheme function from context.",
        "hint": "Use the toggleTheme function from the context object you already destructured.",
        "analogousExample": "// Example: wiring a button click to a handler\nfunction ActionButton() {\n  const { handleClick } = useContext(ActionContext);\n  \n  return (\n    <button onClick={handleClick}>\n      Perform Action\n    </button>\n  );\n}",
        "seedCode": "import React, { useState, useContext, createContext, useMemo } from 'react';\n\nconst ThemeContext = createContext();\n\nconst ThemeProvider = ({ children }) => {\n  const [theme, setTheme] = useState('light');\n  \n  const toggleTheme = () => {\n    setTheme(prevTheme => prevTheme === 'light' ? 'dark' : 'light');\n  };\n  \n  const memoizedValue = useMemo(() => ({\n    theme,\n    toggleTheme\n  }), [theme]);\n  \n  return (\n    <ThemeContext.Provider value={memoizedValue}>\n      {children}\n    </ThemeContext.Provider>\n  );\n};\n\n// Create ThemedButton component here\nconst ThemedButton = () => {\n  const { theme } = useContext(ThemeContext);\n  \n  return (\n    <button>\n      Toggle Theme\n    </button>\n  );\n};",
        "expectedOutcome": "The button will have an onClick handler that triggers the theme toggle functionality when clicked.",
        "successCriteria": [
          "The button element has an onClick attribute that calls toggleTheme from the context."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're calling toggleTheme from the context object. Remember you can destructure it alongside theme.",
        "feedbackWrong": "The button still needs an onClick handler. Try adding onClick={toggleTheme} to the button element after getting toggleTheme from the context.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "onClick",
            "toggleTheme"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 11",
        "title": "Create main App component",
        "instruction": "Create a functional component named App that returns a div containing the ThemeProvider and ThemedButton components.",
        "hint": "Define the App component above the export statement, then return the ThemeProvider wrapping the ThemedButton inside a div.",
        "analogousExample": "// Example: a main component that returns a wrapper\nfunction App() {\n  return (\n    <div>\n      <ChildComponent />\n    </div>\n  );\n}",
        "seedCode": "import React, { useState, useContext, createContext, useMemo } from 'react';\n\nconst ThemeContext = createContext();\n\nconst ThemeProvider = ({ children }) => {\n  const [theme, setTheme] = useState('light');\n  \n  const toggleTheme = () => {\n    setTheme(prevTheme => prevTheme === 'light' ? 'dark' : 'light');\n  };\n  \n  const memoizedValue = useMemo(() => ({\n    theme,\n    toggleTheme\n  }), [theme]);\n  \n  return (\n    <ThemeContext.Provider value={memoizedValue}>\n      {children}\n    </ThemeContext.Provider>\n  );\n};\n\n// Create ThemedButton component here\nconst ThemedButton = () => {\n  const { theme } = useContext(ThemeContext);\n  \n  return (\n    <button>\n      Toggle Theme\n    </button>\n  );\n};\n\n// Create App component here\n",
        "expectedOutcome": "The App component is defined and returns a div containing ThemeProvider and ThemedButton.",
        "successCriteria": [
          "App component is defined and returns a div with ThemeProvider wrapping ThemedButton."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've started the App component, but make sure it returns a div containing ThemeProvider with ThemedButton as its child.",
        "feedbackWrong": "The App component is not properly defined. Create a functional component named App that returns a div with ThemeProvider wrapping ThemedButton.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "function App",
            "App()",
            "return",
            "<ThemeProvider>",
            "<ThemedButton>",
            "</ThemeProvider>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step11",
        "type": "question",
        "phase": "Step 11 of 11",
        "title": "Wrap app with ThemeProvider and add ThemedButton",
        "instruction": "In the App component, return the ThemeProvider wrapping the ThemedButton component.",
        "hint": "The ThemeProvider should be the outermost element, with ThemedButton as its child.",
        "analogousExample": "// Example: Wrapping a component with a provider\nfunction App() {\n  return (\n    <Provider>\n      <ChildComponent />\n    </Provider>\n  );\n}",
        "seedCode": "import React, { useState, useContext, createContext, useMemo } from 'react';\n\nconst ThemeContext = createContext();\n\nconst ThemeProvider = ({ children }) => {\n  const [theme, setTheme] = useState('light');\n  \n  const toggleTheme = () => {\n    setTheme(prevTheme => prevTheme === 'light' ? 'dark' : 'light');\n  };\n  \n  const memoizedValue = useMemo(() => ({\n    theme,\n    toggleTheme\n  }), [theme]);\n  \n  return (\n    <ThemeContext.Provider value={memoizedValue}>\n      {children}\n    </ThemeContext.Provider>\n  );\n};\n\nconst ThemedButton = () => {\n  const { theme } = useContext(ThemeContext);\n  \n  return (\n    <button>\n      Toggle Theme\n    </button>\n  );\n};\n\nconst App = () => {\n  // Return ThemeProvider wrapping ThemedButton here\n};",
        "expectedOutcome": "The App component renders the ThemeProvider with ThemedButton as its child, completing the context setup.",
        "successCriteria": [
          "App returns ThemeProvider wrapping ThemedButton."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure the App component returns the ThemeProvider with ThemedButton as its child.",
        "feedbackWrong": "The App component should return ThemeProvider wrapping ThemedButton. Check the structure of your return statement.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "ThemeProvider",
            "ThemedButton",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import React, useState, useContext, createContext and useMemo",
        "id": "step1"
      },
      {
        "label": "Create a ThemeContext using createContext",
        "id": "step2"
      },
      {
        "label": "Create a ThemeProvider component",
        "id": "step3"
      },
      {
        "label": "Initialize theme state in ThemeProvider",
        "id": "step4"
      },
      {
        "label": "Create toggleTheme handler in ThemeProvider",
        "id": "step5"
      },
      {
        "label": "Memoize context value with useMemo",
        "id": "step6"
      },
      {
        "label": "Return ThemeContext.Provider with memoized value",
        "id": "step7"
      },
      {
        "label": "Add button styling based on theme",
        "id": "step8"
      },
      {
        "label": "Wire onClick to toggleTheme handler",
        "id": "step9"
      },
      {
        "label": "Create main App component",
        "id": "step10"
      },
      {
        "label": "Wrap app with ThemeProvider and add ThemedButton",
        "id": "step11"
      }
    ]
  },
  "source": "content"
}