{
  "success": true,
  "config": {
    "lessonId": "ai-sd-10",
    "track": "sd",
    "lessonNum": 10,
    "title": "Microservices patterns",
    "shortName": "P10",
    "intro": {
      "tag": "LESSON #10",
      "title": "Microservices patterns",
      "body": "In this lesson, you will build a simple, conceptual model of a microservices-based e-commerce system to understand common architectural patterns. You will learn the core concept of service decomposition, where a large application is broken into small, independent services that communicate over a network. The main constructs involved are individual services (like 'Order Service' and 'Inventory Service'), APIs for communication, and patterns such as API Gateway and Service Discovery. By the end, you'll grasp how these patterns improve scalability and maintainability in modern software design.",
      "usecase": "Microservices patterns are essential in modern distributed systems like e-commerce platforms, where separate services handle user authentication, product catalog, shopping cart, and payment processing. In streaming services like Netflix, microservices manage user profiles, content recommendations, and video playback independently. This architecture allows development teams to deploy, scale, and update specific features—such as a search filter or recommendation engine—without disrupting the entire application, enabling faster iteration and resilience."
    },
    "objectives": [
      "Implement a basic microservice architecture by separating application logic into distinct, independent services.",
      "Configure service-to-service communication patterns such as synchronous HTTP calls between microservices.",
      "Apply the API Gateway pattern to centralize request routing and aggregation for client interactions.",
      "Construct a simple event-driven communication flow using a message broker to decouple services.",
      "Initialize and manage separate data stores for different microservices to enforce bounded contexts.",
      "Integrate a service discovery mechanism to enable dynamic location and communication between services.",
      "Manulate circuit breaker patterns to handle failures and improve resilience in inter-service calls."
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Define the core microservices patterns we'll implement",
        "instruction": "Create a function named listMicroservicesPatterns that returns an array of strings containing these five patterns: API Gateway, Service Discovery, Circuit Breaker, Event-Driven Communication, and Database per Service.",
        "hint": "Define a function that returns an array with the five pattern names as strings.",
        "analogousExample": "// Example: a function returning an array of concepts\nfunction listArchitectureConcepts() {\n    return ['Load Balancing', 'Caching', 'Sharding'];\n}",
        "seedCode": "// Create your function here\n",
        "expectedOutcome": "A function is defined that returns an array containing the five specified microservices patterns.",
        "successCriteria": [
          "The function listMicroservicesPatterns exists and returns an array with exactly these five strings: 'API Gateway', 'Service Discovery', 'Circuit Breaker', 'Event-Driven Communication', 'Database per Service'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your function returns an array with all five pattern names exactly as specified.",
        "feedbackWrong": "Create a function named listMicroservicesPatterns that returns an array containing the five specified patterns.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "listMicroservicesPatterns",
            "return",
            "API Gateway",
            "Service Discovery",
            "Circuit Breaker",
            "Event-Driven Communication",
            "Database per Service"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Set up the API Gateway pattern structure",
        "instruction": "Create a function named APIGateway that will act as the single entry point for routing requests to microservices.",
        "hint": "Define a function with the name APIGateway that returns a placeholder value for now.",
        "analogousExample": "// Similar: a function that serves as an entry point\nfunction ServiceEntry() {\n  return null;\n}",
        "seedCode": "// Create your function here\n\n// Create the APIGateway function below",
        "expectedOutcome": "You'll have a function named APIGateway defined, ready to implement routing logic in later steps.",
        "successCriteria": [
          "A function named APIGateway is created and returns a value (like null)."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you've created a function named exactly 'APIGateway' and it returns something (even null).",
        "feedbackWrong": "The step requires creating a function named APIGateway. Please define a function with that name that returns a value.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "APIGateway",
            "function",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Initialize service registry for Service Discovery",
        "instruction": "Create a service registry object that will track available microservices and their endpoints. Name it 'serviceRegistry' and initialize it as an empty object.",
        "hint": "Define a constant variable named 'serviceRegistry' and set it to an empty object {} to start tracking services.",
        "analogousExample": "// Similar: creating a registry object\nconst userRegistry = {};",
        "seedCode": "// Create your function here\n\n// Create the APIGateway function below\n\n// Create the service registry object here",
        "expectedOutcome": "A service registry object is created and ready to store microservice information.",
        "successCriteria": [
          "The 'serviceRegistry' object is defined as an empty object."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you've created an object named 'serviceRegistry' and initialized it as empty.",
        "feedbackWrong": "The service registry object wasn't created correctly. Create a constant named 'serviceRegistry' and set it to an empty object {}.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "serviceRegistry",
            "{}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Define Circuit Breaker pattern logic",
        "instruction": "Create a function named CircuitBreaker that monitors service health and fails fast when services are down. The function should take a service name and return a status.",
        "hint": "Define a function that accepts a service name parameter and returns a status string like 'open', 'closed', or 'half-open'.",
        "analogousExample": "// Example of a monitoring function\nfunction monitorService(service) {\n  if (service.healthy) {\n    return 'operational';\n  } else {\n    return 'down';\n  }\n}",
        "seedCode": "// Create your function here\n\n// Create the APIGateway function below\n\n// Create the service registry object here\n\n// Create the CircuitBreaker function here",
        "expectedOutcome": "A CircuitBreaker function is defined that can be used to check service health and prevent cascading failures.",
        "successCriteria": [
          "The CircuitBreaker function is defined with a service name parameter and returns a status."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your CircuitBreaker function accepts a service name parameter and returns a status value.",
        "feedbackWrong": "The CircuitBreaker function isn't properly defined. It should be a function that takes a service name and returns a status to monitor service health.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "CircuitBreaker",
            "function",
            "service",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Set up Event-Driven Communication infrastructure",
        "instruction": "Create a function named EventBus that will serve as a simple event bus for services to publish and subscribe to events.",
        "hint": "Define a function that returns an object with methods like publish and subscribe to handle events.",
        "analogousExample": "// Example: A basic event bus structure\nfunction EventBus() {\n  return {\n    publish: function(event, data) {},\n    subscribe: function(event, callback) {}\n  };\n}",
        "seedCode": "// Create your function here\n\n// Create the APIGateway function below\n\n// Create the service registry object here\n\n// Create the CircuitBreaker function here\n\n// Create the EventBus function here",
        "expectedOutcome": "The EventBus function is defined and ready to be implemented with publish and subscribe methods.",
        "successCriteria": [
          "The EventBus function is created and returns an object with publish and subscribe methods."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've started the EventBus function, but make sure it returns an object with publish and subscribe methods.",
        "feedbackWrong": "The EventBus function is not correctly defined. Ensure it returns an object with publish and subscribe methods.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "EventBus",
            "publish",
            "subscribe"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Implement Database per Service pattern",
        "instruction": "Create a function named DatabasePerService that demonstrates separate database connections for different services.",
        "hint": "Define a function that returns an object with isolated database connections for each service.",
        "analogousExample": "// Example: function that returns separate data stores\nfunction DatabasePerService() {\n  return {\n    userServiceDB: 'User database connection',\n    orderServiceDB: 'Order database connection'\n  };\n}",
        "seedCode": "// Create your function here\n\n// Create the APIGateway function below\n\n// Create the service registry object here\n\n// Create the CircuitBreaker function here\n\n// Create the EventBus function here\n\n// Create the DatabasePerService function here",
        "expectedOutcome": "A DatabasePerService function is created that returns separate database connections for different services.",
        "successCriteria": [
          "The DatabasePerService function is defined and returns an object with at least two different service database connections."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created a function, but make sure it returns an object with distinct database connections for at least two different services.",
        "feedbackWrong": "The DatabasePerService function is not properly implemented. Create a function that returns separate database connections for different services.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "DatabasePerService",
            "function",
            "return",
            "userServiceDB",
            "orderServiceDB"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Create sample microservices to demonstrate patterns",
        "instruction": "Create a UserService function that returns a simple string describing its purpose.",
        "hint": "Define a function named UserService that returns a string like 'UserService: manages user data'.",
        "analogousExample": "// Example of a simple service function\nfunction ProductService() {\n  return 'ProductService: handles product catalog';\n}",
        "seedCode": "// Create your function here\n\n// Create the APIGateway function below\n\n// Create the service registry object here\n\n// Create the CircuitBreaker function here\n\n// Create the EventBus function here\n\n// Create the DatabasePerService function here\n\n// Create the UserService function here",
        "expectedOutcome": "You will have a UserService function that returns a descriptive string, demonstrating a basic microservice.",
        "successCriteria": [
          "A UserService function is defined and returns a string describing its purpose."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've started creating the UserService, but make sure it returns a descriptive string about its purpose.",
        "feedbackWrong": "The UserService function is not properly defined or doesn't return a string. Try creating a function named UserService that returns a description.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "UserService",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Wire API Gateway to route requests to services",
        "instruction": "Inside the APIGateway function, add logic to route incoming requests to the appropriate microservice based on the request path. Use the service registry to find services.",
        "hint": "Check the request path and call the corresponding service function (like UserService) if the path matches.",
        "analogousExample": "// Example: routing logic in a gateway\nfunction APIGateway(request) {\n  if (request.path === '/users') {\n    return UserService(request);\n  }\n  return { error: 'Route not found' };\n}",
        "seedCode": "// Create your function here\n\n// Create the APIGateway function below\nfunction APIGateway(request) {\n  // Add routing logic here\n}\n\n// Create the service registry object here\nconst serviceRegistry = {\n  users: UserService\n};\n\n// Create the CircuitBreaker function here\nfunction CircuitBreaker(service) {\n  return function(request) {\n    // Circuit breaker logic\n  };\n}\n\n// Create the EventBus function here\nfunction EventBus() {\n  // Event bus logic\n}\n\n// Create the DatabasePerService function here\nfunction DatabasePerService(serviceName) {\n  // Database per service logic\n}\n\n// Create the UserService function here\nfunction UserService(request) {\n  // User service logic\n}",
        "expectedOutcome": "The APIGateway function now contains routing logic that uses the service registry to direct requests to the correct microservice.",
        "successCriteria": [
          "APIGateway checks the request path and calls the appropriate service function (e.g., UserService for '/users')."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added some routing logic, but make sure to check the request path and call the correct service function from the registry.",
        "feedbackWrong": "The routing logic is missing or incorrect. The APIGateway should examine the request path and route to the appropriate microservice.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "APIGateway",
            "request.path",
            "UserService",
            "serviceRegistry"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "Register services with Service Discovery",
        "instruction": "Add a registerService method to the serviceRegistry object that stores services by name.",
        "hint": "You can add a method to the serviceRegistry object that takes a service name and service function as parameters and stores them.",
        "analogousExample": "// Similar: adding a method to an object\nconst registry = {\n  add: function(name, service) {\n    this[name] = service;\n  }\n};",
        "seedCode": "// Create your function here\n\n// Create the APIGateway function below\nfunction APIGateway(request) {\n  // Add routing logic here\n}\n\n// Create the service registry object here\nconst serviceRegistry = {\n  users: UserService,\n  // Add registerService method here\n};\n\n// Create the CircuitBreaker function here\nfunction CircuitBreaker(service) {\n  return function(request) {\n    // Circuit breaker logic\n  };\n}\n\n// Create the EventBus function here\nfunction EventBus() {\n  // Event bus logic\n}\n\n// Create the DatabasePerService function here\nfunction DatabasePerService(serviceName) {\n  // Database per service logic\n}\n\n// Create the UserService function here\nfunction UserService(request) {\n  // User service logic\n}",
        "expectedOutcome": "The serviceRegistry object now has a registerService method that can store services for discovery.",
        "successCriteria": [
          "serviceRegistry has a registerService method that accepts name and service parameters and stores them."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure the registerService method properly stores the service in the registry object.",
        "feedbackWrong": "The serviceRegistry object should have a registerService method that stores services by name.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "registerService",
            "serviceRegistry",
            "function",
            "name",
            "service"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Test the complete microservices patterns implementation",
        "instruction": "Simulate a request through the API Gateway to test the complete microservices patterns implementation.",
        "hint": "Call the APIGateway function with a sample request object that includes a path property to route to a service.",
        "analogousExample": "// Simulating a request through an API gateway\nfunction testGateway() {\n  const request = { path: '/users', method: 'GET' };\n  const response = APIGateway(request);\n  return response;\n}",
        "seedCode": "// Create your function here\n\n// Create the APIGateway function below\nfunction APIGateway(request) {\n  // Add routing logic here\n}\n\n// Create the service registry object here\nconst serviceRegistry = {\n  users: UserService,\n  // Add registerService method here\n};\n\n// Create the CircuitBreaker function here\nfunction CircuitBreaker(service) {\n  return function(request) {\n    // Circuit breaker logic\n  };\n}\n\n// Create the EventBus function here\nfunction EventBus() {\n  // Event bus logic\n}\n\n// Create the DatabasePerService function here\nfunction DatabasePerService(serviceName) {\n  // Database per service logic\n}\n\n// Create the UserService function here\nfunction UserService(request) {\n  // User service logic\n}\n\n// Simulate a request through the API Gateway below",
        "expectedOutcome": "A test function that calls APIGateway with a sample request to verify the routing works.",
        "successCriteria": [
          "The test function calls APIGateway with a request object containing a path property."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure your test function calls APIGateway with a request object that has a path property.",
        "feedbackWrong": "The test function should call APIGateway with a request object to simulate routing.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "APIGateway",
            "request",
            "path"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Define the core microservices patterns we'll implement",
        "id": "step1"
      },
      {
        "label": "Set up the API Gateway pattern structure",
        "id": "step2"
      },
      {
        "label": "Initialize service registry for Service Discovery",
        "id": "step3"
      },
      {
        "label": "Define Circuit Breaker pattern logic",
        "id": "step4"
      },
      {
        "label": "Set up Event-Driven Communication infrastructure",
        "id": "step5"
      },
      {
        "label": "Implement Database per Service pattern",
        "id": "step6"
      },
      {
        "label": "Create sample microservices to demonstrate patterns",
        "id": "step7"
      },
      {
        "label": "Wire API Gateway to route requests to services",
        "id": "step8"
      },
      {
        "label": "Register services with Service Discovery",
        "id": "step9"
      },
      {
        "label": "Test the complete microservices patterns implementation",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}