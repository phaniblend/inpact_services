{
  "success": true,
  "config": {
    "lessonId": "ai-sd-13",
    "track": "sd",
    "lessonNum": 13,
    "title": "File storage systems",
    "shortName": "P13",
    "intro": {
      "tag": "LESSON #13",
      "title": "File storage systems",
      "body": "In this lesson, you will build a conceptual model of a basic file storage system to understand how files are organized, stored, and retrieved. You will learn the core concept of hierarchical directory structures, similar to folders on your computer, and how metadata like file names and sizes are managed. The main technologies involved are abstract data structures such as trees and key-value stores, which represent the system's organization and file attributes. By the end, you'll be able to design a simple system that can create directories, add files, and navigate through paths, giving you a foundational understanding of real-world storage solutions.",
      "usecase": "File storage systems are foundational to countless applications, from cloud storage services like Dropbox and Google Drive to content management systems like WordPress and e-commerce platforms that handle product images. Developers implement these systems when building photo galleries in social media apps, document repositories in enterprise software, or media libraries in streaming services. Understanding file storage patterns is essential for creating efficient upload interfaces, organizing user-generated content, and ensuring reliable retrieval across distributed systems."
    },
    "objectives": [
      "Implement a basic file storage system interface with core operations.",
      "Construct a hierarchical directory structure to organize files.",
      "Configure metadata storage for files, including size and timestamps.",
      "Apply access control mechanisms to restrict file operations.",
      "Integrate a simple file system with a simulated storage backend.",
      "Manipulate file paths to navigate and resolve locations within the system.",
      "Initialize a file system with a root directory and default permissions."
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 9",
        "title": "Import React and useState",
        "instruction": "Import React and useState from 'react' to enable component creation and state management.",
        "hint": "Use the import statement to bring in React and the useState hook.",
        "analogousExample": "// Example: importing React and a hook\nimport React, { useState } from 'react';",
        "seedCode": "// Import React and useState here",
        "expectedOutcome": "The React library and useState hook are imported, making them available for use in the component.",
        "successCriteria": [
          "React and useState are imported from 'react'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're importing both React and useState from 'react'.",
        "feedbackWrong": "That's not quite right. You need to import React and useState from 'react' to proceed.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "React",
            "useState",
            "'react'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 9",
        "title": "Create the main FileStorageSystem component",
        "instruction": "Create a functional component named FileStorageSystem that returns null initially.",
        "hint": "Define a component using either a function declaration or arrow function syntax. The component should return null for now.",
        "analogousExample": "// Example of a component returning null\nfunction ExampleComponent() {\n  return null;\n}",
        "seedCode": "import React, { useState } from 'react';\n\n// Create the FileStorageSystem component here",
        "expectedOutcome": "A FileStorageSystem component is defined and returns null.",
        "successCriteria": [
          "The component is named FileStorageSystem and returns null."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure the component is named exactly 'FileStorageSystem' and returns null.",
        "feedbackWrong": "The component isn't defined correctly. Check the name and that it returns null.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "FileStorageSystem",
            "return",
            "null"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 9",
        "title": "Initialize state for file list",
        "instruction": "Inside your FileStorageSystem component, use the useState hook to create a state variable named 'files' initialized as an empty array.",
        "hint": "Call useState with an empty array [] as the initial value, and destructure the returned array into the state variable and setter function.",
        "analogousExample": "// Similar: state for a list of items\nconst [items, setItems] = useState([]);",
        "seedCode": "import React, { useState } from 'react';\n\n// Create the FileStorageSystem component here\nconst FileStorageSystem = () => {\n  // Initialize files state here\n\n  return (\n    <div>\n      {/* File storage system UI will go here */}\n    </div>\n  );\n};\n\nexport default FileStorageSystem;",
        "expectedOutcome": "The component now has a 'files' state variable ready to store file objects, initialized as an empty array.",
        "successCriteria": [
          "The component contains a useState call that creates a 'files' state variable initialized to an empty array."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using useState with an empty array [] as the initial value and naming the state variable 'files'.",
        "feedbackWrong": "The state initialization isn't quite right. Use the useState hook to create a state variable named 'files' with an empty array as its initial value.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useState",
            "files",
            "[]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 9",
        "title": "Initialize state for selected file",
        "instruction": "Inside the FileStorageSystem component, use useState to create a state variable called selectedFile initialized to null.",
        "hint": "Add a new useState call after the existing files state. The initial value should be null.",
        "analogousExample": "// Similar: state for tracking a selected item\nconst [selectedItem, setSelectedItem] = useState(null);",
        "seedCode": "import React, { useState } from 'react';\n\n// Create the FileStorageSystem component here\nconst FileStorageSystem = () => {\n  // Initialize files state here\n  const [files, setFiles] = useState([]);\n  \n  // Add selectedFile state here\n\n  return (\n    <div>\n      {/* File storage system UI will go here */}\n    </div>\n  );\n};\n\nexport default FileStorageSystem;",
        "expectedOutcome": "The component will have a selectedFile state variable ready to track which file is currently selected.",
        "successCriteria": [
          "selectedFile state is declared using useState with an initial value of null."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using useState to create a selectedFile variable initialized to null.",
        "feedbackWrong": "add a new state variable called selectedFile with an initial value of null.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "selectedFile",
            "useState",
            "null"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 9",
        "title": "Define the removeFile handler function",
        "instruction": "Define a function named removeFile that takes a file name as a parameter and removes that file from the files state array.",
        "hint": "Use the setFiles function to update the state by filtering out the file with the matching name.",
        "analogousExample": "// Example: A function that removes an item from an array\nfunction removeItem(itemName) {\n  setItems(prevItems => prevItems.filter(item => item !== itemName));\n}",
        "seedCode": "import React, { useState } from 'react';\n\nconst FileStorageSystem = () => {\n  const [files, setFiles] = useState([]);\n  const [selectedFile, setSelectedFile] = useState(null);\n\n  // Define the addFile function here\n  \n  // Define the selectFile function here\n  \n  // Define the removeFile function here\n  \n  return (\n    <div>\n      {/* File storage system UI will go here */}\n    </div>\n  );\n};\n\nexport default FileStorageSystem;",
        "expectedOutcome": "You should have a removeFile function that filters the files state to exclude the specified file name.",
        "successCriteria": [
          "The removeFile function is defined, takes a file name parameter, and uses setFiles with a filter to remove that file from the state."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your removeFile function properly filters the files array using the file name parameter.",
        "feedbackWrong": "The removeFile function isn't defined correctly. It should take a file name and update the files state by filtering out that file.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "removeFile",
            "setFiles",
            "filter"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 9",
        "title": "Add the main container structure",
        "instruction": "Inside the return statement of your FileStorageSystem component, replace the existing div with a new div that has className 'file-storage-system' and contains two main sections.",
        "hint": "Create a parent div with the specified className and add two empty section elements inside it.",
        "analogousExample": "// Similar: a container with two sections\n<div className=\"app-container\">\n  <section></section>\n  <section></section>\n</div>",
        "seedCode": "import React, { useState } from 'react';\n\nconst FileStorageSystem = () => {\n  const [files, setFiles] = useState([]);\n  const [selectedFile, setSelectedFile] = useState(null);\n\n  // Define the addFile function here\n  \n  // Define the selectFile function here\n  \n  // Define the removeFile function here\n  \n  return (\n    <div>\n      {/* File storage system UI will go here */}\n    </div>\n  );\n};\n\nexport default FileStorageSystem;",
        "expectedOutcome": "The component now returns a structured container with two main sections ready for file storage UI elements.",
        "successCriteria": [
          "The return statement contains a div with className 'file-storage-system' that has two section elements inside it."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you have exactly two section elements inside the div with className 'file-storage-system'.",
        "feedbackWrong": "The structure doesn't match the requirements. Check that you have a div with className 'file-storage-system' containing two section elements.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "className=\"file-storage-system\"",
            "<section>",
            "</section>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 9",
        "title": "Create the file details display section",
        "instruction": "Inside the component's return statement, add a section element with className 'file-details' that displays information about the selected file. Use conditional rendering to only show this section when a file is selected.",
        "hint": "Add a <section> element with the className 'file-details'. Inside it, use a ternary operator to check if selectedFile exists, then display file details like name and size.",
        "analogousExample": "// Conditional rendering for selected item details\n<section className=\"item-details\">\n  {selectedItem ? (\n    <div>\n      <h3>{selectedItem.name}</h3>\n      <p>Size: {selectedItem.size} KB</p>\n    </div>\n  ) : (\n    <p>No item selected</p>\n  )}\n</section>",
        "seedCode": "import React, { useState } from 'react';\n\nconst FileStorageSystem = () => {\n  const [files, setFiles] = useState([]);\n  const [selectedFile, setSelectedFile] = useState(null);\n\n  // Define the addFile function here\n  \n  // Define the selectFile function here\n  \n  // Define the removeFile function here\n  \n  return (\n    <div>\n      {/* File storage system UI will go here */}\n      <section className=\"file-details\">\n        {/* Add conditional rendering for selected file details here */}\n      </section>\n    </div>\n  );\n};\n\nexport default FileStorageSystem;",
        "expectedOutcome": "A file details section appears in the UI that shows information about the selected file when one is selected, and displays a fallback message when no file is selected.",
        "successCriteria": [
          "The component contains a <section> element with className 'file-details' that uses conditional rendering to display selected file information or a fallback message."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added the section, but make sure to include conditional rendering that checks if selectedFile exists and displays appropriate content.",
        "feedbackWrong": "The file details section is missing or incomplete. Add a <section> with className 'file-details' that conditionally displays file information.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "section",
            "className=\"file-details\"",
            "selectedFile",
            "ternary",
            "conditional"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 9",
        "title": "Add file list items with selection wiring",
        "instruction": "Inside the file list map, create div elements for each file with onClick wired to selectFile.",
        "hint": "Map over the files array and return a div for each file. Use the file's id or name as a key and set onClick to call selectFile with that file.",
        "analogousExample": "// Example: mapping an array to interactive items\nconst items = ['item1', 'item2'];\nconst itemList = items.map(item => (\n  <div key={item} onClick={() => selectItem(item)}>\n    {item}\n  </div>\n));",
        "seedCode": "import React, { useState } from 'react';\n\nconst FileStorageSystem = () => {\n  const [files, setFiles] = useState([]);\n  const [selectedFile, setSelectedFile] = useState(null);\n\n  // Define the addFile function here\n  \n  // Define the selectFile function here\n  \n  // Define the removeFile function here\n  \n  return (\n    <div>\n      {/* File storage system UI will go here */}\n      <section className=\"file-details\">\n        {/* Add conditional rendering for selected file details here */}\n      </section>\n    </div>\n  );\n};\n\nexport default FileStorageSystem;",
        "expectedOutcome": "The file list will display each file as a clickable div that triggers the selectFile handler when clicked.",
        "successCriteria": [
          "A map function is used to iterate over the files array",
          "Each file is rendered as a div element",
          "Each div has an onClick handler that calls selectFile with the file object",
          "Each div has a unique key prop"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure each file div has an onClick handler that calls selectFile with the file object and includes a unique key prop.",
        "feedbackWrong": "The file list items aren't properly wired for selection. Remember to map over the files array and create div elements with onClick handlers that call selectFile.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "files.map",
            "div",
            "onClick",
            "selectFile",
            "key"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 9",
        "title": "Add file management controls",
        "instruction": "Add a 'Add Sample File' button and a 'Remove Selected File' button inside the main container, wiring their onClick handlers to the addFile and removeFile functions respectively.",
        "hint": "Place two button elements with appropriate labels and onClick handlers that call your existing handler functions.",
        "analogousExample": "// Example: adding interactive buttons\n<button onClick={handleAdd}>Add Item</button>\n<button onClick={handleRemove}>Remove Item</button>",
        "seedCode": "import React, { useState } from 'react';\n\nconst FileStorageSystem = () => {\n  const [files, setFiles] = useState([]);\n  const [selectedFile, setSelectedFile] = useState(null);\n\n  // Define the addFile function here\n  \n  // Define the selectFile function here\n  \n  // Define the removeFile function here\n  \n  return (\n    <div>\n      {/* Add file management buttons here */}\n      <section className=\"file-details\">\n        {/* Add conditional rendering for selected file details here */}\n      </section>\n    </div>\n  );\n};\n\nexport default FileStorageSystem;",
        "expectedOutcome": "Two buttons appear in the UI: one to add sample files and another to remove the selected file, both wired to their respective handler functions.",
        "successCriteria": [
          "The component contains an 'Add Sample File' button with onClick={addFile} and a 'Remove Selected File' button with onClick={removeFile}."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added some buttons, but check that both required buttons are present with correct onClick handlers.",
        "feedbackWrong": "The file management controls are missing or incorrectly implemented. Ensure you add both buttons with proper onClick wiring.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<button",
            "onClick={addFile}",
            "onClick={removeFile}",
            "Add Sample File",
            "Remove Selected File"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import React and useState",
        "id": "step1"
      },
      {
        "label": "Create the main FileStorageSystem component",
        "id": "step2"
      },
      {
        "label": "Initialize state for file list",
        "id": "step3"
      },
      {
        "label": "Initialize state for selected file",
        "id": "step4"
      },
      {
        "label": "Define the removeFile handler function",
        "id": "step5"
      },
      {
        "label": "Add the main container structure",
        "id": "step6"
      },
      {
        "label": "Create the file details display section",
        "id": "step7"
      },
      {
        "label": "Add file list items with selection wiring",
        "id": "step8"
      },
      {
        "label": "Add file management controls",
        "id": "step9"
      }
    ]
  },
  "source": "content"
}