{
  "success": true,
  "config": {
    "lessonId": "ai-vue-47",
    "track": "vue",
    "lessonNum": 47,
    "title": "useCallback for Stable References",
    "shortName": "P47",
    "intro": {
      "tag": "LESSON #47",
      "title": "useCallback for Stable References",
      "body": "In this lesson, you will build a simple Vue component that demonstrates how to use the `useCallback` hook to create stable function references. You will learn how `useCallback` helps prevent unnecessary re-renders by memoizing functions, ensuring they remain the same across renders unless their dependencies change. The core concepts covered include function memoization, dependency arrays, and optimizing performance in reactive components. You'll work with Vue's Composition API, specifically the `useCallback` hook, within a Vue single-file component using JavaScript.",
      "usecase": "In real Vue applications, stable callback references are essential for performance optimization in interactive components like data dashboards with dynamic filters. When users adjust date ranges, category selectors, or search inputs, these callbacks are passed to child components that should only re-render when their actual props change. Without useCallback, every parent re-render would create new function references, causing unnecessary child re-renders in components like sortable tables, chart controls, or complex form validators. This pattern is particularly valuable in admin panels where multiple filtering components need to maintain stable identities for Vue's reactivity system to work efficiently."
    },
    "objectives": [
      "Implement a computed property to derive state from reactive data",
      "Construct a method that utilizes reactive dependencies for dynamic behavior",
      "Configure a component to conditionally render elements based on reactive state",
      "Bind event handlers to DOM elements to trigger reactive updates",
      "Initialize reactive state using ref and computed for a functional component",
      "Manipulate reactive state through user interactions to demonstrate reactivity",
      "Integrate multiple reactive properties to manage component logic cohesively"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Import Vue and composition API functions",
        "instruction": "Import Vue, ref, and computed from 'vue'",
        "hint": "Use the import statement to bring in the necessary Vue functionality. You'll need these for creating reactive state and computed properties.",
        "analogousExample": "// Importing multiple items from a module\nimport { createApp, reactive } from 'vue'",
        "seedCode": "// Import Vue, ref, and computed from 'vue'",
        "expectedOutcome": "The import statement will be added to the file, making Vue and its composition API functions available for use.",
        "successCriteria": [
          "Vue, ref, and computed are imported from 'vue'"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you're importing all three items: Vue, ref, and computed from 'vue'.",
        "feedbackWrong": "That doesn't look right. You need to import Vue, ref, and computed from the 'vue' module using the import statement.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "vue",
            "ref",
            "computed",
            "'vue'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Create the main Vue component",
        "instruction": "Create a Vue component named 'StableReferencesDemo' with a setup function that returns an empty object.",
        "hint": "Define a component using Vue's composition API. The setup function should return an object (even if empty for now).",
        "analogousExample": "// Example: basic Vue component\nconst MyComponent = {\n  setup() {\n    return {};\n  }\n};",
        "seedCode": "// Import Vue, ref, and computed from 'vue'\n\nconst StableReferencesDemo = {\n  setup() {\n    return {};\n  }\n};",
        "expectedOutcome": "A Vue component named StableReferencesDemo is created with a setup function that returns an empty object.",
        "successCriteria": [
          "The component is named StableReferencesDemo, has a setup function, and returns an empty object."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you have: 1) Component named StableReferencesDemo, 2) A setup() function, 3) Returns an empty object {}.",
        "feedbackWrong": "The component structure isn't quite right. Check that you have a component named StableReferencesDemo with a setup function returning an empty object.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "StableReferencesDemo",
            "setup",
            "return",
            "{}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Initialize reactive state with ref",
        "instruction": "Inside the setup function, declare a reactive state variable called 'count' using ref and initialize it to 0.",
        "hint": "Use the ref function imported earlier to create a reactive reference with an initial value of 0.",
        "analogousExample": "// Example: creating reactive state\nconst message = ref('Hello');",
        "seedCode": "// Import Vue, ref, and computed from 'vue'\n\nconst StableReferencesDemo = {\n  setup() {\n    // Initialize count state here\n    return {};\n  }\n};",
        "expectedOutcome": "The component will have a reactive count state variable initialized to 0, ready to be used in the UI and handlers.",
        "successCriteria": [
          "A count variable is declared using ref with an initial value of 0 inside the setup function."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're using ref to create the reactive state and initializing it to 0.",
        "feedbackWrong": "The count state wasn't properly initialized. Remember to use ref(0) to create a reactive reference with initial value 0.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "count",
            "ref",
            "0"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Create a regular function without useCallback",
        "instruction": "Inside the setup function, create a regular function named increment that increases the count state by 1.",
        "hint": "Define a function that updates the count value using count.value += 1.",
        "analogousExample": "// Regular function example\nfunction addOne() {\n  number.value += 1;\n}",
        "seedCode": "// Import Vue, ref, and computed from 'vue'\n\nconst StableReferencesDemo = {\n  setup() {\n    // Initialize count state here\n    const count = ref(0);\n    \n    // Create regular increment function here\n    \n    return {};\n  }\n};",
        "expectedOutcome": "A regular increment function is defined inside the setup function that modifies the count state.",
        "successCriteria": [
          "The increment function is defined without useCallback and updates count.value by 1."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've defined a function, but make sure it updates count.value by 1 and is placed inside the setup function.",
        "feedbackWrong": "The increment function should be defined inside setup and update count.value by 1. Try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "increment",
            "function",
            "count.value",
            "+=",
            "1"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Add a button that uses the regular function",
        "instruction": "In the setup function's return object, add a button element with the text 'Increment (Regular)' and wire its click event to call the regular increment function.",
        "hint": "Use @click to bind the click event to your increment function. The button should be inside the return object.",
        "analogousExample": "// Adding a button that calls a function\n<button @click=\"handleClick\">Click me</button>",
        "seedCode": "// Import Vue, ref, and computed from 'vue'\n\nconst StableReferencesDemo = {\n  setup() {\n    // Initialize count state here\n    const count = ref(0);\n    \n    // Create regular increment function here\n    const incrementRegular = () => {\n      count.value++;\n    };\n    \n    return {\n      // Add button here\n    };\n  }\n};",
        "expectedOutcome": "A button labeled 'Increment (Regular)' appears in the UI, which when clicked will call the regular increment function.",
        "successCriteria": [
          "The return object contains a button element with @click bound to incrementRegular and the text 'Increment (Regular)'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure the button has both the correct text and the @click binding to incrementRegular.",
        "feedbackWrong": "The button is missing or not properly connected to the increment function. Check the button's text and click event binding.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "button",
            "@click",
            "incrementRegular",
            "Increment (Regular)"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Add a second button that also uses the regular function",
        "instruction": "In the return object of your setup function, add a second button that also calls the incrementRegular function when clicked.",
        "hint": "Add another button element with an @click handler that calls incrementRegular.",
        "analogousExample": "// Adding another button with the same handler\n<button @click=\"incrementRegular\">Increment Again</button>",
        "seedCode": "// Import Vue, ref, and computed from 'vue'\n\nconst StableReferencesDemo = {\n  setup() {\n    // Initialize count state here\n    const count = ref(0);\n    \n    // Create regular increment function here\n    const incrementRegular = () => {\n      count.value++;\n    };\n    \n    return {\n      // Add button here\n    };\n  }\n};",
        "expectedOutcome": "The component will have two buttons that both increment the count using the same regular function.",
        "successCriteria": [
          "A second button is added to the return object with an @click handler that calls incrementRegular."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You added a second button, but make sure it calls incrementRegular when clicked.",
        "feedbackWrong": "The second button is missing or doesn't call incrementRegular correctly.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "button",
            "@click",
            "incrementRegular"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Import computed for derived state",
        "instruction": "Add computed to the existing import statement from 'vue'.",
        "hint": "Update the import line to include computed alongside ref.",
        "analogousExample": "// Importing multiple Vue APIs\nimport { ref, computed } from 'vue';",
        "seedCode": "// Import Vue, ref, and computed from 'vue'\nimport { ref } from 'vue';\n\nconst StableReferencesDemo = {\n  setup() {\n    // Initialize count state here\n    const count = ref(0);\n    \n    // Create regular increment function here\n    const incrementRegular = () => {\n      count.value++;\n    };\n    \n    return {\n      // Add button here\n    };\n  }\n};",
        "expectedOutcome": "The import statement now includes computed, making it available for creating derived reactive state.",
        "successCriteria": [
          "computed is imported from 'vue' in the same statement as ref."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure computed is included in the import statement from 'vue'.",
        "feedbackWrong": "That's not quite right. You need to add computed to the existing import statement from 'vue'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "{",
            "ref",
            "computed",
            "}",
            "from",
            "'vue'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Create a computed property",
        "instruction": "Inside the setup function, create a computed property named 'countParity' that returns a string indicating whether count is 'even' or 'odd'.",
        "hint": "Use the computed function imported from Vue to create a derived reactive value. The computed function takes a getter function that returns the derived value based on count.value.",
        "analogousExample": "// Similar: computed property for derived state\nconst doubleCount = computed(() => count.value * 2);",
        "seedCode": "// Import Vue, ref, and computed from 'vue'\nimport { ref, computed } from 'vue';\n\nconst StableReferencesDemo = {\n  setup() {\n    // Initialize count state here\n    const count = ref(0);\n    \n    // Create regular increment function here\n    const incrementRegular = () => {\n      count.value++;\n    };\n    \n    // Create computed property here\n    \n    return {\n      // Add button here\n    };\n  }\n};",
        "expectedOutcome": "A computed property 'countParity' is created that returns 'even' when count is even and 'odd' when count is odd.",
        "successCriteria": [
          "The computed property 'countParity' is defined inside setup and returns a string based on count.value % 2."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure countParity returns 'even' or 'odd' based on count.value % 2.",
        "feedbackWrong": "The computed property isn't correctly defined. Use computed(() => count.value % 2 === 0 ? 'even' : 'odd').",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "countParity",
            "computed",
            "count.value",
            "%",
            "even",
            "odd"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "Display the computed property in the template",
        "instruction": "In the return statement of your setup function, add a paragraph element that displays the computed property value.",
        "hint": "You can access the computed property value directly in the template. Use double curly braces to display it.",
        "analogousExample": "// Displaying a computed value\n<p>Computed value: {{ computedValue }}</p>",
        "seedCode": "// Import Vue, ref, and computed from 'vue'\nimport { ref, computed } from 'vue';\n\nconst StableReferencesDemo = {\n  setup() {\n    // Initialize count state here\n    const count = ref(0);\n    \n    // Create regular increment function here\n    const incrementRegular = () => {\n      count.value++;\n    };\n    \n    // Create computed property here\n    const doubledCount = computed(() => count.value * 2);\n    \n    return {\n      // Add button here\n    };\n  }\n};",
        "expectedOutcome": "The computed property value will be displayed in the UI, automatically updating when the count changes.",
        "successCriteria": [
          "A paragraph element exists in the return statement",
          "The paragraph displays the computed property value using template syntax"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you've added a paragraph element in the return statement that displays the computed property value.",
        "feedbackWrong": "The computed property value isn't being displayed in the template. Add a paragraph element that shows the computed value.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<p>",
            "{{",
            "doubledCount",
            "}}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Add a reset button with a new function",
        "instruction": "Inside the setup function, create a new function named resetCount that sets the count value back to 0.",
        "hint": "Define a function that updates count.value to 0.",
        "analogousExample": "// Similar: a function that resets a value\nfunction resetValue() {\n  value.value = 0;\n}",
        "seedCode": "// Import Vue, ref, and computed from 'vue'\nimport { ref, computed } from 'vue';\n\nconst StableReferencesDemo = {\n  setup() {\n    // Initialize count state here\n    const count = ref(0);\n    \n    // Create regular increment function here\n    const incrementRegular = () => {\n      count.value++;\n    };\n    \n    // Create computed property here\n    const doubledCount = computed(() => count.value * 2);\n    \n    // Create reset function here\n    \n    return {\n      // Add button here\n    };\n  }\n};",
        "expectedOutcome": "A resetCount function is defined that can reset the count state to 0.",
        "successCriteria": [
          "The resetCount function is defined and sets count.value to 0."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've defined a function, but make sure it sets count.value to 0 and is named resetCount.",
        "feedbackWrong": "The resetCount function isn't defined correctly. Create a function that sets count.value to 0.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "resetCount",
            "count.value",
            "0"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import Vue and composition API functions",
        "id": "step1"
      },
      {
        "label": "Create the main Vue component",
        "id": "step2"
      },
      {
        "label": "Initialize reactive state with ref",
        "id": "step3"
      },
      {
        "label": "Create a regular function without useCallback",
        "id": "step4"
      },
      {
        "label": "Add a button that uses the regular function",
        "id": "step5"
      },
      {
        "label": "Add a second button that also uses the regular function",
        "id": "step6"
      },
      {
        "label": "Import computed for derived state",
        "id": "step7"
      },
      {
        "label": "Create a computed property",
        "id": "step8"
      },
      {
        "label": "Display the computed property in the template",
        "id": "step9"
      },
      {
        "label": "Add a reset button with a new function",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}