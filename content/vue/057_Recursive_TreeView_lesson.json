{
  "success": true,
  "config": {
    "lessonId": "ai-vue-57",
    "track": "vue",
    "lessonNum": 57,
    "title": "Recursive TreeView",
    "shortName": "P57",
    "intro": {
      "tag": "LESSON #57",
      "title": "Recursive TreeView",
      "body": "In this lesson, you'll build a TreeView component that displays hierarchical data, like a file system or organizational chart, using Vue's reactive system. You'll learn how to implement recursion—a function that calls itself—to render nested tree structures of any depth. The lesson will cover Vue components, props, and conditional rendering with v-if to handle parent-child relationships. By the end, you'll understand how to create flexible, reusable components that can display complex nested data efficiently.",
      "usecase": "Recursive TreeView components are essential in modern applications where hierarchical data needs to be displayed and navigated. You'll find them in file explorers like Windows File Explorer or macOS Finder, where folders contain subfolders in nested structures. They're also used in document outlines in tools like Notion or Google Docs, organizational charts in HR software, and category filters in e-commerce platforms like Amazon. As a Vue developer, you'll implement these patterns to create intuitive navigation systems that users can expand and collapse to explore complex data relationships efficiently."
    },
    "objectives": [
      "Construct a recursive Vue component that renders nested tree data",
      "Initialize and bind component props to control tree node rendering",
      "Implement conditional rendering logic to display child nodes recursively",
      "Configure event handlers to manipulate tree node expansion and collapse",
      "Apply computed properties to manage and transform tree data structures",
      "Integrate recursive components within parent components to build complete tree views",
      "Update component state to reflect changes in tree structure dynamically"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Import Vue and defineComponent",
        "instruction": "Import Vue and defineComponent from the 'vue' package.",
        "hint": "Use the import statement to bring in Vue and defineComponent from the 'vue' module.",
        "analogousExample": "// Example: importing from a module\nimport { ref, computed } from 'vue'",
        "seedCode": "// Import Vue and defineComponent here",
        "expectedOutcome": "The Vue and defineComponent APIs are imported and ready to use.",
        "successCriteria": [
          "Vue and defineComponent are imported from 'vue'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're importing both Vue and defineComponent from the 'vue' package.",
        "feedbackWrong": "That's not quite right. You need to import Vue and defineComponent from 'vue' to proceed.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "Vue",
            "defineComponent",
            "'vue'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Create the TreeView component",
        "instruction": "Create a default export component named TreeView using defineComponent.",
        "hint": "Use defineComponent to define a Vue component with a name property set to 'TreeView'.",
        "analogousExample": "// Example: defining a Vue component\nexport default defineComponent({\n  name: 'MyComponent'\n})",
        "seedCode": "import { defineComponent } from 'vue';\n\n// Create a default export component named TreeView using defineComponent here",
        "expectedOutcome": "A TreeView component is defined and ready to be built upon.",
        "successCriteria": [
          "TreeView component is created as a default export using defineComponent with name property."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using defineComponent and exporting it as default with the name 'TreeView'.",
        "feedbackWrong": "The component wasn't created correctly. Check that you're using defineComponent and exporting it as default.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "TreeView",
            "defineComponent",
            "export default",
            "name"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Define props for tree data",
        "instruction": "Inside the TreeView component, add a props object with a property named 'nodes' that accepts an array.",
        "hint": "In Vue's defineComponent, you can define props as an object where each key is a prop name and the value is the expected type (like Array).",
        "analogousExample": "// Example: props definition in a Vue component\nexport default defineComponent({\n  props: {\n    items: Array\n  }\n});",
        "seedCode": "import { defineComponent } from 'vue';\n\n// Create a default export component named TreeView using defineComponent here\nexport default defineComponent({\n  // Add props object with a 'nodes' property here\n});",
        "expectedOutcome": "The TreeView component will be ready to receive tree data from a parent component through the 'nodes' prop.",
        "successCriteria": [
          "The component has a props object with a 'nodes' property of type Array."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you've added a props object with a 'nodes' property that accepts an array.",
        "feedbackWrong": "The props definition is missing or incorrect. The component needs a props object with a 'nodes' property of type Array.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "props:",
            "nodes:",
            "Array"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Initialize local state for expanded nodes",
        "instruction": "Inside the component's setup function, create a reactive ref named 'expanded' initialized as an empty Set to store expanded node IDs.",
        "hint": "Use Vue's ref function to create reactive state. Initialize it with new Set() to store unique IDs.",
        "analogousExample": "// Similar: reactive state for tracking items\nimport { ref } from 'vue';\nconst selectedItems = ref(new Set());",
        "seedCode": "import { defineComponent } from 'vue';\n\n// Create a default export component named TreeView using defineComponent here\nexport default defineComponent({\n  // Add props object with a 'nodes' property here\n  setup() {\n    // Create a ref named 'expanded' initialized as an empty Set here\n  }\n});",
        "expectedOutcome": "The component will have reactive state to track which tree nodes are expanded.",
        "successCriteria": [
          "A ref named 'expanded' is created inside setup() and initialized with new Set()."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're creating a ref named 'expanded' and initializing it with new Set().",
        "feedbackWrong": "The state wasn't set up correctly. You need to create a ref named 'expanded' inside the setup function.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "expanded",
            "ref",
            "Set",
            "setup"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Create toggle handler function",
        "instruction": "Inside the setup function, create a function named toggleNode that takes a nodeId parameter. The function should toggle the presence of nodeId in the expanded Set.",
        "hint": "Use the expanded.value Set's methods: .has() to check if a nodeId exists, .add() to include it, and .delete() to remove it.",
        "analogousExample": "// Similar: a function that toggles a value in a Set\nfunction toggleItem(item) {\n  if (this.items.has(item)) {\n    this.items.delete(item);\n  } else {\n    this.items.add(item);\n  }\n}",
        "seedCode": "import { defineComponent } from 'vue';\n\nexport default defineComponent({\n  setup() {\n    // Create a function named toggleNode here\n  }\n});",
        "expectedOutcome": "A toggleNode function is defined that can add or remove node IDs from the expanded Set.",
        "successCriteria": [
          "The toggleNode function is defined with a nodeId parameter and toggles the nodeId in the expanded Set."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created a function, but make sure it properly toggles the nodeId in the expanded Set.",
        "feedbackWrong": "The toggleNode function is missing or doesn't toggle the expanded Set correctly.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "toggleNode",
            "nodeId",
            "expanded",
            "has",
            "add",
            "delete"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Add template structure with v-if",
        "instruction": "Add a template section to your component that uses v-if to check if nodes exist before rendering.",
        "hint": "Remember to add a <template> tag and use the v-if directive with a condition that checks if nodes are present.",
        "analogousExample": "// Example: conditional template structure\n<template>\n  <div v-if=\"items.length > 0\">\n    <!-- content here -->\n  </div>\n</template>",
        "seedCode": "import { defineComponent } from 'vue';\n\nexport default defineComponent({\n  setup() {\n    // Create a function named toggleNode here\n  },\n  template: `\n    <div>\n      <!-- Add template with v-if here -->\n    </div>\n  `\n});",
        "expectedOutcome": "The component will have a template section with conditional rendering logic using v-if.",
        "successCriteria": [
          "A template section is added with v-if directive that checks for nodes existence."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you've included the v-if directive properly in the template.",
        "feedbackWrong": "The template structure with v-if is missing or incorrect. Try adding a <template> section with v-if to check if nodes exist.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "template",
            "v-if",
            "nodes"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Render tree nodes with v-for",
        "instruction": "Inside the template's div, add a ul element with v-for to iterate through the nodes prop and render each node.",
        "hint": "Use v-for=\"node in nodes\" on the ul element to loop through the nodes array passed as a prop.",
        "analogousExample": "// Similar: v-for to render list items\n<ul>\n  <li v-for=\"item in items\" :key=\"item.id\">{{ item.name }}</li>\n</ul>",
        "seedCode": "import { defineComponent } from 'vue';\n\nexport default defineComponent({\n  setup() {\n    // Create a function named toggleNode here\n  },\n  template: `\n    <div>\n      <!-- Add template with v-if here -->\n    </div>\n  `\n});",
        "expectedOutcome": "The template now includes a ul element that will render each node from the nodes prop when the component is used with tree data.",
        "successCriteria": [
          "A ul element with v-for=\"node in nodes\" is added inside the div in the template."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you've added a ul element with v-for=\"node in nodes\" inside the div.",
        "feedbackWrong": "The template should contain a ul element with v-for=\"node in nodes\" to iterate through the nodes prop.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "ul",
            "v-for=\"node in nodes\""
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Add node content and toggle button",
        "instruction": "Inside the v-for loop, add an li element that displays the node's label and a button to toggle expansion.",
        "hint": "Use the node parameter from the v-for loop to access the label property. The button should call the toggleNode handler with the node's id.",
        "analogousExample": "// Example: displaying item content with interactive button\n<li>\n  <span>{{ item.name }}</span>\n  <button @click=\"toggleItem(item.id)\">Toggle</button>\n</li>",
        "seedCode": "import { defineComponent } from 'vue';\n\nexport default defineComponent({\n  setup() {\n    // Create a function named toggleNode here\n  },\n  template: `\n    <div>\n      <!-- Add template with v-if here -->\n    </div>\n  `\n});",
        "expectedOutcome": "The component template should include li elements that show node labels and toggle buttons for each node.",
        "successCriteria": [
          "Each tree node displays its label text and has a button that calls toggleNode with the node's id."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure each li contains both the node label display and a button that calls toggleNode with the node id.",
        "feedbackWrong": "The template doesn't yet show node labels with toggle buttons. Add li elements inside the v-for loop to display each node's label and a toggle button.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "li",
            "{{",
            "label",
            "button",
            "@click",
            "toggleNode"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "Wire toggle button to handler",
        "instruction": "Add an @click event handler to the toggle button that calls the toggleNode function with the current node's id.",
        "hint": "Use @click=\"toggleNode(node.id)\" on the button element.",
        "analogousExample": "// Adding click handler to a button\n<button @click=\"handleClick(item.id)\">Toggle</button>",
        "seedCode": "import { defineComponent } from 'vue';\n\nexport default defineComponent({\n  setup() {\n    // Create a function named toggleNode here\n  },\n  template: `\n    <div>\n      <!-- Add template with v-if here -->\n    </div>\n  `\n});",
        "expectedOutcome": "The toggle button should have an @click event that triggers the toggleNode function with the node's id parameter.",
        "successCriteria": [
          "The button element includes @click=\"toggleNode(node.id)\" in the template."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You added an @click handler, but make sure it calls toggleNode with node.id as the argument.",
        "feedbackWrong": "The toggle button needs an @click event handler that calls toggleNode with the node's id.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "@click",
            "toggleNode",
            "node.id"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Add recursive TreeView for child nodes",
        "instruction": "Inside the existing v-if block for when the node has children and is expanded, add a recursive TreeView component that renders the node's children.",
        "hint": "Use the TreeView component itself as a child element, passing the child nodes as props.",
        "analogousExample": "// Recursive component example\nconst RecursiveList = defineComponent({\n  props: ['items'],\n  template: `\n    <ul>\n      <li v-for=\"item in items\" :key=\"item.id\">\n        {{ item.name }}\n        <RecursiveList v-if=\"item.children\" :items=\"item.children\" />\n      </li>\n    </ul>\n  `\n});",
        "seedCode": "import { defineComponent } from 'vue';\n\nexport default defineComponent({\n  name: 'TreeView',\n  props: {\n    nodes: {\n      type: Array,\n      required: true\n    }\n  },\n  setup() {\n    const expandedNodes = ref([]);\n    \n    const toggleNode = (nodeId) => {\n      const index = expandedNodes.value.indexOf(nodeId);\n      if (index > -1) {\n        expandedNodes.value.splice(index, 1);\n      } else {\n        expandedNodes.value.push(nodeId);\n      }\n    };\n    \n    return { expandedNodes, toggleNode };\n  },\n  template: `\n    <div>\n      <ul>\n        <li v-for=\"node in nodes\" :key=\"node.id\">\n          <div>\n            <button @click=\"toggleNode(node.id)\">\n              {{ expandedNodes.includes(node.id) ? '−' : '+' }}\n            </button>\n            {{ node.name }}\n          </div>\n          <div v-if=\"node.children && expandedNodes.includes(node.id)\">\n            <!-- Add recursive TreeView here -->\n          </div>\n        </li>\n      </ul>\n    </div>\n  `\n});",
        "expectedOutcome": "The TreeView component will now recursively render child nodes when a parent node is expanded.",
        "successCriteria": [
          "TreeView component is used recursively inside the v-if block with proper props binding."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you're using the TreeView component inside the v-if block and passing the child nodes as props.",
        "feedbackWrong": "The recursive TreeView wasn't properly implemented. Check that you're using the TreeView component with the correct props binding.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "TreeView",
            ":nodes=\"node.children\"",
            "v-if"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import Vue and defineComponent",
        "id": "step1"
      },
      {
        "label": "Create the TreeView component",
        "id": "step2"
      },
      {
        "label": "Define props for tree data",
        "id": "step3"
      },
      {
        "label": "Initialize local state for expanded nodes",
        "id": "step4"
      },
      {
        "label": "Create toggle handler function",
        "id": "step5"
      },
      {
        "label": "Add template structure with v-if",
        "id": "step6"
      },
      {
        "label": "Render tree nodes with v-for",
        "id": "step7"
      },
      {
        "label": "Add node content and toggle button",
        "id": "step8"
      },
      {
        "label": "Wire toggle button to handler",
        "id": "step9"
      },
      {
        "label": "Add recursive TreeView for child nodes",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}