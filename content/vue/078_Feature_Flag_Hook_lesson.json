{
  "success": true,
  "config": {
    "lessonId": "ai-vue-78",
    "track": "vue",
    "lessonNum": 78,
    "title": "Feature Flag Hook",
    "shortName": "P78",
    "intro": {
      "tag": "LESSON #78",
      "title": "Feature Flag Hook",
      "body": "In this lesson, you will build a custom Vue composable (hook) called `useFeatureFlag` that manages a simple feature flag system. You will learn how to create a reactive state using Vue's `ref` and `computed` to control whether a feature is enabled or disabled. The main technologies involved are Vue 3's Composition API and reactive JavaScript. By the end, you will understand how to encapsulate logic in a reusable hook and apply it to conditionally render UI components based on the flag's value.",
      "usecase": "Feature flags are used in real-world applications to control feature visibility without redeploying code. For example, e-commerce platforms use them to gradually roll out new checkout flows, while SaaS dashboards toggle experimental analytics panels for specific user segments. Developers implement feature flag hooks to conditionally render UI components like modals, forms, or entire sections, enabling A/B testing and controlled releases. This pattern allows teams to manage features dynamically, reducing risk and improving user experience."
    },
    "objectives": [
      "Implement a custom Vue composable function for feature flag management",
      "Initialize reactive state to store feature flag configurations",
      "Configure feature flags with boolean values for conditional rendering",
      "Apply the feature flag hook within Vue components to control UI visibility",
      "Manipulate feature flag values to toggle features dynamically",
      "Integrate the feature flag hook with component lifecycle for persistent state",
      "Bind feature flag conditions to template rendering logic"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Import Vue and ref from vue",
        "instruction": "Import Vue and the ref function from the vue package.",
        "hint": "Use the import statement to bring in Vue and ref from 'vue'.",
        "analogousExample": "// Importing from a package\nimport { ref } from 'vue'",
        "seedCode": "// Import Vue and ref from 'vue'",
        "expectedOutcome": "The necessary Vue APIs for reactive state management are imported.",
        "successCriteria": [
          "Vue and ref are imported from 'vue'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're importing both Vue and ref from 'vue'.",
        "feedbackWrong": "import Vue and ref from the 'vue' package.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "Vue",
            "ref",
            "'vue'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Create the useFeatureFlag hook function",
        "instruction": "Create a function called useFeatureFlag that returns an empty object.",
        "hint": "Define a function with the name useFeatureFlag and have it return {}.",
        "analogousExample": "// Example of a custom hook function\nfunction useCustomHook() {\n  return {};\n}",
        "seedCode": "// Import Vue and ref from 'vue'\n\n// Create the useFeatureFlag hook function",
        "expectedOutcome": "A useFeatureFlag function is defined and returns an empty object.",
        "successCriteria": [
          "The useFeatureFlag function exists and returns an object."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure the function is named exactly useFeatureFlag and returns an object (even if empty).",
        "feedbackWrong": "The function isn't defined correctly. Create a function named useFeatureFlag that returns {}.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useFeatureFlag",
            "return",
            "{}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Add feature flag state using ref",
        "instruction": "Inside the useFeatureFlag hook function, create a reactive state variable called isEnabled using ref, initialized to false.",
        "hint": "Use the ref function you imported earlier to create reactive state. The syntax is: const isEnabled = ref(false);",
        "analogousExample": "// Creating reactive state with ref\nconst count = ref(0);",
        "seedCode": "// Import Vue and ref from 'vue'\nimport { ref } from 'vue';\n\n// Create the useFeatureFlag hook function\nfunction useFeatureFlag() {\n  // Create isEnabled reactive state here\n}",
        "expectedOutcome": "The useFeatureFlag hook will have a reactive isEnabled state variable initialized to false.",
        "successCriteria": [
          "The useFeatureFlag hook contains a const isEnabled = ref(false); declaration."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you're using ref() to create the reactive state and initializing it to false.",
        "feedbackWrong": "The isEnabled reactive state wasn't created correctly. Remember to use ref(false) inside the hook function.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "isEnabled",
            "ref",
            "false"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Add toggle function to the hook",
        "instruction": "Inside the useFeatureFlag function, define a function named toggle that toggles the isEnabled value.",
        "hint": "You can create a function that updates the isEnabled ref value to its opposite boolean state.",
        "analogousExample": "// Example: a toggle function in another context\nfunction useToggle() {\n  const isOn = ref(false);\n  function toggle() {\n    isOn.value = !isOn.value;\n  }\n  return { isOn, toggle };\n}",
        "seedCode": "// Import Vue and ref from 'vue'\nimport { ref } from 'vue';\n\n// Create the useFeatureFlag hook function\nfunction useFeatureFlag() {\n  // Create isEnabled reactive state here\n  // Define toggle function here\n}",
        "expectedOutcome": "The useFeatureFlag function now contains a toggle function that will switch the isEnabled state between true and false.",
        "successCriteria": [
          "A toggle function is defined inside useFeatureFlag that toggles the isEnabled value."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your toggle function properly toggles the isEnabled value.",
        "feedbackWrong": "The toggle function is missing or doesn't toggle the isEnabled value correctly.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "function toggle",
            "isEnabled.value",
            "!"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Return state and toggle from the hook",
        "instruction": "Inside the useFeatureFlag function, return an object containing the isEnabled state and toggle function.",
        "hint": "Make sure to return an object with both properties so components can use them.",
        "analogousExample": "// Returning values from a custom hook\nfunction useCounter() {\n  const count = ref(0);\n  const increment = () => { count.value++ };\n  return { count, increment };\n}",
        "seedCode": "// Import Vue and ref from 'vue'\nimport { ref } from 'vue';\n\n// Create the useFeatureFlag hook function\nfunction useFeatureFlag() {\n  // Create isEnabled reactive state here\n  // Define toggle function here\n  // Return object with isEnabled and toggle\n}",
        "expectedOutcome": "The hook now exposes its state and toggle function for components to use.",
        "successCriteria": [
          "The useFeatureFlag function returns an object containing isEnabled and toggle."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're returning an object with both isEnabled and toggle properties.",
        "feedbackWrong": "The hook should return an object containing both the isEnabled state and toggle function.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "return",
            "{",
            "isEnabled",
            "toggle",
            "}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Create the main Vue component",
        "instruction": "Create a Vue component named 'FeatureFlagDemo' with a setup function that returns an empty template.",
        "hint": "Define a component function that returns an object from its setup function. The template can be empty for now.",
        "analogousExample": "// Example: a basic Vue component\nfunction MyComponent() {\n  const setup = () => {\n    return {};\n  };\n  return {\n    setup\n  };\n}",
        "seedCode": "// Import Vue and ref from 'vue'\nimport { ref } from 'vue';\n\n// Create the useFeatureFlag hook function\nfunction useFeatureFlag() {\n  // Create isEnabled reactive state here\n  // Define toggle function here\n  // Return object with isEnabled and toggle\n}\n\n// Create the main Vue component\n",
        "expectedOutcome": "A component named 'FeatureFlagDemo' is defined with a setup function that returns an empty object.",
        "successCriteria": [
          "The component 'FeatureFlagDemo' is defined as a function with a setup function that returns an object."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you define a function named 'FeatureFlagDemo' with a setup function that returns an object.",
        "feedbackWrong": "The component isn't defined correctly. Create a function named 'FeatureFlagDemo' with a setup function that returns an object.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "FeatureFlagDemo",
            "setup",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Use the feature flag hook in the component",
        "instruction": "Inside your component's setup function, call the useFeatureFlag hook and destructure the returned isEnabled and toggle values.",
        "hint": "Call the hook and destructure the object it returns: const { isEnabled, toggle } = useFeatureFlag();",
        "analogousExample": "// Example: using a custom hook in Vue setup\nfunction useCounter() {\n  const count = ref(0);\n  const increment = () => count.value++;\n  return { count, increment };\n}\n\n// In component setup:\nconst { count, increment } = useCounter();",
        "seedCode": "// Import Vue and ref from 'vue'\nimport { ref } from 'vue';\n\n// Create the useFeatureFlag hook function\nfunction useFeatureFlag() {\n  // Create isEnabled reactive state here\n  // Define toggle function here\n  // Return object with isEnabled and toggle\n}\n\n// Create the main Vue component\n\n// Inside your component's setup function:\n// Call useFeatureFlag and destructure isEnabled and toggle",
        "expectedOutcome": "The component's setup function now has access to the feature flag state and toggle function from the custom hook.",
        "successCriteria": [
          "The useFeatureFlag hook is called inside the component setup, and isEnabled and toggle are destructured from its return value."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're calling useFeatureFlag() and destructuring both isEnabled and toggle from the returned object.",
        "feedbackWrong": "The hook isn't being used correctly. You need to call useFeatureFlag() inside the component setup and destructure the returned values.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useFeatureFlag()",
            "isEnabled",
            "toggle",
            "const",
            "=",
            "{",
            "}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Return the reactive state and toggle from setup",
        "instruction": "In your component's setup function, return an object containing isEnabled and toggle to make them available in the template.",
        "hint": "The setup function should return an object with the properties you want to expose to the template.",
        "analogousExample": "// Returning values from a setup function\nfunction setup() {\n  const count = ref(0);\n  const increment = () => count.value++;\n  return { count, increment };\n}",
        "seedCode": "// Import Vue and ref from 'vue'\nimport { ref } from 'vue';\n\n// Create the useFeatureFlag hook function\nfunction useFeatureFlag() {\n  // Create isEnabled reactive state here\n  // Define toggle function here\n  // Return object with isEnabled and toggle\n}\n\n// Create the main Vue component\n\n// Inside your component's setup function:\n// Call useFeatureFlag and destructure isEnabled and toggle\n// Return an object containing isEnabled and toggle",
        "expectedOutcome": "The setup function returns an object with isEnabled and toggle, making them accessible in the template.",
        "successCriteria": [
          "The setup function returns an object containing both isEnabled and toggle."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're returning an object with both isEnabled and toggle properties.",
        "feedbackWrong": "The setup function needs to return an object containing both isEnabled and toggle.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "return",
            "isEnabled",
            "toggle"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "Add template structure with feature flag status",
        "instruction": "In your Vue component's template section, add a paragraph element that displays the current feature flag status using the isEnabled reactive value.",
        "hint": "Use double curly braces {{ }} to display reactive values in the template. The paragraph should show something like 'Feature flag is: enabled' or 'Feature flag is: disabled' based on the isEnabled value.",
        "analogousExample": "// Displaying reactive state in template\n<template>\n  <p>Current value: {{ count }}</p>\n</template>",
        "seedCode": "// Import Vue and ref from 'vue'\nimport { ref } from 'vue';\n\n// Create the useFeatureFlag hook function\nfunction useFeatureFlag() {\n  // Create isEnabled reactive state here\n  // Define toggle function here\n  // Return object with isEnabled and toggle\n}\n\n// Create the main Vue component\n\n// Inside your component's setup function:\n// Call useFeatureFlag and destructure isEnabled and toggle\n// Return an object containing isEnabled and toggle\n\n<template>\n  <!-- Add your feature flag status paragraph here -->\n</template>",
        "expectedOutcome": "The template will contain a paragraph that dynamically displays whether the feature flag is enabled or disabled based on the reactive isEnabled value.",
        "successCriteria": [
          "A paragraph element exists in the template that displays the isEnabled value using Vue's template interpolation syntax {{ }}."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're using the correct syntax to display the isEnabled value in the template. Remember to use double curly braces {{ }} around the reactive value.",
        "feedbackWrong": "The template doesn't yet show the feature flag status. Add a paragraph element that displays the isEnabled value using Vue's template interpolation syntax.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<p>",
            "{{",
            "isEnabled",
            "}}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Add toggle button and wire click event",
        "instruction": "Add a button element inside your template that calls the toggle function when clicked.",
        "hint": "Use the @click directive to bind the toggle function to the button's click event.",
        "analogousExample": "// Example: button with click handler\n<button @click=\"handleClick\">Toggle</button>",
        "seedCode": "// Import Vue and ref from 'vue'\nimport { ref } from 'vue';\n\n// Create the useFeatureFlag hook function\nfunction useFeatureFlag() {\n  // Create isEnabled reactive state here\n  // Define toggle function here\n  // Return object with isEnabled and toggle\n}\n\n// Create the main Vue component\n\n// Inside your component's setup function:\n// Call useFeatureFlag and destructure isEnabled and toggle\n// Return an object containing isEnabled and toggle\n\n<template>\n  <!-- Add your feature flag status paragraph here -->\n  <!-- Add toggle button here -->\n</template>",
        "expectedOutcome": "The template will include a button that toggles the feature flag state when clicked.",
        "successCriteria": [
          "A button element with @click=\"toggle\" is added to the template."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You added a button, but make sure it's wired to the toggle function using @click.",
        "feedbackWrong": "The button is missing or not properly connected to the toggle function.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<button",
            "@click=\"toggle\""
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import Vue and ref from vue",
        "id": "step1"
      },
      {
        "label": "Create the useFeatureFlag hook function",
        "id": "step2"
      },
      {
        "label": "Add feature flag state using ref",
        "id": "step3"
      },
      {
        "label": "Add toggle function to the hook",
        "id": "step4"
      },
      {
        "label": "Return state and toggle from the hook",
        "id": "step5"
      },
      {
        "label": "Create the main Vue component",
        "id": "step6"
      },
      {
        "label": "Use the feature flag hook in the component",
        "id": "step7"
      },
      {
        "label": "Return the reactive state and toggle from setup",
        "id": "step8"
      },
      {
        "label": "Add template structure with feature flag status",
        "id": "step9"
      },
      {
        "label": "Add toggle button and wire click event",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}