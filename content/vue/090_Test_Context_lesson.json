{
  "success": true,
  "config": {
    "lessonId": "ai-vue-90",
    "track": "vue",
    "lessonNum": 90,
    "title": "Test Context",
    "shortName": "P90",
    "intro": {
      "tag": "LESSON #90",
      "title": "Test Context",
      "body": "In this lesson, you'll learn how to use Test Context in Vue to write clean and maintainable tests. You'll build a simple counter component and create tests that verify its behavior works correctly. The core concept is understanding how to set up a testing environment with proper component mounting and data isolation. You'll work with Vue Test Utils and Jest to create test suites that validate your component's functionality. This will help you write reliable tests that don't interfere with each other.",
      "usecase": "Test Context is a fundamental pattern in Vue for managing and sharing state or logic across multiple components in a unit test suite. In real-world applications, developers use test contexts to isolate and control the environment for testing complex UI features like interactive dashboards, multi-step forms, or dynamic data tables. For example, when testing a dashboard that fetches live data, a test context can mock API responses and provide consistent state, ensuring each component test runs independently and reliably. This pattern is essential for maintaining scalable, maintainable test suites in large-scale Vue applications, such as e-commerce platforms or admin panels, where components depend on shared services or global state."
    },
    "objectives": [
      "Initialize a test context with Vue components and dependencies",
      "Configure test utilities within a Vue testing environment",
      "Apply test context to isolate component behavior in unit tests",
      "Manipulate test data and mock dependencies within the testing context",
      "Integrate test context with Vue Test Utils for component rendering",
      "Control test lifecycle and cleanup using context methods",
      "Construct reusable test setups for multiple Vue component tests"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Import Vue and defineTestSetup from @vue/test-utils",
        "instruction": "Import Vue and defineTestSetup from the @vue/test-utils package to prepare for component testing.",
        "hint": "Use named imports from '@vue/test-utils' to bring in Vue and defineTestSetup.",
        "analogousExample": "// Similar: importing from a package\nimport { ref, computed } from 'vue'",
        "seedCode": "// Import Vue and defineTestSetup from '@vue/test-utils'",
        "expectedOutcome": "The testing utilities are imported and ready to use.",
        "successCriteria": [
          "Vue and defineTestSetup are imported from '@vue/test-utils'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're importing both Vue and defineTestSetup from '@vue/test-utils'.",
        "feedbackWrong": "Check that you're importing Vue and defineTestSetup from the correct package '@vue/test-utils'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "Vue",
            "defineTestSetup",
            "'@vue/test-utils'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Create a basic Vue component for testing",
        "instruction": "Create a Vue component named 'TestComponent' that returns a template with a heading and a paragraph displaying a reactive message.",
        "hint": "Define a component with a template that includes an <h1> and a <p> element. Use the options API with a data function that returns an object containing a message property.",
        "analogousExample": "// Example of a simple Vue component\nconst ExampleComponent = {\n  template: '<div><h2>Example</h2><p>{{ message }}</p></div>',\n  data() {\n    return {\n      message: 'Hello from example!'\n    }\n  }\n}",
        "seedCode": "// Import Vue and defineTestSetup from '@vue/test-utils'\n\n// Create a Vue component named 'TestComponent' here",
        "expectedOutcome": "A Vue component named TestComponent is defined with a template containing an <h1> and a <p> element that displays a reactive message.",
        "successCriteria": [
          "TestComponent is defined as a Vue component with a template and a data function returning an object with a message property."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've started creating the component, but make sure it has both a template with the required elements and a data function returning a message property.",
        "feedbackWrong": "The component definition is missing or incomplete. Create a Vue component named TestComponent with a template and reactive data.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "TestComponent",
            "template",
            "data()",
            "return",
            "message"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Initialize component data with reactive properties",
        "instruction": "Inside the TestComponent, add a data function that returns an object with a property 'message' set to 'Hello from Vue!'",
        "hint": "In Vue components, the data option should be a function that returns an object containing reactive properties.",
        "analogousExample": "// Example of a data function in a Vue component\nexport default {\n  data() {\n    return {\n      count: 0,\n      text: 'Initial text'\n    };\n  }\n};",
        "seedCode": "// Import Vue and defineTestSetup from '@vue/test-utils'\nimport { defineTestSetup } from '@vue/test-utils';\n\n// Create a Vue component named 'TestComponent' here\nexport default {\n  // Add data function here\n};",
        "expectedOutcome": "The TestComponent now has reactive data initialized with a 'message' property.",
        "successCriteria": [
          "TestComponent has a data function that returns an object containing a 'message' property set to 'Hello from Vue!'"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your data function returns an object with exactly the 'message' property set to 'Hello from Vue!'",
        "feedbackWrong": "The component doesn't have the required data function. Remember to add a data function that returns an object with a 'message' property.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "data()",
            "return",
            "message",
            "'Hello from Vue!'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Create a computed property for testing",
        "instruction": "Inside your component, add a computed property named 'message' that returns a string combining the component's data properties.",
        "hint": "In Vue, computed properties are defined in the 'computed' option. Create a function that returns a string using the data properties.",
        "analogousExample": "// Example computed property\ncomputed: {\n  fullName() {\n    return this.firstName + ' ' + this.lastName;\n  }\n}",
        "seedCode": "import { defineTestSetup } from '@vue/test-utils';\n\nexport default {\n  data() {\n    return {\n      // Add your reactive properties here\n    };\n  },\n  // Add computed property here\n};",
        "expectedOutcome": "The component will have a computed property that depends on its data, making it reactive to changes.",
        "successCriteria": [
          "A computed property named 'message' is defined in the component's computed option."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure the computed property is properly defined in the 'computed' option and returns a value.",
        "feedbackWrong": "The computed property is missing or incorrectly defined. Check that it's in the 'computed' option and returns a value based on data properties.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "computed",
            "message()",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Add a method to modify component state",
        "instruction": "In your component, add a 'methods' object with a method called 'incrementCounter' that increases a 'counter' data property by 1 when called.",
        "hint": "Add a 'methods' property to your component object, then define a method inside it that accesses 'this.counter' and increments it.",
        "analogousExample": "// Example: adding a method to a Vue component\nconst exampleComponent = {\n  data() {\n    return {\n      count: 0\n    };\n  },\n  methods: {\n    increment() {\n      this.count += 1;\n    }\n  }\n};",
        "seedCode": "import { defineTestSetup } from '@vue/test-utils';\n\nexport default {\n  data() {\n    return {\n      counter: 0\n    };\n  },\n  // Add computed property here\n  methods: {\n    // Add incrementCounter method here\n  }\n};",
        "expectedOutcome": "Your component now has a method that can modify its reactive state, which will be useful for testing state changes.",
        "successCriteria": [
          "The component has a 'methods' object containing an 'incrementCounter' method that increments 'this.counter' by 1."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your method is properly defined in the 'methods' object and actually increments the counter value.",
        "feedbackWrong": "The method isn't correctly implemented. Check that you've added a 'methods' object with an 'incrementCounter' method that increases 'this.counter'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "methods",
            "incrementCounter",
            "this.counter",
            "+="
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Set up the test context using defineTestSetup",
        "instruction": "Call defineTestSetup to create a test context for your component, passing the component as the first argument.",
        "hint": "Use defineTestSetup with your component as the argument. This sets up the testing environment.",
        "analogousExample": "// Example: creating a test context\nconst testContext = defineTestSetup(MyComponent);",
        "seedCode": "import { defineTestSetup } from '@vue/test-utils';\n\nexport default {\n  data() {\n    return {\n      counter: 0\n    };\n  },\n  // Add computed property here\n  methods: {\n    // Add incrementCounter method here\n  }\n};\n\n// Call defineTestSetup here",
        "expectedOutcome": "A test context is created using defineTestSetup, ready for writing tests.",
        "successCriteria": [
          "defineTestSetup is called with the component as an argument."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're calling defineTestSetup with your component as the argument.",
        "feedbackWrong": "The test context isn't set up correctly. Use defineTestSetup with your component.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "defineTestSetup",
            "(",
            ")"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Write a test to check initial component state",
        "instruction": "Inside the defineTestSetup call, write a test that verifies the component's counter data property has an initial value of 0.",
        "hint": "Use the test function to write a test case that checks if the component's data.counter equals 0.",
        "analogousExample": "// Example test for initial state\ntest('initial counter should be 0', () => {\n  expect(component.data().counter).toBe(0);\n});",
        "seedCode": "import { defineTestSetup } from '@vue/test-utils';\n\nexport default {\n  data() {\n    return {\n      counter: 0\n    };\n  },\n  // Add computed property here\n  methods: {\n    // Add incrementCounter method here\n  }\n};\n\n// Call defineTestSetup here\ndefineTestSetup({\n  component: () => import('./'),\n  tests: () => {\n    // Write your test here\n  }\n});",
        "expectedOutcome": "A test is added to verify that the component's counter data property starts at 0.",
        "successCriteria": [
          "The test function is defined inside tests and checks that component.data().counter equals 0."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your test specifically checks that component.data().counter equals 0.",
        "feedbackWrong": "The test doesn't correctly verify the initial counter value. Check the example and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "test",
            "component.data().counter",
            "toBe(0)",
            "0"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Test computed property values in the context",
        "instruction": "In the defineTestSetup configuration, add a second test that checks the computed property returns the expected value based on component data.",
        "hint": "Add another test case inside the tests function that accesses the computed property and asserts its value matches expectations.",
        "analogousExample": "// Testing a computed property\ntests: () => {\n  it('checks computed property', () => {\n    const wrapper = mount(component);\n    expect(wrapper.vm.computedProperty).toBe(expectedValue);\n  });\n}",
        "seedCode": "import { defineTestSetup } from '@vue/test-utils';\n\nexport default {\n  data() {\n    return {\n      counter: 0\n    };\n  },\n  // Add computed property here\n  methods: {\n    // Add incrementCounter method here\n  }\n};\n\n// Call defineTestSetup here\ndefineTestSetup({\n  component: () => import('./'),\n  tests: () => {\n    // Write your test here\n    // Add test for computed property here\n  }\n});",
        "expectedOutcome": "The test suite will now include a test that verifies the computed property's value based on component data.",
        "successCriteria": [
          "A second test case is added inside the tests function that checks the computed property value."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added a test, but make sure it specifically checks the computed property value. Look at the analogous example for the correct structure.",
        "feedbackWrong": "The test for the computed property is missing or incorrect. Review how to write test cases in Vue Test Utils and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "it(",
            "computed",
            "expect(",
            "toBe("
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "Test component method execution and state changes",
        "instruction": "Inside the tests function, add a test that calls the incrementCounter method and verifies the counter data property increases.",
        "hint": "Use the component instance from the test context to call the method and check the updated data value.",
        "analogousExample": "// Example: testing a method that modifies state\nit('should increment counter when method is called', () => {\n  wrapper.vm.incrementCounter();\n  expect(wrapper.vm.counter).toBe(1);\n});",
        "seedCode": "import { defineTestSetup } from '@vue/test-utils';\n\nexport default {\n  data() {\n    return {\n      counter: 0\n    };\n  },\n  computed: {\n    doubledCounter() {\n      return this.counter * 2;\n    }\n  },\n  methods: {\n    incrementCounter() {\n      this.counter++;\n    }\n  }\n};\n\ndefineTestSetup({\n  component: () => import('./'),\n  tests: () => {\n    it('should have initial counter of 0', () => {\n      expect(wrapper.vm.counter).toBe(0);\n    });\n    it('should compute doubledCounter correctly', () => {\n      expect(wrapper.vm.doubledCounter).toBe(0);\n    });\n    // Add test for method execution here\n  }\n});",
        "expectedOutcome": "A test case that verifies the incrementCounter method correctly updates the counter data property.",
        "successCriteria": [
          "The test calls incrementCounter on the component instance and asserts that counter equals 1."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure your test calls the incrementCounter method and checks the updated counter value.",
        "feedbackWrong": "The test should call the incrementCounter method and verify the counter data property increases to 1.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "incrementCounter",
            "counter",
            "toBe(1)",
            "it(",
            "expect("
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Test component with different props in context",
        "instruction": "Add a new test case inside the tests function that passes a prop to the component and verifies the component's behavior changes accordingly.",
        "hint": "Use wrapper.setProps() to pass props to the component within the test context, then check if the component's state or computed properties update as expected.",
        "analogousExample": "// Example: testing with props\nit('should update when prop changes', () => {\n  wrapper.setProps({ initialValue: 5 });\n  expect(wrapper.vm.counter).toBe(5);\n});",
        "seedCode": "import { defineTestSetup } from '@vue/test-utils';\n\nexport default {\n  data() {\n    return {\n      counter: 0\n    };\n  },\n  computed: {\n    doubledCounter() {\n      return this.counter * 2;\n    }\n  },\n  methods: {\n    incrementCounter() {\n      this.counter++;\n    }\n  }\n};\n\ndefineTestSetup({\n  component: () => import('./'),\n  tests: () => {\n    it('should have initial counter of 0', () => {\n      expect(wrapper.vm.counter).toBe(0);\n    });\n    it('should compute doubledCounter correctly', () => {\n      expect(wrapper.vm.doubledCounter).toBe(0);\n    });\n    // Add test for method execution here\n    // Add test for props here\n  }\n});",
        "expectedOutcome": "The test suite now includes a test that verifies the component responds correctly to different prop values.",
        "successCriteria": [
          "A new test case is added that uses wrapper.setProps() to pass props and checks the component's state or computed properties."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added a test, but make sure it uses wrapper.setProps() to pass props and checks the component's response.",
        "feedbackWrong": "The test for props is missing or incorrect. Add a new it() block that uses wrapper.setProps() and verifies the component's behavior changes.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "it(",
            "wrapper.setProps(",
            "expect(",
            "toBe("
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import Vue and defineTestSetup from @vue/test-utils",
        "id": "step1"
      },
      {
        "label": "Create a basic Vue component for testing",
        "id": "step2"
      },
      {
        "label": "Initialize component data with reactive properties",
        "id": "step3"
      },
      {
        "label": "Create a computed property for testing",
        "id": "step4"
      },
      {
        "label": "Add a method to modify component state",
        "id": "step5"
      },
      {
        "label": "Set up the test context using defineTestSetup",
        "id": "step6"
      },
      {
        "label": "Write a test to check initial component state",
        "id": "step7"
      },
      {
        "label": "Test computed property values in the context",
        "id": "step8"
      },
      {
        "label": "Test component method execution and state changes",
        "id": "step9"
      },
      {
        "label": "Test component with different props in context",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}