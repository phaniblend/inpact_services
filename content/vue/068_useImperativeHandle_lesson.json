{
  "success": true,
  "config": {
    "lessonId": "ai-vue-68",
    "track": "vue",
    "lessonNum": 68,
    "title": "useImperativeHandle",
    "shortName": "P68",
    "intro": {
      "tag": "LESSON #68",
      "title": "useImperativeHandle",
      "body": "In this lesson, you will build a simple form with a custom input component that uses the useImperativeHandle hook to expose a method to its parent. You will learn how useImperativeHandle allows a child component to define custom functions that a parent can call directly, similar to using a ref. The main technologies involved are Vue's Composition API with the ref and defineExpose functions. This is a beginner-friendly introduction to controlling child component behavior from a parent in Vue.",
      "usecase": "In Vue applications, exposing child component methods to parent components is essential for complex UI interactions. This pattern appears in form validation scenarios where a parent needs to trigger validation across multiple child input components, or in dashboard widgets where a parent container needs to programmatically refresh specific chart components. Developers use this approach when building interactive data tables with custom filtering controls, modal dialogs that need external open/close triggers, or complex multi-step forms where navigation logic resides in a parent component but needs to manipulate child step components directly."
    },
    "objectives": [
      "Implement a custom ref using the useImperativeHandle hook",
      "Configure parent components to access child component methods via refs",
      "Control which child component methods are exposed to parent components",
      "Initialize ref forwarding between parent and child components",
      "Manipulate child component state from parent components using exposed methods",
      "Integrate useImperativeHandle with forwardRef for functional components",
      "Construct a form validation pattern where parent components trigger child validation methods"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Import Vue, ref, and defineExpose",
        "instruction": "Import Vue, ref, and defineExpose from 'vue'.",
        "hint": "Use the import statement to bring in the necessary APIs from the Vue package.",
        "analogousExample": "// Example: importing multiple items\nimport { ref, computed } from 'vue'",
        "seedCode": "// Import Vue, ref, and defineExpose from 'vue'",
        "expectedOutcome": "The necessary Vue APIs are imported for using refs and exposing component methods.",
        "successCriteria": [
          "Vue, ref, and defineExpose are imported from 'vue'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You imported some items, but make sure to import Vue, ref, and defineExpose from 'vue'.",
        "feedbackWrong": "Please import Vue, ref, and defineExpose from 'vue' to proceed.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "Vue",
            "ref",
            "defineExpose",
            "'vue'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Create a child component with a template",
        "instruction": "Create a child component named ChildComponent that returns a template with a single div element.",
        "hint": "Define a new component function that returns a template section containing a div.",
        "analogousExample": "// Example of a basic component with template\nfunction ChildComponent() {\n  return {\n    template: '<div></div>'\n  }\n}",
        "seedCode": "// Import Vue, ref, and defineExpose from 'vue'\n\n// Create a child component named ChildComponent that returns a template with a single div element.",
        "expectedOutcome": "A child component is defined with a template containing a div element.",
        "successCriteria": [
          "ChildComponent is defined as a function that returns an object with a template property containing a div element."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure your component returns an object with a template property containing a div element.",
        "feedbackWrong": "The child component isn't properly defined. Check that you've created a function named ChildComponent that returns a template with a div.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "ChildComponent",
            "template",
            "div"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Initialize reactive state in the child component",
        "instruction": "Inside your ChildComponent's setup function, initialize a reactive state variable called 'count' using ref with an initial value of 0.",
        "hint": "Use the ref function imported earlier to create a reactive reference. Remember to return it from the setup function.",
        "analogousExample": "// Example: creating reactive state\nconst count = ref(0);",
        "seedCode": "// Import Vue, ref, and defineExpose from 'vue'\n\n// Create a child component named ChildComponent that returns a template with a single div element.\n\nconst ChildComponent = {\n  setup() {\n    // Initialize count ref here\n    \n    return {\n      // Return count here\n    };\n  },\n  template: '<div>Child Component</div>'\n};",
        "expectedOutcome": "The child component will have a reactive count state variable initialized to 0.",
        "successCriteria": [
          "A ref named 'count' with initial value 0 is created inside the setup function and returned."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you're creating a ref named 'count' with value 0 and returning it from the setup function.",
        "feedbackWrong": "create a reactive state variable called 'count' using the ref function with an initial value of 0.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "count",
            "ref(0)",
            "setup",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Create a method to modify child state",
        "instruction": "Inside the ChildComponent's setup function, create a method named 'increment' that increases the count value by 1.",
        "hint": "Define a regular JavaScript function inside the setup function that modifies the count ref's value.",
        "analogousExample": "// Example: a method that modifies state\nfunction incrementCount() {\n  count.value += 1;\n}",
        "seedCode": "// Import Vue, ref, and defineExpose from 'vue'\n\n// Create a child component named ChildComponent that returns a template with a single div element.\n\nconst ChildComponent = {\n  setup() {\n    // Initialize count ref here\n    \n    // Create increment method here\n    \n    return {\n      // Return count here\n    };\n  },\n  template: '<div>Child Component</div>'\n};",
        "expectedOutcome": "The child component will have an increment method that can modify its internal count state.",
        "successCriteria": [
          "An increment method is defined inside the setup function that increases count by 1."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure the increment method properly increases the count value by 1.",
        "feedbackWrong": "The increment method isn't correctly defined. Check that it's inside the setup function and modifies count.value.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "increment",
            "count.value",
            "+=",
            "1"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Expose the increment method using defineExpose",
        "instruction": "Inside the setup function of ChildComponent, call defineExpose and pass it an object containing the increment method.",
        "hint": "defineExpose is a function that takes an object with the properties you want to expose from the child component.",
        "analogousExample": "// Exposing a method from a child component\nsetup() {\n  const message = ref('Hello');\n  const showMessage = () => console.log(message.value);\n  defineExpose({ showMessage });\n  return { message };\n}",
        "seedCode": "// Import Vue, ref, and defineExpose from 'vue'\n\n// Create a child component named ChildComponent that returns a template with a single div element.\n\nconst ChildComponent = {\n  setup() {\n    // Initialize count ref here\n    \n    // Create increment method here\n    \n    // Expose the increment method using defineExpose\n    \n    return {\n      // Return count here\n    };\n  },\n  template: '<div>Child Component</div>'\n};",
        "expectedOutcome": "The increment method will be accessible from parent components that use ChildComponent.",
        "successCriteria": [
          "defineExpose is called with an object containing the increment method inside the setup function."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you're calling defineExpose with an object that contains the increment method.",
        "feedbackWrong": "Try again. You need to call defineExpose inside the setup function and pass it an object with the increment method.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "defineExpose",
            "increment"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Create a parent component with template",
        "instruction": "Create a parent component named ParentComponent with a template section containing a single div element.",
        "hint": "Define a new component object with a template property that returns a div element.",
        "analogousExample": "// Example: a simple component with template\nconst ParentComponent = {\n  template: '<div>Parent Component</div>'\n};",
        "seedCode": "// Import Vue, ref, and defineExpose from 'vue'\n\n// Create a child component named ChildComponent that returns a template with a single div element.\n\nconst ChildComponent = {\n  setup() {\n    // Initialize count ref here\n    \n    // Create increment method here\n    \n    // Expose the increment method using defineExpose\n    \n    return {\n      // Return count here\n    };\n  },\n  template: '<div>Child Component</div>'\n};\n\n// Create ParentComponent here",
        "expectedOutcome": "You will have a ParentComponent defined with a basic template structure.",
        "successCriteria": [
          "ParentComponent is defined as a component object with a template property containing a div element."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created a component, but make sure it's named ParentComponent and has a template with a div element.",
        "feedbackWrong": "The parent component wasn't created correctly. Check that you defined ParentComponent with a template property.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "ParentComponent",
            "template",
            "'<div>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Import and register the child component in parent",
        "instruction": "Inside the ParentComponent, import the ChildComponent and register it in the components option.",
        "hint": "Use the import statement to bring in ChildComponent, then add a components property to the ParentComponent object.",
        "analogousExample": "// Example: importing and registering a component\nimport ChildComponent from './ChildComponent.vue';\n\nconst ParentComponent = {\n  components: {\n    ChildComponent\n  },\n  setup() {\n    return {};\n  }\n};",
        "seedCode": "// Import Vue, ref, and defineExpose from 'vue'\n\n// Create a child component named ChildComponent that returns a template with a single div element.\n\nconst ChildComponent = {\n  setup() {\n    // Initialize count ref here\n    \n    // Create increment method here\n    \n    // Expose the increment method using defineExpose\n    \n    return {\n      // Return count here\n    };\n  },\n  template: '<div>Child Component</div>'\n};\n\n// Create ParentComponent here\nconst ParentComponent = {\n  // Import and register ChildComponent here\n  setup() {\n    return {};\n  },\n  template: '<div>Parent Component</div>'\n};",
        "expectedOutcome": "The ParentComponent will have access to ChildComponent in its template.",
        "successCriteria": [
          "ChildComponent is imported and registered in the ParentComponent's components option."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure to both import ChildComponent and add it to the components object.",
        "feedbackWrong": "Check the import statement and the components registration. Both are needed.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "ChildComponent",
            "components",
            "ChildComponent"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Create a ref in parent to reference child component",
        "instruction": "Inside the ParentComponent's setup function, initialize a ref called 'childRef' with the value null.",
        "hint": "Use the ref function from Vue to create a reactive reference that can hold the child component instance.",
        "analogousExample": "// Creating a ref in Vue\nimport { ref } from 'vue';\n\nconst myRef = ref(null);",
        "seedCode": "// Import Vue, ref, and defineExpose from 'vue'\n\n// Create a child component named ChildComponent that returns a template with a single div element.\n\nconst ChildComponent = {\n  setup() {\n    // Initialize count ref here\n    \n    // Create increment method here\n    \n    // Expose the increment method using defineExpose\n    \n    return {\n      // Return count here\n    };\n  },\n  template: '<div>Child Component</div>'\n};\n\n// Create ParentComponent here\nconst ParentComponent = {\n  // Import and register ChildComponent here\n  setup() {\n    // Initialize childRef here\n    return {};\n  },\n  template: '<div>Parent Component</div>'\n};",
        "expectedOutcome": "The parent component now has a reactive reference that can be used to access the child component instance.",
        "successCriteria": [
          "A ref named 'childRef' with initial value null is created inside ParentComponent's setup function."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're creating a ref called 'childRef' with null as the initial value inside the setup function.",
        "feedbackWrong": "That doesn't look right. You need to create a ref named 'childRef' with null as the initial value inside ParentComponent's setup function.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "childRef",
            "ref",
            "null"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "Add child component to parent template with ref attribute",
        "instruction": "In the ParentComponent template, add the ChildComponent with the ref attribute bound to childRef.",
        "hint": "Use the component name as a custom element tag and bind the ref attribute to childRef.",
        "analogousExample": "// Example: adding a component with a ref in template\n<template>\n  <div>\n    <ChildComponent ref=\"childRef\" />\n  </div>\n</template>",
        "seedCode": "// Import Vue, ref, and defineExpose from 'vue'\n\n// Create a child component named ChildComponent that returns a template with a single div element.\n\nconst ChildComponent = {\n  setup() {\n    // Initialize count ref here\n    \n    // Create increment method here\n    \n    // Expose the increment method using defineExpose\n    \n    return {\n      // Return count here\n    };\n  },\n  template: '<div>Child Component</div>'\n};\n\n// Create ParentComponent here\nconst ParentComponent = {\n  // Import and register ChildComponent here\n  setup() {\n    // Initialize childRef here\n    return {};\n  },\n  template: '<div>Parent Component</div>'\n};",
        "expectedOutcome": "The parent template now includes the child component with the ref attribute attached, enabling the parent to reference the child component instance.",
        "successCriteria": [
          "ChildComponent is placed in the ParentComponent template with the ref attribute set to childRef."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you've added the ChildComponent to the ParentComponent template with the ref attribute bound to childRef.",
        "feedbackWrong": "The child component is not correctly placed in the parent template with the ref attribute. Check that you're using the component name as a custom element tag and binding the ref attribute to childRef.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<ChildComponent",
            "ref=\"childRef\"",
            "template"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Create button in parent to call child method",
        "instruction": "Add a button element to the ParentComponent template with a click handler that calls childRef.value.increment()",
        "hint": "Use the @click directive to attach the click handler. The handler should access the child component's increment method through the ref.",
        "analogousExample": "// Adding a button with click handler in Vue\n<button @click=\"handleClick\">Click me</button>",
        "seedCode": "// Import Vue, ref, and defineExpose from 'vue'\n\n// Create a child component named ChildComponent that returns a template with a single div element.\n\nconst ChildComponent = {\n  setup() {\n    // Initialize count ref here\n    \n    // Create increment method here\n    \n    // Expose the increment method using defineExpose\n    \n    return {\n      // Return count here\n    };\n  },\n  template: '<div>Child Component</div>'\n};\n\n// Create ParentComponent here\nconst ParentComponent = {\n  // Import and register ChildComponent here\n  setup() {\n    // Initialize childRef here\n    return {};\n  },\n  template: '<div>Parent Component</div>'\n};",
        "expectedOutcome": "The parent component will have a button that, when clicked, calls the child component's increment method through the ref.",
        "successCriteria": [
          "A button element is added to ParentComponent template with @click handler that calls childRef.value.increment()"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added a button, but the click handler might not be correctly calling childRef.value.increment(). Check the syntax.",
        "feedbackWrong": "The button or click handler is missing or incorrect. Make sure to add a button with @click that calls childRef.value.increment().",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "button",
            "@click",
            "childRef.value.increment()"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import Vue, ref, and defineExpose",
        "id": "step1"
      },
      {
        "label": "Create a child component with a template",
        "id": "step2"
      },
      {
        "label": "Initialize reactive state in the child component",
        "id": "step3"
      },
      {
        "label": "Create a method to modify child state",
        "id": "step4"
      },
      {
        "label": "Expose the increment method using defineExpose",
        "id": "step5"
      },
      {
        "label": "Create a parent component with template",
        "id": "step6"
      },
      {
        "label": "Import and register the child component in parent",
        "id": "step7"
      },
      {
        "label": "Create a ref in parent to reference child component",
        "id": "step8"
      },
      {
        "label": "Add child component to parent template with ref attribute",
        "id": "step9"
      },
      {
        "label": "Create button in parent to call child method",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}