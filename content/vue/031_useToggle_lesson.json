{
  "success": true,
  "config": {
    "lessonId": "ai-vue-31",
    "track": "vue",
    "lessonNum": 31,
    "title": "useToggle",
    "shortName": "P31",
    "intro": {
      "tag": "LESSON #31",
      "title": "useToggle",
      "body": "In this lesson, you will build a custom Vue composable called `useToggle` that manages a boolean state and provides a function to toggle it. You will learn the core concept of state management using Vue's `ref` and `computed` APIs to create reactive values. The main technologies involved are Vue 3's Composition API and JavaScript functions for reusable logic. By the end, you'll understand how to encapsulate and reuse stateful logic in your Vue applications.",
      "usecase": "The useToggle pattern is essential for managing binary state across interactive UI components in real applications. Developers use it to control visibility of modals, expand/collapse panels in dashboards, toggle between light/dark themes, and switch filters on data tables. In e-commerce sites, it powers product comparison toggles; in admin panels, it manages sidebar menus and detail view expansions. This reusable hook eliminates repetitive state logic, making code cleaner and more maintainable across complex interfaces."
    },
    "objectives": [
      "Construct a custom useToggle composable function that returns reactive state and a toggle method",
      "Initialize a reactive boolean state variable within the useToggle composable",
      "Implement a toggle function that reverses the boolean state when called",
      "Bind the useToggle composable to a Vue component's setup function",
      "Render the current toggle state in the component template using interpolation",
      "Control the toggle state by wiring a button click event to the toggle method",
      "Manipulate the toggle state by adding reset and set methods to the composable",
      "Integrate multiple independent toggle instances within a single component"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Import ref from Vue",
        "instruction": "Import the ref function from the Vue package.",
        "hint": "Use an import statement to bring in ref from 'vue'.",
        "analogousExample": "// Importing a function from a package\nimport { ref } from 'vue'",
        "seedCode": "// Import ref from Vue here",
        "expectedOutcome": "The ref function is imported and ready to create reactive state.",
        "successCriteria": [
          "ref is imported from 'vue'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you're importing ref specifically from the 'vue' package.",
        "feedbackWrong": "The import statement isn't quite right. Remember to import ref from the 'vue' package.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "ref",
            "vue"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Create the useToggle composable function",
        "instruction": "Create a function named useToggle that accepts an initial value parameter.",
        "hint": "Define a function that takes one parameter, typically named initialValue.",
        "analogousExample": "// Example: a simple named function with a parameter\nfunction useCounter(initialCount) {\n  // logic here\n}",
        "seedCode": "import { ref } from 'vue';\n\n// Create the useToggle function here",
        "expectedOutcome": "A useToggle function is defined with a parameter for the initial value.",
        "successCriteria": [
          "The useToggle function is created and accepts an initial value parameter."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you've created a function named useToggle that takes one parameter.",
        "feedbackWrong": "The function isn't defined correctly. Create a function named useToggle that accepts an initial value parameter.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useToggle",
            "function",
            "(",
            ")"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Initialize reactive state inside useToggle",
        "instruction": "Inside the useToggle function, create a ref that stores the current toggle value, initialized with the initialValue parameter.",
        "hint": "Use the ref function imported from Vue to create reactive state. Pass initialValue as the argument to ref.",
        "analogousExample": "// Example: creating reactive state with ref\nfunction useCounter(initialCount) {\n  const count = ref(initialCount);\n  // ... rest of function\n}",
        "seedCode": "import { ref } from 'vue';\n\n// Create the useToggle function here\nfunction useToggle(initialValue) {\n  // Create a ref for the toggle state here\n}",
        "expectedOutcome": "The useToggle function will have a reactive state variable that tracks the current toggle value.",
        "successCriteria": [
          "A ref variable is declared inside useToggle and initialized with the initialValue parameter."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you're creating a ref variable inside the useToggle function and initializing it with the initialValue parameter.",
        "feedbackWrong": "Inside the useToggle function, use the ref function to create a reactive variable that stores the toggle state, initialized with the initialValue parameter.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "ref",
            "initialValue"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Define the toggle function",
        "instruction": "Inside the useToggle function, define a function named toggle that toggles the ref's value between true and false.",
        "hint": "The toggle function should update the ref's value to the opposite of its current value.",
        "analogousExample": "// Example: a function that toggles a value\nfunction toggleValue(current) {\n  return !current;\n}",
        "seedCode": "import { ref } from 'vue';\n\n// Create the useToggle function here\nfunction useToggle(initialValue) {\n  // Create a ref for the toggle state here\n  const state = ref(initialValue);\n  \n  // Define the toggle function here\n}",
        "expectedOutcome": "The useToggle function now contains a toggle function that can switch the state value.",
        "successCriteria": [
          "A toggle function is defined inside useToggle that toggles the state ref's value."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure the toggle function properly toggles the state value.",
        "feedbackWrong": "The toggle function isn't defined correctly. It should update the state ref to its opposite value.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "function",
            "toggle",
            "state.value",
            "=",
            "!",
            "state.value"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Return state and toggle from useToggle",
        "instruction": "Return an object containing the state ref and toggle function from the useToggle function.",
        "hint": "Use the return keyword followed by an object with properties for the state and toggle function.",
        "analogousExample": "// Example: returning values from a function\nfunction useCounter() {\n  const count = ref(0);\n  const increment = () => { count.value++ };\n  return { count, increment };\n}",
        "seedCode": "import { ref } from 'vue';\n\n// Create the useToggle function here\nfunction useToggle(initialValue) {\n  // Create a ref for the toggle state here\n  const state = ref(initialValue);\n  \n  // Define the toggle function here\n  const toggle = () => { state.value = !state.value };\n  \n  // Return state and toggle here\n}",
        "expectedOutcome": "The useToggle function returns an object with state and toggle properties.",
        "successCriteria": [
          "useToggle returns an object containing state and toggle."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're returning an object with both state and toggle properties.",
        "feedbackWrong": "Check that you're returning an object containing both the state ref and toggle function.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "return",
            "{",
            "state",
            "toggle",
            "}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Create the main Vue component",
        "instruction": "Create a Vue component named ToggleDemo with a setup function that returns an empty object for now.",
        "hint": "In Vue, a component can be defined as a function that returns an object. The setup function is where you'll later use your composable.",
        "analogousExample": "// Example: a simple Vue component\nfunction MyComponent() {\n  const setup = () => {\n    // logic will go here\n    return {};\n  };\n  return {\n    setup\n  };\n}",
        "seedCode": "import { ref } from 'vue';\n\n// Create the useToggle function here\nfunction useToggle(initialValue) {\n  // Create a ref for the toggle state here\n  const state = ref(initialValue);\n  \n  // Define the toggle function here\n  const toggle = () => { state.value = !state.value };\n  \n  // Return state and toggle here\n}\n\n// Create the ToggleDemo component here",
        "expectedOutcome": "A ToggleDemo component with a setup function is created, ready to use the useToggle composable in the next step.",
        "successCriteria": [
          "The ToggleDemo component is defined with a setup function that returns an empty object."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure the component is named ToggleDemo and has a setup function that returns an object.",
        "feedbackWrong": "The component isn't quite right. Remember to create a function named ToggleDemo with a setup function inside.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "ToggleDemo",
            "setup",
            "return",
            "{}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Use the useToggle composable in the component",
        "instruction": "Inside the ToggleDemo component's setup function, call useToggle with an initial value of false and destructure the returned state and toggle.",
        "hint": "Call the useToggle function you created earlier, passing false as the initial value. Use destructuring to get the state and toggle from the returned object.",
        "analogousExample": "// Similar: calling a custom composable\nconst { data, refresh } = useFetch('https://api.example.com');",
        "seedCode": "import { ref } from 'vue';\n\n// Create the useToggle function here\nfunction useToggle(initialValue) {\n  // Create a ref for the toggle state here\n  const state = ref(initialValue);\n  \n  // Define the toggle function here\n  const toggle = () => { state.value = !state.value };\n  \n  // Return state and toggle here\n}\n\n// Create the ToggleDemo component here\nconst ToggleDemo = {\n  setup() {\n    // Call useToggle here with initial value false\n    \n    return {};\n  }\n};",
        "expectedOutcome": "The component will have access to reactive state and a toggle function from the useToggle composable.",
        "successCriteria": [
          "useToggle is called with false as argument",
          "The returned state and toggle are destructured"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're calling useToggle with false and destructuring both state and toggle.",
        "feedbackWrong": "The component setup doesn't properly call useToggle. Review how to call custom composables in Vue.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useToggle(false)",
            "state",
            "toggle"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Add a button to the template",
        "instruction": "In the ToggleDemo component's setup function, return an object that includes a button element in the template section.",
        "hint": "The return statement in setup should contain a template object with a button element.",
        "analogousExample": "// Example: returning a button in a Vue component setup\nconst ExampleComponent = {\n  setup() {\n    return {\n      template: '<button>Click me</button>'\n    };\n  }\n};",
        "seedCode": "import { ref } from 'vue';\n\n// Create the useToggle function here\nfunction useToggle(initialValue) {\n  // Create a ref for the toggle state here\n  const state = ref(initialValue);\n  \n  // Define the toggle function here\n  const toggle = () => { state.value = !state.value };\n  \n  // Return state and toggle here\n}\n\n// Create the ToggleDemo component here\nconst ToggleDemo = {\n  setup() {\n    // Call useToggle here with initial value false\n    \n    return {};\n  }\n};",
        "expectedOutcome": "The ToggleDemo component will have a button element in its template, ready to be wired to the toggle function in the next step.",
        "successCriteria": [
          "The setup function returns an object with a template property containing a button element."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure the setup function returns an object with a template property containing a button element.",
        "feedbackWrong": "The setup function should return an object with a template property that includes a button element. Check the structure of your return statement.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "return",
            "{",
            "template:",
            "'<button>",
            "'",
            "}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "Display the current toggle state",
        "instruction": "In the ToggleDemo component's template, add a paragraph element that displays the current toggle state value using text interpolation.",
        "hint": "Use double curly braces {{ }} to output the state value inside a <p> tag.",
        "analogousExample": "// Displaying a reactive value in template\n<p>Current value: {{ myValue }}</p>",
        "seedCode": "import { ref } from 'vue';\n\n// Create the useToggle function here\nfunction useToggle(initialValue) {\n  // Create a ref for the toggle state here\n  const state = ref(initialValue);\n  \n  // Define the toggle function here\n  const toggle = () => { state.value = !state.value };\n  \n  // Return state and toggle here\n}\n\n// Create the ToggleDemo component here\nconst ToggleDemo = {\n  setup() {\n    // Call useToggle here with initial value false\n    \n    return {};\n  }\n};",
        "expectedOutcome": "The component will show the current boolean toggle state in the UI.",
        "successCriteria": [
          "A paragraph element displays the toggle state value using Vue's text interpolation syntax."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using double curly braces {{ }} to output the state value inside a paragraph element.",
        "feedbackWrong": "The toggle state isn't being displayed. Add a paragraph element with text interpolation showing the state value.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<p>",
            "{{",
            "}}",
            "state"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Wire the button click to the toggle function",
        "instruction": "Add a click event handler to the button that calls the toggle function from useToggle.",
        "hint": "Use the @click directive on the button element and call the toggle function.",
        "analogousExample": "// Example: wiring a button click to a function\n<button @click=\"handleClick\">Click me</button>",
        "seedCode": "import { ref } from 'vue';\n\n// Create the useToggle function here\nfunction useToggle(initialValue) {\n  // Create a ref for the toggle state here\n  const state = ref(initialValue);\n  \n  // Define the toggle function here\n  const toggle = () => { state.value = !state.value };\n  \n  // Return state and toggle here\n}\n\n// Create the ToggleDemo component here\nconst ToggleDemo = {\n  setup() {\n    // Call useToggle here with initial value false\n    \n    return {};\n  }\n};",
        "expectedOutcome": "The button will respond to clicks and call the toggle function when clicked.",
        "successCriteria": [
          "Button has @click directive",
          "@click calls the toggle function"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure the button has an @click directive that calls the toggle function.",
        "feedbackWrong": "The button isn't properly wired to the toggle function. Add an @click directive to the button that calls toggle.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "@click",
            "toggle"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import ref from Vue",
        "id": "step1"
      },
      {
        "label": "Create the useToggle composable function",
        "id": "step2"
      },
      {
        "label": "Initialize reactive state inside useToggle",
        "id": "step3"
      },
      {
        "label": "Define the toggle function",
        "id": "step4"
      },
      {
        "label": "Return state and toggle from useToggle",
        "id": "step5"
      },
      {
        "label": "Create the main Vue component",
        "id": "step6"
      },
      {
        "label": "Use the useToggle composable in the component",
        "id": "step7"
      },
      {
        "label": "Add a button to the template",
        "id": "step8"
      },
      {
        "label": "Display the current toggle state",
        "id": "step9"
      },
      {
        "label": "Wire the button click to the toggle function",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}