{
  "id": "reverse-linked-list",
  "title": "Reverse Linked List",
  "pattern": "linked-list",
  "difficulty": "easy",
  "language": "javascript",
  "status": "draft",
  "flow": [
    {
      "stepId": "title",
      "mentorSays": "At the end of this lesson, you will be able to:\n\n1. Understand what a linked list is and how it works\n2. Reverse a linked list using iterative approach\n3. Use pointers to manipulate node connections\n4. Implement the solution step by step in your chosen language",
      "action": "continue",
      "next": "lesson-illustration"
    },
    {
      "stepId": "lesson-illustration",
      "mentorSays": "Perfect! Now let's understand what the Reverse Linked List lesson is asking.\n\nA linked list is a data structure where each element (called a node) contains:\n- A value (data)\n- A pointer/reference to the next node\n\n**Example:** 1 -> 2 -> 3 -> 4 -> 5 -> null\n\nYour task: Reverse the linked list so it becomes:\n5 -> 4 -> 3 -> 2 -> 1 -> null\n\n**How it works:**\n- We traverse the list once\n- As we go, we reverse the direction of each pointer\n- We need to keep track of: current node, previous node, and next node\n\n**Step-by-step trace:**\n\nInitial: 1 -> 2 -> 3 -> 4 -> 5 -> null\nprev = null, curr = 1\n\nStep 1: Save next (2), reverse link (1 -> null), move pointers\nResult: null <- 1    2 -> 3 -> 4 -> 5 -> null\nprev = 1, curr = 2\n\nStep 2: Save next (3), reverse link (2 -> 1), move pointers\nResult: null <- 1 <- 2    3 -> 4 -> 5 -> null\nprev = 2, curr = 3\n\nStep 3: Save next (4), reverse link (3 -> 2), move pointers\nResult: null <- 1 <- 2 <- 3    4 -> 5 -> null\nprev = 3, curr = 4\n\nStep 4: Save next (5), reverse link (4 -> 3), move pointers\nResult: null <- 1 <- 2 <- 3 <- 4    5 -> null\nprev = 4, curr = 5\n\nStep 5: Save next (null), reverse link (5 -> 4), move pointers\nResult: null <- 1 <- 2 <- 3 <- 4 <- 5\nprev = 5, curr = null\n\nDone! Return prev (which is now the new head: 5)",
      "example": "Original: 1 -> 2 -> 3 -> 4 -> 5 -> null\n\nAfter reversing: 5 -> 4 -> 3 -> 2 -> 1 -> null\n\nVisual:\nBefore: [1] -> [2] -> [3] -> [4] -> [5] -> null\nAfter:  null <- [1] <- [2] <- [3] <- [4] <- [5]",
      "action": "continue",
      "next": "thinking-challenge"
    },
    {
      "stepId": "thinking-challenge",
      "mentorSays": "Now that you understand what the lesson wants, here's the real question:\n\nHow would YOU reverse a linked list?\n\nThink about it for a moment. What approach feels natural to you?",
      "choices": [
        {
          "label": "Use three pointers: prev, curr, next to reverse links one by one",
          "next": "explore-iterative"
        },
        {
          "label": "Use recursion to reverse the rest, then connect",
          "next": "explore-recursive"
        },
        {
          "label": "Convert to array, reverse array, rebuild list",
          "next": "explore-array"
        }
      ]
    },
    {
      "stepId": "explore-recursive",
      "mentorSays": "That's an elegant approach! You're thinking: \"I'll use recursion to reverse the rest of the list, then connect the current node.\"\n\nThis works beautifully! However, recursion uses O(n) extra space for the call stack.\n\nThere's an iterative approach that's equally elegant and uses O(1) space. Would you like to learn that?",
      "action": "continue",
      "next": "explore-iterative"
    },
    {
      "stepId": "explore-array",
      "mentorSays": "That's a creative approach! You're thinking: \"I'll convert the list to an array, reverse the array, then rebuild the list.\"\n\nThis works! However, it uses O(n) extra space and requires two passes.\n\nThere's a more efficient O(1) space approach using pointers. Would you like to learn that?",
      "action": "continue",
      "next": "explore-iterative"
    },
    {
      "stepId": "explore-iterative",
      "mentorSays": "Excellent choice! The iterative approach is both efficient and elegant. Here's the core idea:\n\n**The Algorithm:**\n1. **Initialize**: prev = null, curr = head\n2. **While curr is not null**:\n   - Save next node: next = curr.next\n   - Reverse the link: curr.next = prev\n   - Move pointers forward: prev = curr, curr = next\n3. **Return prev** (which is now the new head)\n\n**Why it works**: At each step, we reverse one link. By the end, all links are reversed!\n\nLet's trace with 1 -> 2 -> 3 -> null:\n\n- **Initial**: prev = null, curr = 1\n- **Step 1**: next = 2, curr.next = null, prev = 1, curr = 2\n  Result: null <- 1    2 -> 3 -> null\n- **Step 2**: next = 3, curr.next = 1, prev = 2, curr = 3\n  Result: null <- 1 <- 2    3 -> null\n- **Step 3**: next = null, curr.next = 2, prev = 3, curr = null\n  Result: null <- 1 <- 2 <- 3\n- **Return prev = 3** (new head) ✓\n\nThis is O(n) time and O(1) space! Now let's code it step by step.",
      "action": "continue",
      "next": "language-selection"
    },
    {
      "stepId": "language-selection",
      "mentorSays": "Great! Now let's code this solution. Which programming language would you like to use?",
      "choices": [
        {
          "label": "JavaScript",
          "next": "variable-check-js"
        },
        {
          "label": "Python",
          "next": "variable-check-python"
        },
        {
          "label": "Java",
          "next": "variable-check-java"
        },
        {
          "label": "C++",
          "next": "variable-check-cpp"
        },
        {
          "label": "TypeScript",
          "next": "variable-check-ts"
        }
      ]
    },
    {
      "stepId": "variable-check-js",
      "mentorSays": "Before we start coding, let me ask: Do you know what a variable is in JavaScript?",
      "choices": [
        {
          "label": "Yes, I know variables",
          "next": "function-check-js"
        },
        {
          "label": "No, explain variables",
          "next": "variable-explanation-js"
        }
      ]
    },
    {
      "stepId": "variable-explanation-js",
      "mentorSays": "A variable is like a labeled box where you can store a value. You give it a name (like 'prev' or 'curr') and put data inside it.\n\nFor example, 'let prev = null;' creates a variable that stores null. Later, you can use that variable name to refer to it.",
      "example": "let prev = null;\nlet curr = head;\n\nNow these variables store node references\nWe can use them to track our position in the list",
      "action": "continue",
      "next": "function-check-js"
    },
    {
      "stepId": "function-check-js",
      "mentorSays": "Before we start coding, let me ask: Do you know what a function is in JavaScript?",
      "choices": [
        {
          "label": "Yes, I know functions",
          "next": "parameter-check-js"
        },
        {
          "label": "No, explain functions",
          "next": "function-explanation-js"
        }
      ]
    },
    {
      "stepId": "function-explanation-js",
      "mentorSays": "A function is a reusable block of code. You give it inputs (called parameters), and it can return an output.\n\nThink of it like a recipe: you provide ingredients (parameters), the function does the work, and gives you a result (return value).",
      "example": "function add(a, b) {\n  return a + b;\n}\n\nadd(3, 5) returns 8\nadd(10, 20) returns 30",
      "action": "continue",
      "next": "parameter-check-js"
    },
    {
      "stepId": "parameter-check-js",
      "mentorSays": "When we write a function, we can give it inputs. These inputs are called 'parameters'. Do you know what parameters are?",
      "choices": [
        {
          "label": "Yes, I know parameters",
          "next": "linked-list-check-js"
        },
        {
          "label": "No, explain parameters",
          "next": "parameter-explanation-js"
        }
      ]
    },
    {
      "stepId": "parameter-explanation-js",
      "mentorSays": "Parameters are like variables that receive values when you call the function. When you write 'function reverseList(head)', the 'head' is a parameter - it's a placeholder that will receive the head node of the linked list when you call the function.",
      "example": "function reverseList(head) {\n  // 'head' is a parameter\n}\n\n// When we call it:\nreverseList(headNode);\n// The head node goes into 'head'",
      "action": "continue",
      "next": "linked-list-check-js"
    },
    {
      "stepId": "linked-list-check-js",
      "mentorSays": "To solve this lesson, we'll need to work with linked lists. Do you know what a linked list is?",
      "choices": [
        {
          "label": "Yes, I know linked lists",
          "next": "loop-check-js"
        },
        {
          "label": "No, explain linked lists",
          "next": "linked-list-explanation-js"
        }
      ]
    },
    {
      "stepId": "linked-list-explanation-js",
      "mentorSays": "A linked list is a data structure where each element (node) contains:\n- A value (data)\n- A reference to the next node\n\nIn JavaScript, a node looks like: { val: 1, next: node2 }\n\nThe list: 1 -> 2 -> 3 means:\n- Node 1 has value 1 and points to node 2\n- Node 2 has value 2 and points to node 3\n- Node 3 has value 3 and points to null (end of list)",
      "example": "// Node structure\nlet node1 = { val: 1, next: null };\nlet node2 = { val: 2, next: null };\nlet node3 = { val: 3, next: null };\n\n// Link them\nnode1.next = node2;\nnode2.next = node3;\n\n// Result: 1 -> 2 -> 3 -> null",
      "action": "continue",
      "next": "loop-check-js"
    },
    {
      "stepId": "loop-check-js",
      "mentorSays": "To solve this lesson, we'll need to use loops. Do you know what a loop is in JavaScript?",
      "choices": [
        {
          "label": "Yes, I know loops",
          "next": "coding-start-js"
        },
        {
          "label": "No, explain loops",
          "next": "loop-explanation-js"
        }
      ]
    },
    {
      "stepId": "loop-explanation-js",
      "mentorSays": "A loop lets you repeat code multiple times. A 'while' loop runs code repeatedly as long as a condition is true.\n\nFor example, 'while (curr !== null)' means: keep running the code as long as curr is not null.",
      "example": "let i = 0;\nwhile (i < 3) {\n  console.log(i);\n  i++;\n}\n\nThis prints: 0, 1, 2",
      "action": "continue",
      "next": "coding-start-js"
    },
    {
      "stepId": "coding-start-js",
      "mentorSays": "Perfect! Let's implement the reverse linked list solution in JavaScript. We'll create a function that takes the head of a linked list and returns the head of the reversed list.",
      "example": "function reverseList(head) {\n  \n}",
      "action": "continue",
      "next": "coding-init-pointers-js"
    },
    {
      "stepId": "coding-init-pointers-js",
      "mentorSays": "First, let's initialize our pointers: prev (previous node) starts as null, and curr (current node) starts as head.",
      "example": "function reverseList(head) {\n  // Initialize pointers\n  let prev = null;\n  let curr = head;\n}",
      "action": "continue",
      "next": "coding-while-loop-js"
    },
    {
      "stepId": "coding-while-loop-js",
      "mentorSays": "Now, let's create a while loop that continues as long as curr is not null (meaning there are still nodes to process).",
      "example": "function reverseList(head) {\n  let prev = null;\n  let curr = head;\n  \n  // Continue while there are nodes to process\n  while (curr !== null) {\n    // Reverse logic here\n  }\n}",
      "action": "continue",
      "next": "coding-save-next-js"
    },
    {
      "stepId": "coding-save-next-js",
      "mentorSays": "Inside the loop, first save the next node before we modify curr.next (otherwise we'll lose the reference!).",
      "example": "function reverseList(head) {\n  let prev = null;\n  let curr = head;\n  \n  while (curr !== null) {\n    // Save next node before modifying curr.next\n    let next = curr.next;\n  }\n}",
      "action": "continue",
      "next": "coding-reverse-link-js"
    },
    {
      "stepId": "coding-reverse-link-js",
      "mentorSays": "Now, reverse the link: make curr.next point to prev (the previous node).",
      "example": "function reverseList(head) {\n  let prev = null;\n  let curr = head;\n  \n  while (curr !== null) {\n    let next = curr.next;\n    \n    // Reverse the link\n    curr.next = prev;\n  }\n}",
      "action": "continue",
      "next": "coding-move-pointers-js"
    },
    {
      "stepId": "coding-move-pointers-js",
      "mentorSays": "Finally, move both pointers forward: prev becomes curr, and curr becomes next.",
      "example": "function reverseList(head) {\n  let prev = null;\n  let curr = head;\n  \n  while (curr !== null) {\n    let next = curr.next;\n    curr.next = prev;\n    \n    // Move pointers forward\n    prev = curr;\n    curr = next;\n  }\n  \n  // Return the new head (prev is now the last node we processed)\n  return prev;\n}",
      "action": "continue",
      "next": "test-code-js"
    },
    {
      "stepId": "test-code-js",
      "mentorSays": "Perfect! Now test your code with the example we used earlier:\n\nInput: 1 -> 2 -> 3 -> 4 -> 5 -> null\n\nExpected output: 5 -> 4 -> 3 -> 2 -> 1 -> null\n\nTry it in your IDE or code editor!",
      "example": "Test input:\n1 -> 2 -> 3 -> 4 -> 5 -> null\n\nExpected output:\n5 -> 4 -> 3 -> 2 -> 1 -> null\n\nAll links are reversed!",
      "action": "continue",
      "next": "final"
    },
    {
      "stepId": "variable-check-python",
      "mentorSays": "Before we start coding, let me ask: Do you know what a variable is in Python?",
      "choices": [
        {
          "label": "Yes, I know variables",
          "next": "function-check-python"
        },
        {
          "label": "No, explain variables",
          "next": "variable-explanation-python"
        }
      ]
    },
    {
      "stepId": "variable-explanation-python",
      "mentorSays": "A variable is like a labeled box where you can store a value. You give it a name (like 'prev' or 'curr') and put data inside it.\n\nFor example, 'prev = None' creates a variable that stores None. Later, you can use that variable name to refer to it.",
      "example": "prev = None\ncurr = head\n\nNow these variables store node references\nWe can use them to track our position in the list",
      "action": "continue",
      "next": "function-check-python"
    },
    {
      "stepId": "function-check-python",
      "mentorSays": "Before we start coding, let me ask: Do you know what a function is in Python?",
      "choices": [
        {
          "label": "Yes, I know functions",
          "next": "parameter-check-python"
        },
        {
          "label": "No, explain functions",
          "next": "function-explanation-python"
        }
      ]
    },
    {
      "stepId": "function-explanation-python",
      "mentorSays": "A function is a reusable block of code. You give it inputs (called parameters), and it can return an output.\n\nThink of it like a recipe: you provide ingredients (parameters), the function does the work, and gives you a result (return value).",
      "example": "def add(a, b):\n    return a + b\n\nadd(3, 5) returns 8\nadd(10, 20) returns 30",
      "action": "continue",
      "next": "parameter-check-python"
    },
    {
      "stepId": "parameter-check-python",
      "mentorSays": "When we write a function, we can give it inputs. These inputs are called 'parameters'. Do you know what parameters are?",
      "choices": [
        {
          "label": "Yes, I know parameters",
          "next": "linked-list-check-python"
        },
        {
          "label": "No, explain parameters",
          "next": "parameter-explanation-python"
        }
      ]
    },
    {
      "stepId": "parameter-explanation-python",
      "mentorSays": "Parameters are like variables that receive values when you call the function. When you write 'def reverse_list(head):', the 'head' is a parameter - it's a placeholder that will receive the head node of the linked list when you call the function.",
      "example": "def reverse_list(head):\n  # 'head' is a parameter\n\n# When we call it:\nreverse_list(head_node)\n# The head node goes into 'head'",
      "action": "continue",
      "next": "linked-list-check-python"
    },
    {
      "stepId": "linked-list-check-python",
      "mentorSays": "To solve this lesson, we'll need to work with linked lists. Do you know what a linked list is?",
      "choices": [
        {
          "label": "Yes, I know linked lists",
          "next": "loop-check-python"
        },
        {
          "label": "No, explain linked lists",
          "next": "linked-list-explanation-python"
        }
      ]
    },
    {
      "stepId": "linked-list-explanation-python",
      "mentorSays": "A linked list is a data structure where each element (node) contains:\n- A value (data)\n- A reference to the next node\n\nIn Python, a node is typically a class instance with 'val' and 'next' attributes.\n\nThe list: 1 -> 2 -> 3 means:\n- Node 1 has value 1 and points to node 2\n- Node 2 has value 2 and points to node 3\n- Node 3 has value 3 and points to None (end of list)",
      "example": "class ListNode:\n    def __init__(self, val=0, next=None):\n        self.val = val\n        self.next = next\n\n# Create nodes\nnode1 = ListNode(1)\nnode2 = ListNode(2)\nnode3 = ListNode(3)\n\n# Link them\nnode1.next = node2\nnode2.next = node3\n\n# Result: 1 -> 2 -> 3 -> None",
      "action": "continue",
      "next": "loop-check-python"
    },
    {
      "stepId": "loop-check-python",
      "mentorSays": "To solve this lesson, we'll need to use loops. Do you know what a loop is in Python?",
      "choices": [
        {
          "label": "Yes, I know loops",
          "next": "coding-start-python"
        },
        {
          "label": "No, explain loops",
          "next": "loop-explanation-python"
        }
      ]
    },
    {
      "stepId": "loop-explanation-python",
      "mentorSays": "A loop lets you repeat code multiple times. A 'while' loop in Python runs code repeatedly as long as a condition is true.\n\nFor example, 'while curr is not None:' means: keep running the code as long as curr is not None.",
      "example": "i = 0\nwhile i < 3:\n    print(i)\n    i += 1\n\nThis prints: 0, 1, 2",
      "action": "continue",
      "next": "coding-start-python"
    },
    {
      "stepId": "coding-start-python",
      "mentorSays": "Perfect! Let's implement the reverse linked list solution in Python. We'll create a function that takes the head of a linked list and returns the head of the reversed list.",
      "example": "def reverse_list(head):\n    \n    pass",
      "action": "continue",
      "next": "coding-init-pointers-python"
    },
    {
      "stepId": "coding-init-pointers-python",
      "mentorSays": "First, let's initialize our pointers: prev (previous node) starts as None, and curr (current node) starts as head.",
      "example": "def reverse_list(head):\n    # Initialize pointers\n    prev = None\n    curr = head\n    \n    pass",
      "action": "continue",
      "next": "coding-while-loop-python"
    },
    {
      "stepId": "coding-while-loop-python",
      "mentorSays": "Now, let's create a while loop that continues as long as curr is not None (meaning there are still nodes to process).",
      "example": "def reverse_list(head):\n    prev = None\n    curr = head\n    \n    # Continue while there are nodes to process\n    while curr is not None:\n        # Reverse logic here\n        pass",
      "action": "continue",
      "next": "coding-save-next-python"
    },
    {
      "stepId": "coding-save-next-python",
      "mentorSays": "Inside the loop, first save the next node before we modify curr.next (otherwise we'll lose the reference!).",
      "example": "def reverse_list(head):\n    prev = None\n    curr = head\n    \n    while curr is not None:\n        # Save next node before modifying curr.next\n        next_node = curr.next\n        \n        pass",
      "action": "continue",
      "next": "coding-reverse-link-python"
    },
    {
      "stepId": "coding-reverse-link-python",
      "mentorSays": "Now, reverse the link: make curr.next point to prev (the previous node).",
      "example": "def reverse_list(head):\n    prev = None\n    curr = head\n    \n    while curr is not None:\n        next_node = curr.next\n        \n        # Reverse the link\n        curr.next = prev\n        \n        pass",
      "action": "continue",
      "next": "coding-move-pointers-python"
    },
    {
      "stepId": "coding-move-pointers-python",
      "mentorSays": "Finally, move both pointers forward: prev becomes curr, and curr becomes next_node.",
      "example": "def reverse_list(head):\n    prev = None\n    curr = head\n    \n    while curr is not None:\n        next_node = curr.next\n        curr.next = prev\n        \n        # Move pointers forward\n        prev = curr\n        curr = next_node\n    \n    # Return the new head (prev is now the last node we processed)\n    return prev",
      "action": "continue",
      "next": "test-code-python"
    },
    {
      "stepId": "test-code-python",
      "mentorSays": "Perfect! Now test your code with the example we used earlier:\n\nInput: 1 -> 2 -> 3 -> 4 -> 5 -> None\n\nExpected output: 5 -> 4 -> 3 -> 2 -> 1 -> None\n\nTry it in your IDE or code editor!",
      "example": "Test input:\n1 -> 2 -> 3 -> 4 -> 5 -> None\n\nExpected output:\n5 -> 4 -> 3 -> 2 -> 1 -> None\n\nAll links are reversed!",
      "action": "continue",
      "next": "final"
    },
    {
      "stepId": "variable-check-java",
      "mentorSays": "Before we start coding, let me ask: Do you know what a variable is in Java?",
      "choices": [
        {
          "label": "Yes, I know variables",
          "next": "function-check-java"
        },
        {
          "label": "No, explain variables",
          "next": "variable-explanation-java"
        }
      ]
    },
    {
      "stepId": "variable-explanation-java",
      "mentorSays": "A variable is like a labeled box where you can store a value. You give it a name (like 'prev' or 'curr') and put data inside it.\n\nFor example, 'ListNode prev = null;' creates a variable that stores null. Later, you can use that variable name to refer to it.",
      "example": "ListNode prev = null;\nListNode curr = head;\n\nNow these variables store node references\nWe can use them to track our position in the list",
      "action": "continue",
      "next": "function-check-java"
    },
    {
      "stepId": "function-check-java",
      "mentorSays": "Before we start coding, let me ask: Do you know what a method is in Java?",
      "choices": [
        {
          "label": "Yes, I know methods",
          "next": "parameter-check-java"
        },
        {
          "label": "No, explain methods",
          "next": "function-explanation-java"
        }
      ]
    },
    {
      "stepId": "function-explanation-java",
      "mentorSays": "A method (Java's version of a function) is a reusable block of code. You give it inputs (called parameters), and it can return an output.\n\nThink of it like a recipe: you provide ingredients (parameters), the method does the work, and gives you a result (return value).",
      "example": "public int add(int a, int b) {\n    return a + b;\n}\n\nadd(3, 5) returns 8\nadd(10, 20) returns 30",
      "action": "continue",
      "next": "parameter-check-java"
    },
    {
      "stepId": "parameter-check-java",
      "mentorSays": "When we write a method, we can give it inputs. These inputs are called 'parameters'. Do you know what parameters are?",
      "choices": [
        {
          "label": "Yes, I know parameters",
          "next": "linked-list-check-java"
        },
        {
          "label": "No, explain parameters",
          "next": "parameter-explanation-java"
        }
      ]
    },
    {
      "stepId": "parameter-explanation-java",
      "mentorSays": "Parameters are like variables that receive values when you call the method. When you write 'public ListNode reverseList(ListNode head)', the 'head' is a parameter - it's a placeholder that will receive the head node of the linked list when you call the method.",
      "example": "public ListNode reverseList(ListNode head) {\n  // 'head' is a parameter\n}\n\n// When we call it:\nreverseList(headNode);\n// The head node goes into 'head'",
      "action": "continue",
      "next": "linked-list-check-java"
    },
    {
      "stepId": "linked-list-check-java",
      "mentorSays": "To solve this lesson, we'll need to work with linked lists. Do you know what a linked list is?",
      "choices": [
        {
          "label": "Yes, I know linked lists",
          "next": "loop-check-java"
        },
        {
          "label": "No, explain linked lists",
          "next": "linked-list-explanation-java"
        }
      ]
    },
    {
      "stepId": "linked-list-explanation-java",
      "mentorSays": "A linked list is a data structure where each element (node) contains:\n- A value (data)\n- A reference to the next node\n\nIn Java, a node is typically a class with 'val' and 'next' fields.\n\nThe list: 1 -> 2 -> 3 means:\n- Node 1 has value 1 and points to node 2\n- Node 2 has value 2 and points to node 3\n- Node 3 has value 3 and points to null (end of list)",
      "example": "class ListNode {\n    int val;\n    ListNode next;\n    ListNode(int x) { val = x; }\n}\n\n// Create nodes\nListNode node1 = new ListNode(1);\nListNode node2 = new ListNode(2);\nListNode node3 = new ListNode(3);\n\n// Link them\nnode1.next = node2;\nnode2.next = node3;\n\n// Result: 1 -> 2 -> 3 -> null",
      "action": "continue",
      "next": "loop-check-java"
    },
    {
      "stepId": "loop-check-java",
      "mentorSays": "To solve this lesson, we'll need to use loops. Do you know what a loop is in Java?",
      "choices": [
        {
          "label": "Yes, I know loops",
          "next": "coding-start-java"
        },
        {
          "label": "No, explain loops",
          "next": "loop-explanation-java"
        }
      ]
    },
    {
      "stepId": "loop-explanation-java",
      "mentorSays": "A loop lets you repeat code multiple times. A 'while' loop runs code repeatedly as long as a condition is true.\n\nFor example, 'while (curr != null)' means: keep running the code as long as curr is not null.",
      "example": "int i = 0;\nwhile (i < 3) {\n    System.out.println(i);\n    i++;\n}\n\nThis prints: 0, 1, 2",
      "action": "continue",
      "next": "coding-start-java"
    },
    {
      "stepId": "coding-start-java",
      "mentorSays": "Perfect! Let's implement the reverse linked list solution in Java. We'll create a method that takes the head of a linked list and returns the head of the reversed list.",
      "example": "public ListNode reverseList(ListNode head) {\n    \n}",
      "action": "continue",
      "next": "coding-init-pointers-java"
    },
    {
      "stepId": "coding-init-pointers-java",
      "mentorSays": "First, let's initialize our pointers: prev (previous node) starts as null, and curr (current node) starts as head.",
      "example": "public ListNode reverseList(ListNode head) {\n    // Initialize pointers\n    ListNode prev = null;\n    ListNode curr = head;\n}",
      "action": "continue",
      "next": "coding-while-loop-java"
    },
    {
      "stepId": "coding-while-loop-java",
      "mentorSays": "Now, let's create a while loop that continues as long as curr is not null (meaning there are still nodes to process).",
      "example": "public ListNode reverseList(ListNode head) {\n    ListNode prev = null;\n    ListNode curr = head;\n    \n    // Continue while there are nodes to process\n    while (curr != null) {\n        // Reverse logic here\n    }\n}",
      "action": "continue",
      "next": "coding-save-next-java"
    },
    {
      "stepId": "coding-save-next-java",
      "mentorSays": "Inside the loop, first save the next node before we modify curr.next (otherwise we'll lose the reference!).",
      "example": "public ListNode reverseList(ListNode head) {\n    ListNode prev = null;\n    ListNode curr = head;\n    \n    while (curr != null) {\n        // Save next node before modifying curr.next\n        ListNode next = curr.next;\n    }\n}",
      "action": "continue",
      "next": "coding-reverse-link-java"
    },
    {
      "stepId": "coding-reverse-link-java",
      "mentorSays": "Now, reverse the link: make curr.next point to prev (the previous node).",
      "example": "public ListNode reverseList(ListNode head) {\n    ListNode prev = null;\n    ListNode curr = head;\n    \n    while (curr != null) {\n        ListNode next = curr.next;\n        \n        // Reverse the link\n        curr.next = prev;\n    }\n}",
      "action": "continue",
      "next": "coding-move-pointers-java"
    },
    {
      "stepId": "coding-move-pointers-java",
      "mentorSays": "Finally, move both pointers forward: prev becomes curr, and curr becomes next.",
      "example": "public ListNode reverseList(ListNode head) {\n    ListNode prev = null;\n    ListNode curr = head;\n    \n    while (curr != null) {\n        ListNode next = curr.next;\n        curr.next = prev;\n        \n        // Move pointers forward\n        prev = curr;\n        curr = next;\n    }\n    \n    // Return the new head (prev is now the last node we processed)\n    return prev;\n}",
      "action": "continue",
      "next": "test-code-java"
    },
    {
      "stepId": "test-code-java",
      "mentorSays": "Perfect! Now test your code with the example we used earlier:\n\nInput: 1 -> 2 -> 3 -> 4 -> 5 -> null\n\nExpected output: 5 -> 4 -> 3 -> 2 -> 1 -> null\n\nTry it in your IDE or code editor!",
      "example": "Test input:\n1 -> 2 -> 3 -> 4 -> 5 -> null\n\nExpected output:\n5 -> 4 -> 3 -> 2 -> 1 -> null\n\nAll links are reversed!",
      "action": "continue",
      "next": "final"
    },
    {
      "stepId": "variable-check-cpp",
      "mentorSays": "Before we start coding, let me ask: Do you know what a variable is in C++?",
      "choices": [
        {
          "label": "Yes, I know variables",
          "next": "function-check-cpp"
        },
        {
          "label": "No, explain variables",
          "next": "variable-explanation-cpp"
        }
      ]
    },
    {
      "stepId": "variable-explanation-cpp",
      "mentorSays": "A variable is like a labeled box where you can store a value. You give it a name (like 'prev' or 'curr') and put data inside it.\n\nFor example, 'ListNode* prev = nullptr;' creates a variable that stores nullptr. Later, you can use that variable name to refer to it.",
      "example": "ListNode* prev = nullptr;\nListNode* curr = head;\n\nNow these variables store node pointers\nWe can use them to track our position in the list",
      "action": "continue",
      "next": "function-check-cpp"
    },
    {
      "stepId": "function-check-cpp",
      "mentorSays": "Before we start coding, let me ask: Do you know what a function is in C++?",
      "choices": [
        {
          "label": "Yes, I know functions",
          "next": "parameter-check-cpp"
        },
        {
          "label": "No, explain functions",
          "next": "function-explanation-cpp"
        }
      ]
    },
    {
      "stepId": "function-explanation-cpp",
      "mentorSays": "A function is a reusable block of code. You give it inputs (called parameters), and it can return an output.\n\nThink of it like a recipe: you provide ingredients (parameters), the function does the work, and gives you a result (return value).",
      "example": "int add(int a, int b) {\n    return a + b;\n}\n\nadd(3, 5) returns 8\nadd(10, 20) returns 30",
      "action": "continue",
      "next": "parameter-check-cpp"
    },
    {
      "stepId": "parameter-check-cpp",
      "mentorSays": "When we write a function, we can give it inputs. These inputs are called 'parameters'. Do you know what parameters are?",
      "choices": [
        {
          "label": "Yes, I know parameters",
          "next": "linked-list-check-cpp"
        },
        {
          "label": "No, explain parameters",
          "next": "parameter-explanation-cpp"
        }
      ]
    },
    {
      "stepId": "parameter-explanation-cpp",
      "mentorSays": "Parameters are like variables that receive values when you call the function. When you write 'ListNode* reverseList(ListNode* head)', the 'head' is a parameter - it's a placeholder that will receive the head node of the linked list when you call the function.",
      "example": "ListNode* reverseList(ListNode* head) {\n  // 'head' is a parameter\n}\n\n// When we call it:\nreverseList(headNode);\n// The head node goes into 'head'",
      "action": "continue",
      "next": "linked-list-check-cpp"
    },
    {
      "stepId": "linked-list-check-cpp",
      "mentorSays": "To solve this lesson, we'll need to work with linked lists. Do you know what a linked list is?",
      "choices": [
        {
          "label": "Yes, I know linked lists",
          "next": "loop-check-cpp"
        },
        {
          "label": "No, explain linked lists",
          "next": "linked-list-explanation-cpp"
        }
      ]
    },
    {
      "stepId": "linked-list-explanation-cpp",
      "mentorSays": "A linked list is a data structure where each element (node) contains:\n- A value (data)\n- A pointer to the next node\n\nIn C++, a node is typically a struct with 'val' and 'next' members.\n\nThe list: 1 -> 2 -> 3 means:\n- Node 1 has value 1 and points to node 2\n- Node 2 has value 2 and points to node 3\n- Node 3 has value 3 and points to nullptr (end of list)",
      "example": "struct ListNode {\n    int val;\n    ListNode* next;\n    ListNode(int x) : val(x), next(nullptr) {}\n};\n\n// Create nodes\nListNode* node1 = new ListNode(1);\nListNode* node2 = new ListNode(2);\nListNode* node3 = new ListNode(3);\n\n// Link them\nnode1->next = node2;\nnode2->next = node3;\n\n// Result: 1 -> 2 -> 3 -> nullptr",
      "action": "continue",
      "next": "loop-check-cpp"
    },
    {
      "stepId": "loop-check-cpp",
      "mentorSays": "To solve this lesson, we'll need to use loops. Do you know what a loop is in C++?",
      "choices": [
        {
          "label": "Yes, I know loops",
          "next": "coding-start-cpp"
        },
        {
          "label": "No, explain loops",
          "next": "loop-explanation-cpp"
        }
      ]
    },
    {
      "stepId": "loop-explanation-cpp",
      "mentorSays": "A loop lets you repeat code multiple times. A 'while' loop runs code repeatedly as long as a condition is true.\n\nFor example, 'while (curr != nullptr)' means: keep running the code as long as curr is not nullptr.",
      "example": "int i = 0;\nwhile (i < 3) {\n    cout << i << endl;\n    i++;\n}\n\nThis prints: 0, 1, 2",
      "action": "continue",
      "next": "coding-start-cpp"
    },
    {
      "stepId": "coding-start-cpp",
      "mentorSays": "Perfect! Let's implement the reverse linked list solution in C++. We'll create a function that takes the head of a linked list and returns the head of the reversed list.",
      "example": "ListNode* reverseList(ListNode* head) {\n    \n}",
      "action": "continue",
      "next": "coding-init-pointers-cpp"
    },
    {
      "stepId": "coding-init-pointers-cpp",
      "mentorSays": "First, let's initialize our pointers: prev (previous node) starts as nullptr, and curr (current node) starts as head.",
      "example": "ListNode* reverseList(ListNode* head) {\n    // Initialize pointers\n    ListNode* prev = nullptr;\n    ListNode* curr = head;\n}",
      "action": "continue",
      "next": "coding-while-loop-cpp"
    },
    {
      "stepId": "coding-while-loop-cpp",
      "mentorSays": "Now, let's create a while loop that continues as long as curr is not nullptr (meaning there are still nodes to process).",
      "example": "ListNode* reverseList(ListNode* head) {\n    ListNode* prev = nullptr;\n    ListNode* curr = head;\n    \n    // Continue while there are nodes to process\n    while (curr != nullptr) {\n        // Reverse logic here\n    }\n}",
      "action": "continue",
      "next": "coding-save-next-cpp"
    },
    {
      "stepId": "coding-save-next-cpp",
      "mentorSays": "Inside the loop, first save the next node before we modify curr->next (otherwise we'll lose the reference!).",
      "example": "ListNode* reverseList(ListNode* head) {\n    ListNode* prev = nullptr;\n    ListNode* curr = head;\n    \n    while (curr != nullptr) {\n        // Save next node before modifying curr->next\n        ListNode* next = curr->next;\n    }\n}",
      "action": "continue",
      "next": "coding-reverse-link-cpp"
    },
    {
      "stepId": "coding-reverse-link-cpp",
      "mentorSays": "Now, reverse the link: make curr->next point to prev (the previous node).",
      "example": "ListNode* reverseList(ListNode* head) {\n    ListNode* prev = nullptr;\n    ListNode* curr = head;\n    \n    while (curr != nullptr) {\n        ListNode* next = curr->next;\n        \n        // Reverse the link\n        curr->next = prev;\n    }\n}",
      "action": "continue",
      "next": "coding-move-pointers-cpp"
    },
    {
      "stepId": "coding-move-pointers-cpp",
      "mentorSays": "Finally, move both pointers forward: prev becomes curr, and curr becomes next.",
      "example": "ListNode* reverseList(ListNode* head) {\n    ListNode* prev = nullptr;\n    ListNode* curr = head;\n    \n    while (curr != nullptr) {\n        ListNode* next = curr->next;\n        curr->next = prev;\n        \n        // Move pointers forward\n        prev = curr;\n        curr = next;\n    }\n    \n    // Return the new head (prev is now the last node we processed)\n    return prev;\n}",
      "action": "continue",
      "next": "test-code-cpp"
    },
    {
      "stepId": "test-code-cpp",
      "mentorSays": "Perfect! Now test your code with the example we used earlier:\n\nInput: 1 -> 2 -> 3 -> 4 -> 5 -> nullptr\n\nExpected output: 5 -> 4 -> 3 -> 2 -> 1 -> nullptr\n\nTry it in your IDE or code editor!",
      "example": "Test input:\n1 -> 2 -> 3 -> 4 -> 5 -> nullptr\n\nExpected output:\n5 -> 4 -> 3 -> 2 -> 1 -> nullptr\n\nAll links are reversed!",
      "action": "continue",
      "next": "final"
    },
    {
      "stepId": "variable-check-ts",
      "mentorSays": "Before we start coding, let me ask: Do you know what a variable is in TypeScript?",
      "choices": [
        {
          "label": "Yes, I know variables",
          "next": "function-check-ts"
        },
        {
          "label": "No, explain variables",
          "next": "variable-explanation-ts"
        }
      ]
    },
    {
      "stepId": "variable-explanation-ts",
      "mentorSays": "A variable is like a labeled box where you can store a value. You give it a name (like 'prev' or 'curr') and put data inside it.\n\nFor example, 'let prev: ListNode | null = null;' creates a variable that stores null. Later, you can use that variable name to refer to it.",
      "example": "let prev: ListNode | null = null;\nlet curr: ListNode | null = head;\n\nNow these variables store node references\nWe can use them to track our position in the list",
      "action": "continue",
      "next": "function-check-ts"
    },
    {
      "stepId": "function-check-ts",
      "mentorSays": "Before we start coding, let me ask: Do you know what a function is in TypeScript?",
      "choices": [
        {
          "label": "Yes, I know functions",
          "next": "parameter-check-ts"
        },
        {
          "label": "No, explain functions",
          "next": "function-explanation-ts"
        }
      ]
    },
    {
      "stepId": "function-explanation-ts",
      "mentorSays": "A function is a reusable block of code. You give it inputs (called parameters), and it can return an output.\n\nThink of it like a recipe: you provide ingredients (parameters), the function does the work, and gives you a result (return value).",
      "example": "function add(a: number, b: number): number {\n  return a + b;\n}\n\nadd(3, 5) returns 8\nadd(10, 20) returns 30",
      "action": "continue",
      "next": "parameter-check-ts"
    },
    {
      "stepId": "parameter-check-ts",
      "mentorSays": "When we write a function, we can give it inputs. These inputs are called 'parameters'. Do you know what parameters are?",
      "choices": [
        {
          "label": "Yes, I know parameters",
          "next": "linked-list-check-ts"
        },
        {
          "label": "No, explain parameters",
          "next": "parameter-explanation-ts"
        }
      ]
    },
    {
      "stepId": "parameter-explanation-ts",
      "mentorSays": "Parameters are like variables that receive values when you call the function. When you write 'function reverseList(head: ListNode | null): ListNode | null', the 'head' is a parameter - it's a placeholder that will receive the head node of the linked list when you call the function.",
      "example": "function reverseList(head: ListNode | null): ListNode | null {\n  // 'head' is a parameter\n}\n\n// When we call it:\nreverseList(headNode);\n// The head node goes into 'head'",
      "action": "continue",
      "next": "linked-list-check-ts"
    },
    {
      "stepId": "linked-list-check-ts",
      "mentorSays": "To solve this lesson, we'll need to work with linked lists. Do you know what a linked list is?",
      "choices": [
        {
          "label": "Yes, I know linked lists",
          "next": "loop-check-ts"
        },
        {
          "label": "No, explain linked lists",
          "next": "linked-list-explanation-ts"
        }
      ]
    },
    {
      "stepId": "linked-list-explanation-ts",
      "mentorSays": "A linked list is a data structure where each element (node) contains:\n- A value (data)\n- A reference to the next node\n\nIn TypeScript, a node is typically a class or interface with 'val' and 'next' properties.\n\nThe list: 1 -> 2 -> 3 means:\n- Node 1 has value 1 and points to node 2\n- Node 2 has value 2 and points to node 3\n- Node 3 has value 3 and points to null (end of list)",
      "example": "class ListNode {\n    val: number;\n    next: ListNode | null;\n    constructor(val?: number, next?: ListNode | null) {\n        this.val = val ?? 0;\n        this.next = next ?? null;\n    }\n}\n\n// Create nodes\nlet node1 = new ListNode(1);\nlet node2 = new ListNode(2);\nlet node3 = new ListNode(3);\n\n// Link them\nnode1.next = node2;\nnode2.next = node3;\n\n// Result: 1 -> 2 -> 3 -> null",
      "action": "continue",
      "next": "loop-check-ts"
    },
    {
      "stepId": "loop-check-ts",
      "mentorSays": "To solve this lesson, we'll need to use loops. Do you know what a loop is in TypeScript?",
      "choices": [
        {
          "label": "Yes, I know loops",
          "next": "coding-start-ts"
        },
        {
          "label": "No, explain loops",
          "next": "loop-explanation-ts"
        }
      ]
    },
    {
      "stepId": "loop-explanation-ts",
      "mentorSays": "A loop lets you repeat code multiple times. A 'while' loop runs code repeatedly as long as a condition is true.\n\nFor example, 'while (curr !== null)' means: keep running the code as long as curr is not null.",
      "example": "let i = 0;\nwhile (i < 3) {\n  console.log(i);\n  i++;\n}\n\nThis prints: 0, 1, 2",
      "action": "continue",
      "next": "coding-start-ts"
    },
    {
      "stepId": "coding-start-ts",
      "mentorSays": "Perfect! Let's implement the reverse linked list solution in TypeScript. We'll create a function that takes the head of a linked list and returns the head of the reversed list.",
      "example": "function reverseList(head: ListNode | null): ListNode | null {\n  \n}",
      "action": "continue",
      "next": "coding-init-pointers-ts"
    },
    {
      "stepId": "coding-init-pointers-ts",
      "mentorSays": "First, let's initialize our pointers: prev (previous node) starts as null, and curr (current node) starts as head.",
      "example": "function reverseList(head: ListNode | null): ListNode | null {\n  // Initialize pointers\n  let prev: ListNode | null = null;\n  let curr: ListNode | null = head;\n}",
      "action": "continue",
      "next": "coding-while-loop-ts"
    },
    {
      "stepId": "coding-while-loop-ts",
      "mentorSays": "Now, let's create a while loop that continues as long as curr is not null (meaning there are still nodes to process).",
      "example": "function reverseList(head: ListNode | null): ListNode | null {\n  let prev: ListNode | null = null;\n  let curr: ListNode | null = head;\n  \n  // Continue while there are nodes to process\n  while (curr !== null) {\n    // Reverse logic here\n  }\n}",
      "action": "continue",
      "next": "coding-save-next-ts"
    },
    {
      "stepId": "coding-save-next-ts",
      "mentorSays": "Inside the loop, first save the next node before we modify curr.next (otherwise we'll lose the reference!).",
      "example": "function reverseList(head: ListNode | null): ListNode | null {\n  let prev: ListNode | null = null;\n  let curr: ListNode | null = head;\n  \n  while (curr !== null) {\n    // Save next node before modifying curr.next\n    let next: ListNode | null = curr.next;\n  }\n}",
      "action": "continue",
      "next": "coding-reverse-link-ts"
    },
    {
      "stepId": "coding-reverse-link-ts",
      "mentorSays": "Now, reverse the link: make curr.next point to prev (the previous node).",
      "example": "function reverseList(head: ListNode | null): ListNode | null {\n  let prev: ListNode | null = null;\n  let curr: ListNode | null = head;\n  \n  while (curr !== null) {\n    let next: ListNode | null = curr.next;\n    \n    // Reverse the link\n    curr.next = prev;\n  }\n}",
      "action": "continue",
      "next": "coding-move-pointers-ts"
    },
    {
      "stepId": "coding-move-pointers-ts",
      "mentorSays": "Finally, move both pointers forward: prev becomes curr, and curr becomes next.",
      "example": "function reverseList(head: ListNode | null): ListNode | null {\n  let prev: ListNode | null = null;\n  let curr: ListNode | null = head;\n  \n  while (curr !== null) {\n    let next: ListNode | null = curr.next;\n    curr.next = prev;\n    \n    // Move pointers forward\n    prev = curr;\n    curr = next;\n  }\n  \n  // Return the new head (prev is now the last node we processed)\n  return prev;\n}",
      "action": "continue",
      "next": "test-code-ts"
    },
    {
      "stepId": "test-code-ts",
      "mentorSays": "Perfect! Now test your code with the example we used earlier:\n\nInput: 1 -> 2 -> 3 -> 4 -> 5 -> null\n\nExpected output: 5 -> 4 -> 3 -> 2 -> 1 -> null\n\nTry it in your IDE or code editor!",
      "example": "Test input:\n1 -> 2 -> 3 -> 4 -> 5 -> null\n\nExpected output:\n5 -> 4 -> 3 -> 2 -> 1 -> null\n\nAll links are reversed!",
      "action": "continue",
      "next": "final"
    },
    {
      "stepId": "final",
      "mentorSays": "🎉 Well done! You've learned to think through the Reverse Linked List lesson. You understand what a linked list is, how to use pointers to reverse links, and how to implement it in O(n) time and O(1) space.\n\nThis kind of thinking—manipulating pointers to reverse data structures—is essential for solving many linked list lessons. Keep practicing!",
      "action": "continue"
    }
  ]
}


