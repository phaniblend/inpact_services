{
  "id": "longest-consecutive-sequence",
  "title": "Longest consecutive sequence",
  "pattern": "hash-map",
  "difficulty": "medium",
  "language": "javascript",
  "status": "draft",
  "flow": [
    {
      "stepId": "title",
      "mentorSays": "At the end of this lesson, you will be able to:\n\n1. Understand what the Longest Consecutive Sequence lesson asks for\n2. Use hash sets to efficiently find consecutive sequences\n3. Identify sequence starts and count consecutive elements\n4. Implement the solution step by step in your chosen language",
      "action": "continue",
      "next": "lesson-illustration"
    },
    {
      "stepId": "lesson-illustration",
      "mentorSays": "Perfect! Now let's understand what the Longest Consecutive Sequence lesson is asking.\n\nGiven an unsorted array of integers, find the length of the longest consecutive elements sequence.\n\nExample: nums = [100, 4, 200, 1, 3, 2]\n\nLet's find consecutive sequences:\n- 100: no consecutive neighbors → sequence length = 1\n- 4: has 3 (before) and potentially others → let's check\n- 200: no consecutive neighbors → sequence length = 1\n- 1: has 2, 3, 4 as consecutive → sequence [1, 2, 3, 4] with length 4\n- 3: part of [1, 2, 3, 4]\n- 2: part of [1, 2, 3, 4]\n\nThe longest consecutive sequence is [1, 2, 3, 4] with length 4.\n\nAnother example: nums = [0, 3, 7, 2, 5, 8, 4, 6, 0, 1]\n- Consecutive sequences: [0, 1, 2, 3, 4, 5, 6, 7, 8] → length 9\n\nThat's what the lesson wants: return the length of the longest consecutive sequence.",
      "example": "nums = [100, 4, 200, 1, 3, 2]\n\nFind consecutive sequences:\n- 100: alone → length 1\n- 200: alone → length 1\n- [1, 2, 3, 4]: consecutive → length 4\n\nAnswer: 4\n\nAnother example:\nnums = [0, 3, 7, 2, 5, 8, 4, 6, 0, 1]\n\nConsecutive sequence: [0, 1, 2, 3, 4, 5, 6, 7, 8]\n\nAnswer: 9",
      "action": "continue",
      "next": "thinking-challenge"
    },
    {
      "stepId": "thinking-challenge",
      "mentorSays": "Now that you understand what the lesson wants, here's the real question:\n\nHow would YOU find the longest consecutive sequence?\n\nThink about it for a moment. What approach feels natural to you?",
      "choices": [
        {
          "label": "Sort the array and find consecutive sequences",
          "next": "explore-sort"
        },
        {
          "label": "Use a hash set and check for sequence starts",
          "next": "explore-hashset"
        },
        {
          "label": "Check each number and count consecutive neighbors",
          "next": "explore-brute-force"
        }
      ]
    },
    {
      "stepId": "explore-sort",
      "mentorSays": "That's a straightforward approach! You're thinking: \"If I sort the array, consecutive numbers will be adjacent.\"\n\nFor [100, 4, 200, 1, 3, 2] sorted → [1, 2, 3, 4, 100, 200]:\n- Check consecutive: [1, 2, 3, 4] → length 4 ✓\n\nThis works! However, sorting takes O(n log n) time.\n\nWould you like to learn the hash set approach which is O(n) time?",
      "action": "continue",
      "next": "explore-hashset"
    },
    {
      "stepId": "explore-brute-force",
      "mentorSays": "That's a good approach! However, checking each number and counting neighbors can be inefficient. The hash set approach is more elegant and efficient.\n\nWould you like to learn it?",
      "action": "continue",
      "next": "explore-hashset"
    },
    {
      "stepId": "explore-hashset",
      "mentorSays": "Excellent choice! The hash set approach is efficient and clever. Here's the core idea:\n\n1. **Put all numbers in a hash set**: This gives us O(1) lookup\n2. **Find sequence starts**: For each number, check if num-1 exists in the set. If it doesn't, this number is the start of a sequence!\n3. **Count consecutive elements**: Starting from a sequence start, count how many consecutive numbers exist (num, num+1, num+2, ...)\n4. **Track maximum**: Keep track of the longest sequence found\n\nLet's trace with [100, 4, 200, 1, 3, 2]:\n\n- **Create set**: {100, 4, 200, 1, 3, 2}\n- **Check 100**: 99 not in set → 100 is a start! Count: 100 → length 1\n- **Check 4**: 3 in set → 4 is NOT a start, skip\n- **Check 200**: 199 not in set → 200 is a start! Count: 200 → length 1\n- **Check 1**: 0 not in set → 1 is a start! Count: 1, 2, 3, 4 → length 4\n- **Check 3**: 2 in set → 3 is NOT a start, skip\n- **Check 2**: 1 in set → 2 is NOT a start, skip\n- **Maximum**: 4\n\nThis is O(n) time! Now let's code it step by step.",
      "action": "continue",
      "next": "language-selection"
    },
    {
      "stepId": "language-selection",
      "mentorSays": "Great! Now let's code this solution. Which programming language would you like to use?",
      "choices": [
        {
          "label": "JavaScript",
          "next": "variable-check-js"
        },
        {
          "label": "Python",
          "next": "variable-check-python"
        },
        {
          "label": "Java",
          "next": "variable-check-java"
        },
        {
          "label": "C++",
          "next": "variable-check-cpp"
        },
        {
          "label": "TypeScript",
          "next": "variable-check-ts"
        }
      ]
    },
    {
      "stepId": "variable-check-js",
      "mentorSays": "Before we start coding, let me ask: Do you know what a variable is in JavaScript?",
      "choices": [
        {
          "label": "Yes, I know variables",
          "next": "function-check-js"
        },
        {
          "label": "No, explain variables",
          "next": "variable-explanation-js"
        }
      ]
    },
    {
      "stepId": "variable-explanation-js",
      "mentorSays": "A variable is like a labeled box where you can store a value. You give it a name (like 'numSet' or 'maxLen') and put data inside it.\n\nFor example, 'let numSet = new Set();' creates a variable that stores a Set (hash set). Later, you can use that variable name to refer to it.",
      "example": "let numSet = new Set();\n\nNow the variable refers to an empty set\nWe can use the variable name to access this set",
      "action": "continue",
      "next": "function-check-js"
    },
    {
      "stepId": "function-check-js",
      "mentorSays": "Before we start coding, let me ask: Do you know what a function is in JavaScript?",
      "choices": [
        {
          "label": "Yes, I know functions",
          "next": "parameter-check-js"
        },
        {
          "label": "No, explain functions",
          "next": "function-explanation-js"
        }
      ]
    },
    {
      "stepId": "function-explanation-js",
      "mentorSays": "A function is a reusable block of code. You give it inputs (called parameters), and it can return an output.\n\nThink of it like a recipe: you provide ingredients (parameters), the function does the work, and gives you a result (return value).",
      "example": "function add(a, b) {\n  return a + b;\n}\n\nadd(3, 5) returns 8\nadd(10, 20) returns 30",
      "action": "continue",
      "next": "parameter-check-js"
    },
    {
      "stepId": "parameter-check-js",
      "mentorSays": "When we write a function, we can give it inputs. These inputs are called 'parameters'. Do you know what parameters are?",
      "choices": [
        {
          "label": "Yes, I know parameters",
          "next": "loop-check-js"
        },
        {
          "label": "No, explain parameters",
          "next": "parameter-explanation-js"
        }
      ]
    },
    {
      "stepId": "parameter-explanation-js",
      "mentorSays": "Parameters are like variables that receive values when you call the function. When you write 'function longestConsecutive(nums)', the 'nums' is a parameter - it's a placeholder that will receive an actual array when you call the function.\n\nWhen you call 'longestConsecutive([100, 4, 200, 1, 3, 2])', the array goes into 'nums'.",
      "example": "function longestConsecutive(nums) {\n  // 'nums' is a parameter\n}\n\n// When we call it:\nlongestConsecutive([100, 4, 200, 1, 3, 2]);\n// The array goes into 'nums'",
      "action": "continue",
      "next": "loop-check-js"
    },
    {
      "stepId": "loop-check-js",
      "mentorSays": "To solve this lesson, we'll need to use loops. Do you know what a loop is in JavaScript?",
      "choices": [
        {
          "label": "Yes, I know loops",
          "next": "coding-start-js"
        },
        {
          "label": "No, explain loops",
          "next": "loop-explanation-js"
        }
      ]
    },
    {
      "stepId": "loop-explanation-js",
      "mentorSays": "A loop lets you repeat code multiple times. A 'for...of' loop runs code for each item in a sequence.\n\nFor example, 'for (let num of nums)' means: run the code for each number in the array nums.",
      "example": "for (let num of [1, 2, 3]) {\n  console.log(num);\n}\n\nThis prints: 1, 2, 3",
      "action": "continue",
      "next": "coding-start-js"
    },
    {
      "stepId": "coding-start-js",
      "mentorSays": "Perfect! Let's implement the longest consecutive sequence solution in JavaScript. We'll create a function that takes an array and returns the length of the longest consecutive sequence.",
      "example": "function longestConsecutive(nums) {\n  \n}",
      "action": "continue",
      "next": "coding-create-set-js"
    },
    {
      "stepId": "coding-create-set-js",
      "mentorSays": "First, let's create a Set and add all numbers to it. This gives us O(1) lookup.",
      "example": "function longestConsecutive(nums) {\n  // Create a set with all numbers\n  let numSet = new Set(nums);\n  // numSet = {100, 4, 200, 1, 3, 2} for [100, 4, 200, 1, 3, 2]\n}",
      "action": "continue",
      "next": "coding-init-max-js"
    },
    {
      "stepId": "coding-init-max-js",
      "mentorSays": "Now, let's initialize a variable to track the maximum consecutive sequence length.",
      "example": "function longestConsecutive(nums) {\n  let numSet = new Set(nums);\n  \n  // Track maximum consecutive sequence length\n  let maxLen = 0;\n}",
      "action": "continue",
      "next": "coding-loop-js"
    },
    {
      "stepId": "coding-loop-js",
      "mentorSays": "Now, let's iterate through each number in the set.",
      "example": "function longestConsecutive(nums) {\n  let numSet = new Set(nums);\n  let maxLen = 0;\n  \n  // Iterate through each number\n  for (let num of numSet) {\n    \n  }\n}",
      "action": "continue",
      "next": "coding-check-start-js"
    },
    {
      "stepId": "coding-check-start-js",
      "mentorSays": "For each number, check if it's the start of a sequence. A number is a start if num-1 is NOT in the set.",
      "example": "function longestConsecutive(nums) {\n  let numSet = new Set(nums);\n  let maxLen = 0;\n  \n  for (let num of numSet) {\n    // Check if this is the start of a sequence\n    if (!numSet.has(num - 1)) {\n      \n    }\n  }\n}",
      "action": "continue",
      "next": "coding-count-sequence-js"
    },
    {
      "stepId": "coding-count-sequence-js",
      "mentorSays": "If it's a start, count how long the consecutive sequence is by checking num+1, num+2, num+3, etc.",
      "example": "function longestConsecutive(nums) {\n  let numSet = new Set(nums);\n  let maxLen = 0;\n  \n  for (let num of numSet) {\n    if (!numSet.has(num - 1)) {\n      // Count consecutive sequence starting from num\n      let currentNum = num;\n      let currentLen = 1;\n      \n      while (numSet.has(currentNum + 1)) {\n        currentNum++;\n        currentLen++;\n      }\n    }\n  }\n}",
      "action": "continue",
      "next": "coding-update-max-js"
    },
    {
      "stepId": "coding-update-max-js",
      "mentorSays": "Update maxLen with the maximum of current maxLen and the current sequence length.",
      "example": "function longestConsecutive(nums) {\n  let numSet = new Set(nums);\n  let maxLen = 0;\n  \n  for (let num of numSet) {\n    if (!numSet.has(num - 1)) {\n      let currentNum = num;\n      let currentLen = 1;\n      \n      while (numSet.has(currentNum + 1)) {\n        currentNum++;\n        currentLen++;\n      }\n      \n      // Update maximum length\n      maxLen = Math.max(maxLen, currentLen);\n    }\n  }\n}",
      "action": "continue",
      "next": "coding-return-js"
    },
    {
      "stepId": "coding-return-js",
      "mentorSays": "Finally, return maxLen.",
      "example": "function longestConsecutive(nums) {\n  let numSet = new Set(nums);\n  let maxLen = 0;\n  \n  for (let num of numSet) {\n    if (!numSet.has(num - 1)) {\n      let currentNum = num;\n      let currentLen = 1;\n      \n      while (numSet.has(currentNum + 1)) {\n        currentNum++;\n        currentLen++;\n      }\n      \n      maxLen = Math.max(maxLen, currentLen);\n    }\n  }\n  \n  return maxLen;\n}",
      "action": "continue",
      "next": "test-code-js"
    },
    {
      "stepId": "test-code-js",
      "mentorSays": "Perfect! Now test your code with the example we used earlier:\n\nnums = [100, 4, 200, 1, 3, 2]\n\nExpected output: 4\n\nTry it in your IDE or code editor!",
      "example": "Test input:\nnums = [100, 4, 200, 1, 3, 2]\n\nExpected output: 4\n\nBecause the longest consecutive sequence is [1, 2, 3, 4] with length 4.",
      "action": "continue",
      "next": "final"
    },
    {
      "stepId": "variable-check-python",
      "mentorSays": "Before we start coding, let me ask: Do you know what a variable is in Python?",
      "choices": [
        {
          "label": "Yes, I know variables",
          "next": "function-check-python"
        },
        {
          "label": "No, explain variables",
          "next": "variable-explanation-python"
        }
      ]
    },
    {
      "stepId": "variable-explanation-python",
      "mentorSays": "A variable is like a labeled box where you can store a value. You give it a name (like 'numSet' or 'maxLen') and put data inside it.\n\nFor example, 'numSet = set(nums)' creates a variable that stores a set (hash set). Later, you can use that variable name to refer to it.",
      "example": "numSet = set()\n\nNow the variable refers to an empty set\nWe can use the variable name to access this set",
      "action": "continue",
      "next": "function-check-python"
    },
    {
      "stepId": "function-check-python",
      "mentorSays": "Before we start coding, let me ask: Do you know what a function is in Python?",
      "choices": [
        {
          "label": "Yes, I know functions",
          "next": "parameter-check-python"
        },
        {
          "label": "No, explain functions",
          "next": "function-explanation-python"
        }
      ]
    },
    {
      "stepId": "function-explanation-python",
      "mentorSays": "A function is a reusable block of code. You give it inputs (called parameters), and it can return an output.\n\nThink of it like a recipe: you provide ingredients (parameters), the function does the work, and gives you a result (return value).",
      "example": "def add(a, b):\n    return a + b\n\nadd(3, 5) returns 8\nadd(10, 20) returns 30",
      "action": "continue",
      "next": "parameter-check-python"
    },
    {
      "stepId": "parameter-check-python",
      "mentorSays": "When we write a function, we can give it inputs. These inputs are called 'parameters'. Do you know what parameters are?",
      "choices": [
        {
          "label": "Yes, I know parameters",
          "next": "loop-check-python"
        },
        {
          "label": "No, explain parameters",
          "next": "parameter-explanation-python"
        }
      ]
    },
    {
      "stepId": "parameter-explanation-python",
      "mentorSays": "Parameters are like variables that receive values when you call the function. When you write 'def longest_consecutive(nums):', the 'nums' is a parameter - it's a placeholder that will receive an actual list when you call the function.\n\nWhen you call 'longest_consecutive([100, 4, 200, 1, 3, 2])', the list goes into 'nums'.",
      "example": "def longest_consecutive(nums):\n  # 'nums' is a parameter\n\n# When we call it:\nlongest_consecutive([100, 4, 200, 1, 3, 2])\n# The list goes into 'nums'",
      "action": "continue",
      "next": "loop-check-python"
    },
    {
      "stepId": "loop-check-python",
      "mentorSays": "To solve this lesson, we'll need to use loops. Do you know what a loop is in Python?",
      "choices": [
        {
          "label": "Yes, I know loops",
          "next": "coding-start-python"
        },
        {
          "label": "No, explain loops",
          "next": "loop-explanation-python"
        }
      ]
    },
    {
      "stepId": "loop-explanation-python",
      "mentorSays": "A loop lets you repeat code multiple times. A 'for' loop in Python runs code for each item in a sequence.\n\nFor example, 'for num in nums:' means: run the code for each number in the list nums.",
      "example": "for num in [1, 2, 3]:\n    print(num)\n\nThis prints: 1, 2, 3",
      "action": "continue",
      "next": "coding-start-python"
    },
    {
      "stepId": "coding-start-python",
      "mentorSays": "Perfect! Let's implement the longest consecutive sequence solution in Python. We'll create a function that takes a list and returns the length of the longest consecutive sequence.",
      "example": "def longest_consecutive(nums):\n    \n    pass",
      "action": "continue",
      "next": "coding-create-set-python"
    },
    {
      "stepId": "coding-create-set-python",
      "mentorSays": "First, let's create a set and add all numbers to it. This gives us O(1) lookup.",
      "example": "def longest_consecutive(nums):\n    # Create a set with all numbers\n    numSet = set(nums)\n    # numSet = {100, 4, 200, 1, 3, 2} for [100, 4, 200, 1, 3, 2]\n    \n    pass",
      "action": "continue",
      "next": "coding-init-max-python"
    },
    {
      "stepId": "coding-init-max-python",
      "mentorSays": "Now, let's initialize a variable to track the maximum consecutive sequence length.",
      "example": "def longest_consecutive(nums):\n    numSet = set(nums)\n    \n    # Track maximum consecutive sequence length\n    maxLen = 0\n    \n    pass",
      "action": "continue",
      "next": "coding-loop-python"
    },
    {
      "stepId": "coding-loop-python",
      "mentorSays": "Now, let's iterate through each number in the set.",
      "example": "def longest_consecutive(nums):\n    numSet = set(nums)\n    maxLen = 0\n    \n    # Iterate through each number\n    for num in numSet:\n        \n    pass",
      "action": "continue",
      "next": "coding-check-start-python"
    },
    {
      "stepId": "coding-check-start-python",
      "mentorSays": "For each number, check if it's the start of a sequence. A number is a start if num-1 is NOT in the set.",
      "example": "def longest_consecutive(nums):\n    numSet = set(nums)\n    maxLen = 0\n    \n    for num in numSet:\n        # Check if this is the start of a sequence\n        if num - 1 not in numSet:\n            \n    pass",
      "action": "continue",
      "next": "coding-count-sequence-python"
    },
    {
      "stepId": "coding-count-sequence-python",
      "mentorSays": "If it's a start, count how long the consecutive sequence is by checking num+1, num+2, num+3, etc.",
      "example": "def longest_consecutive(nums):\n    numSet = set(nums)\n    maxLen = 0\n    \n    for num in numSet:\n        if num - 1 not in numSet:\n            # Count consecutive sequence starting from num\n            currentNum = num\n            currentLen = 1\n            \n            while currentNum + 1 in numSet:\n                currentNum += 1\n                currentLen += 1\n    \n    pass",
      "action": "continue",
      "next": "coding-update-max-python"
    },
    {
      "stepId": "coding-update-max-python",
      "mentorSays": "Update maxLen with the maximum of current maxLen and the current sequence length.",
      "example": "def longest_consecutive(nums):\n    numSet = set(nums)\n    maxLen = 0\n    \n    for num in numSet:\n        if num - 1 not in numSet:\n            currentNum = num\n            currentLen = 1\n            \n            while currentNum + 1 in numSet:\n                currentNum += 1\n                currentLen += 1\n            \n            # Update maximum length\n            maxLen = max(maxLen, currentLen)\n    \n    pass",
      "action": "continue",
      "next": "coding-return-python"
    },
    {
      "stepId": "coding-return-python",
      "mentorSays": "Finally, return maxLen.",
      "example": "def longest_consecutive(nums):\n    numSet = set(nums)\n    maxLen = 0\n    \n    for num in numSet:\n        if num - 1 not in numSet:\n            currentNum = num\n            currentLen = 1\n            \n            while currentNum + 1 in numSet:\n                currentNum += 1\n                currentLen += 1\n            \n            maxLen = max(maxLen, currentLen)\n    \n    return maxLen",
      "action": "continue",
      "next": "test-code-python"
    },
    {
      "stepId": "test-code-python",
      "mentorSays": "Perfect! Now test your code with the example we used earlier:\n\nnums = [100, 4, 200, 1, 3, 2]\n\nExpected output: 4\n\nTry it in your IDE or code editor!",
      "example": "Test input:\nnums = [100, 4, 200, 1, 3, 2]\n\nExpected output: 4\n\nBecause the longest consecutive sequence is [1, 2, 3, 4] with length 4.",
      "action": "continue",
      "next": "final"
    },
    {
      "stepId": "variable-check-java",
      "mentorSays": "Before we start coding, let me ask: Do you know what a variable is in Java?",
      "choices": [
        {
          "label": "Yes, I know variables",
          "next": "function-check-java"
        },
        {
          "label": "No, explain variables",
          "next": "variable-explanation-java"
        }
      ]
    },
    {
      "stepId": "variable-explanation-java",
      "mentorSays": "A variable is like a labeled box where you can store a value. You give it a name (like 'numSet' or 'maxLen') and put data inside it.\n\nFor example, 'Set<Integer> numSet = new HashSet<>();' creates a variable that stores a HashSet. Later, you can use that variable name to refer to it.",
      "example": "Set<Integer> numSet = new HashSet<>();\n\nNow the variable refers to an empty hash set\nWe can use the variable name to access this hash set",
      "action": "continue",
      "next": "function-check-java"
    },
    {
      "stepId": "function-check-java",
      "mentorSays": "Before we start coding, let me ask: Do you know what a method is in Java?",
      "choices": [
        {
          "label": "Yes, I know methods",
          "next": "parameter-check-java"
        },
        {
          "label": "No, explain methods",
          "next": "function-explanation-java"
        }
      ]
    },
    {
      "stepId": "function-explanation-java",
      "mentorSays": "A method (Java's version of a function) is a reusable block of code. You give it inputs (called parameters), and it can return an output.\n\nThink of it like a recipe: you provide ingredients (parameters), the method does the work, and gives you a result (return value).",
      "example": "public int add(int a, int b) {\n    return a + b;\n}\n\nadd(3, 5) returns 8\nadd(10, 20) returns 30",
      "action": "continue",
      "next": "parameter-check-java"
    },
    {
      "stepId": "parameter-check-java",
      "mentorSays": "When we write a method, we can give it inputs. These inputs are called 'parameters'. Do you know what parameters are?",
      "choices": [
        {
          "label": "Yes, I know parameters",
          "next": "loop-check-java"
        },
        {
          "label": "No, explain parameters",
          "next": "parameter-explanation-java"
        }
      ]
    },
    {
      "stepId": "parameter-explanation-java",
      "mentorSays": "Parameters are like variables that receive values when you call the method. When you write 'public int longestConsecutive(int[] nums)', the 'nums' is a parameter - it's a placeholder that will receive an actual array when you call the method.\n\nWhen you call 'longestConsecutive(new int[]{100, 4, 200, 1, 3, 2})', the array goes into 'nums'.",
      "example": "public int longestConsecutive(int[] nums) {\n  // 'nums' is a parameter\n}\n\n// When we call it:\nlongestConsecutive(new int[]{100, 4, 200, 1, 3, 2});\n// The array goes into 'nums'",
      "action": "continue",
      "next": "loop-check-java"
    },
    {
      "stepId": "loop-check-java",
      "mentorSays": "To solve this lesson, we'll need to use loops. Do you know what a loop is in Java?",
      "choices": [
        {
          "label": "Yes, I know loops",
          "next": "coding-start-java"
        },
        {
          "label": "No, explain loops",
          "next": "loop-explanation-java"
        }
      ]
    },
    {
      "stepId": "loop-explanation-java",
      "mentorSays": "A loop lets you repeat code multiple times. A 'for' loop runs code for each item in a sequence.\n\nFor example, 'for (int num : nums)' means: run the code for each number in the array nums.",
      "example": "for (int num : new int[]{1, 2, 3}) {\n    System.out.println(num);\n}\n\nThis prints: 1, 2, 3",
      "action": "continue",
      "next": "coding-start-java"
    },
    {
      "stepId": "coding-start-java",
      "mentorSays": "Perfect! Let's implement the longest consecutive sequence solution in Java. We'll create a method that takes an array and returns the length of the longest consecutive sequence.",
      "example": "public int longestConsecutive(int[] nums) {\n    \n}",
      "action": "continue",
      "next": "coding-create-set-java"
    },
    {
      "stepId": "coding-create-set-java",
      "mentorSays": "First, let's create a HashSet and add all numbers to it. This gives us O(1) lookup.",
      "example": "public int longestConsecutive(int[] nums) {\n    // Create a HashSet with all numbers\n    Set<Integer> numSet = new HashSet<>();\n    for (int num : nums) {\n        numSet.add(num);\n    }\n    // numSet = {100, 4, 200, 1, 3, 2} for [100, 4, 200, 1, 3, 2]\n}",
      "action": "continue",
      "next": "coding-init-max-java"
    },
    {
      "stepId": "coding-init-max-java",
      "mentorSays": "Now, let's initialize a variable to track the maximum consecutive sequence length.",
      "example": "public int longestConsecutive(int[] nums) {\n    Set<Integer> numSet = new HashSet<>();\n    for (int num : nums) {\n        numSet.add(num);\n    }\n    \n    // Track maximum consecutive sequence length\n    int maxLen = 0;\n}",
      "action": "continue",
      "next": "coding-loop-java"
    },
    {
      "stepId": "coding-loop-java",
      "mentorSays": "Now, let's iterate through each number in the set.",
      "example": "public int longestConsecutive(int[] nums) {\n    Set<Integer> numSet = new HashSet<>();\n    for (int num : nums) {\n        numSet.add(num);\n    }\n    \n    int maxLen = 0;\n    \n    // Iterate through each number\n    for (int num : numSet) {\n        \n    }\n}",
      "action": "continue",
      "next": "coding-check-start-java"
    },
    {
      "stepId": "coding-check-start-java",
      "mentorSays": "For each number, check if it's the start of a sequence. A number is a start if num-1 is NOT in the set.",
      "example": "public int longestConsecutive(int[] nums) {\n    Set<Integer> numSet = new HashSet<>();\n    for (int num : nums) {\n        numSet.add(num);\n    }\n    \n    int maxLen = 0;\n    \n    for (int num : numSet) {\n        // Check if this is the start of a sequence\n        if (!numSet.contains(num - 1)) {\n            \n        }\n    }\n}",
      "action": "continue",
      "next": "coding-count-sequence-java"
    },
    {
      "stepId": "coding-count-sequence-java",
      "mentorSays": "If it's a start, count how long the consecutive sequence is by checking num+1, num+2, num+3, etc.",
      "example": "public int longestConsecutive(int[] nums) {\n    Set<Integer> numSet = new HashSet<>();\n    for (int num : nums) {\n        numSet.add(num);\n    }\n    \n    int maxLen = 0;\n    \n    for (int num : numSet) {\n        if (!numSet.contains(num - 1)) {\n            // Count consecutive sequence starting from num\n            int currentNum = num;\n            int currentLen = 1;\n            \n            while (numSet.contains(currentNum + 1)) {\n                currentNum++;\n                currentLen++;\n            }\n        }\n    }\n}",
      "action": "continue",
      "next": "coding-update-max-java"
    },
    {
      "stepId": "coding-update-max-java",
      "mentorSays": "Update maxLen with the maximum of current maxLen and the current sequence length.",
      "example": "public int longestConsecutive(int[] nums) {\n    Set<Integer> numSet = new HashSet<>();\n    for (int num : nums) {\n        numSet.add(num);\n    }\n    \n    int maxLen = 0;\n    \n    for (int num : numSet) {\n        if (!numSet.contains(num - 1)) {\n            int currentNum = num;\n            int currentLen = 1;\n            \n            while (numSet.contains(currentNum + 1)) {\n                currentNum++;\n                currentLen++;\n            }\n            \n            // Update maximum length\n            maxLen = Math.max(maxLen, currentLen);\n        }\n    }\n}",
      "action": "continue",
      "next": "coding-return-java"
    },
    {
      "stepId": "coding-return-java",
      "mentorSays": "Finally, return maxLen.",
      "example": "public int longestConsecutive(int[] nums) {\n    Set<Integer> numSet = new HashSet<>();\n    for (int num : nums) {\n        numSet.add(num);\n    }\n    \n    int maxLen = 0;\n    \n    for (int num : numSet) {\n        if (!numSet.contains(num - 1)) {\n            int currentNum = num;\n            int currentLen = 1;\n            \n            while (numSet.contains(currentNum + 1)) {\n                currentNum++;\n                currentLen++;\n            }\n            \n            maxLen = Math.max(maxLen, currentLen);\n        }\n    }\n    \n    return maxLen;\n}",
      "action": "continue",
      "next": "test-code-java"
    },
    {
      "stepId": "test-code-java",
      "mentorSays": "Perfect! Now test your code with the example we used earlier:\n\nnums = [100, 4, 200, 1, 3, 2]\n\nExpected output: 4\n\nTry it in your IDE or code editor!",
      "example": "Test input:\nnums = [100, 4, 200, 1, 3, 2]\n\nExpected output: 4\n\nBecause the longest consecutive sequence is [1, 2, 3, 4] with length 4.",
      "action": "continue",
      "next": "final"
    },
    {
      "stepId": "variable-check-cpp",
      "mentorSays": "Before we start coding, let me ask: Do you know what a variable is in C++?",
      "choices": [
        {
          "label": "Yes, I know variables",
          "next": "function-check-cpp"
        },
        {
          "label": "No, explain variables",
          "next": "variable-explanation-cpp"
        }
      ]
    },
    {
      "stepId": "variable-explanation-cpp",
      "mentorSays": "A variable is like a labeled box where you can store a value. You give it a name (like 'numSet' or 'maxLen') and put data inside it.\n\nFor example, 'unordered_set<int> numSet;' creates a variable that stores an unordered_set. Later, you can use that variable name to refer to it.",
      "example": "unordered_set<int> numSet;\n\nNow the variable refers to an empty hash set\nWe can use the variable name to access this hash set",
      "action": "continue",
      "next": "function-check-cpp"
    },
    {
      "stepId": "function-check-cpp",
      "mentorSays": "Before we start coding, let me ask: Do you know what a function is in C++?",
      "choices": [
        {
          "label": "Yes, I know functions",
          "next": "parameter-check-cpp"
        },
        {
          "label": "No, explain functions",
          "next": "function-explanation-cpp"
        }
      ]
    },
    {
      "stepId": "function-explanation-cpp",
      "mentorSays": "A function is a reusable block of code. You give it inputs (called parameters), and it can return an output.\n\nThink of it like a recipe: you provide ingredients (parameters), the function does the work, and gives you a result (return value).",
      "example": "int add(int a, int b) {\n    return a + b;\n}\n\nadd(3, 5) returns 8\nadd(10, 20) returns 30",
      "action": "continue",
      "next": "parameter-check-cpp"
    },
    {
      "stepId": "parameter-check-cpp",
      "mentorSays": "When we write a function, we can give it inputs. These inputs are called 'parameters'. Do you know what parameters are?",
      "choices": [
        {
          "label": "Yes, I know parameters",
          "next": "loop-check-cpp"
        },
        {
          "label": "No, explain parameters",
          "next": "parameter-explanation-cpp"
        }
      ]
    },
    {
      "stepId": "parameter-explanation-cpp",
      "mentorSays": "Parameters are like variables that receive values when you call the function. When you write 'int longestConsecutive(vector<int>& nums)', the 'nums' is a parameter - it's a placeholder that will receive an actual vector when you call the function.\n\nWhen you call 'longestConsecutive({100, 4, 200, 1, 3, 2})', the vector goes into 'nums'.",
      "example": "int longestConsecutive(vector<int>& nums) {\n  // 'nums' is a parameter\n}\n\n// When we call it:\nlongestConsecutive({100, 4, 200, 1, 3, 2});\n// The vector goes into 'nums'",
      "action": "continue",
      "next": "loop-check-cpp"
    },
    {
      "stepId": "loop-check-cpp",
      "mentorSays": "To solve this lesson, we'll need to use loops. Do you know what a loop is in C++?",
      "choices": [
        {
          "label": "Yes, I know loops",
          "next": "coding-start-cpp"
        },
        {
          "label": "No, explain loops",
          "next": "loop-explanation-cpp"
        }
      ]
    },
    {
      "stepId": "loop-explanation-cpp",
      "mentorSays": "A loop lets you repeat code multiple times. A 'for' loop runs code for each item in a sequence.\n\nFor example, 'for (int num : nums)' means: run the code for each number in the vector nums.",
      "example": "for (int num : vector<int>{1, 2, 3}) {\n    cout << num << endl;\n}\n\nThis prints: 1, 2, 3",
      "action": "continue",
      "next": "coding-start-cpp"
    },
    {
      "stepId": "coding-start-cpp",
      "mentorSays": "Perfect! Let's implement the longest consecutive sequence solution in C++. We'll create a function that takes a vector and returns the length of the longest consecutive sequence.",
      "example": "int longestConsecutive(vector<int>& nums) {\n    \n}",
      "action": "continue",
      "next": "coding-create-set-cpp"
    },
    {
      "stepId": "coding-create-set-cpp",
      "mentorSays": "First, let's create an unordered_set and add all numbers to it. This gives us O(1) lookup.",
      "example": "int longestConsecutive(vector<int>& nums) {\n    // Create an unordered_set with all numbers\n    unordered_set<int> numSet(nums.begin(), nums.end());\n    // numSet = {100, 4, 200, 1, 3, 2} for [100, 4, 200, 1, 3, 2]\n}",
      "action": "continue",
      "next": "coding-init-max-cpp"
    },
    {
      "stepId": "coding-init-max-cpp",
      "mentorSays": "Now, let's initialize a variable to track the maximum consecutive sequence length.",
      "example": "int longestConsecutive(vector<int>& nums) {\n    unordered_set<int> numSet(nums.begin(), nums.end());\n    \n    // Track maximum consecutive sequence length\n    int maxLen = 0;\n}",
      "action": "continue",
      "next": "coding-loop-cpp"
    },
    {
      "stepId": "coding-loop-cpp",
      "mentorSays": "Now, let's iterate through each number in the set.",
      "example": "int longestConsecutive(vector<int>& nums) {\n    unordered_set<int> numSet(nums.begin(), nums.end());\n    int maxLen = 0;\n    \n    // Iterate through each number\n    for (int num : numSet) {\n        \n    }\n}",
      "action": "continue",
      "next": "coding-check-start-cpp"
    },
    {
      "stepId": "coding-check-start-cpp",
      "mentorSays": "For each number, check if it's the start of a sequence. A number is a start if num-1 is NOT in the set.",
      "example": "int longestConsecutive(vector<int>& nums) {\n    unordered_set<int> numSet(nums.begin(), nums.end());\n    int maxLen = 0;\n    \n    for (int num : numSet) {\n        // Check if this is the start of a sequence\n        if (numSet.find(num - 1) == numSet.end()) {\n            \n        }\n    }\n}",
      "action": "continue",
      "next": "coding-count-sequence-cpp"
    },
    {
      "stepId": "coding-count-sequence-cpp",
      "mentorSays": "If it's a start, count how long the consecutive sequence is by checking num+1, num+2, num+3, etc.",
      "example": "int longestConsecutive(vector<int>& nums) {\n    unordered_set<int> numSet(nums.begin(), nums.end());\n    int maxLen = 0;\n    \n    for (int num : numSet) {\n        if (numSet.find(num - 1) == numSet.end()) {\n            // Count consecutive sequence starting from num\n            int currentNum = num;\n            int currentLen = 1;\n            \n            while (numSet.find(currentNum + 1) != numSet.end()) {\n                currentNum++;\n                currentLen++;\n            }\n        }\n    }\n}",
      "action": "continue",
      "next": "coding-update-max-cpp"
    },
    {
      "stepId": "coding-update-max-cpp",
      "mentorSays": "Update maxLen with the maximum of current maxLen and the current sequence length.",
      "example": "int longestConsecutive(vector<int>& nums) {\n    unordered_set<int> numSet(nums.begin(), nums.end());\n    int maxLen = 0;\n    \n    for (int num : numSet) {\n        if (numSet.find(num - 1) == numSet.end()) {\n            int currentNum = num;\n            int currentLen = 1;\n            \n            while (numSet.find(currentNum + 1) != numSet.end()) {\n                currentNum++;\n                currentLen++;\n            }\n            \n            // Update maximum length\n            maxLen = max(maxLen, currentLen);\n        }\n    }\n}",
      "action": "continue",
      "next": "coding-return-cpp"
    },
    {
      "stepId": "coding-return-cpp",
      "mentorSays": "Finally, return maxLen.",
      "example": "int longestConsecutive(vector<int>& nums) {\n    unordered_set<int> numSet(nums.begin(), nums.end());\n    int maxLen = 0;\n    \n    for (int num : numSet) {\n        if (numSet.find(num - 1) == numSet.end()) {\n            int currentNum = num;\n            int currentLen = 1;\n            \n            while (numSet.find(currentNum + 1) != numSet.end()) {\n                currentNum++;\n                currentLen++;\n            }\n            \n            maxLen = max(maxLen, currentLen);\n        }\n    }\n    \n    return maxLen;\n}",
      "action": "continue",
      "next": "test-code-cpp"
    },
    {
      "stepId": "test-code-cpp",
      "mentorSays": "Perfect! Now test your code with the example we used earlier:\n\nnums = [100, 4, 200, 1, 3, 2]\n\nExpected output: 4\n\nTry it in your IDE or code editor!",
      "example": "Test input:\nnums = [100, 4, 200, 1, 3, 2]\n\nExpected output: 4\n\nBecause the longest consecutive sequence is [1, 2, 3, 4] with length 4.",
      "action": "continue",
      "next": "final"
    },
    {
      "stepId": "variable-check-ts",
      "mentorSays": "Before we start coding, let me ask: Do you know what a variable is in TypeScript?",
      "choices": [
        {
          "label": "Yes, I know variables",
          "next": "function-check-ts"
        },
        {
          "label": "No, explain variables",
          "next": "variable-explanation-ts"
        }
      ]
    },
    {
      "stepId": "variable-explanation-ts",
      "mentorSays": "A variable is like a labeled box where you can store a value. You give it a name (like 'numSet' or 'maxLen') and put data inside it.\n\nFor example, 'let numSet = new Set<number>();' creates a variable that stores a Set. Later, you can use that variable name to refer to it.",
      "example": "let numSet = new Set<number>();\n\nNow the variable refers to an empty set\nWe can use the variable name to access this set",
      "action": "continue",
      "next": "function-check-ts"
    },
    {
      "stepId": "function-check-ts",
      "mentorSays": "Before we start coding, let me ask: Do you know what a function is in TypeScript?",
      "choices": [
        {
          "label": "Yes, I know functions",
          "next": "parameter-check-ts"
        },
        {
          "label": "No, explain functions",
          "next": "function-explanation-ts"
        }
      ]
    },
    {
      "stepId": "function-explanation-ts",
      "mentorSays": "A function is a reusable block of code. You give it inputs (called parameters), and it can return an output.\n\nThink of it like a recipe: you provide ingredients (parameters), the function does the work, and gives you a result (return value).",
      "example": "function add(a: number, b: number): number {\n  return a + b;\n}\n\nadd(3, 5) returns 8\nadd(10, 20) returns 30",
      "action": "continue",
      "next": "parameter-check-ts"
    },
    {
      "stepId": "parameter-check-ts",
      "mentorSays": "When we write a function, we can give it inputs. These inputs are called 'parameters'. Do you know what parameters are?",
      "choices": [
        {
          "label": "Yes, I know parameters",
          "next": "loop-check-ts"
        },
        {
          "label": "No, explain parameters",
          "next": "parameter-explanation-ts"
        }
      ]
    },
    {
      "stepId": "parameter-explanation-ts",
      "mentorSays": "Parameters are like variables that receive values when you call the function. When you write 'function longestConsecutive(nums: number[]): number', the 'nums' is a parameter - it's a placeholder that will receive an actual array when you call the function.\n\nWhen you call 'longestConsecutive([100, 4, 200, 1, 3, 2])', the array goes into 'nums'.",
      "example": "function longestConsecutive(nums: number[]): number {\n  // 'nums' is a parameter\n}\n\n// When we call it:\nlongestConsecutive([100, 4, 200, 1, 3, 2]);\n// The array goes into 'nums'",
      "action": "continue",
      "next": "loop-check-ts"
    },
    {
      "stepId": "loop-check-ts",
      "mentorSays": "To solve this lesson, we'll need to use loops. Do you know what a loop is in TypeScript?",
      "choices": [
        {
          "label": "Yes, I know loops",
          "next": "coding-start-ts"
        },
        {
          "label": "No, explain loops",
          "next": "loop-explanation-ts"
        }
      ]
    },
    {
      "stepId": "loop-explanation-ts",
      "mentorSays": "A loop lets you repeat code multiple times. A 'for...of' loop runs code for each item in a sequence.\n\nFor example, 'for (let num of nums)' means: run the code for each number in the array nums.",
      "example": "for (let num of [1, 2, 3]) {\n  console.log(num);\n}\n\nThis prints: 1, 2, 3",
      "action": "continue",
      "next": "coding-start-ts"
    },
    {
      "stepId": "coding-start-ts",
      "mentorSays": "Perfect! Let's implement the longest consecutive sequence solution in TypeScript. We'll create a function that takes an array and returns the length of the longest consecutive sequence.",
      "example": "function longestConsecutive(nums: number[]): number {\n  \n}",
      "action": "continue",
      "next": "coding-create-set-ts"
    },
    {
      "stepId": "coding-create-set-ts",
      "mentorSays": "First, let's create a Set and add all numbers to it. This gives us O(1) lookup.",
      "example": "function longestConsecutive(nums: number[]): number {\n  // Create a set with all numbers\n  let numSet = new Set<number>(nums);\n  // numSet = {100, 4, 200, 1, 3, 2} for [100, 4, 200, 1, 3, 2]\n}",
      "action": "continue",
      "next": "coding-init-max-ts"
    },
    {
      "stepId": "coding-init-max-ts",
      "mentorSays": "Now, let's initialize a variable to track the maximum consecutive sequence length.",
      "example": "function longestConsecutive(nums: number[]): number {\n  let numSet = new Set<number>(nums);\n  \n  // Track maximum consecutive sequence length\n  let maxLen = 0;\n}",
      "action": "continue",
      "next": "coding-loop-ts"
    },
    {
      "stepId": "coding-loop-ts",
      "mentorSays": "Now, let's iterate through each number in the set.",
      "example": "function longestConsecutive(nums: number[]): number {\n  let numSet = new Set<number>(nums);\n  let maxLen = 0;\n  \n  // Iterate through each number\n  for (let num of numSet) {\n    \n  }\n}",
      "action": "continue",
      "next": "coding-check-start-ts"
    },
    {
      "stepId": "coding-check-start-ts",
      "mentorSays": "For each number, check if it's the start of a sequence. A number is a start if num-1 is NOT in the set.",
      "example": "function longestConsecutive(nums: number[]): number {\n  let numSet = new Set<number>(nums);\n  let maxLen = 0;\n  \n  for (let num of numSet) {\n    // Check if this is the start of a sequence\n    if (!numSet.has(num - 1)) {\n      \n    }\n  }\n}",
      "action": "continue",
      "next": "coding-count-sequence-ts"
    },
    {
      "stepId": "coding-count-sequence-ts",
      "mentorSays": "If it's a start, count how long the consecutive sequence is by checking num+1, num+2, num+3, etc.",
      "example": "function longestConsecutive(nums: number[]): number {\n  let numSet = new Set<number>(nums);\n  let maxLen = 0;\n  \n  for (let num of numSet) {\n    if (!numSet.has(num - 1)) {\n      // Count consecutive sequence starting from num\n      let currentNum = num;\n      let currentLen = 1;\n      \n      while (numSet.has(currentNum + 1)) {\n        currentNum++;\n        currentLen++;\n      }\n    }\n  }\n}",
      "action": "continue",
      "next": "coding-update-max-ts"
    },
    {
      "stepId": "coding-update-max-ts",
      "mentorSays": "Update maxLen with the maximum of current maxLen and the current sequence length.",
      "example": "function longestConsecutive(nums: number[]): number {\n  let numSet = new Set<number>(nums);\n  let maxLen = 0;\n  \n  for (let num of numSet) {\n    if (!numSet.has(num - 1)) {\n      let currentNum = num;\n      let currentLen = 1;\n      \n      while (numSet.has(currentNum + 1)) {\n        currentNum++;\n        currentLen++;\n      }\n      \n      // Update maximum length\n      maxLen = Math.max(maxLen, currentLen);\n    }\n  }\n}",
      "action": "continue",
      "next": "coding-return-ts"
    },
    {
      "stepId": "coding-return-ts",
      "mentorSays": "Finally, return maxLen.",
      "example": "function longestConsecutive(nums: number[]): number {\n  let numSet = new Set<number>(nums);\n  let maxLen = 0;\n  \n  for (let num of numSet) {\n    if (!numSet.has(num - 1)) {\n      let currentNum = num;\n      let currentLen = 1;\n      \n      while (numSet.has(currentNum + 1)) {\n        currentNum++;\n        currentLen++;\n      }\n      \n      maxLen = Math.max(maxLen, currentLen);\n    }\n  }\n  \n  return maxLen;\n}",
      "action": "continue",
      "next": "test-code-ts"
    },
    {
      "stepId": "test-code-ts",
      "mentorSays": "Perfect! Now test your code with the example we used earlier:\n\nnums = [100, 4, 200, 1, 3, 2]\n\nExpected output: 4\n\nTry it in your IDE or code editor!",
      "example": "Test input:\nnums = [100, 4, 200, 1, 3, 2]\n\nExpected output: 4\n\nBecause the longest consecutive sequence is [1, 2, 3, 4] with length 4.",
      "action": "continue",
      "next": "final"
    },
    {
      "stepId": "final",
      "mentorSays": "🎉 Well done! You've learned to think through the Longest Consecutive Sequence lesson. You understand what the lesson asks for, how to use hash sets to efficiently find sequence starts, and how to count consecutive elements.\n\nThis kind of thinking—using hash sets to optimize lookups and identifying sequence starts—is essential for solving many array lessons. Keep practicing!",
      "action": "continue"
    }
  ]
}