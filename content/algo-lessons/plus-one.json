{
  "id": "plus-one",
  "title": "Plus one",
  "pattern": "array-manipulation",
  "difficulty": "easy",
  "language": "javascript",
  "status": "draft",
  "flow": [
    {
      "stepId": "title",
      "mentorSays": "At the end of this lesson, you will be able to:\n\n1. Understand what the Plus One lesson asks for\n2. Add 1 to a number represented as an array of digits\n3. Handle carry propagation correctly\n4. Implement the solution step by step in your chosen language",
      "action": "continue",
      "next": "lesson-illustration"
    },
    {
      "stepId": "lesson-illustration",
      "mentorSays": "Perfect! Now let's understand what the Plus One lesson is asking.\n\nImagine you have an array representing a number: [1, 2, 3]\n\nThis represents the number 123.\n\nYour task: Add 1 to this number and return the result as an array.\n\n123 + 1 = 124\n\nSo the result should be [1, 2, 4].\n\n**But what about cases with carry?**\n\nExample: [9, 9]\n- This represents 99\n- 99 + 1 = 100\n- Result: [1, 0, 0]\n\nNotice: The array length increased from 2 to 3 because of the carry!\n\n**How carry works:**\n- Start from the rightmost digit (ones place)\n- Add 1\n- If result is 10, set digit to 0 and carry 1 to the next position\n- Continue until no more carry or we reach the beginning\n\nLet's trace [9, 9]:\n- Index 1 (ones): 9 + 1 = 10 → set to 0, carry 1\n- Index 0 (tens): 9 + 1 = 10 → set to 0, carry 1\n- No more digits, but we have carry → add new digit 1 at the beginning\n- Result: [1, 0, 0]\n\nThat's what the lesson wants: add 1 to the number represented by the array, handling carry correctly.",
      "example": "digits = [1, 2, 3]\n\nAdd 1: 123 + 1 = 124\n\nResult: [1, 2, 4]\n\n---\n\ndigits = [9, 9]\n\nAdd 1: 99 + 1 = 100\n\nResult: [1, 0, 0]\n\n---\n\ndigits = [4, 3, 2, 1]\n\nAdd 1: 4321 + 1 = 4322\n\nResult: [4, 3, 2, 2]",
      "action": "continue",
      "next": "thinking-challenge"
    },
    {
      "stepId": "thinking-challenge",
      "mentorSays": "Now that you understand what the lesson wants, here's the real question:\n\nHow would YOU add 1 to a number represented as an array?\n\nThink about it for a moment. What approach feels natural to you?",
      "choices": [
        {
          "label": "Start from the right, add 1, handle carry if needed",
          "next": "explore-carry"
        },
        {
          "label": "Convert array to number, add 1, convert back to array",
          "next": "explore-convert"
        },
        {
          "label": "Add 1 to the last digit, then fix any carries",
          "next": "explore-simple-add"
        }
      ]
    },
    {
      "stepId": "explore-convert",
      "mentorSays": "That's a straightforward approach! You're thinking: \"I'll convert the array to a number, add 1, then convert back.\"\n\nFor [1, 2, 3]:\n- Convert: 123\n- Add 1: 124\n- Convert back: [1, 2, 4] ✓\n\nThis works for small numbers! However, for very large numbers (like arrays with 100+ digits), this approach fails because numbers can't represent such large values accurately.\n\nWould you like to learn the carry approach that works for any size?",
      "action": "continue",
      "next": "explore-carry"
    },
    {
      "stepId": "explore-simple-add",
      "mentorSays": "Good thinking! You're thinking: \"Add 1 to the last digit, then handle any carries.\"\n\nThis is exactly the right approach! Let's formalize it:\n1. Start from the rightmost digit\n2. Add 1\n3. If result is 10, set to 0 and carry 1\n4. Continue leftward until no carry\n\nThis is the carry approach! Let's learn it in detail.",
      "action": "continue",
      "next": "explore-carry"
    },
    {
      "stepId": "explore-carry",
      "mentorSays": "Excellent choice! The carry approach is efficient and works for any array size. Here's the core idea:\n\n1. **Start from the right**: Begin with the rightmost digit (ones place)\n2. **Add 1**: Add 1 to the current digit\n3. **Handle carry**: If the result is 10, set digit to 0 and carry 1 to the left\n4. **Continue**: Move left and repeat until no carry or we reach the beginning\n5. **Handle overflow**: If we still have carry at the beginning, add a new digit 1 at the front\n\nLet's trace this with [9, 9]:\n\n- **Start at index 1** (rightmost):\n  - digits[1] = 9\n  - 9 + 1 = 10\n  - Set digits[1] = 0, carry = 1\n  - Array: [9, 0]\n\n- **Move to index 0**:\n  - digits[0] = 9\n  - 9 + 1 = 10\n  - Set digits[0] = 0, carry = 1\n  - Array: [0, 0]\n\n- **Still have carry, but at beginning**:\n  - Add new digit 1 at the front\n  - Result: [1, 0, 0] ✓\n\nLet's trace [1, 2, 3]:\n\n- **Start at index 2** (rightmost):\n  - digits[2] = 3\n  - 3 + 1 = 4\n  - Set digits[2] = 4, no carry\n  - Result: [1, 2, 4] ✓\n\nThis is O(n) time! Now let's code it step by step.",
      "action": "continue",
      "next": "language-selection"
    },
    {
      "stepId": "language-selection",
      "mentorSays": "Great! Now let's code this carry solution. Which programming language would you like to use?",
      "choices": [
        {
          "label": "JavaScript",
          "next": "variable-check-js"
        },
        {
          "label": "Python",
          "next": "variable-check-python"
        },
        {
          "label": "Java",
          "next": "variable-check-java"
        },
        {
          "label": "C++",
          "next": "variable-check-cpp"
        },
        {
          "label": "TypeScript",
          "next": "variable-check-ts"
        }
      ]
    },
    {
      "stepId": "variable-check-js",
      "mentorSays": "Before we start coding, let me ask: Do you know what a variable is in JavaScript?",
      "choices": [
        {
          "label": "Yes, I know variables",
          "next": "function-check-js"
        },
        {
          "label": "No, explain variables",
          "next": "variable-explanation-js"
        }
      ]
    },
    {
      "stepId": "variable-explanation-js",
      "mentorSays": "A variable is like a labeled box where you can store a value. You give it a name (like 'carry' or 'i') and put data inside it.\n\nFor example, 'let carry = 1;' creates a variable that stores the number 1.",
      "example": "let carry = 1;\nlet i = digits.length - 1;\n\nNow these variables store numbers\nWe can use them in our calculations",
      "action": "continue",
      "next": "function-check-js"
    },
    {
      "stepId": "function-check-js",
      "mentorSays": "Before we start coding, let me ask: Do you know what a function is in JavaScript?",
      "choices": [
        {
          "label": "Yes, I know functions",
          "next": "parameter-check-js"
        },
        {
          "label": "No, explain functions",
          "next": "function-explanation-js"
        }
      ]
    },
    {
      "stepId": "function-explanation-js",
      "mentorSays": "A function is a reusable block of code. You give it inputs (called parameters), and it can return an output.\n\nThink of it like a recipe: you provide ingredients (parameters), the function does the work, and gives you a result (return value).",
      "example": "function plusOne(digits) {\n  return digits;\n}\n\nplusOne([1, 2, 3]) returns [1, 2, 4]",
      "action": "continue",
      "next": "parameter-check-js"
    },
    {
      "stepId": "parameter-check-js",
      "mentorSays": "When we write a function, we can give it inputs. These inputs are called 'parameters'. Do you know what parameters are?",
      "choices": [
        {
          "label": "Yes, I know parameters",
          "next": "loop-check-js"
        },
        {
          "label": "No, explain parameters",
          "next": "parameter-explanation-js"
        }
      ]
    },
    {
      "stepId": "parameter-explanation-js",
      "mentorSays": "Parameters are like variables that receive values when you call the function. When you write 'function plusOne(digits)', the 'digits' is a parameter - it's a placeholder that will receive an actual array when you call the function.",
      "example": "function plusOne(digits) {\n  // 'digits' is a parameter\n}\n\n// When we call it:\nplusOne([1, 2, 3]);\n// The array goes into 'digits'",
      "action": "continue",
      "next": "loop-check-js"
    },
    {
      "stepId": "loop-check-js",
      "mentorSays": "To solve this lesson, we'll need to use a loop to iterate through digits from right to left. Do you know what a loop is in JavaScript?",
      "choices": [
        {
          "label": "Yes, I know loops",
          "next": "carry-check-js"
        },
        {
          "label": "No, explain loops",
          "next": "loop-explanation-js"
        }
      ]
    },
    {
      "stepId": "carry-check-js",
      "mentorSays": "Do you understand how carry works in addition?",
      "choices": [
        {
          "label": "Yes, I know carry",
          "next": "coding-start-js"
        },
        {
          "label": "No, explain carry",
          "next": "carry-explanation-js"
        }
      ]
    },
    {
      "stepId": "carry-explanation-js",
      "mentorSays": "When adding 1 to a digit, if the result is 10 or more, we carry 1 to the next digit. For example, 9 + 1 = 10, so we write 0 and carry 1.\n\nlet digits = [9, 9, 9];\nlet carry = 1;\n\n// Add 1 with carry\nfor (let i = digits.length - 1; i >= 0; i--) {\n  let sum = digits[i] + carry;\n  digits[i] = sum % 10;\n  carry = Math.floor(sum / 10);\n}\n\nGot it?",
      "example": "let digits = [9, 9, 9];\nlet carry = 1;\n\n// Add 1 with carry\nfor (let i = digits.length - 1; i >= 0; i--) {\n  let sum = digits[i] + carry;\n  digits[i] = sum % 10;\n  carry = Math.floor(sum / 10);\n}",
      "action": "continue",
      "next": "coding-start-js"
    },
    {
      "stepId": "loop-explanation-js",
      "mentorSays": "A loop lets you repeat code multiple times. A 'for' loop in JavaScript runs code for each item in a sequence.\n\nFor example, you can loop through an array from right to left.",
      "example": "for (let i = digits.length - 1; i >= 0; i--) {\n  // Process digits[i]\n}",
      "action": "continue",
      "next": "coding-start-js"
    },
    {
      "stepId": "coding-start-js",
      "mentorSays": "Perfect! Let's implement the plus one solution in JavaScript. We'll create a function that takes an array of digits and returns the result after adding 1.",
      "example": "function plusOne(digits) {\n  \n}",
      "action": "continue",
      "next": "coding-init-js"
    },
    {
      "stepId": "coding-init-js",
      "mentorSays": "Initialize a carry variable starting with 1 (since we're adding 1).",
      "example": "function plusOne(digits) {\n  let carry = 1;\n}",
      "action": "continue",
      "next": "coding-loop-js"
    },
    {
      "stepId": "coding-loop-js",
      "mentorSays": "Create a loop that starts from the rightmost digit and moves left.",
      "example": "function plusOne(digits) {\n  let carry = 1;\n  \n  for (let i = digits.length - 1; i >= 0; i--) {\n    \n  }\n}",
      "action": "continue",
      "next": "coding-add-carry-js"
    },
    {
      "stepId": "coding-add-carry-js",
      "mentorSays": "Inside the loop, add the carry to the current digit.",
      "example": "function plusOne(digits) {\n  let carry = 1;\n  \n  for (let i = digits.length - 1; i >= 0; i--) {\n    digits[i] += carry;\n  }\n}",
      "action": "continue",
      "next": "coding-handle-carry-js"
    },
    {
      "stepId": "coding-handle-carry-js",
      "mentorSays": "If the digit is 10, set it to 0 and keep carry as 1. Otherwise, set carry to 0 and break.",
      "example": "function plusOne(digits) {\n  let carry = 1;\n  \n  for (let i = digits.length - 1; i >= 0; i--) {\n    digits[i] += carry;\n    \n    if (digits[i] === 10) {\n      digits[i] = 0;\n      carry = 1;\n    } else {\n      carry = 0;\n      break;\n    }\n  }\n}",
      "action": "continue",
      "next": "coding-all-nines-js"
    },
    {
      "stepId": "coding-all-nines-js",
      "mentorSays": "Let's handle a special edge case: what if all digits are 9?\n\nFor [9, 9, 9], adding 1 gives [1, 0, 0, 0]. We need to create a new array with an extra digit at the front!\n\nThis happens when the carry propagates all the way to the beginning.",
      "example": "function plusOne(digits) {\n  let carry = 1;\n  \n  for (let i = digits.length - 1; i >= 0; i--) {\n    let sum = digits[i] + carry;\n    digits[i] = sum % 10;\n    carry = Math.floor(sum / 10);\n    \n    if (carry === 0) break;\n  }\n  \n  // If carry is still 1, we need a new array\n  if (carry === 1) {\n    return [1, ...digits];\n  }\n  \n  return digits;\n}",
      "action": "continue",
      "next": "test-code-js"
    },
    {
      "stepId": "coding-handle-overflow-js",
      "mentorSays": "If we still have carry after the loop, add a new digit 1 at the beginning of the array.",
      "example": "function plusOne(digits) {\n  let carry = 1;\n  \n  for (let i = digits.length - 1; i >= 0; i--) {\n    digits[i] += carry;\n    \n    if (digits[i] === 10) {\n      digits[i] = 0;\n      carry = 1;\n    } else {\n      carry = 0;\n      break;\n    }\n  }\n  \n  if (carry === 1) {\n    digits.unshift(1);\n  }\n  \n  return digits;\n}",
      "action": "continue",
      "next": "test-code-js"
    },
    {
      "stepId": "test-code-js",
      "mentorSays": "Perfect! Test with digits = [1, 2, 3]. Expected: [1, 2, 4]. Also test with [9, 9]. Expected: [1, 0, 0].",
      "example": "Test input 1:\ndigits = [1, 2, 3]\n\nExpected output: [1, 2, 4]\n\nTest input 2:\ndigits = [9, 9]\n\nExpected output: [1, 0, 0]\n\nBecause 99 + 1 = 100, which requires a new digit.",
      "action": "continue",
      "next": "final"
    },
    {
      "stepId": "variable-check-python",
      "mentorSays": "Before we start coding, let me ask: Do you know what a variable is in Python?",
      "choices": [
        {
          "label": "Yes, I know variables",
          "next": "function-check-python"
        },
        {
          "label": "No, explain variables",
          "next": "variable-explanation-python"
        }
      ]
    },
    {
      "stepId": "variable-explanation-python",
      "mentorSays": "A variable is like a labeled box where you can store a value. You give it a name (like 'carry' or 'i') and put data inside it.\n\nFor example, 'carry = 1' creates a variable that stores the number 1.",
      "example": "carry = 1\ni = len(digits) - 1\n\nNow these variables store numbers\nWe can use them in our calculations",
      "action": "continue",
      "next": "function-check-python"
    },
    {
      "stepId": "function-check-python",
      "mentorSays": "Before we start coding, let me ask: Do you know what a function is in Python?",
      "choices": [
        {
          "label": "Yes, I know functions",
          "next": "parameter-check-python"
        },
        {
          "label": "No, explain functions",
          "next": "function-explanation-python"
        }
      ]
    },
    {
      "stepId": "function-explanation-python",
      "mentorSays": "A function is a reusable block of code. You give it inputs (called parameters), and it can return an output.",
      "example": "def plus_one(digits):\n    return digits\n\nplus_one([1, 2, 3]) returns [1, 2, 4]",
      "action": "continue",
      "next": "parameter-check-python"
    },
    {
      "stepId": "parameter-check-python",
      "mentorSays": "When we write a function, we can give it inputs. These inputs are called 'parameters'. Do you know what parameters are?",
      "choices": [
        {
          "label": "Yes, I know parameters",
          "next": "loop-check-python"
        },
        {
          "label": "No, explain parameters",
          "next": "parameter-explanation-python"
        }
      ]
    },
    {
      "stepId": "parameter-explanation-python",
      "mentorSays": "Parameters are like variables that receive values when you call the function. When you write 'def plus_one(digits):', the 'digits' is a parameter - it's a placeholder that will receive an actual list when you call the function.",
      "example": "def plus_one(digits):\n  # 'digits' is a parameter\n\n# When we call it:\nplus_one([1, 2, 3])\n# The list goes into 'digits'",
      "action": "continue",
      "next": "loop-check-python"
    },
    {
      "stepId": "loop-check-python",
      "mentorSays": "To solve this lesson, we'll need to use a loop to iterate through digits from right to left. Do you know what a loop is in Python?",
      "choices": [
        {
          "label": "Yes, I know loops",
          "next": "carry-check-python"
        },
        {
          "label": "No, explain loops",
          "next": "loop-explanation-python"
        }
      ]
    },
    {
      "stepId": "carry-check-python",
      "mentorSays": "Do you understand how carry works in addition?",
      "choices": [
        {
          "label": "Yes, I know carry",
          "next": "coding-start-python"
        },
        {
          "label": "No, explain carry",
          "next": "carry-explanation-python"
        }
      ]
    },
    {
      "stepId": "carry-explanation-python",
      "mentorSays": "When adding 1 to a digit, if the result is 10 or more, we carry 1 to the next digit. For example, 9 + 1 = 10, so we write 0 and carry 1.\n\nlet digits = [9, 9, 9];\nlet carry = 1;\n\n// Add 1 with carry\nfor (let i = digits.length - 1; i >= 0; i--) {\n  let sum = digits[i] + carry;\n  digits[i] = sum % 10;\n  carry = Math.floor(sum / 10);\n}\n\nGot it?",
      "example": "let digits = [9, 9, 9];\nlet carry = 1;\n\n// Add 1 with carry\nfor (let i = digits.length - 1; i >= 0; i--) {\n  let sum = digits[i] + carry;\n  digits[i] = sum % 10;\n  carry = Math.floor(sum / 10);\n}",
      "action": "continue",
      "next": "coding-start-python"
    },
    {
      "stepId": "loop-explanation-python",
      "mentorSays": "A loop lets you repeat code multiple times. A 'for' loop in Python runs code for each item in a sequence.\n\nFor example, you can loop through a list from right to left.",
      "example": "for i in range(len(digits) - 1, -1, -1):\n    # Process digits[i]",
      "action": "continue",
      "next": "coding-start-python"
    },
    {
      "stepId": "coding-start-python",
      "mentorSays": "Perfect! Let's implement the plus one solution in Python. We'll create a function that takes a list of digits and returns the result after adding 1.",
      "example": "def plus_one(digits):\n    \n    pass",
      "action": "continue",
      "next": "coding-init-python"
    },
    {
      "stepId": "coding-init-python",
      "mentorSays": "Initialize a carry variable starting with 1 (since we're adding 1).",
      "example": "def plus_one(digits):\n    carry = 1\n    \n    pass",
      "action": "continue",
      "next": "coding-loop-python"
    },
    {
      "stepId": "coding-loop-python",
      "mentorSays": "Create a loop that starts from the rightmost digit and moves left.",
      "example": "def plus_one(digits):\n    carry = 1\n    \n    for i in range(len(digits) - 1, -1, -1):\n        \n    \n    pass",
      "action": "continue",
      "next": "coding-add-carry-python"
    },
    {
      "stepId": "coding-add-carry-python",
      "mentorSays": "Inside the loop, add the carry to the current digit.",
      "example": "def plus_one(digits):\n    carry = 1\n    \n    for i in range(len(digits) - 1, -1, -1):\n        digits[i] += carry\n    \n    pass",
      "action": "continue",
      "next": "coding-handle-carry-python"
    },
    {
      "stepId": "coding-handle-carry-python",
      "mentorSays": "If the digit is 10, set it to 0 and keep carry as 1. Otherwise, set carry to 0 and break.",
      "example": "def plus_one(digits):\n    carry = 1\n    \n    for i in range(len(digits) - 1, -1, -1):\n        digits[i] += carry\n        \n        if digits[i] == 10:\n            digits[i] = 0\n            carry = 1\n        else:\n            carry = 0\n            break\n    \n    pass",
      "action": "continue",
      "next": "coding-all-nines-python"
    },
    {
      "stepId": "coding-all-nines-python",
      "mentorSays": "Let's handle a special edge case: what if all digits are 9?\n\nFor [9, 9, 9], adding 1 gives [1, 0, 0, 0]. We need to create a new array with an extra digit at the front!\n\nThis happens when the carry propagates all the way to the beginning.",
      "example": "function plusOne(digits) {\n  let carry = 1;\n  \n  for (let i = digits.length - 1; i >= 0; i--) {\n    let sum = digits[i] + carry;\n    digits[i] = sum % 10;\n    carry = Math.floor(sum / 10);\n    \n    if (carry === 0) break;\n  }\n  \n  // If carry is still 1, we need a new array\n  if (carry === 1) {\n    return [1, ...digits];\n  }\n  \n  return digits;\n}",
      "action": "continue",
      "next": "test-code-python"
    },
    {
      "stepId": "coding-handle-overflow-python",
      "mentorSays": "If we still have carry after the loop, add a new digit 1 at the beginning of the list.",
      "example": "def plus_one(digits):\n    carry = 1\n    \n    for i in range(len(digits) - 1, -1, -1):\n        digits[i] += carry\n        \n        if digits[i] == 10:\n            digits[i] = 0\n            carry = 1\n        else:\n            carry = 0\n            break\n    \n    if carry == 1:\n        digits.insert(0, 1)\n    \n    return digits",
      "action": "continue",
      "next": "test-code-python"
    },
    {
      "stepId": "test-code-python",
      "mentorSays": "Perfect! Test with digits = [1, 2, 3]. Expected: [1, 2, 4]. Also test with [9, 9]. Expected: [1, 0, 0].",
      "example": "Test input 1:\ndigits = [1, 2, 3]\n\nExpected output: [1, 2, 4]\n\nTest input 2:\ndigits = [9, 9]\n\nExpected output: [1, 0, 0]\n\nBecause 99 + 1 = 100, which requires a new digit.",
      "action": "continue",
      "next": "final"
    },
    {
      "stepId": "variable-check-java",
      "mentorSays": "Before we start coding, let me ask: Do you know what a variable is in Java?",
      "choices": [
        {
          "label": "Yes, I know variables",
          "next": "function-check-java"
        },
        {
          "label": "No, explain variables",
          "next": "variable-explanation-java"
        }
      ]
    },
    {
      "stepId": "variable-explanation-java",
      "mentorSays": "A variable is like a labeled box where you can store a value. You give it a name (like 'carry' or 'i') and put data inside it.\n\nFor example, 'int carry = 1;' creates a variable that stores the number 1.",
      "example": "int carry = 1;\nint i = digits.length - 1;\n\nNow these variables store numbers\nWe can use them in our calculations",
      "action": "continue",
      "next": "function-check-java"
    },
    {
      "stepId": "function-check-java",
      "mentorSays": "Before we start coding, let me ask: Do you know what a method is in Java?",
      "choices": [
        {
          "label": "Yes, I know methods",
          "next": "parameter-check-java"
        },
        {
          "label": "No, explain methods",
          "next": "function-explanation-java"
        }
      ]
    },
    {
      "stepId": "function-explanation-java",
      "mentorSays": "A method (Java's version of a function) is a reusable block of code. You give it inputs (called parameters), and it can return an output.",
      "example": "public int[] plusOne(int[] digits) {\n    return digits;\n}\n\nplusOne(new int[]{1, 2, 3}) returns [1, 2, 4]",
      "action": "continue",
      "next": "parameter-check-java"
    },
    {
      "stepId": "parameter-check-java",
      "mentorSays": "When we write a method, we can give it inputs. These inputs are called 'parameters'. Do you know what parameters are?",
      "choices": [
        {
          "label": "Yes, I know parameters",
          "next": "loop-check-java"
        },
        {
          "label": "No, explain parameters",
          "next": "parameter-explanation-java"
        }
      ]
    },
    {
      "stepId": "parameter-explanation-java",
      "mentorSays": "Parameters are like variables that receive values when you call the method. When you write 'public int[] plusOne(int[] digits)', the 'digits' is a parameter - it's a placeholder that will receive an actual array when you call the method.",
      "example": "public int[] plusOne(int[] digits) {\n  // 'digits' is a parameter\n}\n\n// When we call it:\nplusOne(new int[]{1, 2, 3});\n// The array goes into 'digits'",
      "action": "continue",
      "next": "loop-check-java"
    },
    {
      "stepId": "loop-check-java",
      "mentorSays": "To solve this lesson, we'll need to use a loop to iterate through digits from right to left. Do you know what a loop is in Java?",
      "choices": [
        {
          "label": "Yes, I know loops",
          "next": "carry-check-java"
        },
        {
          "label": "No, explain loops",
          "next": "loop-explanation-java"
        }
      ]
    },
    {
      "stepId": "carry-check-java",
      "mentorSays": "Do you understand how carry works in addition?",
      "choices": [
        {
          "label": "Yes, I know carry",
          "next": "coding-start-java"
        },
        {
          "label": "No, explain carry",
          "next": "carry-explanation-java"
        }
      ]
    },
    {
      "stepId": "carry-explanation-java",
      "mentorSays": "When adding 1 to a digit, if the result is 10 or more, we carry 1 to the next digit. For example, 9 + 1 = 10, so we write 0 and carry 1.\n\nlet digits = [9, 9, 9];\nlet carry = 1;\n\n// Add 1 with carry\nfor (let i = digits.length - 1; i >= 0; i--) {\n  let sum = digits[i] + carry;\n  digits[i] = sum % 10;\n  carry = Math.floor(sum / 10);\n}\n\nGot it?",
      "example": "let digits = [9, 9, 9];\nlet carry = 1;\n\n// Add 1 with carry\nfor (let i = digits.length - 1; i >= 0; i--) {\n  let sum = digits[i] + carry;\n  digits[i] = sum % 10;\n  carry = Math.floor(sum / 10);\n}",
      "action": "continue",
      "next": "coding-start-java"
    },
    {
      "stepId": "loop-explanation-java",
      "mentorSays": "A loop lets you repeat code multiple times. A 'for' loop in Java runs code for each item in a sequence.\n\nFor example, you can loop through an array from right to left.",
      "example": "for (int i = digits.length - 1; i >= 0; i--) {\n    // Process digits[i]\n}",
      "action": "continue",
      "next": "coding-start-java"
    },
    {
      "stepId": "coding-start-java",
      "mentorSays": "Perfect! Let's implement the plus one solution in Java. We'll create a method that takes an array of digits and returns the result after adding 1.",
      "example": "public int[] plusOne(int[] digits) {\n    \n}",
      "action": "continue",
      "next": "coding-init-java"
    },
    {
      "stepId": "coding-init-java",
      "mentorSays": "Initialize a carry variable starting with 1 (since we're adding 1).",
      "example": "public int[] plusOne(int[] digits) {\n    int carry = 1;\n}",
      "action": "continue",
      "next": "coding-loop-java"
    },
    {
      "stepId": "coding-loop-java",
      "mentorSays": "Create a loop that starts from the rightmost digit and moves left.",
      "example": "public int[] plusOne(int[] digits) {\n    int carry = 1;\n    \n    for (int i = digits.length - 1; i >= 0; i--) {\n        \n    }\n}",
      "action": "continue",
      "next": "coding-add-carry-java"
    },
    {
      "stepId": "coding-add-carry-java",
      "mentorSays": "Inside the loop, add the carry to the current digit.",
      "example": "public int[] plusOne(int[] digits) {\n    int carry = 1;\n    \n    for (int i = digits.length - 1; i >= 0; i--) {\n        digits[i] += carry;\n    }\n}",
      "action": "continue",
      "next": "coding-handle-carry-java"
    },
    {
      "stepId": "coding-handle-carry-java",
      "mentorSays": "If the digit is 10, set it to 0 and keep carry as 1. Otherwise, set carry to 0 and break.",
      "example": "public int[] plusOne(int[] digits) {\n    int carry = 1;\n    \n    for (int i = digits.length - 1; i >= 0; i--) {\n        digits[i] += carry;\n        \n        if (digits[i] == 10) {\n            digits[i] = 0;\n            carry = 1;\n        } else {\n            carry = 0;\n            break;\n        }\n    }\n}",
      "action": "continue",
      "next": "coding-all-nines-java"
    },
    {
      "stepId": "coding-all-nines-java",
      "mentorSays": "Let's handle a special edge case: what if all digits are 9?\n\nFor [9, 9, 9], adding 1 gives [1, 0, 0, 0]. We need to create a new array with an extra digit at the front!\n\nThis happens when the carry propagates all the way to the beginning.",
      "example": "function plusOne(digits) {\n  let carry = 1;\n  \n  for (let i = digits.length - 1; i >= 0; i--) {\n    let sum = digits[i] + carry;\n    digits[i] = sum % 10;\n    carry = Math.floor(sum / 10);\n    \n    if (carry === 0) break;\n  }\n  \n  // If carry is still 1, we need a new array\n  if (carry === 1) {\n    return [1, ...digits];\n  }\n  \n  return digits;\n}",
      "action": "continue",
      "next": "test-code-java"
    },
    {
      "stepId": "coding-handle-overflow-java",
      "mentorSays": "If we still have carry after the loop, create a new array with length + 1, set first element to 1, and copy the rest.",
      "example": "import java.util.Arrays;\n\npublic int[] plusOne(int[] digits) {\n    int carry = 1;\n    \n    for (int i = digits.length - 1; i >= 0; i--) {\n        digits[i] += carry;\n        \n        if (digits[i] == 10) {\n            digits[i] = 0;\n            carry = 1;\n        } else {\n            carry = 0;\n            break;\n        }\n    }\n    \n    if (carry == 1) {\n        int[] result = new int[digits.length + 1];\n        result[0] = 1;\n        System.arraycopy(digits, 0, result, 1, digits.length);\n        return result;\n    }\n    \n    return digits;\n}",
      "action": "continue",
      "next": "test-code-java"
    },
    {
      "stepId": "test-code-java",
      "mentorSays": "Perfect! Test with digits = [1, 2, 3]. Expected: [1, 2, 4]. Also test with [9, 9]. Expected: [1, 0, 0].",
      "example": "Test input 1:\ndigits = [1, 2, 3]\n\nExpected output: [1, 2, 4]\n\nTest input 2:\ndigits = [9, 9]\n\nExpected output: [1, 0, 0]\n\nBecause 99 + 1 = 100, which requires a new digit.",
      "action": "continue",
      "next": "final"
    },
    {
      "stepId": "variable-check-cpp",
      "mentorSays": "Before we start coding, let me ask: Do you know what a variable is in C++?",
      "choices": [
        {
          "label": "Yes, I know variables",
          "next": "function-check-cpp"
        },
        {
          "label": "No, explain variables",
          "next": "variable-explanation-cpp"
        }
      ]
    },
    {
      "stepId": "variable-explanation-cpp",
      "mentorSays": "A variable is like a labeled box where you can store a value. You give it a name (like 'carry' or 'i') and put data inside it.\n\nFor example, 'int carry = 1;' creates a variable that stores the number 1.",
      "example": "int carry = 1;\nint i = digits.size() - 1;\n\nNow these variables store numbers\nWe can use them in our calculations",
      "action": "continue",
      "next": "function-check-cpp"
    },
    {
      "stepId": "function-check-cpp",
      "mentorSays": "Before we start coding, let me ask: Do you know what a function is in C++?",
      "choices": [
        {
          "label": "Yes, I know functions",
          "next": "parameter-check-cpp"
        },
        {
          "label": "No, explain functions",
          "next": "function-explanation-cpp"
        }
      ]
    },
    {
      "stepId": "function-explanation-cpp",
      "mentorSays": "A function is a reusable block of code. You give it inputs (called parameters), and it can return an output.",
      "example": "vector<int> plusOne(vector<int>& digits) {\n    return digits;\n}\n\nplusOne({1, 2, 3}) returns [1, 2, 4]",
      "action": "continue",
      "next": "parameter-check-cpp"
    },
    {
      "stepId": "parameter-check-cpp",
      "mentorSays": "When we write a function, we can give it inputs. These inputs are called 'parameters'. Do you know what parameters are?",
      "choices": [
        {
          "label": "Yes, I know parameters",
          "next": "loop-check-cpp"
        },
        {
          "label": "No, explain parameters",
          "next": "parameter-explanation-cpp"
        }
      ]
    },
    {
      "stepId": "parameter-explanation-cpp",
      "mentorSays": "Parameters are like variables that receive values when you call the function. When you write 'vector<int> plusOne(vector<int>& digits)', the 'digits' is a parameter - it's a placeholder that will receive an actual vector when you call the function.",
      "example": "vector<int> plusOne(vector<int>& digits) {\n  // 'digits' is a parameter\n}\n\n// When we call it:\nplusOne({1, 2, 3});\n// The vector goes into 'digits'",
      "action": "continue",
      "next": "loop-check-cpp"
    },
    {
      "stepId": "loop-check-cpp",
      "mentorSays": "To solve this lesson, we'll need to use a loop to iterate through digits from right to left. Do you know what a loop is in C++?",
      "choices": [
        {
          "label": "Yes, I know loops",
          "next": "carry-check-cpp"
        },
        {
          "label": "No, explain loops",
          "next": "loop-explanation-cpp"
        }
      ]
    },
    {
      "stepId": "carry-check-cpp",
      "mentorSays": "Do you understand how carry works in addition?",
      "choices": [
        {
          "label": "Yes, I know carry",
          "next": "coding-start-cpp"
        },
        {
          "label": "No, explain carry",
          "next": "carry-explanation-cpp"
        }
      ]
    },
    {
      "stepId": "carry-explanation-cpp",
      "mentorSays": "When adding 1 to a digit, if the result is 10 or more, we carry 1 to the next digit. For example, 9 + 1 = 10, so we write 0 and carry 1.\n\nlet digits = [9, 9, 9];\nlet carry = 1;\n\n// Add 1 with carry\nfor (let i = digits.length - 1; i >= 0; i--) {\n  let sum = digits[i] + carry;\n  digits[i] = sum % 10;\n  carry = Math.floor(sum / 10);\n}\n\nGot it?",
      "example": "let digits = [9, 9, 9];\nlet carry = 1;\n\n// Add 1 with carry\nfor (let i = digits.length - 1; i >= 0; i--) {\n  let sum = digits[i] + carry;\n  digits[i] = sum % 10;\n  carry = Math.floor(sum / 10);\n}",
      "action": "continue",
      "next": "coding-start-cpp"
    },
    {
      "stepId": "loop-explanation-cpp",
      "mentorSays": "A loop lets you repeat code multiple times. A 'for' loop in C++ runs code for each item in a sequence.\n\nFor example, you can loop through a vector from right to left.",
      "example": "for (int i = digits.size() - 1; i >= 0; i--) {\n    // Process digits[i]\n}",
      "action": "continue",
      "next": "coding-start-cpp"
    },
    {
      "stepId": "coding-start-cpp",
      "mentorSays": "Perfect! Let's implement the plus one solution in C++. We'll create a function that takes a vector of digits and returns the result after adding 1.",
      "example": "#include <vector>\n\nvector<int> plusOne(vector<int>& digits) {\n    \n}",
      "action": "continue",
      "next": "coding-init-cpp"
    },
    {
      "stepId": "coding-init-cpp",
      "mentorSays": "Initialize a carry variable starting with 1 (since we're adding 1).",
      "example": "#include <vector>\n\nvector<int> plusOne(vector<int>& digits) {\n    int carry = 1;\n}",
      "action": "continue",
      "next": "coding-loop-cpp"
    },
    {
      "stepId": "coding-loop-cpp",
      "mentorSays": "Create a loop that starts from the rightmost digit and moves left.",
      "example": "#include <vector>\n\nvector<int> plusOne(vector<int>& digits) {\n    int carry = 1;\n    \n    for (int i = digits.size() - 1; i >= 0; i--) {\n        \n    }\n}",
      "action": "continue",
      "next": "coding-add-carry-cpp"
    },
    {
      "stepId": "coding-add-carry-cpp",
      "mentorSays": "Inside the loop, add the carry to the current digit.",
      "example": "#include <vector>\n\nvector<int> plusOne(vector<int>& digits) {\n    int carry = 1;\n    \n    for (int i = digits.size() - 1; i >= 0; i--) {\n        digits[i] += carry;\n    }\n}",
      "action": "continue",
      "next": "coding-handle-carry-cpp"
    },
    {
      "stepId": "coding-handle-carry-cpp",
      "mentorSays": "If the digit is 10, set it to 0 and keep carry as 1. Otherwise, set carry to 0 and break.",
      "example": "#include <vector>\n\nvector<int> plusOne(vector<int>& digits) {\n    int carry = 1;\n    \n    for (int i = digits.size() - 1; i >= 0; i--) {\n        digits[i] += carry;\n        \n        if (digits[i] == 10) {\n            digits[i] = 0;\n            carry = 1;\n        } else {\n            carry = 0;\n            break;\n        }\n    }\n}",
      "action": "continue",
      "next": "coding-all-nines-cpp"
    },
    {
      "stepId": "coding-all-nines-cpp",
      "mentorSays": "Let's handle a special edge case: what if all digits are 9?\n\nFor [9, 9, 9], adding 1 gives [1, 0, 0, 0]. We need to create a new array with an extra digit at the front!\n\nThis happens when the carry propagates all the way to the beginning.",
      "example": "function plusOne(digits) {\n  let carry = 1;\n  \n  for (let i = digits.length - 1; i >= 0; i--) {\n    let sum = digits[i] + carry;\n    digits[i] = sum % 10;\n    carry = Math.floor(sum / 10);\n    \n    if (carry === 0) break;\n  }\n  \n  // If carry is still 1, we need a new array\n  if (carry === 1) {\n    return [1, ...digits];\n  }\n  \n  return digits;\n}",
      "action": "continue",
      "next": "test-code-cpp"
    },
    {
      "stepId": "coding-handle-overflow-cpp",
      "mentorSays": "If we still have carry after the loop, insert 1 at the beginning of the vector.",
      "example": "#include <vector>\n\nvector<int> plusOne(vector<int>& digits) {\n    int carry = 1;\n    \n    for (int i = digits.size() - 1; i >= 0; i--) {\n        digits[i] += carry;\n        \n        if (digits[i] == 10) {\n            digits[i] = 0;\n            carry = 1;\n        } else {\n            carry = 0;\n            break;\n        }\n    }\n    \n    if (carry == 1) {\n        digits.insert(digits.begin(), 1);\n    }\n    \n    return digits;\n}",
      "action": "continue",
      "next": "test-code-cpp"
    },
    {
      "stepId": "test-code-cpp",
      "mentorSays": "Perfect! Test with digits = [1, 2, 3]. Expected: [1, 2, 4]. Also test with [9, 9]. Expected: [1, 0, 0].",
      "example": "Test input 1:\ndigits = [1, 2, 3]\n\nExpected output: [1, 2, 4]\n\nTest input 2:\ndigits = [9, 9]\n\nExpected output: [1, 0, 0]\n\nBecause 99 + 1 = 100, which requires a new digit.",
      "action": "continue",
      "next": "final"
    },
    {
      "stepId": "variable-check-ts",
      "mentorSays": "Before we start coding, let me ask: Do you know what a variable is in TypeScript?",
      "choices": [
        {
          "label": "Yes, I know variables",
          "next": "function-check-ts"
        },
        {
          "label": "No, explain variables",
          "next": "variable-explanation-ts"
        }
      ]
    },
    {
      "stepId": "variable-explanation-ts",
      "mentorSays": "A variable is like a labeled box where you can store a value. You give it a name (like 'carry' or 'i') and put data inside it.\n\nFor example, 'let carry = 1;' creates a variable that stores the number 1.",
      "example": "let carry = 1;\nlet i = digits.length - 1;\n\nNow these variables store numbers\nWe can use them in our calculations",
      "action": "continue",
      "next": "function-check-ts"
    },
    {
      "stepId": "function-check-ts",
      "mentorSays": "Before we start coding, let me ask: Do you know what a function is in TypeScript?",
      "choices": [
        {
          "label": "Yes, I know functions",
          "next": "parameter-check-ts"
        },
        {
          "label": "No, explain functions",
          "next": "function-explanation-ts"
        }
      ]
    },
    {
      "stepId": "function-explanation-ts",
      "mentorSays": "A function is a reusable block of code. You give it inputs (called parameters), and it can return an output.",
      "example": "function plusOne(digits: number[]): number[] {\n    return digits;\n}\n\nplusOne([1, 2, 3]) returns [1, 2, 4]",
      "action": "continue",
      "next": "parameter-check-ts"
    },
    {
      "stepId": "parameter-check-ts",
      "mentorSays": "When we write a function, we can give it inputs. These inputs are called 'parameters'. Do you know what parameters are?",
      "choices": [
        {
          "label": "Yes, I know parameters",
          "next": "loop-check-ts"
        },
        {
          "label": "No, explain parameters",
          "next": "parameter-explanation-ts"
        }
      ]
    },
    {
      "stepId": "parameter-explanation-ts",
      "mentorSays": "Parameters are like variables that receive values when you call the function. When you write 'function plusOne(digits: number[]): number[]', the 'digits' is a parameter - it's a placeholder that will receive an actual array when you call the function.",
      "example": "function plusOne(digits: number[]): number[] {\n  // 'digits' is a parameter\n}\n\n// When we call it:\nplusOne([1, 2, 3]);\n// The array goes into 'digits'",
      "action": "continue",
      "next": "loop-check-ts"
    },
    {
      "stepId": "loop-check-ts",
      "mentorSays": "To solve this lesson, we'll need to use a loop to iterate through digits from right to left. Do you know what a loop is in TypeScript?",
      "choices": [
        {
          "label": "Yes, I know loops",
          "next": "carry-check-ts"
        },
        {
          "label": "No, explain loops",
          "next": "loop-explanation-ts"
        }
      ]
    },
    {
      "stepId": "carry-check-ts",
      "mentorSays": "Do you understand how carry works in addition?",
      "choices": [
        {
          "label": "Yes, I know carry",
          "next": "coding-start-ts"
        },
        {
          "label": "No, explain carry",
          "next": "carry-explanation-ts"
        }
      ]
    },
    {
      "stepId": "carry-explanation-ts",
      "mentorSays": "When adding 1 to a digit, if the result is 10 or more, we carry 1 to the next digit. For example, 9 + 1 = 10, so we write 0 and carry 1.\n\nlet digits = [9, 9, 9];\nlet carry = 1;\n\n// Add 1 with carry\nfor (let i = digits.length - 1; i >= 0; i--) {\n  let sum = digits[i] + carry;\n  digits[i] = sum % 10;\n  carry = Math.floor(sum / 10);\n}\n\nGot it?",
      "example": "let digits = [9, 9, 9];\nlet carry = 1;\n\n// Add 1 with carry\nfor (let i = digits.length - 1; i >= 0; i--) {\n  let sum = digits[i] + carry;\n  digits[i] = sum % 10;\n  carry = Math.floor(sum / 10);\n}",
      "action": "continue",
      "next": "coding-start-ts"
    },
    {
      "stepId": "loop-explanation-ts",
      "mentorSays": "A loop lets you repeat code multiple times. A 'for' loop in TypeScript runs code for each item in a sequence.\n\nFor example, you can loop through an array from right to left.",
      "example": "for (let i = digits.length - 1; i >= 0; i--) {\n  // Process digits[i]\n}",
      "action": "continue",
      "next": "coding-start-ts"
    },
    {
      "stepId": "coding-start-ts",
      "mentorSays": "Perfect! Let's implement the plus one solution in TypeScript. We'll create a function that takes an array of digits and returns the result after adding 1.",
      "example": "function plusOne(digits: number[]): number[] {\n  \n}",
      "action": "continue",
      "next": "coding-init-ts"
    },
    {
      "stepId": "coding-init-ts",
      "mentorSays": "Initialize a carry variable starting with 1 (since we're adding 1).",
      "example": "function plusOne(digits: number[]): number[] {\n  let carry = 1;\n}",
      "action": "continue",
      "next": "coding-loop-ts"
    },
    {
      "stepId": "coding-loop-ts",
      "mentorSays": "Create a loop that starts from the rightmost digit and moves left.",
      "example": "function plusOne(digits: number[]): number[] {\n  let carry = 1;\n  \n  for (let i = digits.length - 1; i >= 0; i--) {\n    \n  }\n}",
      "action": "continue",
      "next": "coding-add-carry-ts"
    },
    {
      "stepId": "coding-add-carry-ts",
      "mentorSays": "Inside the loop, add the carry to the current digit.",
      "example": "function plusOne(digits: number[]): number[] {\n  let carry = 1;\n  \n  for (let i = digits.length - 1; i >= 0; i--) {\n    digits[i] += carry;\n  }\n}",
      "action": "continue",
      "next": "coding-handle-carry-ts"
    },
    {
      "stepId": "coding-handle-carry-ts",
      "mentorSays": "If the digit is 10, set it to 0 and keep carry as 1. Otherwise, set carry to 0 and break.",
      "example": "function plusOne(digits: number[]): number[] {\n  let carry = 1;\n  \n  for (let i = digits.length - 1; i >= 0; i--) {\n    digits[i] += carry;\n    \n    if (digits[i] === 10) {\n      digits[i] = 0;\n      carry = 1;\n    } else {\n      carry = 0;\n      break;\n    }\n  }\n}",
      "action": "continue",
      "next": "coding-all-nines-ts"
    },
    {
      "stepId": "coding-all-nines-ts",
      "mentorSays": "Let's handle a special edge case: what if all digits are 9?\n\nFor [9, 9, 9], adding 1 gives [1, 0, 0, 0]. We need to create a new array with an extra digit at the front!\n\nThis happens when the carry propagates all the way to the beginning.",
      "example": "function plusOne(digits) {\n  let carry = 1;\n  \n  for (let i = digits.length - 1; i >= 0; i--) {\n    let sum = digits[i] + carry;\n    digits[i] = sum % 10;\n    carry = Math.floor(sum / 10);\n    \n    if (carry === 0) break;\n  }\n  \n  // If carry is still 1, we need a new array\n  if (carry === 1) {\n    return [1, ...digits];\n  }\n  \n  return digits;\n}",
      "action": "continue",
      "next": "test-code-ts"
    },
    {
      "stepId": "coding-handle-overflow-ts",
      "mentorSays": "If we still have carry after the loop, add a new digit 1 at the beginning of the array.",
      "example": "function plusOne(digits: number[]): number[] {\n  let carry = 1;\n  \n  for (let i = digits.length - 1; i >= 0; i--) {\n    digits[i] += carry;\n    \n    if (digits[i] === 10) {\n      digits[i] = 0;\n      carry = 1;\n    } else {\n      carry = 0;\n      break;\n    }\n  }\n  \n  if (carry === 1) {\n    digits.unshift(1);\n  }\n  \n  return digits;\n}",
      "action": "continue",
      "next": "test-code-ts"
    },
    {
      "stepId": "test-code-ts",
      "mentorSays": "Perfect! Test with digits = [1, 2, 3]. Expected: [1, 2, 4]. Also test with [9, 9]. Expected: [1, 0, 0].",
      "example": "Test input 1:\ndigits = [1, 2, 3]\n\nExpected output: [1, 2, 4]\n\nTest input 2:\ndigits = [9, 9]\n\nExpected output: [1, 0, 0]\n\nBecause 99 + 1 = 100, which requires a new digit.",
      "action": "continue",
      "next": "final"
    },
    {
      "stepId": "final",
      "mentorSays": "🎉 Well done! You've learned to think through the Plus One lesson. You understand what the lesson asks for, how to add 1 to a number represented as an array of digits, and how to handle carry propagation correctly.\n\nThis kind of thinking—handling carry in digit-by-digit arithmetic—is essential for solving many number manipulation lessons. Keep practicing!",
      "action": "continue"
    }
  ]
}