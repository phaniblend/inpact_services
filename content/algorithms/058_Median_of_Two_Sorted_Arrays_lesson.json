{
  "success": true,
  "config": {
    "lessonId": "ai-algorithms-58",
    "track": "algorithms",
    "lessonNum": 58,
    "title": "Median of Two Sorted Arrays",
    "shortName": "P58",
    "intro": {
      "tag": "LESSON #58",
      "title": "Median of Two Sorted Arrays",
      "body": "In this lesson, you will build a function that finds the median value from two separate sorted arrays. You will learn the core concept of merging arrays and applying binary search to efficiently locate the median without fully combining the arrays. The main technologies involved are JavaScript arrays, conditional logic, and mathematical operations for index calculations. This beginner-friendly approach will help you understand how to handle sorted data and compute statistical measures programmatically.",
      "usecase": "Finding the median of two sorted arrays is a core pattern in data processing and analytics applications. For example, a financial dashboard might need to merge and analyze two sorted streams of transaction data from different sources to calculate a central spending trend. Similarly, a real-time monitoring system could combine sorted log entries from multiple servers to determine the median response time, helping identify performance bottlenecks. This algorithm is essential for any developer building systems that require efficient merging and statistical analysis of ordered datasets."
    },
    "objectives": [
      "Implement a function to merge two sorted arrays into a single sorted array",
      "Apply logic to calculate the median from a sorted array of numbers",
      "Construct a complete solution that combines array merging and median calculation",
      "Manipulate array indices to efficiently find median elements",
      "Initialize and configure test cases to validate the median calculation",
      "Integrate edge case handling for arrays of different lengths",
      "Control the flow of operations to handle empty or single-element arrays"
    ],
    "steps": [
      {
        "type": "reasoning",
        "id": "step1",
        "phase": "Step 1 of 16",
        "title": "Welcome",
        "content": {
          "body": "Finding the median of two sorted arrays efficiently is a classic challenge. Today we'll learn how to use binary search to solve it without merging the arrays. You'll discover how to cut the search space in half at each step.",
          "title": "Welcome"
        }
      },
      {
        "type": "reasoning",
        "id": "step2",
        "phase": "Step 2 of 16",
        "title": "What you'll learn",
        "content": {
          "body": "1. Explain why binary search works for finding the median of two sorted arrays.\n2. Implement the partition approach using binary search.\n3. Handle edge cases when the target partition isn't found immediately.\n4. Calculate the median correctly from partition points.",
          "title": "What you'll learn"
        }
      },
      {
        "type": "discovery",
        "id": "step3",
        "phase": "Step 3 of 16",
        "title": "Think about it",
        "content": {
          "body": "You have two sorted arrays: [1, 3, 5] and [2, 4, 6]. The combined sorted array would be [1, 2, 3, 4, 5, 6]. The median is (3 + 4)/2 = 3.5. How could you find this without actually merging the arrays?",
          "title": "Think about it"
        }
      },
      {
        "type": "discovery",
        "id": "step4",
        "phase": "Step 4 of 16",
        "title": "Look for patterns",
        "content": {
          "body": "In the combined array, the median sits at the boundary between the left and right halves. For two arrays, we need to find partition points that split both arrays so that all elements on the left are ≤ all elements on the right. How might you find such partitions?",
          "title": "Look for patterns"
        }
      },
      {
        "type": "reflection",
        "id": "step5",
        "phase": "Step 5 of 16",
        "title": "What you considered",
        "content": {
          "body": "You might have thought about checking different split points manually. That's like a linear search through possible partitions - checking each one from start to end.",
          "title": "What you considered"
        }
      },
      {
        "type": "scale-problem",
        "id": "step6",
        "phase": "Step 6 of 16",
        "title": "Why linear search fails",
        "content": {
          "body": "For arrays of length m and n, there are m+n+1 possible partition points to check. Linear checking would be O(m+n), which is too slow for large arrays. We need a smarter way to find the correct partition.",
          "title": "Why linear search fails"
        }
      },
      {
        "type": "concept-bridge",
        "id": "step7",
        "phase": "Step 7 of 16",
        "title": "Key insight",
        "content": {
          "body": "Since the arrays are sorted, we can use binary search on the smaller array to find the correct partition. At each step, we check if our partition is correct - if not, we eliminate half of the remaining search space. This works because only one half can contain the correct partition.",
          "title": "Key insight",
          "visualMetaphor": "Repeatedly cut the range in half; only one half can contain the answer."
        }
      },
      {
        "type": "reveal-idea",
        "id": "step8",
        "phase": "Step 8 of 16",
        "title": "The binary search approach",
        "content": {
          "body": "We'll binary search on the smaller array to find partition points i and j such that: 1) All elements left of i and j are ≤ all elements right of i and j, and 2) The total elements on left ≈ total on right. When we find this, the median comes from elements around these partitions.",
          "title": "The binary search approach"
        }
      },
      {
        "type": "flow-explainer",
        "id": "step9",
        "phase": "Step 9 of 16",
        "title": "Algorithm steps",
        "content": {
          "body": "1. Ensure nums1 is the smaller array (swap if needed)\n2. Binary search on nums1 to find partition i\n3. Calculate partition j based on i\n4. Check if partition is correct (maxLeft ≤ minRight)\n5. If not, adjust search range and repeat\n6. When correct partition found, calculate median",
          "title": "Algorithm steps"
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 16",
        "title": "Set up the function",
        "instruction": "Create the function skeleton that takes two arrays and returns their median.",
        "hint": "Define a function with two parameters.",
        "analogousExample": "",
        "seedCode": "function findMedianSortedArrays(nums1, nums2) {\n    // We'll implement the solution here\n}",
        "expectedOutcome": "Function declaration ready for implementation",
        "successCriteria": [],
        "feedbackCorrect": "Great! You've set up the function structure.",
        "feedbackPartial": "Make sure you have both parameters.",
        "feedbackWrong": "Define a function named findMedianSortedArrays that takes nums1 and nums2.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "function",
            "findMedianSortedArrays",
            "nums1",
            "nums2"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step11",
        "type": "question",
        "phase": "Step 11 of 16",
        "title": "Ensure nums1 is smaller",
        "instruction": "At the beginning of the function, ensure nums1 is the smaller array. If not, swap them.",
        "hint": "Compare lengths and swap if needed.",
        "analogousExample": "",
        "seedCode": "function findMedianSortedArrays(nums1, nums2) {\n    // Ensure nums1 is the smaller array\n    if (nums1.length > nums2.length) {\n        [nums1, nums2] = [nums2, nums1];\n    }\n    \n    const m = nums1.length;\n    const n = nums2.length;\n}",
        "expectedOutcome": "nums1 is guaranteed to be the smaller array",
        "successCriteria": [],
        "feedbackCorrect": "Perfect! This ensures we binary search on the smaller array.",
        "feedbackPartial": "Check that you're swapping when nums1 is larger.",
        "feedbackWrong": "Swap nums1 and nums2 if nums1.length > nums2.length, then store lengths in m and n.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "if",
            "nums1.length",
            ">",
            "nums2.length",
            "[nums1, nums2]",
            "[nums2, nums1]",
            "m =",
            "n ="
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step12",
        "type": "question",
        "phase": "Step 12 of 16",
        "title": "Set up binary search bounds",
        "instruction": "Initialize the binary search variables: low, high, and total length for median calculation.",
        "hint": "We'll search on nums1, so low starts at 0, high at m.",
        "analogousExample": "",
        "seedCode": "function findMedianSortedArrays(nums1, nums2) {\n    if (nums1.length > nums2.length) {\n        [nums1, nums2] = [nums2, nums1];\n    }\n    \n    const m = nums1.length;\n    const n = nums2.length;\n    \n    // Binary search setup\n    let low = 0;\n    let high = m;\n    const total = m + n;\n    const half = Math.floor((total + 1) / 2);\n}",
        "expectedOutcome": "Binary search bounds and helper variables initialized",
        "successCriteria": [],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "Make sure you have low, high, total, and half variables.",
        "feedbackWrong": "Initialize low=0, high=m, total=m+n, and half=floor((total+1)/2).",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "low = 0",
            "high = m",
            "total = m + n",
            "half =",
            "Math.floor",
            "(total + 1) / 2"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step13",
        "type": "question",
        "phase": "Step 13 of 16",
        "title": "Implement binary search loop",
        "instruction": "Create the while loop for binary search. Inside, calculate partition points i and j.",
        "hint": "Continue while low <= high. Calculate i as midpoint, j based on i.",
        "analogousExample": "",
        "seedCode": "function findMedianSortedArrays(nums1, nums2) {\n    if (nums1.length > nums2.length) {\n        [nums1, nums2] = [nums2, nums1];\n    }\n    \n    const m = nums1.length;\n    const n = nums2.length;\n    \n    let low = 0;\n    let high = m;\n    const total = m + n;\n    const half = Math.floor((total + 1) / 2);\n    \n    while (low <= high) {\n        const i = Math.floor((low + high) / 2);  // Partition in nums1\n        const j = half - i;                      // Partition in nums2\n    }\n}",
        "expectedOutcome": "Binary search loop with partition calculations",
        "successCriteria": [],
        "feedbackCorrect": "Great! The loop will search for the correct partition.",
        "feedbackPartial": "Check that i is the midpoint and j depends on i and half.",
        "feedbackWrong": "Create while(low <= high) loop with i = floor((low+high)/2) and j = half - i.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "while",
            "low <= high",
            "i =",
            "Math.floor",
            "(low + high) / 2",
            "j = half - i"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step14",
        "type": "question",
        "phase": "Step 14 of 16",
        "title": "Check partition and adjust search",
        "instruction": "Inside the loop, check if the partition is correct and adjust search bounds accordingly.",
        "hint": "Get elements around partitions and compare maxLeft with minRight.",
        "analogousExample": "",
        "seedCode": "function findMedianSortedArrays(nums1, nums2) {\n    if (nums1.length > nums2.length) {\n        [nums1, nums2] = [nums2, nums1];\n    }\n    \n    const m = nums1.length;\n    const n = nums2.length;\n    \n    let low = 0;\n    let high = m;\n    const total = m + n;\n    const half = Math.floor((total + 1) / 2);\n    \n    while (low <= high) {\n        const i = Math.floor((low + high) / 2);\n        const j = half - i;\n        \n        // Get elements around partitions\n        const nums1Left = i > 0 ? nums1[i - 1] : -Infinity;\n        const nums1Right = i < m ? nums1[i] : Infinity;\n        const nums2Left = j > 0 ? nums2[j - 1] : -Infinity;\n        const nums2Right = j < n ? nums2[j] : Infinity;\n        \n        // Check if partition is correct\n        if (nums1Left <= nums2Right && nums2Left <= nums1Right) {\n            // Partition found - we'll calculate median next\n        } else if (nums1Left > nums2Right) {\n            high = i - 1;  // Move partition left in nums1\n        } else {\n            low = i + 1;   // Move partition right in nums1\n        }\n    }\n}",
        "expectedOutcome": "Partition validation and search adjustment logic",
        "successCriteria": [],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "Make sure you handle edge cases with Infinity/-Infinity and adjust bounds correctly.",
        "feedbackWrong": "Get elements around partitions with boundary checks, then adjust high if nums1Left > nums2Right, else adjust low.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "nums1Left",
            "nums1Right",
            "nums2Left",
            "nums2Right",
            "Infinity",
            "-Infinity",
            "if (nums1Left <= nums2Right && nums2Left <= nums1Right)",
            "high = i - 1",
            "low = i + 1"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step15",
        "type": "question",
        "phase": "Step 15 of 16",
        "title": "Calculate and return median",
        "instruction": "Complete the function by calculating the median when correct partition is found and returning it.",
        "hint": "Check if total length is odd or even to calculate median correctly.",
        "analogousExample": "",
        "seedCode": "function findMedianSortedArrays(nums1, nums2) {\n    if (nums1.length > nums2.length) {\n        [nums1, nums2] = [nums2, nums1];\n    }\n    \n    const m = nums1.length;\n    const n = nums2.length;\n    \n    let low = 0;\n    let high = m;\n    const total = m + n;\n    const half = Math.floor((total + 1) / 2);\n    \n    while (low <= high) {\n        const i = Math.floor((low + high) / 2);\n        const j = half - i;\n        \n        const nums1Left = i > 0 ? nums1[i - 1] : -Infinity;\n        const nums1Right = i < m ? nums1[i] : Infinity;\n        const nums2Left = j > 0 ? nums2[j - 1] : -Infinity;\n        const nums2Right = j < n ? nums2[j] : Infinity;\n        \n        if (nums1Left <= nums2Right && nums2Left <= nums1Right) {\n            // Calculate median\n            const maxLeft = Math.max(nums1Left, nums2Left);\n            if (total % 2 === 1) {\n                return maxLeft;\n            }\n            const minRight = Math.min(nums1Right, nums2Right);\n            return (maxLeft + minRight) / 2;\n        } else if (nums1Left > nums2Right) {\n            high = i - 1;\n        } else {\n            low = i + 1;\n        }\n    }\n    \n    return 0; // Should never reach here with valid input\n}",
        "expectedOutcome": "Complete function that returns the median",
        "successCriteria": [],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "Check your median calculation for both odd and even total lengths.",
        "feedbackWrong": "When partition is correct, calculate maxLeft = max(nums1Left, nums2Left). If total is odd, return maxLeft. If even, return (maxLeft + minRight)/2 where minRight = min(nums1Right, nums2Right).",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "Math.max(nums1Left, nums2Left)",
            "total % 2 === 1",
            "return maxLeft",
            "Math.min(nums1Right, nums2Right)",
            "return (maxLeft + minRight) / 2"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "type": "complete",
        "id": "step16",
        "phase": "Step 16 of 16",
        "title": "Well done!",
        "content": {
          "body": "You've successfully implemented an efficient O(log(min(m,n))) solution for finding the median of two sorted arrays using binary search. Remember: 1) Always work with the smaller array, 2) The key is finding partitions where maxLeft ≤ minRight, and 3) Binary search cuts the possibilities in half each time. This approach avoids merging the arrays entirely!",
          "title": "Well done!"
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Welcome",
        "id": "step1"
      },
      {
        "label": "What you'll learn",
        "id": "step2"
      },
      {
        "label": "Think about it",
        "id": "step3"
      },
      {
        "label": "Look for patterns",
        "id": "step4"
      },
      {
        "label": "What you considered",
        "id": "step5"
      },
      {
        "label": "Why linear search fails",
        "id": "step6"
      },
      {
        "label": "Key insight",
        "id": "step7"
      },
      {
        "label": "The binary search approach",
        "id": "step8"
      },
      {
        "label": "Algorithm steps",
        "id": "step9"
      },
      {
        "label": "Set up the function",
        "id": "step10"
      },
      {
        "label": "Ensure nums1 is smaller",
        "id": "step11"
      },
      {
        "label": "Set up binary search bounds",
        "id": "step12"
      },
      {
        "label": "Implement binary search loop",
        "id": "step13"
      },
      {
        "label": "Check partition and adjust search",
        "id": "step14"
      },
      {
        "label": "Calculate and return median",
        "id": "step15"
      },
      {
        "label": "Well done!",
        "id": "step16"
      }
    ],
    "algorithmFamily": "binary-search",
    "familyTeachingProfile": {
      "mentalModel": "Cut the search space in half",
      "conceptBridge": "Explain sorted prerequisite and midpoint reasoning; focus on eliminating half at each step.",
      "visualMetaphor": "Repeatedly cut the range in half; only one half can contain the answer.",
      "commonConfusions": [
        "Why must the array be sorted?",
        "What if the target is not found?"
      ]
    }
  },
  "source": "content"
}