{
  "success": true,
  "config": {
    "lessonId": "algorithms-two-sum",
    "track": "algorithms",
    "problemNum": 1,
    "title": "Solving Two Sum Efficiently with Hash Maps in JavaScript",
    "shortName": "Two Sum",
    "intro": {
      "tag": "ALGORITHM",
      "title": "Solving Two Sum Efficiently with Hash Maps in JavaScript",
      "body": "This lesson guides you through solving the Two Sum problem, starting with a brute force approach, analyzing its inefficiency, and then implementing an optimized solution using a hash map. You'll learn how to think about problem constraints, trade-offs, and how to use data structures effectively.",
      "usecase": "The Two Sum problem is foundational for understanding how to optimize algorithms using data structures. It's commonly used in financial analysis (e.g., finding two transactions that sum to a suspicious amount), data validation, and pair-based searches in large datasets."
    },
    "objectives": [
      "Analyze the time complexity of a brute force solution for Two Sum",
      "Explain the role of a hash map in reducing time complexity from O(n²) to O(n)",
      "Implement a hash map to store and retrieve indices of array elements",
      "Compute the complement of a number relative to a target sum",
      "Handle edge cases such as empty arrays or no valid pairs",
      "Compare and contrast brute force and optimized solutions in terms of efficiency"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 11",
        "title": "Understand the Two Sum Problem",
        "instruction": "Read the problem statement carefully: Given an array of integers `nums` and an integer `target`, return the indices of the two numbers that add up to `target`. Each input has exactly one solution, and you may not use the same element twice.\n\n**Analogy:** Imagine you are a cashier. You need to find two receipts in a pile that add up to a specific amount. You can only pick each receipt once.\n\n**Hint:** Focus on what you need to return: the indices of the two numbers, not the numbers themselves.",
        "hint": "Focus on what you need to return: the indices of the two numbers, not the numbers themselves.",
        "seedCode": "// Two Sum: given nums and target, return indices of two numbers that add to target.\n// You may not use the same element twice.\n\nfunction twoSum(nums, target) {\n  // Your solution here\n}\n",
        "expectedOutcome": "You understand the problem: return indices of two numbers that add to target.",
        "successCriteria": ["Understand array indices and target sum."],
        "feedbackCorrect": "Great, you've got the problem clear.",
        "feedbackPartial": "Review: we return indices of the two numbers, not the values.",
        "feedbackWrong": "Return the indices of the two numbers that add up to target.",
        "evaluation": {
          "mode": "keyword_match",
          "required": ["function", "twoSum", "nums", "target"],
          "partialThreshold": 0.5,
          "correctThreshold": 0.6
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 11",
        "title": "Brainstorm a Brute Force Solution",
        "instruction": "Write down how you would solve this problem by checking every possible pair of numbers in the array. Use two nested loops to compare each element with every other element that comes after it.\n\n**Analogy:** This is like checking every pair of receipts in the pile to see if they add up to the target amount.\n\n**Hint:** The outer loop runs from the first element to the second-to-last element. The inner loop runs from the current element of the outer loop to the end of the array.",
        "hint": "The outer loop runs from the first element to the second-to-last. The inner loop runs from the current element of the outer loop to the end of the array.",
        "seedCode": "function twoSum(nums, target) {\n  // Write your brute force solution here: two nested loops\n  // Outer: i from 0 to nums.length - 1\n  // Inner: j from i + 1 to nums.length - 1\n  // If nums[i] + nums[j] === target, return [i, j]\n}\n",
        "expectedOutcome": "Two nested loops; check each pair; return [i, j] when sum equals target.",
        "successCriteria": ["Use two loops", "Compare nums[i] + nums[j] to target", "Return indices."],
        "feedbackCorrect": "Good, you have the brute force structure.",
        "feedbackPartial": "Use j = i + 1 so you don't use the same element twice.",
        "feedbackWrong": "Use two loops: for i, then for j from i+1; if nums[i] + nums[j] === target return [i, j].",
        "evaluation": {
          "mode": "keyword_match",
          "required": ["for", "i", "j", "nums[i]", "nums[j]", "target", "return"],
          "partialThreshold": 0.4,
          "correctThreshold": 0.7
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 11",
        "title": "Implement the Brute Force Solution",
        "instruction": "Write the code for the brute force solution. Use two loops: the outer loop picks the first number, and the inner loop picks the second number. If their sum equals the target, return their indices.\n\n**Hint:** Use `nums[i] + nums[j] === target` to check if the two numbers add up to the target.",
        "hint": "Use nums[i] + nums[j] === target to check; return [i, j].",
        "seedCode": "function twoSum(nums, target) {\n  for (let i = 0; i < nums.length; i++) {\n    for (let j = i + 1; j < nums.length; j++) {\n      if (nums[i] + nums[j] === target) {\n        return [i, j];\n      }\n    }\n  }\n  return [];\n}\n",
        "expectedOutcome": "Brute force works: O(n²) time.",
        "successCriteria": ["Nested loops with i and j", "Sum check", "Return [i, j]."],
        "feedbackCorrect": "Correct brute force implementation.",
        "feedbackPartial": "Ensure j starts at i + 1 and you return [i, j].",
        "feedbackWrong": "Use for (i) and for (j = i+1); if (nums[i] + nums[j] === target) return [i, j].",
        "evaluation": {
          "mode": "keyword_match",
          "required": ["for", "let i", "let j", "i + 1", "nums[i]", "nums[j]", "target", "return [i, j]"],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 11",
        "title": "Analyze Time Complexity of Brute Force",
        "instruction": "Calculate the time complexity of your brute force solution. How many comparisons are made for an array of size `n`? The result is **O(n²)** — the runtime grows quadratically with input size.\n\n**Hint:** Count the number of iterations for the inner loop for each iteration of the outer loop.",
        "hint": "Inner loop runs about n times per outer iteration; outer runs n times → O(n²).",
        "seedCode": "// Brute force two sum: O(n²) time complexity\n// Why? For each of n elements we check up to n-1 pairs → n*(n-1)/2 ≈ n²\n\nfunction twoSum(nums, target) {\n  for (let i = 0; i < nums.length; i++) {\n    for (let j = i + 1; j < nums.length; j++) {\n      if (nums[i] + nums[j] === target) return [i, j];\n    }\n  }\n  return [];\n}\n",
        "expectedOutcome": "Acknowledge O(n²) time complexity.",
        "successCriteria": ["Understand quadratic complexity."],
        "feedbackCorrect": "Right — we need a better approach.",
        "feedbackPartial": "Brute force does about n² comparisons.",
        "feedbackWrong": "Time complexity is O(n²) because of nested loops.",
        "evaluation": {
          "mode": "keyword_match",
          "required": ["n", "O"],
          "partialThreshold": 0.3,
          "correctThreshold": 0.6
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 11",
        "title": "Introduce the Concept of a Hash Map",
        "instruction": "Learn about hash maps in JavaScript. A hash map (or object) allows you to store key-value pairs and perform lookups in constant time, O(1). Think of it as a dictionary where you can quickly look up a key to get its value. We'll use the array values as keys and indices as values.\n\n**Hint:** In JavaScript, an object can be used as a hash map: `{ 2: 0 }` means the value `2` is at index `0`.",
        "hint": "Object: keys = array values, values = indices. O(1) lookup.",
        "seedCode": "// Hash map: store value → index. Lookup in O(1).\n// We'll use one pass: for each num, check if (target - num) is in the map.\n\nfunction twoSum(nums, target) {\n  // We'll add a hash map in the next steps\n  return [];\n}\n",
        "expectedOutcome": "Understand that a hash map gives O(1) lookup.",
        "successCriteria": ["Understand hash map for O(1) lookup."],
        "feedbackCorrect": "Good — we'll use it to optimize.",
        "feedbackPartial": "Hash map stores key-value pairs; lookup is O(1).",
        "feedbackWrong": "Use an object as a hash map for constant-time lookup.",
        "evaluation": {
          "mode": "keyword_match",
          "required": ["function", "twoSum", "return"],
          "partialThreshold": 0.5,
          "correctThreshold": 0.6
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 11",
        "title": "Initialize a Hash Map",
        "instruction": "Create an empty object called `numMap` to store numbers and their indices as you iterate through the array.\n\n**Hint:** Use `let numMap = {};`",
        "hint": "Use let numMap = {};",
        "seedCode": "function twoSum(nums, target) {\n  let numMap = {};\n  // Your code here\n}\n",
        "expectedOutcome": "numMap is initialized as an empty object.",
        "successCriteria": ["Declare numMap as empty object."],
        "feedbackCorrect": "Good, hash map is ready.",
        "feedbackPartial": "Initialize with let numMap = {};",
        "feedbackWrong": "Use let numMap = {}; to create the hash map.",
        "evaluation": {
          "mode": "keyword_match",
          "required": ["numMap", "{}"],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 11",
        "title": "Iterate Through the Array",
        "instruction": "Write a loop to go through each element in the `nums` array. Use `for (let i = 0; i < nums.length; i++)`.",
        "hint": "Use a for loop with index i from 0 to nums.length.",
        "seedCode": "function twoSum(nums, target) {\n  let numMap = {};\n  for (let i = 0; i < nums.length; i++) {\n    // Your code here\n  }\n}\n",
        "expectedOutcome": "Loop over each index i.",
        "successCriteria": ["For loop over nums with index i."],
        "feedbackCorrect": "Good, we're iterating.",
        "feedbackPartial": "Use for (let i = 0; i < nums.length; i++).",
        "feedbackWrong": "Add: for (let i = 0; i < nums.length; i++) { ... }",
        "evaluation": {
          "mode": "keyword_match",
          "required": ["for", "let i", "nums.length", "i++"],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 11",
        "title": "Calculate the Complement",
        "instruction": "For each number in the array, calculate its complement: the value needed to reach the target. Store this in a variable called `complement`.\n\n**Hint:** Use `let complement = target - nums[i];`",
        "hint": "complement = target - nums[i];",
        "seedCode": "function twoSum(nums, target) {\n  let numMap = {};\n  for (let i = 0; i < nums.length; i++) {\n    let complement = target - nums[i];\n    // Your code here\n  }\n}\n",
        "expectedOutcome": "complement is computed as target - nums[i].",
        "successCriteria": ["Compute complement = target - nums[i]."],
        "feedbackCorrect": "Good, complement is the value we need to find.",
        "feedbackPartial": "Use let complement = target - nums[i];",
        "feedbackWrong": "Add: let complement = target - nums[i];",
        "evaluation": {
          "mode": "keyword_match",
          "required": ["complement", "target", "nums[i]"],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 11",
        "title": "Check if Complement Exists in Hash Map",
        "instruction": "Before storing the current number, check if its complement exists in `numMap`. If it does, return the indices of the complement and the current number.\n\n**Hint:** Use `if (complement in numMap)` then `return [numMap[complement], i];`",
        "hint": "if (complement in numMap) return [numMap[complement], i];",
        "seedCode": "function twoSum(nums, target) {\n  let numMap = {};\n  for (let i = 0; i < nums.length; i++) {\n    let complement = target - nums[i];\n    if (complement in numMap) {\n      return [numMap[complement], i];\n    }\n    // Store current number in map (next step)\n  }\n}\n",
        "expectedOutcome": "If complement is in numMap, return [numMap[complement], i].",
        "successCriteria": ["Check complement in numMap", "Return both indices."],
        "feedbackCorrect": "Correct — we found a pair.",
        "feedbackPartial": "Check (complement in numMap) and return [numMap[complement], i].",
        "feedbackWrong": "Add: if (complement in numMap) return [numMap[complement], i];",
        "evaluation": {
          "mode": "keyword_match",
          "required": ["complement", "numMap", "return", "numMap[complement]", "i"],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 11",
        "title": "Store Current Number in Hash Map",
        "instruction": "If the complement is not found, store the current number (`nums[i]`) as a key in `numMap`, with its index (`i`) as the value.\n\n**Hint:** Use `numMap[nums[i]] = i;`",
        "hint": "numMap[nums[i]] = i;",
        "seedCode": "function twoSum(nums, target) {\n  let numMap = {};\n  for (let i = 0; i < nums.length; i++) {\n    let complement = target - nums[i];\n    if (complement in numMap) {\n      return [numMap[complement], i];\n    }\n    numMap[nums[i]] = i;\n  }\n  // Handle no solution (next step)\n}\n",
        "expectedOutcome": "Store nums[i] → i in numMap when complement not found.",
        "successCriteria": ["Assign numMap[nums[i]] = i."],
        "feedbackCorrect": "Good, we're building the map as we go.",
        "feedbackPartial": "Use numMap[nums[i]] = i; after the complement check.",
        "feedbackWrong": "Add: numMap[nums[i]] = i; so we can look up this value later.",
        "evaluation": {
          "mode": "keyword_match",
          "required": ["numMap[nums[i]]", "i"],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step11",
        "type": "question",
        "phase": "Step 11 of 11",
        "title": "Handle Edge Cases",
        "instruction": "Test your function with edge cases: empty array, no solution, duplicate numbers. Add a `return [];` at the end of the function for cases where no solution is found (though the problem states there is exactly one solution).",
        "hint": "Add return []; after the loop.",
        "seedCode": "function twoSum(nums, target) {\n  let numMap = {};\n  for (let i = 0; i < nums.length; i++) {\n    let complement = target - nums[i];\n    if (complement in numMap) {\n      return [numMap[complement], i];\n    }\n    numMap[nums[i]] = i;\n  }\n  return [];\n}\n",
        "expectedOutcome": "Complete O(n) solution with fallback return [].",
        "successCriteria": ["Return [] when no pair found."],
        "feedbackCorrect": "Well done! You've implemented the optimized Two Sum.",
        "feedbackPartial": "Add return []; after the loop.",
        "feedbackWrong": "After the loop, add return []; for the no-solution case.",
        "evaluation": {
          "mode": "keyword_match",
          "required": ["return", "[]"],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      { "id": "intro", "label": "Intro" },
      { "id": "objectives", "label": "Objectives" },
      { "id": "step1", "label": "Understand the Two Sum Problem" },
      { "id": "step2", "label": "Brainstorm Brute Force" },
      { "id": "step3", "label": "Implement Brute Force" },
      { "id": "step4", "label": "Analyze O(n²)" },
      { "id": "step5", "label": "Introduce Hash Map" },
      { "id": "step6", "label": "Initialize Hash Map" },
      { "id": "step7", "label": "Iterate Array" },
      { "id": "step8", "label": "Calculate Complement" },
      { "id": "step9", "label": "Check Complement in Map" },
      { "id": "step10", "label": "Store in Hash Map" },
      { "id": "step11", "label": "Handle Edge Cases" }
    ]
  },
  "source": "content"
}
