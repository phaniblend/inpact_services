{
  "success": true,
  "config": {
    "lessonId": "ai-angular-96",
    "track": "angular",
    "lessonNum": 96,
    "title": "Test useFetch",
    "shortName": "P96",
    "intro": {
      "tag": "LESSON #96",
      "title": "Test useFetch",
      "body": "In this lesson, you will build a test for a custom Angular hook called useFetch, which handles data fetching in a component. You'll learn how to write unit tests using Jasmine and Angular's TestBed to verify that the hook correctly manages loading states, successful data retrieval, and error handling. The core concept involves mocking HTTP requests with HttpClientTestingModule and HttpTestingController to simulate API responses without making real network calls. By the end, you'll understand how to test asynchronous logic and ensure your custom hooks work reliably in an Angular application.",
      "usecase": "Testing custom hooks like useFetch is essential for building reliable data-driven applications in Angular. In real-world products, this pattern appears when validating data fetching logic for dashboard widgets, admin panels displaying user statistics, or e-commerce product listings with filters. Developers must ensure these hooks handle loading states, errors, and data transformations correctly to prevent UI inconsistencies. Proper testing prevents bugs in critical user flows like search results, financial data displays, or real-time notifications."
    },
    "objectives": [
      "Construct a test suite for the useFetch custom hook using Angular TestBed",
      "Initialize mock HTTP responses with HttpClientTestingModule",
      "Configure test cases to verify loading, error, and data states",
      "Manipulate test conditions to simulate different API response scenarios",
      "Implement assertions to validate the hook's behavior with successful and failed requests",
      "Apply dependency injection patterns to test the hook's integration with Angular services",
      "Control test execution flow to verify state transitions during asynchronous operations"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Import HttpClientModule and HttpClientTestingModule",
        "instruction": "In the app.module.ts file, import HttpClientModule and HttpClientTestingModule from '@angular/common/http' and '@angular/common/http/testing' respectively.",
        "hint": "You need to add two import statements at the top of your module file. The first import should bring in HttpClientModule for the component, and the second should bring in HttpClientTestingModule for testing.",
        "analogousExample": "// Example of importing Angular modules\nimport { HttpClientModule } from '@angular/common/http';\nimport { HttpClientTestingModule } from '@angular/common/http/testing';",
        "seedCode": "// Import HttpClientModule and HttpClientTestingModule here",
        "expectedOutcome": "The app.module.ts file will have the necessary imports for HTTP testing functionality.",
        "successCriteria": [
          "HttpClientModule and HttpClientTestingModule are imported from their respective Angular packages."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've imported one of the modules, but you need both. Make sure to import HttpClientModule from '@angular/common/http' and HttpClientTestingModule from '@angular/common/http/testing'.",
        "feedbackWrong": "The imports are missing or incorrect. You need to import both HttpClientModule and HttpClientTestingModule from their respective Angular packages.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "HttpClientModule",
            "HttpClientTestingModule",
            "@angular/common/http",
            "@angular/common/http/testing"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Import TestBed and ComponentFixture",
        "instruction": "Import TestBed and ComponentFixture from '@angular/core/testing'.",
        "hint": "Add the import statement for TestBed and ComponentFixture from the Angular testing utilities module.",
        "analogousExample": "// Import testing utilities\nimport { TestBed, ComponentFixture } from '@angular/core/testing';",
        "seedCode": "// Import HttpClientModule and HttpClientTestingModule here\nimport { HttpClientModule } from '@angular/common/http';\nimport { HttpClientTestingModule } from '@angular/common/http/testing';\n\n// Import TestBed and ComponentFixture here",
        "expectedOutcome": "You'll have imported the necessary testing utilities for configuring the testing module and component testing.",
        "successCriteria": [
          "TestBed and ComponentFixture are imported from '@angular/core/testing'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're importing both TestBed and ComponentFixture from '@angular/core/testing'.",
        "feedbackWrong": "The import statement is incorrect. You need to import TestBed and ComponentFixture from '@angular/core/testing'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "TestBed",
            "ComponentFixture",
            "'@angular/core/testing'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Import the component to test",
        "instruction": "Import the component class that contains the useFetch functionality. Add an import statement for the component you'll be testing.",
        "hint": "Use the import keyword followed by the component class name and its file path.",
        "analogousExample": "// Example: importing a component\nimport { UserProfileComponent } from './user-profile.component';",
        "seedCode": "// Import HttpClientModule and HttpClientTestingModule here\nimport { HttpClientModule } from '@angular/common/http';\nimport { HttpClientTestingModule } from '@angular/common/http/testing';\n\n// Import TestBed and ComponentFixture here\n\n// Import the component to test here",
        "expectedOutcome": "The component class containing useFetch functionality is imported and ready to be used in the test setup.",
        "successCriteria": [
          "The component class is imported with a valid import statement."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added an import, but make sure it's for the correct component class that contains the useFetch logic.",
        "feedbackWrong": "The component class import is missing or incorrect. Check that you're importing the right component from the correct file path.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "from",
            "'.component'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Create the test suite",
        "instruction": "Create a describe block for the component test suite. Use 'describe' with a string description and an empty function body.",
        "hint": "The describe block defines the test suite. It takes two arguments: a description string and a callback function.",
        "analogousExample": "// Example test suite structure\ndescribe('ComponentName', () => {\n  // Tests will go here\n});",
        "seedCode": "// Import HttpClientModule and HttpClientTestingModule here\nimport { HttpClientModule } from '@angular/common/http';\nimport { HttpClientTestingModule } from '@angular/common/http/testing';\n\n// Import TestBed and ComponentFixture here\n\n// Import the component to test here\n\ndescribe('', () => {});",
        "expectedOutcome": "A describe block is added to the file with an empty function body, ready for test setup.",
        "successCriteria": [
          "The test file contains a describe block with a description string and an empty callback function."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added a describe block, but make sure it has both a description string and a callback function.",
        "feedbackWrong": "The test suite structure is missing. Add a describe block with a description and callback function.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "describe",
            "(",
            ")",
            "=>",
            "{"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Configure the testing module",
        "instruction": "Inside the describe block, use TestBed.configureTestingModule to set up the testing module with HttpClientModule and HttpClientTestingModule.",
        "hint": "Call TestBed.configureTestingModule with an object containing an imports array that includes HttpClientModule and HttpClientTestingModule.",
        "analogousExample": "// Example: configuring a testing module\nTestBed.configureTestingModule({\n  imports: [HttpClientModule, HttpClientTestingModule]\n});",
        "seedCode": "// Import HttpClientModule and HttpClientTestingModule here\nimport { HttpClientModule } from '@angular/common/http';\nimport { HttpClientTestingModule } from '@angular/common/http/testing';\n\n// Import TestBed and ComponentFixture here\n\n// Import the component to test here\n\ndescribe('', () => {\n  // Configure the testing module here\n});",
        "expectedOutcome": "The testing module is configured with the required HTTP modules for testing.",
        "successCriteria": [
          "TestBed.configureTestingModule is called with an imports array containing HttpClientModule and HttpClientTestingModule."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure to include both HttpClientModule and HttpClientTestingModule in the imports array.",
        "feedbackWrong": "The testing module configuration is missing. Use TestBed.configureTestingModule with HttpClientModule and HttpClientTestingModule.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "TestBed.configureTestingModule",
            "imports",
            "HttpClientModule",
            "HttpClientTestingModule"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Initialize the component fixture",
        "instruction": "Inside the test suite's beforeEach block, use TestBed.createComponent to create the component fixture and store it in a variable named fixture.",
        "hint": "You'll need to call TestBed.createComponent() with your component class as the argument, then assign the result to a fixture variable.",
        "analogousExample": "// Similar: creating a component fixture\nbeforeEach(() => {\n  fixture = TestBed.createComponent(MyComponent);\n});",
        "seedCode": "// Import HttpClientModule and HttpClientTestingModule here\nimport { HttpClientModule } from '@angular/common/http';\nimport { HttpClientTestingModule } from '@angular/common/http/testing';\n\n// Import TestBed and ComponentFixture here\n\n// Import the component to test here\n\ndescribe('', () => {\n  // Configure the testing module here\n  \n  // Initialize the component fixture here\n});",
        "expectedOutcome": "The component fixture is created and ready for testing.",
        "successCriteria": [
          "TestBed.createComponent is called inside beforeEach, and the result is assigned to a fixture variable."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're calling TestBed.createComponent() inside the beforeEach block and storing the result in a fixture variable.",
        "feedbackWrong": "The component fixture wasn't properly initialized. Check that you're using TestBed.createComponent() inside beforeEach.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "beforeEach",
            "TestBed.createComponent",
            "fixture"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Inject HttpClient for testing",
        "instruction": "Inside the test suite, use TestBed.inject to get the HttpClient service instance and assign it to a variable named httpClient.",
        "hint": "Use TestBed.inject(HttpClient) to retrieve the HttpClient service instance for testing.",
        "analogousExample": "// Injecting a service in a test\nconst myService = TestBed.inject(MyService);",
        "seedCode": "// Import HttpClientModule and HttpClientTestingModule here\nimport { HttpClientModule } from '@angular/common/http';\nimport { HttpClientTestingModule } from '@angular/common/http/testing';\n\n// Import TestBed and ComponentFixture here\nimport { TestBed, ComponentFixture } from '@angular/core/testing';\n\n// Import the component to test here\nimport { MyComponent } from './my.component';\n\ndescribe('MyComponent', () => {\n  let fixture: ComponentFixture<MyComponent>;\n  let component: MyComponent;\n  \n  beforeEach(() => {\n    TestBed.configureTestingModule({\n      imports: [HttpClientTestingModule],\n      declarations: [MyComponent]\n    });\n    \n    fixture = TestBed.createComponent(MyComponent);\n    component = fixture.componentInstance;\n  });\n  \n  // Inject HttpClient for testing here\n});",
        "expectedOutcome": "The HttpClient service is injected and available for mocking responses in the test suite.",
        "successCriteria": [
          "TestBed.inject(HttpClient) is used to retrieve the HttpClient service instance and assign it to a variable named httpClient."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using TestBed.inject(HttpClient) and assigning it to a variable named httpClient.",
        "feedbackWrong": "The HttpClient service injection is missing or incorrect. Use TestBed.inject(HttpClient) to get the service instance.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "TestBed.inject",
            "HttpClient",
            "httpClient"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Set up mock HTTP response",
        "instruction": "Inside the test suite, inject HttpClientTestingController and use it to create a mock response for the HTTP request.",
        "hint": "Use TestBed.inject() to get an instance of HttpClientTestingController, then call its expectOne() method to set up an expectation for the request URL.",
        "analogousExample": "// Example: setting up a mock HTTP response\nconst httpTestingController = TestBed.inject(HttpClientTestingController);\nconst mockReq = httpTestingController.expectOne('api/data');\nmockReq.flush({ data: 'test' });",
        "seedCode": "// Import HttpClientModule and HttpClientTestingModule here\nimport { HttpClientModule } from '@angular/common/http';\nimport { HttpClientTestingModule } from '@angular/common/http/testing';\n\n// Import TestBed and ComponentFixture here\nimport { TestBed, ComponentFixture } from '@angular/core/testing';\n\n// Import the component to test here\nimport { MyComponent } from './my.component';\n\ndescribe('MyComponent', () => {\n  let fixture: ComponentFixture<MyComponent>;\n  let component: MyComponent;\n  \n  beforeEach(() => {\n    TestBed.configureTestingModule({\n      imports: [HttpClientTestingModule],\n      declarations: [MyComponent]\n    });\n    \n    fixture = TestBed.createComponent(MyComponent);\n    component = fixture.componentInstance;\n  });\n  \n  // Inject HttpClient for testing here\n  \n  // Set up mock HTTP response here\n});",
        "expectedOutcome": "The test suite will have a mock HTTP response configured using HttpClientTestingController.",
        "successCriteria": [
          "HttpClientTestingController is injected and used to create a mock request expectation with expectOne() and flush()."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure to inject HttpClientTestingController and use both expectOne() and flush() methods.",
        "feedbackWrong": "The mock response setup is incomplete. You need to inject HttpClientTestingController and create a mock request with expectOne() and flush().",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "HttpClientTestingController",
            "TestBed.inject",
            "expectOne",
            "flush"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "Trigger the fetch method",
        "instruction": "Inside the test suite, after the mock setup, call the component's fetch method to initiate the HTTP request.",
        "hint": "You can call the fetch method directly on the component instance. This will trigger the HTTP request that your mock is set up to intercept.",
        "analogousExample": "// Example: calling a component method in a test\ncomponent.loadData();",
        "seedCode": "// Import HttpClientModule and HttpClientTestingModule here\nimport { HttpClientModule } from '@angular/common/http';\nimport { HttpClientTestingModule } from '@angular/common/http/testing';\n\n// Import TestBed and ComponentFixture here\nimport { TestBed, ComponentFixture } from '@angular/core/testing';\n\n// Import the component to test here\nimport { MyComponent } from './my.component';\n\ndescribe('MyComponent', () => {\n  let fixture: ComponentFixture<MyComponent>;\n  let component: MyComponent;\n  \n  beforeEach(() => {\n    TestBed.configureTestingModule({\n      imports: [HttpClientTestingModule],\n      declarations: [MyComponent]\n    });\n    \n    fixture = TestBed.createComponent(MyComponent);\n    component = fixture.componentInstance;\n  });\n  \n  // Inject HttpClient for testing here\n  \n  // Set up mock HTTP response here\n  \n  // Call the component's fetch method here\n});",
        "expectedOutcome": "The component's fetch method is called, initiating the HTTP request that will be intercepted by the mock.",
        "successCriteria": [
          "The component.fetch() method is called within the test suite."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're calling the fetch method on the component instance within the test suite.",
        "feedbackWrong": "The fetch method wasn't called. Remember to call component.fetch() to initiate the HTTP request.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "component.fetch",
            "component.fetch()"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Verify the response handling",
        "instruction": "Add an assertion inside the test to verify that the component's data property is correctly updated after receiving the mock HTTP response.",
        "hint": "Use the expect() function to check if component.data matches the mock response data you set up earlier.",
        "analogousExample": "// Example assertion for verifying component state\nexpect(component.data).toEqual({ id: 1, name: 'Test Data' });",
        "seedCode": "// Import HttpClientModule and HttpClientTestingModule here\nimport { HttpClientModule } from '@angular/common/http';\nimport { HttpClientTestingModule } from '@angular/common/http/testing';\n\n// Import TestBed and ComponentFixture here\nimport { TestBed, ComponentFixture } from '@angular/core/testing';\n\n// Import the component to test here\nimport { MyComponent } from './my.component';\n\ndescribe('MyComponent', () => {\n  let fixture: ComponentFixture<MyComponent>;\n  let component: MyComponent;\n  \n  beforeEach(() => {\n    TestBed.configureTestingModule({\n      imports: [HttpClientTestingModule],\n      declarations: [MyComponent]\n    });\n    \n    fixture = TestBed.createComponent(MyComponent);\n    component = fixture.componentInstance;\n  });\n  \n  // Inject HttpClient for testing here\n  \n  // Set up mock HTTP response here\n  \n  // Call the component's fetch method here\n  \n  // Add your assertion here to verify response handling\n});",
        "expectedOutcome": "The test now includes an assertion that checks if the component correctly processes the HTTP response data.",
        "successCriteria": [
          "An expect() assertion is added that verifies component.data matches the expected mock response data."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your assertion specifically checks component.data against the mock response data.",
        "feedbackWrong": "Try adding an expect() statement that compares component.data with the mock data you set up earlier.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "expect",
            "component.data",
            "toEqual",
            "mock"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import HttpClientModule and HttpClientTestingModule",
        "id": "step1"
      },
      {
        "label": "Import TestBed and ComponentFixture",
        "id": "step2"
      },
      {
        "label": "Import the component to test",
        "id": "step3"
      },
      {
        "label": "Create the test suite",
        "id": "step4"
      },
      {
        "label": "Configure the testing module",
        "id": "step5"
      },
      {
        "label": "Initialize the component fixture",
        "id": "step6"
      },
      {
        "label": "Inject HttpClient for testing",
        "id": "step7"
      },
      {
        "label": "Set up mock HTTP response",
        "id": "step8"
      },
      {
        "label": "Trigger the fetch method",
        "id": "step9"
      },
      {
        "label": "Verify the response handling",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}