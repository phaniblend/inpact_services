{
  "success": true,
  "config": {
    "lessonId": "ai-angular-18",
    "track": "angular",
    "lessonNum": 18,
    "title": "Color Picker",
    "shortName": "P18",
    "intro": {
      "tag": "LESSON #18",
      "title": "Color Picker",
      "body": "In this lesson, you'll build an interactive color picker component that allows users to select colors from a palette and see real-time previews. You'll learn how to use Angular's reactive forms and two-way data binding to capture user input and update the UI dynamically. The project will teach you essential Angular concepts including component communication, event handling, and property binding while working with HTML color input elements and CSS styling. You'll also explore how to manage component state and emit custom events to share the selected color with parent components. By the end of this lesson, you'll have a fully functional color picker that demonstrates core Angular patterns for building interactive user interfaces.",
      "usecase": "Color pickers are essential components in design tools like Canva, Figma, and Adobe Creative Suite, where users need to select colors for text, shapes, and backgrounds. They appear in content management systems and website builders like WordPress and Wix, enabling users to customize themes and brand colors. E-commerce platforms use color pickers in product customization features, allowing customers to preview items in different colors before purchase. Social media platforms and messaging apps implement color pickers for user interface personalization, story creation, and custom emoji reactions."
    },
    "objectives": [
      "Implement a color picker component with property binding for color values",
      "Configure two-way data binding to synchronize color selection with parent components",
      "Apply event handling to capture and respond to color change events",
      "Construct form controls that integrate with Angular's reactive forms system",
      "Bind dynamic CSS properties to reflect selected color values in real-time",
      "Manipulate color formats between hex, RGB, and HSL representations",
      "Initialize default color values and validation rules for user input",
      "Control component state to manage color picker visibility and interactions"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Import Angular Component and FormsModule",
        "instruction": "Import Component from @angular/core and FormsModule from @angular/forms",
        "hint": "Use the import statement to bring in Component from the core Angular package and FormsModule from the forms package. These will be needed for creating the component and handling form inputs.",
        "analogousExample": "// Similar: importing multiple items from different packages\nimport { Component } from '@angular/core';\nimport { FormsModule } from '@angular/forms';",
        "seedCode": "// Import the required Angular modules here\n",
        "expectedOutcome": "The necessary Angular imports are added to enable component creation and form handling for the color picker.",
        "successCriteria": [
          "Component is imported from @angular/core and FormsModule is imported from @angular/forms"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've imported some of the required modules. Make sure you have both Component from @angular/core and FormsModule from @angular/forms.",
        "feedbackWrong": "import Component from @angular/core and FormsModule from @angular/forms. These imports are essential for creating components and handling form data.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "Component",
            "@angular/core",
            "FormsModule",
            "@angular/forms",
            "import"
          ],
          "partialThreshold": 0.4,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Create ColorPicker component",
        "instruction": "Create the ColorPicker component class with the @Component decorator that includes a selector, template, and styleUrls property.",
        "hint": "Use the @Component decorator above a class declaration. The decorator should include selector, template, and styleUrls properties.",
        "analogousExample": "// Similar: basic component structure\n@Component({\n  selector: 'app-greeting',\n  template: '<h1>Hello</h1>',\n  styleUrls: ['./greeting.component.css']\n})\nexport class GreetingComponent {}",
        "seedCode": "// Import the required Angular modules here\n\n// Create your ColorPicker component here",
        "expectedOutcome": "A ColorPicker component class is defined with @Component decorator containing selector, template, and styleUrls properties.",
        "successCriteria": [
          "ColorPicker component class exists with @Component decorator that includes selector 'app-color-picker', a template property, and styleUrls array."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "Good start! Make sure your component includes the @Component decorator with selector, template, and styleUrls properties.",
        "feedbackWrong": "create a class with the @Component decorator that includes the required properties: selector, template, and styleUrls.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "@Component",
            "ColorPicker",
            "class",
            "selector",
            "template",
            "styleUrls"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Initialize color state property",
        "instruction": "Add a selectedColor property to the ColorPicker component and initialize it with the default value '#ff0000' (red).",
        "hint": "In Angular components, properties are declared as class members. Initialize the property with a string value representing a hex color.",
        "analogousExample": "// Similar: component property with default value\nexport class UserProfile {\n  username: string = 'guest';\n}",
        "seedCode": "// Import the required Angular modules here\n\n// Create your ColorPicker component here\n// Add selectedColor property here",
        "expectedOutcome": "The ColorPicker component will have a selectedColor property initialized to '#ff0000' to track the currently selected color.",
        "successCriteria": [
          "The ColorPicker component contains a selectedColor property initialized to '#ff0000'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track with adding a property. Make sure it's named 'selectedColor' and initialized with the hex value '#ff0000'.",
        "feedbackWrong": "add a class property named 'selectedColor' and set its initial value to '#ff0000'. This property will hold the currently selected color.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "selectedColor",
            "#ff0000"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Define color change handler method",
        "instruction": "Add an onColorChange method to the ColorPicker component that takes a color parameter and updates the selectedColor property",
        "hint": "Create a method that accepts a color parameter and assigns it to the selectedColor property using this.selectedColor = color",
        "analogousExample": "// Similar: a method that updates a property\nonValueChange(newValue: string) {\n  this.currentValue = newValue;\n}",
        "seedCode": "// Import the required Angular modules here\n\n// Create your ColorPicker component here\n// Add selectedColor property here\n\n// Add onColorChange method here",
        "expectedOutcome": "The component will have an onColorChange method that can update the selectedColor property when called",
        "successCriteria": [
          "An onColorChange method is defined that takes a color parameter and updates the selectedColor property"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "Good start! Make sure your method takes a color parameter and updates the selectedColor property.",
        "feedbackWrong": "The onColorChange method should accept a color parameter and assign it to this.selectedColor. Check your method syntax.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "onColorChange",
            "selectedColor",
            "color"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Create basic component template structure",
        "instruction": "Add a template property to the ColorPicker component with a container div element that has the class 'color-picker-container'",
        "hint": "In Angular components, the template property defines the HTML structure. Add a div element with a CSS class to serve as the main container for the color picker interface.",
        "analogousExample": "// Similar: component with template property\n@Component({\n  selector: 'app-example',\n  template: '<div class=\"example-container\"></div>'\n})",
        "seedCode": "import { Component } from '@angular/core';\nimport { FormsModule } from '@angular/forms';\n\n@Component({\n  selector: 'app-color-picker',\n  // Add template property here\n})\nexport class ColorPicker {\n  selectedColor: string = '#000000';\n\n  onColorChange(event: Event): void {\n    const target = event.target as HTMLInputElement;\n    this.selectedColor = target.value;\n  }\n}",
        "expectedOutcome": "The ColorPicker component will have a template property with a container div that provides the foundation for the color picker interface.",
        "successCriteria": [
          "Template property is added to the component decorator with a div element that has the class 'color-picker-container'"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "Good start! Make sure the template property contains a div element with the class 'color-picker-container' to serve as the main container.",
        "feedbackWrong": "The template property should contain a div element with the class 'color-picker-container'. This will be the main container for the color picker interface.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "template",
            "div",
            "color-picker-container"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Add color input control",
        "instruction": "Add an input element with type='color' and bind it to selectedColor using ngModel",
        "hint": "Use <input type='color'> and add [(ngModel)] attribute to bind to selectedColor property",
        "analogousExample": "// Similar: text input with two-way binding\n<input type=\"text\" [(ngModel)]=\"username\">",
        "seedCode": "import { Component } from '@angular/core';\nimport { FormsModule } from '@angular/forms';\n\n@Component({\n  selector: 'app-color-picker',\n  template: `\n    <div>\n      <h2>Color Picker</h2>\n      <!-- Add color input here -->\n    </div>\n  `\n})\nexport class ColorPicker {\n  selectedColor: string = '#000000';\n\n  onColorChange(event: Event): void {\n    const target = event.target as HTMLInputElement;\n    this.selectedColor = target.value;\n  }\n}",
        "expectedOutcome": "A color input control is added that allows users to select colors and is bound to the selectedColor property",
        "successCriteria": [
          "Input element with type='color' is present and uses [(ngModel)] binding to selectedColor"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "Good start! Make sure your input has type='color' and uses [(ngModel)] for two-way binding.",
        "feedbackWrong": "add an input element with type='color' and bind it using [(ngModel)]=\"selectedColor\".",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "input",
            "type=\"color\"",
            "[(ngModel)]",
            "selectedColor"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Display selected color value",
        "instruction": "Add a paragraph element below the color input that displays the current selectedColor value using Angular interpolation",
        "hint": "Use double curly braces {{ }} to display the selectedColor property value in the template",
        "analogousExample": "// Displaying a property value in the template\n<p>Current value: {{ myProperty }}</p>",
        "seedCode": "import { Component } from '@angular/core';\nimport { FormsModule } from '@angular/forms';\n\n@Component({\n  selector: 'app-color-picker',\n  template: `\n    <div>\n      <h2>Color Picker</h2>\n      <input type=\"color\" [(ngModel)]=\"selectedColor\" (change)=\"onColorChange($event)\" />\n      <!-- Add paragraph to display selected color here -->\n    </div>\n  `\n})\nexport class ColorPicker {\n  selectedColor: string = '#000000';\n\n  onColorChange(event: Event): void {\n    const target = event.target as HTMLInputElement;\n    this.selectedColor = target.value;\n  }\n}",
        "expectedOutcome": "A paragraph element that shows the current color value using interpolation, updating as the user selects different colors",
        "successCriteria": [
          "A paragraph element is added that displays the selectedColor value using Angular interpolation syntax {{ selectedColor }}"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "Good start! Make sure you're using the correct interpolation syntax with double curly braces to display the selectedColor property.",
        "feedbackWrong": "add a paragraph element that uses Angular interpolation {{ }} to display the selectedColor property value.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<p>",
            "selectedColor",
            "{{",
            "}}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Add color preview display",
        "instruction": "Add a div element below the paragraph that displays a color preview with dynamic background color styling bound to selectedColor",
        "hint": "Use property binding with [style.background-color] to set the background color dynamically based on the selectedColor property",
        "analogousExample": "// Similar: dynamic styling with property binding\n<div [style.background-color]=\"themeColor\"></div>",
        "seedCode": "import { Component } from '@angular/core';\nimport { FormsModule } from '@angular/forms';\n\n@Component({\n  selector: 'app-color-picker',\n  template: `\n    <div>\n      <h2>Color Picker</h2>\n      <input type=\"color\" [(ngModel)]=\"selectedColor\" (change)=\"onColorChange($event)\" />\n      <p>Selected color: {{selectedColor}}</p>\n      <!-- Add color preview div here -->\n    </div>\n  `\n})\nexport class ColorPicker {\n  selectedColor: string = '#000000';\n\n  onColorChange(event: Event): void {\n    const target = event.target as HTMLInputElement;\n    this.selectedColor = target.value;\n  }\n}",
        "expectedOutcome": "A div element with dynamic background color styling that shows a visual preview of the selected color",
        "successCriteria": [
          "A div element with [style.background-color] property binding set to selectedColor is added below the paragraph"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "Good start! Make sure the div uses property binding [style.background-color] to dynamically set the background color.",
        "feedbackWrong": "The div needs property binding with [style.background-color] set to selectedColor to show the color preview.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "div",
            "[style.background-color]",
            "selectedColor"
          ],
          "partialThreshold": 0.6,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "Wire color input change event",
        "instruction": "Add (ngModelChange) event binding to the color input to call onColorChange when the color value changes",
        "hint": "Use (ngModelChange)=\"onColorChange($event)\" on the input element to trigger the handler when the model value changes",
        "analogousExample": "// Similar: binding to model change event\n<input type=\"text\" [(ngModel)]=\"username\" (ngModelChange)=\"onUsernameChange($event)\" />",
        "seedCode": "import { Component } from '@angular/core';\nimport { FormsModule } from '@angular/forms';\n\n@Component({\n  selector: 'app-color-picker',\n  template: `\n    <div>\n      <h2>Color Picker</h2>\n      <input type=\"color\" [(ngModel)]=\"selectedColor\" (change)=\"onColorChange($event)\" />\n      <p>Selected color: {{selectedColor}}</p>\n      <!-- Add color preview div here -->\n    </div>\n  `\n})\nexport class ColorPicker {\n  selectedColor: string = '#000000';\n\n  onColorChange(event: Event): void {\n    const target = event.target as HTMLInputElement;\n    this.selectedColor = target.value;\n  }\n}",
        "expectedOutcome": "The color input will have both (change) and (ngModelChange) event bindings to ensure proper reactivity",
        "successCriteria": [
          "The input element includes (ngModelChange)=\"onColorChange($event)\" binding in addition to the existing (change) binding"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "Good start! Make sure you're adding (ngModelChange)=\"onColorChange($event)\" to the input element alongside the existing bindings.",
        "feedbackWrong": "The (ngModelChange) binding is missing. Add (ngModelChange)=\"onColorChange($event)\" to the color input element to handle model changes.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "(ngModelChange)",
            "onColorChange",
            "$event"
          ],
          "partialThreshold": 0.6,
          "correctThreshold": 0.9
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Add component styling",
        "instruction": "Add styles to the component using the styles property to enhance the visual appearance with proper spacing, layout, and color for the color picker elements.",
        "hint": "Use the styles property array in the @Component decorator to add CSS rules for div, h2, input, and p elements with appropriate spacing and styling.",
        "analogousExample": "// Adding styles to a component\n@Component({\n  selector: 'app-example',\n  template: `<div><h1>Title</h1></div>`,\n  styles: [`\n    div { padding: 20px; }\n    h1 { color: blue; }\n  `]\n})",
        "seedCode": "import { Component } from '@angular/core';\nimport { FormsModule } from '@angular/forms';\n\n@Component({\n  selector: 'app-color-picker',\n  template: `\n    <div>\n      <h2>Color Picker</h2>\n      <input type=\"color\" [(ngModel)]=\"selectedColor\" (change)=\"onColorChange($event)\" />\n      <p>Selected color: {{selectedColor}}</p>\n      <!-- Add color preview div here -->\n    </div>\n  `,\n  // Add styles property here\n})\nexport class ColorPicker {\n  selectedColor: string = '#000000';\n\n  onColorChange(event: Event): void {\n    const target = event.target as HTMLInputElement;\n    this.selectedColor = target.value;\n  }\n}",
        "expectedOutcome": "The component will have a styles property with CSS rules that improve the visual presentation of the color picker with proper spacing and styling.",
        "successCriteria": [
          "The @Component decorator includes a styles property with CSS rules for the color picker elements."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "Good start on adding styles. Make sure to include CSS rules for proper spacing and layout of the color picker elements.",
        "feedbackWrong": "The styles property needs to be added to the @Component decorator with CSS rules for the color picker elements.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "styles",
            "padding",
            "margin"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.7
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import Angular Component and FormsModule",
        "id": "step1"
      },
      {
        "label": "Create ColorPicker component",
        "id": "step2"
      },
      {
        "label": "Initialize color state property",
        "id": "step3"
      },
      {
        "label": "Define color change handler method",
        "id": "step4"
      },
      {
        "label": "Create basic component template structure",
        "id": "step5"
      },
      {
        "label": "Add color input control",
        "id": "step6"
      },
      {
        "label": "Display selected color value",
        "id": "step7"
      },
      {
        "label": "Add color preview display",
        "id": "step8"
      },
      {
        "label": "Wire color input change event",
        "id": "step9"
      },
      {
        "label": "Add component styling",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}