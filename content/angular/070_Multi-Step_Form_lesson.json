{
  "success": true,
  "config": {
    "lessonId": "ai-angular-70",
    "track": "angular",
    "lessonNum": 70,
    "title": "Multi-Step Form",
    "shortName": "P70",
    "intro": {
      "tag": "LESSON #70",
      "title": "Multi-Step Form",
      "body": "In this lesson, you will build a multi-step form that guides users through a sign-up process in separate, manageable sections. You'll learn how to manage form state and navigation between steps using Angular's reactive forms and component-based architecture. The main technologies involved are TypeScript for type safety, Angular components for structuring the UI, and RxJS Observables for handling state changes. By the end, you'll have a functional form that validates user input and progresses through multiple stages with clear visual feedback.",
      "usecase": "Multi-step forms are essential in modern web applications where complex data collection needs to be broken into manageable sections. You'll find them in e-commerce checkout flows (like Amazon or Shopify), account creation wizards (such as LinkedIn or banking platforms), and insurance quote calculators. As an Angular developer, you'll implement these patterns to guide users through lengthy processes while maintaining clear progress indicators and validation at each stage. This approach improves user experience by reducing cognitive load and preventing form abandonment in real-world applications."
    },
    "objectives": [
      "Construct a multi-step form component with separate sections for each step",
      "Implement state management to track the current step and form data",
      "Configure navigation controls to move between form steps",
      "Bind form inputs to component properties using Angular's two-way data binding",
      "Apply validation logic to control step progression",
      "Render dynamic step indicators that update based on current progress",
      "Integrate form submission logic that processes all collected data"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 7",
        "title": "Import Angular modules and dependencies",
        "instruction": "Import CommonModule from '@angular/common', and FormsModule and ReactiveFormsModule from '@angular/forms'.",
        "hint": "Use the import statement with the correct module names and paths. These modules are needed for form handling in Angular.",
        "analogousExample": "// Example of importing Angular modules\nimport { CommonModule } from '@angular/common';\nimport { FormsModule, ReactiveFormsModule } from '@angular/forms';",
        "seedCode": "// Import CommonModule from '@angular/common', and FormsModule and ReactiveFormsModule from '@angular/forms'.",
        "expectedOutcome": "The necessary Angular modules for forms are imported, enabling form functionality in the application.",
        "successCriteria": [
          "CommonModule is imported from '@angular/common', and FormsModule and ReactiveFormsModule are imported from '@angular/forms'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've imported some modules, but check if all required ones (CommonModule, FormsModule, ReactiveFormsModule) are present with correct paths.",
        "feedbackWrong": "The imports are missing or incorrect. Ensure you import CommonModule from '@angular/common', and FormsModule and ReactiveFormsModule from '@angular/forms'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "CommonModule",
            "@angular/common",
            "FormsModule",
            "ReactiveFormsModule",
            "@angular/forms"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 7",
        "title": "Create the main form component",
        "instruction": "Create a new Angular component named MultiStepFormComponent with the @Component decorator and an empty template.",
        "hint": "Use the @Component decorator from '@angular/core' to define the component. Set the selector to 'app-multi-step-form' and template to an empty string for now.",
        "analogousExample": "// Example: basic component structure\nimport { Component } from '@angular/core';\n\n@Component({\n  selector: 'app-example',\n  template: ''\n})\nexport class ExampleComponent {}",
        "seedCode": "import { CommonModule } from '@angular/common';\nimport { FormsModule, ReactiveFormsModule } from '@angular/forms';\n\n// Create the MultiStepFormComponent here",
        "expectedOutcome": "A MultiStepFormComponent class with @Component decorator is created, ready for form logic.",
        "successCriteria": [
          "MultiStepFormComponent is defined with @Component decorator, selector 'app-multi-step-form', and empty template."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Ensure the component has the @Component decorator with selector 'app-multi-step-form' and an empty template.",
        "feedbackWrong": "The component isn't set up correctly. Review Angular component basics: use @Component decorator with selector and template.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "@Component",
            "MultiStepFormComponent",
            "selector",
            "template"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 7",
        "title": "Define the form steps interface",
        "instruction": "Inside the MultiStepFormComponent class, define a TypeScript interface named FormStep with id (number), title (string), and completed (boolean) properties.",
        "hint": "Use the 'interface' keyword followed by the interface name and property definitions with type annotations.",
        "analogousExample": "// Example interface definition\ninterface Task {\n  id: number;\n  title: string;\n  isDone: boolean;\n}",
        "seedCode": "import { CommonModule } from '@angular/common';\nimport { FormsModule, ReactiveFormsModule } from '@angular/forms';\n\n// Create the MultiStepFormComponent here\n\n// Define the FormStep interface here",
        "expectedOutcome": "You'll have a FormStep interface that defines the structure for each step in your multi-step form.",
        "successCriteria": [
          "A FormStep interface is defined with id: number, title: string, and completed: boolean properties."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure your interface has all three required properties with correct TypeScript types.",
        "feedbackWrong": "The FormStep interface isn't properly defined. Check that you used the 'interface' keyword and included all three properties with correct types.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "interface",
            "FormStep",
            "id:",
            "title:",
            "completed:",
            "number",
            "string",
            "boolean"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 7",
        "title": "Add step navigation structure",
        "instruction": "In the component's template, add a <div> container that uses *ngFor to iterate over the steps array and display each step's title and completion status.",
        "hint": "Use *ngFor=\"let step of steps\" to loop through the steps. Display step.title and step.completed inside the div.",
        "analogousExample": "// Example: displaying items with *ngFor\n<div *ngFor=\"let item of items\">\n  <p>{{ item.name }} - {{ item.status }}</p>\n</div>",
        "seedCode": "import { CommonModule } from '@angular/common';\nimport { FormsModule, ReactiveFormsModule } from '@angular/forms';\n\n// Create the MultiStepFormComponent here\n\n// Define the FormStep interface here\ninterface FormStep {\n  title: string;\n  completed: boolean;\n}\n\n// Inside MultiStepFormComponent class:\n// Initialize the steps array property here\n\n// Add currentStepIndex property here\n\n// Add nextStep() method here\n\n// Add previousStep() method here\n\n// Add step navigation structure here\n<div *ngFor=\"let step of steps\">\n  <!-- Display step title and completion status -->\n</div>",
        "expectedOutcome": "A navigation UI structure is created that will display each step's title and completion status when the component renders.",
        "successCriteria": [
          "The template contains a <div> with *ngFor=\"let step of steps\" that displays step.title and step.completed."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using *ngFor to iterate over the steps array and displaying both the title and completion status.",
        "feedbackWrong": "The step navigation structure is missing or incorrect. You need to add a <div> with *ngFor that displays each step's title and completion status.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "*ngFor=\"let step of steps\"",
            "step.title",
            "step.completed"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 7",
        "title": "Create step content container",
        "instruction": "In your component's template, add a <div> element with *ngIf to display content only when currentStepIndex equals 0 (for the first step).",
        "hint": "Use Angular's *ngIf structural directive to conditionally render the div based on the current step index.",
        "analogousExample": "// Conditional display based on index\n<div *ngIf=\"currentStepIndex === 0\">\n  <!-- Step 1 content -->\n</div>",
        "seedCode": "import { CommonModule } from '@angular/common';\nimport { FormsModule, ReactiveFormsModule } from '@angular/forms';\n\n// Create the MultiStepFormComponent here\n\n// Define the FormStep interface here\ninterface FormStep {\n  title: string;\n  completed: boolean;\n}\n\n// Inside MultiStepFormComponent class:\n// Initialize the steps array property here\n\n// Add currentStepIndex property here\n\n// Add nextStep() method here\n\n// Add previousStep() method here\n\n// Add step navigation structure here\n<div *ngFor=\"let step of steps\">\n  <!-- Display step title and completion status -->\n</div>\n\n<!-- Add step content container here -->",
        "expectedOutcome": "A div element will be added that conditionally displays content for the first step.",
        "successCriteria": [
          "The template contains a <div> with *ngIf=\"currentStepIndex === 0\"."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You added a div, but make sure it has the correct *ngIf condition to check currentStepIndex.",
        "feedbackWrong": "The step content container is missing or incorrect. Add a <div> with *ngIf to conditionally display based on currentStepIndex.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<div",
            "*ngIf",
            "currentStepIndex",
            "===",
            "0"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 7",
        "title": "Add navigation buttons",
        "instruction": "Inside the step content container, add two buttons: one labeled 'Previous' that calls the previousStep method when clicked, and one labeled 'Next' that calls the nextStep method when clicked. Make sure the 'Previous' button is disabled when currentStepIndex is 0.",
        "hint": "Use Angular's event binding syntax (click) to call the methods, and property binding [disabled] to conditionally disable the 'Previous' button.",
        "analogousExample": "// Example: Navigation buttons in Angular\n<button (click)=\"goBack()\" [disabled]=\"isFirstStep\">Back</button>\n<button (click)=\"goForward()\">Next</button>",
        "seedCode": "import { CommonModule } from '@angular/common';\nimport { FormsModule, ReactiveFormsModule } from '@angular/forms';\n\n// Create the MultiStepFormComponent here\n\n// Define the FormStep interface here\ninterface FormStep {\n  title: string;\n  completed: boolean;\n}\n\n// Inside MultiStepFormComponent class:\n// Initialize the steps array property here\n\n// Add currentStepIndex property here\n\n// Add nextStep() method here\n\n// Add previousStep() method here\n\n// Add step navigation structure here\n<div *ngFor=\"let step of steps\">\n  <!-- Display step title and completion status -->\n</div>\n\n<!-- Add step content container here -->\n<div>\n  <!-- Add navigation buttons here -->\n</div>",
        "expectedOutcome": "Two navigation buttons appear in the step content container: 'Previous' and 'Next'. The 'Previous' button is disabled on the first step.",
        "successCriteria": [
          "The 'Previous' button calls previousStep on click and is disabled when currentStepIndex is 0. The 'Next' button calls nextStep on click."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added the buttons, but check that the 'Previous' button is properly disabled on the first step and both buttons have the correct click handlers.",
        "feedbackWrong": "The navigation buttons are missing or not properly configured. Make sure you have both 'Previous' and 'Next' buttons with (click) handlers and [disabled] binding for 'Previous'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<button",
            "(click)=\"previousStep()\"",
            "(click)=\"nextStep()\"",
            "[disabled]=\"currentStepIndex === 0\""
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 7",
        "title": "Add completion message",
        "instruction": "Add a completion message that displays when all steps are completed. Use *ngIf to conditionally show a paragraph with a success message.",
        "hint": "Check if currentStepIndex equals the last step index (steps.length - 1) to determine when to show the completion message.",
        "analogousExample": "// Conditionally showing a message\n<div *ngIf=\"isComplete\">\n  <p>All steps completed successfully!</p>\n</div>",
        "seedCode": "import { CommonModule } from '@angular/common';\nimport { FormsModule, ReactiveFormsModule } from '@angular/forms';\n\n// Create the MultiStepFormComponent here\n\n// Define the FormStep interface here\ninterface FormStep {\n  title: string;\n  completed: boolean;\n}\n\n// Inside MultiStepFormComponent class:\n// Initialize the steps array property here\n\n// Add currentStepIndex property here\n\n// Add nextStep() method here\n\n// Add previousStep() method here\n\n// Add step navigation structure here\n<div *ngFor=\"let step of steps\">\n  <!-- Display step title and completion status -->\n</div>\n\n<!-- Add step content container here -->\n<div>\n  <!-- Add navigation buttons here -->\n  <!-- Add name and email input fields here -->\n  \n  <!-- Add completion message here -->\n</div>",
        "expectedOutcome": "A completion message appears when the user reaches the final step, providing feedback that all steps are completed.",
        "successCriteria": [
          "A paragraph with a success message is conditionally rendered using *ngIf when currentStepIndex equals the last step index."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure the completion message only shows when the user is on the final step. Check your *ngIf condition.",
        "feedbackWrong": "The completion message isn't appearing correctly. Remember to use *ngIf with a condition that checks if currentStepIndex equals steps.length - 1.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "*ngIf",
            "currentStepIndex",
            "steps.length",
            "paragraph",
            "completed"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import Angular modules and dependencies",
        "id": "step1"
      },
      {
        "label": "Create the main form component",
        "id": "step2"
      },
      {
        "label": "Define the form steps interface",
        "id": "step3"
      },
      {
        "label": "Add step navigation structure",
        "id": "step4"
      },
      {
        "label": "Create step content container",
        "id": "step5"
      },
      {
        "label": "Add navigation buttons",
        "id": "step6"
      },
      {
        "label": "Add completion message",
        "id": "step7"
      }
    ]
  },
  "source": "content"
}