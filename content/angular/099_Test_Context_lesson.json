{
  "success": true,
  "config": {
    "lessonId": "ai-angular-99",
    "track": "angular",
    "lessonNum": 99,
    "title": "Test Context",
    "shortName": "P99",
    "intro": {
      "tag": "LESSON #99",
      "title": "Test Context",
      "body": "In this lesson, you will build a simple Angular component that uses a test context to isolate and verify its behavior. You will learn how to set up a testing environment using Angular's TestBed to configure dependencies and test components in isolation. The core concept is understanding how a test context provides a controlled environment for unit testing, ensuring your component works correctly without external interference. You will use key technologies like TypeScript, Jasmine for testing syntax, and Angular's testing utilities to write and run your tests.",
      "usecase": "Test Context is essential for isolating component behavior in complex Angular applications like e-commerce dashboards, where you need to test product filters without loading the entire inventory system. Developers use it to mock dependencies like authentication services when testing user profile forms, ensuring login logic works independently of backend APIs. This pattern appears in admin panels where you test data table sorting without connecting to live databases, or in interactive UI components like modals that require specific context to validate user interactions. By creating focused test environments, developers can verify individual features work correctly before integrating them into the full application."
    },
    "objectives": [
      "Initialize a test context with TestBed.configureTestingModule",
      "Configure component dependencies using TestBed declarations and imports",
      "Render components within a test context using TestBed.createComponent",
      "Manipulate component instances and DOM elements during testing",
      "Apply test context to verify component template bindings",
      "Integrate mock services and providers into test environments",
      "Control component lifecycle events within the test context"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Import TestBed, ComponentFixture, and Component from Angular testing utilities",
        "instruction": "Import TestBed and ComponentFixture from '@angular/core/testing' and Component from '@angular/core'.",
        "hint": "Use the import statement with curly braces to import multiple symbols from the same module.",
        "analogousExample": "// Importing multiple utilities\nimport { TestBed, ComponentFixture } from '@angular/core/testing';\nimport { Component } from '@angular/core';",
        "seedCode": {
          "App.tsx": "// Import TestBed, ComponentFixture, and Component here\n\n",
          "store.ts": "// store setup"
        },
        "expectedOutcome": "The testing utilities are imported and ready to be used for component testing.",
        "successCriteria": [
          "TestBed and ComponentFixture are imported from '@angular/core/testing', and Component is imported from '@angular/core'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've imported some of the required utilities. Make sure to import TestBed and ComponentFixture from '@angular/core/testing' and Component from '@angular/core'.",
        "feedbackWrong": "The imports are not correct. You need to import TestBed and ComponentFixture from '@angular/core/testing' and Component from '@angular/core'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "TestBed",
            "ComponentFixture",
            "@angular/core/testing",
            "Component",
            "@angular/core"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Create a simple test component class",
        "instruction": "Define a simple Angular component class named TestComponent with a template that contains a heading element displaying 'Test Component'.",
        "hint": "Use the @Component decorator to define the component's selector and template. The template should be a simple string containing an <h1> element.",
        "analogousExample": "// Example: a basic component class\n@Component({\n  selector: 'app-example',\n  template: '<h1>Example Component</h1>'\n})\nexport class ExampleComponent {}",
        "seedCode": {
          "App.tsx": "// Import TestBed, ComponentFixture, and Component here\nimport { TestBed, ComponentFixture, Component } from '@angular/core/testing';\n\n// Define your TestComponent class below\n",
          "store.ts": "// store setup"
        },
        "expectedOutcome": "You will have a TestComponent class that can be used in your TestBed configuration for testing.",
        "successCriteria": [
          "TestComponent class is defined with @Component decorator, selector 'app-test', and template '<h1>Test Component</h1>'"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure your component has the @Component decorator with both selector and template properties defined correctly.",
        "feedbackWrong": "The component definition seems incomplete or incorrect. Check that you've used the @Component decorator with the required properties.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "@Component",
            "TestComponent",
            "selector",
            "template",
            "<h1>Test Component</h1>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Configure the testing module with TestBed",
        "instruction": "Inside a describe block (or before your test), call TestBed.configureTestingModule() and pass an object with a declarations array containing your TestComponent.",
        "hint": "Use TestBed.configureTestingModule({ declarations: [TestComponent] }) to set up the testing module.",
        "analogousExample": "// Setting up a testing module\nTestBed.configureTestingModule({\n  declarations: [MyTestComponent]\n});",
        "seedCode": {
          "App.tsx": "import { TestBed, ComponentFixture, Component } from '@angular/core/testing';\n\n@Component({\n  selector: 'app-test',\n  template: '<p>Test Component</p>'\n})\nexport class TestComponent {}\n\n// Configure the testing module here",
          "store.ts": "// store setup"
        },
        "expectedOutcome": "The testing module is configured with TestComponent declared, ready for creating the component fixture.",
        "successCriteria": [
          "TestBed.configureTestingModule is called with an object containing declarations: [TestComponent]."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're calling TestBed.configureTestingModule() and passing the correct configuration object.",
        "feedbackWrong": "The testing module isn't configured correctly. Use TestBed.configureTestingModule() with declarations: [TestComponent].",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "TestBed.configureTestingModule",
            "declarations",
            "TestComponent"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Compile the testing module",
        "instruction": "Call TestBed.compileComponents() to compile the testing module after the TestBed configuration.",
        "hint": "Add TestBed.compileComponents() after the TestBed.configureTestingModule call. This is an async operation that prepares components for testing.",
        "analogousExample": "// Example: compiling a testing module\nTestBed.compileComponents();",
        "seedCode": {
          "App.tsx": "import { TestBed, ComponentFixture, Component } from '@angular/core/testing';\n\n@Component({\n  selector: 'app-test',\n  template: '<p>Test Component</p>'\n})\nexport class TestComponent {}\n\n// Configure the testing module here\nTestBed.configureTestingModule({\n  declarations: [TestComponent]\n});\n\n// Call TestBed.compileComponents() here",
          "store.ts": "// store setup"
        },
        "expectedOutcome": "The testing module will be compiled, allowing components to be instantiated in tests.",
        "successCriteria": [
          "TestBed.compileComponents() is called after TestBed.configureTestingModule."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you call TestBed.compileComponents() after the TestBed configuration.",
        "feedbackWrong": "The testing module compilation step is missing. Add TestBed.compileComponents() after the TestBed.configureTestingModule call.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "TestBed.compileComponents()"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Create the component fixture",
        "instruction": "Call TestBed.createComponent() to create a ComponentFixture for the TestComponent and assign it to a variable named fixture.",
        "hint": "Use TestBed.createComponent(TestComponent) to create the fixture. Store the result in a const variable with type ComponentFixture<TestComponent>.",
        "analogousExample": "// Creating a fixture for a component\nconst fixture: ComponentFixture<TestComponent> = TestBed.createComponent(TestComponent);",
        "seedCode": {
          "App.tsx": "import { TestBed, ComponentFixture, Component } from '@angular/core/testing';\n\n@Component({\n  selector: 'app-test',\n  template: '<p>Test Component</p>'\n})\nexport class TestComponent {}\n\n// Configure the testing module here\nTestBed.configureTestingModule({\n  declarations: [TestComponent]\n});\n\n// Call TestBed.compileComponents() here\nTestBed.compileComponents();\n\n// Create the component fixture here\n",
          "store.ts": "// store setup"
        },
        "expectedOutcome": "A fixture variable is created that holds the ComponentFixture instance for TestComponent.",
        "successCriteria": [
          "TestBed.createComponent(TestComponent) is called and the result is assigned to a variable named fixture with type ComponentFixture<TestComponent>."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're calling TestBed.createComponent(TestComponent) and storing the result in a variable named fixture.",
        "feedbackWrong": "The fixture wasn't created correctly. You need to call TestBed.createComponent(TestComponent) and assign it to a variable.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "TestBed.createComponent",
            "TestComponent",
            "fixture",
            "ComponentFixture"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Access the component instance",
        "instruction": "Access the component instance from the fixture and store it in a variable named 'componentInstance'.",
        "hint": "Use fixture.componentInstance to get the component instance.",
        "analogousExample": "// Accessing a component instance\nconst componentInstance = fixture.componentInstance;",
        "seedCode": {
          "App.tsx": "import { TestBed, ComponentFixture, Component } from '@angular/core/testing';\n\n@Component({\n  selector: 'app-test',\n  template: '<p>Test Component</p>'\n})\nexport class TestComponent {}\n\n// Configure the testing module here\nTestBed.configureTestingModule({\n  declarations: [TestComponent]\n});\n\n// Call TestBed.compileComponents() here\nTestBed.compileComponents();\n\n// Create the component fixture here\n\n// Access the component instance here\n",
          "store.ts": "// store setup"
        },
        "expectedOutcome": "The component instance is retrieved and stored in a variable for later use in tests.",
        "successCriteria": [
          "The component instance is accessed using fixture.componentInstance and stored in a variable named 'componentInstance'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using fixture.componentInstance and storing it in a variable named 'componentInstance'.",
        "feedbackWrong": "access the component instance using fixture.componentInstance and store it in a variable.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "fixture.componentInstance",
            "componentInstance"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Trigger change detection",
        "instruction": "Call fixture.detectChanges() to trigger initial change detection after creating the component fixture.",
        "hint": "Use the fixture variable you created earlier and call its detectChanges() method.",
        "analogousExample": "// Similar: triggering change detection\nfixture.detectChanges();",
        "seedCode": {
          "App.tsx": "import { TestBed, ComponentFixture, Component } from '@angular/core/testing';\n\n@Component({\n  selector: 'app-test',\n  template: '<p>Test Component</p>'\n})\nexport class TestComponent {}\n\n// Configure the testing module here\nTestBed.configureTestingModule({\n  declarations: [TestComponent]\n});\n\n// Call TestBed.compileComponents() here\nTestBed.compileComponents();\n\n// Create the component fixture here\nlet fixture: ComponentFixture<TestComponent>;\nfixture = TestBed.createComponent(TestComponent);\n\n// Access the component instance here\nlet component: TestComponent;\ncomponent = fixture.componentInstance;\n\n// Trigger change detection here\n",
          "store.ts": "// store setup"
        },
        "expectedOutcome": "The component's template will be rendered and any initial data bindings will be processed.",
        "successCriteria": [
          "fixture.detectChanges() is called after creating the component fixture."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're calling detectChanges() on the fixture variable.",
        "feedbackWrong": "Try calling fixture.detectChanges() to trigger the initial change detection cycle.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "fixture.detectChanges()"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Access the native DOM element",
        "instruction": "Access the native DOM element from the fixture and store it in a variable called 'nativeElement'.",
        "hint": "Use fixture.nativeElement to get the rendered DOM element.",
        "analogousExample": "// Accessing a DOM element in a test\nlet element = fixture.nativeElement;",
        "seedCode": {
          "App.tsx": "import { TestBed, ComponentFixture, Component } from '@angular/core/testing';\n\n@Component({\n  selector: 'app-test',\n  template: '<p>Test Component</p>'\n})\nexport class TestComponent {}\n\n// Configure the testing module here\nTestBed.configureTestingModule({\n  declarations: [TestComponent]\n});\n\n// Call TestBed.compileComponents() here\nTestBed.compileComponents();\n\n// Create the component fixture here\nlet fixture: ComponentFixture<TestComponent>;\nfixture = TestBed.createComponent(TestComponent);\n\n// Access the component instance here\nlet component: TestComponent;\ncomponent = fixture.componentInstance;\n\n// Trigger change detection here\n\n// Access the native DOM element here\nlet nativeElement: HTMLElement;",
          "store.ts": "// store setup"
        },
        "expectedOutcome": "The native DOM element is accessible via fixture.nativeElement and stored in a variable.",
        "successCriteria": [
          "A variable named 'nativeElement' is declared and assigned the value of fixture.nativeElement."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using fixture.nativeElement and storing it in a variable called 'nativeElement'.",
        "feedbackWrong": "That doesn't match what we need. Use fixture.nativeElement to get the DOM element and store it in a variable named 'nativeElement'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "nativeElement",
            "fixture.nativeElement"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "Write a test to verify component creation",
        "instruction": "Write an expect statement to verify the component instance exists. Use the component variable you already declared.",
        "hint": "Use the expect function with component as the argument and chain .toBeTruthy() to assert it exists.",
        "analogousExample": "// Basic test assertion\nexpect(someValue).toBeTruthy();",
        "seedCode": {
          "App.tsx": "import { TestBed, ComponentFixture, Component } from '@angular/core/testing';\n\n@Component({\n  selector: 'app-test',\n  template: '<p>Test Component</p>'\n})\nexport class TestComponent {}\n\n// Configure the testing module here\nTestBed.configureTestingModule({\n  declarations: [TestComponent]\n});\n\n// Call TestBed.compileComponents() here\nTestBed.compileComponents();\n\n// Create the component fixture here\nlet fixture: ComponentFixture<TestComponent>;\nfixture = TestBed.createComponent(TestComponent);\n\n// Access the component instance here\nlet component: TestComponent;\ncomponent = fixture.componentInstance;\n\n// Trigger change detection here\n\n// Access the native DOM element here\nlet nativeElement: HTMLElement;\n\n// Write your test assertion here\nexpect(component).toBeTruthy();",
          "store.ts": "// store setup"
        },
        "expectedOutcome": "A test assertion is added to verify the component instance exists.",
        "successCriteria": [
          "An expect statement checks that component is truthy using .toBeTruthy()."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using the component variable and calling .toBeTruthy() on it.",
        "feedbackWrong": "The test assertion is missing or incorrect. Use expect(component).toBeTruthy() to verify the component instance exists.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "expect(component)",
            "toBeTruthy()"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Write a test to verify template content",
        "instruction": "Write an expect statement to check if the paragraph text 'Test Component' appears in the native DOM element.",
        "hint": "Use the nativeElement's textContent property and the toContain matcher to verify the expected text.",
        "analogousExample": "// Example: verifying text content in a test\nexpect(nativeElement.textContent).toContain('Expected Text');",
        "seedCode": {
          "App.tsx": "import { TestBed, ComponentFixture, Component } from '@angular/core/testing';\n\n@Component({\n  selector: 'app-test',\n  template: '<p>Test Component</p>'\n})\nexport class TestComponent {}\n\n// Configure the testing module here\nTestBed.configureTestingModule({\n  declarations: [TestComponent]\n});\n\n// Call TestBed.compileComponents() here\nTestBed.compileComponents();\n\n// Create the component fixture here\nlet fixture: ComponentFixture<TestComponent>;\nfixture = TestBed.createComponent(TestComponent);\n\n// Access the component instance here\nlet component: TestComponent;\ncomponent = fixture.componentInstance;\n\n// Trigger change detection here\nfixture.detectChanges();\n\n// Access the native DOM element here\nlet nativeElement: HTMLElement;\nnativeElement = fixture.nativeElement;\n\n// Write your test assertion here\nexpect(component).toBeTruthy();\n\n// Write your template content test here\n",
          "store.ts": "// store setup"
        },
        "expectedOutcome": "The test now verifies that the component's template renders the expected text content.",
        "successCriteria": [
          "An expect statement is added that checks if nativeElement.textContent contains 'Test Component'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you're using nativeElement.textContent and the toContain matcher to check for 'Test Component'.",
        "feedbackWrong": "The test for template content isn't quite right. You need to write an expect statement that checks if the paragraph text appears in the DOM.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "expect",
            "nativeElement.textContent",
            "toContain",
            "'Test Component'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import TestBed, ComponentFixture, and Component from Angular testing utilities",
        "id": "step1"
      },
      {
        "label": "Create a simple test component class",
        "id": "step2"
      },
      {
        "label": "Configure the testing module with TestBed",
        "id": "step3"
      },
      {
        "label": "Compile the testing module",
        "id": "step4"
      },
      {
        "label": "Create the component fixture",
        "id": "step5"
      },
      {
        "label": "Access the component instance",
        "id": "step6"
      },
      {
        "label": "Trigger change detection",
        "id": "step7"
      },
      {
        "label": "Access the native DOM element",
        "id": "step8"
      },
      {
        "label": "Write a test to verify component creation",
        "id": "step9"
      },
      {
        "label": "Write a test to verify template content",
        "id": "step10"
      }
    ],
    "answerShape": "multi-file"
  },
  "source": "content"
}