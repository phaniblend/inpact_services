{
  "success": true,
  "config": {
    "lessonId": "ai-angular-65",
    "track": "angular",
    "lessonNum": 65,
    "title": "Error Boundary",
    "shortName": "P65",
    "intro": {
      "tag": "LESSON #65",
      "title": "Error Boundary",
      "body": "In this lesson, you will build a simple Angular application that demonstrates error boundaries by creating a component that catches and displays errors gracefully. You will learn the core concept of error handling in Angular using lifecycle hooks and custom error handling logic. The main technologies involved are Angular components, TypeScript, and the ErrorHandler interface. By the end, you will understand how to prevent your app from crashing when unexpected errors occur.",
      "usecase": "Error boundaries are essential in production Angular applications where components can fail unexpectedly. In a financial dashboard, if a chart component crashes while fetching live market data, an error boundary prevents the entire dashboard from breaking and shows a fallback UI instead. Similarly, in e-commerce product listings, if a product card component fails to render due to missing data, the boundary isolates the error so users can still browse other products. This pattern ensures critical application sections remain functional even when non-critical components encounter errors."
    },
    "objectives": [
      "Construct an Angular error boundary component using @Component decorator",
      "Implement error handling logic with try-catch blocks in component methods",
      "Configure the error boundary to intercept errors from child components",
      "Render fallback UI content when errors are caught",
      "Bind error state to template display logic",
      "Integrate the error boundary component into the application component tree",
      "Initialize error boundary with default error state properties"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Import Component, ErrorHandler, Injectable, and NgModule from Angular core",
        "instruction": "Import Component, ErrorHandler, Injectable, and NgModule from '@angular/core'.",
        "hint": "Use the import statement with curly braces to import multiple symbols from the Angular core module.",
        "analogousExample": "// Importing multiple Angular core modules\nimport { Component, Injectable } from '@angular/core';",
        "seedCode": "// Import Component, ErrorHandler, Injectable, and NgModule from '@angular/core'",
        "expectedOutcome": "The Angular core modules needed for error handling are imported.",
        "successCriteria": [
          "Component, ErrorHandler, Injectable, and NgModule are imported from '@angular/core'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You imported some modules, but make sure to include all four: Component, ErrorHandler, Injectable, and NgModule.",
        "feedbackWrong": "The imports are missing or incorrect. Please import Component, ErrorHandler, Injectable, and NgModule from '@angular/core'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "Component",
            "ErrorHandler",
            "Injectable",
            "NgModule",
            "@angular/core"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Create a custom ErrorHandler service",
        "instruction": "Create a new service class named 'CustomErrorHandler' that extends Angular's built-in ErrorHandler and mark it as Injectable.",
        "hint": "Use the @Injectable() decorator above your class definition and make sure it extends ErrorHandler.",
        "analogousExample": "// Example: A service that extends a base class\n@Injectable()\nexport class CustomLogger extends Logger {\n  log(message: string) {\n    console.log(`Custom: ${message}`);\n  }\n}",
        "seedCode": "import { Component, ErrorHandler, Injectable, NgModule } from '@angular/core';\n\n@Injectable()\nexport class CustomErrorHandler extends ErrorHandler {\n  \n}",
        "expectedOutcome": "You'll have a CustomErrorHandler service class ready to implement custom error handling logic.",
        "successCriteria": [
          "The CustomErrorHandler class exists, extends ErrorHandler, and is decorated with @Injectable()."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure your class extends ErrorHandler and has the @Injectable() decorator.",
        "feedbackWrong": "The service class isn't properly defined. Remember to create a class named CustomErrorHandler that extends ErrorHandler and add the @Injectable() decorator.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "CustomErrorHandler",
            "extends",
            "ErrorHandler",
            "@Injectable()"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Override the handleError method",
        "instruction": "Inside your CustomErrorHandler class, override the handleError method to log the error to the console with a custom message.",
        "hint": "Use the override keyword before the method name and implement the method body to call console.error with your message.",
        "analogousExample": "// Example: Overriding a method in a class\nclass BaseLogger {\n  log(message: string) {\n    console.log(message);\n  }\n}\n\nclass CustomLogger extends BaseLogger {\n  override log(message: string) {\n    console.error(`Custom: ${message}`);\n  }\n}",
        "seedCode": "import { Component, ErrorHandler, Injectable, NgModule } from '@angular/core';\n\n@Injectable()\nexport class CustomErrorHandler extends ErrorHandler {\n  // Override the handleError method here\n}",
        "expectedOutcome": "The CustomErrorHandler class will have a handleError method that logs errors with a custom message.",
        "successCriteria": [
          "The handleError method is overridden with the override keyword and contains console.error with a custom message."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using the override keyword and implementing the method body to log the error.",
        "feedbackWrong": "The handleError method needs to be overridden with the override keyword and should log errors to the console.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "override",
            "handleError",
            "console.error"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Create a component that will throw an error",
        "instruction": "Create a new Angular component named 'ErrorThrowingComponent' with a simple template containing a button and a method that throws an error when the button is clicked.",
        "hint": "Use the @Component decorator to define the component. Add a button in the template with a click event that calls a method. In that method, throw a new Error.",
        "analogousExample": "// Example: A component with a button that triggers an error\n@Component({\n  selector: 'app-error-button',\n  template: `<button (click)=\"throwError()\">Throw Error</button>`\n})\nexport class ErrorButtonComponent {\n  throwError() {\n    throw new Error('Something went wrong!');\n  }\n}",
        "seedCode": "import { Component, ErrorHandler, Injectable, NgModule } from '@angular/core';\n\n@Injectable()\nexport class CustomErrorHandler extends ErrorHandler {\n  // Override the handleError method here\n}\n\n// Create the ErrorThrowingComponent here",
        "expectedOutcome": "You will have a new component that can be used to simulate an error condition in your application.",
        "successCriteria": [
          "The ErrorThrowingComponent is created with a @Component decorator, contains a button in its template, and has a method that throws an error when the button is clicked."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure your component has both a template with a button and a method that throws an error when that button is clicked.",
        "feedbackWrong": "The component doesn't seem to be set up correctly. Remember to use @Component decorator, include a button in the template, and create a method that throws an error.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "@Component",
            "ErrorThrowingComponent",
            "throwError",
            "throw new Error",
            "(click)=\"throwError()\""
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Add a button to trigger the error",
        "instruction": "In the ErrorThrowingComponent's template, add a button with the text 'Throw Error' that calls a method named throwError when clicked.",
        "hint": "Use the (click) event binding to call the throwError method. The button should be inside the component's template.",
        "analogousExample": "// Example: Button with click handler in Angular\n@Component({\n  selector: 'app-example',\n  template: `<button (click)=\"handleClick()\">Click Me</button>`\n})\nexport class ExampleComponent {\n  handleClick() {\n    console.log('Button clicked');\n  }\n}",
        "seedCode": "import { Component, ErrorHandler, Injectable, NgModule } from '@angular/core';\n\n@Injectable()\nexport class CustomErrorHandler extends ErrorHandler {\n  // Override the handleError method here\n}\n\n// Create the ErrorThrowingComponent here\n@Component({\n  selector: 'app-error-throwing',\n  template: `\n    <button (click)=\"throwError()\">Throw Error</button>\n  `\n})\nexport class ErrorThrowingComponent {\n  throwError() {\n    // Method to throw an error\n  }\n}",
        "expectedOutcome": "The component template now includes a button that will trigger an error when clicked.",
        "successCriteria": [
          "The ErrorThrowingComponent has a button with (click)=\"throwError()\" and the text 'Throw Error' in its template."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure the button has the correct text and the (click) event binding calls throwError().",
        "feedbackWrong": "The button is missing or not properly configured. Check the template for the button with (click)=\"throwError()\".",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "(click)=\"throwError()\"",
            "Throw Error",
            "button"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Register the custom ErrorHandler in the module",
        "instruction": "Add your CustomErrorHandler service to the providers array of the NgModule.",
        "hint": "In the NgModule decorator, include providers: [CustomErrorHandler] to replace Angular's default error handler.",
        "analogousExample": "// Example: Registering a service in NgModule\n@NgModule({\n  declarations: [AppComponent],\n  imports: [BrowserModule],\n  providers: [MyCustomService], // Add service here\n  bootstrap: [AppComponent]\n})\nexport class AppModule {}",
        "seedCode": "import { Component, ErrorHandler, Injectable, NgModule } from '@angular/core';\n\n@Injectable()\nexport class CustomErrorHandler extends ErrorHandler {\n  // Override the handleError method here\n}\n\n// Create the ErrorThrowingComponent here\n@Component({\n  selector: 'app-error-throwing',\n  template: `\n    <button (click)=\"throwError()\">Throw Error</button>\n  `\n})\nexport class ErrorThrowingComponent {\n  throwError() {\n    // Method to throw an error\n  }\n}\n\n// Add NgModule decorator with providers array here\n@NgModule({\n  declarations: [ErrorThrowingComponent],\n  imports: [],\n  providers: [], // Add CustomErrorHandler here\n  bootstrap: [ErrorThrowingComponent]\n})\nexport class AppModule {}",
        "expectedOutcome": "The NgModule will be configured to use your custom error handler service instead of Angular's default.",
        "successCriteria": [
          "CustomErrorHandler is listed in the providers array of the NgModule decorator."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure CustomErrorHandler is added to the providers array inside the NgModule decorator.",
        "feedbackWrong": "The custom error handler isn't registered yet. Add CustomErrorHandler to the providers array in the NgModule.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "providers",
            "CustomErrorHandler"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Add error state to the component",
        "instruction": "In the ErrorThrowingComponent class, add a boolean property named 'hasError' to track whether an error has occurred.",
        "hint": "Add a property with type boolean and initialize it to false in the component class.",
        "analogousExample": "// Example: adding a boolean property to track state\nclass ExampleComponent {\n  hasError: boolean = false;\n}",
        "seedCode": "import { Component, ErrorHandler, Injectable, NgModule } from '@angular/core';\n\n@Injectable()\nexport class CustomErrorHandler extends ErrorHandler {\n  // Override the handleError method here\n}\n\n// Create the ErrorThrowingComponent here\n@Component({\n  selector: 'app-error-throwing',\n  template: `\n    <button (click)=\"throwError()\">Throw Error</button>\n  `\n})\nexport class ErrorThrowingComponent {\n  // Add a boolean property named 'hasError' here\n  \n  throwError() {\n    // Method to throw an error\n  }\n}\n\n// Add NgModule decorator with providers array here\n@NgModule({\n  declarations: [ErrorThrowingComponent],\n  imports: [],\n  providers: [], // Add CustomErrorHandler here\n  bootstrap: [ErrorThrowingComponent]\n})\nexport class AppModule {}",
        "expectedOutcome": "The ErrorThrowingComponent will have a boolean property 'hasError' initialized to false, ready to track error conditions.",
        "successCriteria": [
          "The ErrorThrowingComponent class contains a boolean property named 'hasError' initialized to false."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you've added a boolean property named 'hasError' with an initial value of false in the ErrorThrowingComponent class.",
        "feedbackWrong": "The component doesn't have the required error state property. Add a boolean property named 'hasError' initialized to false in the ErrorThrowingComponent class.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "hasError",
            "boolean",
            "=",
            "false"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Update the error handler to set the error state",
        "instruction": "In the CustomErrorHandler class, modify the handleError method to update the ErrorThrowingComponent's hasError property to true when an error occurs.",
        "hint": "You'll need to access the ErrorThrowingComponent instance. Consider how you might pass component state to the error handler.",
        "analogousExample": "// Example: updating component state from a service\nclass ErrorService {\n  private componentRef: any;\n\n  setComponent(comp: any) {\n    this.componentRef = comp;\n  }\n\n  handleError(error: any) {\n    console.error('Error caught:', error);\n    if (this.componentRef) {\n      this.componentRef.hasError = true;\n    }\n  }\n}",
        "seedCode": "import { Component, ErrorHandler, Injectable, NgModule } from '@angular/core';\n\n@Injectable()\nexport class CustomErrorHandler extends ErrorHandler {\n  // Override the handleError method here\n  handleError(error: any) {\n    // Update component error state here\n  }\n}\n\n// Create the ErrorThrowingComponent here\n@Component({\n  selector: 'app-error-throwing',\n  template: `\n    <button (click)=\"throwError()\">Throw Error</button>\n  `\n})\nexport class ErrorThrowingComponent {\n  // Add a boolean property named 'hasError' here\n  hasError = false;\n  \n  throwError() {\n    // Method to throw an error\n  }\n}\n\n// Add NgModule decorator with providers array here\n@NgModule({\n  declarations: [ErrorThrowingComponent],\n  imports: [],\n  providers: [], // Add CustomErrorHandler here\n  bootstrap: [ErrorThrowingComponent]\n})\nexport class AppModule {}",
        "expectedOutcome": "The handleError method now updates the component's error state when an error is caught.",
        "successCriteria": [
          "The CustomErrorHandler's handleError method sets the ErrorThrowingComponent's hasError property to true."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure the handleError method explicitly sets the component's hasError property to true.",
        "feedbackWrong": "The handleError method should update the component's error state. Review how to access and modify component properties from the error handler.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "handleError",
            "hasError",
            "=",
            "true"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "Add conditional rendering based on error state",
        "instruction": "In the ErrorThrowingComponent template, use *ngIf to show an error message when hasError is true, and keep the button visible only when hasError is false.",
        "hint": "You can use two separate *ngIf directives: one for the error message and one for the button. The error message should display when hasError is true.",
        "analogousExample": "// Example of conditional rendering with *ngIf\n@Component({\n  selector: 'app-example',\n  template: `\n    <p *ngIf=\"isVisible\">This is visible!</p>\n    <button *ngIf=\"!isVisible\">Show</button>\n  `\n})\nexport class ExampleComponent {\n  isVisible = false;\n}",
        "seedCode": "import { Component, ErrorHandler, Injectable, NgModule } from '@angular/core';\n\n@Injectable()\nexport class CustomErrorHandler extends ErrorHandler {\n  // Override the handleError method here\n  handleError(error: any) {\n    // Update component error state here\n  }\n}\n\n// Create the ErrorThrowingComponent here\n@Component({\n  selector: 'app-error-throwing',\n  template: `\n    <button (click)=\"throwError()\">Throw Error</button>\n  `\n})\nexport class ErrorThrowingComponent {\n  // Add a boolean property named 'hasError' here\n  hasError = false;\n  \n  throwError() {\n    // Method to throw an error\n  }\n}\n\n// Add NgModule decorator with providers array here\n@NgModule({\n  declarations: [ErrorThrowingComponent],\n  imports: [],\n  providers: [], // Add CustomErrorHandler here\n  bootstrap: [ErrorThrowingComponent]\n})\nexport class AppModule {}",
        "expectedOutcome": "The component template now includes conditional rendering that displays an error message when an error occurs and hides the button, showing the button only when no error is present.",
        "successCriteria": [
          "The template contains *ngIf directives that conditionally show an error message when hasError is true and the button when hasError is false."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you have both *ngIf conditions: one for the error message and one for the button.",
        "feedbackWrong": "The template doesn't yet have the conditional rendering. Remember to use *ngIf to show the error message when hasError is true and the button when hasError is false.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "*ngIf",
            "hasError",
            "error",
            "button"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Add a reset mechanism to recover from errors",
        "instruction": "Add a reset button to the ErrorThrowingComponent template that clears the error state when clicked.",
        "hint": "Add a button element next to the existing button in the template, and bind its click event to a method that sets hasError back to false.",
        "analogousExample": "// Adding a reset button in a component\n@Component({\n  selector: 'app-example',\n  template: `\n    <button (click)=\"reset()\">Reset</button>\n  `\n})\nexport class ExampleComponent {\n  reset() {\n    // Reset logic here\n  }\n}",
        "seedCode": "import { Component, ErrorHandler, Injectable, NgModule } from '@angular/core';\n\n@Injectable()\nexport class CustomErrorHandler extends ErrorHandler {\n  // Override the handleError method here\n  handleError(error: any) {\n    // Update component error state here\n  }\n}\n\n// Create the ErrorThrowingComponent here\n@Component({\n  selector: 'app-error-throwing',\n  template: `\n    <button (click)=\"throwError()\">Throw Error</button>\n  `\n})\nexport class ErrorThrowingComponent {\n  // Add a boolean property named 'hasError' here\n  hasError = false;\n  \n  throwError() {\n    // Method to throw an error\n  }\n}\n\n// Add NgModule decorator with providers array here\n@NgModule({\n  declarations: [ErrorThrowingComponent],\n  imports: [],\n  providers: [], // Add CustomErrorHandler here\n  bootstrap: [ErrorThrowingComponent]\n})\nexport class AppModule {}",
        "expectedOutcome": "The component template will have a second button labeled 'Reset' that, when clicked, will clear the error state.",
        "successCriteria": [
          "A reset button is added to the template with a click handler that sets hasError to false."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added a reset button, but make sure it properly clears the error state when clicked.",
        "feedbackWrong": "The reset button or its functionality is missing. Add a button that clears the error state.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "reset",
            "hasError",
            "false",
            "(click)"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import Component, ErrorHandler, Injectable, and NgModule from Angular core",
        "id": "step1"
      },
      {
        "label": "Create a custom ErrorHandler service",
        "id": "step2"
      },
      {
        "label": "Override the handleError method",
        "id": "step3"
      },
      {
        "label": "Create a component that will throw an error",
        "id": "step4"
      },
      {
        "label": "Add a button to trigger the error",
        "id": "step5"
      },
      {
        "label": "Register the custom ErrorHandler in the module",
        "id": "step6"
      },
      {
        "label": "Add error state to the component",
        "id": "step7"
      },
      {
        "label": "Update the error handler to set the error state",
        "id": "step8"
      },
      {
        "label": "Add conditional rendering based on error state",
        "id": "step9"
      },
      {
        "label": "Add a reset mechanism to recover from errors",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}