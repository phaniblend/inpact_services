{
  "success": true,
  "config": {
    "lessonId": "ai-js-48",
    "track": "js",
    "lessonNum": 48,
    "title": "Build a typeahead autocomplete box",
    "shortName": "P48",
    "intro": {
      "tag": "LESSON #48",
      "title": "Build a typeahead autocomplete box",
      "body": "In this lesson, you will build a typeahead autocomplete box—a search input that suggests matching options as the user types. You'll learn how to filter an array of data based on user input to display real-time suggestions. The main technologies involved are JavaScript arrays, string methods, and DOM event handling for input changes. This project will help you understand dynamic UI updates and basic search functionality in a web application.",
      "usecase": "Typeahead autocomplete is essential for modern user interfaces where quick search and data entry are required. You'll find this pattern in e-commerce search bars that suggest products as you type, form fields that auto-complete addresses or email addresses, and dashboard filters that help users narrow down large datasets. As a developer, implementing this feature improves user experience by reducing typing effort, preventing input errors, and helping users discover available options faster. This skill is valuable for building responsive search interfaces, smart form components, and interactive data exploration tools."
    },
    "objectives": [
      "Construct a basic HTML structure for the typeahead input and results container",
      "Initialize and manage the state for user input and filtered suggestions",
      "Implement an event handler to capture and process user keystrokes",
      "Apply a filtering algorithm to match input against a dataset of suggestions",
      "Render dynamic search results that update based on the current input",
      "Bind keyboard navigation events to allow selection of suggestions",
      "Control the visibility of the results dropdown based on user interaction"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 11",
        "title": "Import React and useState",
        "instruction": "Import React and the useState hook from the 'react' package.",
        "hint": "Use the import statement to bring in React and useState from 'react'.",
        "analogousExample": "// Importing React and a hook\nimport React, { useState } from 'react';",
        "seedCode": "// Import React and useState from 'react'",
        "expectedOutcome": "You'll have React and useState available to use in your component.",
        "successCriteria": [
          "React and useState are imported from 'react'."
        ],
        "feedbackCorrect": "Great! You've successfully imported React and useState.",
        "feedbackPartial": "You're close. Make sure you're importing both React and useState from 'react'.",
        "feedbackWrong": "That's not quite right. Try importing React and useState from 'react'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "React",
            "useState",
            "'react'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 11",
        "title": "Create the main Typeahead component",
        "instruction": "Create a functional component named Typeahead that returns an empty div element.",
        "hint": "Define a function called Typeahead that returns JSX containing a <div></div>.",
        "analogousExample": "// Example of a functional component\nfunction MyComponent() {\n  return <div></div>;\n}",
        "seedCode": "import React, { useState } from 'react';\n// Create the Typeahead component here",
        "expectedOutcome": "A Typeahead component is defined and returns an empty div.",
        "successCriteria": [
          "The Typeahead component is defined and returns a div element."
        ],
        "feedbackCorrect": "Great! You've created the Typeahead component structure.",
        "feedbackPartial": "You're close. Make sure the component returns a div element.",
        "feedbackWrong": "The component isn't defined correctly. Create a function named Typeahead that returns a div.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "Typeahead",
            "return",
            "<div>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 11",
        "title": "Define the sample data array",
        "instruction": "Inside the Typeahead component, create a constant array named 'sampleData' containing several fruit names (e.g., 'Apple', 'Banana', 'Cherry') that will be used for autocomplete suggestions.",
        "hint": "Declare the array using 'const' inside the component function body, before the return statement. You can include 5-10 fruit names as strings.",
        "analogousExample": "// Example: creating a static array of colors\nconst colors = ['Red', 'Blue', 'Green', 'Yellow'];",
        "seedCode": "import React, { useState } from 'react';\n// Create the Typeahead component here\nconst Typeahead = () => {\n  // Define the sample data array here\n\n  return null;\n};",
        "expectedOutcome": "The component will have a static dataset ready to be used for filtering suggestions based on user input.",
        "successCriteria": [
          "A constant array named 'sampleData' is declared inside the Typeahead component with at least three fruit name strings."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you've declared a constant array with fruit names inside the component.",
        "feedbackWrong": "The sample data array isn't defined correctly. Create a constant array named 'sampleData' with fruit name strings inside the component.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "sampleData",
            "const",
            "["
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 11",
        "title": "Initialize state for the input value",
        "instruction": "Inside your Typeahead component, use the useState hook to create a state variable called 'inputValue' and its setter function, initializing it to an empty string.",
        "hint": "Call useState with an empty string as the initial value, and destructure the returned array into two variables (e.g., const [inputValue, setInputValue] = useState('')).",
        "analogousExample": "// Example: creating a state variable for a text input\nconst [text, setText] = useState('');",
        "seedCode": "import React, { useState } from 'react';\n// Create the Typeahead component here\nconst Typeahead = () => {\n  // Define the sample data array here\n  const items = ['Apple', 'Banana', 'Cherry', 'Date', 'Elderberry'];\n\n  // Initialize state for the input value here\n\n  return null;\n};",
        "expectedOutcome": "The component now has a state variable to track the user's typed input.",
        "successCriteria": [
          "The useState hook is called with an empty string, and the returned array is destructured into 'inputValue' and 'setInputValue' (or similar names)."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're calling useState with an empty string and destructuring the result into two variables.",
        "feedbackWrong": "Inside the component, use useState to create a state variable for the input value.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useState",
            "inputValue",
            "setInputValue",
            "''"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 11",
        "title": "Initialize state for filtered suggestions",
        "instruction": "Inside your Typeahead component, use useState to create a state variable named 'suggestions' and its setter function, initializing it to an empty array.",
        "hint": "Add a new useState call that returns an array with two elements: the state variable and its setter function.",
        "analogousExample": "// Similar: state for a list\nconst [items, setItems] = useState([]);",
        "seedCode": "import React, { useState } from 'react';\n// Create the Typeahead component here\nconst Typeahead = () => {\n  // Define the sample data array here\n  const items = ['Apple', 'Banana', 'Cherry', 'Date', 'Elderberry'];\n\n  // Initialize state for the input value here\n\n  // Initialize state for filtered suggestions here\n\n  return null;\n};",
        "expectedOutcome": "The component will have a 'suggestions' state variable ready to store filtered matches from the items array.",
        "successCriteria": [
          "The component contains a useState call that declares a 'suggestions' state variable initialized to an empty array."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using useState to create a state variable named 'suggestions' initialized as an empty array.",
        "feedbackWrong": "Try again. You need to add a useState call inside the component to create a 'suggestions' state variable.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useState",
            "suggestions",
            "[]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 11",
        "title": "Create the filter handler function",
        "instruction": "Inside your Typeahead component, create a function named handleInputChange that takes an event parameter, updates the inputValue state, and filters the items array to update the suggestions state.",
        "hint": "The function should get the current input value from event.target.value, use setInputValue to update state, then filter items to include only those that start with the input value (case-insensitive).",
        "analogousExample": "// Example: filter handler function\nfunction handleInputChange(event) {\n  const value = event.target.value;\n  setInputValue(value);\n  const filtered = items.filter(item => \n    item.toLowerCase().startsWith(value.toLowerCase())\n  );\n  setSuggestions(filtered);\n}",
        "seedCode": "import React, { useState } from 'react';\n// Create the Typeahead component here\nconst Typeahead = () => {\n  // Define the sample data array here\n  const items = ['Apple', 'Banana', 'Cherry', 'Date', 'Elderberry'];\n\n  // Initialize state for the input value here\n  const [inputValue, setInputValue] = useState('');\n\n  // Initialize state for filtered suggestions here\n  const [suggestions, setSuggestions] = useState([]);\n\n  // Create the filter handler function here\n\n  return null;\n};",
        "expectedOutcome": "You'll have a handleInputChange function that updates both state variables based on user input.",
        "successCriteria": [
          "The handleInputChange function is defined, takes an event parameter, calls setInputValue with event.target.value, and filters items to update suggestions state."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure your function updates both inputValue and suggestions states based on the input value.",
        "feedbackWrong": "The function should update inputValue state and filter suggestions. Check that you're using setInputValue and setSuggestions correctly.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "handleInputChange",
            "event",
            "setInputValue",
            "setSuggestions",
            "filter"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 11",
        "title": "Add the input element and wire its onChange",
        "instruction": "Inside your component's return statement, add an input element with its value bound to the inputValue state and its onChange event wired to call handleInputChange.",
        "hint": "Use the input element's value attribute to bind it to inputValue, and set onChange to handleInputChange.",
        "analogousExample": "// Example: input element with value binding and onChange\n<input value={inputValue} onChange={handleInputChange} />",
        "seedCode": "import React, { useState } from 'react';\n// Create the Typeahead component here\nconst Typeahead = () => {\n  // Define the sample data array here\n  const items = ['Apple', 'Banana', 'Cherry', 'Date', 'Elderberry'];\n\n  // Initialize state for the input value here\n  const [inputValue, setInputValue] = useState('');\n\n  // Initialize state for filtered suggestions here\n  const [suggestions, setSuggestions] = useState([]);\n\n  // Create the filter handler function here\n  const handleInputChange = (e) => {\n    const value = e.target.value;\n    setInputValue(value);\n    const filtered = items.filter(item =>\n      item.toLowerCase().includes(value.toLowerCase())\n    );\n    setSuggestions(filtered);\n  };\n\n  // Create the suggestion selection handler here\n  const handleSelect = (item) => {\n    setInputValue(item);\n    setSuggestions([]);\n  };\n\n  return (\n    <div>\n      {/* Add input element here */}\n    </div>\n  );\n};",
        "expectedOutcome": "The input element will be rendered with its value controlled by the inputValue state, and typing will trigger the filter handler.",
        "successCriteria": [
          "The input element has value={inputValue} and onChange={handleInputChange} attributes."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added an input element, but make sure it has both value={inputValue} and onChange={handleInputChange} attributes.",
        "feedbackWrong": "The input element is missing or not properly wired. Add an input element with value={inputValue} and onChange={handleInputChange}.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "input",
            "value={inputValue}",
            "onChange={handleInputChange}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 11",
        "title": "Add a container for the suggestions list",
        "instruction": "Inside the return statement of your Typeahead component, add a div element after the input element to serve as the container for the suggestions list.",
        "hint": "Add a <div> element right after the closing tag of the input element. This div will eventually hold the list of suggestions.",
        "analogousExample": "// Example: adding a container element\n<div>\n  <input />\n  <div className=\"suggestions-container\"></div>\n</div>",
        "seedCode": "import React, { useState } from 'react';\n// Create the Typeahead component here\nconst Typeahead = () => {\n  // Define the sample data array here\n  const items = ['Apple', 'Banana', 'Cherry', 'Date', 'Elderberry'];\n\n  // Initialize state for the input value here\n  const [inputValue, setInputValue] = useState('');\n\n  // Initialize state for filtered suggestions here\n  const [suggestions, setSuggestions] = useState([]);\n\n  // Create the filter handler function here\n  const handleInputChange = (e) => {\n    const value = e.target.value;\n    setInputValue(value);\n    const filtered = items.filter(item =>\n      item.toLowerCase().includes(value.toLowerCase())\n    );\n    setSuggestions(filtered);\n  };\n\n  // Create the suggestion selection handler here\n  const handleSelect = (item) => {\n    setInputValue(item);\n    setSuggestions([]);\n  };\n\n  return (\n    <div>\n      {/* Add input element here */}\n      <input value={inputValue} onChange={handleInputChange} />\n      \n    </div>\n  );\n};",
        "expectedOutcome": "The component will have a div element after the input that will later hold the suggestions list.",
        "successCriteria": [
          "A div element is added after the input element in the return statement."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you've added a div element after the input element inside the return statement.",
        "feedbackWrong": "Add a div element after the input element to create a container for the suggestions list.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<div>",
            "</div>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 11",
        "title": "Map over suggestions to display list items",
        "instruction": "Inside the suggestions container, map over the suggestions state array to render each item as a list item (li).",
        "hint": "Use the map method on the suggestions array to create an array of JSX elements. Each suggestion should be wrapped in an <li> tag.",
        "analogousExample": "// Example: mapping an array to list items\nconst fruits = ['Apple', 'Banana', 'Cherry'];\nconst fruitList = fruits.map((fruit, index) => (\n  <li key={index}>{fruit}</li>\n));",
        "seedCode": "import React, { useState } from 'react';\n// Create the Typeahead component here\nconst Typeahead = () => {\n  // Define the sample data array here\n  const items = ['Apple', 'Banana', 'Cherry', 'Date', 'Elderberry'];\n\n  // Initialize state for the input value here\n  const [inputValue, setInputValue] = useState('');\n\n  // Initialize state for filtered suggestions here\n  const [suggestions, setSuggestions] = useState([]);\n\n  // Create the filter handler function here\n  const handleInputChange = (e) => {\n    const value = e.target.value;\n    setInputValue(value);\n    const filtered = items.filter(item =>\n      item.toLowerCase().includes(value.toLowerCase())\n    );\n    setSuggestions(filtered);\n  };\n\n  // Create the suggestion selection handler here\n  const handleSelect = (item) => {\n    setInputValue(item);\n    setSuggestions([]);\n  };\n\n  return (\n    <div>\n      {/* Add input element here */}\n      <input value={inputValue} onChange={handleInputChange} />\n      \n    </div>\n  );\n};",
        "expectedOutcome": "The suggestions array is mapped to <li> elements, each displaying a suggestion.",
        "successCriteria": [
          "The suggestions container contains a map function that returns <li> elements for each suggestion."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you're using the map method on the suggestions array and returning an <li> for each item.",
        "feedbackWrong": "Try again. You need to map over the suggestions array to create list items.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "suggestions.map",
            "<li>",
            "key"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 11",
        "title": "Wire click events to suggestion items",
        "instruction": "Add an onClick handler to each suggestion list item that calls the handleSelect function with the item's text.",
        "hint": "Inside the map function where you render each suggestion, add onClick={() => handleSelect(item)} to the <li> element.",
        "analogousExample": "// Adding click handler to list items\nconst items = ['Option1', 'Option2'];\nconst handleClick = (item) => console.log(item);\n\nitems.map(item => (\n  <li key={item} onClick={() => handleClick(item)}>\n    {item}\n  </li>\n));",
        "seedCode": "import React, { useState } from 'react';\n// Create the Typeahead component here\nconst Typeahead = () => {\n  // Define the sample data array here\n  const items = ['Apple', 'Banana', 'Cherry', 'Date', 'Elderberry'];\n\n  // Initialize state for the input value here\n  const [inputValue, setInputValue] = useState('');\n\n  // Initialize state for filtered suggestions here\n  const [suggestions, setSuggestions] = useState([]);\n\n  // Create the filter handler function here\n  const handleInputChange = (e) => {\n    const value = e.target.value;\n    setInputValue(value);\n    const filtered = items.filter(item =>\n      item.toLowerCase().includes(value.toLowerCase())\n    );\n    setSuggestions(filtered);\n  };\n\n  // Create the suggestion selection handler here\n  const handleSelect = (item) => {\n    setInputValue(item);\n    setSuggestions([]);\n  };\n\n  return (\n    <div>\n      {/* Add input element here */}\n      <input value={inputValue} onChange={handleInputChange} />\n      \n    </div>\n  );\n};",
        "expectedOutcome": "Each suggestion list item will be clickable and will call handleSelect when clicked.",
        "successCriteria": [
          "The onClick handler is added to each <li> element inside the suggestions map, calling handleSelect with the item's text."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure the onClick handler calls handleSelect with the correct item text.",
        "feedbackWrong": "The click handler isn't properly connected. Check that you're adding onClick={() => handleSelect(item)} to each <li> element.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "onClick",
            "handleSelect",
            "item"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step11",
        "type": "question",
        "phase": "Step 11 of 11",
        "title": "Conditionally show the suggestions container",
        "instruction": "Wrap the suggestions container in a conditional that only renders it when the suggestions array is not empty.",
        "hint": "You can use a ternary operator or logical AND (&&) to conditionally render the suggestions list based on whether suggestions has items.",
        "analogousExample": "// Conditional rendering example\nconst Example = ({ items }) => {\n  return (\n    <div>\n      {items.length > 0 && (\n        <ul>\n          {items.map(item => <li key={item}>{item}</li>)}\n        </ul>\n      )}\n    </div>\n  );\n};",
        "seedCode": "import React, { useState } from 'react';\n// Create the Typeahead component here\nconst Typeahead = () => {\n  // Define the sample data array here\n  const items = ['Apple', 'Banana', 'Cherry', 'Date', 'Elderberry'];\n\n  // Initialize state for the input value here\n  const [inputValue, setInputValue] = useState('');\n\n  // Initialize state for filtered suggestions here\n  const [suggestions, setSuggestions] = useState([]);\n\n  // Create the filter handler function here\n  const handleInputChange = (e) => {\n    const value = e.target.value;\n    setInputValue(value);\n    const filtered = items.filter(item =>\n      item.toLowerCase().includes(value.toLowerCase())\n    );\n    setSuggestions(filtered);\n  };\n\n  // Create the suggestion selection handler here\n  const handleSelect = (item) => {\n    setInputValue(item);\n    setSuggestions([]);\n  };\n\n  return (\n    <div>\n      {/* Add input element here */}\n      <input value={inputValue} onChange={handleInputChange} />\n      \n    </div>\n  );\n};",
        "expectedOutcome": "The suggestions list will only appear when there are matching items, and disappear when the input is cleared or a suggestion is selected.",
        "successCriteria": [
          "The suggestions container is wrapped in a conditional that checks if suggestions array has items."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but the conditional might not be checking the suggestions array correctly.",
        "feedbackWrong": "The suggestions list should only appear when there are filtered suggestions. Try using a conditional like `suggestions.length > 0 && (...)`.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "suggestions",
            "length",
            ">",
            "0",
            "&&",
            "<ul>",
            "map"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import React and useState",
        "id": "step1"
      },
      {
        "label": "Create the main Typeahead component",
        "id": "step2"
      },
      {
        "label": "Define the sample data array",
        "id": "step3"
      },
      {
        "label": "Initialize state for the input value",
        "id": "step4"
      },
      {
        "label": "Initialize state for filtered suggestions",
        "id": "step5"
      },
      {
        "label": "Create the filter handler function",
        "id": "step6"
      },
      {
        "label": "Add the input element and wire its onChange",
        "id": "step7"
      },
      {
        "label": "Add a container for the suggestions list",
        "id": "step8"
      },
      {
        "label": "Map over suggestions to display list items",
        "id": "step9"
      },
      {
        "label": "Wire click events to suggestion items",
        "id": "step10"
      },
      {
        "label": "Conditionally show the suggestions container",
        "id": "step11"
      }
    ]
  },
  "source": "content"
}