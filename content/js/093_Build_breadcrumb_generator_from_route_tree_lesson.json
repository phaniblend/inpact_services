{
  "success": true,
  "config": {
    "lessonId": "ai-js-93",
    "track": "js",
    "lessonNum": 93,
    "title": "Build breadcrumb generator from route tree",
    "shortName": "P93",
    "intro": {
      "tag": "LESSON #93",
      "title": "Build breadcrumb generator from route tree",
      "body": "In this lesson, you will build a breadcrumb generator that takes a route tree object and returns a list of breadcrumb links. You will learn how to traverse nested data structures using recursion, a core programming concept where a function calls itself. The main technologies involved are JavaScript objects, arrays, and recursive functions. By the end, you'll have a working function that can generate breadcrumb paths from any given route tree.",
      "usecase": "Breadcrumb generators are essential navigation patterns in complex web applications like e-commerce platforms, content management systems, and enterprise dashboards. When users navigate through multi-level product categories on Amazon or drill down into nested folders in Google Drive, breadcrumbs provide clear location context and one-click access to parent levels. Developers implement this pattern to transform hierarchical route data into visual trails that improve user orientation and reduce navigation friction. This skill is particularly valuable when building admin panels, documentation sites, or any application with deeply nested content structures."
    },
    "objectives": [
      "Construct a route tree data structure to represent hierarchical navigation paths",
      "Implement a recursive function to traverse the route tree and extract breadcrumb data",
      "Manipulate the extracted data to format breadcrumb labels and URLs",
      "Initialize and render a breadcrumb component with the generated navigation links",
      "Configure the breadcrumb generator to handle dynamic route changes",
      "Integrate the breadcrumb component into a navigation system",
      "Apply the breadcrumb generator to different route tree structures"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 9",
        "title": "Import React and useState",
        "instruction": "Import React and the useState hook from the 'react' library.",
        "hint": "Use the import statement to bring in both React and useState from the 'react' package.",
        "analogousExample": "// Example: importing React and a hook\nimport React, { useState } from 'react';",
        "seedCode": "// Import React and useState here",
        "expectedOutcome": "You'll have access to React and the useState hook for managing state in your component.",
        "successCriteria": [
          "React and useState are imported from 'react'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're importing both React and useState from 'react'. Check your import statement syntax.",
        "feedbackWrong": "That's not quite right. You need to import both React and useState from the 'react' library. Try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "React",
            "useState",
            "'react'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 9",
        "title": "Create the main BreadcrumbGenerator component",
        "instruction": "Create a functional component named BreadcrumbGenerator that returns null initially.",
        "hint": "Define the component using either a function declaration or arrow function syntax. Make sure it returns null.",
        "analogousExample": "// Example of a component that returns null\nfunction ExampleComponent() {\n  return null;\n}",
        "seedCode": "import React, { useState } from 'react';\n// Create the BreadcrumbGenerator component here",
        "expectedOutcome": "A functional component named BreadcrumbGenerator is defined and returns null.",
        "successCriteria": [
          "The BreadcrumbGenerator component is defined and returns null."
        ],
        "feedbackCorrect": "Great! You've created the main component structure.",
        "feedbackPartial": "You're close. Make sure you define a component named BreadcrumbGenerator that returns null.",
        "feedbackWrong": "The component isn't defined correctly. Check the name and that it returns null.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "BreadcrumbGenerator",
            "return",
            "null"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 9",
        "title": "Define the route tree data structure",
        "instruction": "Inside your BreadcrumbGenerator component, define a constant named 'routeTree' with a sample nested route object. The object should have properties: 'id', 'name', and 'children' (an array).",
        "hint": "Create a JavaScript object with nested objects inside the 'children' array to represent a route hierarchy.",
        "analogousExample": "// Example of a nested data structure\nconst menu = {\n  id: 'home',\n  name: 'Home',\n  children: [\n    { id: 'products', name: 'Products', children: [] },\n    { id: 'about', name: 'About Us', children: [] }\n  ]\n};",
        "seedCode": "import React, { useState } from 'react';\nimport React, { useState } from 'react';\n\n// Create the BreadcrumbGenerator component here\nconst BreadcrumbGenerator = () => {\n  // Define the routeTree constant here\n  \n  return null;\n};",
        "expectedOutcome": "A 'routeTree' constant is defined inside the component, containing a sample route tree with at least one level of nesting.",
        "successCriteria": [
          "The 'routeTree' constant exists and is an object with 'id', 'name', and 'children' properties, where 'children' is an array of similar objects."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure 'routeTree' is an object with 'id', 'name', and 'children' properties. The 'children' should be an array, even if empty.",
        "feedbackWrong": "The route tree constant is missing or incorrectly structured. It should be a JavaScript object with 'id', 'name', and 'children' properties.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "routeTree",
            "id",
            "name",
            "children"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 9",
        "title": "Initialize state for current path",
        "instruction": "Inside the BreadcrumbGenerator component, initialize a state variable called currentPath using useState. Set its default value to an empty array.",
        "hint": "Use the useState hook you imported earlier. The syntax is: const [currentPath, setCurrentPath] = useState([]);",
        "analogousExample": "// Example: state initialization\nconst [items, setItems] = useState([]);",
        "seedCode": "import React, { useState } from 'react';\n\nconst BreadcrumbGenerator = () => {\n  // Define the routeTree constant here\n  \n  // Initialize currentPath state here\n  \n  return null;\n};",
        "expectedOutcome": "The component now has a state variable currentPath initialized to an empty array, ready to track the breadcrumb path.",
        "successCriteria": [
          "currentPath state is declared using useState",
          "Default value is an empty array []"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using useState with the variable name 'currentPath' and setting its default value to an empty array [].",
        "feedbackWrong": "initialize a state variable called currentPath with useState, defaulting to an empty array.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "currentPath",
            "useState",
            "[]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 9",
        "title": "Display the route tree structure",
        "instruction": "Inside the return statement of your BreadcrumbGenerator component, add a <pre> tag that displays the routeTree constant to show the available routes for selection.",
        "hint": "Use JSX syntax with a <pre> tag and embed the routeTree constant inside curly braces to display it as formatted JSON.",
        "analogousExample": "// Displaying an object in a pre tag\nconst DisplayData = () => {\n  const data = { name: 'John', age: 30 };\n  return (\n    <pre>{JSON.stringify(data, null, 2)}</pre>\n  );\n};",
        "seedCode": "import React, { useState } from 'react';\n\nconst BreadcrumbGenerator = () => {\n  // Define the routeTree constant here\n  const routeTree = {\n    name: 'Home',\n    children: [\n      { name: 'Products', children: [\n        { name: 'Electronics', children: [] },\n        { name: 'Clothing', children: [] }\n      ]},\n      { name: 'About', children: [] }\n    ]\n  };\n  \n  // Initialize currentPath state here\n  const [currentPath, setCurrentPath] = useState([]);\n  \n  // Create findPath helper function here\n  const findPath = (node, targetName, path = []) => {\n    const newPath = [...path, node.name];\n    if (node.name === targetName) return newPath;\n    for (const child of node.children) {\n      const result = findPath(child, targetName, newPath);\n      if (result) return result;\n    }\n    return null;\n  };\n  \n  // Define handleRouteSelect function here\n  const handleRouteSelect = (routeName) => {\n    const path = findPath(routeTree, routeName);\n    if (path) setCurrentPath(path);\n  };\n  \n  return null;\n};",
        "expectedOutcome": "The routeTree data structure will be displayed in the UI inside a <pre> tag, showing the available routes in a readable format.",
        "successCriteria": [
          "A <pre> tag is added to the return statement that displays the routeTree constant using JSON.stringify for formatting."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you're using a <pre> tag and displaying the routeTree constant inside it with proper formatting.",
        "feedbackWrong": "The route tree structure isn't displayed correctly. Check that you've added a <pre> tag in the return statement and are showing the routeTree constant.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<pre>",
            "routeTree",
            "return"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 9",
        "title": "Add route selection dropdown",
        "instruction": "Inside your component's return statement, add a select dropdown element with options for each route in your route tree, and wire its onChange event to call handleRouteSelect with the selected route name.",
        "hint": "Create a select element with options for 'Home', 'Products', 'Electronics', 'Clothing', and 'About'. Use the onChange event handler to update the current path.",
        "analogousExample": "// Example: Dropdown for selecting categories\n<select onChange={(e) => handleCategorySelect(e.target.value)}>\n  <option value=\"fruits\">Fruits</option>\n  <option value=\"vegetables\">Vegetables</option>\n</select>",
        "seedCode": "import React, { useState } from 'react';\n\nconst BreadcrumbGenerator = () => {\n  // Define the routeTree constant here\n  const routeTree = {\n    name: 'Home',\n    children: [\n      { name: 'Products', children: [\n        { name: 'Electronics', children: [] },\n        { name: 'Clothing', children: [] }\n      ]},\n      { name: 'About', children: [] }\n    ]\n  };\n  \n  // Initialize currentPath state here\n  const [currentPath, setCurrentPath] = useState([]);\n  \n  // Create findPath helper function here\n  const findPath = (node, targetName, path = []) => {\n    const newPath = [...path, node.name];\n    if (node.name === targetName) return newPath;\n    for (const child of node.children) {\n      const result = findPath(child, targetName, newPath);\n      if (result) return result;\n    }\n    return null;\n  };\n  \n  // Define handleRouteSelect function here\n  const handleRouteSelect = (routeName) => {\n    const path = findPath(routeTree, routeName);\n    if (path) setCurrentPath(path);\n  };\n  \n  return (\n    <div>\n      <h1>Breadcrumb Generator</h1>\n      {/* Add route selection dropdown here */}\n    </div>\n  );\n};",
        "expectedOutcome": "A dropdown appears in the UI with options for all routes in the tree. Selecting a route updates the current path state.",
        "successCriteria": [
          "The select dropdown is present with options for each route name, and its onChange event calls handleRouteSelect with the selected value."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added a dropdown, but make sure it has options for all route names and the onChange handler properly calls handleRouteSelect with the selected value.",
        "feedbackWrong": "The dropdown is missing or not properly configured. Check that you have a select element with options for each route, and that onChange calls handleRouteSelect with the selected route name.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "select",
            "onChange",
            "handleRouteSelect",
            "option"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 9",
        "title": "Display the current breadcrumb path",
        "instruction": "Inside the component's return statement, add a <div> element below the existing <h1> that displays the currentPath array with route names separated by '>' symbols.",
        "hint": "You can use the map method on currentPath to join the route names with ' > ' separator.",
        "analogousExample": "// Displaying an array as breadcrumb\nconst path = ['Home', 'Products', 'Electronics'];\nconst breadcrumb = path.join(' > ');\n// Result: 'Home > Products > Electronics'",
        "seedCode": "import React, { useState } from 'react';\n\nconst BreadcrumbGenerator = () => {\n  // Define the routeTree constant here\n  const routeTree = {\n    name: 'Home',\n    children: [\n      { name: 'Products', children: [\n        { name: 'Electronics', children: [] },\n        { name: 'Clothing', children: [] }\n      ]},\n      { name: 'About', children: [] }\n    ]\n  };\n  \n  // Initialize currentPath state here\n  const [currentPath, setCurrentPath] = useState([]);\n  \n  // Create findPath helper function here\n  const findPath = (node, targetName, path = []) => {\n    const newPath = [...path, node.name];\n    if (node.name === targetName) return newPath;\n    for (const child of node.children) {\n      const result = findPath(child, targetName, newPath);\n      if (result) return result;\n    }\n    return null;\n  };\n  \n  // Define handleRouteSelect function here\n  const handleRouteSelect = (routeName) => {\n    const path = findPath(routeTree, routeName);\n    if (path) setCurrentPath(path);\n  };\n  \n  return (\n    <div>\n      <h1>Breadcrumb Generator</h1>\n      {/* Add breadcrumb display here */}\n    </div>\n  );\n};",
        "expectedOutcome": "The UI will show a breadcrumb trail (e.g., 'Home > Products > Electronics') when a route is selected from the dropdown.",
        "successCriteria": [
          "A <div> element displays the currentPath array with route names joined by '>' symbols."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you're displaying the currentPath array with proper formatting (route names separated by '>').",
        "feedbackWrong": "The breadcrumb isn't displaying correctly. Check that you're rendering the currentPath state and formatting it with '>' separators.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "currentPath",
            "join",
            ">",
            "div"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 9",
        "title": "Add visual styling to breadcrumb display",
        "instruction": "Add a className 'breadcrumb' to the breadcrumb display div and map through currentPath to create styled span elements with className 'crumb' for each path segment.",
        "hint": "Inside the breadcrumb display div, add className='breadcrumb' and use currentPath.map() to generate span elements with className='crumb' and key.",
        "analogousExample": "// Example: Styled list display\nconst items = ['apple', 'banana', 'cherry'];\nreturn (\n  <div className='fruit-list'>\n    {items.map((item, index) => (\n      <span key={index} className='fruit-item'>{item}</span>\n    ))}\n  </div>\n);",
        "seedCode": "import React, { useState } from 'react';\n\nconst BreadcrumbGenerator = () => {\n  // Define the routeTree constant here\n  const routeTree = {\n    name: 'Home',\n    children: [\n      { name: 'Products', children: [\n        { name: 'Electronics', children: [] },\n        { name: 'Clothing', children: [] }\n      ]},\n      { name: 'About', children: [] }\n    ]\n  };\n  \n  // Initialize currentPath state here\n  const [currentPath, setCurrentPath] = useState([]);\n  \n  // Create findPath helper function here\n  const findPath = (node, targetName, path = []) => {\n    const newPath = [...path, node.name];\n    if (node.name === targetName) return newPath;\n    for (const child of node.children) {\n      const result = findPath(child, targetName, newPath);\n      if (result) return result;\n    }\n    return null;\n  };\n  \n  // Define handleRouteSelect function here\n  const handleRouteSelect = (routeName) => {\n    const path = findPath(routeTree, routeName);\n    if (path) setCurrentPath(path);\n  };\n  \n  return (\n    <div>\n      <h1>Breadcrumb Generator</h1>\n      {/* Add breadcrumb display here */}\n    </div>\n  );\n};",
        "expectedOutcome": "The breadcrumb display div will have a className 'breadcrumb' and contain styled span elements for each segment in currentPath.",
        "successCriteria": [
          "A div with className='breadcrumb' exists and maps currentPath to span elements with className='crumb' and proper keys."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure to add className='breadcrumb' to the div and map through currentPath to create span elements with className='crumb'.",
        "feedbackWrong": "The breadcrumb display doesn't have the required styling. Add a className to the div and map through currentPath to create styled span elements.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "className='breadcrumb'",
            "currentPath.map",
            "className='crumb'",
            "span"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 9",
        "title": "Add default selection and final polish",
        "instruction": "Add a useEffect hook to set the initial breadcrumb path to 'Home' when the component first loads.",
        "hint": "Import useEffect from 'react', then add it inside your component to call handleRouteSelect with 'Home' when the component mounts.",
        "analogousExample": "// Example: useEffect for initial setup\nuseEffect(() => {\n  setInitialValue('default');\n}, []);",
        "seedCode": "import React, { useState } from 'react';\n\nconst BreadcrumbGenerator = () => {\n  // Define the routeTree constant here\n  const routeTree = {\n    name: 'Home',\n    children: [\n      { name: 'Products', children: [\n        { name: 'Electronics', children: [] },\n        { name: 'Clothing', children: [] }\n      ]},\n      { name: 'About', children: [] }\n    ]\n  };\n  \n  // Initialize currentPath state here\n  const [currentPath, setCurrentPath] = useState([]);\n  \n  // Create findPath helper function here\n  const findPath = (node, targetName, path = []) => {\n    const newPath = [...path, node.name];\n    if (node.name === targetName) return newPath;\n    for (const child of node.children) {\n      const result = findPath(child, targetName, newPath);\n      if (result) return result;\n    }\n    return null;\n  };\n  \n  // Define handleRouteSelect function here\n  const handleRouteSelect = (routeName) => {\n    const path = findPath(routeTree, routeName);\n    if (path) setCurrentPath(path);\n  };\n  \n  // Add useEffect here to set initial path\n  \n  return (\n    <div>\n      <h1>Breadcrumb Generator</h1>\n      {/* Add breadcrumb display here */}\n    </div>\n  );\n};",
        "expectedOutcome": "The component will automatically show 'Home' as the initial breadcrumb path when it first renders.",
        "successCriteria": [
          "useEffect is imported and used to call handleRouteSelect('Home') with an empty dependency array."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure useEffect is imported and calls handleRouteSelect('Home') when the component mounts.",
        "feedbackWrong": "The initial path isn't set. Add a useEffect hook that runs once to set the breadcrumb to 'Home'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useEffect",
            "handleRouteSelect",
            "'Home'",
            "[]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import React and useState",
        "id": "step1"
      },
      {
        "label": "Create the main BreadcrumbGenerator component",
        "id": "step2"
      },
      {
        "label": "Define the route tree data structure",
        "id": "step3"
      },
      {
        "label": "Initialize state for current path",
        "id": "step4"
      },
      {
        "label": "Display the route tree structure",
        "id": "step5"
      },
      {
        "label": "Add route selection dropdown",
        "id": "step6"
      },
      {
        "label": "Display the current breadcrumb path",
        "id": "step7"
      },
      {
        "label": "Add visual styling to breadcrumb display",
        "id": "step8"
      },
      {
        "label": "Add default selection and final polish",
        "id": "step9"
      }
    ]
  },
  "source": "content"
}