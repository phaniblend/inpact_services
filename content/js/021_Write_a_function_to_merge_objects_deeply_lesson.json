{
  "success": true,
  "config": {
    "lessonId": "ai-js-21",
    "track": "js",
    "lessonNum": 21,
    "title": "Write a function to merge objects deeply",
    "shortName": "P21",
    "intro": {
      "tag": "LESSON #21",
      "title": "Write a function to merge objects deeply",
      "body": "In this lesson, you will build a JavaScript function that deeply merges two objects, combining their properties even when they are nested. You will learn the core programming concept of recursion, which allows a function to call itself to handle nested structures. The main technologies involved are JavaScript objects, conditional statements, and loops for iteration. By the end, you'll understand how to create a robust utility that can merge complex data structures, a common requirement in real-world applications.",
      "usecase": "Deep object merging is essential when managing complex application state in real-world JavaScript applications. For example, when building dashboard interfaces that combine user preferences with default settings, developers need to merge configuration objects without losing nested properties. This pattern appears in form libraries that merge validation rules, e-commerce platforms that combine product variants, and content management systems that layer content overrides on base templates. Mastering deep merging helps developers handle configuration management, state updates, and data transformation tasks efficiently."
    },
    "objectives": [
      "Implement a function that accepts multiple objects as arguments",
      "Construct a recursive algorithm to handle nested object properties",
      "Manipulate object properties by merging values from source to target",
      "Apply type checking to distinguish between objects and other data types",
      "Initialize a new object to store the merged result without mutating inputs",
      "Integrate array handling logic for properties containing arrays",
      "Configure the function to handle edge cases like null values and circular references"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 9",
        "title": "Create a function skeleton for deepMerge",
        "instruction": "Create a function named deepMerge that takes two parameters (obj1 and obj2) and returns an empty object for now.",
        "hint": "You can use either a function declaration or arrow function syntax. The function should accept two parameters and return an empty object literal {}.",
        "analogousExample": "// Example of a simple function skeleton\nfunction combine(a, b) {\n  return {};\n}",
        "seedCode": "// Create your deepMerge function here\n",
        "expectedOutcome": "A function named deepMerge is created with two parameters and returns an empty object.",
        "successCriteria": [
          "Function deepMerge exists, accepts two parameters (obj1 and obj2), and returns an empty object {}."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure your function is named deepMerge, takes two parameters (obj1 and obj2), and returns an empty object {}.",
        "feedbackWrong": "Create a function named deepMerge that accepts two parameters and returns an empty object.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "deepMerge",
            "obj1",
            "obj2",
            "return",
            "{}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 9",
        "title": "Add base case for non-object values",
        "instruction": "Inside your deepMerge function, add a type check: if either obj1 or obj2 is not an object (use typeof and check for null), return obj2 (or a fallback).",
        "hint": "Use typeof to check if the value is an 'object' and also verify it's not null, since typeof null is 'object'.",
        "analogousExample": "// Example: type check for non-object values\nfunction addNumbers(a, b) {\n  if (typeof a !== 'number' || typeof b !== 'number') {\n    return 0;\n  }\n  return a + b;\n}",
        "seedCode": "// Create your deepMerge function here\nfunction deepMerge(obj1, obj2) {\n  // Add base case for non-object values here\n}",
        "expectedOutcome": "The function will handle non-object inputs by returning obj2 (or a fallback) instead of trying to merge them.",
        "successCriteria": [
          "The deepMerge function includes a type check that returns obj2 when either argument is not an object."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure to check both obj1 and obj2, and handle the case where typeof returns 'object' but the value is null.",
        "feedbackWrong": "The base case is missing or incorrect. Remember to check if either argument is not an object (using typeof and null check) and return obj2 in that case.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "typeof",
            "object",
            "null",
            "return",
            "obj2"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 9",
        "title": "Create a result object to store merged values",
        "instruction": "Inside your deepMerge function, create a new empty object variable called result to hold the merged values.",
        "hint": "You can declare a variable with let or const and assign it an empty object literal {}.",
        "analogousExample": "// Creating a result object in a function\nfunction combineData(data1, data2) {\n  const result = {};\n  // ... rest of function\n}",
        "seedCode": "// Create your deepMerge function here\nfunction deepMerge(obj1, obj2) {\n  // Add base case for non-object values here\n  \n}",
        "expectedOutcome": "The function will have a result variable initialized as an empty object, ready to store merged properties.",
        "successCriteria": [
          "A variable named result is declared and initialized as an empty object {} inside the deepMerge function."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you've declared a variable named result and assigned it an empty object {} inside the function.",
        "feedbackWrong": "Inside the deepMerge function, create a variable called result and set it to an empty object {}.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "result",
            "=",
            "{}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 9",
        "title": "Copy all properties from the first object",
        "instruction": "Inside your deepMerge function, add a for...in loop to iterate over all keys in obj1 and copy each property to the result object.",
        "hint": "Use for (let key in obj1) { result[key] = obj1[key]; } to copy properties from the first object.",
        "analogousExample": "// Example: copying properties from one object to another\nfunction copyProperties(source, target) {\n  for (let key in source) {\n    target[key] = source[key];\n  }\n}",
        "seedCode": "// Create your deepMerge function here\nfunction deepMerge(obj1, obj2) {\n  // Add base case for non-object values here\n  const result = {};\n  \n  // Copy all properties from the first object\n  \n}",
        "expectedOutcome": "The result object will contain all properties from obj1 after this step.",
        "successCriteria": [
          "A for...in loop is used to iterate over obj1 keys, and each property is assigned to result[key]."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're using a for...in loop and assigning each property to result[key].",
        "feedbackWrong": "Try using a for...in loop to iterate over obj1 keys and copy each property to the result object.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "for",
            "in",
            "obj1",
            "result",
            "key"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 9",
        "title": "Handle merging properties from the second object",
        "instruction": "Add a for...in loop to iterate through the properties of the second object (obj2). Inside the loop, check if the property key already exists in the result object and if both values (result[key] and obj2[key]) are objects. If both conditions are true, prepare for recursion by adding a comment indicating you'll handle this case later.",
        "hint": "You'll need to add another for...in loop similar to the one for obj1, but this time iterate over obj2. Inside the loop, use an if statement to check if result[key] and obj2[key] are both objects using typeof.",
        "analogousExample": "// Example: checking if two values are objects\nfor (let key in obj2) {\n  if (typeof result[key] === 'object' && typeof obj2[key] === 'object') {\n    // Both are objects - will handle merging recursively\n  }\n}",
        "seedCode": "// Create your deepMerge function here\nfunction deepMerge(obj1, obj2) {\n  // Add base case for non-object values here\n  const result = {};\n  \n  // Copy all properties from the first object\n  for (let key in obj1) {\n    result[key] = obj1[key];\n  }\n  \n  // Add for...in loop for obj2 here\n  \n}",
        "expectedOutcome": "The function will have a second loop that iterates through obj2's properties and identifies cases where both values are objects, preparing for recursive merging in the next step.",
        "successCriteria": [
          "A for...in loop for obj2 is added, with an if statement checking if both result[key] and obj2[key] are objects, and a comment indicating recursion will be handled later."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you have both the for...in loop for obj2 and the conditional check for object types.",
        "feedbackWrong": "add a for...in loop that iterates through obj2's properties and checks if both values are objects.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "for",
            "key",
            "in",
            "obj2",
            "typeof",
            "object"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 9",
        "title": "Implement recursive deep merge for nested objects",
        "instruction": "Inside the for...in loop for obj2, add an if statement that checks if both result[key] and obj2[key] are objects. If true, assign result[key] = deepMerge(result[key], obj2[key]) to merge them recursively.",
        "hint": "Use typeof to check if both values are objects and not null. Remember to call deepMerge recursively with the two nested objects.",
        "analogousExample": "// Example: recursive call for nested objects\nfunction mergeNested(objA, objB) {\n  const merged = {};\n  for (let key in objB) {\n    if (typeof merged[key] === 'object' && typeof objB[key] === 'object' && merged[key] !== null && objB[key] !== null) {\n      merged[key] = mergeNested(merged[key], objB[key]);\n    }\n  }\n  return merged;\n}",
        "seedCode": "// Create your deepMerge function here\nfunction deepMerge(obj1, obj2) {\n  // Add base case for non-object values here\n  const result = {};\n  \n  // Copy all properties from the first object\n  for (let key in obj1) {\n    result[key] = obj1[key];\n  }\n  \n  // Add for...in loop for obj2 here\n  for (let key in obj2) {\n    // Add recursive merge logic here\n  }\n  \n}",
        "expectedOutcome": "The function will now handle nested objects by merging them recursively instead of overwriting.",
        "successCriteria": [
          "The for...in loop for obj2 includes an if statement that checks if both result[key] and obj2[key] are objects, and calls deepMerge recursively when true."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your if statement checks both values are objects (and not null) before calling deepMerge recursively.",
        "feedbackWrong": "Try again. Inside the loop for obj2, you need to check if both the existing result[key] and obj2[key] are objects, then recursively call deepMerge to merge them.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "if",
            "typeof",
            "object",
            "deepMerge",
            "result[key]",
            "obj2[key]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 9",
        "title": "Return the merged result object",
        "instruction": "At the end of your deepMerge function, after both for...in loops, add a return statement to return the result object.",
        "hint": "Place the return statement after the closing brace of the second for...in loop, but before the closing brace of the function.",
        "analogousExample": "// Example: returning a result from a function\nfunction calculateSum(a, b) {\n  const sum = a + b;\n  return sum;\n}",
        "seedCode": "// Create your deepMerge function here\nfunction deepMerge(obj1, obj2) {\n  // Add base case for non-object values here\n  const result = {};\n  \n  // Copy all properties from the first object\n  for (let key in obj1) {\n    result[key] = obj1[key];\n  }\n  \n  // Add for...in loop for obj2 here\n  for (let key in obj2) {\n    // Add recursive merge logic here\n    // Handle non-object values from the second object\n  }\n  \n  // Return the merged result object here\n}",
        "expectedOutcome": "The deepMerge function will now return the merged object when called.",
        "successCriteria": [
          "The deepMerge function ends with a return statement that returns the result object."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're returning the result object at the end of the function.",
        "feedbackWrong": "The function should return the result object after processing both input objects.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "return",
            "result"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 9",
        "title": "Test the function with sample objects",
        "instruction": "Create two sample objects with nested properties and call deepMerge with them. Log the result to the console to verify the merge works.",
        "hint": "Define two objects with some overlapping and some unique properties, including nested objects. Then call deepMerge with those objects and console.log the result.",
        "analogousExample": "// Example: testing a function\nfunction add(a, b) { return a + b; }\nconst result = add(2, 3);\nconsole.log(result); // logs 5",
        "seedCode": "// Create your deepMerge function here\nfunction deepMerge(obj1, obj2) {\n  // Add base case for non-object values here\n  const result = {};\n  \n  // Copy all properties from the first object\n  for (let key in obj1) {\n    result[key] = obj1[key];\n  }\n  \n  // Add for...in loop for obj2 here\n  for (let key in obj2) {\n    // Add recursive merge logic here\n    // Handle non-object values from the second object\n  }\n  \n  // Return the merged result object here\n}\n\n// Create sample objects and test deepMerge here",
        "expectedOutcome": "The deepMerge function is called with two sample objects, and the merged result is logged to the console.",
        "successCriteria": [
          "Two sample objects are created, deepMerge is called with them, and the result is logged using console.log."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure to create two objects with nested properties, call deepMerge with them, and log the result.",
        "feedbackWrong": "The step requires creating sample objects, calling deepMerge with them, and logging the result. Please try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "deepMerge",
            "console.log"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 9",
        "title": "Add edge case handling for arrays",
        "instruction": "Modify the deepMerge function to treat arrays as non-objects that should be overwritten, not merged. Add an Array.isArray check in the base case or merge logic to handle this edge case.",
        "hint": "Check if either value is an array using Array.isArray() and return the second value directly in that case, preventing recursive merging of arrays.",
        "analogousExample": "// Example: handling arrays in a merge function\nfunction shallowMerge(obj1, obj2) {\n  const result = {};\n  for (let key in obj1) result[key] = obj1[key];\n  for (let key in obj2) {\n    if (Array.isArray(obj2[key])) {\n      result[key] = obj2[key]; // overwrite with array\n    } else {\n      result[key] = obj2[key];\n    }\n  }\n  return result;\n}",
        "seedCode": "// Create your deepMerge function here\nfunction deepMerge(obj1, obj2) {\n  // Add base case for non-object values here\n  const result = {};\n  \n  // Copy all properties from the first object\n  for (let key in obj1) {\n    result[key] = obj1[key];\n  }\n  \n  // Add for...in loop for obj2 here\n  for (let key in obj2) {\n    // Add recursive merge logic here\n    // Handle non-object values from the second object\n  }\n  \n  // Return the merged result object here\n}\n\n// Create sample objects and test deepMerge here",
        "expectedOutcome": "The deepMerge function will correctly handle arrays by overwriting them instead of attempting to merge their elements, making the function more robust for real-world use cases.",
        "successCriteria": [
          "The deepMerge function includes an Array.isArray check that treats arrays as non-objects to be overwritten, preventing recursive merging of array contents."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but the array handling isn't quite complete. Make sure you're using Array.isArray() to check if either value is an array and returning the second value directly in that case.",
        "feedbackWrong": "The array handling isn't implemented correctly. Remember to add an Array.isArray check to treat arrays as non-objects that should be overwritten rather than merged recursively.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "Array.isArray",
            "obj2",
            "result",
            "key"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Create a function skeleton for deepMerge",
        "id": "step1"
      },
      {
        "label": "Add base case for non-object values",
        "id": "step2"
      },
      {
        "label": "Create a result object to store merged values",
        "id": "step3"
      },
      {
        "label": "Copy all properties from the first object",
        "id": "step4"
      },
      {
        "label": "Handle merging properties from the second object",
        "id": "step5"
      },
      {
        "label": "Implement recursive deep merge for nested objects",
        "id": "step6"
      },
      {
        "label": "Return the merged result object",
        "id": "step7"
      },
      {
        "label": "Test the function with sample objects",
        "id": "step8"
      },
      {
        "label": "Add edge case handling for arrays",
        "id": "step9"
      }
    ]
  },
  "source": "content"
}