{
  "success": true,
  "config": {
    "lessonId": "ai-js-51",
    "track": "js",
    "lessonNum": 51,
    "title": "Build Kanban style card movement",
    "shortName": "P51",
    "intro": {
      "tag": "LESSON #51",
      "title": "Build Kanban style card movement",
      "body": "In this lesson, you will build a Kanban-style card movement system, where you can drag and drop cards between different columns. You will learn how to handle drag-and-drop events in JavaScript to create an interactive user interface. The main technologies involved include HTML for structure, CSS for styling, and JavaScript for implementing the drag-and-drop logic. By the end, you will understand how to manage state and update the DOM dynamically based on user interactions.",
      "usecase": "Kanban-style card movement is a fundamental UI pattern used in project management tools like Trello, Jira, and Asana, where developers implement drag-and-drop functionality to move tasks between columns. This pattern also appears in dashboard builders, content management systems, and design tools like Figma, where users rearrange elements visually. As a developer, you'll frequently build these interactive interfaces to help users organize workflows, prioritize items, or customize layouts. Mastering this skill enables you to create intuitive, productivity-focused applications that users interact with daily."
    },
    "objectives": [
      "Construct a Kanban board layout with multiple columns using HTML and CSS",
      "Initialize card data as JavaScript objects with properties like title, description, and status",
      "Implement a function to render cards dynamically into their corresponding columns",
      "Apply event listeners to cards to enable drag-and-drop interactions",
      "Manipulate card status when moving between columns during drag operations",
      "Update the visual state of columns to provide drop zone feedback",
      "Integrate drag-and-drop functionality to control card movement across the entire board"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 11",
        "title": "Import React and useState",
        "instruction": "Import React and the useState hook from 'react'.",
        "hint": "Use the import statement to bring in the necessary modules for building React components and managing state.",
        "analogousExample": "// Example: importing React and useState\nimport React, { useState } from 'react';",
        "seedCode": "// Import React and useState here",
        "expectedOutcome": "The React library and useState hook are imported and ready to use.",
        "successCriteria": [
          "React and useState are imported from 'react'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you import both React and useState from 'react'.",
        "feedbackWrong": "Check your import statement. You need to import React and useState from 'react'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "React",
            "useState",
            "'react'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 11",
        "title": "Create the main Kanban component",
        "instruction": "Create a function component named KanbanBoard that returns null for now.",
        "hint": "You can use either a function declaration or an arrow function. The component should be named KanbanBoard and return null.",
        "analogousExample": "// Example of a component that returns null\nfunction MyComponent() {\n  return null;\n}",
        "seedCode": "import React, { useState } from 'react';\n// Create the KanbanBoard component here",
        "expectedOutcome": "A KanbanBoard component function is created and returns null.",
        "successCriteria": [
          "A function named KanbanBoard is defined and returns null."
        ],
        "feedbackCorrect": "Great! You've created the main KanbanBoard component.",
        "feedbackPartial": "You've created a component, but make sure it's named exactly KanbanBoard and returns null.",
        "feedbackWrong": "The component isn't defined correctly. Create a function named KanbanBoard that returns null.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "KanbanBoard",
            "return",
            "null"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 11",
        "title": "Initialize state for columns",
        "instruction": "Inside your KanbanBoard component, use useState to initialize state with three columns: 'todo', 'inProgress', and 'done'. Each column should be an array that will hold card objects.",
        "hint": "Call useState with an object containing three keys, each set to an empty array.",
        "analogousExample": "// Example: initializing state with multiple values\nconst [items, setItems] = useState({\n  pending: [],\n  active: [],\n  completed: []\n});",
        "seedCode": "import React, { useState } from 'react';\n// Create the KanbanBoard component here\nconst KanbanBoard = () => {\n  // Initialize state for columns here\n\n  return null;\n};",
        "expectedOutcome": "Your component will have state ready to manage cards in three columns.",
        "successCriteria": [
          "useState is called with an object containing 'todo', 'inProgress', and 'done' keys, each set to an empty array."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're using useState with an object that has exactly three keys: 'todo', 'inProgress', and 'done', each initialized as an empty array.",
        "feedbackWrong": "The state initialization doesn't match the expected structure. Use useState with an object containing three keys: 'todo', 'inProgress', and 'done', each set to an empty array.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "useState",
            "todo",
            "inProgress",
            "done",
            "[]"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 11",
        "title": "Create the moveCard handler function",
        "instruction": "Inside your KanbanBoard component, create a function named moveCard that takes cardId and targetColumn as parameters. For now, just define the function signature and an empty body.",
        "hint": "You can use either a function declaration or arrow function syntax. The function should be defined inside the component but before the return statement.",
        "analogousExample": "// Example: a handler function with parameters\nfunction handleMove(itemId, newLocation) {\n  // Logic will go here\n}",
        "seedCode": "import React, { useState } from 'react';\n\nconst KanbanBoard = () => {\n  const [columns, setColumns] = useState(['To Do', 'In Progress', 'Done']);\n  \n  // Initialize state for cards here\n  \n  // Create the moveCard function here\n  \n  return null;\n};",
        "expectedOutcome": "A moveCard function is defined inside the KanbanBoard component with two parameters: cardId and targetColumn.",
        "successCriteria": [
          "The moveCard function is defined with the correct name and two parameters (cardId, targetColumn)."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure you've defined a function named moveCard that takes two parameters: cardId and targetColumn.",
        "feedbackWrong": "The moveCard function isn't defined correctly. Create a function named moveCard with two parameters (cardId, targetColumn) inside the KanbanBoard component.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "moveCard",
            "cardId",
            "targetColumn"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 11",
        "title": "Implement moveCard logic",
        "instruction": "Inside your moveCard function, implement the logic to update the cards state by filtering out the moved card from its current column and adding it to the target column.",
        "hint": "You'll need to use setCards with a function that receives the current cards state, then return a new array that filters out the card from its current column and adds it to the target column.",
        "analogousExample": "// Similar: updating an array of items\nconst moveItem = (itemId, fromIndex, toIndex) => {\n  setItems(prevItems => {\n    const newItems = [...prevItems];\n    const [movedItem] = newItems.splice(fromIndex, 1);\n    newItems.splice(toIndex, 0, movedItem);\n    return newItems;\n  });\n};",
        "seedCode": "import React, { useState } from 'react';\n\nconst KanbanBoard = () => {\n  const [columns, setColumns] = useState(['To Do', 'In Progress', 'Done']);\n  \n  // Initialize state for cards here\n  \n  const moveCard = (cardId, fromColumn, toColumn) => {\n    // Implement moveCard logic here\n  };\n  \n  return null;\n};",
        "expectedOutcome": "The moveCard function should properly update the cards state when called, removing the card from its current column and adding it to the target column.",
        "successCriteria": [
          "The moveCard function uses setCards with a callback to update state, filters the card from its current column, and adds it to the target column."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but check that your moveCard function properly updates the cards state. Make sure you're using setCards with a callback and correctly filtering/adding the card.",
        "feedbackWrong": "The moveCard function doesn't properly update the cards state. Review the logic needed to filter the card from its current column and add it to the target column.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "setCards",
            "prevCards",
            "filter",
            "fromColumn",
            "toColumn",
            "cardId"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 11",
        "title": "Return the main container structure",
        "instruction": "Return a div element with the className 'kanban-board' as the main container for your Kanban board.",
        "hint": "Replace the current return statement with a div that has the specified className.",
        "analogousExample": "// Returning a main container div\nfunction App() {\n  return (\n    <div className=\"app-container\">\n      {/* Content will go here */}\n    </div>\n  );\n}",
        "seedCode": "import React, { useState } from 'react';\n\nconst KanbanBoard = () => {\n  const [columns, setColumns] = useState(['To Do', 'In Progress', 'Done']);\n  \n  // Initialize state for cards here\n  \n  const moveCard = (cardId, fromColumn, toColumn) => {\n    // Implement moveCard logic here\n  };\n  \n  return null;\n};",
        "expectedOutcome": "The component now returns a div with className 'kanban-board' as the main container.",
        "successCriteria": [
          "The KanbanBoard component returns a div element with className 'kanban-board'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're returning a div element with exactly the className 'kanban-board'.",
        "feedbackWrong": "The main container structure isn't quite right. Remember to return a div with className 'kanban-board'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "return",
            "<div",
            "className=\"kanban-board\"",
            "</div>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 11",
        "title": "Map through columns to create column containers",
        "instruction": "Inside the KanbanBoard component's return statement, map through the columns state to create a div for each column. Use the map method and return a div with a className of 'column' and a key prop set to the column name.",
        "hint": "Use columns.map(column => <div key={column} className='column'></div>) inside the return statement.",
        "analogousExample": "// Similar: mapping an array to create elements\nconst fruits = ['apple', 'banana', 'orange'];\nconst fruitList = fruits.map(fruit => <div key={fruit} className='fruit'>{fruit}</div>);",
        "seedCode": "import React, { useState } from 'react';\n\nconst KanbanBoard = () => {\n  const [columns, setColumns] = useState(['To Do', 'In Progress', 'Done']);\n  \n  // Initialize state for cards here\n  \n  const moveCard = (cardId, fromColumn, toColumn) => {\n    // Implement moveCard logic here\n  };\n  \n  return (\n    <div className=\"kanban-board\">\n      {columns.map(column => (\n        <div key={column} className=\"column\">\n          {/* Column content will go here */}\n        </div>\n      ))}\n    </div>\n  );\n};",
        "expectedOutcome": "The KanbanBoard component now renders a container div with three column divs inside, each representing a column from the columns state.",
        "successCriteria": [
          "The columns state is mapped to create div elements with className='column' and proper key props."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're using the map method on the columns array and returning div elements with the correct className and key prop.",
        "feedbackWrong": "use the map method on the columns array to create a div for each column with className='column' and key={column}.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "columns.map",
            "key={column}",
            "className=\"column\""
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 11",
        "title": "Display column titles",
        "instruction": "Inside each column container, add an h3 element that displays the column name as its text content.",
        "hint": "Place the h3 element inside the column div. Use the column variable from the map function to set the text content.",
        "analogousExample": "// Similar: displaying a title in a mapped container\n{items.map(item => (\n  <div key={item.id} className=\"item\">\n    <h3>{item.name}</h3>\n  </div>\n))}",
        "seedCode": "import React, { useState } from 'react';\n\nconst KanbanBoard = () => {\n  const [columns, setColumns] = useState(['To Do', 'In Progress', 'Done']);\n  \n  // Initialize state for cards here\n  \n  const moveCard = (cardId, fromColumn, toColumn) => {\n    // Implement moveCard logic here\n  };\n  \n  return (\n    <div className=\"kanban-board\">\n      {columns.map(column => (\n        <div key={column} className=\"column\">\n          {/* Add h3 element with column title here */}\n        </div>\n      ))}\n    </div>\n  );\n};",
        "expectedOutcome": "Each column container will now display its name as a heading.",
        "successCriteria": [
          "An h3 element is added inside each column div, displaying the column name as text content."
        ],
        "feedbackCorrect": "Great! You've successfully added titles to each column.",
        "feedbackPartial": "You're on the right track. Make sure you've added an h3 element inside each column div with the column name as its content.",
        "feedbackWrong": "Check that you've added an h3 element inside the column divs with the correct column name.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "<h3>",
            "{column}",
            "</h3>"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 11",
        "title": "Map through cards to render card elements",
        "instruction": "Inside the column container, after the column title, map through the cards that belong to that column to create individual card elements.",
        "hint": "Use the filter method on the cards array to get cards for the current column, then map over them to create div elements with card content.",
        "analogousExample": "// Example: rendering items in a list\nconst items = ['Task 1', 'Task 2', 'Task 3'];\nreturn (\n  <div>\n    {items.map(item => (\n      <div key={item} className=\"item\">{item}</div>\n    ))}\n  </div>\n);",
        "seedCode": "import React, { useState } from 'react';\n\nconst KanbanBoard = () => {\n  const [columns, setColumns] = useState(['To Do', 'In Progress', 'Done']);\n  \n  // Initialize state for cards here\n  \n  const moveCard = (cardId, fromColumn, toColumn) => {\n    // Implement moveCard logic here\n  };\n  \n  return (\n    <div className=\"kanban-board\">\n      {columns.map(column => (\n        <div key={column} className=\"column\">\n          <h3>{column}</h3>\n          {/* Map through cards for this column here */}\n        </div>\n      ))}\n    </div>\n  );\n};",
        "expectedOutcome": "Each column will display its title followed by a list of card elements for that column.",
        "successCriteria": [
          "The column container includes a map over cards filtered by column, creating div elements with card content."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're filtering cards by the current column and creating proper card elements with content.",
        "feedbackWrong": "The cards aren't being rendered correctly. Check that you're filtering cards by column and mapping them to create div elements.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "filter",
            "map",
            "div",
            "className",
            "card"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 11",
        "title": "Add move buttons to each card",
        "instruction": "Inside the card mapping in your KanbanBoard component, add a button for moving cards to the next column. The button should call moveCard with the card's id, current column, and target column when clicked.",
        "hint": "You can add a button element with an onClick handler. Inside the handler, call moveCard with appropriate arguments based on the current column index.",
        "analogousExample": "// Example: Adding an interactive button to an item\n{\n  items.map(item => (\n    <div key={item.id}>\n      <p>{item.name}</p>\n      <button onClick={() => handleAction(item.id)}>Action</button>\n    </div>\n  ))\n}",
        "seedCode": "import React, { useState } from 'react';\n\nconst KanbanBoard = () => {\n  const [columns, setColumns] = useState(['To Do', 'In Progress', 'Done']);\n  \n  // Initialize state for cards here\n  \n  const moveCard = (cardId, fromColumn, toColumn) => {\n    // Implement moveCard logic here\n  };\n  \n  return (\n    <div className=\"kanban-board\">\n      {columns.map(column => (\n        <div key={column} className=\"column\">\n          <h3>{column}</h3>\n          {/* Map through cards for this column here */}\n          {/* Add move buttons to each card */}\n        </div>\n      ))}\n    </div>\n  );\n};",
        "expectedOutcome": "Each card will have a button that triggers the moveCard function with appropriate parameters when clicked.",
        "successCriteria": [
          "A button element is added inside the card mapping with an onClick handler that calls moveCard with card id, current column, and target column arguments."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added a button, but make sure it's inside the card mapping and calls moveCard with the required three arguments: card id, current column, and target column.",
        "feedbackWrong": "The move buttons aren't properly implemented. Check that you've added button elements with onClick handlers that call moveCard with the correct parameters.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "button",
            "onClick",
            "moveCard",
            "cardId",
            "fromColumn",
            "toColumn"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step11",
        "type": "question",
        "phase": "Step 11 of 11",
        "title": "Style the Kanban board with basic CSS",
        "instruction": "Add a style object to the KanbanBoard component to style the board layout, columns, and cards with basic CSS properties.",
        "hint": "Create a style object with properties like display, flexDirection, gap, border, padding, etc. Apply these styles to your JSX elements using the style attribute.",
        "analogousExample": "// Example: styling a container with flex layout\nconst styles = {\n  container: {\n    display: 'flex',\n    gap: '20px',\n    padding: '20px',\n    backgroundColor: '#f5f5f5'\n  },\n  item: {\n    border: '1px solid #ccc',\n    padding: '10px',\n    borderRadius: '4px'\n  }\n};\n\n// In JSX:\n<div style={styles.container}>\n  <div style={styles.item}>Item 1</div>\n</div>",
        "seedCode": "import React, { useState } from 'react';\n\nconst KanbanBoard = () => {\n  const [columns, setColumns] = useState(['To Do', 'In Progress', 'Done']);\n  \n  // Initialize state for cards here\n  \n  const moveCard = (cardId, fromColumn, toColumn) => {\n    // Implement moveCard logic here\n  };\n  \n  // Add style object here\n  \n  return (\n    <div className=\"kanban-board\">\n      {columns.map(column => (\n        <div key={column} className=\"column\">\n          <h3>{column}</h3>\n          {/* Map through cards for this column here */}\n          {/* Add move buttons to each card */}\n        </div>\n      ))}\n    </div>\n  );\n};",
        "expectedOutcome": "The KanbanBoard component will have a style object with CSS properties for board layout, columns, and cards, ready to be applied to the JSX elements.",
        "successCriteria": [
          "A style object is created with at least three CSS properties for board layout, columns, or cards."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added some styling, but make sure your style object includes at least three different CSS properties for the board, columns, or cards.",
        "feedbackWrong": "The style object is missing or doesn't contain enough CSS properties. Try creating an object with properties like display, flexDirection, border, padding, or margin.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "styles",
            "{",
            "}",
            "display",
            "flex",
            "border",
            "padding",
            "margin",
            "backgroundColor",
            "gap"
          ],
          "partialThreshold": 0.4,
          "correctThreshold": 0.7
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import React and useState",
        "id": "step1"
      },
      {
        "label": "Create the main Kanban component",
        "id": "step2"
      },
      {
        "label": "Initialize state for columns",
        "id": "step3"
      },
      {
        "label": "Create the moveCard handler function",
        "id": "step4"
      },
      {
        "label": "Implement moveCard logic",
        "id": "step5"
      },
      {
        "label": "Return the main container structure",
        "id": "step6"
      },
      {
        "label": "Map through columns to create column containers",
        "id": "step7"
      },
      {
        "label": "Display column titles",
        "id": "step8"
      },
      {
        "label": "Map through cards to render card elements",
        "id": "step9"
      },
      {
        "label": "Add move buttons to each card",
        "id": "step10"
      },
      {
        "label": "Style the Kanban board with basic CSS",
        "id": "step11"
      }
    ]
  },
  "source": "content"
}