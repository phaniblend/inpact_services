{
  "success": true,
  "config": {
    "lessonId": "ai-js-94",
    "track": "js",
    "lessonNum": 94,
    "title": "Build client analytics tracker queue",
    "shortName": "P94",
    "intro": {
      "tag": "LESSON #94",
      "title": "Build client analytics tracker queue",
      "body": "In this lesson, you will build a client-side analytics tracker queue, a system that collects and temporarily stores user interaction data (like clicks or page views) before sending it to a server. You will learn how to implement a queue data structure using JavaScript arrays to manage these events in order. The core programming concepts involved include array methods, event handling, and asynchronous operations. By the end, you will have a functional queue that can add events, process them, and handle errors, all within a single JavaScript file.",
      "usecase": "In modern web applications, analytics tracking queues are essential for collecting user behavior data without blocking the main thread. You'll see this pattern in e-commerce sites like Amazon, where product views and cart additions are tracked while maintaining smooth page interactions. Social platforms like Facebook use similar queues to record post engagements and ad clicks without disrupting the user experience. This approach ensures reliable data collection while keeping applications responsive during high-traffic periods."
    },
    "objectives": [
      "Implement a queue data structure for storing analytics events",
      "Initialize a tracker object with methods for managing the queue",
      "Apply event handling to capture user interactions for analytics",
      "Construct a function to process and send queued analytics data",
      "Configure batch processing logic to optimize network requests",
      "Integrate the analytics tracker into a simple web application",
      "Manipulate queue operations to add, remove, and clear events"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 11",
        "title": "Create the analytics queue array",
        "instruction": "Create a variable called 'analyticsQueue' and set it to an empty array.",
        "hint": "Use the 'const' keyword to declare a variable named 'analyticsQueue' and assign it an empty array using square brackets [].",
        "analogousExample": "// Similar: creating an empty array for a shopping cart\nconst shoppingCart = [];",
        "seedCode": "// Create your analytics queue array here\n",
        "expectedOutcome": "You will have a variable named 'analyticsQueue' initialized as an empty array, ready to store analytics events.",
        "successCriteria": [
          "The variable 'analyticsQueue' is declared and set to an empty array []."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you've created a variable named 'analyticsQueue' and set it to an empty array [].",
        "feedbackWrong": "Create a variable called 'analyticsQueue' and assign it an empty array using square brackets [].",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "analyticsQueue",
            "[]",
            "="
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 11",
        "title": "Create the addToQueue function",
        "instruction": "Create a function named 'addToQueue' that takes an 'event' parameter and pushes it to the 'analyticsQueue' array.",
        "hint": "Define a function that accepts an event parameter and uses the .push() method to add it to the array.",
        "analogousExample": "// Similar: function that adds items to an array\nfunction addItem(item) {\n  shoppingCart.push(item);\n}",
        "seedCode": "// Create your analytics queue array here\n\n// Create the addToQueue function here",
        "expectedOutcome": "A function named addToQueue is defined that can add events to the analytics queue.",
        "successCriteria": [
          "The addToQueue function is defined, takes an event parameter, and pushes it to the analyticsQueue array."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure your function is named 'addToQueue', takes an 'event' parameter, and uses .push() to add it to the analyticsQueue array.",
        "feedbackWrong": "The function isn't quite right. Remember to create a function named 'addToQueue' that accepts an event parameter and pushes it to the analyticsQueue array.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "addToQueue",
            "event",
            "push",
            "analyticsQueue"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 11",
        "title": "Create the processQueue function",
        "instruction": "Create a function named 'processQueue' that will process events from the analyticsQueue array. The function should log each event to the console.",
        "hint": "Define a function that loops through the analyticsQueue array and logs each event.",
        "analogousExample": "// Example: a function that processes items\nfunction processItems(items) {\n  items.forEach(item => {\n    console.log(item);\n  });\n}",
        "seedCode": "// Create your analytics queue array here\n\n// Create the addToQueue function here\n\n// Create the processQueue function here",
        "expectedOutcome": "A function named processQueue is defined that will iterate through the analyticsQueue array and log each event.",
        "successCriteria": [
          "The processQueue function is defined and logs events from the analyticsQueue array."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created a function, but make sure it's named 'processQueue' and logs events from the analyticsQueue array.",
        "feedbackWrong": "The processQueue function is not defined correctly. Check the function name and its logic.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "processQueue",
            "analyticsQueue",
            "forEach",
            "console.log"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 11",
        "title": "Implement queue processing logic",
        "instruction": "Inside the processQueue function, add a while loop that processes events from the analyticsQueue array.",
        "hint": "Use a while loop that continues as long as there are events in the queue. Inside the loop, you can remove events from the front of the array.",
        "analogousExample": "// Example: while loop processing an array\nfunction processItems(items) {\n  while (items.length > 0) {\n    const item = items.shift();\n    console.log('Processing:', item);\n  }\n}",
        "seedCode": "// Create your analytics queue array here\n\n// Create the addToQueue function here\n\n// Create the processQueue function here\n\nfunction processQueue() {\n  // Add while loop here\n}",
        "expectedOutcome": "The processQueue function will have a while loop that can process events from the analyticsQueue array.",
        "successCriteria": [
          "The processQueue function contains a while loop that checks if analyticsQueue has events to process."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're adding a while loop inside the processQueue function.",
        "feedbackWrong": "Try again. You need to add a while loop inside the processQueue function to process events from the analyticsQueue array.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "while",
            "analyticsQueue",
            "length"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 11",
        "title": "Add mock sendEvent function",
        "instruction": "Create a function called 'sendEvent' that logs events to the console. This function will simulate sending analytics events.",
        "hint": "Define a function named sendEvent that takes an event parameter and uses console.log to output it.",
        "analogousExample": "// Example of a simple logging function\nfunction logMessage(message) {\n  console.log(message);\n}",
        "seedCode": "// Create your analytics queue array here\n\n// Create the addToQueue function here\n\n// Create the processQueue function here\n\nfunction processQueue() {\n  // Add while loop here\n}\n\n// Create the sendEvent function here",
        "expectedOutcome": "You will have a sendEvent function that logs events to the console.",
        "successCriteria": [
          "A function named sendEvent is defined and logs events using console.log."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you've defined a function named sendEvent that logs events to the console.",
        "feedbackWrong": "The sendEvent function is not defined or doesn't log events. Create a function named sendEvent that logs events to the console.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "sendEvent",
            "console.log"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 11",
        "title": "Integrate sendEvent into processQueue",
        "instruction": "Inside the while loop in your processQueue function, call the sendEvent function with the current event from the queue and",
        "hint": "Within the loop, you can access the first event in the queue, pass it to sendEvent, and then use shift() to remove it.",
        "analogousExample": "// Example: processing items from an array\nfunction processItems(items) {\n  while (items.length > 0) {\n    const item = items[0];\n    logItem(item);\n    items.shift();\n  }\n}",
        "seedCode": "// Create your analytics queue array here\n\n// Create the addToQueue function here\n\n// Create the processQueue function here\n\nfunction processQueue() {\n  // Add while loop here\n}\n\n// Create the sendEvent function here",
        "expectedOutcome": "The processQueue function will now send each event in the queue and remove it after sending.",
        "successCriteria": [
          "The while loop in processQueue calls sendEvent with the first queue event and removes that event using shift()."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure to both call sendEvent with the event and remove it from the queue inside the loop.",
        "feedbackWrong": "The loop should call sendEvent with the current event and then remove that event from the queue. Review the example.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "sendEvent",
            "shift()"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 11",
        "title": "remove that event from the queue",
        "instruction": "remove that event from the queue.",
        "hint": "Within the loop, you can access the first event in the queue, pass it to sendEvent, and then use shift() to remove it.",
        "analogousExample": "// Example: processing items from an array\nfunction processItems(items) {\n  while (items.length > 0) {\n    const item = items[0];\n    logItem(item);\n    items.shift();\n  }\n}",
        "seedCode": "// Create your analytics queue array here\n\n// Create the addToQueue function here\n\n// Create the processQueue function here\n\nfunction processQueue() {\n  // Add while loop here\n}\n\n// Create the sendEvent function here",
        "expectedOutcome": "",
        "successCriteria": [
          "Complete the step as instructed."
        ],
        "feedbackCorrect": "Great! Step completed.",
        "feedbackPartial": "Review the instruction and try again.",
        "feedbackWrong": "Check the instruction and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "sendEvent",
            "shift()"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 11",
        "title": "Create the startProcessing function",
        "instruction": "Create a function named 'startProcessing' that will set up an interval to call 'processQueue' every 2 seconds.",
        "hint": "Use setInterval to repeatedly call processQueue. The interval should run every 2000 milliseconds (2 seconds).",
        "analogousExample": "// Example: a function that starts an interval\nfunction startTimer() {\n  setInterval(() => {\n    console.log('Tick');\n  }, 1000);\n}",
        "seedCode": "// Create your analytics queue array here\n\n// Create the addToQueue function here\n\n// Create the processQueue function here\n\nfunction processQueue() {\n  // Add while loop here\n}\n\n// Create the sendEvent function here\n\n// Create the startProcessing function here",
        "expectedOutcome": "A new function named 'startProcessing' is defined that uses setInterval to call 'processQueue' every 2 seconds.",
        "successCriteria": [
          "The 'startProcessing' function is defined and uses setInterval to call 'processQueue' with a 2000ms interval."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created a function, but check that it's named 'startProcessing' and uses setInterval to call 'processQueue' every 2000ms.",
        "feedbackWrong": "The function wasn't created correctly. Make sure to define a function named 'startProcessing' that sets up an interval to call 'processQueue' every 2 seconds.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "startProcessing",
            "setInterval",
            "processQueue",
            "2000"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 11",
        "title": "Implement interval-based processing",
        "instruction": "Inside the startProcessing function, use setInterval to call processQueue every 5 seconds (5000 milliseconds).",
        "hint": "setInterval takes a function to call and a delay in milliseconds. You'll need to store the interval ID in a variable.",
        "analogousExample": "// Similar: interval for periodic task\nfunction startUpdates() {\n  const intervalId = setInterval(() => {\n    console.log('Updating...');\n  }, 2000);\n}",
        "seedCode": "// Create your analytics queue array here\n\n// Create the addToQueue function here\n\n// Create the processQueue function here\n\nfunction processQueue() {\n  // Add while loop here\n}\n\n// Create the sendEvent function here\n\n// Create the startProcessing function here\n\nfunction startProcessing() {\n  // Use setInterval here to call processQueue every 5 seconds\n}",
        "expectedOutcome": "The startProcessing function will now automatically process the analytics queue every 5 seconds when called.",
        "successCriteria": [
          "The startProcessing function contains setInterval that calls processQueue with a 5000ms delay."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but check that you're calling processQueue inside setInterval and using the correct delay (5000ms).",
        "feedbackWrong": "Try again. Inside startProcessing, use setInterval to call processQueue every 5 seconds (5000 milliseconds).",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "setInterval",
            "processQueue",
            "5000"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 11",
        "title": "Create the stopProcessing function",
        "instruction": "Create a function called 'stopProcessing' that clears the processing interval by using clearInterval.",
        "hint": "You'll need to store the interval ID returned by setInterval in a variable that stopProcessing can access.",
        "analogousExample": "// Example: a function that stops a timer\nlet timerId;\n\nfunction stopTimer() {\n  clearInterval(timerId);\n}",
        "seedCode": "// Create your analytics queue array here\n\n// Create the addToQueue function here\n\n// Create the processQueue function here\n\nfunction processQueue() {\n  // Add while loop here\n}\n\n// Create the sendEvent function here\n\n// Create the startProcessing function here\n\nfunction startProcessing() {\n  // Use setInterval here to call processQueue every 5 seconds\n}\n\n// Create the stopProcessing function here",
        "expectedOutcome": "A stopProcessing function that will clear the interval when called.",
        "successCriteria": [
          "The stopProcessing function is defined and uses clearInterval."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure your function is named 'stopProcessing' and uses clearInterval.",
        "feedbackWrong": "The stopProcessing function is missing or incorrect. It should clear the processing interval.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "stopProcessing",
            "clearInterval"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step11",
        "type": "question",
        "phase": "Step 11 of 11",
        "title": "Export the queue functions",
        "instruction": "Export the addToQueue, startProcessing, and stopProcessing functions so they can be used in other modules.",
        "hint": "Use the export keyword before each function declaration, or use a single export statement at the end.",
        "analogousExample": "// Exporting functions\nfunction add(a, b) { return a + b; }\nfunction subtract(a, b) { return a - b; }\nexport { add, subtract };",
        "seedCode": "// Create your analytics queue array here\n\n// Create the addToQueue function here\n\n// Create the processQueue function here\n\nfunction processQueue() {\n  // Add while loop here\n}\n\n// Create the sendEvent function here\n\n// Create the startProcessing function here\n\nfunction startProcessing() {\n  // Use setInterval here to call processQueue every 5 seconds\n}\n\n// Create the stopProcessing function here\n\n// Export the functions here",
        "expectedOutcome": "The addToQueue, startProcessing, and stopProcessing functions are exported and ready to be imported elsewhere.",
        "successCriteria": [
          "addToQueue, startProcessing, and stopProcessing are exported."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've exported some functions, but make sure to export all three: addToQueue, startProcessing, and stopProcessing.",
        "feedbackWrong": "The functions are not exported correctly. Use the export keyword to make them available outside this module.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "export",
            "addToQueue",
            "startProcessing",
            "stopProcessing"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Create the analytics queue array",
        "id": "step1"
      },
      {
        "label": "Create the addToQueue function",
        "id": "step2"
      },
      {
        "label": "Create the processQueue function",
        "id": "step3"
      },
      {
        "label": "Implement queue processing logic",
        "id": "step4"
      },
      {
        "label": "Add mock sendEvent function",
        "id": "step5"
      },
      {
        "label": "Integrate sendEvent into processQueue",
        "id": "step6"
      },
      {
        "label": "remove that event from the queue",
        "id": "step7"
      },
      {
        "label": "Create the startProcessing function",
        "id": "step8"
      },
      {
        "label": "Implement interval-based processing",
        "id": "step9"
      },
      {
        "label": "Create the stopProcessing function",
        "id": "step10"
      },
      {
        "label": "Export the queue functions",
        "id": "step11"
      }
    ]
  },
  "source": "content"
}