{
  "success": true,
  "config": {
    "lessonId": "ai-js-33",
    "track": "js",
    "lessonNum": 33,
    "title": "Write a custom bind polyfill",
    "shortName": "P33",
    "intro": {
      "tag": "LESSON #33",
      "title": "Write a custom bind polyfill",
      "body": "In this lesson, you will build your own version of the JavaScript `bind` method, called a polyfill, which allows a function to be called with a specific `this` context and preset arguments. You will learn the core concept of function context (`this`) and how to manipulate it using `call` or `apply`. The main technologies involved are JavaScript functions, the `arguments` object, and closure to capture the bound context and arguments. By the end, you'll understand how `bind` works internally and be able to implement it from scratch, enhancing your grasp of advanced JavaScript function methods.",
      "usecase": "The bind method is essential in real-world JavaScript applications for managing function context, especially in UI components and event-driven architectures. For example, in a dashboard with interactive charts, you might bind a specific chart instance's method to a click handler to ensure it updates the correct data set when a user selects a filter. Similarly, in a form with dynamic validation, binding validation functions to specific input fields ensures they access the correct field state when triggered by events like blur or change. Understanding how to implement a custom bind polyfill helps developers maintain control over function execution in legacy browsers or custom frameworks, ensuring consistent behavior across diverse environments."
    },
    "objectives": [
      "Implement a custom function that replicates the core behavior of the native bind method.",
      "Construct a polyfill that returns a new bound function.",
      "Apply the custom bind to preserve the original function's context.",
      "Manipulate arguments to support partial application in your polyfill.",
      "Integrate prototype inheritance to attach your polyfill to the Function object.",
      "Configure the bound function to handle the new operator correctly.",
      "Initialize and test your polyfill with various function and context scenarios."
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 13",
        "title": "Create a test function to demonstrate bind behavior",
        "instruction": "Create a simple function named 'greet' that takes two parameters: 'name' and 'age'. Inside the function, log a greeting message to the console that includes both parameters.",
        "hint": "Define a function with two parameters and use console.log() to output a message containing those parameters.",
        "analogousExample": "// Example: a function that logs a message\nfunction introduce(person, city) {\n    console.log(`Hi, I'm ${person} from ${city}.`);\n}",
        "seedCode": "// Create your greet function here\n",
        "expectedOutcome": "You'll have a function that can be used to test how bind works by creating new functions with preset arguments.",
        "successCriteria": [
          "A function named 'greet' is defined with two parameters and logs a message containing both parameters."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close! Make sure your function has two parameters and logs a message containing both.",
        "feedbackWrong": "Check that you've created a function named 'greet' with two parameters that logs a message using both parameters.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "function",
            "greet",
            "name",
            "age",
            "console.log"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 13",
        "title": "Create an object to serve as the 'this' context",
        "instruction": "Create an object named 'person' with a 'name' property set to a string value (e.g., 'Alice').",
        "hint": "Define a plain JavaScript object with a property that your test function can reference via 'this'.",
        "analogousExample": "// Example: an object with a property\nconst car = {\n  brand: 'Toyota'\n};",
        "seedCode": "// Create your greet function here\n\n// Create your person object here",
        "expectedOutcome": "You'll have an object that can be used as the 'this' context when testing your bind polyfill.",
        "successCriteria": [
          "An object named 'person' exists with a 'name' property."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created an object, but make sure it has a 'name' property.",
        "feedbackWrong": "Try creating an object with a 'name' property to use as the 'this' context.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "person",
            "name"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 13",
        "title": "Test native bind method to understand expected beh",
        "instruction": "Call the native bind method on your greet function with person as the 'this' context,",
        "hint": "Use the bind method on your function and store the result in a variable, then call that variable.",
        "analogousExample": "// Example: binding a function to an object\nfunction sayHello() {\n  console.log(`Hello, ${this.name}`);\n}\n\nconst user = { name: 'Alice' };\nconst boundHello = sayHello.bind(user);\nboundHello();",
        "seedCode": "// Create your greet function here\n\n// Create your person object here\n\n// Call bind on greet with person as context, then execute",
        "expectedOutcome": "You'll see the bound function execute with the correct 'this' context, demonstrating what your custom bind should replicate.",
        "successCriteria": [
          "The native bind method is called on greet with person as the context, and the bound function is executed."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you both bind the function to the person object and then call the resulting bound function.",
        "feedbackWrong": "call .bind() on your greet function, passing person as the argument, then execute the returned function.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "bind",
            "person",
            "()"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 13",
        "title": "execute the bound function",
        "instruction": "execute the bound function.",
        "hint": "Use the bind method on your function and store the result in a variable, then call that variable.",
        "analogousExample": "// Example: binding a function to an object\nfunction sayHello() {\n  console.log(`Hello, ${this.name}`);\n}\n\nconst user = { name: 'Alice' };\nconst boundHello = sayHello.bind(user);\nboundHello();",
        "seedCode": "// Create your greet function here\n\n// Create your person object here\n\n// Call bind on greet with person as context, then execute",
        "expectedOutcome": "",
        "successCriteria": [
          "Complete the step as instructed."
        ],
        "feedbackCorrect": "Great! Step completed.",
        "feedbackPartial": "Review the instruction and try again.",
        "feedbackWrong": "Check the instruction and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "bind",
            "person",
            "()"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 13",
        "title": "Start implementing customBind function",
        "instruction": "Define a function named customBind that takes one parameter called context. The function should return a new function that will later be called.",
        "hint": "Start with function customBind(context) { return function() { } } or use arrow functions. The inner function doesn't need to do anything yet.",
        "analogousExample": "// Example: a function that returns another function\nfunction createGreeter(name) {\n  return function() {\n    console.log('Hello ' + name);\n  };\n}",
        "seedCode": "// Create your greet function here\n\n// Create your person object here\n\n// Call bind on greet with person as context, then execute\n\n// Define customBind function here\n",
        "expectedOutcome": "A customBind function is defined with the correct structure: it takes a context parameter and returns a new function.",
        "successCriteria": [
          "The customBind function is defined with one parameter named context, and it returns a new function (even if empty)."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure customBind takes exactly one parameter (context) and returns a function. Check your syntax.",
        "feedbackWrong": "The customBind function isn't defined correctly. It should take one parameter (context) and return a new function.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "customBind",
            "context",
            "return",
            "function"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 13",
        "title": "Capture initial arguments in customBind",
        "instruction": "Inside your customBind function, capture the arguments passed after the context parameter and store them in a variable called 'boundArgs'.",
        "hint": "You can use the rest parameter syntax (...args) to collect all arguments after the first one, or use Array.from(arguments).slice(1).",
        "analogousExample": "// Example of capturing arguments after the first\nfunction customBind(context, ...boundArgs) {\n  // boundArgs now contains all arguments after context\n}",
        "seedCode": "// Create your greet function here\n\n// Create your person object here\n\n// Call bind on greet with person as context, then execute\n\n// Define customBind function here\nfunction customBind(context) {\n  // Capture arguments after context here\n}",
        "expectedOutcome": "The customBind function will store the initial arguments passed during binding for later use when the bound function is called.",
        "successCriteria": [
          "The customBind function has a variable 'boundArgs' that contains an array of arguments passed after the context parameter."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're capturing ALL arguments after the context parameter. Check if you're using the rest parameter syntax or properly slicing the arguments array.",
        "feedbackWrong": "The customBind function doesn't seem to capture the initial arguments correctly. Remember you need to store the arguments passed after the context parameter in a variable called 'boundArgs'.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "boundArgs",
            "arguments",
            "context"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 13",
        "title": "Return a bound function from customBind",
        "instruction": "Inside your customBind function, return a new function that will be called later.",
        "hint": "The function you return should be able to accept arguments when it's eventually invoked.",
        "analogousExample": "// Example of a function returning another function\nfunction createGreeter(greeting) {\n  return function(name) {\n    return `${greeting}, ${name}!`;\n  }\n}",
        "seedCode": "// Create your greet function here\n\n// Create your person object here\n\n// Call bind on greet with person as context, then execute\n\n// Define customBind function here\nfunction customBind(context) {\n  // Capture arguments after context here\n  \n  // Return a new function here\n}",
        "expectedOutcome": "The customBind function now returns a new function instead of executing immediately.",
        "successCriteria": [
          "customBind returns a function when called."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure customBind returns a function declaration or arrow function.",
        "feedbackWrong": "Check that customBind returns a function. Remember bind doesn't execute immediately—it returns a callable function.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "return",
            "function"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 13",
        "title": "Implement 'this' context binding in returned function",
        "instruction": "Inside the returned function in customBind, call the original function with the stored 'this' context using the apply method.",
        "hint": "Use apply on the original function (which you have access to via closure) with the stored context as the first argument.",
        "analogousExample": "// Example: calling a function with a specific context\nfunction original() {}\nfunction wrapper(context) {\n  return function() {\n    original.apply(context);\n  };\n}",
        "seedCode": "// Create your greet function here\n\n// Create your person object here\n\n// Call bind on greet with person as context, then execute\n\n// Define customBind function here\nfunction customBind(context) {\n  // Capture arguments after context here\n  \n  // Return a new function here\n  return function() {\n    // Call the original function with the stored context using apply\n  };\n}",
        "expectedOutcome": "The returned function will properly call the original function with the bound 'this' context when invoked.",
        "successCriteria": [
          "The returned function uses apply to call the original function with the stored context."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're using apply to call the original function with the stored context.",
        "feedbackWrong": "The returned function should call the original function with the stored context using apply.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "apply",
            "context"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 13",
        "title": "Combine initial and call-time arguments",
        "instruction": "Inside the returned function of customBind, combine the stored initial arguments (args) with the arguments passed when calling the bound function (arguments).",
        "hint": "You can use the spread operator to combine arrays: [...args, ...arguments].",
        "analogousExample": "// Example: combining arguments in a function\nfunction combineArgs(initialArgs) {\n  return function(...callArgs) {\n    return [...initialArgs, ...callArgs];\n  };\n}",
        "seedCode": "// Create your greet function here\n\n// Create your person object here\n\n// Call bind on greet with person as context, then execute\n\n// Define customBind function here\nfunction customBind(context) {\n  // Capture arguments after context here\n  \n  // Return a new function here\n  return function() {\n    // Combine stored args with arguments passed to this function\n    // Call the original function with the stored context using apply\n  };\n}",
        "expectedOutcome": "The bound function will correctly receive both the arguments passed during binding and those passed during invocation.",
        "successCriteria": [
          "The returned function combines the stored initial arguments with the call-time arguments."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you're combining both sets of arguments inside the returned function.",
        "feedbackWrong": "The arguments aren't being combined correctly. Remember to merge the stored args with the arguments passed to the bound function.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "args",
            "arguments",
            "spread",
            "apply",
            "call"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 13",
        "title": "Attach customBind to Function.prototype",
        "instruction": "Attach your customBind function to Function.prototype so it can be called as a method on any function.",
        "hint": "Assign customBind to Function.prototype.customBind. This makes it available on all functions, similar to the native bind method.",
        "analogousExample": "// Adding a method to all arrays\nArray.prototype.customMethod = function() {\n  return this.length * 2;\n};\n\n// Now any array can call customMethod\nconst arr = [1, 2, 3];\nconsole.log(arr.customMethod()); // Outputs 6",
        "seedCode": "// Create your greet function here\n\n// Create your person object here\n\n// Call bind on greet with person as context, then execute\n\n// Define customBind function here\nfunction customBind(context) {\n  // Capture arguments after context here\n  \n  // Return a new function here\n  return function() {\n    // Check if called with new operator here\n    \n    // Combine stored args with arguments passed to this function\n    // Call the original function with the stored context using apply\n  };\n}\n\n// Attach customBind to Function.prototype here",
        "expectedOutcome": "Your customBind function will be available as a method on all functions, allowing you to call it like: someFunction.customBind(context).",
        "successCriteria": [
          "Function.prototype.customBind is assigned and references your customBind function."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're assigning to Function.prototype.customBind, not just declaring a separate function.",
        "feedbackWrong": "Try again. You need to assign your customBind function to Function.prototype.customBind so it becomes a method available on all functions.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "Function.prototype.customBind",
            "=",
            "customBind"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step11",
        "type": "question",
        "phase": "Step 11 of 13",
        "title": "Test customBind with basic usage",
        "instruction": "Test your customBind implementation by calling it on the greet function with person as context,",
        "hint": "Use the same syntax you used for native bind: greet.customBind(person)('Hello')",
        "analogousExample": "// Testing a bound function\nconst boundFunc = originalFunc.customBind(context);\nconst result = boundFunc('arg1', 'arg2');",
        "seedCode": "// Create your greet function here\nfunction greet(greeting, name) {\n  return `${greeting}, ${name}! I'm ${this.firstName} ${this.lastName}.`;\n}\n\n// Create your person object here\nconst person = {\n  firstName: 'John',\n  lastName: 'Doe'\n};\n\n// Call bind on greet with person as context, then execute\nconst nativeBound = greet.bind(person);\nconsole.log(nativeBound('Hello', 'World'));\n\n// Define customBind function here\nfunction customBind(context) {\n  const fn = this;\n  const args = Array.prototype.slice.call(arguments, 1);\n  \n  return function() {\n    const callArgs = args.concat(Array.prototype.slice.call(arguments));\n    \n    if (this instanceof boundFn) {\n      return new fn(...callArgs);\n    }\n    \n    return fn.apply(context, callArgs);\n  };\n}\n\n// Attach customBind to Function.prototype here\nFunction.prototype.customBind = customBind;\n\n// Test customBind with basic usage here",
        "expectedOutcome": "You should see the same output from both native bind and customBind when called with the same arguments.",
        "successCriteria": [
          "customBind is called on greet with person as context, and the bound function is executed with arguments that produce the expected greeting string."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've started testing, but make sure you're calling customBind on the greet function and executing it with arguments.",
        "feedbackWrong": "The test isn't complete. You need to call greet.customBind(person) and then execute the returned function with arguments.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "greet.customBind",
            "person",
            "(",
            ")"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step12",
        "type": "question",
        "phase": "Step 12 of 13",
        "title": "execute the bound function with arguments",
        "instruction": "execute the bound function with arguments.",
        "hint": "Use the same syntax you used for native bind: greet.customBind(person)('Hello')",
        "analogousExample": "// Testing a bound function\nconst boundFunc = originalFunc.customBind(context);\nconst result = boundFunc('arg1', 'arg2');",
        "seedCode": "// Create your greet function here\nfunction greet(greeting, name) {\n  return `${greeting}, ${name}! I'm ${this.firstName} ${this.lastName}.`;\n}\n\n// Create your person object here\nconst person = {\n  firstName: 'John',\n  lastName: 'Doe'\n};\n\n// Call bind on greet with person as context, then execute\nconst nativeBound = greet.bind(person);\nconsole.log(nativeBound('Hello', 'World'));\n\n// Define customBind function here\nfunction customBind(context) {\n  const fn = this;\n  const args = Array.prototype.slice.call(arguments, 1);\n  \n  return function() {\n    const callArgs = args.concat(Array.prototype.slice.call(arguments));\n    \n    if (this instanceof boundFn) {\n      return new fn(...callArgs);\n    }\n    \n    return fn.apply(context, callArgs);\n  };\n}\n\n// Attach customBind to Function.prototype here\nFunction.prototype.customBind = customBind;\n\n// Test customBind with basic usage here",
        "expectedOutcome": "",
        "successCriteria": [
          "Complete the step as instructed."
        ],
        "feedbackCorrect": "Great! Step completed.",
        "feedbackPartial": "Review the instruction and try again.",
        "feedbackWrong": "Check the instruction and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "greet.customBind",
            "person",
            "(",
            ")"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step13",
        "type": "question",
        "phase": "Step 13 of 13",
        "title": "Test customBind with partial arguments and new operator",
        "instruction": "Add test cases to verify customBind works with partial argument application and the new operator.",
        "hint": "Call customBind with some arguments pre-filled, then call the bound function with remaining arguments. Also test using 'new' with a bound constructor function.",
        "analogousExample": "// Testing partial arguments\nconst add = (a, b) => a + b;\nconst addFive = add.customBind(null, 5);\nconsole.log(addFive(10)); // 15\n\n// Testing new operator\nfunction Person(name) { this.name = name; }\nconst BoundPerson = Person.customBind(null);\nconst instance = new BoundPerson('Alice');\nconsole.log(instance.name); // 'Alice'",
        "seedCode": "// Create your greet function here\nfunction greet(greeting, name) {\n  return `${greeting}, ${name}! I'm ${this.firstName} ${this.lastName}.`;\n}\n\n// Create your person object here\nconst person = {\n  firstName: 'John',\n  lastName: 'Doe'\n};\n\n// Call bind on greet with person as context, then execute\nconst nativeBound = greet.bind(person);\nconsole.log(nativeBound('Hello', 'World'));\n\n// Define customBind function here\nfunction customBind(context) {\n  const fn = this;\n  const args = Array.prototype.slice.call(arguments, 1);\n  \n  return function() {\n    const callArgs = args.concat(Array.prototype.slice.call(arguments));\n    \n    if (this instanceof boundFn) {\n      return new fn(...callArgs);\n    }\n    \n    return fn.apply(context, callArgs);\n  };\n}\n\n// Attach customBind to Function.prototype here\nFunction.prototype.customBind = customBind;\n\n// Test customBind with basic usage here\n\n// Add test cases for partial arguments and new operator here",
        "expectedOutcome": "The customBind polyfill correctly handles partial argument application and constructor invocation with the new operator.",
        "successCriteria": [
          "Test cases are added that demonstrate partial argument binding and new operator usage with customBind."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added some tests, but ensure you cover both partial argument application and the new operator scenario.",
        "feedbackWrong": "The test cases are missing or incorrect. Review how native bind handles partial arguments and constructor calls, then implement similar tests for customBind.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "customBind",
            "partial",
            "arguments",
            "new",
            "test"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Create a test function to demonstrate bind behavior",
        "id": "step1"
      },
      {
        "label": "Create an object to serve as the 'this' context",
        "id": "step2"
      },
      {
        "label": "Test native bind method to understand expected beh",
        "id": "step3"
      },
      {
        "label": "execute the bound function",
        "id": "step4"
      },
      {
        "label": "Start implementing customBind function",
        "id": "step5"
      },
      {
        "label": "Capture initial arguments in customBind",
        "id": "step6"
      },
      {
        "label": "Return a bound function from customBind",
        "id": "step7"
      },
      {
        "label": "Implement 'this' context binding in returned function",
        "id": "step8"
      },
      {
        "label": "Combine initial and call-time arguments",
        "id": "step9"
      },
      {
        "label": "Attach customBind to Function.prototype",
        "id": "step10"
      },
      {
        "label": "Test customBind with basic usage",
        "id": "step11"
      },
      {
        "label": "execute the bound function with arguments",
        "id": "step12"
      },
      {
        "label": "Test customBind with partial arguments and new operator",
        "id": "step13"
      }
    ]
  },
  "source": "content"
}