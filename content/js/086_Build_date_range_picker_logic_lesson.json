{
  "success": true,
  "config": {
    "lessonId": "ai-js-86",
    "track": "js",
    "lessonNum": 86,
    "title": "Build date range picker logic",
    "shortName": "P86",
    "intro": {
      "tag": "LESSON #86",
      "title": "Build date range picker logic",
      "body": "In this lesson, you will build the logic for a date range picker, a common UI component that lets users select a start and end date. You will learn to manage two date values in state and ensure the end date is never earlier than the start date. The core programming concepts involve using JavaScript's Date object and conditional logic to validate the date range. You'll work with state variables, event handlers, and basic date comparisons to create a functional and user-friendly component.",
      "usecase": "Date range pickers are essential UI components in modern web applications, particularly in data-heavy interfaces like analytics dashboards, financial platforms, and e-commerce systems. Developers implement them in reporting tools to filter time-series data, in booking systems for selecting travel dates, and in admin panels for filtering logs or transactions. These components appear in forms, filter panels, and modal dialogs where users need to specify start and end dates for queries. Mastering this logic enables you to build intuitive date selection experiences that are fundamental to data exploration and user workflows."
    },
    "objectives": [
      "Construct a function to initialize date range state with start and end dates",
      "Implement validation logic to ensure the start date precedes the end date",
      "Apply date manipulation methods to calculate the duration between selected dates",
      "Configure event handlers to update the date range based on user input",
      "Integrate the date range logic with UI components to display selected dates",
      "Manipulate date objects to format and parse dates for user interaction",
      "Control the state flow to reset or modify the date range as needed"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Import date-fns library for date manipulation",
        "instruction": "Import the format and addDays functions from the date-fns library.",
        "hint": "Use the import statement with destructuring to bring in both functions from the 'date-fns' package.",
        "analogousExample": "// Importing multiple functions from a library\nimport { useState, useEffect } from 'react';",
        "seedCode": "// Import format and addDays from 'date-fns'",
        "expectedOutcome": "The date-fns library functions are imported and ready to use for date manipulation.",
        "successCriteria": [
          "format and addDays are imported from 'date-fns'."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure to import both format and addDays from 'date-fns'.",
        "feedbackWrong": "That doesn't look right. You need to import both format and addDays functions from the date-fns library.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "import",
            "format",
            "addDays",
            "'date-fns'"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Create the main dateRangePicker function",
        "instruction": "Create a function named dateRangePicker that will contain all the date range logic. Make it return null for now.",
        "hint": "You can use either a function declaration or arrow function syntax. Remember to include the return statement.",
        "analogousExample": "// Example: creating a simple function\nfunction calculateTotal() {\n  return 0;\n}",
        "seedCode": "// Import format and addDays from 'date-fns'\n\n// Create the dateRangePicker function here",
        "expectedOutcome": "A dateRangePicker function is defined and returns null.",
        "successCriteria": [
          "The code contains a function named dateRangePicker that returns null."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you've created a function named dateRangePicker that returns null.",
        "feedbackWrong": "The function isn't defined correctly. Create a function named dateRangePicker that returns null.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "dateRangePicker",
            "return",
            "null"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Initialize start and end date state variables",
        "instruction": "Inside your dateRangePicker function, declare two state variables: startDate initialized to today's date, and endDate initialized to 7 days from today. Use the format function from date-fns to store them as strings in 'yyyy-MM-dd' format.",
        "hint": "You'll need to get today's date with new Date(), then use addDays to get a date 7 days later. Format both dates using format from date-fns.",
        "analogousExample": "// Example: initializing date state variables\nconst today = new Date();\nconst nextWeek = addDays(today, 7);\nconst startDate = format(today, 'yyyy-MM-dd');\nconst endDate = format(nextWeek, 'yyyy-MM-dd');",
        "seedCode": "// Import format and addDays from 'date-fns'\nimport { format, addDays } from 'date-fns';\n\n// Create the dateRangePicker function here\nfunction dateRangePicker() {\n  // Initialize startDate and endDate state variables here\n  \n  return null;\n}",
        "expectedOutcome": "Your function will have startDate and endDate variables containing formatted date strings representing today and 7 days from today.",
        "successCriteria": [
          "The dateRangePicker function contains startDate and endDate variables initialized with formatted date strings using format from date-fns."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure both dates are formatted using the format function from date-fns.",
        "feedbackWrong": "Check that you're using format from date-fns to create date strings, and that endDate is exactly 7 days after startDate.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "startDate",
            "endDate",
            "format",
            "addDays",
            "new Date()"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Create formatDate helper function",
        "instruction": "Inside the dateRangePicker function, create a helper function named formatDate that takes a date parameter and returns it formatted as 'MM/dd/yyyy' using the format function from date-fns.",
        "hint": "Define a function that accepts a date, then use format(date, 'MM/dd/yyyy') to return the formatted string.",
        "analogousExample": "// Helper function to format a date\nfunction formatDate(date) {\n  return format(date, 'MM/dd/yyyy');\n}",
        "seedCode": "// Import format and addDays from 'date-fns'\nimport { format, addDays } from 'date-fns';\n\n// Create the dateRangePicker function here\nfunction dateRangePicker() {\n  // Initialize startDate and endDate state variables here\n  \n  // Create formatDate helper function here\n  \n  return null;\n}",
        "expectedOutcome": "A formatDate helper function is defined inside dateRangePicker, ready to format dates.",
        "successCriteria": [
          "The formatDate function is defined inside dateRangePicker, accepts a date parameter, and returns the formatted string using format from date-fns."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure formatDate is defined inside dateRangePicker, takes a date parameter, and uses the format function with 'MM/dd/yyyy'.",
        "feedbackWrong": "The formatDate helper function isn't defined correctly. It should be inside dateRangePicker, accept a date, and return format(date, 'MM/dd/yyyy').",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "formatDate",
            "date",
            "format",
            "MM/dd/yyyy"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Create updateStartDate handler function",
        "instruction": "Inside your dateRangePicker function, create a function named updateStartDate that takes a newDate parameter and updates the startDate variable.",
        "hint": "Define a function that accepts a parameter and sets startDate to that value. You can use a function declaration or arrow function.",
        "analogousExample": "// Example of a handler function\nfunction updateValue(newValue) {\n  // Update state or variable here\n}",
        "seedCode": "// Import format and addDays from 'date-fns'\nimport { format, addDays } from 'date-fns';\n\n// Create the dateRangePicker function here\nfunction dateRangePicker() {\n  // Initialize startDate and endDate state variables here\n  \n  // Create formatDate helper function here\n  \n  // Create updateStartDate handler function here\n  \n  return null;\n}",
        "expectedOutcome": "The dateRangePicker function now contains an updateStartDate handler function that can be used to update the start date.",
        "successCriteria": [
          "An updateStartDate function is defined inside dateRangePicker with a newDate parameter."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure updateStartDate is defined as a function inside dateRangePicker and accepts a newDate parameter.",
        "feedbackWrong": "The updateStartDate function is not properly defined. It should be a function inside dateRangePicker that takes a newDate parameter.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "updateStartDate",
            "function",
            "newDate"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Create updateEndDate handler function",
        "instruction": "Create a function named updateEndDate that takes a date parameter and updates the endDate state variable, ensuring it doesn't come before startDate.",
        "hint": "Inside the dateRangePicker function, add a new function that sets endDate only if the new date is after startDate.",
        "analogousExample": "// Similar: a handler that validates before updating\nfunction updateEndDate(newDate) {\n  if (newDate > startDate) {\n    setEndDate(newDate);\n  }\n}",
        "seedCode": "// Import format and addDays from 'date-fns'\nimport { format, addDays } from 'date-fns';\n\n// Create the dateRangePicker function here\nfunction dateRangePicker() {\n  // Initialize startDate and endDate state variables here\n  \n  // Create formatDate helper function here\n  \n  // Create updateStartDate handler function here\n  \n  // Create updateEndDate handler function here\n  \n  return null;\n}",
        "expectedOutcome": "An updateEndDate function is added to the dateRangePicker function that properly validates and updates the end date.",
        "successCriteria": [
          "The updateEndDate function exists, takes a date parameter, and includes logic to prevent setting endDate before startDate."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've created the updateEndDate function, but it's missing the validation to prevent endDate from coming before startDate.",
        "feedbackWrong": "The updateEndDate function is not properly implemented. Make sure it takes a date parameter and includes validation logic.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "updateEndDate",
            "function",
            "newDate",
            "startDate",
            "endDate"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Return the date range picker object with state and handlers",
        "instruction": "Inside the dateRangePicker function, return an object containing the current state (startDate and endDate) and all handler functions (updateStartDate, updateEndDate, formatDate, getDateRange).",
        "hint": "The return statement should provide an object with properties for each piece of state and each helper/handler function you've created.",
        "analogousExample": "// Example: returning an object from a function\nfunction createCounter() {\n  let count = 0;\n  function increment() { count++; }\n  function getCount() { return count; }\n  return { count, increment, getCount };\n}",
        "seedCode": "// Import format and addDays from 'date-fns'\nimport { format, addDays } from 'date-fns';\n\n// Create the dateRangePicker function here\nfunction dateRangePicker() {\n  // Initialize startDate and endDate state variables here\n  \n  // Create formatDate helper function here\n  \n  // Create updateStartDate handler function here\n  \n  // Create updateEndDate handler function here\n  \n  // Create getDateRange helper function here\n  \n  return null;\n}",
        "expectedOutcome": "The dateRangePicker function will return an object with all the state and functionality needed to use the date range picker.",
        "successCriteria": [
          "The dateRangePicker function returns an object containing startDate, endDate, updateStartDate, updateEndDate, formatDate, and getDateRange properties."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure your return statement includes an object with all the required properties: startDate, endDate, updateStartDate, updateEndDate, formatDate, and getDateRange.",
        "feedbackWrong": "The return statement doesn't match the expected structure. You need to return an object containing all the state variables and handler functions.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "return",
            "{",
            "startDate",
            "endDate",
            "updateStartDate",
            "updateEndDate",
            "formatDate",
            "getDateRange",
            "}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Create and export the dateRangePicker instance",
        "instruction": "Create a constant named 'picker' that calls the dateRangePicker function,",
        "hint": "You can create a constant and assign it the result of calling dateRangePicker(), then use 'export default' to export it.",
        "analogousExample": "// Similar: create and export a function instance\nfunction createCounter() {\n  return { count: 0 };\n}\nconst counter = createCounter();\nexport default counter;",
        "seedCode": "// Import format and addDays from 'date-fns'\nimport { format, addDays } from 'date-fns';\n\n// Create the dateRangePicker function here\nfunction dateRangePicker() {\n  // Initialize startDate and endDate state variables here\n  \n  // Create formatDate helper function here\n  \n  // Create updateStartDate handler function here\n  \n  // Create updateEndDate handler function here\n  \n  // Create getDateRange helper function here\n  \n  return null;\n}\n\n// Create picker instance and export it here",
        "expectedOutcome": "The dateRangePicker function is instantiated and exported as the default export, making it available for use in other modules.",
        "successCriteria": [
          "A constant named 'picker' is created and assigned the result of calling dateRangePicker()",
          "The picker constant is exported as the default export"
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you've both created the 'picker' constant AND exported it as the default export. Check that you have both parts.",
        "feedbackWrong": "create a constant called 'picker' that stores the result of calling dateRangePicker(), then export it as the default export.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "picker",
            "dateRangePicker()",
            "export default"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "export it as the default export",
        "instruction": "export it as the default export.",
        "hint": "You can create a constant and assign it the result of calling dateRangePicker(), then use 'export default' to export it.",
        "analogousExample": "// Similar: create and export a function instance\nfunction createCounter() {\n  return { count: 0 };\n}\nconst counter = createCounter();\nexport default counter;",
        "seedCode": "// Import format and addDays from 'date-fns'\nimport { format, addDays } from 'date-fns';\n\n// Create the dateRangePicker function here\nfunction dateRangePicker() {\n  // Initialize startDate and endDate state variables here\n  \n  // Create formatDate helper function here\n  \n  // Create updateStartDate handler function here\n  \n  // Create updateEndDate handler function here\n  \n  // Create getDateRange helper function here\n  \n  return null;\n}\n\n// Create picker instance and export it here",
        "expectedOutcome": "",
        "successCriteria": [
          "Complete the step as instructed."
        ],
        "feedbackCorrect": "Great! Step completed.",
        "feedbackPartial": "Review the instruction and try again.",
        "feedbackWrong": "Check the instruction and try again.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "picker",
            "dateRangePicker()",
            "export default"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Test the date range picker logic with console output",
        "instruction": "Inside the dateRangePicker function, add console.log statements to test the formatDate, updateStartDate, updateEndDate, and getDateRange methods. Log the results of calling each method with sample dates.",
        "hint": "After creating your picker instance, call its methods and log the outputs. For example, log picker.formatDate(new Date()) to see the formatted date.",
        "analogousExample": "// Testing a similar utility function\nfunction testCalculator() {\n  const calc = calculator();\n  console.log('Add:', calc.add(2, 3));\n  console.log('Subtract:', calc.subtract(5, 2));\n}",
        "seedCode": "// Import format and addDays from 'date-fns'\nimport { format, addDays } from 'date-fns';\n\n// Create the dateRangePicker function here\nfunction dateRangePicker() {\n  // Initialize startDate and endDate state variables here\n  \n  // Create formatDate helper function here\n  \n  // Create updateStartDate handler function here\n  \n  // Create updateEndDate handler function here\n  \n  // Create getDateRange helper function here\n  \n  return null;\n}\n\n// Create picker instance and export it here\nconst picker = dateRangePicker();\nconsole.log('Testing date range picker:');\n// Add your console.log test statements here",
        "expectedOutcome": "The console will display formatted dates, updated start/end dates, and the calculated date range when the picker methods are called.",
        "successCriteria": [
          "Console.log statements are added that test formatDate, updateStartDate, updateEndDate, and getDateRange methods with sample dates."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added some console.log statements, but make sure to test all four methods: formatDate, updateStartDate, updateEndDate, and getDateRange.",
        "feedbackWrong": "The testing logic is missing or incomplete. Add console.log statements that call each picker method with sample dates to verify functionality.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "console.log",
            "formatDate",
            "updateStartDate",
            "updateEndDate",
            "getDateRange"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import date-fns library for date manipulation",
        "id": "step1"
      },
      {
        "label": "Create the main dateRangePicker function",
        "id": "step2"
      },
      {
        "label": "Initialize start and end date state variables",
        "id": "step3"
      },
      {
        "label": "Create formatDate helper function",
        "id": "step4"
      },
      {
        "label": "Create updateStartDate handler function",
        "id": "step5"
      },
      {
        "label": "Create updateEndDate handler function",
        "id": "step6"
      },
      {
        "label": "Return the date range picker object with state and handlers",
        "id": "step7"
      },
      {
        "label": "Create and export the dateRangePicker instance",
        "id": "step8"
      },
      {
        "label": "export it as the default export",
        "id": "step9"
      },
      {
        "label": "Test the date range picker logic with console output",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}