{
  "success": true,
  "config": {
    "lessonId": "ai-js-61",
    "track": "js",
    "lessonNum": 61,
    "title": "Write Promise race from scratch",
    "shortName": "P61",
    "intro": {
      "tag": "LESSON #61",
      "title": "Write Promise race from scratch",
      "body": "In this lesson, you will build your own version of the Promise.race() method from scratch using pure JavaScript. You'll learn how this function works by taking an array of promises and returning a new promise that settles (resolves or rejects) as soon as the first input promise settles. The core concept involves handling multiple asynchronous operations concurrently and responding to the fastest one. You'll use fundamental JavaScript constructs like Promises, then(), catch(), and array iteration. By the end, you'll understand the internal mechanics of Promise.race and how to implement custom promise-based utilities.",
      "usecase": "In real-world applications, you often need to fetch data from multiple sources but only care about the fastest response. For example, a dashboard might display user data by querying several redundant API endpoints, and you want to show results as soon as the first one succeeds. Implementing `Promise.race` allows you to handle these competitive asynchronous operations efficiently, improving perceived performance and user experience in features like search suggestions, fallback service calls, or real-time data updates."
    },
    "objectives": [
      "Construct a function that accepts an array of promises as input",
      "Implement logic to track the resolution or rejection of each promise",
      "Manipulate promise states to identify the first settled promise",
      "Configure the function to resolve with the value of the first fulfilled promise",
      "Control the function to reject with the reason of the first rejected promise",
      "Integrate the custom Promise.race into a sample application",
      "Initialize multiple asynchronous operations and apply your custom race function"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 10",
        "title": "Create a function skeleton for promiseRace",
        "instruction": "Create a function called promiseRace that takes an array of promises as its parameter and returns a new Promise.",
        "hint": "Start by writing a function declaration or arrow function with the name 'promiseRace'. It should accept one parameter (an array) and return a new Promise.",
        "analogousExample": "// Example: a function that returns a promise\nfunction fetchData() {\n  return new Promise((resolve, reject) => {\n    // Some async logic\n  });\n}",
        "seedCode": "// Create your promiseRace function here",
        "expectedOutcome": "A function named promiseRace is created with the correct signature.",
        "successCriteria": [
          "The code defines a function called promiseRace that takes one parameter and returns a new Promise."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your function is named exactly 'promiseRace', takes one parameter (an array), and returns a new Promise.",
        "feedbackWrong": "Create a function called promiseRace that accepts an array parameter and returns a new Promise.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "promiseRace",
            "return",
            "new Promise"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 10",
        "title": "Validate the input parameter",
        "instruction": "Inside your promiseRace function, add a validation check: if the input is not an array, immediately return a rejected Promise with an error message.",
        "hint": "You can use Array.isArray() to check if the argument is an array. If it's not, return Promise.reject() with a descriptive error string.",
        "analogousExample": "// Similar: validating input in a function\nfunction processItems(items) {\n  if (!Array.isArray(items)) {\n    return Promise.reject('Input must be an array');\n  }\n  // rest of function\n}",
        "seedCode": "// Create your promiseRace function here\n\nfunction promiseRace(promises) {\n  // Add input validation here\n}",
        "expectedOutcome": "The function will now validate its input and return a rejected Promise immediately when the input is not an array.",
        "successCriteria": [
          "The promiseRace function includes an if statement that checks if promises is not an array using Array.isArray(), and returns Promise.reject() with an error message when the condition is true."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've added some validation, but make sure to check if the input is NOT an array and return a rejected Promise with an error message when it's invalid.",
        "feedbackWrong": "The validation check is missing or incorrect. Remember to use Array.isArray() to check the input type and return Promise.reject() with an error when it's not an array.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "Array.isArray",
            "Promise.reject",
            "promises"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 10",
        "title": "Handle empty array case",
        "instruction": "Inside your promiseRace function, after validating that the input is an array, add a condition to check if the array is empty. If it is empty, return a new Promise that never settles (remains pending forever).",
        "hint": "You can check if the array length is 0. To return a promise that never settles, return a new Promise without calling resolve or reject.",
        "analogousExample": "// Example: a function that returns a pending promise for empty input\nfunction handleEmpty(inputArray) {\n  if (inputArray.length === 0) {\n    return new Promise(() => {});\n  }\n}",
        "seedCode": "// Create your promiseRace function here\n\nfunction promiseRace(promises) {\n  // Add input validation here\n  if (!Array.isArray(promises)) {\n    throw new TypeError('Input must be an array');\n  }\n  \n  // Handle empty array case here\n}",
        "expectedOutcome": "The promiseRace function will now return a pending Promise when given an empty array, matching the expected edge case behavior.",
        "successCriteria": [
          "The function includes a condition that checks if the promises array is empty and returns a new Promise with an empty executor function."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you're checking if the array is empty and returning a new Promise that never resolves or rejects.",
        "feedbackWrong": "The empty array case isn't handled correctly. Check if the array length is 0 and return a new Promise with an empty executor function.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "if",
            "promises.length",
            "=== 0",
            "return new Promise",
            "() => {}"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 10",
        "title": "Initialize the result tracking",
        "instruction": "Inside your promiseRace function, declare a variable named 'settled' and initialize it to false. This variable will track whether any promise in the array has settled (resolved or rejected).",
        "hint": "You can declare this variable using 'let' since its value will change later. Place it after the input validation and empty array handling.",
        "analogousExample": "// Example: tracking a completion state\nlet completed = false;",
        "seedCode": "// Create your promiseRace function here\n\nfunction promiseRace(promises) {\n  // Add input validation here\n  if (!Array.isArray(promises)) {\n    throw new TypeError('Input must be an array');\n  }\n  \n  // Handle empty array case here\n  \n  // Initialize result tracking\n}",
        "expectedOutcome": "Your promiseRace function will now have a 'settled' variable that starts as false, ready to track when the first promise settles.",
        "successCriteria": [
          "The promiseRace function contains a variable declaration for 'settled' initialized to false."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you've declared a variable named 'settled' and set its initial value to false.",
        "feedbackWrong": "The step requires declaring a variable to track settlement. Try adding 'let settled = false;' inside the function.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "let",
            "settled",
            "=",
            "false"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 10",
        "title": "Iterate through the promises array",
        "instruction": "Inside your promiseRace function, add a for loop that iterates through each promise in the promises array.",
        "hint": "You can use a standard for loop with an index variable, or a for...of loop to iterate through the array elements.",
        "analogousExample": "// Example: iterating through an array\nfunction processItems(items) {\n  for (let i = 0; i < items.length; i++) {\n    // Process each item\n  }\n}",
        "seedCode": "// Create your promiseRace function here\n\nfunction promiseRace(promises) {\n  // Add input validation here\n  if (!Array.isArray(promises)) {\n    throw new TypeError('Input must be an array');\n  }\n  \n  // Handle empty array case here\n  \n  // Initialize result tracking\n  \n  // Iterate through the promises array here\n}",
        "expectedOutcome": "Your promiseRace function should now include a loop structure that will process each promise in the input array.",
        "successCriteria": [
          "The function contains a for loop or for...of loop that iterates through the promises array."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your loop properly iterates through all elements in the promises array. Check your loop condition or iteration method.",
        "feedbackWrong": "The iteration logic is missing or incorrect. You need to add a loop that goes through each promise in the promises array.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "for",
            "promises",
            "length"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 10",
        "title": "Attach then handler to each promise",
        "instruction": "Inside the for loop, attach a .then() handler to each promise that will execute if that promise resolves first.",
        "hint": "For each promise in the array, call .then() with a callback function that receives the resolved value.",
        "analogousExample": "// Similar: attaching a then handler to a promise\npromise.then((value) => {\n  console.log('Resolved with:', value);\n});",
        "seedCode": "// Create your promiseRace function here\n\nfunction promiseRace(promises) {\n  // Add input validation here\n  if (!Array.isArray(promises)) {\n    throw new TypeError('Input must be an array');\n  }\n  \n  // Handle empty array case here\n  \n  // Initialize result tracking\n  \n  // Iterate through the promises array here\n  for (let i = 0; i < promises.length; i++) {\n    const promise = promises[i];\n    // Attach then handler here\n  }\n}",
        "expectedOutcome": "Each promise in the array should have a .then() handler attached to handle its resolution.",
        "successCriteria": [
          "The for loop contains a .then() call on each promise with a callback function."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close. Make sure you're calling .then() on each promise inside the loop with a callback function.",
        "feedbackWrong": "That's not quite right. You need to attach a .then() handler to each promise in the loop to handle when it resolves.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            ".then",
            "promise",
            "=>",
            "value"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 10",
        "title": "Implement the resolution callback logic",
        "instruction": "Inside the then callback you attached to each promise, add logic to check if no promise has settled yet using your flag. If the flag indicates no settlement, set the flag to true and resolve the outer Promise with the value from this promise.",
        "hint": "You'll need to access the resolve function from the outer Promise and call it with the value when this is the first promise to settle.",
        "analogousExample": "// Similar: handling first completion in a race\nfunction handleFirstCompletion(value, hasResolved, resolve) {\n  if (!hasResolved) {\n    hasResolved = true;\n    resolve(value);\n  }\n}",
        "seedCode": "// Create your promiseRace function here\n\nfunction promiseRace(promises) {\n  // Add input validation here\n  if (!Array.isArray(promises)) {\n    throw new TypeError('Input must be an array');\n  }\n  \n  // Handle empty array case here\n  \n  // Initialize result tracking\n  \n  // Iterate through the promises array here\n  for (let i = 0; i < promises.length; i++) {\n    const promise = promises[i];\n    // Attach then handler here\n    promise.then((value) => {\n      // Implement resolution callback logic here\n    });\n  }\n}",
        "expectedOutcome": "The then callback now contains logic that will resolve the outer Promise with the value from the first promise that settles, preventing subsequent resolutions.",
        "successCriteria": [
          "The then callback includes a check for whether any promise has settled, sets a flag when the first promise settles, and calls the outer Promise's resolve function with the value."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure your callback checks if a promise has already settled and only resolves the outer Promise if it's the first one.",
        "feedbackWrong": "The resolution callback logic isn't quite right. Remember to track whether any promise has settled and resolve the outer Promise only for the first one.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "if",
            "hasResolved",
            "resolve",
            "value"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 10",
        "title": "Attach catch handler to each promise",
        "instruction": "In the promiseRace function, chain a .catch() method to each promise inside the loop to handle potential rejections.",
        "hint": "After .then(), add .catch() with a callback that receives the rejection reason.",
        "analogousExample": "// Similar: handling promise rejection\npromise.then(value => console.log(value)).catch(error => console.error(error));",
        "seedCode": "// Create your promiseRace function here\n\nfunction promiseRace(promises) {\n  // Add input validation here\n  if (!Array.isArray(promises)) {\n    throw new TypeError('Input must be an array');\n  }\n  \n  // Handle empty array case here\n  \n  // Initialize result tracking\n  \n  // Iterate through the promises array here\n  for (let i = 0; i < promises.length; i++) {\n    const promise = promises[i];\n    // Attach then handler here\n    promise.then((value) => {\n      // Implement resolution callback logic here\n    });\n    // Attach catch handler here\n  }\n}",
        "expectedOutcome": "Each promise in the loop will have both .then() and .catch() handlers attached.",
        "successCriteria": [
          "The code includes .catch() chained to each promise with a callback function."
        ],
        "feedbackCorrect": "Great! You've added rejection handling for each promise.",
        "feedbackPartial": "You're close. Make sure to chain .catch() to each promise inside the loop.",
        "feedbackWrong": "The catch handler is missing or incorrectly placed. Each promise needs a .catch() method.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            ".catch",
            "promise",
            "catch"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 10",
        "title": "Implement the rejection callback logic",
        "instruction": "Inside the catch handler for each promise, check if no promise has settled yet. If true, set the settled flag to true and reject the outer Promise with the reason from this promise.",
        "hint": "Use the same pattern as the resolution callback but for rejection. Check if settled is false, then set settled to true and call reject(reason).",
        "analogousExample": "// Handling rejection in a similar pattern\npromise.catch((reason) => {\n  if (!settled) {\n    settled = true;\n    reject(reason);\n  }\n});",
        "seedCode": "// Create your promiseRace function here\n\nfunction promiseRace(promises) {\n  // Add input validation here\n  if (!Array.isArray(promises)) {\n    throw new TypeError('Input must be an array');\n  }\n  \n  // Handle empty array case here\n  \n  // Initialize result tracking\n  \n  // Iterate through the promises array here\n  for (let i = 0; i < promises.length; i++) {\n    const promise = promises[i];\n    // Attach then handler here\n    promise.then((value) => {\n      // Implement resolution callback logic here\n    });\n    // Attach catch handler here\n    promise.catch((reason) => {\n      // Implement rejection callback logic here\n    });\n  }\n}",
        "expectedOutcome": "The catch handler will now properly handle the first rejection among the promises, rejecting the outer Promise with that reason.",
        "successCriteria": [
          "The catch callback includes a check for the settled flag and calls reject(reason) when it's the first settlement."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure to check the settled flag before rejecting and to set the flag to true when rejecting.",
        "feedbackWrong": "The rejection callback logic is missing or incorrect. Remember to check if settled is false, then set settled to true and call reject(reason).",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "catch",
            "reason",
            "settled",
            "reject"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step10",
        "type": "question",
        "phase": "Step 10 of 10",
        "title": "Return the outer Promise",
        "instruction": "Complete your promiseRace function by returning the Promise you created earlier (the one that will be resolved or rejected by the handlers).",
        "hint": "Remember that you need to return the Promise object from your function so callers can use .then() and .catch() on it.",
        "analogousExample": "// Example: a function that returns a Promise\nfunction fetchData() {\n  return new Promise((resolve, reject) => {\n    // Promise logic here\n  });\n}",
        "seedCode": "// Create your promiseRace function here\n\nfunction promiseRace(promises) {\n  // Add input validation here\n  if (!Array.isArray(promises)) {\n    throw new TypeError('Input must be an array');\n  }\n  \n  // Handle empty array case here\n  \n  // Initialize result tracking\n  \n  // Iterate through the promises array here\n  for (let i = 0; i < promises.length; i++) {\n    const promise = promises[i];\n    // Attach then handler here\n    promise.then((value) => {\n      // Implement resolution callback logic here\n    });\n    // Attach catch handler here\n    promise.catch((reason) => {\n      // Implement rejection callback logic here\n    });\n  }\n  \n  // Return the Promise here\n}",
        "expectedOutcome": "Your promiseRace function should now return a Promise that resolves or rejects based on the first settled promise in the input array.",
        "successCriteria": [
          "The promiseRace function returns a Promise object."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're close, but make sure you're returning the Promise object from your function. Look at the analogous example for reference.",
        "feedbackWrong": "The function doesn't return a Promise yet. Remember that promiseRace needs to return a Promise that will be resolved or rejected based on the first settled promise.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "return",
            "new Promise",
            "promiseRace"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Create a function skeleton for promiseRace",
        "id": "step1"
      },
      {
        "label": "Validate the input parameter",
        "id": "step2"
      },
      {
        "label": "Handle empty array case",
        "id": "step3"
      },
      {
        "label": "Initialize the result tracking",
        "id": "step4"
      },
      {
        "label": "Iterate through the promises array",
        "id": "step5"
      },
      {
        "label": "Attach then handler to each promise",
        "id": "step6"
      },
      {
        "label": "Implement the resolution callback logic",
        "id": "step7"
      },
      {
        "label": "Attach catch handler to each promise",
        "id": "step8"
      },
      {
        "label": "Implement the rejection callback logic",
        "id": "step9"
      },
      {
        "label": "Return the outer Promise",
        "id": "step10"
      }
    ]
  },
  "source": "content"
}