{
  "success": true,
  "config": {
    "lessonId": "ai-js-58",
    "track": "js",
    "lessonNum": 58,
    "title": "Write request deduplication for identical fetches",
    "shortName": "P58",
    "intro": {
      "tag": "LESSON #58",
      "title": "Write request deduplication for identical fetches",
      "body": "In this lesson, you will build a request deduplication function to prevent sending multiple identical network requests at the same time. You will learn how to use a Map object to track ongoing requests by their unique identifiers, such as URLs. The core programming concept involves caching promises to ensure that duplicate requests reuse the same pending promise instead of creating new ones. You will work with JavaScript's fetch API, async/await, and promise handling. By the end, you'll have a reusable utility that improves application performance by reducing unnecessary network traffic.",
      "usecase": "Request deduplication is a critical performance pattern in modern web applications, especially in data-rich dashboards and interactive UIs. For example, in a financial dashboard where multiple widgets might simultaneously request the same stock price data, deduplication prevents redundant network calls, reducing server load and speeding up the interface. Similarly, in an e-commerce product page with multiple recommendation panels, deduplicating identical product detail fetches ensures a smoother user experience and conserves bandwidth. This pattern is essential for developers building efficient, scalable frontends that handle concurrent data fetching without waste."
    },
    "objectives": [
      "Implement a cache object to store pending fetch requests",
      "Construct a deduplication function that checks for identical requests",
      "Apply request deduplication logic to prevent duplicate API calls",
      "Configure the fetch wrapper to return cached promises for identical requests",
      "Manipulate the cache to remove completed requests",
      "Integrate the deduplication system into existing fetch operations",
      "Initialize a request tracking mechanism using unique identifiers"
    ],
    "steps": [
      {
        "id": "step1",
        "type": "question",
        "phase": "Step 1 of 9",
        "title": "Import required modules",
        "instruction": "Import the fetch API and create a Map for tracking requests. Since we're using plain JavaScript, we'll import fetch from the global scope and declare a Map variable.",
        "hint": "You don't need to import fetch as it's globally available in browsers. Just declare a Map variable to store ongoing requests.",
        "analogousExample": "// Similar: declaring a Map for tracking\nconst requestCache = new Map();",
        "seedCode": "// Import fetch API and create a Map for tracking requests\n// Write your code here",
        "expectedOutcome": "A Map variable is declared to track duplicate fetch requests.",
        "successCriteria": [
          "A Map variable is declared to store ongoing fetch requests."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure you've declared a Map variable to store ongoing requests.",
        "feedbackWrong": "We need to declare a Map variable to track duplicate fetch requests.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "Map",
            "new",
            "Map()"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step2",
        "type": "question",
        "phase": "Step 2 of 9",
        "title": "Create a cache object to store ongoing requests",
        "instruction": "Create a Map object named 'requestCache' to store promises for ongoing fetch requests.",
        "hint": "Use 'const requestCache = new Map();' to create an empty Map.",
        "analogousExample": "// Creating a Map for storing data\nconst userCache = new Map();",
        "seedCode": "// Import fetch API and create a Map for tracking requests\n// Write your code here\n\nconst requestCache = new Map();",
        "expectedOutcome": "A Map object named 'requestCache' is created to track duplicate requests.",
        "successCriteria": [
          "The code contains 'const requestCache = new Map();'"
        ],
        "feedbackCorrect": "Great! You've created a Map to track ongoing requests.",
        "feedbackPartial": "You're close. Make sure you create a Map object named 'requestCache'.",
        "feedbackWrong": "Try again. You need to create a Map object named 'requestCache' to store promises for ongoing requests.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "requestCache",
            "Map"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step3",
        "type": "question",
        "phase": "Step 3 of 9",
        "title": "Create the deduplicateFetch function",
        "instruction": "Define a function called deduplicateFetch that accepts a URL and options as parameters.",
        "hint": "You can use either a function declaration or an arrow function. The function should take two parameters: url and options.",
        "analogousExample": "// Example: a function that takes parameters\nfunction fetchData(url, options) {\n  // Function body\n}",
        "seedCode": "// Import fetch API and create a Map for tracking requests\n// Write your code here\n\nconst requestCache = new Map();\n\n// Define deduplicateFetch function here",
        "expectedOutcome": "A function named deduplicateFetch is defined with url and options parameters.",
        "successCriteria": [
          "The deduplicateFetch function is defined and accepts url and options parameters."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've defined a function, but check that it's named deduplicateFetch and accepts both url and options parameters.",
        "feedbackWrong": "The function isn't defined correctly. Make sure you create a function called deduplicateFetch that takes url and options as parameters.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "deduplicateFetch",
            "url",
            "options"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step4",
        "type": "question",
        "phase": "Step 4 of 9",
        "title": "Generate a unique key for each request",
        "instruction": "Inside the deduplicateFetch function, create a unique key by combining the URL and options parameters. Convert the options object to a JSON string using JSON.stringify() and concatenate it with the URL.",
        "hint": "You can create a string like `${url}-${JSON.stringify(options)}` to generate a unique identifier for each request.",
        "analogousExample": "// Creating a unique key from parameters\nfunction getCacheKey(url, options) {\n  return `${url}-${JSON.stringify(options)}`;\n}",
        "seedCode": "// Import fetch API and create a Map for tracking requests\n// Write your code here\n\nconst requestCache = new Map();\n\n// Define deduplicateFetch function here\nfunction deduplicateFetch(url, options = {}) {\n  // Generate unique key here\n}",
        "expectedOutcome": "The deduplicateFetch function will have a unique key generation mechanism that combines URL and options.",
        "successCriteria": [
          "The deduplicateFetch function contains code that creates a unique string key from the url and options parameters."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure to combine both the URL and stringified options into a single unique key.",
        "feedbackWrong": "The unique key generation is missing or incorrect. Remember to create a string that combines the URL and JSON.stringify(options).",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "url",
            "JSON.stringify",
            "options",
            "key"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step5",
        "type": "question",
        "phase": "Step 5 of 9",
        "title": "Check if request is already in progress",
        "instruction": "Inside the deduplicateFetch function, check if the request key already exists in the requestCache Map. If it does, return the existing Promise instead of making a new fetch.",
        "hint": "Use the Map's has() method to check for the key's existence. If it exists, use get() to retrieve and return the Promise.",
        "analogousExample": "// Checking for existing entry in a cache\nfunction getCachedData(key) {\n  if (cache.has(key)) {\n    return cache.get(key);\n  }\n  // Continue with fetching new data\n}",
        "seedCode": "// Import fetch API and create a Map for tracking requests\n// Write your code here\n\nconst requestCache = new Map();\n\n// Define deduplicateFetch function here\nfunction deduplicateFetch(url, options = {}) {\n  // Generate unique key here\n  const key = `${url}-${JSON.stringify(options)}`;\n  \n  // Check if request is already in progress\n  \n}",
        "expectedOutcome": "The function should check for duplicate requests before making new fetch calls.",
        "successCriteria": [
          "The deduplicateFetch function checks if the request key exists in requestCache and returns the cached Promise when found."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure to check if the key exists in the cache and return the existing Promise when found.",
        "feedbackWrong": "The function should check if the request key exists in the cache Map before proceeding. Use the Map's has() and get() methods.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "has",
            "get",
            "return",
            "requestCache",
            "key"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step6",
        "type": "question",
        "phase": "Step 6 of 9",
        "title": "Return existing promise for duplicate requests",
        "instruction": "Inside the deduplicateFetch function, if the request key already exists in the requestCache, return the cached promise immediately.",
        "hint": "Check if the key exists in the Map using has(), then retrieve and return the promise using get().",
        "analogousExample": "// Similar: returning cached value\nfunction getCachedData(key) {\n  if (cache.has(key)) {\n    return cache.get(key);\n  }\n  // ... rest of function\n}",
        "seedCode": "// Import fetch API and create a Map for tracking requests\n// Write your code here\n\nconst requestCache = new Map();\n\n// Define deduplicateFetch function here\nfunction deduplicateFetch(url, options = {}) {\n  // Generate unique key here\n  const key = `${url}-${JSON.stringify(options)}`;\n  \n  // Check if request is already in progress\n  if (requestCache.has(key)) {\n    // Return the existing promise\n  }\n}",
        "expectedOutcome": "The function will now return the cached promise when a duplicate request is detected, preventing duplicate network calls.",
        "successCriteria": [
          "The deduplicateFetch function checks if the key exists in requestCache and returns the cached promise when found."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're both checking if the key exists AND returning the cached promise. Use requestCache.has(key) to check and requestCache.get(key) to retrieve the promise.",
        "feedbackWrong": "Let's focus on the duplicate check. Inside the if statement, you need to return the promise stored in the cache for that key. Use requestCache.get(key) to retrieve it.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "requestCache.has(key)",
            "return",
            "requestCache.get(key)"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step7",
        "type": "question",
        "phase": "Step 7 of 9",
        "title": "Make the actual fetch request for new requests",
        "instruction": "Inside the deduplicateFetch function, after checking for existing requests, call the native fetch API with the provided url and options for new requests.",
        "hint": "Use the fetch() function with the url and options parameters. This should be placed in the else block after the existing request check.",
        "analogousExample": "// Making a fetch call for new requests\nconst promise = fetch(url, options);",
        "seedCode": "// Import fetch API and create a Map for tracking requests\n// Write your code here\n\nconst requestCache = new Map();\n\n// Define deduplicateFetch function here\nfunction deduplicateFetch(url, options = {}) {\n  // Generate unique key here\n  const key = `${url}-${JSON.stringify(options)}`;\n  \n  // Check if request is already in progress\n  if (requestCache.has(key)) {\n    // Return the existing promise\n  } else {\n    // Make the actual fetch request here\n  }\n}",
        "expectedOutcome": "The deduplicateFetch function now makes actual fetch calls for unique requests that aren't already in progress.",
        "successCriteria": [
          "The fetch() function is called with url and options parameters inside the else block."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track. Make sure to call fetch() with both the url and options parameters inside the else block.",
        "feedbackWrong": "The fetch call for new requests is missing or incorrect. Remember to call fetch(url, options) inside the else block.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "fetch",
            "url",
            "options"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step8",
        "type": "question",
        "phase": "Step 8 of 9",
        "title": "Store the fetch promise in cache",
        "instruction": "Inside the `deduplicateFetch` function, after the `else` block where you make the actual fetch request, store the fetch promise in the `requestCache` Map using the unique key.",
        "hint": "Use the `requestCache.set(key, promise)` method to store the promise. This should be done after creating the fetch promise but before returning it.",
        "analogousExample": "// Storing a value in a Map\nconst cache = new Map();\nconst key = 'myKey';\nconst value = fetch('https://api.example.com');\ncache.set(key, value);",
        "seedCode": "// Import fetch API and create a Map for tracking requests\n// Write your code here\n\nconst requestCache = new Map();\n\n// Define deduplicateFetch function here\nfunction deduplicateFetch(url, options = {}) {\n  // Generate unique key here\n  const key = `${url}-${JSON.stringify(options)}`;\n  \n  // Check if request is already in progress\n  if (requestCache.has(key)) {\n    // Return the existing promise\n  } else {\n    // Make the actual fetch request here\n    // Store the fetch promise in cache here\n  }\n}",
        "expectedOutcome": "The fetch promise will be stored in the cache Map so that subsequent identical requests can return the same promise instead of making a new network request.",
        "successCriteria": [
          "The `requestCache.set(key, promise)` method is called inside the `else` block to store the fetch promise."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You're on the right track, but make sure you're storing the actual fetch promise (not just the URL) in the cache using `requestCache.set()`.",
        "feedbackWrong": "The fetch promise isn't being stored in the cache. Remember to use `requestCache.set(key, promise)` inside the `else` block after creating the fetch request.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "requestCache.set",
            "key",
            "promise"
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      },
      {
        "id": "step9",
        "type": "question",
        "phase": "Step 9 of 9",
        "title": "Test the deduplicateFetch function",
        "instruction": "Create two simultaneous test calls to deduplicateFetch with the same URL and options to demonstrate deduplication.",
        "hint": "Call deduplicateFetch twice with identical arguments and log the results. You can use a mock API endpoint like 'https://jsonplaceholder.typicode.com/todos/1'.",
        "analogousExample": "// Testing a function with duplicate calls\nfunction testDeduplication() {\n  const result1 = deduplicateFetch('https://api.example.com/data');\n  const result2 = deduplicateFetch('https://api.example.com/data');\n  console.log('Are promises the same?', result1 === result2);\n}",
        "seedCode": "// Import fetch API and create a Map for tracking requests\n// Write your code here\n\nconst requestCache = new Map();\n\n// Define deduplicateFetch function here\nfunction deduplicateFetch(url, options = {}) {\n  // Generate unique key here\n  const key = `${url}-${JSON.stringify(options)}`;\n  \n  // Check if request is already in progress\n  if (requestCache.has(key)) {\n    // Return the existing promise\n    return requestCache.get(key);\n  } else {\n    // Make the actual fetch request here\n    const fetchPromise = fetch(url, options);\n    // Store the fetch promise in cache here\n    requestCache.set(key, fetchPromise);\n    // TODO: Clean up cache after request completes\n    return fetchPromise;\n  }\n}\n\n// Add your test calls here",
        "expectedOutcome": "Two identical calls to deduplicateFetch return the same Promise object, demonstrating that duplicate requests are being deduplicated.",
        "successCriteria": [
          "Two calls to deduplicateFetch with identical arguments are made and their return values are logged or compared."
        ],
        "feedbackCorrect": "Nice! Proceed to the next step.",
        "feedbackPartial": "You've made test calls, but ensure both calls use identical URL and options to properly test deduplication.",
        "feedbackWrong": "The test calls are missing or don't properly demonstrate deduplication. Create two simultaneous calls with the same arguments.",
        "evaluation": {
          "mode": "keyword_match",
          "required": [
            "deduplicateFetch",
            "deduplicateFetch",
            "console.log",
            "==="
          ],
          "partialThreshold": 0.5,
          "correctThreshold": 0.8
        }
      }
    ],
    "sideItems": [
      {
        "label": "Intro",
        "id": "intro"
      },
      {
        "label": "Objectives",
        "id": "objectives"
      },
      {
        "label": "Import required modules",
        "id": "step1"
      },
      {
        "label": "Create a cache object to store ongoing requests",
        "id": "step2"
      },
      {
        "label": "Create the deduplicateFetch function",
        "id": "step3"
      },
      {
        "label": "Generate a unique key for each request",
        "id": "step4"
      },
      {
        "label": "Check if request is already in progress",
        "id": "step5"
      },
      {
        "label": "Return existing promise for duplicate requests",
        "id": "step6"
      },
      {
        "label": "Make the actual fetch request for new requests",
        "id": "step7"
      },
      {
        "label": "Store the fetch promise in cache",
        "id": "step8"
      },
      {
        "label": "Test the deduplicateFetch function",
        "id": "step9"
      }
    ]
  },
  "source": "content"
}