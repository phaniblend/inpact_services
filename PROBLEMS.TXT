# 100 React Interview Coding Problems
## Categorized by Level: Junior | Mid | Senior

---

## 🟢 JUNIOR LEVEL (1–3 years)
*Tests: React basics, JSX, props, state, events, simple hooks*

---

### Hooks & State

1. **Counter App** — Build a counter with increment, decrement, and reset buttons using `useState`
2. **Toggle Visibility** — Show/hide a paragraph on button click
3. **Controlled Input** — Build a controlled text input that displays character count in real time
4. **Form with Validation** — Build a login form with email/password validation using `useState`
5. **Color Picker** — Change background color of a div based on a dropdown selection
6. **Simple Timer** — Build a stopwatch with start, stop, reset using `useState` + `useEffect`
7. **useEffect Basics** — Fetch a list of users from JSONPlaceholder and display them
8. **Dependency Array** — Explain and demonstrate the difference between `useEffect` with no deps, empty deps `[]`, and `[value]`
9. **Cleanup in useEffect** — Build a component that subscribes to a window resize event and cleans up properly
10. **Multiple State Vars** — Build a registration form tracking: name, email, password, confirmPassword as separate state vars

---

### Props & Components

11. **Reusable Button** — Build a `Button` component that accepts: `label`, `onClick`, `variant` (primary/secondary/danger), `disabled`
12. **Card Component** — Build a `Card` component with: title, description, image, footer slot via props
13. **Props Drilling** — Pass data 3 levels deep without Context — explain why this gets painful
14. **Default Props** — Build a `Avatar` component with default props for size and placeholder image
15. **Children Prop** — Build a `Container` component that wraps any children with a styled div
16. **Conditional Rendering** — Show different UI based on: loading / error / empty / data states
17. **List Rendering** — Render a list of products with proper `key` props — explain why keys matter
18. **PropTypes / TypeScript Interface** — Add TypeScript interfaces to a `UserCard` component
19. **Component Composition** — Build a `PageLayout` with Header, Sidebar, Main, Footer as named slots
20. **Event Handling** — Build a form where pressing Enter submits, Escape clears, without using a submit button

---

### Styling & CSS

21. **Conditional Classes** — Apply different CSS classes based on component state
22. **Inline Styles** — Build a progress bar with dynamic width via inline styles
23. **CSS Modules** — Convert a component from global CSS to CSS Modules
24. **Styled Component Pattern** — Build a themed button using CSS variables

---

### Simple Patterns

25. **Lifting State Up** — Build two sibling components that share state via parent
26. **Controlled vs Uncontrolled** — Explain the difference with a working example of each
27. **Simple Todo List** — Add, complete, delete todos — full CRUD with `useState`
28. **Star Rating Component** — Build a clickable 5-star rating component
29. **Accordion** — Build a single-open accordion with multiple panels
30. **Image Gallery** — Build a grid of images with a click-to-enlarge modal

---

## 🟡 MID LEVEL (3–5 years)
*Tests: Custom hooks, Context, performance basics, patterns, TypeScript*

---

### Custom Hooks

31. **`useFetch`** — Custom hook for data fetching with loading/error/data states + AbortController cleanup
32. **`useDebounce`** — Debounce any value by a configurable delay
33. **`useLocalStorage`** — Sync state with localStorage — read on mount, write on change
34. **`useToggle`** — Generic boolean toggle hook with optional initial value
35. **`useWindowSize`** — Track window width/height with resize listener and cleanup
36. **`usePrevious`** — Return the previous value of any state using `useRef`
37. **`useClickOutside`** — Detect clicks outside a referenced element — used for dropdowns/modals
38. **`useKeyPress`** — Detect when a specific keyboard key is pressed
39. **`useOnlineStatus`** — Track whether the user is online or offline
40. **`useMediaQuery`** — Return true/false based on a CSS media query string

---

### Context & Global State

41. **Theme Context** — Build a dark/light mode toggle using Context + `useReducer`
42. **Auth Context** — Build a mock auth system: login, logout, protected route, user in context
43. **Cart Context** — Build a shopping cart: add, remove, update quantity, total price
44. **Notification Context** — Build a toast notification system: push notifications, auto-dismiss, stack limit
45. **Context Performance** — Identify and fix a Context that re-renders the entire tree on every update — solution: split contexts or use `memo`
46. **`useReducer` vs `useState`** — Refactor a complex multi-field form from multiple `useState` calls to a single `useReducer`
47. **Compound Component Pattern** — Build a `<Tabs>` component using Context internally so child `<Tab>` components are aware of active state

---

### Performance

48. **Unnecessary Re-renders** — Given a component tree with a performance bug, identify and fix using `React.memo`, `useMemo`, `useCallback`
49. **`useMemo` for Expensive Computation** — Filter + sort a large list — memoize the result
50. **`useCallback` for Stable References** — Prevent a child component from re-rendering when parent re-renders by stabilising a callback
51. **React.memo** — Wrap a pure child component and demonstrate it stops re-rendering when parent state changes
52. **List Virtualization** — Explain and implement windowing for a 10,000 item list using `react-window`
53. **Lazy Loading Routes** — Implement code splitting with `React.lazy` + `Suspense` on all routes
54. **Image Lazy Loading** — Build an image component that only loads when it enters the viewport using `IntersectionObserver`

---

### Patterns & Architecture

55. **Higher Order Component (HOC)** — Build a `withAuth` HOC that redirects unauthenticated users
56. **Render Props** — Build a `MouseTracker` component using render props pattern
57. **Controlled Component Pattern** — Build a fully controlled `DatePicker` component
58. **Portal** — Render a modal outside the root DOM node using `ReactDOM.createPortal`
59. **Error Boundary** — Build a class-based Error Boundary that catches render errors and shows a fallback UI
60. **Recursive Component** — Build a recursive `TreeView` component for nested folder structures
61. **Pagination** — Build client-side pagination for a list of 100 items
62. **Infinite Scroll** — Load more items as user scrolls to the bottom using `IntersectionObserver`
63. **Debounced Search** — Combine `useDebounce` + `useFetch` to build a real-time search box that fires only after typing stops
64. **Multi-Step Form** — Build a 3-step form with validation at each step, back/next navigation, state persisted across steps

---

### TypeScript

65. **Generic Component** — Build a generic `List<T>` component that renders any array with a custom render function
66. **Discriminated Union Props** — Build a `Button` where `variant="link"` requires an `href` prop and `variant="action"` requires `onClick`
67. **`useRef` Typing** — Correctly type `useRef` for a DOM element vs a mutable value
68. **Event Typing** — Correctly type `onChange`, `onClick`, `onSubmit` handlers in TypeScript
69. **Generic Custom Hook** — Add TypeScript generics to a `useFetch<T>` hook so consumers get typed responses
70. **Utility Types** — Use `Partial<T>`, `Pick<T>`, `Omit<T>` to build flexible component prop interfaces

---

## 🔴 SENIOR LEVEL (5+ years)
*Tests: Architecture, advanced patterns, performance at scale, state design, testing, real-world tradeoffs*

---

### Advanced Hooks

71. **`useImperativeHandle`** — Expose a custom API from a child component to a parent using `forwardRef` + `useImperativeHandle` — e.g. a custom input that exposes `.focus()` and `.clear()`
72. **`useSyncExternalStore`** — Subscribe to an external store (e.g. a custom event emitter) safely in concurrent mode
73. **`useTransition`** — Mark a slow state update as non-urgent to keep UI responsive — demonstrate with a heavy filtered list
74. **`useDeferredValue`** — Defer re-rendering of an expensive component while keeping input responsive
75. **`useLayoutEffect` vs `useEffect`** — Explain the difference with a concrete example — when does each fire and why does it matter for DOM measurements

---

### Architecture & Design

76. **Mini Redux from Scratch** — Build a global state system using only `useReducer` + `Context` + custom `useSelector`-style hook
77. **Optimistic UI** — Build a todo list where adding an item updates the UI instantly, then syncs to the server — roll back on failure
78. **Request Deduplication** — Build a fetch layer that prevents duplicate in-flight requests for the same URL
79. **Polling Hook** — Build a `usePolling` hook that refetches a URL on a configurable interval and stops on unmount
80. **WebSocket Hook** — Build a `useWebSocket` hook that connects, reconnects on disconnect, and exposes send + last message
81. **Feature Flag Hook** — Build a `useFeatureFlag(flagName)` hook that reads from a context and determines if a feature is enabled
82. **Undo/Redo** — Build an undo/redo system for a text editor using `useReducer` and a history stack
83. **Form Library from Scratch** — Build a mini `useForm` hook: register fields, track values/errors/touched, handle submit
84. **Component Library with Theming** — Design a token-based theming system for a component library using CSS variables + Context
85. **Micro-frontend Shell** — Explain and sketch how you'd integrate a React app as a micro-frontend using Module Federation

---

### Performance at Scale

86. **Race Condition Fix** — Given code where fast typing causes stale API responses to overwrite fresh ones — identify and fix using AbortController
87. **Memoization Strategy** — Given a dashboard with 20 widgets, design a memoization strategy that avoids unnecessary re-renders without over-engineering
88. **Bundle Analysis** — Given a slow initial load, walk through how you'd use Webpack Bundle Analyzer + code splitting to reduce bundle size
89. **Concurrent Mode Gotchas** — Explain what tearing is in concurrent React and how `useSyncExternalStore` solves it
90. **Memory Leak Hunt** — Given a component with a memory leak (event listener not cleaned up, interval not cleared, subscription not unsubscribed) — find and fix all three

---

### Testing

91. **Test a Custom Hook** — Write a test for `useFetch` using `renderHook` from React Testing Library — mock the fetch, test loading/error/data states
92. **Test Async Component** — Write a test for a component that fetches data — use `msw` to mock the API, assert loading → data transition
93. **Test User Interactions** — Write a test for a multi-step form — simulate filling fields, clicking Next, submitting, asserting success state
94. **Test Context** — Write a test for a component that consumes Auth Context — wrap in a mock provider, assert conditional rendering
95. **Test Error Boundary** — Write a test that throws an error inside a child component and asserts the Error Boundary fallback UI renders

---

### Real-World Design Questions

96. **Design a `DataTable` Component API** — Design the props interface for a reusable, sortable, filterable, paginated data table — justify every decision
97. **Design an Auth Flow** — Design the full React-side auth flow: login, token storage, refresh, protected routes, logout — no backend needed, just the frontend architecture
98. **Design a Notification System** — Design a toast/notification system that supports: success/error/info types, auto-dismiss, manual dismiss, max 3 visible, queue overflow
99. **Design a Permission System** — Build a `<Can action="edit" resource="post">` component that shows/hides UI based on user roles — design the data model and the component API
100. **Design a Real-Time Dashboard** — Design a dashboard that polls 5 different endpoints at different intervals, shows stale indicators, handles errors per-widget independently — what hooks, what state shape, what components

---

## 📊 Summary by Category

| Category | Junior | Mid | Senior |
|---|---|---|---|
| Hooks & State | 1–10 | 31–40 | 71–75 |
| Components & Props | 11–24 | — | — |
| Context & Global State | 25–26 | 41–47 | 76–77 |
| Performance | — | 48–54 | 86–90 |
| Patterns & Architecture | 27–30 | 55–64 | 78–85 |
| TypeScript | — | 65–70 | — |
| Testing | — | — | 91–95 |
| System Design | — | — | 96–100 |

---

## 🗺️ Recommended Learning Order

```
Start here (Junior):
  1 → 7 → 27 → 11 → 17 → 25

Build momentum (Mid):
  31 → 32 → 33 → 63 → 41 → 46 → 48 → 64

Level up (Senior):
  66 → 73 → 74 → 76 → 81 → 86 → 91 → 96
```

---

*Use this list as the problem library for the PAAL system.*
*Each problem maps to at least 2–3 teachable concepts.*